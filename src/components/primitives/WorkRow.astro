---
/**
 * WorkRow — MASTER §5.5
 *
 * Single numbered row in the editorial work list. Three-column grid:
 *   - 56px: number column (meta-mono, var(--ink-muted), 1rem/500)
 *   - 1fr:  body column (title in .h2-project, stack in uppercase mono)
 *   - auto: meta column (year + hover-reveal accent arrow)
 *
 * Hover/focus: title gets accent underline; arrow fades in (120ms) and slides 4px right
 * (180ms ease-out). Phase 16 MOTN-03 upgrade — replaces v1.1's opacity-only fade per the
 * v1.2 motion canonical doc (design-system/MOTION.md). Reduced-motion neutralizes the slide
 * AND the fade; the accent title underline remains as the affordance under reduce.
 *
 * Per D-02: primitive lives in src/components/primitives/
 * Per D-03: no Tailwind utilities inside primitive markup
 * Per D-04: props stay as MASTER §5.5 specifies (number/title/stack/year/href)
 * Per D-14: layout scoped here
 *
 * Phase 23 D-08/D-14: optional `tagline` renders a single .body line between the
 * title and stack (featured tier on /projects + Home teaser). When absent the row
 * is byte-identical to the pre-Phase-23 primitive (the tagline element and the
 * title 8px override both drop out), so the rest tier and the motion/focus tests
 * are untouched. The tagline takes NO hover state — only the title carries the
 * accent underline affordance (MASTER §7 single-signal rule).
 */
interface Props {
  number: string;
  title: string;
  stack: string;
  year: string;
  href: string;
  tagline?: string;
}

const { number, title, stack, year, href, tagline } = Astro.props;
---

<a class="work-row" href={href}>
  <span class="work-num meta-mono tabular">{number}</span>
  <div class="work-body">
    <h2 class="h2-project work-title">{title}</h2>
    {tagline && <p class="body work-tagline">{tagline}</p>}
    <div class="work-stack">{stack}</div>
  </div>
  <div class="work-meta">
    <span class="meta-mono tabular work-year">{year}</span>
    <span class="work-arrow" aria-hidden="true">→</span>
  </div>
</a>

<style>
  .work-row {
    display: grid;
    grid-template-columns: 56px 1fr auto;
    gap: 24px;
    padding: 28px 0;
    border-bottom: 1px solid var(--rule);
    color: inherit;
    text-decoration: none;
    cursor: pointer;
    align-items: start;
  }

  .work-num {
    color: var(--ink-muted);
    font-size: 1rem;
    font-weight: 500;
  }

  .work-title {
    color: var(--ink);
    margin-bottom: 12px;
  }

  /* Phase 23 D-08: featured-row tagline (one .body line, muted, no hover state).
     Structural next-sibling :has() tightens the title→tagline gap to 8px only
     when a tagline follows; a tagline-less row never matches, so it keeps the
     12px title→stack rhythm byte-identical to the pre-Phase-23 primitive. */
  .work-tagline {
    color: var(--ink-muted);
    margin-bottom: 12px;
  }

  .work-title:has(+ .work-tagline) {
    margin-bottom: 8px;
  }

  .work-stack {
    font-family: var(--font-mono);
    font-weight: 400;
    font-size: 0.8125rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--ink-faint);
  }

  .work-meta {
    display: flex;
    align-items: baseline;
    gap: 8px;
    color: var(--ink-faint);
  }

  .work-arrow {
    color: var(--accent);
    opacity: 0;
    transform: translateX(0);
    transition: opacity 120ms ease, transform 180ms ease-out;
  }

  .work-row:hover .work-title,
  .work-row:focus-visible .work-title {
    text-decoration: underline;
    text-decoration-color: var(--accent);
    text-decoration-thickness: 1.5px;
    text-underline-offset: 4px;
  }

  .work-row:hover .work-arrow,
  .work-row:focus-visible .work-arrow {
    opacity: 1;
    transform: translateX(4px);
  }

  .work-row:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }

  /* Phase 16 MOTN-03 / D-23 / D-24: paired reduce override.
     With reduce, the arrow stays opacity 0 (hidden); the accent title underline
     above (color-only state change, sub-200ms — MASTER.md §6.2) still appears
     as the affordance. */
  @media (prefers-reduced-motion: reduce) {
    .work-arrow {
      transition: none;
      transform: translateX(0);
      opacity: 0;
    }
    .work-row:hover .work-arrow,
    .work-row:focus-visible .work-arrow {
      transform: translateX(0);
      opacity: 0;
    }
  }
</style>
