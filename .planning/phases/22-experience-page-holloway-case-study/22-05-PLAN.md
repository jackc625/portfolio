---
phase: 22-experience-page-holloway-case-study
plan: 05
type: execute
wave: 3
depends_on: [22-02, 22-03, 22-04]
files_modified: []
autonomous: false
requirements: [EXP-02, EXP-03, EXP-04, EXP-05]
must_haves:
  truths:
    - "pnpm build generates /experience/index.html and /experience/holloway/index.html and does NOT generate /experience/balfour-beatty"
    - "pnpm exec astro check exits 0/0/0; pnpm test is fully green including the D-26 chat-surface battery; pnpm sync:experience:check exits 0"
    - "package.json dependencies are byte-identical to the phase start (QA-02: zero new runtime deps)"
    - "A human confirms both pages render per the frontend-design skill's decisions against MASTER.md + 22-UI-SPEC.md (SC5)"
  artifacts: []
  key_links:
    - "the full pnpm test run is the D-26 chat-surface regression gate after all nav/page/global-adjacent edits are merged"
    - "the build-output route set is the EXP-05 structural proof (no Balfour detail route)"
---

<objective>
Phase capstone gate: after the Wave 2 implementation plans (nav, listing, detail) merge, run the full milestone-cross-cutting gate suite and confirm the SC5 visual sign-off. This is the single place the D-26 chat-surface battery, the astro-check type gate, the drift gate, the route-generation build, and the QA-02 zero-new-dep lock are all verified together against the complete diff, plus a human confirms the frontend-design-driven composition (SC5d, which cannot be automated).

Purpose: Prove SC1-SC5 hold end-to-end before /gsd-verify-work, and give the frontend-design visual decisions a human confirmation.
Output: A green full-suite + build gate run and an approved visual checkpoint. No source files modified (verification-only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-experience-page-holloway-case-study/22-CONTEXT.md
@.planning/phases/22-experience-page-holloway-case-study/22-UI-SPEC.md
@.planning/phases/22-experience-page-holloway-case-study/22-VALIDATION.md
@design-system/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + build + drift + dependency-lock gate run</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/22-experience-page-holloway-case-study/22-VALIDATION.md (the per-task verification map + sampling rates)
    - .planning/phases/22-experience-page-holloway-case-study/22-RESEARCH.md (Validation Architecture: the exact gate commands + QA-02 byte-identical lock)
    - .planning/STATE.md (v1.3 close snapshot: prior pnpm test count / astro check 0/0/0 baseline; D-26/D-15 invariants)
  </read_first>
  <action>
    Run the phase gate suite and record each result in the SUMMARY: (1) `pnpm exec astro check` — must exit 0/0/0 (SC5a). (2) `pnpm test` — full Vitest suite must be green, including the new experience-nav / experience-summary / experience-detail / experience-voice-em-dash tests AND the D-26 chat-surface battery (SC5b / QA-01). (3) `pnpm sync:experience:check` — must exit 0, proving D-08's frontmatter edit introduced no body drift (SC5c). (4) `pnpm build` — must succeed and generate `dist/experience/index.html` and `dist/experience/holloway/index.html` while generating NO `dist/experience/balfour-beatty` directory (the EXP-05 structural proof — verify by listing the built output). (5) Confirm `package.json` dependencies are byte-identical to the phase start (QA-02 — no new runtime deps; e.g. `git diff --stat package.json pnpm-lock.yaml` shows no dependency changes). If any gate is red, do NOT self-approve — record the failure and stop for triage.
  </action>
  <acceptance_criteria>
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm test` is fully green (experience tests + D-26 battery)
    - `pnpm sync:experience:check` exits 0
    - `pnpm build` succeeds; built output contains `experience/index.html` and `experience/holloway/index.html` and NO `experience/balfour-beatty` route
    - `package.json` + lockfile show no dependency additions (QA-02)
    - All five results are recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec astro check && pnpm test && pnpm sync:experience:check && pnpm build</automated>
  </verify>
  <done>All five gates recorded green: astro check 0/0/0, full suite (incl. D-26 battery), drift check, build with the correct route set, and the QA-02 dependency lock.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual sign-off — Experience surface (SC5d)</name>
  <read_first>
    - design-system/MASTER.md (the locked editorial contract the visuals must conform to)
    - .planning/phases/22-experience-page-holloway-case-study/22-UI-SPEC.md (tokens/typography/copy/interaction contract)
    - .planning/phases/22-experience-page-holloway-case-study/22-03-SUMMARY.md + 22-04-SUMMARY.md (the frontend-design attributions to confirm against)
  </read_first>
  <action>Pause for human verification of the frontend-design-driven Experience composition. The human walks the how-to-verify steps against the running dev server and confirms the pages match design-system/MASTER.md + 22-UI-SPEC.md and the attributions in the 22-03/22-04 SUMMARYs. Do not self-approve; wait for the resume signal.</action>
  <acceptance_criteria>
    - The human confirms all 9 how-to-verify steps pass and types "approved" (or the issues are captured for follow-up)
    - Both pages conform to MASTER.md (six tokens, Geist fonts, restrained motion, visible focus rings) and the recorded frontend-design decisions (SC5d)
    - No em dashes appear in any visible copy
  </acceptance_criteria>
  <what-built>
    The Experience surface: a nav-reachable /experience listing (Holloway rich summary + Balfour lightweight "Earlier" entry) and the /experience/holloway deep-dive, with all visual/layout form decided by the frontend-design skill against design-system/MASTER.md + 22-UI-SPEC.md (SC5). Both pages passed the automated gate suite in Task 1.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:4321/experience.
    2. Confirm the primary nav shows `experience` FIRST (experience · works · about · contact) on desktop; shrink to the mobile menu and confirm the same order; confirm the `experience` link shows the accent-underline active state on this page.
    3. On /experience, confirm Holloway reads as a rich scannable summary: the meta line (role · Holloway Company · May 2026 – Present · stack), the first-person summary sentence, ALL 5 highlights, and a "Read the full case study →" link. Confirm the company reads "Holloway Company" (no leading "The").
    4. Confirm Balfour renders as a visibly lighter, non-linked "Earlier" entry (role · May 2023 – Aug 2023 · its 1-2 lines) — it must NOT be a link and must read as career context, not a second case study.
    5. Click "Read the full case study →" → confirm /experience/holloway shows the mono eyebrow (dateRange · stack), H1 "Holloway Company", the summary as the lead, the full Overview + 9 highlights + Themes body, and NO external-links row.
    6. Confirm a "← Back to experience" link appears at both the top (under the header) and the bottom, and each returns to /experience.
    7. Confirm there is NO /experience/balfour-beatty page (navigating to it should 404).
    8. Confirm the overall look matches the MASTER.md editorial system (six tokens, Geist fonts, restrained motion, visible focus rings) and the frontend-design decisions recorded in the 22-03/22-04 SUMMARYs (SC5d).
    9. Confirm zero em dashes in any visible copy (en dashes in date ranges are fine).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregate diff → chat surface | This gate is where the merged nav + page + styling diff is verified against the D-26 chat-surface battery (QA-01) — the last line of defense before /gsd-verify-work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Tampering / DoS | Chat-surface regression from any Wave 2 edit | high | mitigate | `pnpm test` runs the full D-26 battery against the merged diff here; a red battery blocks the phase (no self-approval). |
| T-22-03 | Info Disclosure | Confidential-contract data in rendered pages | medium | mitigate | The human-verify checkpoint includes a content check for credentials/PII/internal URLs in the rendered deep-dive. |
| T-22-07 | Tampering | Unreviewed dependency drift (QA-02) | high | mitigate | Task 1 asserts package.json + lockfile show no dependency additions; a change blocks the gate. |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No installs anywhere in the phase; QA-02 byte-identical lock verified in Task 1. |
</threat_model>

<verification>
- Task 1's four commands all pass and the built route set is correct (has /experience + /experience/holloway, lacks /experience/balfour-beatty).
- QA-02 dependency lock confirmed (no package.json/lockfile dependency changes).
- The human-verify checkpoint is approved (SC5d visual sign-off).
</verification>

<success_criteria>
Every phase success criterion is proven together: SC1 (nav + route), SC2 (Holloway summary), SC3 (deep-dive + back), SC4 (Balfour lightweight, no route), SC5 (astro check 0/0/0, D-26 battery green, frontend-design-attributed visuals, zero new deps). The phase is ready for /gsd-verify-work.
</success_criteria>

## Artifacts this phase produces (this plan)

- None (verification-only plan; produces the gate results recorded in `22-05-SUMMARY.md` and the approved visual checkpoint).

<output>
Create `.planning/phases/22-experience-page-holloway-case-study/22-05-SUMMARY.md` when done.
</output>
