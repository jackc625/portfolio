---
phase: 22-experience-page-holloway-case-study
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/components/primitives/Header.astro
  - src/components/primitives/MobileMenu.astro
autonomous: true
requirements: [EXP-02]
must_haves:
  truths:
    - "An `experience` nav link appears FIRST in the primary nav (experience · works · about · contact) on every page, in both desktop Header and MobileMenu"
    - "Visiting /experience or /experience/[id] renders the experience nav link in its active state (accent underline)"
  artifacts:
    - src/components/primitives/Header.astro
    - src/components/primitives/MobileMenu.astro
  key_links:
    - "navLinks[0].href === /experience feeds the shared nav render loop in both primitives"
    - "isActive uses currentPath.startsWith('/experience') so /experience/[id] also marks the entry active"
---

<objective>
Add the `experience` primary-nav entry FIRST in both nav primitives (D-03), so a visitor can reach the Experience surface from site-wide navigation (EXP-02, SC1). This is a surgical two-line-per-file edit: add `{ href: "/experience", label: "experience" }` as the first `navLinks` element and an `isActive` `startsWith("/experience")` branch in each of Header.astro and MobileMenu.astro. Turns tests/build/experience-nav.test.ts GREEN.

Purpose: Establish the route+active pairing that gives /experience and /experience/[id] active-state coverage for free (mirrors how /projects covers /projects/[id]).
Output: Two edited nav primitives; the MobileMenu focus-trap `<script>` and `.chat-widget` inert handling are left byte-untouched (D-26 chat-surface battery).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-experience-page-holloway-case-study/22-CONTEXT.md
@.planning/phases/22-experience-page-holloway-case-study/22-PATTERNS.md
@.planning/phases/22-experience-page-holloway-case-study/22-UI-SPEC.md

@src/components/primitives/Header.astro
@src/components/primitives/MobileMenu.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `experience` first in Header.astro nav (D-03)</name>
  <files>src/components/primitives/Header.astro</files>
  <read_first>
    - src/components/primitives/Header.astro (navLinks lines 23-27; isActive lines 29-35; the render loop lines 42-54 and .nav-link.is-active style lines 120-126 are NOT edited)
    - .planning/phases/22-experience-page-holloway-case-study/22-PATTERNS.md (Header edit excerpt + line refs)
  </read_first>
  <action>
    In src/components/primitives/Header.astro, prepend `{ href: "/experience", label: "experience" }` as the FIRST element of the `navLinks` array (before the existing `/projects` "works" entry), yielding order experience · works · about · contact per D-03. Add a new branch to `isActive` returning `currentPath.startsWith("/experience")` when `href === "/experience"`, placed before the existing branches, mirroring the `/projects` branch pattern exactly. Do NOT modify the nav render loop, the `<style>` block (the `.nav-link.is-active` accent underline already applies), or the hamburger/container-query logic. No new CSS.
  </action>
  <acceptance_criteria>
    - Header.astro `navLinks` first element is `{ href: "/experience", label: "experience" }` (index of `/experience` < index of `/projects`)
    - `isActive` contains a `currentPath.startsWith("/experience")` branch
    - No diff to the render loop, `<style>`, or hamburger logic
    - `pnpm exec astro check` exits 0/0/0
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec astro check</automated>
  </verify>
  <done>Header renders experience-first nav with an active-state branch covering /experience and /experience/[id].</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the nav entry in MobileMenu.astro — focus-trap script untouched (D-03, D-26)</name>
  <files>src/components/primitives/MobileMenu.astro</files>
  <read_first>
    - src/components/primitives/MobileMenu.astro (navLinks lines 38-42; isActive lines 52-57; the `<script>` focus-trap/inert block lines 225-380 must NOT be touched; socialLinks lines 45-50 unrelated)
    - .planning/phases/22-experience-page-holloway-case-study/22-PATTERNS.md (MobileMenu parallel-edit note + D-26 warning)
  </read_first>
  <action>
    Apply the identical edit as Task 1 to src/components/primitives/MobileMenu.astro: prepend `{ href: "/experience", label: "experience" }` as the first `navLinks` element and add the `currentPath.startsWith("/experience")` branch to `isActive`. Leave the `socialLinks` array, the `.mobile-menu-link.is-active` style, and — critically — the entire `<script>` focus-trap block (setupFocusTrap re-query, Escape/backdrop close, `document.querySelector(".chat-widget").setAttribute("inert", ...)` handling, DOMContentLoaded + astro:page-load init) byte-for-byte unchanged. That script is under the D-26 chat-surface battery; any change risks the QA-01 invariant.
  </action>
  <acceptance_criteria>
    - MobileMenu.astro `navLinks` first element is `{ href: "/experience", label: "experience" }`
    - `isActive` contains a `currentPath.startsWith("/experience")` branch
    - The `<script>` block (lines ~225-380, including `.chat-widget` inert handling) is unchanged (diff shows no hunk inside the script)
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm exec vitest run tests/build/experience-nav.test.ts` passes (both primitives now experience-first)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/build/experience-nav.test.ts</automated>
  </verify>
  <done>Both nav primitives carry experience-first with active-state; the MobileMenu focus-trap/inert script is provably untouched; experience-nav.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared layout (Header/MobileMenu) → chat surface | Both nav primitives are BaseLayout children; MobileMenu's `<script>` manages `.chat-widget` inert state (D-26). An edit here can regress the chat focus-trap / a11y contract (QA-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Tampering / DoS | MobileMenu focus-trap + `.chat-widget` inert `<script>` (D-26 chat-surface battery) | high | mitigate | Edit is confined to the `navLinks` array + `isActive` function only; the `<script>` block is left byte-identical. Wave 3 (22-05) runs `pnpm test` (D-26 battery) after this merges. |
| T-22-02 | Tampering | Nav markup rendering user-controlled href | low | accept | hrefs are static string literals authored in-repo (no user input); no injection vector. |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (QA-02 lock); no install task. |
</threat_model>

<verification>
- `pnpm exec astro check` exits 0/0/0 across both edited primitives.
- `pnpm exec vitest run tests/build/experience-nav.test.ts` passes.
- `git diff src/components/primitives/MobileMenu.astro` shows no changes inside the `<script>` block (focus-trap / `.chat-widget` inert untouched).
- The D-26 chat-surface battery is re-run at the Wave 3 gate (22-05) via `pnpm test`.
</verification>

<success_criteria>
The primary nav shows `experience` first on every page (desktop + mobile), the entry is active on /experience and /experience/[id], and the chat-surface script is provably unchanged. EXP-02 (nav half) satisfied.
</success_criteria>

## Artifacts this phase produces (this plan)

- Modified: `src/components/primitives/Header.astro` — adds `navLinks` entry `{ href: "/experience", label: "experience" }` (first) + `isActive` `startsWith("/experience")` branch
- Modified: `src/components/primitives/MobileMenu.astro` — same parallel nav edit

(No net-new symbols; the `/experience` nav href is the new site-wide navigation target.)

<output>
Create `.planning/phases/22-experience-page-holloway-case-study/22-02-SUMMARY.md` when done.
</output>
