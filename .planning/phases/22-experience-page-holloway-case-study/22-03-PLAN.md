---
phase: 22-experience-page-holloway-case-study
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/pages/experience.astro
  - src/content/experience/holloway.mdx
autonomous: true
requirements: [EXP-02, EXP-03, EXP-05]
must_haves:
  truths:
    - "/experience renders Holloway as a rich scannable summary: role · company · dates · stack meta line + first-person summary + all 5 highlights + a 'Read the full case study →' link to /experience/holloway"
    - "/experience renders Balfour as a visibly lighter, non-linked 'Earlier' entry (role · dates · its 1-2 highlight lines) with no case-study link"
    - "Entries appear reverse-chronologically (Holloway first, Balfour second) via sortExperienceEntries — not an inline .sort()"
    - "The company renders as 'Holloway Company' (no leading 'The') everywhere on the summary"
  artifacts:
    - src/pages/experience.astro
    - src/content/experience/holloway.mdx
  key_links:
    - "sortExperienceEntries(getCollection('experience')) drives order; .find(hasCaseStudy) selects Holloway, .filter(!hasCaseStudy) selects earlier work"
    - "Holloway summary deep-dive link href = /experience/${holloway.id} → /experience/holloway"
    - "holloway.mdx company frontmatter feeds both this summary meta AND the 22-04 detail H1 (D-08 single-source)"
---

<objective>
Build the `/experience` listing page (src/pages/experience.astro) mirroring src/pages/projects.astro, rendering the Phase 21 `experience` collection as the D-04 asymmetric two-tier layout: Holloway as a rich featured summary (D-05: meta · first-person summary · all 5 highlights · deep-dive link) and Balfour as a lightweight non-linked "Earlier" entry (D-06). Also apply the D-08 company normalization in holloway.mdx frontmatter (`"The Holloway Company"` → `"Holloway Company"`), which this summary meta is the first consumer of. Turns tests/content/experience-summary.test.ts GREEN.

Purpose: Deliver the reachable Experience page (EXP-02 route) with the 30-second recruiter payload (EXP-03) and the structurally-honest lightweight earlier-work entry (EXP-05 listing treatment).
Output: One new listing page + one frontmatter edit. All visual form (card vs bordered block, the "Earlier" cue, spacing/type ramp) is decided by the frontend-design skill against design-system/MASTER.md + 22-UI-SPEC.md (SC5). Data plumbing and content are fixed by the decisions; pixels are the skill's.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-experience-page-holloway-case-study/22-CONTEXT.md
@.planning/phases/22-experience-page-holloway-case-study/22-RESEARCH.md
@.planning/phases/22-experience-page-holloway-case-study/22-PATTERNS.md
@.planning/phases/22-experience-page-holloway-case-study/22-UI-SPEC.md
@design-system/MASTER.md

# Analogs + data
@src/pages/projects.astro
@src/lib/experience.ts
@src/content/experience/holloway.mdx
@src/content/experience/balfour-beatty.mdx
@src/components/primitives/Container.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-08 company normalization in holloway.mdx frontmatter</name>
  <files>src/content/experience/holloway.mdx</files>
  <read_first>
    - src/content/experience/holloway.mdx (company frontmatter line 3)
    - .planning/phases/22-experience-page-holloway-case-study/22-RESEARCH.md (Pitfall 1: sync-experience.mjs diffs BODY only; frontmatter is drift-gate-exempt)
    - scripts/sync-experience.mjs — NOT edited; referenced only to confirm frontmatter is read verbatim from the .mdx
  </read_first>
  <action>
    Edit ONLY the `company:` frontmatter value in src/content/experience/holloway.mdx from `"The Holloway Company"` to `"Holloway Company"` (D-08 — drop the leading "The"). Do NOT touch the MDX body, any other frontmatter field, or Experience/HOLLOWAY_EXPERIENCE.md. This is drift-gate-safe: scripts/sync-experience.mjs reassembles frontmatter read verbatim from the .mdx + body from the fenced source, so only the body is effectively diffed — a frontmatter edit cannot produce drift (Pitfall 1). Leave the body intro blockquote's "the Holloway Company" prose verbatim per D-09 (do not route this through the source .md).
  </action>
  <acceptance_criteria>
    - holloway.mdx line 3 reads `company: "Holloway Company"` (no "The")
    - The MDX body and all other frontmatter fields are unchanged
    - `pnpm sync:experience:check` exits 0 (no drift introduced)
    - `pnpm exec astro check` exits 0/0/0
  </acceptance_criteria>
  <verify>
    <automated>pnpm sync:experience:check</automated>
  </verify>
  <done>Company normalized to "Holloway Company" in frontmatter only; drift gate green; body untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build /experience listing page — asymmetric two-tier (D-04/D-05/D-06), frontend-design-composed</name>
  <files>src/pages/experience.astro</files>
  <read_first>
    - src/pages/projects.astro (full — the listing-shell analog: BaseLayout → section → Container; imports lines 2-6; note it maps a UNIFORM WorkRow list, which this page does NOT)
    - src/lib/experience.ts (sortExperienceEntries — reverse-chron by startDate desc; do NOT inline .sort())
    - src/content/experience/holloway.mdx + balfour-beatty.mdx (the two rendered entries: fields role/company/dateRange/techStack/summary/highlights/hasCaseStudy)
    - .planning/phases/22-experience-page-holloway-case-study/22-UI-SPEC.md (Copywriting Contract + Typography + Color + Spacing — the token/type/copy contract)
    - .planning/phases/22-experience-page-holloway-case-study/22-PATTERNS.md (experience.astro assignment: imports + data selection excerpt)
    - design-system/MASTER.md (locked six-token palette, type-role classes, restrained-motion + focus-ring rules)
  </read_first>
  <action>
    Create src/pages/experience.astro mirroring src/pages/projects.astro's shell. Import getCollection from astro:content, BaseLayout, Container, and sortExperienceEntries from ../lib/experience. Compute `const entries = sortExperienceEntries(await getCollection("experience"))`, `const holloway = entries.find((e) => e.data.hasCaseStudy)`, `const earlier = entries.filter((e) => !e.data.hasCaseStudy)` (do NOT inline a .sort() — experience sorts by startDate desc, not order). Wrap in BaseLayout with title `Experience` and a positioning-neutral description using an EN dash (D-10) — the correct string is "Production engineering experience – the Holloway Connect contract and earlier work." (NOT an em dash; the UI-SPEC copy-table line 103 em dash is a transcription error superseded by the zero-em-dash rule). Render the D-04 asymmetric two-tier: (a) Holloway rich summary exposing the meta line role · company · dateRange · techStack (uppercased, joined with ` · `), the first-person `summary` sentence, ALL 5 `highlights`, and a mono "Read the full case study →" link with href `/experience/${holloway.id}` (D-05); (b) Balfour as a lighter, NON-LINKED "Earlier" entry showing role · dateRange + its highlight lines with a quiet mono "EARLIER" cue (D-06) — no href, no case-study link. INVOKE the frontend-design skill (frontend-design:frontend-design) to decide the exact visual form (card vs bordered block vs section, the "Earlier" divider/label treatment, spacing from the MASTER scale, which type-role class each element takes) against design-system/MASTER.md + 22-UI-SPEC.md (SC5); attribute the choices in the SUMMARY. Keep all styling in a page-scoped `<style>` block using only the six global tokens (`var(--ink)` etc.) — do NOT edit src/styles/global.css. Zero em dashes in any authored string.
  </action>
  <acceptance_criteria>
    - src/pages/experience.astro exists; uses `sortExperienceEntries(await getCollection("experience"))` (no inline `.sort(`)
    - Holloway block renders role, company, dateRange, techStack, summary, all 5 highlights, and a link with href `/experience/holloway`
    - Balfour block renders role + dateRange + its highlight lines with an "EARLIER"/earlier cue and has NO href/case-study link
    - The BaseLayout description string contains no `—` (em dash); uses `–` (en dash)
    - No diff to src/styles/global.css; new styles are in a page-scoped `<style>`
    - Frontend-design skill invoked for the two-tier + "Earlier" cue composition; attribution recorded in SUMMARY (SC5)
    - `pnpm exec astro check` exits 0/0/0; `pnpm exec vitest run tests/content/experience-summary.test.ts` passes; `pnpm exec vitest run tests/content/experience-voice-em-dash.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/content/experience-summary.test.ts tests/content/experience-voice-em-dash.test.ts</automated>
  </verify>
  <done>/experience renders the reverse-chron two-tier: Holloway rich summary with the deep-dive link + Balfour non-linked "Earlier" entry; company shows "Holloway Company"; global.css untouched; frontend-design attributed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content (MDX frontmatter/body) → rendered HTML | The `experience` collection is Zod-validated first-party content, rendered to static HTML at build; no runtime user input reaches this page. |
| listing page styling → chat surface | Prefer page-scoped `<style>`; editing global.css would pull the D-26 chat-surface battery into scope (QA-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering / EoP | MDX/collection content rendered into HTML | low | accept | Content is trusted, in-repo, Zod-validated (content.config.ts) and synced from fenced sources; rendered at build by Astro, not at runtime; no user-supplied markup. |
| T-22-03 | Info Disclosure | Confidential-contract exposure via summary copy | low | accept | Summary renders verbatim owner-approved frontmatter (summary + 5 highlights); no credentials, client PII, or internal URLs; no external links on the listing. |
| T-22-05 | Tampering | global.css edit regressing chat surface | high | mitigate | New CSS confined to a page-scoped `<style>` block using existing global tokens; global.css is not touched; Wave 3 (22-05) runs `pnpm test` (D-26 battery) as the guard. |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (QA-02 lock); no install task. |
</threat_model>

<verification>
- `pnpm exec astro check` exits 0/0/0 (catches Pitfall 2 field errors, e.g. using `year` — experience has `dateRange`).
- `pnpm exec vitest run tests/content/experience-summary.test.ts tests/content/experience-voice-em-dash.test.ts` passes.
- `pnpm sync:experience:check` exits 0 (D-08 introduced no body drift).
- `git diff src/styles/global.css` is empty.
</verification>

<success_criteria>
/experience is reachable and renders the reverse-chron asymmetric two-tier: Holloway's full 30-second payload (meta + summary + 5 highlights + deep-dive link) and Balfour's non-linked "Earlier" entry, with company normalized to "Holloway Company" and the frontend-design skill's visual decisions attributed. EXP-02 (route), EXP-03, EXP-05 (listing) satisfied.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files (exclude from drift/source-grounding verification):
- `src/pages/experience.astro` — NEW route `GET /experience` (listing page)
- The `/experience/${holloway.id}` → `/experience/holloway` deep-dive link (target route built by 22-04)
- Modified: `src/content/experience/holloway.mdx` — `company` frontmatter normalized to `"Holloway Company"` (D-08)

<output>
Create `.planning/phases/22-experience-page-holloway-case-study/22-03-SUMMARY.md` when done.
</output>
