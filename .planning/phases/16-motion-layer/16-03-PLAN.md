---
phase: 16
plan: 03
type: execute
wave: 1
depends_on:
  - "16-01"
files_modified:
  - design-system/MOTION.md
  - design-system/MASTER.md
autonomous: true
requirements:
  - MOTN-09
tags:
  - documentation
  - design-system
  - motion
  - phase-16
  - tdd-green

must_haves:
  truths:
    - "`design-system/MOTION.md` exists at repo root and is the v1.2 motion canonical doc — property whitelist + duration bands + easing defaults + per-animation specs (MOTN-01..MOTN-07) + reduced-motion contract."
    - "`design-system/MASTER.md` §6 is replaced with a short stub (≤12 lines) that points to MOTION.md (per D-02)."
    - "MASTER.md §8 anti-pattern list still bans GSAP and `<ClientRouter />` (those bans survive verbatim — D-02 says §8 stays intact for these)."
    - "MASTER.md §8 reconciles the `::view-transition-*` keyframe ban — adds carve-out language permitting MOTION.md authorization (MOTN-01 ships those keyframes)."
    - "MASTER.md §11 Changelog gains a v1.2 / Phase 16 entry."
    - "All assertions in `tests/build/motion-doc.test.ts` flip from RED to GREEN."
    - "MOTION.md uses third-person spec voice (per MEMORY voice-split rule for design-system docs)."
    - "MOTION.md does NOT introduce any new dependency, code, or runtime artifact — pure documentation."
  artifacts:
    - path: "design-system/MOTION.md"
      provides: "v1.2 motion canonical doc — supersedes MASTER.md §6"
      min_lines: 120
      max_lines: 280
      contains: "Property Whitelist"
    - path: "design-system/MASTER.md"
      provides: "§6 replaced with stub; §8 amended; §11 changelog appended"
      min_lines_unchanged: 800
  key_links:
    - from: "design-system/MASTER.md §6"
      to: "design-system/MOTION.md"
      via: "markdown link"
      pattern: "\\(MOTION\\.md\\)|\\(\\./MOTION\\.md\\)"
    - from: "design-system/MASTER.md §8"
      to: "design-system/MOTION.md MOTN-01 carve-out"
      via: "MOTION.md or MOTN-01 reference within §8"
      pattern: "MOTION\\.md|MOTN-01"
    - from: "design-system/MOTION.md §Reduced-motion contract"
      to: "src/styles/global.css (Plan 04 + 05)"
      via: "describes the @media gating contract Plans 04+05 implement"
      pattern: "no-preference|reduce"

---

<objective>
Author `design-system/MOTION.md` as the v1.2 motion canonical doc and rewrite `design-system/MASTER.md` §6 to a short stub pointer (D-01, D-02, D-03 → MOTN-09).

Purpose: Establish a single authoritative source for all v1.2+ motion decisions. MASTER.md remains the v1.1 design lock for non-motion concerns (color tokens, type roles, layout grammar, primitives, accent rules); MOTION.md becomes the standalone motion contract that future phases reference. Reconciles MASTER.md §8 anti-pattern list with the `::view-transition-*` keyframes that Plan 04 will ship.

Output: 1 new markdown file (`design-system/MOTION.md`, ~150-250 lines) + 3 surgical edits to `design-system/MASTER.md` (§6 replacement, §8 amendment, §11 changelog entry). Zero source code changes. Zero `package.json` edits.

This plan runs in parallel with Plan 02 (no file overlap — observer factory + scroll-depth is `src/`-only; this plan is `design-system/`-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-motion-layer/16-CONTEXT.md
@.planning/phases/16-motion-layer/16-PATTERNS.md
@.planning/phases/16-motion-layer/16-RESEARCH.md
@.planning/phases/16-motion-layer/16-VALIDATION.md
@.planning/phases/16-motion-layer/16-UI-SPEC.md

<interfaces>
<!-- Reference points the doc must lock down -->

From `design-system/MASTER.md` (current state — read before editing):
- Lines 1-9: header pattern (milestone, phase, status, source visual reference, separator)
- Lines 660-693: current §6 "Motion (the pragmatic motion line)" — this entire span gets REPLACED with the stub
- Lines 729-748: current §8 "Anti-patterns" — line 747 has the view-transition ban that needs reconciliation
- Lines 800+: §11 Changelog (existing) — append a v1.2 / Phase 16 entry

From `.planning/phases/16-motion-layer/16-UI-SPEC.md`:
- §"Property whitelist" table (transform, opacity, box-shadow allowed; width/height/padding/etc PROHIBITED)
- §"Duration bands" table (Snap 120 / Tap 180 / Reveal 250-350 / View-transition 200 / Loop-fast 600 / Loop-slow 2500)
- §"Easing defaults" table (ease-out entrances, ease-in-out loops, ease default browser hover)
- §"Animation specs" table — 7 rows MOTN-01..MOTN-07 with full From→To / Duration / Easing / Reduced-motion data
- §"Reduced-motion contract (MOTN-08)" — D-23 / D-24 / D-25 rules

From `tests/build/motion-doc.test.ts` (Plan 01 wrote — read every it() block; MOTION.md must contain every asserted token):
- MOTION.md exists; contains "Property Whitelist" section header; contains transform / opacity / box-shadow; contains all 7 duration markers; contains ease-out / ease-in-out; references MOTN-01..MOTN-07; does NOT contain `cubic-bezier(`; does NOT contain `import gsap`
- MASTER.md §6 contains "v1.1 motion lock superseded by v1.2"; links to MOTION.md; §6 ≤12 lines; §8 still bans GSAP; §8 references MOTION.md / MOTN-01 + view-transition; §8 still bans ClientRouter; §11 Changelog has v1.2 + Phase 16
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author design-system/MOTION.md (NEW — v1.2 motion canonical doc)</name>
  <files>design-system/MOTION.md</files>

  <read_first>
    - design-system/MASTER.md lines 1-30 (header pattern, lock contract style, milestone declaration shape)
    - design-system/MASTER.md §1-§5 (numbered section headers, table style — `| col | col |`)
    - design-system/MASTER.md §11 Changelog (existing entries — to mirror version style)
    - .planning/phases/16-motion-layer/16-UI-SPEC.md (the FULL motion contract — every value in MOTION.md must come from this spec; MOTION.md is the prose-canonicalization of the spec)
    - .planning/phases/16-motion-layer/16-CONTEXT.md D-01..D-25 (decisions list; D-04 selectors, D-05 IO opts, D-06 reveal motion values, D-09/D-10 word-stagger, D-14/D-15 pulse, D-20/D-21 view-transitions, D-23/D-24/D-25 reduced-motion contract)
    - .planning/phases/16-motion-layer/16-PATTERNS.md section "design-system/MOTION.md (NEW)" — header pattern + section structure recommendation
    - tests/build/motion-doc.test.ts (the contract — every assertion must pass after this file is written)
    - MEMORY: project_voice_split.md (third-person spec voice for design-system docs — "Pulse animates the bubble at 2500ms ease-in-out infinite", NOT first-person "I designed", NOT third-person-about-Jack "Jack designed")
  </read_first>

  <action>
Create `design-system/MOTION.md` (NEW file at repo root in `design-system/` directory). Use this exact structure — 10 numbered sections + header + final changelog. Total target: ~180-220 lines.

```markdown
# Jack Cutrara Portfolio — Motion System (MOTION.md)

> **Milestone:** v1.2 Motion Layer
> **Phase:** 16 (Motion)
> **Status:** Locked at Phase 16 sign-off — supersedes MASTER.md §6 (v1.1 motion lock)
> **Authoritative for:** all v1.2+ motion decisions. MASTER.md §2-§5, §7-§10 still bind for non-motion concerns.

---

## 1. Overview

This document is the canonical contract for motion in the Jack Cutrara portfolio site. It supersedes the v1.1 motion stance in MASTER.md §6, which has been replaced with a stub pointer to this file.

The site's motion philosophy is **tasteful and quiet**. The editorial design system locked in v1.1 favors restraint over spectacle; motion in v1.2 layers on top of that locked surface to add cadence and hierarchy without compromising performance, accessibility, or the editorial aesthetic.

Three principles bind every animation in this document:

1. **Native platform only.** Cross-document `@view-transition`, IntersectionObserver, CSS `@keyframes`, CSS transitions. Zero `package.json` runtime dependencies for motion. GSAP is prohibited (MASTER.md §8). `<ClientRouter />` is prohibited (MASTER.md §8). The `motion` npm package and Lenis smooth-scroll are documented anti-patterns.
2. **Reduced-motion is a first-class concern.** Every entrance animation lives inside `@media (prefers-reduced-motion: no-preference)`; every looped animation is paired with an explicit `@media (prefers-reduced-motion: reduce)` neutralizer. The first test in every motion feature is the reduced-motion negative case.
3. **Lighthouse holds.** Performance ≥ 99, Accessibility ≥ 95, Best Practices = 100, SEO = 100, TBT ≤ 150 ms, CLS ≤ 0.01. Motion that risks any of these gates does not ship.

This document is technical specification — third-person spec voice, no marketing prose.

---

## 2. Property Whitelist

Animations may move only the following CSS properties. Every other property is prohibited unless an amendment to this document is committed first.

| Property | Allowed | Why |
|----------|---------|-----|
| `transform` (translate, scale) | YES | GPU-composited; no layout / paint cost |
| `opacity` | YES | GPU-composited; no layout / paint cost |
| `box-shadow` | YES (chat bubble pulse only) | Paint-only; no layout shift; one declared use site (MOTN-04) |
| `color`, `background-color`, `text-decoration-color` | YES (existing hover/focus state transitions only — sub-200ms) | Inherited from MASTER.md §6.2; not new |
| `width`, `height`, `margin`, `padding`, `top` / `left` / `right` / `bottom`, `border-width`, `font-size` | PROHIBITED | Layout-triggering; banned to protect Lighthouse Performance ≥ 99 and CLS ≤ 0.01 |
| `filter`, `backdrop-filter` | PROHIBITED | Expensive paint; not needed for the editorial cadence |

The `box-shadow` allowance is scoped narrowly to the chat bubble pulse ring (MOTN-04). No other component may animate `box-shadow`.

---

## 3. Duration Bands

| Band | Duration | Used by |
|------|----------|---------|
| Snap | 120 ms | Legacy hover transitions (MASTER.md §6.2 — already on the floor) |
| Tap | 180 ms | WorkRow arrow (MOTN-03), chat panel scale-in (MOTN-05) |
| Reveal | 250–350 ms (default 300 ms for section reveals; 250 ms for word-stagger per word) | Scroll-reveal (MOTN-02), word-stagger (MOTN-07) |
| View-transition | 200 ms | Cross-document fade (MOTN-01) |
| Loop-fast | 600 ms | Typing-dot bounce (MOTN-06 — already live) |
| Loop-slow | 2500 ms | Chat bubble pulse (MOTN-04) |

**Tolerance:** ±20 ms is acceptable rounding; outside that band, file an amendment to this document.

---

## 4. Easing Defaults

| Use | Easing | Why |
|-----|--------|-----|
| Entrances (reveal, stagger, scale-in, view-transition) | `ease-out` | Decelerating curve — feels "settling into place" |
| Loops (pulse, typing-dot bounce) | `ease-in-out` | Symmetric breathing rhythm |
| Hover / focus state changes | `ease` (browser default) | Inherited from MASTER.md §6.2 |

`cubic-bezier()` custom curves are prohibited in v1.2. The named easings above cover every animation specified in this document. Adding a custom curve later requires an amendment.

---

## 5. Animation Specs

Each row below is the locked specification for one animation. Every value originates in `.planning/phases/16-motion-layer/16-UI-SPEC.md` and must remain consistent if either document is amended.

| ID | Name | Trigger | From → To | Duration | Easing | Reduced-motion |
|----|------|---------|-----------|----------|--------|----------------|
| MOTN-01 | Page-enter fade | Cross-document navigation (`@view-transition { navigation: auto; }`) | `::view-transition-old(root)` opacity 1→0; `::view-transition-new(root)` opacity 0→1 | 200 ms | `ease-out` | `@media (prefers-reduced-motion: reduce)` neutralizes via `animation: none` on both pseudo-elements |
| MOTN-02 | Scroll-reveal | IntersectionObserver `rootMargin: "0px 0px -10% 0px"`, `threshold: 0`; one-shot via `unobserve()` after first intersect | opacity 0→1 + translateY 12px→0 | 300 ms | `ease-out` | Wrapped in `@media (prefers-reduced-motion: no-preference) { ... }`. Under `reduce`, elements render at resting state immediately. |
| MOTN-03 | WorkRow arrow slide-in | `.work-row:hover` / `.work-row:focus-visible` | `.work-arrow` opacity 0→1 + translateX 0→4px | 180 ms | `ease-out` | Inside `no-preference`. Under `reduce`, arrow remains opacity 0; accent title underline still appears as the affordance. |
| MOTN-04 | Chat bubble pulse | Idle (no hover, no focus, panel closed) | Combined: box-shadow ring `0 0 0 0 rgba(230, 57, 70, 0.4)` → `0 0 0 16px rgba(230, 57, 70, 0)`; transform scale(1.0) → scale(1.02) at 50% → scale(1.0) at 100% | 2500 ms | `ease-in-out` (loop) | Paused on `:hover`, `:focus-visible`, and `[data-pulse-paused]` (set by `chat.ts` `openPanel()`). Paired `@media (prefers-reduced-motion: reduce) { #chat-bubble { animation: none; } }` neutralizer. |
| MOTN-05 | Chat panel scale-in | Panel open (`chat.ts openPanel()` adds `.is-open` class) | transform scale(0.96) → scale(1.0) + opacity 0→1; `transform-origin: bottom right` | 180 ms | `ease-out` | Inside `no-preference`. Under `reduce`, panel renders at scale(1) opacity 1 immediately. |
| MOTN-06 | Typing-dot bounce | Chat SSE streaming active (`#chat-typing` becomes `display: flex`) | translateY 0 → -4px → 0 (3 dots, 150 ms phase-shift each) | 600 ms | `ease-in-out` (loop) | Already live at `src/styles/global.css:260-280`. v1.2 extends with paired `reduce` override for parity (`.typing-dot { animation: none; }`). |
| MOTN-07 | `.h1-section` word-stagger | Same IntersectionObserver trigger as MOTN-02 | Per-word: opacity 0→1 + translateY 8px→0; 60 ms delay between words via `--i` CSS custom property and `animation-delay: calc(var(--i) * 60ms)` | 250 ms per word | `ease-out` | Inside `no-preference`. Under `reduce`, `motion.ts` skips the span-wrap entirely; heading renders as plain text in resting state. **Never applied to `.display`.** |

**`.display` exclusion (MOTN-07):** the homepage `.display` wordmark is excluded from word-stagger AND scroll-reveal. The hero remains untouched, satisfying Phase 16 success criterion #1.

---

## 6. Reduced-Motion Contract (MOTN-08)

Two patterns coexist in the codebase:

1. **Wrap-in-no-preference** — used for entrance animations (MOTN-01, MOTN-02, MOTN-05, MOTN-07):

   ```css
   @media (prefers-reduced-motion: no-preference) {
     /* entrance animation only fires when user hasn't requested reduce */
   }
   ```

2. **Unconditional + paired-reduce-override** — used for loops (MOTN-04 chat pulse, MOTN-06 typing-dot bounce):

   ```css
   /* unconditional rule */
   #chat-bubble { animation: chat-pulse 2500ms ease-in-out infinite; }
   /* paired override neutralizes the rule under reduce */
   @media (prefers-reduced-motion: reduce) {
     #chat-bubble { animation: none; }
   }
   ```

**Pattern selection:** entrance animations should default to OFF for reduce users (no-preference wrap). Loops with state-signaling side effects (the bubble pulse signals "active state") follow the paired-override pattern so the animation declaration is co-located with its neutralizer for reviewability.

`motion.ts` reads `matchMedia('(prefers-reduced-motion: reduce)').matches` once at init time. When `reduce` matches, `motion.ts` returns immediately — no IntersectionObserver is constructed, no `splitWords` runs, no DOM mutation occurs. Heading text renders as plain text (no `<span class="word">` children). The `change` event on the media query is intentionally NOT listened to — cross-document navigation re-reads the media query on the next page load, which is acceptable for the editorial cadence.

**The first test in every motion feature is the reduced-motion negative case** (ROADMAP "Reduced-Motion Contract" gate).

---

## 7. File Ownership

| File | Role | Status |
|------|------|--------|
| `design-system/MOTION.md` (this file) | v1.2 motion canonical doc | NEW (Phase 16 Plan 03) |
| `design-system/MASTER.md` §6 | 3-line stub pointing to MOTION.md | REPLACED (Phase 16 Plan 03) |
| `design-system/MASTER.md` §8 | Anti-pattern list — `::view-transition-*` ban amended to permit MOTION.md authorization | AMENDED (Phase 16 Plan 03) |
| `src/scripts/lib/observer.ts` | Shared IntersectionObserver factory | NEW (Phase 16 Plan 02) |
| `src/scripts/motion.ts` | Scroll-reveal + word-stagger + reduced-motion gate | NEW (Phase 16 Plan 04) |
| `src/scripts/scroll-depth.ts` | Refactored to consume `makeRevealObserver` factory | MODIFIED (Phase 16 Plan 02) |
| `src/scripts/chat.ts:451-457` | `startPulse` / `stopPulse` replaced with `data-pulse-paused` attribute toggling | MODIFIED (Phase 16 Plan 05; D-26 chat regression gate applies) |
| `src/styles/global.css` | View-transition rule + chat-pulse keyframes + reveal classes + word-stagger + chat-panel scale-in | MODIFIED (Phase 16 Plan 04 + Plan 05) |
| `src/styles/global.css:260-280` | Already-live `@keyframes typing-bounce` | DO NOT TOUCH (MOTN-06 zero-cost; optional `reduce` extension at Plan 05's discretion) |
| `src/components/primitives/WorkRow.astro:81-98` | Arrow upgrade (opacity 120ms + transform 180ms ease-out, hover translateX 4px, paired `reduce` override) | MODIFIED (Phase 16 Plan 06) |
| `src/layouts/BaseLayout.astro:118-121` | Adds `import "../scripts/motion.ts";` to existing processed script block | MODIFIED (Phase 16 Plan 04) |

---

## 8. Lighthouse Gate (MOTN-10)

After every motion change, Lighthouse CI runs on the homepage plus one project detail page. Acceptable thresholds:

- Performance ≥ 99
- Accessibility ≥ 95
- Best Practices = 100
- SEO = 100
- Total Blocking Time ≤ 150 ms
- Cumulative Layout Shift ≤ 0.01

Stress conditions verified by automated tests:

- `grep -c "will-change" src/styles/global.css` returns 0 — `will-change` declarations risk sticky GPU promotion that compounds over a session
- `grep -c "cubic-bezier(" src/styles/global.css` returns 0 — named easings only
- IntersectionObserver target count stays under 30 per page (the largest count is `/projects/[id]` with the 4 scroll-depth sentinels + reveal targets — measured well under cap)
- No new `package.json` runtime dependency

If any threshold drops below the bar after a motion-related change, the change does not merge.

---

## 9. Anti-Patterns

These do not ship under any circumstance in v1.2+:

- **`cubic-bezier()` custom curves** — named easings only (§4)
- **`will-change` declarations** — transform and opacity are GPU-composited natively; sticky promotion compounds across navigations
- **Layout-triggering property animations** (width / height / padding / margin / top / left / etc.) — banned by §2 property whitelist
- **`<ClientRouter />`** — Phase 8 removed it; `@view-transition` cross-document is the v1.2 path (MOTN-01)
- **`transition:persist` on Astro elements** — collides with chat localStorage persistence (Phase 7 D-29)
- **GSAP** — uninstalled in Phase 8; reintroduction requires reverting v1.1 sign-off
- **Lenis smooth-scroll** — known Astro compatibility issues; native CSS `scroll-behavior: smooth` covers v1.2 needs
- **`motion` npm package** — listed in REQUIREMENTS.md Future as optional; not on the v1.2 path
- **Scroll-driven animations beyond the MOTN-02 reveal** — no parallax, no pin-to-viewport, no scroll-progress bars
- **Canvas / WebGL / Three.js for decorative motion** — banned in MASTER.md §8; not reintroduced

---

## 10. Changelog

- **v1.2 — Phase 16 (2026-04-XX):** Initial authoring. MASTER.md §6 superseded by this document. Property whitelist, duration bands, easing defaults, MOTN-01..MOTN-07 specs, reduced-motion contract, Lighthouse gate, anti-patterns locked at Phase 16 sign-off.
```

**Voice rule reminder:** every sentence above is third-person spec voice. No "I designed", no "Jack designed", no marketing words. The doc reads as engineering specification.

**Length verification:** target ~180-220 lines. Density over padding. Every paragraph adds substantive contract content.

**Token budget guard:** no instances of `cubic-bezier(`, no `import gsap` references except inside the §9 anti-patterns list (banned context only).

After writing, run `pnpm vitest run tests/build/motion-doc.test.ts -t "MOTION.md" --reporter=dot`. The 8 MOTION.md-specific tests should all be GREEN.
  </action>

  <verify>
    <automated>pnpm vitest run tests/build/motion-doc.test.ts -t "MOTION.md" --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - File `design-system/MOTION.md` exists at repo root
    - `wc -l design-system/MOTION.md` reports 120-280 lines
    - `grep -c '^## ' design-system/MOTION.md` returns >= 9 (at least 9 numbered sections)
    - `grep -c 'Property Whitelist\|Property whitelist' design-system/MOTION.md` returns >= 1
    - `grep -c 'Duration Bands\|Duration bands' design-system/MOTION.md` returns >= 1
    - `grep -c 'Reduced-Motion\|Reduced-motion' design-system/MOTION.md` returns >= 1
    - All 7 motion IDs MOTN-01..MOTN-07 appear: `for id in MOTN-01 MOTN-02 MOTN-03 MOTN-04 MOTN-05 MOTN-06 MOTN-07; do grep -q "$id" design-system/MOTION.md; done` succeeds
    - All 7 duration markers present: `for d in 120ms 180ms 200ms 250 300ms 350 600ms 2500ms; do grep -q "$d" design-system/MOTION.md; done` succeeds
    - `grep -c 'transform' design-system/MOTION.md` returns >= 3
    - `grep -c 'opacity' design-system/MOTION.md` returns >= 3
    - `grep -c 'box-shadow' design-system/MOTION.md` returns >= 2
    - `grep -c 'ease-out' design-system/MOTION.md` returns >= 3
    - `grep -c 'ease-in-out' design-system/MOTION.md` returns >= 2
    - `grep -c 'cubic-bezier(' design-system/MOTION.md` returns 0
    - `grep -c 'import gsap' design-system/MOTION.md` returns 0
    - `pnpm vitest run tests/build/motion-doc.test.ts -t "MOTION.md" --reporter=dot` exits 0 — all MOTION.md tests GREEN
  </acceptance_criteria>

  <done>design-system/MOTION.md exists with 10 sections, 120-280 lines, all required tokens. tests/build/motion-doc.test.ts MOTION.md tests GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite MASTER.md §6 to stub + amend §8 view-transition reconciliation + append §11 changelog entry</name>
  <files>design-system/MASTER.md</files>

  <read_first>
    - design-system/MASTER.md (full §6 region around lines 660-693 — the prose to be replaced)
    - design-system/MASTER.md §8 lines 729-748 (current anti-pattern list — line 747 has the view-transition ban)
    - design-system/MASTER.md §11 Changelog (read existing entries; mirror their format)
    - .planning/phases/16-motion-layer/16-PATTERNS.md section "design-system/MASTER.md §6 (MODIFIED — replace with 3-line stub)"
    - .planning/phases/16-motion-layer/16-CONTEXT.md D-02
    - tests/build/motion-doc.test.ts (the contract for §6 stub + §8 reconciliation + §11 changelog)
  </read_first>

  <action>
Make THREE surgical edits to `design-system/MASTER.md`:

**Edit 1 — Replace §6 (current lines 660-693) with stub.** Locate the current §6 header `## 6. Motion (the pragmatic motion line)` and the boundary at the next `## 7. Accent usage` header. Replace EVERYTHING between (inclusive of `## 6.` heading, exclusive of `## 7.` heading) with this exact content:

```markdown
## 6. Motion (superseded by v1.2)

> **v1.1 motion lock superseded by v1.2.** See [`design-system/MOTION.md`](MOTION.md) for the v1.2 motion canonical doc — property whitelist, duration bands, easing defaults, per-animation specs, and the reduced-motion contract.

The §6.1 anti-pattern stance (no GSAP, no `<ClientRouter />`, no canvas hero, no scroll-trigger animations) survives in §8 below except where MOTION.md explicitly carves out an exception (cross-document `@view-transition` in MOTN-01 supersedes the previous blanket ban on `::view-transition-*` keyframes — restricted to root-level fade only; no morph, no named-element).

```

The replacement is exactly 5 content lines (header + blockquote + paragraph + blank) — well under the ≤12 line budget the test asserts. Do NOT preserve §6.1 / §6.2 / §6.3 / §6.4 subsections — they are explicitly rewritten away by D-02.

**Edit 2 — Amend §8 line 747** (the existing line):

`**No view transition `::view-transition-*` keyframes** — they were removed with `<ClientRouter />`. Do not re-add CSS for view transitions in any form.`

Replace with:

`**No view transition `::view-transition-*` keyframes except as authorized by MOTION.md** — Phase 8 removed the v1.0 keyframes alongside `<ClientRouter />`. v1.2's MOTN-01 reauthorizes a single use site: cross-document `@view-transition { navigation: auto; }` with `::view-transition-old(root)` and `::view-transition-new(root)` opacity-only fades. No morph, no named-element, no project↔project keyframes — the v1.3 named-element morph is a Future Requirement.`

This single-bullet amendment preserves the §8 anti-pattern format exactly. All other bullets in §8 stay byte-identical (GSAP ban, ClientRouter ban, canvas hero ban, etc.). Do NOT touch lines 736 (GSAP), 737 (ClientRouter), or any other §8 entry.

**Edit 3 — Append §11 Changelog entry.** Locate `## 11. Changelog` and append at the end of the section:

```markdown
- **v1.2 / Phase 16 (2026-04-XX):** §6 replaced with stub pointer to `design-system/MOTION.md` (the v1.2 motion canonical doc). §8 `::view-transition-*` keyframe ban amended to permit MOTION.md authorization (MOTN-01 cross-document fade). All other §8 anti-patterns survive intact (GSAP, `<ClientRouter />`, canvas hero, etc.). Non-motion sections (§2 colors, §3 typography, §4 layout, §5 primitives, §7 accent, §9 chat token map, §10 chat bubble exception) remain the v1.1 design lock.
```

Use the placeholder `2026-04-XX` for the date — the executor at run time will sub in the actual date. Date format: ISO-8601 (`2026-04-DD`). If the executor lands the change on 2026-04-29 the entry reads `2026-04-29`.

**Verification at commit time:**

1. Read `design-system/MASTER.md` after editing — confirm:
   - The §6 region (between `## 6.` and `## 7.`) is exactly 5-12 lines and contains the stub language.
   - The §8 view-transition bullet was amended (test will grep `MOTION.md` AND `view-transition` inside §8).
   - The §11 changelog has a new entry with `v1.2` and `Phase 16`.

2. Run `pnpm vitest run tests/build/motion-doc.test.ts -t "MASTER" --reporter=dot`. The 6 MASTER.md-specific tests should all be GREEN.

3. Run `git diff design-system/MASTER.md` and confirm:
   - §6 region replaced (large diff)
   - §8 single-line amendment (small diff — one bullet rewritten, all other bullets byte-identical)
   - §11 new entry appended (small diff)
   - No accidental edits elsewhere — every other section byte-identical
  </action>

  <verify>
    <automated>pnpm vitest run tests/build/motion-doc.test.ts --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'v1.1 motion lock superseded by v1.2' design-system/MASTER.md` returns 1
    - `grep -c 'design-system/MOTION.md\|(MOTION.md)' design-system/MASTER.md` returns >= 1 (link in §6 stub)
    - `awk '/^## 6\./,/^## 7\./' design-system/MASTER.md | wc -l` returns <= 12 (§6 stub line budget)
    - `awk '/^## 8\./,/^## 9\./' design-system/MASTER.md | grep -c 'No GSAP'` returns 1 (GSAP ban preserved)
    - `awk '/^## 8\./,/^## 9\./' design-system/MASTER.md | grep -c 'ClientRouter'` returns >= 1 (ClientRouter ban preserved)
    - `awk '/^## 8\./,/^## 9\./' design-system/MASTER.md | grep -cE 'MOTION\.md|MOTN-01'` returns >= 1 (view-transition reconciliation present)
    - `awk '/^## 8\./,/^## 9\./' design-system/MASTER.md | grep -c 'view-transition'` returns >= 1 (view-transition mention preserved with carve-out)
    - `awk '/^## 11\./,EOF' design-system/MASTER.md | grep -cE 'v1\.2.*Phase 16|Phase 16.*v1\.2'` returns >= 1 (changelog entry present)
    - `pnpm vitest run tests/build/motion-doc.test.ts --reporter=dot` exits 0 — all 13+ tests in motion-doc.test.ts GREEN
    - `git diff design-system/MASTER.md` shows changes ONLY in §6 (replaced), §8 (one bullet amended), §11 (one entry appended) — no accidental edits elsewhere
  </acceptance_criteria>

  <done>MASTER.md §6 replaced with ≤12-line stub pointing to MOTION.md. §8 view-transition bullet amended to permit MOTION.md / MOTN-01 carve-out (all other §8 bullets byte-identical). §11 Changelog appended with v1.2 / Phase 16 entry. tests/build/motion-doc.test.ts ALL GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → markdown source | Documentation files committed to git; static authoring; no untrusted input |
| markdown source → executor reading next phase | Plans 04-06 read MOTION.md / MASTER.md to understand contract; documentation is the source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering | MOTION.md content drift from UI-SPEC | mitigate | tests/build/motion-doc.test.ts asserts every duration / property / easing / motion ID present in source — drift fires CI |
| T-16-03-02 | Tampering | MASTER.md §8 silent removal of GSAP / ClientRouter ban | mitigate | tests/build/motion-doc.test.ts asserts GSAP + ClientRouter still in §8 — drift fires CI |
| T-16-03-03 | Information Disclosure | n/a | accept | Documentation contains no secrets, no PII; public design-system content |
| T-16-03-04 | Repudiation | git history | mitigate | Two atomic commits (MOTION.md creation, MASTER.md amendments); commit messages reference D-01 / D-02 |
| T-16-03-05 | Spoofing | n/a | accept | No auth surface |
| T-16-03-06 | Elevation of Privilege | n/a | accept | Documentation has no runtime / privilege surface |

ASVS L1: not applicable — this is documentation, not application surface.
</threat_model>

<verification>
After both tasks complete:
- `design-system/MOTION.md` exists with all required sections, tokens, motion IDs
- `design-system/MASTER.md` §6 replaced (≤12 lines), §8 amended (view-transition carve-out), §11 has v1.2 / Phase 16 changelog entry
- All MASTER.md non-motion sections (§2-§5, §7, §9, §10) byte-identical to pre-plan
- `tests/build/motion-doc.test.ts` ALL tests GREEN (was RED at end of Plan 01)
- `pnpm test` shows N more GREEN tests (the Plan 01 RED stubs in motion-doc.test.ts flipped GREEN)
- Zero source code changes; zero `package.json` changes; zero `src/**` edits
</verification>

<success_criteria>
- MOTN-09 closed in REQUIREMENTS.md (in 16-03-SUMMARY.md mark as complete)
- MOTION.md is the single authoritative motion contract for v1.2+
- MASTER.md remains the v1.1 design lock for non-motion concerns; the §6 stub explicitly points readers to MOTION.md
- §8 view-transition reconciliation explicitly captured (the "planner action item" called out in CONTEXT.md is honored)
- All tests/build/motion-doc.test.ts assertions GREEN
- Plan completes within ~25% context (~10-15 minutes — pure markdown authoring)
</success_criteria>

<output>
After completion, create `.planning/phases/16-motion-layer/16-03-SUMMARY.md` with:
- Files changed (1 new MOTION.md + 1 modified MASTER.md) and LOC delta per file
- §6 line count post-edit; §8 amended-bullet shape; §11 changelog entry shape
- tests/build/motion-doc.test.ts RED → GREEN flip count
- Pointer to next plan (16-04 motion.ts + global.css scroll-reveal/word-stagger/view-transition)
- MOTN-09 marked Complete in REQUIREMENTS.md
</output>
