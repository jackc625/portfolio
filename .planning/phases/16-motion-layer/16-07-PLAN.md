---
phase: 16
plan: 07
type: execute
wave: 4
depends_on:
  - "16-02"
  - "16-03"
  - "16-04"
  - "16-05"
  - "16-06"
files_modified:
  - .planning/phases/16-motion-layer/16-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - MOTN-10
tags:
  - phase-gate
  - lighthouse
  - d26-gate
  - close-out
  - phase-16

must_haves:
  truths:
    - "`pnpm test` full suite is 100% GREEN — every Phase 16 test plus every pre-existing test passes."
    - "`pnpm build` end-to-end is clean — `build:chat-context && wrangler types && astro check && astro build && pages-compat` all 11 routes prerender with zero errors / warnings."
    - "Lighthouse CI on homepage `localhost:4321/` passes the gate: Performance ≥ 99 / Accessibility ≥ 95 / Best Practices = 100 / SEO = 100 / TBT ≤ 150 ms / CLS ≤ 0.01."
    - "Lighthouse CI on one project detail page (`localhost:4321/projects/seatwatch`) passes the same gate."
    - "D-26 chat regression battery passes: tests/api/chat.test.ts + tests/api/security.test.ts + tests/api/prompt-injection.test.ts + tests/client/markdown.test.ts + tests/client/chat-copy-button.test.ts + tests/client/sse-truncation.test.ts + tests/client/focus-visible.test.ts + tests/client/chat-widget-header.test.ts ALL GREEN."
    - "`src/pages/api/chat.ts` is BYTE-IDENTICAL to pre-phase state (D-15 server byte-identical gate) — verified via `git diff <phase-start-sha> -- src/pages/api/chat.ts | wc -l` returns 0."
    - "Manual UAT for visual cadence: human verifies cross-document `@view-transition` fade, scroll-reveal, word-stagger, WorkRow arrow, chat bubble pulse + pause-on-open, chat panel scale-in, typing-dot bounce on `localhost:4321` (or Cloudflare Pages preview)."
    - "Manual UAT for reduced-motion contract: human enables `prefers-reduced-motion: reduce` via DevTools Rendering panel and verifies no entrance fades, no stagger, no scale-in, no view-transition fade — and the chat bubble pulse stops."
    - "16-VERIFICATION.md authored and committed."
    - "STATE.md + ROADMAP.md + REQUIREMENTS.md mark Phase 16 complete."
    - "`design-system/MOTION.md` and `design-system/MASTER.md` (§6 stub + §8 reconciliation + §11 changelog) committed."
    - "Zero new `package.json` runtime dependencies added across the entire phase — verified via `git diff <phase-start-sha> -- package.json pnpm-lock.yaml | grep -c 'dependencies\\|gsap\\|motion\\|lenis\\|@vueuse'` returns 0 for any new addition (existing deps preserved verbatim)."
  artifacts:
    - path: ".planning/phases/16-motion-layer/16-VERIFICATION.md"
      provides: "Final phase verification record — Lighthouse table, D-26 battery table, manual UAT checklist, byte-identical proofs"
      contains: "Performance ≥ 99"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MOTN-01..MOTN-10 marked Complete with phase plan provenance"
    - path: ".planning/STATE.md"
      provides: "Phase 16 marked complete; v1.2 milestone set to in_review or complete depending on user UAT pass"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16 status updated to Complete; Plans line shows 7/7"
  key_links:
    - from: ".planning/phases/16-motion-layer/16-VERIFICATION.md"
      to: "Lighthouse JSON output"
      via: "embedded scores table + JSON output snippet"
      pattern: "Performance.*99|99.*Performance"
    - from: ".planning/REQUIREMENTS.md"
      to: "16-XX-SUMMARY.md trail"
      via: "phase plan IDs in MOTN-XX rows"
      pattern: "16-0[1-7]"

---

<objective>
Close out Phase 16. Validate that the full motion layer ships within the locked budgets — Lighthouse 99/95/100/100, zero new runtime deps, D-26 chat regression preserved, reduced-motion contract honored — then update STATE.md / ROADMAP.md / REQUIREMENTS.md / 16-VERIFICATION.md and request user UAT signoff.

Purpose: This is the phase gate. Plans 02-06 produced verifiable artifacts; Plan 07 runs Lighthouse on a real headless Chrome (which jsdom cannot mock), captures the manual UAT checklist that the human must sign, and closes Phase 16 in the planning tree.

Output: 16-VERIFICATION.md (NEW) + 3 modified planning files (REQUIREMENTS.md, STATE.md, ROADMAP.md). Zero source code changes — this plan does not touch `src/`, `tests/`, or `design-system/`.

Why this plan is `autonomous: false`: it includes a `checkpoint:human-verify` task for visual UAT (cross-document fade, scroll-reveal cadence, word-stagger, chat bubble pulse) that requires a human to look at a real browser. Lighthouse CI itself is automated (Task 1) but the visual cadence verification (Task 2) is the manual gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-motion-layer/16-CONTEXT.md
@.planning/phases/16-motion-layer/16-RESEARCH.md
@.planning/phases/16-motion-layer/16-VALIDATION.md
@.planning/phases/16-motion-layer/16-UI-SPEC.md
@design-system/MOTION.md

<interfaces>
<!-- Reference shape for prior phase verification doc -->

From `.planning/phases/15-analytics-instrumentation/15-VERIFICATION.md` (the precedent — read for shape):
- Top-level title + phase identifier
- Status: pending / passed / failed
- Lighthouse table (homepage + one project page)
- D-26 chat regression table
- Manual UAT checklist
- Sign-off line

From `.planning/phases/16-motion-layer/16-VALIDATION.md` (the validation contract — Plan 07 fills the per-task verification map):
- Test infrastructure (Vitest 4.1.0 + jsdom 29.0.1)
- Sampling rate (`pnpm test` per wave; full suite + Lighthouse before /gsd-verify-work)
- Manual-only verifications table (Lighthouse, SSE streaming visual parity, view-transition fade, view-transition reduce neutralization)

From .planning/phases/16-motion-layer/16-UI-SPEC.md §"Lighthouse gate (MOTN-10)":
- Performance ≥ 99
- Accessibility ≥ 95
- Best Practices = 100
- SEO = 100
- TBT ≤ 150 ms
- CLS ≤ 0.01

From .planning/phases/16-motion-layer/16-VALIDATION.md §"Manual-Only Verifications":
| Behavior | Requirement | Test Instructions |
|----------|-------------|-------------------|
| Lighthouse CI scores hold | MOTN-10 | `npx lighthouse http://localhost:4321 --only-categories=performance,accessibility,best-practices,seo --output=json` PLUS `/projects/[id]` URL |
| SSE streaming visual parity | MOTN-04, MOTN-05, MOTN-06 | UAT: open chat, send message, observe pulse pauses on open, dots bounce during stream, panel scales in on open |
| Cross-document `@view-transition` fade with default motion preferences | MOTN-01 | UAT: home → /projects → /about navigation in Chrome 126+ / Safari 18.2+ — observe fade |
| Cross-document `@view-transition` neutralized with `prefers-reduced-motion: reduce` | MOTN-08 | UAT: DevTools → Rendering → emulate `reduce` → repeat navigation; verify no fade |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run Lighthouse CI + full test suite + D-26 battery; capture results into 16-VERIFICATION.md</name>
  <files>.planning/phases/16-motion-layer/16-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/15-analytics-instrumentation/15-VERIFICATION.md (the precedent — match its overall structure: status frontmatter, Lighthouse table, D-26 table, manual UAT checklist, sign-off line)
    - .planning/phases/16-motion-layer/16-VALIDATION.md (the validation contract — populate the Per-Task Verification Map with actual test results)
    - .planning/phases/16-motion-layer/16-UI-SPEC.md §"Lighthouse gate (MOTN-10)" + §"D-26 chat regression gate"
    - 16-01-SUMMARY.md, 16-02-SUMMARY.md, 16-03-SUMMARY.md, 16-04-SUMMARY.md, 16-05-SUMMARY.md, 16-06-SUMMARY.md (the phase plan summaries — quote LOC deltas, test count deltas, D-26 results)
  </read_first>

  <action>
**Sub-step 1: Start the dev server in the background.**

```bash
pnpm dev &  # or run-in-background
```

Wait for the server to report listening on `http://localhost:4321/`. Verify with `curl -s -o /dev/null -w "%{http_code}" http://localhost:4321/` returning 200.

**Sub-step 2: Run Lighthouse CI on homepage.**

```bash
npx --yes lighthouse http://localhost:4321/ \
  --only-categories=performance,accessibility,best-practices,seo \
  --output=json \
  --output-path=.planning/phases/16-motion-layer/lighthouse-home.json \
  --quiet \
  --chrome-flags="--headless --no-sandbox"
```

Capture the four category scores (Performance / Accessibility / Best Practices / SEO) plus TBT and CLS from the JSON output:

```bash
node -e "const j = require('./.planning/phases/16-motion-layer/lighthouse-home.json'); console.log({ p: j.categories.performance.score*100, a: j.categories.accessibility.score*100, bp: j.categories['best-practices'].score*100, seo: j.categories.seo.score*100, tbt: j.audits['total-blocking-time'].numericValue, cls: j.audits['cumulative-layout-shift'].numericValue });"
```

**Sub-step 3: Run Lighthouse CI on one project detail page.**

```bash
npx --yes lighthouse http://localhost:4321/projects/seatwatch \
  --only-categories=performance,accessibility,best-practices,seo \
  --output=json \
  --output-path=.planning/phases/16-motion-layer/lighthouse-project.json \
  --quiet \
  --chrome-flags="--headless --no-sandbox"
```

Capture scores the same way.

**Sub-step 4: Run the full test suite.**

```bash
pnpm test --reporter=dot 2>&1 | tail -20
```

Capture: total tests, passes, failures (must be 0 failures).

**Sub-step 5: Run the D-26 chat regression battery explicitly.**

```bash
pnpm vitest run \
  tests/api/chat.test.ts \
  tests/api/security.test.ts \
  tests/api/prompt-injection.test.ts \
  tests/client/markdown.test.ts \
  tests/client/chat-copy-button.test.ts \
  tests/client/sse-truncation.test.ts \
  tests/client/focus-visible.test.ts \
  tests/client/chat-widget-header.test.ts \
  --reporter=dot 2>&1 | tail -15
```

Capture: each file's pass/fail count.

**Sub-step 6: Verify D-15 server byte-identical.**

```bash
git log --oneline -1 src/pages/api/chat.ts
```

Expected: the most recent commit touching `src/pages/api/chat.ts` is from a phase BEFORE Phase 16 (e.g., Phase 14 or 15). If a Phase 16 commit appears in the log, the gate is broken.

Also:
```bash
git diff <phase-start-commit> HEAD -- src/pages/api/chat.ts | wc -l
```
(Where `<phase-start-commit>` is the commit just BEFORE the Phase 16 work began — typically the most recent commit on `main` before any 16-XX commits.) Expected: `0`.

**Sub-step 7: Verify zero new dependencies.**

```bash
git diff <phase-start-commit> HEAD -- package.json pnpm-lock.yaml
```

Expected: `0` lines (zero new runtime deps, zero new devDeps; zero `pnpm install` runs across the phase).

If any line of diff exists, report it in 16-VERIFICATION.md as a deviation. Per CONTEXT.md / RESEARCH.md zero-new-deps preferred path, this is a phase failure unless explicitly approved.

**Sub-step 8: Stop the dev server.**

```bash
kill %1  # or pkill -f "astro dev"
```

**Sub-step 9: Write 16-VERIFICATION.md.** Use this exact frontmatter and structure:

```markdown
---
phase: 16
slug: motion-layer
status: pending  # toggle to "passed" after Task 2 human-verify completes
gate: phase-16-final
created: 2026-04-XX
---

# Phase 16 — Verification Record

> Final-phase gate evidence for the v1.2 Motion Layer. Lighthouse on real headless Chrome (cannot be jsdom-mocked); D-26 chat regression battery; full test suite; D-15 server byte-identical; zero new runtime deps; manual UAT checklist for the visual motion cadence and reduced-motion neutralization.

---

## 1. Lighthouse CI (MOTN-10)

**Command:** `npx lighthouse <URL> --only-categories=performance,accessibility,best-practices,seo --output=json --chrome-flags="--headless --no-sandbox"`

| URL | Performance | Accessibility | Best Practices | SEO | TBT (ms) | CLS | Gate |
|-----|-------------|---------------|----------------|-----|----------|-----|------|
| `http://localhost:4321/` | <NN> | <NN> | <NN> | <NN> | <NN> | <NN> | <PASS / FAIL> |
| `http://localhost:4321/projects/seatwatch` | <NN> | <NN> | <NN> | <NN> | <NN> | <NN> | <PASS / FAIL> |

**Gate thresholds (per UI-SPEC § Lighthouse gate):**
- Performance ≥ 99
- Accessibility ≥ 95
- Best Practices = 100
- SEO = 100
- TBT ≤ 150 ms
- CLS ≤ 0.01

JSON outputs: `.planning/phases/16-motion-layer/lighthouse-home.json` and `.planning/phases/16-motion-layer/lighthouse-project.json`.

---

## 2. D-26 Chat Regression Battery

| Test File | Result | Test Count |
|-----------|--------|------------|
| tests/api/chat.test.ts | <PASS / FAIL> | <N>/<N> |
| tests/api/security.test.ts | <PASS / FAIL> | <N>/<N> |
| tests/api/prompt-injection.test.ts | <PASS / FAIL> | <N>/<N> |
| tests/client/markdown.test.ts | <PASS / FAIL> | <N>/<N> |
| tests/client/chat-copy-button.test.ts | <PASS / FAIL> | <N>/<N> |
| tests/client/sse-truncation.test.ts | <PASS / FAIL> | <N>/<N> |
| tests/client/focus-visible.test.ts | <PASS / FAIL> | <N>/<N> |
| tests/client/chat-widget-header.test.ts | <PASS / FAIL> | <N>/<N> |

**D-26 invariants verified (pass requires ALL GREEN above):**
- XSS sanitization (DOMPurify strict whitelist on bot markdown)
- Exact-origin CORS allow-list
- 5/60s rate limit (Cloudflare rate-limiter binding path; see 14-VERIFICATION.md note)
- 30s AbortController timeout
- Focus-trap re-query on every Tab keypress (dynamic re-query)
- localStorage persistence (50 msg cap / 24h TTL)
- SSE streaming with `async:false` marked parse
- Markdown rendering via DOMPurify
- Clipboard idempotency (JS boolean + DOM data-attribute)

---

## 3. Full Test Suite

**Command:** `pnpm test --reporter=dot`

| Metric | Value |
|--------|-------|
| Total tests | <N> |
| Passed | <N> |
| Failed | <N> |
| Skipped | <N> |
| Duration | <N>s |

Pre-Phase-16 baseline: 262 GREEN. Post-Phase-16 expected: ~290+ GREEN (+~30 motion-related tests).

---

## 4. D-15 Server Byte-Identical Gate

**Command:** `git diff <phase-start-commit> HEAD -- src/pages/api/chat.ts | wc -l`

| Check | Result |
|-------|--------|
| `src/pages/api/chat.ts` diff vs phase start | <N> lines |
| Expected | 0 |
| Gate | <PASS / FAIL> |

---

## 5. Zero New Runtime Dependencies Gate

**Command:** `git diff <phase-start-commit> HEAD -- package.json pnpm-lock.yaml | wc -l`

| Check | Result |
|-------|--------|
| `package.json` diff vs phase start | <N> lines |
| `pnpm-lock.yaml` diff vs phase start | <N> lines |
| Combined | <N> lines |
| Expected | 0 (zero new deps, zero new devDeps, zero `pnpm install` runs) |
| Gate | <PASS / FAIL> |

---

## 6. Manual UAT (filled by Task 2 human-verify)

| # | Behavior | Steps | Result |
|---|----------|-------|--------|
| 1 | Cross-document `@view-transition` fade (MOTN-01) | Default motion prefs in Chrome 126+; navigate `/` → `/projects` → `/about`; observe brief opacity fade | <PASS / FAIL> |
| 2 | Scroll-reveal on home / about / project detail (MOTN-02) | Scroll down on `/about`; section header + paragraphs fade-up as they enter the lower 90% viewport | <PASS / FAIL> |
| 3 | `.h1-section` word-stagger (MOTN-07) | Reload `/about`; section header words appear left-to-right with ~60ms stagger | <PASS / FAIL> |
| 4 | `.display` UNTOUCHED on homepage (D-08 / MOTN-07 exclusion) | Reload `/`; the giant Jack Cutrara wordmark renders instantly with no fade, no stagger | <PASS / FAIL> |
| 5 | WorkRow arrow slide-in (MOTN-03) | Hover or focus a row in the home page work list; the `→` arrow fades in AND translates 4px right | <PASS / FAIL> |
| 6 | Chat bubble pulse — alive but quiet (MOTN-04) | Land on any page; observe 2.5s ring-expand pulse loop on the bubble bottom-right | <PASS / FAIL> |
| 7 | Pulse pauses on hover/focus (D-15) | Hover the bubble → pulse stops; un-hover → pulse resumes | <PASS / FAIL> |
| 8 | Pulse pauses while panel open (D-15) | Click the bubble to open the panel → pulse stops; close → pulse resumes after focus restoration | <PASS / FAIL> |
| 9 | Chat panel scale-in (MOTN-05) | Click the bubble → panel scales 96% → 100% from bottom-right corner with opacity fade-in | <PASS / FAIL> |
| 10 | Typing-dot bounce during SSE (MOTN-06) | Open chat, send a message; while bot is responding, observe 3-dot bounce | <PASS / FAIL> |
| 11 | **Reduced-motion** — entrance animations off (MOTN-08) | DevTools → Rendering panel → emulate `prefers-reduced-motion: reduce`; reload `/about`; section reveals immediately, words don't stagger, panel doesn't scale, page-enter doesn't fade | <PASS / FAIL> |
| 12 | **Reduced-motion** — chat bubble pulse off (MOTN-08 / D-24) | With reduce emulated, observe the chat bubble — pulse animation is OFF (no ring, no scale, no breathing) | <PASS / FAIL> |
| 13 | **Reduced-motion** — typing-dot bounce off (MOTN-06 parity, optional) | With reduce emulated, send a message in chat; the typing-dot bounce is paused/off | <PASS / FAIL> |

**Browser tested:** <Chrome NNN.NNN.NNN.NNN / Safari NN.N> on <macOS / Windows / Linux>

**UAT signoff:** _Pending — Jack to verify locally._

---

## 7. Phase Sign-Off

- [ ] Lighthouse gate held on both URLs (Section 1)
- [ ] D-26 chat regression battery ALL GREEN (Section 2)
- [ ] Full test suite GREEN (Section 3)
- [ ] D-15 server byte-identical (Section 4)
- [ ] Zero new runtime deps (Section 5)
- [ ] Manual UAT 13 / 13 PASS (Section 6)

When all 6 boxes are checked and the user signs off, set `status: passed` in this file's frontmatter and commit. Then update STATE.md / ROADMAP.md / REQUIREMENTS.md (Task 3).
```

Substitute the bracketed values from the actual sub-step output. Use `<NN>` placeholders ONLY for the manual UAT section that Task 2 fills.

Commit `16-VERIFICATION.md` + the two Lighthouse JSON outputs:

```bash
git add .planning/phases/16-motion-layer/16-VERIFICATION.md .planning/phases/16-motion-layer/lighthouse-home.json .planning/phases/16-motion-layer/lighthouse-project.json
git commit -m "docs(16): phase 16 verification scaffold + Lighthouse + D-26 battery results

🤖 Generated with [Claude Code](https://claude.com/claude-code)
"
```
  </action>

  <verify>
    <automated>pnpm test --reporter=dot && test -f .planning/phases/16-motion-layer/16-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/16-motion-layer/16-VERIFICATION.md` exists
    - File `.planning/phases/16-motion-layer/lighthouse-home.json` exists with non-zero size
    - File `.planning/phases/16-motion-layer/lighthouse-project.json` exists with non-zero size
    - Lighthouse home Performance score is >= 99 (extracted from JSON)
    - Lighthouse home Accessibility score is >= 95
    - Lighthouse home Best Practices score is 100
    - Lighthouse home SEO score is 100
    - Lighthouse home TBT is <= 150 ms
    - Lighthouse home CLS is <= 0.01
    - Same gates hold for the project detail URL
    - `pnpm test --reporter=dot` total failures == 0
    - D-26 battery: each of the 8 chat-related test files reports 0 failures
    - `git diff $(git log --format=%H .planning/phases/16-motion-layer/16-CONTEXT.md | tail -1)~1 HEAD -- src/pages/api/chat.ts | wc -l` returns 0 (D-15 server byte-identical from phase start)
    - `git diff $(git log --format=%H .planning/phases/16-motion-layer/16-CONTEXT.md | tail -1)~1 HEAD -- package.json pnpm-lock.yaml | wc -l` returns 0 (zero new deps)
    - 16-VERIFICATION.md contains all 7 numbered sections (Lighthouse / D-26 / Full Suite / D-15 / Zero Deps / Manual UAT / Sign-Off)
    - 16-VERIFICATION.md contains the 13-row Manual UAT checklist with all rows in pending state
    - Frontmatter `status: pending` (Task 2 will toggle to `passed` after UAT)
    - `pnpm check` 0/0/0
  </acceptance_criteria>

  <done>16-VERIFICATION.md authored with all automated gates filled (Lighthouse + D-26 + full suite + D-15 + zero-deps); manual UAT section pending Task 2; 2 Lighthouse JSON files committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — visual cadence verification (motion + reduced-motion)</name>

  <what-built>
    Phase 16 ships the v1.2 Motion Layer:
    - Cross-document `@view-transition` page-enter fade (MOTN-01)
    - IntersectionObserver scroll-reveal of section headers + work rows + project prose + about paragraphs (MOTN-02)
    - WorkRow arrow opacity + 4px translateX slide-in on hover/focus (MOTN-03)
    - Chat bubble idle pulse with hover/focus/panel-open pause + reduce override (MOTN-04)
    - Chat panel scale-in on open (MOTN-05)
    - Typing-dot bounce during SSE streaming (MOTN-06 — already live, +reduce parity)
    - `.h1-section` word-stagger via JS span-wrap + CSS animation-delay (MOTN-07; `.display` excluded)
    - All entrances inside `@media (prefers-reduced-motion: no-preference)`; chat pulse paired with explicit reduce override (MOTN-08)

    Lighthouse passed automated gate. D-26 chat regression battery passed. D-15 server byte-identical. Zero new runtime deps.

    Pending: visual cadence verification by a human watching real browser navigation + DevTools reduced-motion emulation.
  </what-built>

  <how-to-verify>
    **Setup (5 minutes):**

    1. Run `pnpm dev` — confirm `http://localhost:4321/` returns 200
    2. Open Chrome 126+ or Safari 18.2+ at `http://localhost:4321/`
    3. Open DevTools (F12) — keep the Rendering panel handy (`Cmd/Ctrl+Shift+P` → "Show Rendering")

    **UAT 1 — Page-enter fade (MOTN-01):**
    - From `/`, click any nav link (`/projects` or `/about`)
    - Expected: brief 200ms opacity fade between pages — NOT a hard cut, NOT a long crossfade
    - Repeat in both directions; verify cadence is consistent

    **UAT 2 — Scroll-reveal (MOTN-02):**
    - Navigate to `/about`
    - Slowly scroll down — expected: each paragraph fades in + lifts 12px as it enters the lower 90% of the viewport
    - Section header `01 ABOUT` reveals once when you arrive at it (not at the moment any pixel is visible)
    - Cadence ~300ms ease-out per element

    **UAT 3 — Word-stagger (MOTN-07):**
    - Reload `/about`
    - Watch the `01 ABOUT` section header — expected: words appear left-to-right with ~60ms stagger between them
    - Verify: stagger fires ONCE per page load (not on every scroll)

    **UAT 4 — `.display` UNTOUCHED (D-08 / MOTN-07 exclusion):**
    - Reload `/`
    - Watch the giant `JACK CUTRARA` display wordmark — expected: renders INSTANTLY with no fade, no stagger
    - This is the success criterion #1 lock — the homepage hero stays untouched

    **UAT 5 — WorkRow arrow (MOTN-03):**
    - On `/`, hover any row in the work list (e.g., `01 SeatWatch — ...`)
    - Expected: `→` arrow fades from invisible to visible AND translates 4px to the right
    - Tab into the row via keyboard — same effect under `:focus-visible`
    - Verify: title underline (1.5px accent) ALSO appears on hover/focus (must coexist with arrow)

    **UAT 6 — Chat bubble pulse (MOTN-04):**
    - On any page, observe the chat bubble at bottom-right
    - Expected: ~2.5s loop — soft accent ring expands outward + bubble subtly scales 1 → 1.02 → 1
    - "Alive but quiet" — not flashy, not demanding

    **UAT 7 — Pulse pauses on hover/focus (D-15):**
    - Hover the bubble → pulse should freeze where it is
    - Move cursor away → pulse resumes from where it paused (not restart)
    - Tab into the bubble → pulse pauses; Tab away → resumes

    **UAT 8 — Pulse pauses on panel open (D-15):**
    - Click the bubble to open the panel — pulse stops
    - Close panel (× button or Escape) — focus returns to bubble; pulse resumes ~200ms after focus restoration
    - Verify focus ring on bubble (red 2px outline) appears AT the same instant pulse resumes

    **UAT 9 — Chat panel scale-in (MOTN-05):**
    - Watch the panel-open animation: panel scales from 96% → 100% AND opacity 0 → 1, anchored at bottom-right
    - Cadence: 180ms ease-out
    - Close + reopen to verify

    **UAT 10 — Typing-dot bounce (MOTN-06):**
    - In the chat panel, type any short message and press Enter
    - During the SSE response (before tokens arrive), expected: 3 dots bouncing in sequence (150ms phase shift each)
    - Dots disappear when first token arrives

    **UAT 11 — Reduced-motion: entrances OFF (MOTN-08):**
    - In DevTools Rendering panel, set "Emulate CSS media feature prefers-reduced-motion" to "reduce"
    - Reload `/about`
    - Expected: section headers + paragraphs render INSTANTLY at full opacity — NO fade, NO stagger, NO 12px lift
    - Navigate `/about` → `/projects` → `/` — verify NO page-enter fade (`@view-transition` neutralized)
    - Verify: word-stagger DOES NOT FIRE — section headers render as plain text (no `<span class="word">` children visible in DOM inspector)

    **UAT 12 — Reduced-motion: chat pulse OFF (MOTN-04 / D-24):**
    - With `reduce` still emulated, observe chat bubble — expected: NO pulse, NO ring, NO scale
    - Bubble renders at its resting state (red 48×48 round, no animation)

    **UAT 13 — Reduced-motion: typing-dot OFF (MOTN-06 parity, optional):**
    - With `reduce` still emulated, open chat and send a message
    - Observe: typing-dot bounce is OFF or paused (3 dots render but don't move)

    **Reset:** disable the prefers-reduced-motion emulation in DevTools when done.

    **Failure modes that should fire FAIL:**
    - Page-enter fade is missing entirely under default motion preferences (likely browser version too old — Chrome <126 / Safari <18.2 / Firefox <144)
    - Word-stagger fires on `.display` (homepage hero animates — that's a regression of D-08)
    - Chat bubble pulse keeps animating with `reduce` emulated (the paired override at `@media (prefers-reduced-motion: reduce) { #chat-bubble { animation: none; } }` is missing or below the unconditional rule)
    - Hover state shows arrow but NOT the title underline — an accent reservation list regression
    - Pulse keeps animating while panel is open — D-15 ordering bug

    **For each row of the 13-row UAT table in `.planning/phases/16-motion-layer/16-VERIFICATION.md` Section 6:** mark PASS or FAIL with a one-line note if FAIL.

    **After all 13 rows are scored:** if all PASS, set frontmatter `status: passed` in 16-VERIFICATION.md AND check the 6 sign-off boxes in Section 7. If ANY FAIL, leave status as `pending`, file the issue in 16-VERIFICATION.md Section 6, and decide whether to fix in-place (return to the relevant Plan 04/05/06 + re-execute) or open a gap-closure plan via `/gsd-plan-phase 16 --gaps`.
  </how-to-verify>

  <resume-signal>
    Type `approved` if all 13 UAT rows PASS. Otherwise, type `failed: <one-line summary>` and decide on gap-closure path with the orchestrator.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md + ROADMAP.md + REQUIREMENTS.md to mark Phase 16 complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/STATE.md (the YAML frontmatter `progress` block + the per-phase decisions section)
    - .planning/ROADMAP.md (the §"Progress" table + the Phase 16 row + the §"## Phases" Phase 16 entry)
    - .planning/REQUIREMENTS.md (the v1.2 Requirements block, MOTN-01..MOTN-10 rows + the Traceability table at the bottom)
    - 16-VERIFICATION.md (Task 1 + 2 results — copy the test count, Lighthouse scores, UAT 13/13 status)
    - 16-01..16-06 SUMMARY.md files (the per-plan provenance to cite in REQUIREMENTS.md MOTN-XX row updates)
  </read_first>

  <action>
**Edit 1 — REQUIREMENTS.md:** Mark MOTN-01..MOTN-10 as Complete with phase plan provenance. Open `.planning/REQUIREMENTS.md` and update each MOTN-XX line in the §Motion (MOTN) section AND the §Traceability table:

- MOTN-01: `[x] **MOTN-01**: Page-enter fade via native cross-document `@view-transition` CSS at-rule (no ClientRouter, no JS router) — Complete (16-04)`
- MOTN-02: `[x] **MOTN-02**: Scroll-reveal utility module ... — Complete (16-04)`
- MOTN-03: `[x] **MOTN-03**: WorkRow arrow slide-in ... — Complete (16-06)`
- MOTN-04: `[x] **MOTN-04**: Chat bubble idle pulse ... — Complete (16-05)`
- MOTN-05: `[x] **MOTN-05**: Chat panel open scale-in ... — Complete (16-05)`
- MOTN-06: `[x] **MOTN-06**: Typing-dot bounce during SSE streaming ... — Complete (already live; reduce override added in 16-05)`
- MOTN-07: `[x] **MOTN-07**: Section heading word-stagger ... — Complete (16-04)`
- MOTN-08: `[x] **MOTN-08**: All motion wrapped ... — Complete (16-04 + 16-05 + 16-06)`
- MOTN-09: `[x] **MOTN-09**: MASTER.md §5/§6 amended ... — Complete (16-03)`
- MOTN-10: `[x] **MOTN-10**: Lighthouse gate passes ... — Complete (16-07; verified at 16-VERIFICATION.md §1)`

In the Traceability table at the bottom, change all 10 MOTN-XX rows' Status column from `Pending` to `Complete (16-XX)` matching the plan IDs above.

Update the §"Coverage" line at the bottom: total v1.2 requirements = 36 / 36 mapped, 36 / 36 complete (was 26 / 36).

**Edit 2 — ROADMAP.md:** Update Phase 16 status to Complete in three places:

1. The Phase listing under §"### 🚧 v1.2 Polish (In Progress)" — change `- [ ] **Phase 16: Motion Layer**` to `- [x] **Phase 16: Motion Layer** — completed 2026-04-XX` (substitute actual date)
2. The Phase 16 details under §"### Phase 16: Motion Layer" — append after the "Plans: TBD" line, replace `Plans: TBD` with `Plans: 7 plans` and list:
   ```
   - [x] 16-01-PLAN.md — Wave 0 RED test stubs (8 test files)
   - [x] 16-02-PLAN.md — observer factory + scroll-depth refactor (D-17, D-19)
   - [x] 16-03-PLAN.md — design-system/MOTION.md + MASTER.md §6 stub + §8 reconciliation (MOTN-09)
   - [x] 16-04-PLAN.md — motion.ts + global.css scroll-reveal/word-stagger/view-transition (MOTN-01, MOTN-02, MOTN-07, MOTN-08)
   - [x] 16-05-PLAN.md — chat.ts D-15 ordering + global.css chat-pulse + chat-panel scale-in + typing-dot reduce parity (MOTN-04, MOTN-05, MOTN-06; D-26 gate)
   - [x] 16-06-PLAN.md — WorkRow arrow upgrade (MOTN-03)
   - [x] 16-07-PLAN.md — phase gate: Lighthouse CI + D-26 + final test sweep + close-out (MOTN-10)
   ```
3. The §"## Progress" table at the bottom: change Phase 16 row from `0/TBD ... Not started` to `7/7 Complete 2026-04-XX`

**Edit 3 — STATE.md:** Update the YAML frontmatter and Current Position narrative:

In frontmatter:
```yaml
gsd_state_version: 1.0
milestone: v1.2
milestone_name: Polish
status: complete  # was in_progress
stopped_at: "Phase 16 complete — Motion Layer shipped. Lighthouse 99/95/100/100 held on home + /projects/seatwatch. D-26 chat regression battery ALL GREEN. D-15 server byte-identical (api/chat.ts diff = 0). Zero new runtime deps. Manual UAT 13/13 PASS. v1.2 milestone CLOSED — 36/36 requirements complete across Phases 12-16."
last_updated: "2026-04-XXTHH:MM:SSZ"
last_activity: "2026-04-XX -- Phase 16 closed; v1.2 milestone complete; ready for milestone retrospective"
progress:
  total_phases: 5
  completed_phases: 5  # was 4
  total_plans: 34      # was 27 (27 + 7 Phase 16 plans)
  completed_plans: 34  # was 27
  percent: 100
```

In the Current Position narrative section, append a new "Phase 16 decisions" block summarizing key takeaways (mirror Phase 15 decisions block style — terse, decision-grade observations like "Phase 16-01: Wave 0 RED stubs landed 30+ failing tests across 8 files; baseline GREEN tests for typing-bounce byte-equiv + Phase 7 display-toggle invariants documented per Phase 14-05 plan-text-mismatch precedent"). Each plan gets ~3-7 bullet points capturing surprises, decisions made, and gate results.

**Verification:**

```bash
pnpm check
pnpm test --reporter=dot
```
ALL GREEN — no regressions from documentation edits.

Commit:
```bash
git add .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
git commit -m "docs(16): close phase 16 — Motion Layer shipped, v1.2 milestone complete

🤖 Generated with [Claude Code](https://claude.com/claude-code)
"
```
  </action>

  <verify>
    <automated>pnpm test --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '\[x\] \*\*MOTN-' .planning/REQUIREMENTS.md` returns 10 (all 10 motion requirements complete)
    - `grep -c 'Pending' .planning/REQUIREMENTS.md` (within MOTN rows of Traceability table) returns 0 — verify with: `awk '/MOTN-/' .planning/REQUIREMENTS.md | grep -c Pending` returns 0
    - `grep -c 'Phase 16: Motion Layer\] — completed\|Phase 16.*completed 2026' .planning/ROADMAP.md` returns >= 1 (status updated)
    - `grep -c '7/7' .planning/ROADMAP.md` returns >= 1 (Phase 16 plans complete)
    - STATE.md frontmatter `status: complete` (`grep -m1 'status:' .planning/STATE.md` returns line containing "complete")
    - STATE.md `progress.total_plans: 34` and `completed_plans: 34`
    - `pnpm check` 0 errors / 0 warnings / 0 hints
    - `pnpm test --reporter=dot` ALL GREEN (regression check after documentation edits — no test files modified, but verifying no unexpected interaction)
    - 16-VERIFICATION.md frontmatter `status: passed` (Task 2 toggled this — verify with `grep 'status: passed' .planning/phases/16-motion-layer/16-VERIFICATION.md` returns 1 line)
  </acceptance_criteria>

  <done>STATE.md / ROADMAP.md / REQUIREMENTS.md all reflect Phase 16 complete. v1.2 milestone closed at 36/36 requirements and 34/34 plans. 16-VERIFICATION.md status: passed. Final commit lands on the phase work tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lighthouse CLI → headless Chrome | Local-only test execution; no production deployment in this plan |
| filesystem → planning files | Documentation-only edits; no source code changes |
| user → manual UAT sign-off | Human-in-the-loop gate; cannot be automated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07-01 | Tampering | Lighthouse JSON tampered to fake passing scores | accept | Local execution; output committed to git for audit; if score drift suspected, re-run; the gate is repeatable |
| T-16-07-02 | Tampering | Manual UAT sign-off without actually verifying | mitigate | The 13-row UAT table is granular — each row is independently verifiable; signoff comments document specific behavior observed |
| T-16-07-03 | Information Disclosure | Lighthouse JSON contains URLs / paths | accept | All URLs are localhost or public production paths; no secrets in artifact |
| T-16-07-04 | Denial-of-Service | Dev server fails to start during Lighthouse run | mitigate | Sub-step 1 verifies server returns 200 before proceeding; if fails, abort and investigate |
| T-16-07-05 | Repudiation | git history | mitigate | Two atomic commits (Task 1 verification scaffold + Task 3 close-out); commit messages reference MOTN-10 phase gate |
| T-16-07-06 | Spoofing | n/a | accept | No auth surface |
| T-16-07-07 | Elevation of Privilege | n/a | accept | No privilege boundaries |
| T-16-07-08 | D-26 chat regression at phase close | full Phase 7 surface | mitigate | Task 1 sub-step 5 explicitly runs the 8-file D-26 battery; phase cannot close if any of those tests fails |

ASVS L1: not directly applicable (this is the verification gate, not application surface). The gate enforces that the Phase 7 ASVS L1 controls (CSP via DOMPurify, CORS, rate limit, input validation) remain in place across Phase 16.
</threat_model>

<verification>
After all 3 tasks complete:
- 16-VERIFICATION.md exists with all 7 sections filled (Lighthouse scores + D-26 battery + full suite + D-15 + zero deps + manual UAT 13/13 + sign-off)
- Lighthouse JSON outputs committed
- 16-VERIFICATION.md status: passed (Task 2 toggle)
- STATE.md milestone v1.2 status: complete
- ROADMAP.md Phase 16 marked complete with 7/7 plans
- REQUIREMENTS.md MOTN-01..MOTN-10 all marked Complete
- Final test suite GREEN; pnpm build clean; pnpm check 0/0/0
- Manual UAT signed off by Jack
</verification>

<success_criteria>
- MOTN-10 (Lighthouse gate) closed at the phase boundary
- v1.2 milestone closed (36/36 requirements complete; 34/34 plans complete)
- D-26 chat regression gate held across the entire phase (verified end-to-end)
- D-15 server byte-identical gate held (api/chat.ts unchanged)
- Zero new runtime dependencies (CONTEXT.md / RESEARCH.md preferred path honored)
- Plan completes within ~30% context (~10-15 minutes for Task 1, ~30-60 minutes human time for Task 2 manual UAT, ~5 minutes for Task 3)
</success_criteria>

<output>
After completion, create `.planning/phases/16-motion-layer/16-07-SUMMARY.md` with:
- Lighthouse scores (home + project page) with PASS/FAIL gate
- D-26 chat regression battery test count summary
- Full test suite count (262 baseline → ~290+ post-Phase-16)
- D-15 server byte-identical confirmation
- Zero-deps confirmation
- Manual UAT 13/13 result
- v1.2 milestone closure note (36/36 requirements; 34/34 plans)
- Pointer to next milestone (v1.3 — deferred to user direction)
</output>
