---
phase: 24-positioning-shift-home-teaser
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/pages/index.astro
  - src/components/ContactSection.astro
autonomous: true
requirements: [HOME-01, POS-01, POS-04]
must_haves:
  truths:
    - "The Home page opens with a concise Holloway EXPERIENCE teaser (role/company/dates + one metric + a link to /experience) for the 30-second recruiter scan (HOME-01, D-01..D-05)"
    - "Home section numbers read 01 EXPERIENCE / 02 WORK / 03 ABOUT / 04 CONTACT (D-01)"
    - "The Home JSON-LD Person schema carries jobTitle + alumniOf + hasCredential and the SEO description signals production/contract experience (POS-04, D-14, D-15)"
    - "The Home hero lead ('Software engineer building reliable, production-grade systems.') stays UNCHANGED (D-08), and the sharpened Home description= (D-15) is a DISTINCT string from it"
  artifacts:
    - src/pages/index.astro
    - src/components/ContactSection.astro
  key_links:
    - "index.astro imports alumniOfSchema + hasCredentialSchema from src/data/education.ts (24-01) and spreads them into personSchema rendered via JsonLd.astro"
    - "The teaser reuses getCollection('experience') + sortExperienceEntries + the hasCaseStudy Holloway entry — no data duplication (D-05)"
    - "The CONTACT §-number lives as a hardcoded literal in ContactSection.astro:28, NOT a SectionHeader prop (RESEARCH Pitfall 3)"
---

<objective>
Rebuild the Home page for the positioning shift: add the new `01 EXPERIENCE` Holloway teaser placed FIRST (HOME-01), renumber the existing section headers, enrich the JSON-LD Person schema, and sharpen the SEO description — then fix the CONTACT §-number literal so the whole Home sequence reads 01/02/03/04.

Purpose: put the strongest new-grad credibility signal (shipped production experience) at the top of the 30-second scan and make the metadata reflect the software-engineer-with-production-experience positioning (POS-01, POS-04). This plan drives Gates B + C from RED to GREEN.

Output: a modified `src/pages/index.astro` (teaser section + renumbered WORK/ABOUT headers + enriched `personSchema` + sharpened `description`) and a one-line renumber in `src/components/ContactSection.astro`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md
@.planning/phases/24-positioning-shift-home-teaser/24-PATTERNS.md
@.planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md

# The teaser structure + reduced-motion + query-guard analogs
@src/pages/experience.astro
@src/content/experience/holloway.mdx
@src/components/JsonLd.astro
@design-system/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design + build the 01 EXPERIENCE Home teaser (frontend-design routed, D-20)</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md §"Teaser internal structure" (lines 72-86) and §Copywriting Contract (the teaser summary + metric drafts, lines 190-196) and §Motion (line 227)
    - design-system/MASTER.md (six-token palette, type-role classes, page-scoped CSS, §7 accent-signal rule, restrained-motion contract) — the D-20 contract the frontend-design skill validates against
    - src/pages/experience.astro (lines 6, 10-40 = collection query + hasCaseStudy find guard to reuse verbatim with an index.astro throw message; lines 51-69 = the `.featured` article structure to mirror trimmed; lines 141-169 + 222-233 = the `.deep-link` reduced-motion contract)
    - src/pages/index.astro (lines 2-16 frontmatter imports; lines 72-75 + 114-129 = the in-file `.see-all-work` deep-link idiom + reduced-motion CSS to clone under a teaser-distinct selector)
    - src/content/experience/holloway.mdx (frontmatter role/company/dateRange/summary + highlights[0] the teaser draws from)
  </read_first>
  <action>
    First route the teaser's visual form through the frontend-design skill against design-system/MASTER.md and the UI-SPEC teaser table — confirm the compact `.featured` mirror (eyebrow / title / summary / ONE highlight / deep-link), the six-token palette, the accent reserved only for the link (MASTER §7), and the reduced-motion contract before writing CSS. Then in src/pages/index.astro add a new `<section>` placed FIRST (above the existing WORK section), labelled with `<SectionHeader number="01" title="EXPERIENCE" id="section-experience" />` and NO count prop. In the frontmatter, reuse the experience collection query: import sortExperienceEntries from ../lib/experience, run `sortExperienceEntries(await getCollection("experience"))`, find the entry where hasCaseStudy is true, and keep the strict-TS throw guard (throw with an index.astro-prefixed message if the Holloway entry is absent) — do NOT inline a `.sort()` (sortExperienceEntries orders by startDate desc; anti-pattern per RESEARCH line 202). Render the teaser article as a trimmed clone of experience.astro's `.featured`: eyebrow `{role} · {dateRange}` (.meta-mono, --ink-muted), title `{company}` (.h2-project, --ink), OMIT the tech-stack line (D-05), summary (.lead, --ink) using the UI-SPEC teaser sentence draft, and exactly ONE metric highlight (.body) carrying the `1,400` test-suite-growth metric (D-02/D-03) — either holloway.data.highlights[0] or the trimmed UI-SPEC draft; both are covered by Gate A. Add the link `See the experience →` targeting `href="/experience"` (the listing, D-04), cloning the in-file `.see-all-work` markup + CSS under a teaser-distinct selector (e.g. `.experience-link` / `.experience-arrow`) so it does not collide with the WORK section's `.see-all-work`; keep the color-change affordance + arrow-opacity reveal + focus ring `outline: 2px solid var(--accent); outline-offset: 2px` and the `prefers-reduced-motion` override that drops the reveal but keeps the color change. All teaser CSS is page-scoped in index.astro's `<style>` — do NOT touch global.css or BaseLayout.astro (D-19). Keep every teaser string em-dash-free (U+2014); en dash permitted (the reused dateRange is `May 2026 – Present`).
  </action>
  <verify>
    <automated>pnpm build && pnpm exec vitest run tests/build/home-teaser-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The built dist/client/index.html renders an experience section whose SectionHeader label is `§ 01 · EXPERIENCE` positioned before the WORK section
    - The teaser contains `<a href="/experience">` (exactly the listing route, not /experience/holloway) and the `1,400` metric substring
    - `grep -c "sortExperienceEntries" src/pages/index.astro` returns 1 and index.astro contains a hasCaseStudy find guard that throws on a missing Holloway entry (no inline `.sort()` on experience)
    - The tech-stack line is OMITTED from the teaser (D-05); no `.featured-stack` element renders in the experience section
    - index.astro `<style>` contains a `prefers-reduced-motion: reduce` block for the teaser link that keeps the color change and drops the arrow opacity reveal
    - No edit was made to src/styles/global.css or src/layouts/BaseLayout.astro (D-19)
  </acceptance_criteria>
  <done>Home opens with the frontend-design-routed 01 EXPERIENCE teaser sourced from the live collection, linking to /experience with the 1,400 metric; the render gate's teaser assertions are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Renumber Home headers + enrich personSchema + sharpen SEO description</name>
  <files>src/pages/index.astro, src/components/ContactSection.astro</files>
  <read_first>
    - src/pages/index.astro (line 60 WORK `number="01"`; line 82 ABOUT `number="02"`; lines 21-32 the current personSchema object; line 35 the BaseLayout `description=` prop; line 12 the about import)
    - src/components/ContactSection.astro (line 28 the hardcoded `&sect; 03 &middot; CONTACT` literal — NOT a SectionHeader prop; and the doc-comment references at lines 3 and 10)
    - src/data/education.ts (the alumniOfSchema + hasCredentialSchema exports to import and spread — created in 24-01)
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md §Layout renumber table (lines 62-67) + the sharpened SEO description draft (line 210)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Pattern 3 (personSchema enrichment, lines 182-199) and Pitfall 3 (the CONTACT literal, lines 233-237)
  </read_first>
  <action>
    In src/pages/index.astro renumber the two SectionHeader props: WORK `number="01"` becomes `number="02"` (keep its existing count prop) and ABOUT `number="02"` becomes `number="03"` — these are prop-value edits only; do NOT edit SectionHeader.astro. Enrich the personSchema object (lines 21-32): import alumniOfSchema and hasCredentialSchema from ../data/education, add `jobTitle: "Software Engineer"` (D-07/D-14), and add `alumniOf: alumniOfSchema` + `hasCredential: hasCredentialSchema` (D-14); leave the render path unchanged (`<JsonLd schema={personSchema} />`) — do NOT hand-roll a `<script>` tag (V5.3). Sharpen the BaseLayout `description=` prop on the Home page to the D-15 draft signalling production/contract experience (UI-SPEC line 210) — this is a per-page prop, no BaseLayout.astro edit; keep it em-dash-free (U+2014). Finally, in src/components/ContactSection.astro edit the hardcoded section-header literal at line 28 from `&sect; 03 &middot; CONTACT` to `&sect; 04 &middot; CONTACT` (D-01), and update the two doc-comment references ("§ 03") at lines 3 and 10 to "§ 04" for consistency. This literal renders only on Home (showSectionHeader default false on /contact), so the change is Home-only in effect.
  </action>
  <verify>
    <automated>pnpm build && pnpm exec vitest run tests/build/home-teaser-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "&sect; 04" src/components/ContactSection.astro` returns 1 and `grep -c "&sect; 03" src/components/ContactSection.astro` returns 0
    - Gate B passes: the built Home section-label sequence is exactly `["§ 01 · EXPERIENCE", "§ 02 · WORK", "§ 03 · ABOUT", "§ 04 · CONTACT"]`
    - Gate C passes: the rendered ld+json JSON.parses with `@type` Person, `jobTitle` "Software Engineer", `alumniOf` including Western Governors University + Virginia Tech, `hasCredential` including LPI Linux Essentials, and Virginia Tech absent from hasCredential
    - `grep -c "jobTitle" src/pages/index.astro` returns 1 and index.astro imports alumniOfSchema + hasCredentialSchema from ../data/education
    - The Home `description=` prop text differs from the current placeholder and contains zero U+2014 code points (Gate A covers index.astro)
    - index.astro still contains the hero `.lead` string "Software engineer building reliable, production-grade systems." UNCHANGED (D-08 — the hero lead is not touched by this phase)
    - The Home `description=` prop value is a DISTINCT string from that hero lead line (D-15 sharpened description is not a copy of the hero lead)
    - `pnpm exec astro check` stays 0/0/0
  </acceptance_criteria>
  <done>Home headers read 01/02/03/04, personSchema carries jobTitle + alumniOf + hasCredential, the SEO description is sharpened, and the CONTACT literal is renumbered; Gates B + C are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time collection → static HTML | Holloway teaser fields come from the content collection at build; no runtime input |
| education fragments → `ld+json` script | schema data reaches a `<script type="application/ld+json">` via JsonLd.astro |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02 | Tampering (XSS) | personSchema → JsonLd.astro | low | mitigate | All schema data flows through the existing JsonLd.astro serializer that escapes `<`/`>`/`&`/U+2028/U+2029; this plan spreads author-controlled constants and does not hand-roll a script tag (V5.3 output encoding) |
| T-24-03 | Tampering (invariant break) | chat-surface via BaseLayout.astro / global.css | low | mitigate | D-19: all teaser + schema + description edits are page-scoped in index.astro / a per-page prop / ContactSection.astro; Gate E asserts BaseLayout anchors intact. If BaseLayout/global.css are touched, run the D-26 battery + sse-snapshot at the 24-04 gate |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero package installs (D-19); dep-lock gate at 24-04 asserts package.json byte-identical |
</threat_model>

<verification>
- `pnpm build` succeeds; `pnpm exec vitest run tests/build/home-teaser-render.test.ts` exits 0 (Gates B + C GREEN)
- `pnpm exec vitest run tests/content/site-copy-em-dash.test.ts` shows the index.astro em-dash assertion GREEN (register assertions on about.ts/about.astro still owned by 24-03)
- `pnpm exec astro check` is 0/0/0
- `git diff --name-only` shows no change to src/layouts/BaseLayout.astro or src/styles/global.css (D-19)
</verification>

<success_criteria>
- Home surfaces a concise Holloway teaser linking to /experience (HOME-01, SC5)
- Home reads 01 EXPERIENCE / 02 WORK / 03 ABOUT / 04 CONTACT (D-01)
- JSON-LD Person schema reflects the job title + education; SEO description reflects the positioning (POS-04, SC4)
- No new runtime dependencies; no chat-surface file touched
</success_criteria>

## Artifacts this phase produces

New symbols introduced by THIS plan (exclude from source-drift verification):
- The Home `#section-experience` teaser section and its page-scoped classes (`.experience-teaser` / `.featured-teaser` / teaser eyebrow/title/summary/highlight + `.experience-link` / `.experience-arrow`, final names at executor discretion per frontend-design)
- New `personSchema` fields: `jobTitle`, `alumniOf`, `hasCredential`
- Consumes (created in 24-01): `alumniOfSchema`, `hasCredentialSchema` from `src/data/education.ts`

<output>
Create `.planning/phases/24-positioning-shift-home-teaser/24-02-SUMMARY.md` when done.
</output>
