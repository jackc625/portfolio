---
phase: 24-positioning-shift-home-teaser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/education.ts
  - tests/content/education-module.test.ts
  - tests/content/site-copy-em-dash.test.ts
  - tests/build/home-teaser-render.test.ts
  - tests/build/chat-surface-untouched.test.ts
autonomous: true
requirements: [POS-03, POS-04]
must_haves:
  truths:
    - "src/data/education.ts is the single source of truth for education display facts AND the JSON-LD schema fragments (POS-03, POS-04, D-11)"
    - "The Wave 0 validation gates exist so the D-18 em-dash/register landmine and the D-01 renumber + POS-04 JSON-LD shape are caught by CI, not by eyeballs"
  artifacts:
    - src/data/education.ts
    - tests/content/education-module.test.ts
    - tests/content/site-copy-em-dash.test.ts
    - tests/build/home-teaser-render.test.ts
    - tests/build/chat-surface-untouched.test.ts
  key_links:
    - "education.ts exports alumniOfSchema + hasCredentialSchema consumed by index.astro personSchema (24-02) and EDUCATION + CREDENTIALS consumed by about.astro block (24-03)"
    - "Gate D (education-module) is GREEN once education.ts exists; Gates A + B/C are authored RED and drive 24-02 / 24-03 to GREEN (Nyquist Wave 0)"
---

<objective>
Establish the Phase 24 foundation: the shared `src/data/education.ts` module (education facts + ready-to-spread schema.org fragments) and the four Nyquist Wave 0 validation gates that the rest of the phase drives from RED to GREEN.

Purpose: education.ts is the single source of truth (D-11) that both the `/about` education block (24-03) and the Home `personSchema` JSON-LD (24-02) read, preventing drift. The gates convert the D-18 (em-dash/register) and D-19 (chat-surface) manual guardrails and the D-01/POS-04 render + JSON-LD contracts into automated Vitest gates over the files the existing voice gates do NOT scan.

Output: 1 new data module + 4 new test files. Gate D (education-module) is GREEN in this plan; Gates A (site-copy) and B/C (home-teaser-render) are authored intentionally RED against the current source and go GREEN as 24-02 / 24-03 land.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md
@.planning/phases/24-positioning-shift-home-teaser/24-VALIDATION.md
@.planning/phases/24-positioning-shift-home-teaser/24-PATTERNS.md

# Analog source for the module shape and the test idioms
@src/data/contact.ts
@tests/content/experience-voice-em-dash.test.ts
@tests/content/voice-banlist.test.ts
@tests/build/featured-tier-render.test.ts
@tests/client/about-data.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/data/education.ts (single source of truth) + Gate D unit test</name>
  <files>src/data/education.ts, tests/content/education-module.test.ts</files>
  <read_first>
    - src/data/contact.ts (the data-module SSoT convention to clone: doc header naming consumers, typed `as const`, voice-neutral facts, zero em dashes)
    - src/data/about.ts (existing escape convention: curly quotes, non-breaking space ` `, en dash for date ranges, zero U+2014)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Pattern 2 (lines 135-180 — the exact recommended module shape) and Assumptions A1/A2 (schema field caveats)
    - tests/client/about-data.test.ts (the import-and-assert unit-gate analog for Gate D)
  </read_first>
  <behavior>
    - EDUCATION.degree === "B.S. Computer Science"; EDUCATION.institution === "Western Governors University"; EDUCATION.date === "May 2026"; EDUCATION.transferredFrom === "Virginia Tech"
    - CREDENTIALS contains an entry named "LPI Linux Essentials" with issuer "Linux Professional Institute"
    - alumniOfSchema DOES include "Virginia Tech" (attended = honest per D-10) and "Western Governors University"
    - hasCredentialSchema: every entry has @type === "EducationalOccupationalCredential"; NO entry's recognizedBy.name is "Virginia Tech" (VT is not a credential — D-10); includes the WGU B.S. degree and the LPI cert
  </behavior>
  <action>
    Create src/data/education.ts following the src/data/contact.ts SSoT convention: a doc header naming its consumers (about.astro education block [24-03], index.astro personSchema [24-02], and the deferred Phase 25 chat consumer), then typed exports. Use the exact shape specified in 24-RESEARCH.md Pattern 2 (lines 138-179): a `Credential` interface (`name: string; issuer?: string`); an `EDUCATION` object marked `as const` with fields degree "B.S. Computer Science", institution "Western Governors University", date "May 2026", transferredFrom "Virginia Tech"; a `CREDENTIALS: Credential[]` array holding a single entry name "LPI Linux Essentials" issuer "Linux Professional Institute"; and the two schema.org fragments `alumniOfSchema` (CollegeOrUniversity objects for Western Governors University AND Virginia Tech) and `hasCredentialSchema` (an EducationalOccupationalCredential of credentialCategory "degree" named "Bachelor of Science in Computer Science" recognizedBy Western Governors University with validFrom "2026-05", plus a mapped certificate entry per CREDENTIALS). VT appears ONLY in alumniOfSchema, never in hasCredentialSchema (D-10 — do not imply a VT credential). All strings voice-neutral facts; zero em dash (U+2014); en dash permitted. Then author tests/content/education-module.test.ts as an import-and-assert unit gate (node env) mirroring tests/client/about-data.test.ts, asserting every item in the <behavior> block above.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/content/education-module.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run tests/content/education-module.test.ts` exits 0 (Gate D GREEN)
    - `pnpm exec astro check` remains 0 errors / 0 warnings / 0 hints with education.ts added
    - src/data/education.ts contains zero U+2014 code points (verify with the site-copy gate authored in Task 2, and by hand per D-18)
    - `grep -c "Virginia Tech" src/data/education.ts` returns a count that appears in alumniOfSchema but the module's hasCredentialSchema entries carry no recognizedBy.name of "Virginia Tech" (asserted by Gate D)
    - Module exports exactly: `Credential` (type), `EDUCATION`, `CREDENTIALS`, `alumniOfSchema`, `hasCredentialSchema`
  </acceptance_criteria>
  <done>education.ts exists as the SSoT with the exact Pattern 2 shape; Gate D is GREEN; astro check stays 0/0/0.</done>
</task>

<task type="auto">
  <name>Task 2: Author Gate A — tests/content/site-copy-em-dash.test.ts (D-18 automation)</name>
  <files>tests/content/site-copy-em-dash.test.ts</files>
  <!-- planner-discipline-allow: junior -->
  <!-- planner-discipline-allow: senior -->
  <read_first>
    - tests/content/experience-voice-em-dash.test.ts (the readFileSync source-scan + `(src.match(/—/g) ?? []).length` idiom to clone; note it hardcodes MDX paths — this is the exact scan-scope gap Gate A closes)
    - tests/content/voice-banlist.test.ts (the BANLIST regex-array idiom for the register words)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md §Validation Architecture Gate A spec (lines 389-393)
    - src/data/about.ts and src/pages/about.astro (the files that currently carry the register landmine word Gate A must eventually flag — this is why Gate A is RED until 24-02/24-03)
  </read_first>
  <action>
    Create tests/content/site-copy-em-dash.test.ts as a node-env source-scan gate (readFileSync). Scan set: src/data/about.ts, src/data/education.ts, src/pages/index.astro, src/pages/about.astro, and src/components/ContactSection.astro. Assert per file that the count of U+2014 (em dash) code points is 0 (en dash U+2013 is permitted). Add a register banlist (case-insensitive word-boundary regexes) applied to src/data/about.ts and src/pages/about.astro asserting zero matches for the self-applied seniority-qualifier word "junior", the word "senior", and the pattern "5+ years" (the D-07 honest-new-grad register). Mirror the failure-message style of experience-voice-em-dash.test.ts so an offender names the file. This gate is intentionally RED now (about.ts and about.astro still carry the banned register word pre-revision) and is driven GREEN by 24-02 (index.astro) and 24-03 (about.ts + about.astro). education.ts and ContactSection.astro must already pass the em-dash assertion. Do NOT weaken the assertions to make them pass now — RED-until-impl is the intended Nyquist signal.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/content/site-copy-em-dash.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/content/site-copy-em-dash.test.ts exists and runs (the vitest command executes the suite)
    - The em-dash assertion for src/data/education.ts and src/components/ContactSection.astro PASSES (those files are already clean)
    - The register-word assertions for src/data/about.ts and src/pages/about.astro are present and currently FAIL (RED) — the failure message names the offending file; this is the documented pre-impl state, closed by 24-03
    - The banned register regexes use case-insensitive word boundaries: one for the seniority-qualifier word, one for "senior", one for "5+ years"
  </acceptance_criteria>
  <done>Gate A is authored with em-dash + register assertions over the five gate-UNSCANNED files; education.ts/ContactSection.astro pass; about.ts/about.astro register assertions are RED pending 24-03 (documented).</done>
</task>

<task type="auto">
  <name>Task 3: Author Gate B/C (home-teaser-render) + Gate E (chat-surface-untouched tripwire)</name>
  <files>tests/build/home-teaser-render.test.ts, tests/build/chat-surface-untouched.test.ts</files>
  <read_first>
    - tests/build/featured-tier-render.test.ts (clone the `// @vitest-environment jsdom` header, `existsSync` dist guard + "run `pnpm build` first" message, and the DOMParser idiom for Gate B/C)
    - src/components/primitives/SectionHeader.astro (renders `§ {number} · {title}` — the exact label shape Gate B asserts)
    - src/components/JsonLd.astro (the escaping serializer — Gate C must JSON.parse the script textContent, NOT substring-match; the `<`→`<` escapes are valid JSON, per RESEARCH Pitfall 5)
    - src/layouts/BaseLayout.astro (lines 20-87 — the canonical anchors Gate E asserts still present: `ogImage = "/og-default.png"`, `titleDefault="Jack Cutrara | Software Engineer"`, the OG `width: 1200,` / `height: 630,` block)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Gate B/C/E specs (lines 395-411)
  </read_first>
  <action>
    Create tests/build/home-teaser-render.test.ts as a jsdom render gate (`// @vitest-environment jsdom`; read dist/client/index.html; `existsSync` guard that surfaces "run `pnpm build` first"). Gate B: select the section-label spans (`.section-header .label-mono`, which covers both the SectionHeader primitive output and the ContactSection literal), map trimmed/whitespace-normalized textContent, and assert the exact ordered sequence `["§ 01 · EXPERIENCE", "§ 02 · WORK", "§ 03 · ABOUT", "§ 04 · CONTACT"]` (D-01). Assert an `<a>` inside the experience section with `href="/experience"` (exactly the listing route, NOT /experience/holloway — D-04) whose text or a sibling contains the metric substring `1,400` (D-03). Gate C (same file): select `script[type="application/ld+json"]`, `JSON.parse` its textContent, and assert `@type === "Person"`, `jobTitle === "Software Engineer"`, `alumniOf` names include "Western Governors University" AND "Virginia Tech", `hasCredential` includes an entry named "LPI Linux Essentials", and NO hasCredential entry references "Virginia Tech" (D-10). Also create tests/build/chat-surface-untouched.test.ts (Gate E, D-19 tripwire): a node-env readFileSync source guard asserting src/layouts/BaseLayout.astro still contains its canonical anchors `ogImage = "/og-default.png"`, `titleDefault="Jack Cutrara | Software Engineer"`, and the OG `width: 1200` + `height: 630` declarations, so any accidental structural edit is caught. Gates B/C are intentionally RED until 24-02 rebuilds Home with the teaser + renumber + enriched schema; Gate E is GREEN now.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/chat-surface-untouched.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/build/home-teaser-render.test.ts exists with the jsdom env header and the existsSync dist guard; Gate C parses JSON-LD via JSON.parse (no substring match on the escaped script bytes)
    - tests/build/chat-surface-untouched.test.ts (Gate E) exists and PASSES: `pnpm exec vitest run tests/build/chat-surface-untouched.test.ts` exits 0
    - Gate B asserts the four-item ordered label sequence ending `§ 04 · CONTACT`; Gate B's teaser-link assertion targets `href="/experience"` and the `1,400` metric substring
    - With no fresh dist/ present, Gate B/C degrade cleanly via the existsSync guard (they do not throw an opaque file-read error)
  </acceptance_criteria>
  <done>Gate B/C authored (RED until 24-02 build) and Gate E authored + GREEN; both use the exact repo jsdom / readFileSync idioms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author content → build-time module | education.ts strings are author-controlled constants compiled at build; no user input crosses here |
| module → JSON-LD `<script>` | education fragments will be serialized into a `ld+json` script (in 24-02) via the existing JsonLd.astro escaper |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering (XSS) | education.ts strings reaching a `ld+json` script | low | mitigate | Fragments are consumed via JsonLd.astro's `<`/`>`/`&`/U+2028/U+2029 escaping (24-02); this plan only defines the constants, never hand-rolls a `<script>` tag (V5.3 output encoding) |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs ZERO packages (D-19: no new runtime deps); vitest + jsdom already present. The capstone dep-lock gate (24-04) asserts package.json byte-identical |
</threat_model>

<verification>
- `pnpm exec vitest run tests/content/education-module.test.ts` exits 0 (Gate D GREEN)
- `pnpm exec vitest run tests/build/chat-surface-untouched.test.ts` exits 0 (Gate E GREEN)
- `pnpm exec astro check` is 0/0/0 with education.ts added
- Gate A and Gate B/C are authored and present; their RED-until-impl state is documented in their `<done>` notes (closed by 24-02 / 24-03)
- No file under src/layouts/ or src/styles/global.css was modified (D-19)
</verification>

<success_criteria>
- src/data/education.ts exists as the single source of truth (facts + alumniOfSchema + hasCredentialSchema), VT in alumniOf only (D-10)
- Four test files exist: education-module (GREEN), site-copy-em-dash (Gate A, partial RED), home-teaser-render (Gate B/C, RED pending build), chat-surface-untouched (Gate E, GREEN)
- Zero new runtime dependencies added
</success_criteria>

## Artifacts this phase produces

New symbols created by THIS plan (exclude from source-drift verification):
- `src/data/education.ts` and its exports: `Credential` (interface), `EDUCATION`, `CREDENTIALS`, `alumniOfSchema`, `hasCredentialSchema`
- `tests/content/education-module.test.ts` (Gate D)
- `tests/content/site-copy-em-dash.test.ts` (Gate A)
- `tests/build/home-teaser-render.test.ts` (Gates B + C)
- `tests/build/chat-surface-untouched.test.ts` (Gate E, D-19 tripwire)

<output>
Create `.planning/phases/24-positioning-shift-home-teaser/24-01-SUMMARY.md` when done.
</output>
