---
phase: 24-positioning-shift-home-teaser
plan: 04
type: execute
wave: 3
depends_on: [24-02, 24-03]
files_modified:
  - public/og-default.png
autonomous: false
requirements: [POS-04]
must_haves:
  truths:
    - "A real 1200x630 editorial og-default.png social card reflecting the software-engineer positioning replaces the placeholder (POS-04, D-16)"
    - "The full phase gate is green: build + full test suite (incl the untouched D-26 chat battery + sse-snapshot) + astro check 0/0/0 + dep-lock + no chat-surface file changed + byte-identical portfolio-context.json (D-19, QA-01, QA-02)"
    - "Jack has reviewed the final copy and frontend-design has signed off the teaser, education block, /about layout, and OG card (D-20)"
  artifacts:
    - public/og-default.png
  key_links:
    - "og-default.png is already referenced by BaseLayout.astro's ogImage default — this is an asset swap only, no wiring change (D-16, D-19)"
    - "The Google Rich Results Test validates the enriched Person JSON-LD beyond the in-test JSON.parse shape (POS-04, RESEARCH A1)"
---

<objective>
Ship the real OG social card and run the Phase 24 capstone gate with the human sign-offs.

Purpose: replace the placeholder `public/og-default.png` with a real 1200x630 editorial card that reflects the new positioning (POS-04, D-16), then prove the whole phase holds its cross-cutting guardrails green (D-19 chat-surface invariants, QA-01/QA-02) and gather the human reviews that the copy and visual work are Claude-drafts / Jack-approves (D-20, copy discretion).

Output: a swapped `public/og-default.png` asset and a green capstone gate with recorded human approvals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md
@.planning/phases/24-positioning-shift-home-teaser/24-VALIDATION.md
@.planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md

# The asset is already wired here (no edit) + the copy drafts under review
@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design + ship the real og-default.png (frontend-design routed, D-20/D-16)</name>
  <files>public/og-default.png</files>
  <read_first>
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md line 219 (the OG card concept: name + "Software Engineer" + short tagline, six-token palette bg #FAFAF7 / ink #0A0A0A / single accent #E63946 dot, Geist type) and the Color section
    - design-system/MASTER.md (six-token palette + Geist typography — the D-20 contract the frontend-design skill validates against)
    - src/layouts/BaseLayout.astro (lines 23, 73-78 — the ogImage default already points at /og-default.png and declares width 1200 / height 630; this is an asset swap ONLY, no source edit)
  </read_first>
  <action>
    Route the OG card concept through the frontend-design skill against design-system/MASTER.md and the UI-SPEC OG line: an editorial 1200x630 card with the name "Jack Cutrara", the label "Software Engineer", and a short non-inflated tagline, on the six-token palette (bg #FAFAF7, ink #0A0A0A, a single accent #E63946 dot), Geist type — no other colors, no icons. Produce the real 1200x630 PNG and write it to public/og-default.png, replacing the placeholder. Do NOT edit BaseLayout.astro or any wiring (D-16/D-19) — the default ogImage already references this path. Any tagline text follows the zero-em-dash (U+2014) and no-inflated-claims rules. If a deterministic PNG-rendering path is not available to the executor, prepare the card design/spec and defer producing the final rendered asset to the human checkpoint (Task 3), where Jack confirms the shipped file.
  </action>
  <verify>
    <automated>node -e "const b=require('fs').readFileSync('public/og-default.png');console.log(b.readUInt32BE(16)+'x'+b.readUInt32BE(20))"</automated>
  </verify>
  <acceptance_criteria>
    - The dimension command prints `1200x630` (the PNG IHDR width x height)
    - public/og-default.png differs from the prior placeholder (git shows the asset modified)
    - No edit was made to src/layouts/BaseLayout.astro (asset swap only, D-16/D-19)
  </acceptance_criteria>
  <done>public/og-default.png is a real 1200x630 editorial card on the six-token palette; BaseLayout wiring untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Phase capstone gate — build + full suite + astro check + dep-lock + D-19 tripwires</name>
  <files>(none — verification-only gate; runs build + test + astro check across the phase)</files>
  <read_first>
    - .planning/phases/24-positioning-shift-home-teaser/24-VALIDATION.md §"Sampling Rate" (the full-suite command) and §"Wave 0 Requirements" (all gates that must be green)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Pitfall 4 (portfolio-context.json is regenerated byte-identical from UNCHANGED inputs — confirm as a regression check; do NOT edit any chat-context input, D-17) and §Sampling Rate (line 416)
    - src/layouts/BaseLayout.astro + src/styles/global.css (the D-19 files that must remain unchanged by this phase)
  </read_first>
  <action>
    Run the full phase gate and record results in the SUMMARY. Run `pnpm build` (regenerates dist/ AND portfolio-context.json via build:chat-context) and confirm it succeeds. Run `pnpm test` and confirm the whole suite is green, including: Gate A (site-copy-em-dash), Gate B/C (home-teaser-render), Gate D (education-module), Gate E (chat-surface-untouched), the existing about-data word-count gate, and the untouched D-26 chat-surface battery + sse-snapshot.test.ts (D-15 byte-identical anchor) + chat-context-integrity.test.ts + chat-knowledge-voice.test.ts. Run `pnpm exec astro check` and confirm 0 errors / 0 warnings / 0 hints. Assert dependency lock: package.json `dependencies` are byte-identical to the phase-start state (no new runtime deps, D-19/QA-02). Assert `git diff --name-only` shows NO change to src/layouts/BaseLayout.astro or src/styles/global.css (D-19); if either changed, STOP and run the D-26 battery + sse-snapshot as the mandated fallback before proceeding. Confirm the regenerated portfolio-context.json is byte-identical to its committed version (Pitfall 4 — no chat input was touched, D-17). Also confirm no edit was made to about-chat.ts, portfolio-context.json/.static.json, or scripts/build-chat-context.mjs (D-17, Phase 25 scope).
  </action>
  <verify>
    <automated>pnpm build && pnpm test && pnpm exec astro check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; `pnpm test` exits 0 (full suite green incl Gates A/B/C/D/E, D-26 battery, sse-snapshot, chat-context-integrity, chat-knowledge-voice); `pnpm exec astro check` reports 0/0/0
    - `git diff --name-only` lists no change to src/layouts/BaseLayout.astro or src/styles/global.css (D-19)
    - `git status --porcelain src/data/about-chat.ts src/data/portfolio-context.json src/data/portfolio-context.static.json scripts/build-chat-context.mjs` shows portfolio-context.json byte-identical and the other three unmodified (D-17)
    - package.json `dependencies` unchanged from phase start (no new runtime deps, QA-02)
  </acceptance_criteria>
  <done>Full phase gate is green: build + test + astro check pass; chat-surface files and dependencies are untouched; portfolio-context.json is byte-identical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — copy review + frontend-design 6-pillar + Google Rich Results + OG card</name>
  <files>(none — blocking human verification checkpoint; not auto-approvable)</files>
  <action>This is a BLOCKING human checkpoint (do NOT auto-approve; workflow.auto_advance does not apply). Present the built result to Jack and wait for an explicit reply. The concrete verification steps are enumerated in how-to-verify below: (1) copy review of the drafted wording, (2) frontend-design 6-pillar sign-off, (3) Google Rich Results Test on the Home JSON-LD, (4) OG card confirmation. Apply any requested copy or design edits before marking the phase complete.</action>
  <what-built>
    Phase 24 positioning shift is implemented and the automated gate is green: the Home 01 EXPERIENCE Holloway teaser (linking to /experience with the 0-to-~1,400 metric), the renumbered Home sections (01/02/03/04), the enriched Person JSON-LD (jobTitle + alumniOf + hasCredential), the sharpened SEO descriptions, the revised first-person About copy (intro/P1/P3; P2 verbatim), the /about Education block (WGU B.S. CS May 2026 + Virginia Tech transfer + LPI Linux Essentials), and the real 1200x630 og-default.png card.
  </what-built>
  <how-to-verify>
    1. COPY REVIEW (Claude drafted, you approve final wording — CONTEXT discretion): run `pnpm dev` and read the revised copy in situ — ABOUT_INTRO / ABOUT_P1 / ABOUT_P3 on `/about` and the Home ABOUT preview (confirm ABOUT_P2 is unchanged), the teaser summary sentence + the one metric line on Home, the sharpened Home + /about SEO descriptions (view source `<meta name="description">`), and the Education block labels. Confirm each reads as an honest new-grad software engineer with production experience, first person, no self-diminishing label, no senior/5+ years framing, and zero em dashes. Reply with edits or approval.
    2. FRONTEND-DESIGN 6-PILLAR SIGN-OFF (D-20): confirm the teaser form, the /about education block, any /about layout change, and the og-default.png card all pass the frontend-design 6 pillars against design-system/MASTER.md (copywriting, visuals, color, typography, spacing, registry-N/A). Record PASS per pillar.
    3. JSON-LD VALIDITY (POS-04 / RESEARCH A1): paste the built Home page (or its ld+json) into the Google Rich Results Test and confirm the Person entity parses with no errors — jobTitle "Software Engineer", alumniOf (WGU + Virginia Tech), hasCredential (LPI Linux Essentials + WGU B.S. degree, Virginia Tech NOT a credential).
    4. OG CARD: open public/og-default.png and confirm it is the real 1200x630 editorial card on the six-token palette with Geist type (not the placeholder).
  </how-to-verify>
  <resume-signal>Reply "approved" (or list copy edits / design changes to apply before merge).</resume-signal>
  <verify>
    <human-check>Jack confirms: copy reads as an honest new-grad software engineer with production experience (first person, no self-diminishing label, no senior/5+ years framing, zero em dashes); frontend-design 6 pillars PASS on the teaser, education block, /about layout, and OG card; Google Rich Results Test parses the Person entity with no errors; og-default.png is the real 1200x630 card.</human-check>
  </verify>
  <done>Jack replied "approved" (or all requested edits were applied and re-approved); all four sign-offs recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset → social/link preview | og-default.png is served as a static file; no user input, no runtime processing |
| build artifacts → chat pipeline | portfolio-context.json is regenerated at build from chat inputs this phase does not touch (D-17) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03 | Tampering (invariant break) | chat-surface via BaseLayout.astro / global.css / chat inputs | low | mitigate | D-19/D-17: the capstone gate asserts BaseLayout.astro + global.css unchanged and portfolio-context.json byte-identical; the untouched D-26 battery + sse-snapshot run green in the full suite |
| T-24-05 | Spoofing (misleading preview) | og-default.png content | low | accept | Author-controlled static card; content reviewed at the human checkpoint against MASTER.md and the honest-register copy rules |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero package installs across the phase (D-19); this gate asserts package.json dependencies byte-identical (QA-02) |
</threat_model>

<verification>
- `pnpm build` + `pnpm test` + `pnpm exec astro check` all green (full suite incl D-26 battery + sse-snapshot + Gates A/B/C/D/E)
- OG dimension check prints 1200x630
- BaseLayout.astro / global.css unchanged; portfolio-context.json byte-identical; no chat input edited (D-17/D-19)
- Human checkpoint approved: copy review + frontend-design 6-pillar sign-off + Google Rich Results Test + OG card confirmation
</verification>

<success_criteria>
- Site metadata (SEO description + JSON-LD Person schema + OG card) reflects the updated positioning and job title (POS-04, SC4)
- The phase holds every cross-cutting guardrail (D-17, D-19, QA-01, QA-02) with no new runtime dependencies
- Jack approved the final copy and frontend-design signed off all visual surfaces (D-20)
</success_criteria>

## Artifacts this phase produces

New/updated artifacts introduced by THIS plan (exclude from source-drift verification):
- `public/og-default.png` (real 1200x630 editorial card, replacing the placeholder)

<output>
Create `.planning/phases/24-positioning-shift-home-teaser/24-04-SUMMARY.md` when done.
</output>
