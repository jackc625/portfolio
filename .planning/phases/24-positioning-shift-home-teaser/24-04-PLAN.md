---
phase: 24-positioning-shift-home-teaser
plan: 04
type: execute
wave: 3
depends_on: [24-02, 24-03]
files_modified:
  - public/og-default.png
  - scripts/verify-phase24-og.mjs
  - tests/content/site-copy-em-dash.test.ts
autonomous: false
requirements: [POS-04]
must_haves:
  truths:
    - "A real 1200x630 editorial og-default.png social card reflecting the software-engineer positioning replaces the placeholder, produced by ONE deterministic mechanism that fails closed if unavailable, and proven distinct from the placeholder by a phase-start hash difference + PNG signature + max-file-size check (POS-04, D-16; review fix #5)"
    - "Gate A (site-copy em-dash/register over all five gate-UNSCANNED files) is AUTHORED here -- the point where about.ts, education.ts, index.astro, about.astro, and ContactSection.astro are all clean -- so it is GREEN at this boundary (review fix #1)"
    - "All new Phase 24 copy in the voice-gate-UNSCANNED files (src/data/about.ts, src/data/education.ts, and the Home teaser/metadata strings in index.astro, about.astro, ContactSection.astro) contains zero U+2014 em dashes -- the site-wide zero-em-dash ban (chat pipeline exempt) holds for the src/data/* and page copy that the MDX-only voice gates never scan, enforced by Gate A here (D-18)"
    - "The phase-wide D-19/D-17 invariants are proven by comparing current protected-file hashes + the dependencies object against the 24-01 phase-start baseline (scripts/verify-phase24-invariants.mjs), NOT by working-tree git diff/status which reads clean after per-task commits (review fix #4)"
    - "The full phase gate is green: build + full test suite (incl the untouched D-26 chat battery + sse-snapshot) + astro check 0/0/0 (D-19, QA-01, QA-02)"
    - "Jack has reviewed the final copy and frontend-design has signed off the teaser, education block, /about layout, and OG card at 375px AND 1440px with keyboard-focus, reduced-motion, and horizontal-overflow checks (D-20; review fix #6)"
  artifacts:
    - public/og-default.png
    - scripts/verify-phase24-og.mjs
    - tests/content/site-copy-em-dash.test.ts
  key_links:
    - "og-default.png is already referenced by BaseLayout.astro's ogImage default -- this is an asset swap only, no wiring change (D-16, D-19)"
    - "scripts/verify-phase24-og.mjs reads .planning/.../24-BASELINE.json og_placeholder_hash (captured 24-01) to prove the new asset differs from the placeholder (review fix #5)"
    - "scripts/verify-phase24-invariants.mjs (24-01) compares protected-file hashes + dependencies against the phase-start baseline (review fix #4)"
    - "Gate A (tests/content/site-copy-em-dash.test.ts) is authored HERE; the Schema Markup Validator (validator.schema.org) validates the standalone Person JSON-LD -- NOT the Google Rich Results Test (review fix #6)"
---

<objective>
Ship the real OG social card, author the comprehensive site-copy Gate A, and run the Phase 24 capstone gate with the human sign-offs.

Purpose: replace the placeholder `public/og-default.png` with a real 1200x630 editorial card that reflects the new positioning (POS-04, D-16) via ONE deterministic production mechanism that fails closed if unavailable (review fix #5); author Gate A here where all five scanned files are finally clean so it is GREEN at its own boundary (review fix #1); prove the whole phase holds its cross-cutting guardrails by comparing against the 24-01 phase-start baseline rather than a working-tree diff (review fix #4); and gather the human reviews that the copy and visual work are Claude-drafts / Jack-approves at the concrete viewports the UI spec requires (D-20, copy discretion, review fix #6).

Output: a swapped `public/og-default.png` asset + its deterministic verifier, the authored+GREEN Gate A, and a green capstone gate with recorded human approvals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md
@.planning/phases/24-positioning-shift-home-teaser/24-VALIDATION.md
@.planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md

# The asset is already wired here (no edit) + the phase-start baseline + Gate A analogs
@src/layouts/BaseLayout.astro
@.planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json
@tests/content/experience-voice-em-dash.test.ts
@tests/content/voice-banlist.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the real og-default.png via ONE deterministic path (fail closed) + verifier</name>
  <files>public/og-default.png, scripts/verify-phase24-og.mjs</files>
  <read_first>
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md line 219 (the OG card concept: name + "Software Engineer" + short tagline, six-token palette bg #FAFAF7 / ink #0A0A0A / single accent #E63946 dot, Geist type) and the Color section
    - design-system/MASTER.md (six-token palette + Geist typography -- the D-20 contract the frontend-design skill validates against)
    - src/layouts/BaseLayout.astro (lines 23, 73-78 -- the ogImage default already points at /og-default.png and declares width 1200 / height 630; this is an asset swap ONLY, no source edit)
    - .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json (the `og_placeholder_hash` captured at phase start -- the verifier asserts the new asset differs from it)
  </read_first>
  <action>
    Route the OG card concept through the frontend-design skill against design-system/MASTER.md and the UI-SPEC OG line: an editorial 1200x630 card with the name "Jack Cutrara", the label "Software Engineer", and a short non-inflated tagline, on the six-token palette (bg #FAFAF7, ink #0A0A0A, a single accent #E63946 dot), Geist type -- no other colors, no icons. Any tagline text follows the zero-em-dash (U+2014) and no-inflated-claims rules.

    Then produce the real 1200x630 PNG by ONE deterministic mechanism, chosen and PREFLIGHTED before committing (review fix #5 -- do NOT ship only a spec, do NOT defer creating the file to the human checkpoint). Recommended path: render a self-contained SVG of the card to PNG via the already-present `sharp` (Astro's bundled image processor -- zero new runtime deps); acceptable fallback: a Playwright screenshot of a page-scoped HTML card at a 1200x630 viewport (Playwright is an existing devDep). Preflight the chosen mechanism first (confirm it emits a valid PNG); if NEITHER mechanism can produce a real PNG in this environment, STOP and surface the blocker in the SUMMARY -- do NOT mark the task complete with a placeholder or a spec-only deliverable. Write the result to public/og-default.png, replacing the placeholder. Do NOT edit BaseLayout.astro or any wiring (D-16/D-19) -- the default ogImage already references this path.

    Also create scripts/verify-phase24-og.mjs (Node built-ins only -- `node:fs`, `node:crypto`; zero new deps) that reads .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json and asserts, exiting nonzero on any failure: (1) public/og-default.png begins with the PNG signature bytes 89 50 4E 47 0D 0A 1A 0A; (2) its IHDR width x height is exactly 1200 x 630 (readUInt32BE at offsets 16 and 20); (3) its file size is at most a sane editorial-card cap (e.g. 512 KB) so a corrupt/oversized asset fails; and (4) its SHA-256 DIFFERS from baseline.og_placeholder_hash (proving it is not the placeholder -- the plain dimension check alone passes on the placeholder, which is already 1200x630; review fix #5 + the "dimension check does not distinguish the placeholder" finding).
  </action>
  <verify>
    <automated>node scripts/verify-phase24-og.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase24-og.mjs` exits 0: PNG signature valid, IHDR is 1200x630, file size <= the cap, and the SHA-256 DIFFERS from the phase-start placeholder hash (review fix #5)
    - public/og-default.png was produced by a preflighted deterministic mechanism (sharp SVG->PNG or Playwright screenshot); if neither was available the task was STOPPED, not completed with a spec
    - No edit was made to src/layouts/BaseLayout.astro (asset swap only, D-16/D-19)
  </acceptance_criteria>
  <done>public/og-default.png is a real, deterministically-produced 1200x630 editorial card that provably differs from the placeholder; the verifier is GREEN; BaseLayout wiring untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Author Gate A + phase capstone gate (build + full suite + astro check + baseline comparison)</name>
  <files>tests/content/site-copy-em-dash.test.ts</files>
  <!-- planner-discipline-allow: junior -->
  <!-- planner-discipline-allow: senior -->
  <read_first>
    - tests/content/experience-voice-em-dash.test.ts (the readFileSync source-scan + em-dash-count idiom to clone for Gate A; note it hardcodes MDX paths -- the exact scan-scope gap Gate A closes)
    - tests/content/voice-banlist.test.ts (the BANLIST regex-array idiom for the register words)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md §Validation Architecture Gate A spec (lines 389-393) and Pitfall 4 (portfolio-context.json regenerates byte-identical from UNCHANGED inputs -- confirm as a regression check; do NOT edit any chat-context input, D-17)
    - .planning/phases/24-positioning-shift-home-teaser/24-VALIDATION.md §"Full suite command" + §"Wave 0 Requirements" (all gates that must be green)
    - scripts/verify-phase24-invariants.mjs + .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json (created in 24-01 -- the authoritative protected-file + dependency comparison)
  </read_first>
  <action>
    First author tests/content/site-copy-em-dash.test.ts (Gate A) as a node-env source-scan gate (readFileSync). Author it HERE (not 24-01) because it scans five files owned across the parallel 24-02 + 24-03, and only at the capstone are ALL of them clean -- so it is GREEN at its own boundary (review fix #1). Scan set: src/data/about.ts, src/data/education.ts, src/pages/index.astro, src/pages/about.astro, src/components/ContactSection.astro. Assert per file that the count of U+2014 (em dash) code points is 0 (en dash U+2013 permitted). Add a register banlist (case-insensitive word-boundary regexes) applied to src/data/about.ts and src/pages/about.astro (and index.astro) asserting zero matches for the self-applied seniority-qualifier word "junior", "senior", and "5+ years" (the D-07 honest-new-grad register). Mirror the failure-message style of experience-voice-em-dash.test.ts so an offender names the file.

    Then run the full phase gate and record results in the SUMMARY. Run `pnpm build` (regenerates dist/ AND portfolio-context.json via build:chat-context) and confirm it succeeds. Run `pnpm test` and confirm the whole suite is green, including: Gate A (just authored), Gate B/C (home-teaser-render, 24-02), Gate D (education-module, 24-01), Gate E (chat-surface-untouched, 24-01), about-education-render (24-03), the existing about-data word-count gate, and the untouched D-26 chat-surface battery + sse-snapshot.test.ts (D-15 byte-identical anchor) + chat-context-integrity.test.ts + chat-knowledge-voice.test.ts. Run `pnpm exec astro check` and confirm 0/0/0. Finally run `node scripts/verify-phase24-invariants.mjs` (24-01) AFTER the build so the regenerated portfolio-context.json is what gets hashed: it asserts every D-19/D-17 protected file (BaseLayout.astro, global.css, chat.ts, api/chat.ts, about-chat.ts, portfolio-context.json, portfolio-context.static.json, build-chat-context.mjs) still matches its phase-start hash AND the package.json dependencies object is byte-identical to the phase-start snapshot (review fix #4 -- this proves the phase-wide invariants across ALL committed tasks, which a working-tree `git diff`/`git status` cannot). If the invariant script reports ANY drift, STOP: if a protected file legitimately had to change, run the D-26 battery + sse-snapshot as the mandated D-19 fallback and record it; a dependency change is a hard QA-02 failure.
  </action>
  <verify>
    <automated>pnpm build && pnpm test && pnpm exec astro check && node scripts/verify-phase24-invariants.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; `pnpm test` exits 0 (full suite green incl Gate A just authored, Gates B/C/D/E, about-education-render, D-26 battery, sse-snapshot, chat-context-integrity, chat-knowledge-voice); `pnpm exec astro check` reports 0/0/0
    - `node scripts/verify-phase24-invariants.mjs` exits 0: all eight protected files match their phase-start hashes AND package.json dependencies are byte-identical to the baseline (review fix #4)
    - Gate A is authored and GREEN: zero U+2014 in all five scanned files, zero banned register words in about.ts / about.astro / index.astro (review fix #1)
    - The regenerated portfolio-context.json hash equals the phase-start baseline (byte-identical, Pitfall 4); about-chat.ts / portfolio-context.static.json / build-chat-context.mjs match baseline (untouched, D-17)
  </acceptance_criteria>
  <done>Gate A authored + GREEN; full phase gate is green (build + test + astro check); the baseline comparison proves protected files + dependencies are unchanged phase-wide.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off -- copy review + 6-pillar at 375px/1440px + Schema Markup Validator + OG card</name>
  <files>(none -- blocking human verification checkpoint; not auto-approvable)</files>
  <action>This is a BLOCKING human checkpoint (do NOT auto-approve; workflow.auto_advance does not apply). Present the built result to Jack and wait for an explicit reply. The concrete verification steps are enumerated in how-to-verify below: (1) copy review, (2) frontend-design 6-pillar sign-off at the two required viewports with the three interaction checks, (3) Schema Markup Validator on the Home Person JSON-LD, (4) OG card confirmation. Apply any requested copy or design edits before marking the phase complete.</action>
  <what-built>
    Phase 24 positioning shift is implemented and the automated gate is green: the Home 01 EXPERIENCE Holloway teaser (linking to /experience with the 0-to-~1,400 metric, id-guarded query), the renumbered Home sections (01/02/03/04) with ContactSection em-dash-clean, the enriched Person JSON-LD (jobTitle + alumniOf + hasCredential), the sharpened SEO descriptions, the revised first-person About copy (intro/P1/P3; P2 verbatim), the /about Education block (WGU B.S. CS May 2026 + Virginia Tech transfer + LPI Linux Essentials) with a render regression test, and the real 1200x630 og-default.png card.
  </what-built>
  <how-to-verify>
    1. COPY REVIEW (Claude drafted, you approve final wording -- CONTEXT discretion): run `pnpm dev` and read the revised copy in situ -- ABOUT_INTRO / ABOUT_P1 / ABOUT_P3 on `/about` and the Home ABOUT preview (confirm ABOUT_P2 is unchanged), the teaser summary sentence + the one metric line on Home, the sharpened Home + /about SEO descriptions (view source `<meta name="description">`), and the Education block labels. Confirm each reads as an honest new-grad software engineer with production experience, first person, no self-diminishing label, no senior/5+ years framing, and zero em dashes. Reply with edits or approval.
    2. FRONTEND-DESIGN 6-PILLAR SIGN-OFF at the required viewports (D-20; review fix #6, per UI-SPEC lines 99-103): capture and review screenshots at BOTH 375px and 1440px of the Home teaser and the /about education block. Confirm the 6 pillars (copywriting, visuals, color, typography, spacing, registry-N/A) against design-system/MASTER.md AND these interaction checks: keyboard-focus shows the `2px solid var(--accent)` focus ring on the teaser deep-link; `prefers-reduced-motion` drops the arrow opacity reveal while keeping the color-change affordance; and at 375px the long teaser highlight WRAPS with NO horizontal overflow (the eyebrow may wrap to two lines). Record PASS per pillar + per interaction check.
    3. JSON-LD VALIDITY (POS-04 / RESEARCH A1; review fix #6 -- correct validator): paste the built Home page (or its ld+json) into the SCHEMA MARKUP VALIDATOR at validator.schema.org (NOT the Google Rich Results Test -- Rich Results checks rich-result *eligibility* and Google's profile feature expects ProfilePage with Person as mainEntity, whereas this site emits a standalone Person; validator.schema.org is the correct tool for a standalone Person). Confirm the Person entity parses with no errors -- jobTitle "Software Engineer", alumniOf (WGU + Virginia Tech), hasCredential (LPI Linux Essentials + WGU B.S. degree, Virginia Tech NOT a credential).
    4. OG CARD: open public/og-default.png and confirm it is the real 1200x630 editorial card on the six-token palette with Geist type (not the placeholder).
  </how-to-verify>
  <resume-signal>Reply "approved" (or list copy edits / design changes to apply before merge).</resume-signal>
  <verify>
    <human-check>Jack confirms: copy reads as an honest new-grad software engineer with production experience (first person, no self-diminishing label, no senior/5+ years framing, zero em dashes); frontend-design 6 pillars PASS at 375px AND 1440px on the teaser, education block, and /about layout, with keyboard-focus ring + reduced-motion + no-horizontal-overflow verified; validator.schema.org parses the standalone Person entity with no errors; og-default.png is the real 1200x630 card.</human-check>
  </verify>
  <done>Jack replied "approved" (or all requested edits were applied and re-approved); all four sign-offs recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset -> social/link preview | og-default.png is served as a static file; no user input, no runtime processing |
| build artifacts -> chat pipeline | portfolio-context.json is regenerated at build from chat inputs this phase does not touch (D-17) |
| phase-start baseline -> capstone assertion | the capstone trusts 24-BASELINE.json to represent the true phase-start state of protected files + dependencies + the og placeholder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03 | Tampering (invariant break) | chat-surface via BaseLayout.astro / global.css / chat inputs | low | mitigate | D-19/D-17: the capstone runs scripts/verify-phase24-invariants.mjs to prove BaseLayout.astro + global.css + all chat files match their phase-start hashes across ALL committed tasks and portfolio-context.json regenerates byte-identical; the untouched D-26 battery + sse-snapshot run green in the full suite (review fix #4) |
| T-24-05 | Spoofing (misleading preview) | og-default.png content | low | accept | Author-controlled static card; content reviewed at the human checkpoint against MASTER.md and the honest-register copy rules; the verifier proves it is a real 1200x630 PNG distinct from the placeholder |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero package installs across the phase (D-19); this gate asserts package.json dependencies byte-identical to the phase-start baseline (QA-02); the OG verifier + Gate A use Node built-ins / existing vitest only |
</threat_model>

<verification>
- `node scripts/verify-phase24-og.mjs` exits 0 (real 1200x630 PNG, signature valid, size capped, differs from the placeholder hash)
- `pnpm build` + `pnpm test` + `pnpm exec astro check` all green (full suite incl Gate A + Gates B/C/D/E + about-education-render + D-26 battery + sse-snapshot)
- `node scripts/verify-phase24-invariants.mjs` exits 0 (protected files + dependencies match the phase-start baseline; review fix #4)
- Human checkpoint approved: copy review + frontend-design 6-pillar sign-off at 375px/1440px with focus/reduced-motion/overflow checks + Schema Markup Validator + OG card confirmation
</verification>

<success_criteria>
- Site metadata (SEO description + JSON-LD Person schema + OG card) reflects the updated positioning and job title (POS-04, SC4)
- The phase holds every cross-cutting guardrail (D-17, D-19, QA-01, QA-02) with no new runtime dependencies, proven against the phase-start baseline (review fix #4)
- Gate A is authored + GREEN; the OG asset is real and provably distinct from the placeholder (review fix #1, #5)
- Jack approved the final copy and frontend-design signed off all visual surfaces at both required viewports (D-20; review fix #6)
</success_criteria>

## Review Findings Addressed (Phase 24 cross-AI review, codex gpt-5.6-sol)

- **#4 (HIGH) working-tree checks cannot prove phase-wide invariants after commits**: replaced the `git diff --name-only` / `git status --porcelain` checks with `node scripts/verify-phase24-invariants.mjs`, which compares current protected-file hashes + the dependencies object against the 24-01 phase-start baseline -- catching an accidental BaseLayout / global-CSS / chat / dependency edit committed anywhere in 24-01..03.
- **#5 (HIGH) OG production path contradictory**: Task 1 now produces the PNG via ONE preflighted deterministic mechanism and fails closed (STOP, not spec-only) if unavailable; the human checkpoint confirms the file, it does not create it.
- **#5 / MEDIUM (dimension check does not distinguish the placeholder)**: scripts/verify-phase24-og.mjs adds a PNG-signature check, a max-file-size cap, and a SHA-256 difference against the phase-start placeholder hash on top of the 1200x630 dimension check.
- **#1 (HIGH) intentionally-RED gate**: Gate A is authored HERE (the point where all five scanned files are clean), so it is GREEN at its own boundary.
- **#6 (MEDIUM) wrong structured-data validator**: the checkpoint uses the Schema Markup Validator (validator.schema.org) for the standalone Person, not the Google Rich Results Test. DEFERRAL/REJECTION: this phase intentionally does NOT adopt ProfilePage (that would change the emitted schema shape and is out of the POS-04 scope), so Rich Results is not the right tool.
- **#6 (MEDIUM) visual verification lacks concrete viewports/interactions**: the checkpoint now requires screenshots at 375px AND 1440px plus keyboard-focus, reduced-motion, and horizontal-overflow checks per UI-SPEC lines 99-103.

## Artifacts this phase produces

New/updated artifacts introduced by THIS plan (exclude from source-drift verification):
- `public/og-default.png` (real 1200x630 editorial card, replacing the placeholder)
- `scripts/verify-phase24-og.mjs` (deterministic OG verifier; Node built-ins only)
- `tests/content/site-copy-em-dash.test.ts` (Gate A; authored here per review fix #1)

<output>
Create `.planning/phases/24-positioning-shift-home-teaser/24-04-SUMMARY.md` when done.
</output>
