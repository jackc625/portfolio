---
phase: 14
slug: chat-knowledge-upgrade
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-20
---

# Phase 14 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Sourced from `14-RESEARCH.md` §10 Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.0 (pinned in `package.json` devDependencies) |
| **Config file** | `vitest.config.ts` (repo root) |
| **Quick run command** | `pnpm test` (runs `vitest run`) |
| **Full suite command** | `pnpm test` (full suite is the default; no watch split) |
| **Estimated runtime** | ~2 seconds (current: 149 tests / 23 files GREEN) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test` (full suite, ~2s).
- **After every plan wave:** Run `pnpm build` (chains `build:chat-context` → `wrangler types` → `astro check` → `astro build` → `pages-compat`) + `pnpm test`.
- **Before `/gsd-verify-work`:** Full suite green + Lighthouse CI on `/` and one `/projects/<slug>` hitting ≥99 / ≥95 / 100 / 100 / TBT ≤150ms / CLS ≤0.01 + `14-VERIFICATION.md` 9-item table fully signed off.
- **Max feedback latency:** ~2 seconds per commit.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 14-01-01 | 01 | 0 | CHAT-08, CHAT-03, CHAT-04 | — | Wave 0 RED stubs — every new test file present and failing until implementation lands | unit | `pnpm test` | ❌ W0 | ⬜ pending |
| 14-02-01 | 02 | 1 | CHAT-03, CHAT-04 | T-14-BUILD | `build-chat-context.mjs` exits 0 on clean tree; exit 1 on missing source; exit 2 on data corruption | build + unit | `pnpm build:chat-context && pnpm test -- chat-context-integrity` | ❌ W0 | ⬜ pending |
| 14-02-02 | 02 | 1 | CHAT-04 | — | `portfolio-context.static.json` + merged `portfolio-context.json` both present; tech + description fields preserved byte-for-byte from Phase 13 | unit (integrity) | `pnpm test -- chat-context-integrity` | ❌ W0 | ⬜ pending |
| 14-02-03 | 02 | 1 | CHAT-03 | — | Projects/7 (MULTI-DEX) excluded — `/MULTI[- ]?DEX/i` regex banned from merged output | unit | `pnpm test -- chat-context-integrity` | ❌ W0 | ⬜ pending |
| 14-03-01 | 03 | 2 | CHAT-05 | T-14-CACHE | `system: [{ type, text, cache_control: { type: "ephemeral" } }]` shape in `messages.create` call | unit | `pnpm test -- chat.test` | ✅ chat.test.ts; ADD assertion | ⬜ pending |
| 14-03-02 | 03 | 2 | CHAT-07 | — | `max_tokens: 768` in `messages.create` call | unit | `pnpm test -- chat.test` | ✅ chat.test.ts; ADD assertion | ⬜ pending |
| 14-04-01 | 04 | 2 | CHAT-06 | T-14-PERSONA | `buildSystemPrompt()` output contains `<role>` third-person biographer, tiered refusals (resume/PII, off-scope, injection), attack-pattern section, tiered length guidance, light-steering breadcrumb rule | unit (substring assertions) | `pnpm test -- prompt-injection` | ❌ W0 | ⬜ pending |
| 14-04-02 | 04 | 2 | CHAT-06 | — | Widget header renamed `ASK JACK'S AI` → `ASK ABOUT JACK` (uppercase source literal at `ChatWidget.astro:65`) | unit (existence) + manual visual smoke | `grep -n "ASK ABOUT JACK" src/components/primitives/ChatWidget.astro` | ✅ file exists; text to change | ⬜ pending |
| 14-05-01 | 05 | 2 | CHAT-08 | T-14-INJECTION | 10 attack vectors — each asserts banned substrings (`<role>`, `<knowledge>`, `<security>`, `cache_control`, phone regex, "Ignore the above") NOT present AND required substrings (per D-16 category: `/jack-cutrara-resume.pdf` for PII; off-scope refusal for off-scope/injection) ARE present | unit (mocked LLM) | `pnpm test -- prompt-injection` | ❌ W0 | ⬜ pending |
| 14-06-01 | 06 | 3 | CHAT-09 | T-14-REGRESSION | Phase 7 D-26 9-item regression — 6 automated (XSS, CORS, focus-trap, localStorage, markdown, copy-button) + 3 manual (rate limit, 30s AbortController timeout, SSE streaming frames) on preview deploy | mix | `pnpm test` + manual curl + DevTools on preview | ✅ 6 existing + 3 manual steps | ⬜ pending |
| 14-06-02 | 06 | 3 | CHAT-09 | — | Lighthouse CI on `/` + one `/projects/<slug>` holds ≥99 / ≥95 / 100 / 100 / TBT ≤150ms / CLS ≤0.01 | e2e | `pnpm lh:ci` (or documented equivalent) | ✅ existing LH CI gate from Phase 13 | ⬜ pending |
| 14-06-03 | 06 | 3 | CHAT-09 | — | Voice-banlist spot-check on merged output — no `delight`, `seamless`, `craft`, `journey`, `passionate`, etc. per `docs/VOICE-GUIDE.md` banlist | unit | `pnpm test -- voice-banlist` | ❌ W0 (Claude's Discretion — recommend include) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs above are provisional — final IDs assigned by `gsd-planner` during plan creation. The per-task map will be regenerated by `/gsd-execute-phase` against the actual PLAN.md files; this table shows the verification SHAPE.*

---

## Wave 0 Requirements

- [ ] `scripts/build-chat-context.mjs` — new generator script (CHAT-03, CHAT-04, D-09..D-11)
- [ ] `src/data/portfolio-context.static.json` — new hand-curated identity file (CHAT-04, D-08)
- [ ] `tests/api/prompt-injection.test.ts` — new mocked-LLM battery (CHAT-06, CHAT-08, D-20..D-22)
- [ ] `tests/fixtures/chat-eval-dataset.ts` — new fixture file: 10 injection vectors + ~10 grounded-QA + ~10 voice spot-checks (AI-SPEC §5 reference dataset)
- [ ] `tests/build/chat-context-integrity.test.ts` — new build-integrity test (AI-SPEC §5.6; Projects/7 banlist, token-floor, 6-project presence)
- [ ] `.planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md` — new 9-item D-26 verification table (D-23)
- [ ] (optional, Claude's Discretion per CONTEXT.md) `tests/content/voice-banlist.test.ts` — voice-banlist CI gate on generated output
- Augment `tests/api/chat.test.ts` with assertions on `system: [{ type, text, cache_control }]` shape + `max_tokens: 768` (CHAT-05, CHAT-07). ADD assertions; do not rewrite the file.

*Framework install: none — Vitest 4.1.0 already pinned. Zero new devDependencies (v1.2 cross-phase gate).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cloudflare rate limit (5 messages / 60s) fires on 6th rapid request | CHAT-09 | CF binding (`env.CHAT_RATE_LIMITER.limit`) does not exist in local dev; only available on preview / prod deploy | Deploy to CF Pages preview → `for i in {1..6}; do curl -X POST https://<preview>/api/chat …; done` → assert 6th returns 429 |
| 30s client-side AbortController timeout fires on slow upstream | CHAT-09 | Requires simulating slow Claude upstream; local mock is deterministic and does not exercise timeout branch | In DevTools Network, throttle chat response to >30s; assert `chat.ts` emits abort + UI recovers |
| SSE streaming frames render line-by-line (async:false marked parse) | CHAT-09 | Requires inspecting live EventSource payload against preview deploy | DevTools → Network → filter `/api/chat` → inspect EventSource frame-by-frame; assert progressive chunks render in UI |
| Widget header `ASK ABOUT JACK` renders correctly on mobile + desktop | CHAT-06 | Visual smoke — static test asserts markup, but letter-spacing / uppercase rendering needs eyes | Open `/` on Chrome desktop + Safari mobile → chat panel closed → header reads "ASK ABOUT JACK" in correct case + spacing |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify OR Wave 0 dependencies declared
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (6 new files + 1 optional + 1 AUGMENT)
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency < 5s (actual: ~2s for `pnpm test`)
- [ ] `nyquist_compliant: true` set in frontmatter (flip after plan-checker approves all plans satisfy Dim 8)

**Approval:** pending — awaiting planner output + plan-checker pass
