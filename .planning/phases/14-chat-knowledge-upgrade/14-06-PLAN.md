---
phase: 14-chat-knowledge-upgrade
plan: 06
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04, 14-05]
files_modified:
  - .planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md
  - .planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [CHAT-09]
threats_addressed: [T-14-REGRESSION, T-14-CACHE, T-14-INJECTION, T-14-PII-LEAK, T-14-PERSONA, T-14-BUILD, T-14-DATA-INTEGRITY]

must_haves:
  truths:
    - "14-VERIFICATION.md exists with a 9-row D-26 regression table"
    - "6 of 9 D-26 items have automated PASS evidence via pnpm test"
    - "3 of 9 D-26 items (rate limit, 30s AbortController, SSE streaming) have manual preview-deploy evidence paste"
    - "Lighthouse CI run against / + /projects/<slug> holds >=99/>=95/100/100 / TBT <=150ms / CLS <=0.01"
    - "Phase 14 SUMMARY documents which T-14 threat IDs are RETIRED and cites the mitigation evidence"
    - "ROADMAP Phase 14 row flips to Complete; STATE.md reflects phase close"
  artifacts:
    - path: ".planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md"
      provides: "9-row D-26 regression table + Lighthouse CI results + manual preview evidence"
      contains: "Phase 14"
    - path: ".planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md"
      provides: "Phase 14 close-out: CHAT-03..09 requirement status, threat retirement, follow-ups"
      contains: "CHAT-09"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 row flipped to Complete with plan-count and completion date"
      contains: "Phase 14"
    - path: ".planning/STATE.md"
      provides: "Updated position, decisions, progress counters reflecting Phase 14 complete"
      contains: "Phase 14"
  key_links:
    - from: "14-VERIFICATION.md rows 3, 4, 7"
      to: "Cloudflare Pages preview deploy URL"
      via: "manual curl + DevTools evidence pastes; binding/upstream-dependent items not exercisable in local dev"
      pattern: "MANUAL preview"
    - from: "14-VERIFICATION.md rows 1, 2, 5, 6, 8, 9"
      to: "pnpm test output"
      via: "per-row test-file reference + final pnpm test exit code"
      pattern: "tests/client/|tests/api/"
---

<objective>
Author `14-VERIFICATION.md` with the 9-row D-26 regression table per D-23 / CONTEXT.md / RESEARCH §8, execute the 6 automated rows via `pnpm test`, execute the 3 manual rows (rate limit, 30s AbortController, SSE streaming) against the Cloudflare Pages preview deploy, run Lighthouse CI on `/` and one `/projects/<slug>`, retire every T-14-* threat with cited evidence, and close Phase 14 by updating ROADMAP.md + STATE.md + authoring the phase SUMMARY.

Purpose: Implements CHAT-09 (Phase 7 regression battery passes) and closes Phase 14 formally. Serves as the phase gate before `/gsd-verify-work` is run.
Output: 1 new planning doc (14-VERIFICATION.md), 1 new close-out SUMMARY (14-SUMMARY.md), 2 updated planning state files (STATE.md, ROADMAP.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-chat-knowledge-upgrade/14-CONTEXT.md
@.planning/phases/14-chat-knowledge-upgrade/14-RESEARCH.md
@.planning/phases/14-chat-knowledge-upgrade/14-VALIDATION.md
@.planning/phases/14-chat-knowledge-upgrade/14-01-SUMMARY.md
@.planning/phases/14-chat-knowledge-upgrade/14-02-SUMMARY.md
@.planning/phases/14-chat-knowledge-upgrade/14-03-SUMMARY.md
@.planning/phases/14-chat-knowledge-upgrade/14-04-SUMMARY.md
@.planning/phases/14-chat-knowledge-upgrade/14-05-SUMMARY.md
@.planning/milestones/v1.0-phases/07-chatbot-feature/07-VERIFICATION.md
@.planning/phases/13-content-pass-projects-sync/13-09-PLAN.md

<interfaces>
D-26 9-item regression battery coverage (from CONTEXT.md D-23 + RESEARCH §8):

- Item 1 XSS sanitization: tests/client/markdown.test.ts
- Item 2 CORS exact-origin: tests/api/security.test.ts
- Item 3 Rate limit 5/60s via CF binding: MANUAL curl sequence on preview
- Item 4 30s AbortController timeout: MANUAL DevTools throttle on preview
- Item 5 Focus trap re-query: tests/client/focus-visible.test.ts
- Item 6 localStorage 50-msg cap / 24h TTL: existing chat.ts tests (partial)
- Item 7 SSE streaming line-by-line: MANUAL DevTools EventStream on preview
- Item 8 Markdown rendering DOMPurify strict: tests/client/markdown.test.ts
- Item 9 Copy-to-clipboard parity: tests/client/chat-copy-button.test.ts

T-14-* threat retirement cross-reference:

- T-14-CACHE: Plan 14-03 SDK-shape tests + Plan 14-04 JSON.stringify determinism
- T-14-PERSONA: Plan 14-01 describe block 3 assertions
- T-14-INJECTION: Plans 14-01 + 14-05 full injection battery + Plan 14-04 security section
- T-14-PII-LEAK: D-05 resume excluded, D-16 tier 1 refusal, GLOBAL_BANNED_REGEXES phone regex
- T-14-BUILD: Plan 14-02 allow-list + regex guard, Plan 14-01 Task 3 integrity test
- T-14-REGRESSION: THIS PLAN (9-item checklist + Lighthouse CI pass)
- T-14-DATA-INTEGRITY: Plan 14-01 Task 3 token floor >=4096, Plan 14-02 normalize() CRLF, generator determinism
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 14-VERIFICATION.md template with 9-row D-26 table</name>
  <read_first>
    - .planning/phases/14-chat-knowledge-upgrade/14-RESEARCH.md §8 (9-row table template)
    - .planning/phases/14-chat-knowledge-upgrade/14-CONTEXT.md D-23
    - .planning/milestones/v1.0-phases/07-chatbot-feature/07-VERIFICATION.md (analog heading structure)
    - .planning/phases/13-content-pass-projects-sync/13-09-PLAN.md (Phase 13 regression-gate plan shape)
  </read_first>
  <files>.planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md` — the template Task 2 fills with automated results and Task 3 fills with manual preview evidence + Lighthouse scores.

    Write the full file with this structure (exact content follows; keep all `pending` markers for Tasks 2 + 3 to fill):

    **Each numbered item below maps to an `## H2` section heading in the generated doc (except item 1, which is the file's H1). Bold in this plan indicates the section TITLE; it must be rendered as an H2 heading (e.g., `## Purpose`, `## Scope`, `## Preconditions`, `## Verification Table`, `## Lighthouse CI Gate`, `## Automated Test Results`, `## Threat Retirement`, `## Sign-off`), not as bold inline text.**

    1. H1: `# Phase 14: D-26 Regression Gate Verification`
    2. **Purpose** paragraph — CHAT-09 acceptance criterion, re-verifies Phase 7 battery after Phase 14 upgrades.
    3. **Scope** paragraph — names the 4 files Phase 14 touched (chat.ts cache_control+max_tokens, system-prompt.ts rewrite, ChatWidget.astro header, portfolio-context.json now generated).
    4. **Preconditions** bullet list — Plans 14-01..14-05 complete, preview deploy URL available, wrangler authenticated.
    5. **Verification Table** — the 9-row table. Columns: `# | Item | Evidence Source | Automation | Manual Step | Status`. Status column uses `pending` until filled. Use the RESEARCH §8 template rows verbatim:
       - Row 1: XSS sanitization, tests/client/markdown.test.ts, automated PASS/FAIL, optional manual paste of `<script>alert(1)</script>` into chat input.
       - Row 2: CORS exact-origin, tests/api/security.test.ts, automated PASS/FAIL, no manual.
       - Row 3: Rate limit 5/60s, N/A local, MANUAL preview curl loop: `for i in $(seq 1 7); do curl -sS -o /dev/null -w "%{http_code}\n" -X POST -H "Origin: <preview-url>" -H "Content-Type: application/json" -d '{"messages":[{"role":"user","content":"hi"}]}' <preview-url>/api/chat; done` expect five 200s then 429.
       - Row 4: 30s AbortController, N/A local, MANUAL DevTools Network → Slow 3G throttle, observe abort + UI recovery after 30s.
       - Row 5: Focus trap re-query, tests/client/focus-visible.test.ts, automated PASS/FAIL.
       - Row 6: localStorage 50-msg / 24h TTL, existing chat.ts persistence tests (partial), optional manual: send 2 messages, reload within 1 min (rehydrate), then clear localStorage timestamp and reload (cleared).
       - Row 7: SSE streaming, N/A local, MANUAL DevTools → Network → filter /api/chat → EventStream tab, verify progressive frames + Content-Encoding: none header + Content-Type: text/event-stream.
       - Row 8: Markdown rendering DOMPurify strict, tests/client/markdown.test.ts, automated PASS/FAIL.
       - Row 9: Copy-to-clipboard parity, tests/client/chat-copy-button.test.ts, automated PASS/FAIL.
    6. **Lighthouse CI Gate** table — 2 rows (`/`, `/projects/seatwatch`); columns Performance / A11y / BP / SEO / TBT / CLS / Status; all cells `TBD`.
    7. **Automated Test Results** table — Pre/Post counts (pre: 23 files / 149 tests) + deltas; all post cells `TBD`.
    8. **Threat Retirement** table — 7 rows mapping T-14-* IDs to mitigation plan + evidence + Status (all `pending`).
    9. **Sign-off** checklist — 6 checkboxes (9 D-26 rows closed, Lighthouse gate holds, 7 threats retired, full pnpm test GREEN, pnpm build clean, CF Pages dashboard command verified = pnpm build default).
    10. **Approver** and **Date** placeholders at the bottom.

    File ends with trailing newline.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md && grep -c "D-26" .planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md` exists
    - `grep -c "^# Phase 14: D-26 Regression Gate Verification" 14-VERIFICATION.md` returns 1
    - `grep -c "^## Verification Table" 14-VERIFICATION.md` returns 1
    - `grep -c "^## Lighthouse CI Gate" 14-VERIFICATION.md` returns 1
    - `grep -c "^## Threat Retirement" 14-VERIFICATION.md` returns 1
    - `grep -c "^## Sign-off" 14-VERIFICATION.md` returns 1
    - The Verification Table has exactly 9 data rows matching items 1-9 above
    - The Threat Retirement table has exactly 7 rows for T-14-CACHE, T-14-PERSONA, T-14-INJECTION, T-14-PII-LEAK, T-14-BUILD, T-14-REGRESSION, T-14-DATA-INTEGRITY
    - At least 15 `pending` or `TBD` markers remain (cells for Tasks 2 + 3 to fill)
    - `grep -c "MANUAL preview" 14-VERIFICATION.md` returns at least 3 (rows 3, 4, 7)
  </acceptance_criteria>
  <done>
    14-VERIFICATION.md template exists with full shape; all evidence cells marked pending; ready for Task 2 to fill automated rows + Task 3 to fill manual + Lighthouse rows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute automated verification rows + fill 14-VERIFICATION.md automated cells</name>
  <read_first>
    - .planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md (just authored)
    - tests/client/markdown.test.ts, tests/api/security.test.ts, tests/client/focus-visible.test.ts, tests/client/chat-copy-button.test.ts (reference — confirm they exist)
  </read_first>
  <files>.planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md</files>
  <action>
    Run the automated regression battery and record results in 14-VERIFICATION.md. Steps:

    1. Run `pnpm test -- markdown.test` → capture pass count → fill Row 1 Status: `PASS (<N> tests)` + date.
    2. Run `pnpm test -- security.test` → fill Row 2 Status.
    3. Row 3 stays `MANUAL preview: deferred to Task 3`.
    4. Row 4 stays `MANUAL preview: deferred to Task 3`.
    5. Run `pnpm test -- focus-visible` → fill Row 5 Status.
    6. Run `pnpm test -- chat` (localStorage coverage) → fill Row 6 Status with partial-coverage note.
    7. Row 7 stays `MANUAL preview: deferred to Task 3`.
    8. Row 8 already covered by Row 1's markdown.test → fill Row 8 Status: `PASS (tests/client/markdown.test.ts — shared coverage with Row 1)`.
    9. Run `pnpm test -- chat-copy-button` → fill Row 9 Status.

    10. Run full `pnpm test` — capture total file count + total test count + pass count; fill Automated Test Results table:
       - Pre-Phase-14: 23 files / 149 tests / 149 passing (from STATE.md pre-phase baseline)
       - Post-Phase-14: actual counts from `pnpm test` output
       - Delta: post minus pre

    11. Fill Threat Retirement table Status columns for EVERY threat that is satisfied by a GREEN automated test:
       - T-14-CACHE → `RETIRED — chat.test.ts SDK request shape (5 tests GREEN) + cache_control literal present in chat.ts`
       - T-14-PERSONA → `RETIRED — prompt-injection.test.ts block 3 (18 tests GREEN)`
       - T-14-INJECTION → `RETIRED — prompt-injection.test.ts full battery (37+ tests GREEN)`
       - T-14-PII-LEAK → `RETIRED — D-05 resume exclusion confirmed; RESUME_REFUSAL present in system prompt; GLOBAL_BANNED_REGEXES phone regex asserted on all mocked responses`
       - T-14-BUILD → `RETIRED — chat-context-integrity.test.ts (9 tests GREEN); grep -c "MULTI-DEX" src/data/portfolio-context.json = 0`
       - T-14-REGRESSION → stay `pending — awaiting Task 3 manual preview verification`
       - T-14-DATA-INTEGRITY → `RETIRED — chat-context-integrity token floor test GREEN; pnpm build:chat-context:check idempotent exit 0`

    12. Do NOT fill the Lighthouse table rows (Task 3 owns).
    13. Do NOT fill sign-off checkboxes yet (Task 3 closes them).
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -10 && grep -c "RETIRED" .planning/phases/14-chat-knowledge-upgrade/14-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Full `pnpm test` exit code 0
    - Rows 1, 2, 5, 6, 8, 9 of Verification Table have Status = `PASS (<file>, N tests, <date>)` — no `pending` markers remain in automated rows
    - Rows 3, 4, 7 still marked `MANUAL preview: deferred to Task 3`
    - Automated Test Results table has concrete post-plan numbers (not TBD)
    - Threat Retirement table has 6 of 7 rows marked `RETIRED` with cited evidence; only T-14-REGRESSION remains `pending`
    - No changes to the table structure or column headers
  </acceptance_criteria>
  <done>
    All 6 automated D-26 rows PASS-verified with test-count + date evidence. 6 of 7 threats RETIRED. Full pnpm test GREEN. 14-VERIFICATION.md ready for Task 3 manual preview work.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual preview-deploy verification + Lighthouse CI</name>
  <what-built>
    Plans 14-01 through 14-05 shipped the full Phase 14 upgrade:
    - scripts/build-chat-context.mjs generating portfolio-context.json at build time
    - src/data/portfolio-context.static.json (hand-curated identity)
    - src/pages/api/chat.ts with cache_control: ephemeral + max_tokens: 768
    - src/prompts/system-prompt.ts rewritten for third-person biographer + tiered refusals + attack-pattern list
    - src/components/chat/ChatWidget.astro header renamed ASK ABOUT JACK
    - tests/api/prompt-injection.test.ts + tests/fixtures/chat-eval-dataset.ts (37+ tests)
    - tests/build/chat-context-integrity.test.ts (9 tests)

    Task 2 just closed 6 of 9 D-26 regression items via pnpm test and retired 6 of 7 T-14-* threats. This checkpoint closes the remaining 3 D-26 rows (rate limit, AbortController, SSE) and runs Lighthouse CI — both require a Cloudflare Pages preview deploy.
  </what-built>
  <how-to-verify>
    **Step 1 — Deploy to CF Pages preview:**
    ```bash
    # From a clean working tree on the Phase 14 branch:
    pnpm build
    # Push branch to origin; Cloudflare Pages auto-builds a preview. OR:
    pnpm wrangler pages deploy dist --project-name=<project> --branch=<branch>
    # Record the preview URL, e.g. https://phase-14.<project>.pages.dev
    ```

    **Step 2 — Row 3 (Rate limit 5/60s):** From your laptop:
    ```bash
    PREVIEW=https://<preview-url>
    for i in $(seq 1 7); do
      curl -sS -o /dev/null -w "%{http_code}\n" -X POST \
        -H "Origin: $PREVIEW" \
        -H "Content-Type: application/json" \
        -d '{"messages":[{"role":"user","content":"hi"}]}' \
        $PREVIEW/api/chat
    done
    ```
    Expected output: 5 lines of `200` then `429` on the 6th and 7th (rate-limiter binding fires).

    Paste the literal curl output into Row 3 Status. If 6th/7th are NOT 429: fail the checkpoint; debug the CF Pages rate-limiter binding in wrangler.jsonc (do NOT edit in this plan — escalate).

    **Step 3 — Row 4 (30s AbortController):** In Chrome DevTools against the preview URL:
    1. Open Network tab → Throttling dropdown → select "Slow 3G" (or custom "Offline after N seconds" throttle).
    2. Open the chat widget, type any question, press Enter.
    3. Observe: after ~30s the client emits an abort (chat.ts AbortController pathway); UI returns to ready state (not stuck on typing indicator).
    4. Paste observed behavior + approximate timestamp into Row 4 Status.

    **Step 4 — Row 7 (SSE streaming frames):** In Chrome DevTools against preview:
    1. Open Network tab, no throttling.
    2. Open chat, send a message.
    3. Click the `/api/chat` request in Network tab → EventStream tab.
    4. Verify: multiple `data: {"text":"..."}` frames arriving one at a time → terminating with `data: [DONE]`.
    5. Click Headers tab → verify response headers include `Content-Type: text/event-stream` AND `Content-Encoding: none`.
    6. Paste a frame excerpt (or screenshot filename) + the Content-Encoding header value into Row 7 Status.

    **Step 5 — Lighthouse CI:** Run Lighthouse (headless Chrome or dashboard) against the preview URL, both `/` and `/projects/seatwatch`. Record Performance / Accessibility / Best Practices / SEO / TBT / CLS into the Lighthouse CI Gate table. Gate thresholds: Perf ≥99, A11y ≥95, BP 100, SEO 100, TBT ≤150ms, CLS ≤0.01.

    **Step 6 — CF Pages build command check (L5 landmine):** Visit the CF Pages dashboard → Settings → Build & deployments → confirm "Build command" reads `pnpm build` (default) and not a custom override. Paste screenshot filename or literal value into the Sign-off section.

    **Step 7 — Retire T-14-REGRESSION:** If Steps 2-5 all pass, update the Threat Retirement row for T-14-REGRESSION to `RETIRED — 9-item D-26 gate closed; Lighthouse CI hold confirmed`.

    **Step 8 — Close Sign-off checkboxes:** Mark each of the 6 Sign-off checkboxes. Fill Approver (Jack Cutrara) and Date.
  </how-to-verify>
  <resume-signal>
    Reply with "verified" (all rows green, Lighthouse holds, T-14-REGRESSION retired) OR describe specific failures:
    - Rate-limit regression → which HTTP code appeared where
    - AbortController UI hang → at what timestamp
    - SSE frame boundary failure → what Content-Encoding value appeared
    - Lighthouse below gate → which metric regressed and by how much
    - CF Pages build command override → what value is configured
  </resume-signal>
  <acceptance_criteria>
    - 14-VERIFICATION.md Rows 3, 4, 7 all have Status = PASS with literal evidence pasted (curl output / DevTools observation / frame log)
    - Lighthouse CI Gate table rows both filled with actual numbers; all 6 metrics per row hit the threshold
    - Threat Retirement table row T-14-REGRESSION marked RETIRED
    - All 6 Sign-off checkboxes marked
    - Approver + Date filled
    - No `pending` or `TBD` markers remain anywhere in 14-VERIFICATION.md
  </acceptance_criteria>
  <done>
    9 of 9 D-26 items PASS; Lighthouse CI holds; 7 of 7 T-14-* threats RETIRED; 14-VERIFICATION.md complete and signed. CHAT-09 closed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author 14-SUMMARY.md phase close-out</name>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - 14-01-SUMMARY.md through 14-05-SUMMARY.md (plan-level summaries for cross-reference)
    - 14-VERIFICATION.md (completed in Task 3)
    - .planning/STATE.md (for accumulated-context aggregation)
    - .planning/ROADMAP.md §Phase 14 (success criteria)
  </read_first>
  <files>.planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md</files>
  <action>
    Create `.planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md` following the summary template structure. Sections and required content:

    1. **H1:** `# Phase 14: Chat Knowledge Upgrade — Summary`
    2. **Phase metadata:** Phase ID, branch, start date, end date, total plans, total commits.
    3. **Requirements closed:** Table of CHAT-03 through CHAT-09 with Status=Closed + citation of the plan/test/evidence that proves each:
       - CHAT-03: Plan 14-02 — scripts/build-chat-context.mjs integrated into build chain
       - CHAT-04: Plan 14-02 — portfolio-context.static.json + generated portfolio-context.json split
       - CHAT-05: Plan 14-03 — cache_control: ephemeral asserted via chat.test.ts SDK shape tests
       - CHAT-06: Plan 14-04 — buildSystemPrompt rewrite, prompt-injection.test.ts block 3 GREEN
       - CHAT-07: Plan 14-03 — max_tokens: 768 asserted via chat.test.ts
       - CHAT-08: Plans 14-01 + 14-05 — 10-vector D-22 battery + drift guard, 37+ tests GREEN
       - CHAT-09: Plan 14-06 (this plan) — 9-item D-26 regression + Lighthouse CI confirmed
    4. **Decisions realized:** reference all 24 D-01..D-24 decisions — for each, one-line note confirming implementation (or explicit deferral with source). Focus on non-trivial decisions: D-04 Projects/7 exclusion (allow-list + regex guard + test banlist), D-06 truncation (Daytrade actual word count + marker), D-12 single cache_control breakpoint, D-16 refusal copy verbatim.
    5. **Threat retirement:** Table of all 7 T-14-* with status RETIRED + evidence.
    6. **Metrics:** test count pre/post, chat.ts LOC delta (~+6), system-prompt.ts LOC delta (~+70), generator LOC (new ~200), zero-new-deps gate held, Lighthouse scores.
    7. **Decisions logged during execution:** Aggregate any "during execution" decisions from 14-01..14-05 SUMMARYs that Jack should know about for future phases.
    8. **Open follow-ups:** list deferred items per CONTEXT.md Deferred Ideas that are NOT now in scope:
       - Live-API injection test suite (manual-only gate)
       - Cache-hit-rate observability (Phase 15 Analytics placeholder)
       - Extended 1-hour cache TTL (premature at portfolio traffic)
       - Projects/7 inclusion (requires v1.3+ audit)
    9. **Next phase:** Phase 15 Analytics Instrumentation (ROADMAP §Phase 15); cite the ANAL-03 hook that forwards chat:analytics CustomEvent + the F6 cache-hit-rate placeholder from 14-AI-SPEC §6.
    10. **Sign-off:** final checkbox list mirroring 14-VERIFICATION.md Sign-off section.

    File ends with trailing newline. Use Markdown tables, bullet lists, inline code fencing for file paths. Do NOT use emoji. Voice is third-person factual ("Phase 14 delivered...", "Plan 14-03 replaced...").
  </action>
  <verify>
    <automated>test -f .planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md && grep -c "CHAT-" .planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md && grep -c "T-14-" .planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md` exists
    - Contains H1 `# Phase 14: Chat Knowledge Upgrade — Summary`
    - Every requirement CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08, CHAT-09 referenced with Closed status + evidence citation
    - Every threat T-14-CACHE, T-14-PERSONA, T-14-INJECTION, T-14-PII-LEAK, T-14-BUILD, T-14-REGRESSION, T-14-DATA-INTEGRITY marked RETIRED with evidence
    - Every decision D-01 through D-24 cited at least once (grep `grep -c "D-[012][0-9]" 14-SUMMARY.md` returns at least 24)
    - Open follow-ups section names at least 4 deferred items by explicit decision reference
    - Next phase reference cites Phase 15 + ANAL-03
    - No emoji in file
  </acceptance_criteria>
  <done>
    Phase 14 SUMMARY committed; requirements + threats + decisions + metrics + follow-ups + handoff to Phase 15 all documented.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md + STATE.md to reflect Phase 14 complete</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 14 row and v1.2 milestone progress table)
    - .planning/STATE.md (Current Position, Accumulated Context sections)
    - 14-VERIFICATION.md, 14-SUMMARY.md (just authored)
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    **Part A — Update ROADMAP.md:**

    1. In the `### 🚧 v1.2 Polish (In Progress)` section, flip the Phase 14 line from:
       ```
       - [ ] **Phase 14: Chat Knowledge Upgrade** — Build-time context generation, Anthropic prompt caching, tuned persona, prompt-injection hardening
       ```
       to:
       ```
       - [x] **Phase 14: Chat Knowledge Upgrade** — Build-time context generation, Anthropic prompt caching, tuned persona, prompt-injection hardening (completed <YYYY-MM-DD>)
       ```
       Use actual date `date +%Y-%m-%d` at execution time.

    2. In the Progress table at the bottom, update the Phase 14 row:
       ```
       | 14. Chat Knowledge Upgrade | v1.2 | 6/6 | Complete | <YYYY-MM-DD> |
       ```
       (6 plans total: 14-01 through 14-06.)

    3. In the `### Phase 14: Chat Knowledge Upgrade` details block, change `**Plans**: TBD` to `**Plans**: 6 plans` and add the plan list:
       ```
       - [x] 14-01-PLAN.md — Test stubs + eval fixture (Wave 0 RED)
       - [x] 14-02-PLAN.md — Generator + static/generated split + build-chain wiring (CHAT-03, CHAT-04)
       - [x] 14-03-PLAN.md — SDK wire-up: cache_control + max_tokens (CHAT-05, CHAT-07)
       - [x] 14-04-PLAN.md — System prompt rewrite + widget header rename (CHAT-06)
       - [x] 14-05-PLAN.md — Injection battery GREEN + drift-guard (CHAT-08)
       - [x] 14-06-PLAN.md — D-26 regression + Lighthouse CI + phase close-out (CHAT-09)
       ```

    **Part B — Update STATE.md:**

    1. Update `stopped_at` to `Phase 14 complete — ready for /gsd-verify-work`.
    2. Update `last_updated` to current timestamp.
    3. Update `last_activity` to `<YYYY-MM-DD> -- Phase 14 complete`.
    4. Update `progress:` block: `completed_phases` +1 (becomes 3); `completed_plans` +6 (adds 14-01..14-06); recalculate `percent`.
    5. In `## Current Position`:
       - Update `Phase:` line to `Phase: 14 (chat-knowledge-upgrade) — COMPLETE (ready for /gsd-verify-work)`
       - Update `Plan:` line to `Plan: 6 of 6 complete`
       - Rewrite the `Status:` paragraph to mirror the Phase 13 close-out shape: requirements closed, threats retired, Lighthouse hold confirmed, next: `/gsd-verify-work` or `/gsd-discuss-phase 15`.
       - Update Progress bar: `Progress: [██████████] 100% (6 / 6 plans)`
    6. In `## Accumulated Context` → `### Decisions`, add a new `**Phase 14 decisions:**` block aggregating any execution-time decisions from Tasks 4's plan-level summaries (refusal copy finalization, truncation boundary, any research-recommendation divergence).
    7. Update `## Session Continuity`: `Last session` timestamp, `Stopped at: Phase 14 complete`, `Resume file: .planning/phases/14-chat-knowledge-upgrade/14-SUMMARY.md`.
    8. In `## Deferred Items` table, append rows for Phase 14 deferred items per CONTEXT.md: live-API injection suite, cache hit-rate observability, 1-hour TTL, Projects/7 inclusion.
  </action>
  <verify>
    <automated>grep -c "Phase 14: Chat Knowledge Upgrade" .planning/ROADMAP.md && grep "14. Chat Knowledge Upgrade" .planning/ROADMAP.md && grep "stopped_at" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[x\\] \\*\\*Phase 14" .planning/ROADMAP.md` returns 1 (flipped from [ ] to [x])
    - `grep "14. Chat Knowledge Upgrade" .planning/ROADMAP.md` returns a line containing `Complete` and a 2026-04-XX date
    - ROADMAP Phase 14 detail block: `grep -c "14-01-PLAN.md\|14-02-PLAN.md\|14-03-PLAN.md\|14-04-PLAN.md\|14-05-PLAN.md\|14-06-PLAN.md" .planning/ROADMAP.md` returns 6
    - STATE.md `stopped_at:` line contains `Phase 14 complete`
    - STATE.md Current Position Phase line contains `COMPLETE`
    - STATE.md progress: `completed_phases: 3` (was 2) and `completed_plans: 21` (was 15; +6)
    - STATE.md has new `**Phase 14 decisions:**` block under Accumulated Context
    - `pnpm check` clean (no parse regression in any TypeScript file); `pnpm test` full suite GREEN
  </acceptance_criteria>
  <done>
    ROADMAP + STATE flip Phase 14 to complete; plan list populated; session-continuity resume pointer set to 14-SUMMARY.md; Phase 14 decisions logged in Accumulated Context for future phases.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview deploy → prod | Manual verification happens on preview; items 3/4/7 must match production behavior (same rate-limiter binding, same Anthropic upstream) |
| Planning docs → future phases | 14-SUMMARY.md and STATE.md Accumulated Context feed next phase planner; stale or wrong content misleads Phase 15 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-REGRESSION | Tampering / DoS (any Phase 7 D-26 invariant broken) | 9-row checklist + Lighthouse CI | mitigate | 6 items automated via pnpm test (Task 2); 3 items manually verified on preview deploy with literal curl/DevTools evidence pasted (Task 3); Lighthouse CI gates Performance/A11y/BP/SEO/TBT/CLS. All 9 must PASS before Task 3 signs off. |
| T-14-CACHE | DoS (cache-silent-disable regression) | Plan 14-03 chat.test.ts SDK shape block (verified here) | retire | chat.test.ts `SDK request shape` 5 tests GREEN in full suite run. Source-regex assertions guard against future system:string collapses. |
| T-14-INJECTION | Elevation of Privilege (jailbreak regression) | Plan 14-01 + 14-05 prompt-injection.test.ts (verified here) | retire | 37+ tests GREEN; 10 D-22 vectors pass banned+required gate; drift-guard bidirectional invariants hold. |
| T-14-PII-LEAK | Information Disclosure | Plan 14-04 system prompt (verified here) | retire | RESUME_REFUSAL in `<security>` block; phone regex asserted absent from every mocked response; resume PDF text never entered knowledge base per D-05. |
| T-14-PERSONA | Spoofing (warm-colleague voice regression) | Plan 14-04 system prompt (verified here) | retire | buildSystemPrompt contract block 18 tests GREEN; third-person framing asserted; first-person regex negated. |
| T-14-BUILD | Tampering (Projects/7 leak via generator) | Plan 14-02 + 14-01 Task 3 (verified here) | retire | chat-context-integrity 9 tests GREEN including MULTI-DEX regex banlist; generator's allow-list guard rejects drift. |
| T-14-DATA-INTEGRITY | DoS (cache-bust via token floor undershoot) | Plan 14-02 generator + Plan 14-01 Task 3 (verified here) | retire | Token floor asserted >= 4096; `pnpm build:chat-context:check` idempotent; CRLF normalization applied on every file read. |
</threat_model>

<verification>
- 14-VERIFICATION.md complete with all 9 D-26 rows PASS, Lighthouse CI hold confirmed, all 7 T-14-* threats RETIRED
- 14-SUMMARY.md complete with requirements + threats + decisions + follow-ups
- ROADMAP.md Phase 14 row flipped to [x] Complete with 6-plan list
- STATE.md reflects Phase 14 complete, session continuity set to 14-SUMMARY.md
- Full `pnpm test` suite GREEN
- `pnpm build` clean end-to-end
</verification>

<success_criteria>
- CHAT-09 closed — 9-item D-26 regression battery PASSES with evidence
- Lighthouse CI on `/` + `/projects/seatwatch` holds the v1.2 threshold gate
- 7 of 7 T-14-* threats RETIRED with cited evidence
- All 7 CHAT-XX requirements closed in 14-SUMMARY.md
- ROADMAP v1.2 Progress reflects 3 of 5 phases complete (12, 13, 14)
- STATE.md session continuity points at 14-SUMMARY.md
- Zero new runtime dependencies added across the phase
</success_criteria>

<output>
Task 4 already creates the 14-SUMMARY.md — no additional close-out artifact needed. Task 5 updates the durable planning state. After this plan, the natural next invocations are:
- `/gsd-verify-work` to independently audit Phase 14 completion, OR
- `/gsd-discuss-phase 15` to move onto Analytics Instrumentation.
</output>
