---
phase: 23-projects-reconciliation-featured-tier
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified: []
autonomous: false
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04]

must_haves:
  truths:
    - "pnpm build and pnpm test are green end-to-end - the D-15 skip is validated through the full build chain (QA-02)"
    - "Chat context is still exactly 6 projects; the build-output gates (no-MDX-in-worker, featured-tier-render) pass against fresh dist/"
    - "No new runtime dependencies were added across the phase (QA-02)"
    - "The #7 case-study copy (D-02/D-03) and the featured-tier visuals (SC5) are human-signed-off"
  artifacts: []
  key_links:
    - "the phase-gate pnpm build produces dist/ that the no-MDX-in-worker and featured-tier-render gates need"
    - "content revisions loop back through the Projects/7 fence + pnpm sync:projects, never the MDX body"
    - "frontend-design 6-pillar sign-off is the SC5 gate against 23-UI-SPEC.md + MASTER.md"
---

<!-- Review incorporation (Phase 23) — this plan addresses cross-AI review findings from 23-REVIEWS.md:
  - F3 (LOW): Generated-output drift guard added. Because `pnpm build` runs build:chat-context in WRITE mode (rewrites src/data/portfolio-context.json on drift), Task 1 now runs `pnpm build:chat-context:check` BEFORE `pnpm build` (clean baseline) AND `git diff --exit-code -- src/data/portfolio-context.json` AFTER `pnpm build` (catches a silent rewrite). Reflected in action step 0/1, verify block, acceptance, and <verification>.
  - F4 (LOW): The QA-02 no-new-dependency check is made concrete and includes the lockfile: `git diff --exit-code -- package.json pnpm-lock.yaml` (was only implied on package.json). Reflected in action step 5, verify block, acceptance, and <verification>.
-->

<objective>
Run the phase capstone gate: prove the whole build and full test suite are green end-to-end, confirm no new runtime dependencies, and capture the two required human sign-offs (Jack's review of the #7 case-study copy per D-02/D-03, and the frontend-design 6-pillar visual sign-off per SC5).

Purpose: The prior three plans landed the data, the gate coverage, and the UI. This plan is the single place a full `pnpm build` runs - which produces the fresh dist/ that the build-output gates (no-MDX-in-worker, featured-tier-render) require, and which validates the D-15 skip through the real build chain (build:chat-context -> astro check -> astro build). It is also where the two non-automatable gates (content honesty and visual sign-off) are recorded. This plan writes no source; it verifies and signs off.
Output: a green end-to-end gate and a recorded human approval.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-projects-reconciliation-featured-tier/23-VALIDATION.md
@.planning/phases/23-projects-reconciliation-featured-tier/23-UI-SPEC.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite phase gate - build, test, drift, dependency lock</name>
  <files>(none - runs build/test/drift scripts; writes no source)</files>
  <read_first>
    - package.json (the "build" script chain and the scripts: build:chat-context, sync:check, build:chat-context:check, test; and the dependencies block for the QA-02 no-new-dep lock)
    - .planning/phases/23-projects-reconciliation-featured-tier/23-VALIDATION.md (the phase-gate sampling row: build + full test + sync:check + build:chat-context:check)
  </read_first>
  <action>
    Run the full phase gate and confirm each exit code; if any gate is red, STOP and report the failure verbatim (do not paper over or skip):
    0. GENERATED-OUTPUT DRIFT GUARD (F3): run `pnpm build:chat-context:check` BEFORE `pnpm build` to establish a clean pre-build baseline. `pnpm build` runs `build:chat-context` in WRITE mode (scripts/build-chat-context.mjs:601-614 rewrites src/data/portfolio-context.json when drift exists), so running the :check AFTER the build can mask a regression. The design intends a byte-identical re-emit (the D-15 skip changes nothing about the 6 emitted projects), so the happy path has no drift; this guard catches a regression that would otherwise slip through silently.
    1. `pnpm build` - runs build:chat-context (validates the D-15 skip through the real chain), then wrangler types, then astro check (must be 0/0/0), then astro build (produces dist/ for the build-output gates). Immediately AFTER the build, run `git diff --exit-code -- src/data/portfolio-context.json` (F3): it MUST show NO diff - a non-zero exit means the write-mode `pnpm build` silently rewrote the chat corpus (a regression), and the gate FAILS.
    2. `pnpm test` - the full Vitest suite must be green: the site-side gates now cover 7 projects; the build-output gates (no-mdx-in-worker-bundle, featured-tier-render) pass against the fresh dist/; and the chat-side pins are still green at exactly 6 projects with the banned source absent.
    3. `pnpm sync:check` exits 0 (no #7 MDX-vs-source drift).
    4. `pnpm build:chat-context:check` exits 0 (portfolio-context.json still exactly 6 projects).
    5. QA-02 / D-17 dependency lock (F4): run `git diff --exit-code -- package.json pnpm-lock.yaml` - it MUST show NO diff, proving no runtime dependency was added anywhere in the phase (concrete no-new-dep check, lockfile included, not just package.json). Also confirm no schema change and astro check 0/0/0.
  </action>
  <verify>
    <automated>pnpm build:chat-context:check</automated>
    <automated>pnpm build</automated>
    <automated>git diff --exit-code -- src/data/portfolio-context.json</automated>
    <automated>pnpm test</automated>
    <automated>pnpm sync:check</automated>
    <automated>pnpm build:chat-context:check</automated>
    <automated>git diff --exit-code -- package.json pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build:chat-context:check` exits 0 BEFORE `pnpm build` (clean pre-build baseline), AND `git diff --exit-code -- src/data/portfolio-context.json` shows NO diff AFTER `pnpm build` (the write-mode build did not silently rewrite the chat corpus) (F3).
    - `pnpm build` exits 0 (astro check inside reports 0/0/0; dist/ produced).
    - `pnpm test` reports 0 failures: 7 site-side projects covered; no-mdx-in-worker + featured-tier-render green against fresh dist/; chat-side pins green at 6 projects (banned source absent).
    - `pnpm sync:check` and `pnpm build:chat-context:check` both exit 0.
    - `git diff --exit-code -- package.json pnpm-lock.yaml` shows NO diff - no new runtime dependency added anywhere in the phase, lockfile included (QA-02) (F4).
  </acceptance_criteria>
  <done>The whole build and full suite are green end-to-end; the D-15 skip is validated through the real build chain; no new dependencies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off - #7 case-study copy (D-02/D-03) + featured-tier visuals (SC5)</name>
  <files>(none - human verification only; content edits loop through Projects/7 + pnpm sync:projects)</files>
  <action>Pause for blocking human verification. Present the two surfaces below and do NOT proceed or mark the phase complete until the reviewer types "approved". If edits are requested, apply content fixes to the fenced source in Projects/7 and re-run pnpm sync:projects (never the MDX body), or apply visual fixes via page-scoped/primitive-scoped CSS, then re-present.</action>
  <what-built>
    The Multi-Chain EVM (#7) case study - a >=600-word first-person case study fenced in Projects/7, synced into src/content/projects/multi-chain-evm.mdx, rendered at /projects/multi-chain-evm - and the two-tier featured layout: a distinct FEATURED tier (3 rows with tagline) above a MORE WORK tier (4 compact rows) on /projects, plus the 3-featured teaser with taglines and a See all work link on Home. All visuals were routed through the frontend-design skill against design-system/MASTER.md and 23-UI-SPEC.md.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` and open the two surfaces, then confirm both gates:

    (A) Content review - Jack (D-02 / D-03). Open http://localhost:4321/projects/multi-chain-evm and read the case study. Confirm: the framing is honest live real-capital operation with NO profit/loss or returns claims (D-03); there are zero em dashes; the tagline (80 characters or fewer) reads well as a capability line; the techStack list is accurate; and the year is correct. If any edit is needed, revise the fenced source between the CASE-STUDY markers in `Projects/7 - MULTI-DEX CRYPTO TRADER.md` and re-run `pnpm sync:projects` - never edit the MDX body directly.

    (B) frontend-design 6-pillar UI checker sign-off - SC5. Verify the featured tier against 23-UI-SPEC.md (Checker Sign-Off) and design-system/MASTER.md across the six pillars: Copywriting (FEATURED / MORE WORK labels; See all work), Visuals (numbered editorial rows, hairline dividers, no cards/shadows/radius), Color (six tokens only; accent only on interactive title/arrow/link, never on the tagline or tier labels), Typography (title 1.75rem -> tagline .body/--ink-muted -> mono stack), Spacing (title->tagline 8px, tagline->stack 12px, MORE WORK 48px break), Registry Safety (n/a - first-party primitives). Check http://localhost:4321/projects and http://localhost:4321/ at 375px and 1440px: FEATURED tier (3 rows, tagline) above MORE WORK (4 rows), numbered 01-07 continuously; the See all work link present on Home; no cards/shadows/border-radius anywhere.
  </how-to-verify>
  <resume-signal>Type "approved" to accept both gates, or list the required content and/or visual edits.</resume-signal>
  <done>Both gates approved: the #7 case-study copy is honest and em-dash-free (D-02/D-03) and the featured tier passes the frontend-design 6-pillar sign-off (SC5); approval recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan: runs the existing build/test scripts and records human sign-off. No source changes, no runtime input, no network, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Repudiation | phase-gate sign-off | low | mitigate | The two human gates (content honesty D-02/D-03, visual SC5) are captured as an explicit blocking checkpoint with a recorded resume signal, so the sign-off is auditable in the SUMMARY. |
| T-23-04-INT | Information Disclosure | chat corpus | medium | mitigate | The gate re-runs build:chat-context:check + the chat-side pins to confirm #7 is still excluded (D-15) after the full build - a final guard that the site change did not leak #7 into chat. |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs (QA-02); the gate explicitly re-confirms dependencies are byte-identical. |
</threat_model>

<verification>
- `pnpm build:chat-context:check` exits 0 BEFORE `pnpm build`; `git diff --exit-code -- src/data/portfolio-context.json` shows NO diff AFTER `pnpm build` (F3 - the write-mode build did not rewrite the chat corpus).
- `pnpm build` exits 0 (astro check 0/0/0; dist/ produced).
- `pnpm test` fully green (7 site-side projects; build-output gates green vs fresh dist/; chat-side pins green at 6).
- `pnpm sync:check` + `pnpm build:chat-context:check` exit 0.
- `git diff --exit-code -- package.json pnpm-lock.yaml` shows NO diff (QA-02 no-new-dep, lockfile included) (F4).
- Human checkpoint approved: #7 copy (D-02/D-03) and featured-tier visuals (SC5) signed off.
</verification>

<success_criteria>
- End-to-end build + full suite green; D-15 validated through the real build chain (SC5 / QA-02).
- Chat still exactly 6 projects; no new runtime deps (D-15 / QA-02).
- Human sign-off recorded for the #7 case-study copy and the featured-tier visuals (SC1 / SC5).
</success_criteria>

<artifacts_produced>
None - verification-and-sign-off plan; writes no source files. Produces the phase SUMMARY and the recorded human approval.
</artifacts_produced>

<output>
Create `.planning/phases/23-projects-reconciliation-featured-tier/23-04-SUMMARY.md` when done.
</output>
