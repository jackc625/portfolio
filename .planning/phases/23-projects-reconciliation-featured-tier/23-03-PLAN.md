---
phase: 23-projects-reconciliation-featured-tier
plan: 03
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/components/primitives/WorkRow.astro
  - src/pages/projects.astro
  - src/pages/index.astro
  - tests/build/featured-tier-render.test.ts
autonomous: true
requirements: [PROJ-02, PROJ-03, PROJ-04]

must_haves:
  truths:
    - "/projects shows a visually distinct FEATURED tier (3 rows with tagline) above a MORE WORK tier (4 compact rows) (PROJ-02)"
    - "All 7 projects render, the rest tier keeps SolSniper/Optimize-AI/Clipify/DayTrade reachable below the featured tier (PROJ-03)"
    - "Home keeps the 3-featured teaser with taglines and a See all work link to /projects; both pages derive from the same featured/order partition (PROJ-04)"
    - "Rows are numbered continuously 01-07 by the order field across both tiers"
  artifacts:
    - "optional tagline prop on src/components/primitives/WorkRow.astro"
    - "two-tier layout in src/pages/projects.astro"
    - "richer featured rows + See all work link in src/pages/index.astro"
    - "tests/build/featured-tier-render.test.ts"
  key_links:
    - "WorkRow stays byte-identical when tagline is absent (preserves the 4 rest rows + motion/focus tests)"
    - "page-scoped <style> only - no global.css / BaseLayout touch keeps the D-26 chat-surface battery out of scope"
    - "both /projects and Home read the same getCollection -> featured-partition -> order-sort finalized in Plan 01"
---

<objective>
Build the asymmetric two-tier featured layout on /projects and apply the same richer featured-row treatment on the Home work list, all routed through the frontend-design skill against design-system/MASTER.md and the phase UI-SPEC.

Purpose: This is the visible reconciliation. The data model is finalized (Plan 01); this plan renders it - a distinct FEATURED tier above a MORE WORK tier on /projects, and a 3-featured teaser plus a See all work link on Home - by extending one primitive (WorkRow) with an optional tagline line and partitioning both pages by the featured boolean. SC5 requires every pixel decision (divider form, tier labels, tagline rhythm) to come from the frontend-design skill; the UI-SPEC is that routed contract.
Output: an extended WorkRow, a two-tier projects.astro, an enriched index.astro, and the SC2 render gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-projects-reconciliation-featured-tier/23-UI-SPEC.md
@.planning/phases/23-projects-reconciliation-featured-tier/23-PATTERNS.md
@design-system/MASTER.md
@src/pages/experience.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WorkRow.astro with an optional tagline prop (frontend-design routed)</name>
  <files>src/components/primitives/WorkRow.astro</files>
  <read_first>
    - src/components/primitives/WorkRow.astro (the current 5-prop primitive: Props interface, the .work-body markup between .work-title and .work-stack, and the scoped style incl. .work-title margin-bottom 12px and the hover/focus + reduced-motion contract)
    - .planning/phases/23-projects-reconciliation-featured-tier/23-UI-SPEC.md (Typography - the tagline type-role rationale; Featured-row form - title->tagline 8px, tagline->stack 12px)
    - design-system/MASTER.md (section 3 type roles: .body and .h2-project; section 5.5 WorkRow; section 7 accent signal rule; section 8 anti-card stance)
    - src/pages/experience.astro (the Phase 22 featured-block rhythm: featured-title margin-bottom 8px, featured-stack, the earlier-divider hairline idiom - the closest shipped analog)
  </read_first>
  <action>
    Invoke the frontend-design:frontend-design skill first (SC5 is mandatory) to finalize the exact tagline selector and vertical rhythm against MASTER.md and the UI-SPEC before editing.

    Extend WorkRow.astro in place - do NOT fork a new FeaturedRow primitive (reuse-over-invention; a fork would duplicate the hover/focus/reduced-motion CSS and risk the motion tests). Add an optional `tagline?: string` to the Props interface and destructure it. In the markup, render a tagline paragraph with class `body work-tagline` between `.work-title` and `.work-stack`, rendered only when tagline is set (conditional render, so the row is byte-identical when tagline is absent). Scoped `<style>` only - NO Tailwind utilities. Add `.work-tagline { color: var(--ink-muted); margin-bottom: 12px; }`. When the tagline is present, the title-to-tagline gap must be 8px and the tagline-to-stack gap 12px per the UI-SPEC; apply the title 8px override only when a tagline exists (via a `:has(.work-tagline)` selector on the body, or an equivalent modifier - the frontend-design skill picks the exact selector). The default no-tagline row keeps its current title margin-bottom of 12px.

    Constraints (MASTER + D-17): no schema change and no new runtime deps; stay inside the six-token palette (--bg / --ink / --ink-muted / --ink-faint / --rule / --accent); no card, shadow, or border-radius; the tagline text takes NO hover color change (only the title carries the accent underline affordance, so the row keeps a single unambiguous signal). Preserve the existing hover/focus and prefers-reduced-motion blocks unchanged. astro check must stay 0/0/0.
  </action>
  <verify>
    <automated>pnpm exec astro check</automated>
    <automated>pnpm exec vitest run tests/motion</automated>
  </verify>
  <acceptance_criteria>
    - WorkRow.astro Props declares an optional `tagline?: string`; the markup renders a `.work-tagline` paragraph (.body, color var(--ink-muted)) between title and stack ONLY when tagline is set.
    - When tagline is absent the rendered row is byte-identical to the previous behavior (rest rows unchanged).
    - Scoped style adds .work-tagline (margin-bottom 12px) and a tagline-present title 8px gap; no Tailwind utility classes; no card/shadow/border-radius introduced.
    - `pnpm exec astro check` reports 0/0/0.
    - `pnpm exec vitest run tests/motion` (work-arrow-motion, motion-css-rules) is green.
  </acceptance_criteria>
  <done>WorkRow renders a tagline line when provided and is byte-identical otherwise; the rhythm was finalized by the frontend-design skill.</done>
</task>

<task type="auto">
  <name>Task 2: Rework projects.astro into the two-tier FEATURED / MORE WORK layout + author the SC2 render gate</name>
  <files>src/pages/projects.astro, tests/build/featured-tier-render.test.ts</files>
  <read_first>
    - src/pages/projects.astro (the current flat list: getCollection sort by order, single SectionHeader count, the WorkRow map, the stack join idiom techStack.join(" · ").toUpperCase())
    - src/pages/experience.astro (the tier-divider analog: .earlier-divider flex row with a .label-mono label + a 1px --rule hairline; margin-top 48px for the tier break)
    - .planning/phases/23-projects-reconciliation-featured-tier/23-UI-SPEC.md (Two-tier structure ascii, Interaction & Component contract, divider form, spacing scale: FEATURED tight under the section rule, MORE WORK 48px above)
    - design-system/MASTER.md (section 4 spacing; section 5.5 numbered-row grammar; section 8 no cards)
  </read_first>
  <action>
    Invoke the frontend-design:frontend-design skill (SC5) for the tier-divider and label form + spacing before editing.

    Rework src/pages/projects.astro to the D-07/D-08/D-09 asymmetric two-tier layout. Keep exactly ONE SectionHeader (number 01, title WORK) with count set to `${all.length} / ${all.length}` (which renders 7 / 7). Partition in frontmatter from a single collection read using the D-12 single-distinction mechanism: sort all projects by data.order ascending, then split into a featured array (data.featured true) and a rest array (data.featured false). Render, in order: a FEATURED tier sub-label with a trailing hairline (mirror the experience.astro earlier-divider idiom: a flex row, a .label-mono label in --ink-faint, a 1px --rule hairline filling the remaining width) sitting directly under the SectionHeader rule; then the featured WorkRows WITH `tagline={p.data.tagline}`; then a MORE WORK divider using the same idiom with margin-top 48px; then the rest WorkRows WITHOUT a tagline. Number EVERY row by `String(p.data.order).padStart(2, "0")` so both tiers read 01 through 07 continuously (featured 01-03, rest 04-07). Keep the stack join idiom verbatim. The tier labels/dividers are sub-labels, NOT numbered SectionHeaders (D-09 - preserve the single page-level 01 WORK numbering). Page-scoped `<style>` only; do NOT touch global.css or BaseLayout.astro (keeps the D-26 chat-surface battery out of scope).

    Then create tests/build/featured-tier-render.test.ts (net-new, SC2 build-output gate). It reads the built /projects HTML from dist/ and asserts the rendered markup contains the class token `work-tagline` exactly 3 times (only the featured rows carry a tagline) and that there are 7 work rows numbered 01 through 07. Its runtime assertion is validated against a fresh dist/ at the Plan 04 phase-gate `pnpm build`; author the file so it runs there.
  </action>
  <verify>
    <automated>pnpm exec astro check</automated>
  </verify>
  <acceptance_criteria>
    - projects.astro renders one SectionHeader (01 WORK, count 7 / 7), a FEATURED tier (3 rows WITH tagline, numbered 01-03) above a MORE WORK tier (4 rows WITHOUT tagline, numbered 04-07), rows numbered by the order field continuously 01-07.
    - The FEATURED and MORE WORK markers are sub-labels with trailing hairlines (experience.astro divider idiom), not numbered SectionHeaders; MORE WORK opens with a 48px top gap.
    - Only page-scoped <style> changed; global.css and BaseLayout.astro are untouched.
    - `pnpm exec astro check` reports 0/0/0.
    - tests/build/featured-tier-render.test.ts exists and encodes: `work-tagline` appears exactly 3 times and 7 rows numbered 01-07 (validated at the Plan 04 build).
  </acceptance_criteria>
  <done>/projects presents the asymmetric two-tier featured layout per the UI-SPEC, and the render gate is authored.</done>
</task>

<task type="auto">
  <name>Task 3: Enrich the Home work list with taglines + a See all work link; fix the stale featured comment</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (the WORK section: the featured filter, the WorkRow map, the count expression; the ABOUT section .read-more link markup + scoped style; the stale line-17 comment naming the old featured set)
    - .planning/phases/23-projects-reconciliation-featured-tier/23-UI-SPEC.md (Home work list D-13/D-14; the See all work copy and .read-more-mirroring form)
    - src/pages/projects.astro (the featured treatment to match one-to-one - one row treatment everywhere)
  </read_first>
  <action>
    Invoke the frontend-design:frontend-design skill (SC5) for the See all work link placement and form (it mirrors the ABOUT .read-more idiom) before editing.

    In src/pages/index.astro: (1) Keep the existing 3-featured teaser - the featured filter is already correct after Plan 01, so make NO logic change to the partition; just add `tagline={p.data.tagline}` to each featured WorkRow call so Home rows use the same richer treatment as /projects (D-14). Leave the count expression rendering 3 / 7. (2) Add a See all work link to /projects at the END of the WORK section work-list, mirroring the ABOUT .read-more idiom: a .label-mono anchor, display inline-block, margin-top 24px, color --ink-muted default with a hover to --accent and an arrow opacity reveal; use a page-scoped class (reuse the .read-more rule shape under a name such as .see-all-work). Copy text: `See all work` followed by a right-arrow entity. (3) Update the stale comment (the line naming the old featured set "SeatWatch, NFL Prediction, SolSniper") to the new set "SeatWatch, Multi-Chain EVM Trader, NFL Prediction" - comment only, not user-facing. Page-scoped `<style>` only; do NOT touch global.css.
  </action>
  <verify>
    <automated>pnpm exec astro check</automated>
    <automated>pnpm exec vitest run tests/content</automated>
  </verify>
  <acceptance_criteria>
    - index.astro featured WorkRow calls pass `tagline={p.data.tagline}`; the WORK count still renders 3 / 7.
    - A See all work link to /projects exists after the WORK work-list, mirroring the .read-more idiom (.label-mono, --ink-muted default, --accent on hover, arrow reveal).
    - The stale featured-set comment is updated to "SeatWatch, Multi-Chain EVM Trader, NFL Prediction".
    - Only page-scoped <style> changed; global.css untouched.
    - `pnpm exec astro check` reports 0/0/0; `pnpm exec vitest run tests/content` stays green.
  </acceptance_criteria>
  <done>Home shows the 3-featured teaser with taglines plus a See all work link to /projects; the stale comment is fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static page render + a scoped-CSS primitive extension; no runtime input, no network, no auth. The only cross-cutting risk is a shared-file edit (global.css / BaseLayout) touching the chat surface - explicitly avoided (page-scoped CSS only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03 | Tampering | shared style surface (global.css / BaseLayout.astro) | low | mitigate | All tier/tagline/link styling is page-scoped or primitive-scoped; global.css and BaseLayout.astro are untouched, so the D-26 chat-surface battery and D-15 SSE anchor stay out of scope. Verified by the acceptance criteria and the git diff. |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs (QA-02). No package-manager surface. |
</threat_model>

<verification>
- `pnpm exec astro check` reports 0/0/0.
- `pnpm exec vitest run tests/motion` green (WorkRow byte-identical when tagline absent - rest rows and motion/focus contract preserved).
- `pnpm exec vitest run tests/content` green.
- tests/build/featured-tier-render.test.ts authored (work-tagline x3, 7 rows 01-07) - runtime-validated at the Plan 04 build.
- git diff touches only WorkRow.astro, projects.astro, index.astro, and the new render test; global.css and BaseLayout.astro untouched.
</verification>

<success_criteria>
- /projects renders a distinct FEATURED tier (3, with tagline) above a MORE WORK tier (4, compact), numbered 01-07 (SC2 / PROJ-02).
- SolSniper, Optimize-AI, Clipify, DayTrade remain reachable in the rest tier (SC3 / PROJ-03).
- Home shows the 3-featured teaser with taglines + a See all work link; both pages derive from the same featured/order distinction (SC4 / PROJ-04).
- All visual decisions routed through frontend-design; astro check stays 0/0/0; no shared-file/chat-surface edit (SC5).
</success_criteria>

<artifacts_produced>
Symbols and files this plan creates (for source-grounding / drift exclusion):
- New optional prop `tagline?: string` and CSS class `.work-tagline` on src/components/primitives/WorkRow.astro.
- New page-scoped tier markup/classes in src/pages/projects.astro (FEATURED / MORE WORK sub-label dividers).
- New See all work link + `.see-all-work` class in src/pages/index.astro.
- New test file: tests/build/featured-tier-render.test.ts (SC2 render gate).
</artifacts_produced>

<output>
Create `.planning/phases/23-projects-reconciliation-featured-tier/23-03-SUMMARY.md` when done.
</output>
