---
phase: 23-projects-reconciliation-featured-tier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-chat-context.mjs
  - Projects/7 - MULTI-DEX CRYPTO TRADER.md
  - src/content/projects/multi-chain-evm.mdx
  - src/content/projects/nfl-predict.mdx
  - src/content/projects/solsniper.mdx
  - src/content/projects/optimize-ai.mdx
  - src/content/projects/clipify.mdx
  - src/content/projects/daytrade.mdx
  - tests/content/projects-collection.test.ts
  - tests/content/projects-ordering.test.ts
autonomous: true
requirements: [PROJ-01, PROJ-03, PROJ-04]

must_haves:
  truths:
    - "/projects/multi-chain-evm builds as a full case-study page synced through sync-projects.mjs (PROJ-01)"
    - "Exactly 3 projects are featured: SeatWatch, Multi-Chain EVM Trader, NFL Prediction (SolSniper demoted to featured:false, kept) (PROJ-03)"
    - "order values across all 7 project MDX are contiguous unique {1,2,3,4,5,6,7} driving one featured-then-rest distinction (PROJ-04)"
    - "Chat context stays exactly 6 projects; Multi-Chain EVM is excluded from portfolio-context.json until Phase 25 (D-15)"
  artifacts:
    - src/content/projects/multi-chain-evm.mdx
    - "fenced case study in Projects/7 - MULTI-DEX CRYPTO TRADER.md"
    - "D-15 slug-skip branch in scripts/build-chat-context.mjs"
    - tests/content/projects-ordering.test.ts
  key_links:
    - "multi-chain-evm.mdx source: field -> Projects/7 fence -> sync-projects.mjs body replacement"
    - "D-15 slug-skip lands (Task 1) BEFORE the #7 MDX references Projects/7 (Task 2) -> build never goes red"
    - "chat-side test pins left unchanged keep chat at exactly 6 projects"
---

<objective>
Reconcile the project data model: sync the Multi-Chain EVM trader (#7) onto the site as a full case study, apply the featured/order distinction so exactly three projects are featured, and keep the Multi-Chain EVM project OUT of the chat corpus until Phase 25 by re-plumbing the chat-context build.

Purpose: This is the data-and-pipeline foundation for the whole phase. Every downstream plan (content-gate coverage, two-tier UI, phase gate) reads the collection this plan finalizes. It also owns the single hard landmine of the phase: creating an MDX that points at Projects/7 hard-fails the build unless the D-15 chat-context skip lands first (build:chat-context runs first in pnpm build).
Output: multi-chain-evm.mdx (synced), a fenced case study in the #7 source, the D-15 slug-skip, reassigned featured/order across five sibling MDX, and the SC4 ordering property gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-projects-reconciliation-featured-tier/23-CONTEXT.md
@.planning/phases/23-projects-reconciliation-featured-tier/23-RESEARCH.md
@.planning/phases/23-projects-reconciliation-featured-tier/23-PATTERNS.md
@src/content.config.ts
@scripts/sync-projects.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-plumb build-chat-context.mjs with the D-15 multi-chain-evm slug skip</name>
  <files>scripts/build-chat-context.mjs</files>
  <read_first>
    - scripts/build-chat-context.mjs (the main() loop ~lines 430-504: the try/catch per MDX, the chatSummary hard-fail inside buildProjectBlock ~line 389, the defensive MULTI-DEX regex ~line 457, and the errorCount>0 -> process.exit(2) at line 504)
    - tests/build/chat-context-integrity.test.ts (the pin this must keep green: 6 EXPECTED_SLUGS, Multi-Chain EVM absent)
    - package.json (the "build" script: build:chat-context runs FIRST in the chain, so any exit-2 here aborts the whole build)
  </read_first>
  <action>
    In scripts/build-chat-context.mjs, at the TOP of the `for (const mdxPath of mdxFiles)` loop (immediately after the loop opener near line 438, BEFORE the `try {` and therefore before both hard-fails), insert an explicit slug skip: when `basename(mdxPath, ".mdx")` equals the exact slug `multi-chain-evm`, `continue`. Add a comment citing Phase 23 / D-15 and noting Phase 25 / CHAT-10 lifts it. `basename` is already imported (line 29) so add no new import.

    This single continue sidesteps BOTH hard-fails: the mandatory chatSummary check (which #7 will not have per D-05) and the defensive source-path guard. Softening only the regex is INSUFFICIENT (the chatSummary throw still breaks the build) - the slug must be skipped entirely before buildProjectBlock runs.

    Keep the existing defensive source-path regex (the MULTI-DEX allow-list reinforcement near line 457) intact as dormant defense-in-depth - it never fires now but still guards a future different-slug mistake. <!-- planner-discipline-allow: MULTI-DEX --> Do not touch the sort or emit logic: src/data/portfolio-context.json must re-emit byte-identical at exactly 6 projects. Optionally refresh the stale header comment (lines 6-16) to note the explicit slug-skip is now the #7 exclusion mechanism (non-blocking hygiene).
  </action>
  <verify>
    <automated>pnpm build:chat-context:check</automated>
    <automated>pnpm exec vitest run tests/build/chat-context-integrity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-chat-context.mjs main loop has `if (basename(mdxPath, ".mdx") === "multi-chain-evm") continue;` placed before the try/buildProjectBlock and before the source-path regex.
    - `pnpm build:chat-context:check` exits 0 (portfolio-context.json unchanged - drift check passes).
    - Emitted src/data/portfolio-context.json still contains exactly 6 projects; multi-chain-evm is absent.
    - tests/build/chat-context-integrity.test.ts passes (6 slugs, banned source absent).
    - The defensive source-path regex is still present (not deleted); no new import added.
  </acceptance_criteria>
  <done>Chat-context build passes and emits byte-identical 6-project JSON; the #7 slug is skipped before either hard-fail; the defensive regex is retained.</done>
</task>

<task type="auto">
  <name>Task 2: Author + fence the #7 case study, create multi-chain-evm.mdx frontmatter, sync body, extend the collection slug gate</name>
  <files>Projects/7 - MULTI-DEX CRYPTO TRADER.md, src/content/projects/multi-chain-evm.mdx, tests/content/projects-collection.test.ts</files>
  <read_first>
    - Projects/1 - SEATWATCH.md (the fence + extended-reference structure to mirror: H1 title above the fence, CASE-STUDY-START/END wrapping the 5-H2 case study, "FULL TECHNICAL REFERENCE" below)
    - Projects/7 - MULTI-DEX CRYPTO TRADER.md (current unfenced ~400-line README titled "Crypto Snipe Bot"; D-04 prepends above it, leaves it verbatim below)
    - src/content/projects/seatwatch.mdx (frontmatter reference field set; note #7 OMITS chatSummary/demoUrl)
    - src/content.config.ts (the projects Zod schema - do NOT modify; confirms field constraints: tagline max 80, techStack min 1, order int min 1, year 4-digit, category enum)
    - scripts/sync-projects.mjs (the fence contract: markers appear exactly once, START before END, body-only replacement, never creates MDX)
    - tests/content/projects-collection.test.ts (EXPECTED_SLUGS array + the "exactly 6 MDX entries" title to extend)
  </read_first>
  <action>
    Do these in order so the build never goes red (Task 1's skip is already in place):

    1. Prepend the fenced case study to Projects/7 - MULTI-DEX CRYPTO TRADER.md (D-04: prepend, do NOT rewrite the README). Structure top-to-bottom: an H1 line `# Multi-Chain EVM Trader`; then a fence `<!-- CASE-STUDY-START -->` ... `<!-- CASE-STUDY-END -->` wrapping a 600-900 word FIRST-PERSON case study in exactly this five-H2 shape and order: `## Problem`, `## Approach & Architecture`, `## Tradeoffs`, `## Outcome`, `## Learnings`. Leave the existing `# Crypto Snipe Bot` README verbatim BELOW the END marker as the Phase-25 chat extended reference. Each fence marker string appears EXACTLY once; START before END. Use ZERO em dashes (site-wide ban) - en dashes, middots, or hyphens only. Frame the Outcome/Learnings per D-03 on honest live real-capital operation with NO profit/loss or returns claims; lean on the engineering rigor the README documents: the eight-stage safety pipeline, restart-safe persisted exit state in SQLite, the pluggable per-chain MEV transport (Flashbots bundle / bloXroute private tx / public RPC), volatility-adaptive trailing exits, and structural test invariants. Avoid the marketing words the voice-banlist gate rejects.

    2. Create src/content/projects/multi-chain-evm.mdx with HAND-AUTHORED FRONTMATTER ONLY (the sync script fills the body; never author prose in the MDX). Fields per D-01/D-05/D-06: title `Multi-Chain EVM Trader`; tagline a capability-angle line of 80 characters or fewer (draft: `Automated multi-chain DEX sniping with volatility-adaptive exits`); description one recruiter/OG-meta sentence; techStack an array of at least one string derived from the README stack section (draft: TypeScript, Ethers.js, SQLite, Express, WebSocket, React); featured true; status `completed`; category `other`; order 2; year `2026` (draft - Jack confirms at the phase gate); source `Projects/7 - MULTI-DEX CRYPTO TRADER.md`. OMIT chatSummary, githubUrl, demoUrl, and thumbnail entirely.

    3. Run `pnpm sync:projects` to fill the MDX body from the fenced source.

    4. In tests/content/projects-collection.test.ts add the slug `multi-chain-evm` to EXPECTED_SLUGS in alphabetical position (between `daytrade` and `nfl-predict`) and change the "exactly 6 MDX entries" test title so it reads 7. Leave the "exactly 3 entries are marked featured" assertion unchanged - it is validated in Task 3.

    Do NOT run the featured-count or ordering gates as this task's verify; those go green only after Task 3 reassigns siblings (transient during the plan - expected).
  </action>
  <verify>
    <automated>pnpm sync:check</automated>
    <automated>pnpm exec astro check</automated>
    <automated>pnpm exec vitest run tests/content/source-files-exist.test.ts</automated>
    <automated>pnpm build:chat-context:check</automated>
  </verify>
  <acceptance_criteria>
    - Projects/7 - MULTI-DEX CRYPTO TRADER.md contains exactly one CASE-STUDY-START and one CASE-STUDY-END (START before END) wrapping a five-H2 case study (Problem / Approach & Architecture / Tradeoffs / Outcome / Learnings); the "# Crypto Snipe Bot" README remains verbatim below END.
    - src/content/projects/multi-chain-evm.mdx exists with featured: true, order: 2, category: "other", source: "Projects/7 - MULTI-DEX CRYPTO TRADER.md", and NO chatSummary / githubUrl / demoUrl keys.
    - `pnpm sync:check` exits 0 (MDX body matches the fenced source; no drift).
    - `pnpm exec astro check` reports 0 errors, 0 warnings, 0 hints.
    - tests/content/source-files-exist.test.ts passes (the #7 source path resolves).
    - projects-collection.test.ts EXPECTED_SLUGS has 7 entries including 'multi-chain-evm'; the "exactly N MDX entries" title reads 7.
    - `pnpm build:chat-context:check` exits 0 and portfolio-context.json still contains exactly 6 projects (multi-chain-evm absent).
  </acceptance_criteria>
  <done>#7 is authored and fenced in the source, its MDX exists with a synced body and correct frontmatter, sync drift is 0, astro check is clean, and chat stays at 6.</done>
</task>

<task type="auto">
  <name>Task 3: Reassign featured/order across the five siblings + author the SC4 ordering property gate</name>
  <files>src/content/projects/nfl-predict.mdx, src/content/projects/solsniper.mdx, src/content/projects/optimize-ai.mdx, src/content/projects/clipify.mdx, src/content/projects/daytrade.mdx, tests/content/projects-ordering.test.ts</files>
  <read_first>
    - src/content/projects/nfl-predict.mdx, solsniper.mdx, optimize-ai.mdx, clipify.mdx, daytrade.mdx (the frontmatter featured/order lines to edit; leave every other field and the body byte-identical)
    - src/content/projects/seatwatch.mdx (reference: order 1 stays, featured stays true - do NOT touch)
    - 23-RESEARCH.md Priority 5 (the exact current-vs-target featured/order diff table)
    - tests/content/projects-collection.test.ts (the array idiom to mirror in the new ordering test; reads files directly, imports nothing from astro:content)
  </read_first>
  <action>
    Edit ONLY the `featured:` and `order:` frontmatter lines (per D-10/D-11); leave body and all other frontmatter byte-identical:
    - nfl-predict.mdx: order 2 becomes 3 (featured stays true).
    - solsniper.mdx: featured true becomes false; order 3 becomes 4.
    - optimize-ai.mdx: order 4 becomes 5 (featured stays false).
    - clipify.mdx: order 5 becomes 6 (featured stays false).
    - daytrade.mdx: order 6 becomes 7 (featured stays false).
    Do NOT change any `title:` values - the frontmatter titles ("NFL Prediction System", "Optimize AI", "Daytrade") stay as authored; rewriting titles is out of scope.

    Then create tests/content/projects-ordering.test.ts as a net-new Vitest property test proving SC4 / PROJ-04 and the D-12 single-distinction mechanism (partition by the featured boolean, sort each tier by order) independent of page rendering. Read every src/content/projects/*.mdx frontmatter directly (mirror the projects-collection.test.ts readdir/readFile idiom; import nothing from astro:content). Assert: (a) the multiset of `order` values equals exactly the set {1,2,3,4,5,6,7} - seven entries, contiguous, unique; (b) the slugs with `featured: true` equal exactly {seatwatch, multi-chain-evm, nfl-predict}; (c) every file slug is one of the seven expected slugs.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/content</automated>
    <automated>pnpm exec astro check</automated>
    <automated>pnpm sync:check</automated>
  </verify>
  <acceptance_criteria>
    - order values across the 7 project MDX equal exactly {1,2,3,4,5,6,7} (unique, contiguous); solsniper is featured:false with order 4.
    - featured:true slugs are exactly {seatwatch, multi-chain-evm, nfl-predict}.
    - tests/content/projects-ordering.test.ts exists and passes.
    - tests/content/projects-collection.test.ts "exactly 3 entries are marked featured" passes (3).
    - `pnpm exec vitest run tests/content` is fully green; `pnpm exec astro check` reports 0/0/0; `pnpm sync:check` exits 0.
    - No `title:` frontmatter value was changed in any project MDX.
  </acceptance_criteria>
  <done>The single featured/order distinction is applied - order is contiguous 1-7 with exactly 3 featured - and the ordering property gate proves SC4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script -> committed chat artifact | The D-15 slug-skip must not alter src/data/portfolio-context.json beyond excluding #7; the chat corpus is a committed build artifact consumed by the chat Worker |
| source .md fence -> synced MDX body | Author-controlled content is machine-copied into the MDX body; the sync path-escape guard remains intact and unmodified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | build-chat-context.mjs slug-skip | medium | mitigate | Skip is scoped to the exact slug `multi-chain-evm`; verified byte-identical by `build:chat-context:check` + chat-context-integrity.test.ts (6 slugs, banned source absent). Defensive source-path regex retained as defense-in-depth. |
| T-23-01-INT | Information Disclosure | chat corpus | medium | mitigate | #7 stays out of portfolio-context.json until Phase 25 (D-15). The chat-side pins (chat-context-integrity, chat-knowledge-voice, prompt-injection) are left unchanged and actively verify the exclusion. |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero packages (QA-02 forbids new runtime deps). No package-manager surface; nothing to slopcheck. |
</threat_model>

<verification>
- `pnpm exec astro check` reports 0 errors / 0 warnings / 0 hints.
- `pnpm sync:check` exits 0 (no MDX-vs-source drift for #7).
- `pnpm build:chat-context:check` exits 0 and portfolio-context.json holds exactly 6 projects (#7 excluded).
- `pnpm exec vitest run tests/content` is green: projects-collection (7 entries, exactly 3 featured) + projects-ordering (order {1..7} unique, featured set exact).
- chat-context-integrity.test.ts green (chat pins unchanged; the D-15 skip verified).
</verification>

<success_criteria>
- #7 is a synced case-study MDX (frontmatter hand-authored, body machine-synced) with a fenced source (SC1 / PROJ-01).
- Exactly 3 featured; SolSniper demoted to featured:false but present; all 7 projects in the collection (SC3 / PROJ-03).
- One featured/order distinction, order contiguous 1-7, proven by the ordering gate (SC4 / PROJ-04).
- Chat stays exactly 6 projects; the build passes end-to-end because the D-15 skip preceded the #7 MDX (D-15).
</success_criteria>

<artifacts_produced>
Symbols and files this plan creates (for source-grounding / drift exclusion):
- New content entry: src/content/projects/multi-chain-evm.mdx (slug `multi-chain-evm`); the detail route /projects/multi-chain-evm is generated generically by the existing src/pages/projects/[id].astro (no new route file).
- New fenced CASE-STUDY block (H1 `# Multi-Chain EVM Trader` + CASE-STUDY-START/END) prepended into Projects/7 - MULTI-DEX CRYPTO TRADER.md.
- New branch in scripts/build-chat-context.mjs: the `multi-chain-evm` top-of-loop `continue` (D-15 skip).
- New test file: tests/content/projects-ordering.test.ts (SC4 property gate).
- Modified test: tests/content/projects-collection.test.ts (EXPECTED_SLUGS 6 -> 7, "6 entries" -> 7).
</artifacts_produced>

<output>
Create `.planning/phases/23-projects-reconciliation-featured-tier/23-01-SUMMARY.md` when done.
</output>
