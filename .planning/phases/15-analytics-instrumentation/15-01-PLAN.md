---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - tests/build/umami-tag-present.test.ts
autonomous: true
requirements: [ANAL-01, ANAL-02, ANAL-06]
user_setup:
  - service: umami
    why: "Cookie-free product analytics — ANAL-01"
    env_vars: []
    dashboard_config:
      - task: "Create Umami Cloud website entry for jackcutrara.com; copy the data-website-id UUID into src/layouts/BaseLayout.astro (replaces TODO_PHASE_15_UMAMI_ID literal before merge to main)"
        location: "Umami Cloud dashboard → Websites → Add website"
  - service: cloudflare
    why: "Core Web Vitals secondary — ANAL-02 (dashboard-toggle only, no code)"
    env_vars: []
    dashboard_config:
      - task: "Enable Web Analytics on the portfolio Pages project so the beacon auto-injects on jackcutrara.com production responses; capture dashboard screenshot for 15-VERIFICATION.md"
        location: "Cloudflare Dashboard → Workers & Pages → portfolio → Metrics → Web Analytics → Enable"
must_haves:
  truths:
    - "Production HTML for jackcutrara.com contains exactly one <script is:inline defer src=\"https://cloud.umami.is/script.js\" data-website-id=... data-domains=\"jackcutrara.com\"></script> tag inside <head>"
    - "Dev and preview builds contain zero references to cloud.umami.is anywhere in HTML output"
    - "Cloudflare Web Analytics beacon (static.cloudflareinsights.com) auto-injects on production HTML after dashboard toggle"
    - "No cookie-consent banner ships (Umami + CF Web Analytics are cookie-free by contract — ANAL-06)"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Umami script tag, PROD-gated, is:inline, defer"
      contains: "cloud.umami.is/script.js"
    - path: "tests/build/umami-tag-present.test.ts"
      provides: "Source-text assertions that the Umami tag exists with correct attributes (ANAL-01 S1-S5)"
      exports: []
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "cloud.umami.is/script.js"
      via: "<script is:inline defer src=...> inside {import.meta.env.PROD && ...} JSX-expression gate"
      pattern: "is:inline\\s+defer\\s+src=\"https://cloud\\.umami\\.is/script\\.js\""
    - from: "tests/build/umami-tag-present.test.ts"
      to: "src/layouts/BaseLayout.astro"
      via: "readFileSync source-text pattern (analog tests/client/chat-widget-header.test.ts)"
      pattern: "readFileSync.*BaseLayout\\.astro"
---

<objective>
Land the Umami Cloud analytics `<script>` tag in `BaseLayout.astro` with belt-and-suspenders production gating (D-01, D-02, D-03), and ship a source-text test asserting the tag's exact shape so future edits cannot silently break the contract. Cloudflare Web Analytics (ANAL-02) is documented as a dashboard toggle — no BaseLayout change per D-04, but a pre-deploy task is recorded in `user_setup` so Plan 05's `15-VERIFICATION.md` has a concrete checklist to cite.

Purpose: ANAL-01 (Umami tag present, env-gated) + ANAL-02 (CF Web Analytics enabled via CF dashboard) + ANAL-06 (no cookie banner, by design of both vendors) — the three environment-plumbing requirements that have no dependency on any other Phase 15 surface.

Output: `src/layouts/BaseLayout.astro` modified with one frontmatter-conditional `<script is:inline defer …>` tag after `<SEO …/>` and before `<Font …/>` preloads; `tests/build/umami-tag-present.test.ts` created and passing; Umami UUID committed as `TODO_PHASE_15_UMAMI_ID` placeholder to be replaced by Jack pre-deploy (documented in `user_setup.dashboard_config`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-analytics-instrumentation/15-CONTEXT.md
@.planning/phases/15-analytics-instrumentation/15-RESEARCH.md
@.planning/phases/15-analytics-instrumentation/15-PATTERNS.md
@.planning/phases/15-analytics-instrumentation/15-VALIDATION.md

# Existing implementation sites
@src/layouts/BaseLayout.astro
@src/components/JsonLd.astro
@tests/client/chat-widget-header.test.ts

<interfaces>
<!-- BaseLayout.astro current surface (line numbers from 2026-04-23 snapshot) -->

Line 81-83 — the three existing `<Font />` preloads:
```astro
<Font cssVariable="--font-display-src" />
<Font cssVariable="--font-body-src" />
<Font cssVariable="--font-mono-src" />
```

Line 84 — the existing frontmatter-conditional-render precedent (EXACT pattern to mirror for `import.meta.env.PROD && ...`):
```astro
{noindex && <meta name="robots" content="noindex, nofollow" />}
```

Line 80 — `<SEO ... />` closing tag (the Umami insert lands immediately after this line).

Line 97 — `<ChatWidget />` (relevant to Plan 02 which adds an `<script>` import block here; NOT this plan).

<!-- JsonLd.astro — the only existing `<script is:inline>` precedent in the repo -->
From src/components/JsonLd.astro:
```astro
<script is:inline type="application/ld+json" set:html={serialized} />
```
(Different semantics — inline content vs external src — but confirms `is:inline` is the correct Astro directive for preserving data-* attributes verbatim.)

<!-- chat-widget-header.test.ts — the source-text assertion analog -->
```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { join } from "node:path";

const widgetSource = readFileSync(
  join(process.cwd(), "src", "components", "chat", "ChatWidget.astro"),
  "utf8"
);
// then `expect(widgetSource).toContain("ASK ABOUT JACK")` etc.
```
This is the ONLY test pattern in the repo that reads an `.astro` source file for invariant assertions. Vitest runs from `process.cwd()` = project root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/build/umami-tag-present.test.ts (Wave 0 RED)</name>
  <read_first>
    - tests/client/chat-widget-header.test.ts (analog — the source-text `readFileSync` + `.toContain` pattern)
    - src/layouts/BaseLayout.astro (current state — will be modified in Task 2; tests must RED now, GREEN after Task 2)
    - .planning/phases/15-analytics-instrumentation/15-VALIDATION.md §Per-Task Verification Map (ANAL-01 row)
    - .planning/phases/15-analytics-instrumentation/15-RESEARCH.md §10.4 (Gap-closing tests — `tests/build/umami-tag-present.test.ts` describe block)
    - .planning/phases/15-analytics-instrumentation/15-PATTERNS.md §`tests/build/umami-tag-present.test.ts` (analog + code excerpts)
    - .planning/phases/15-analytics-instrumentation/15-CONTEXT.md D-01, D-02, D-03 (locked decisions the assertions encode)
  </read_first>
  <behavior>
    - RED before Task 2 executes: all Umami-presence assertions fail because BaseLayout.astro does not yet contain the tag
    - GREEN after Task 2 executes: every assertion passes against the in-tree BaseLayout.astro source
    - No build step required — tests read `src/layouts/BaseLayout.astro` source text via `readFileSync` (NOT from `dist/client/`; per 15-PATTERNS.md there is no dist/-read precedent and 15-RESEARCH.md §10.4 recommends source-text)
    - Tests import NOTHING from the module under test (no `// @vitest-environment jsdom` header needed — pure string assertions in node env)
  </behavior>
  <action>
Create `tests/build/umami-tag-present.test.ts` with the following exact shape (no DOM env, just `readFileSync` + `.toContain` / `.toMatch` / `.not.toContain`). Mirror `tests/client/chat-widget-header.test.ts:1-23` verbatim for imports + `readFileSync` setup; swap the path to `src/layouts/BaseLayout.astro`. Read with `join(process.cwd(), "src", "layouts", "BaseLayout.astro")`, `"utf8"`.

Author one `describe` block titled `"Umami analytics tag (ANAL-01 / D-01, D-02, D-03)"` containing these six `it` blocks verbatim in this order:

1. `it("references cloud.umami.is/script.js as the external src")` — `expect(layoutSource).toContain("cloud.umami.is/script.js")`

2. `it("is rendered with is:inline so Astro preserves data-* attributes verbatim")` — `expect(layoutSource).toMatch(/is:inline[\s\S]{0,200}?cloud\.umami\.is\/script\.js/)` (multiline regex asserts `is:inline` and the src URL live in the same tag; allows up to 200 chars between them so attribute order is flexible)

3. `it("has data-domains=\"jackcutrara.com\" for server-side hostname filtering (D-01)")` — `expect(layoutSource).toContain('data-domains="jackcutrara.com"')`

4. `it("has a defer attribute for non-blocking load (D-02)")` — `expect(layoutSource).toMatch(/defer[\s\S]{0,200}?cloud\.umami\.is|cloud\.umami\.is[\s\S]{0,200}?defer/)` (defer appears within the same tag as the umami src)

5. `it("commits a data-website-id attribute (D-03 — public literal, no env var)")` — `expect(layoutSource).toMatch(/data-website-id=(\{[^}]+\}|"[^"]+")/)` (matches either JSX expression form `{WEBSITE_ID}` or bare string form `"TODO_..."` — flexible to the implementation in Task 2)

6. `it("is gated by import.meta.env.PROD (D-01 belt-and-suspenders — absent in dev/preview builds)")` — `expect(layoutSource).toMatch(/\{import\.meta\.env\.PROD\s*&&/)`

Additionally author a second `describe` block titled `"BaseLayout is:inline precedent integrity"` with one guard `it`:

7. `it("does not introduce a second un-scoped is:inline for Umami (catches future drift)")` — `const occurrences = (layoutSource.match(/is:inline/g) ?? []).length; expect(occurrences).toBeLessThanOrEqual(2);` (current: zero in BaseLayout; Task 2 adds one for Umami — anything >2 signals accidental duplication)

Do NOT assert the literal UUID string. D-03 places a static literal that Jack replaces pre-deploy; asserting the UUID would break CI on every Umami-site rotation. The regex in assertion #5 verifies the attribute exists with any non-empty value.

Do NOT assert `async` attribute presence or absence. Per 15-RESEARCH.md §3.3 + L2 the recommendation is `defer`-only (CONTEXT.md D-02 said "async+defer" but HTML spec + Umami docs converge on `defer`). Assertion #4 locks `defer`; no counter-assertion on `async` so the executor has flexibility if Task 2 needs it for some reason.

Run the test file with `pnpm test -- tests/build/umami-tag-present.test.ts` — all 7 tests MUST RED at this point (BaseLayout.astro has not been modified yet). Commit as RED.

Commit message: `test(15-01): RED test stubs for Umami tag presence (ANAL-01)`
  </action>
  <verify>
    <automated>pnpm test -- tests/build/umami-tag-present.test.ts 2>&1 | grep -E "(FAIL|passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/build/umami-tag-present.test.ts` exists
    - `grep -c "describe" tests/build/umami-tag-present.test.ts` returns `2`
    - `grep -c "it(" tests/build/umami-tag-present.test.ts` returns `7`
    - `grep -c "readFileSync" tests/build/umami-tag-present.test.ts` returns at least `1`
    - `grep -c "cloud.umami.is/script.js" tests/build/umami-tag-present.test.ts` returns at least `1`
    - `grep -c "data-domains=\"jackcutrara.com\"" tests/build/umami-tag-present.test.ts` returns at least `1`
    - `grep -c "import.meta.env.PROD" tests/build/umami-tag-present.test.ts` returns at least `1`
    - `grep -c "@vitest-environment" tests/build/umami-tag-present.test.ts` returns `0` (pure node env — no jsdom needed for string assertions)
    - `pnpm test -- tests/build/umami-tag-present.test.ts` exits non-zero with exactly 7 failing tests (RED state; BaseLayout.astro still unmodified)
    - Git commit message matches regex `^test\(15-01\):.*ANAL-01`
  </acceptance_criteria>
  <done>
    tests/build/umami-tag-present.test.ts exists with 7 failing assertions (RED); test structure matches 15-RESEARCH.md §10.4 exactly; committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert Umami tag into BaseLayout.astro with PROD gate (GREEN)</name>
  <read_first>
    - src/layouts/BaseLayout.astro (full file — read lines 1-100 to see current frontmatter, `<head>` structure around lines 50-86, and body structure around lines 87-100)
    - src/components/JsonLd.astro (is:inline precedent; copy the exact `<script is:inline ...>` shape, NOT the `set:html` attribute which is for inline content)
    - tests/build/umami-tag-present.test.ts (the assertions authored in Task 1 — edit BaseLayout.astro to make all 7 GREEN)
    - .planning/phases/15-analytics-instrumentation/15-RESEARCH.md §3.2 (exact Astro template shape for the conditional render; §L3 for is:inline landmine; §L4 for the JSX-expression-must-be-in-template-position landmine)
    - .planning/phases/15-analytics-instrumentation/15-PATTERNS.md §`src/layouts/BaseLayout.astro` (analog + the exact insert shape to copy)
    - .planning/phases/15-analytics-instrumentation/15-CONTEXT.md D-01, D-02, D-03 (locked decisions)
  </read_first>
  <behavior>
    - Production build (`pnpm build`) emits BaseLayout.astro-rendered HTML containing the Umami `<script>` tag
    - Dev server (`astro dev`) and preview deploys (via `astro build` with `import.meta.env.PROD === false`) do NOT emit the Umami tag in HTML output (Vite static-replaces `import.meta.env.PROD` → `false` → conditional short-circuits → Astro renders nothing)
    - `data-website-id` is a static literal placeholder `TODO_PHASE_15_UMAMI_ID` committed to source; Jack replaces pre-deploy per `user_setup.dashboard_config`
    - Tag attributes: `is:inline`, `defer`, `src="https://cloud.umami.is/script.js"`, `data-website-id={WEBSITE_ID}` (or hardcoded string), `data-domains="jackcutrara.com"`. No `async`, no `integrity`, no `crossorigin`
    - Tag placement: immediately after `<SEO … />` (line 80) and BEFORE the first `<Font cssVariable="--font-display-src" />` (line 81)
    - Frontmatter adds ONE new `const WEBSITE_ID = "TODO_PHASE_15_UMAMI_ID";` line (or inlines the literal in the tag — equivalent; RESEARCH.md §3.2 shows the `const` form for readability)
  </behavior>
  <action>
Edit `src/layouts/BaseLayout.astro`:

1. **Frontmatter (top of file, inside the `---` fence):** Add the line:
```ts
const WEBSITE_ID = "TODO_PHASE_15_UMAMI_ID"; // D-03: public literal (Jack replaces with Umami Cloud website UUID pre-deploy, per 15-01-PLAN user_setup.dashboard_config)
```

2. **`<head>` section:** Find the existing `<SEO … />` block ending at line 80. Insert the following Astro JSX fragment BETWEEN the `<SEO … />` closing `/>` (line 80) and the first `<Font cssVariable="--font-display-src" />` (line 81):

```astro
    {/* Phase 15 ANAL-01 / D-01 / D-02 / D-03 — Umami Cloud analytics tag */}
    {/* PROD gate (D-01 belt-and-suspenders): absent from HTML on dev + preview. */}
    {/* data-domains="jackcutrara.com" (D-01 server-side filter); is:inline (Astro landmine L3 — */}
    {/* required to preserve external src + data-* verbatim); defer only (L2 — HTML spec: async+defer → async wins; Umami docs use defer). */}
    {import.meta.env.PROD && (
      <script
        is:inline
        defer
        src="https://cloud.umami.is/script.js"
        data-website-id={WEBSITE_ID}
        data-domains="jackcutrara.com"
      />
    )}
```

CRITICAL invariants (mirror 15-RESEARCH.md §3 + L3 + L4 exactly):
- `is:inline` MUST be present — without it, Astro tries to bundle `cloud.umami.is/script.js` as a local module and mangles `data-*` attributes
- The `{import.meta.env.PROD && <script …/>}` expression lives in the `.astro` TEMPLATE (HTML JSX position), NOT inside another `<script>` block (L4)
- `defer` present, `async` ABSENT (L2 — vendor docs + HTML spec converge on defer-only)
- The `<script>` tag is SELF-CLOSING (`/>`) — Astro supports this syntax for void elements in JSX-ish position
- NO content between the `<script …>` open and close — external `src` only, zero inline body

Do NOT:
- Add an `integrity=` SRI hash — Umami does not publish one (T-15-06 threat accepted; see threat_model block)
- Add `crossorigin="anonymous"` — not documented in Umami's reference snippet
- Enable `data-performance` — CF Web Analytics covers Core Web Vitals per D-04 (duplicate CWV collection wastes budget)
- Enable `data-do-not-track` — Umami is already cookie-free; this flag toggles DNT-header respect which would reduce signal for no gain
- Change the position of `<Font …/>` preloads or `{noindex && <meta …/>}` — the Umami tag slots IN FRONT of `<Font>` per D-02 and AFTER `<SEO>`
- Touch the `<body>` in this task — Plan 02 adds the `<script>` import block for analytics.ts near `<ChatWidget />` (line 97); do not pre-emptively author that here

Run the test suite:
```bash
pnpm test -- tests/build/umami-tag-present.test.ts
```
All 7 assertions MUST go from RED → GREEN. If any stay RED, the tag shape drifted from the assertions — reconcile before committing.

Run the full build to confirm Astro accepts the syntax:
```bash
pnpm build
```
Must exit 0. Zero warnings. `dist/client/index.html` (or any dist/client/*.html) should contain the literal `cloud.umami.is/script.js` string since this is a PROD build.

Run the full test suite to catch any adjacent regressions:
```bash
pnpm test
```
Expected: all pre-existing tests GREEN + 7 new tests GREEN. No pre-existing flips.

Commit message: `feat(15-01): inject Umami analytics tag in BaseLayout with PROD gate (ANAL-01 / D-01..D-03)`
  </action>
  <verify>
    <automated>pnpm test -- tests/build/umami-tag-present.test.ts &amp;&amp; pnpm build 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cloud.umami.is/script.js' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c 'data-domains="jackcutrara.com"' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c 'is:inline' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c 'defer' src/layouts/BaseLayout.astro` returns at least `1` (exactly within the Umami tag; the regex does not need to exclude the word appearing elsewhere — no other `defer` exists in the file pre-plan)
    - `grep -c 'import.meta.env.PROD' src/layouts/BaseLayout.astro` returns exactly `1`
    - `grep -c 'TODO_PHASE_15_UMAMI_ID' src/layouts/BaseLayout.astro` returns at least `1` (the literal placeholder)
    - `grep -c 'async' src/layouts/BaseLayout.astro` returns `0` (confirms L2 resolution — defer only)
    - `grep -c 'integrity=' src/layouts/BaseLayout.astro` returns `0` (T-15-06: SRI not feasible for Umami's rolling script; residual risk accepted)
    - `pnpm test -- tests/build/umami-tag-present.test.ts` exits 0 with all 7 tests GREEN
    - `pnpm build` exits 0 with zero warnings (lightning-css, astro check, ESLint all clean per DEBT-01 baseline)
    - `grep -c 'cloud.umami.is' dist/client/index.html` (or any page HTML under dist/client/) returns at least `1` (confirming PROD build emits the tag)
    - `pnpm test` full suite exits 0 with no pre-existing test regressions
    - Git commit message matches regex `^feat\(15-01\):.*ANAL-01`
  </acceptance_criteria>
  <done>
    BaseLayout.astro has the Umami tag with PROD gate; all 7 tests GREEN; `pnpm build` clean; full suite clean; committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ cloud.umami.is | Third-party JS loaded cross-origin; no SRI; vendor-controlled |
| Browser ↔ jackcutrara.com (CF edge) | CF proxy may auto-inject CF Web Analytics beacon (`static.cloudflareinsights.com`) at edge response time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | Umami event payload | n/a this plan | Plan 02 owns `analytics.ts` forwarder; no event payloads are emitted by this plan's code surface |
| T-15-02 | Integrity | Umami script loading on non-production hostnames (preview subdomain data pollution) | mitigate | D-01 belt-and-suspenders implemented: `{import.meta.env.PROD && <script …/>}` static-replaces the branch to `false` in dev/preview builds so the tag is literally absent from HTML. Second line: `data-domains="jackcutrara.com"` causes Umami's ingest pipeline to drop events from mismatched `window.location.hostname` values. Task 1 test #6 locks the PROD gate; Task 1 test #3 locks the data-domains value. Catches regression if either is removed. |
| T-15-03 | Tampering | SSE `truncated` frame injection | n/a this plan | Plan 04 owns chat.ts SSE parser; not a concern here |
| T-15-04 | Information Disclosure | Outbound-click href metadata | n/a this plan | Plan 02 owns the delegated listener; D-12 hostname+path policy is implemented there |
| T-15-05 | Information Disclosure | Resume-download double-count inflation | n/a this plan | Plan 02 owns the dedup path |
| T-15-06 | Tampering | Umami JS supply-chain (cloud.umami.is compromise injecting malicious JS via the external script tag) | accept | SRI hash is not practical for Umami's auto-updating tracker script (vendor rolls fixes without coordinated hash rotation per 15-RESEARCH.md §1.7). Residual risk accepted: free-tier portfolio, low-value target, no auth/payment surface on the site, no user PII in scope. Mitigating factor: Umami is owned by Umami Software (active maintainer); compromise would affect thousands of sites simultaneously and be detected industry-wide. Annotation lives as the `integrity=` absence — Task 2 acceptance criterion explicitly forbids adding an SRI hash without CONTEXT amendment. |
| T-15-07 | Information Disclosure | CF Web Analytics beacon auto-inject fails silently on custom domain | accept | Per 15-RESEARCH.md §4.5 + A4 MEDIUM confidence: CF auto-inject has community-reported regressions. D-04 fallback path (explicit `<script>` in BaseLayout with same PROD gate) is pre-approved in CONTEXT.md Claude's Discretion. Plan 05 `15-VERIFICATION.md` mandates a post-deploy `curl https://jackcutrara.com/ \| grep cloudflareinsights` check + fallback branching. This plan lands the happy path (no explicit beacon script); Plan 05 verifies. |
</threat_model>

<verification>
**Automated (run at plan end):**
```bash
pnpm test -- tests/build/umami-tag-present.test.ts   # 7/7 GREEN
pnpm build                                            # zero warnings, dist/client/index.html contains "cloud.umami.is"
pnpm test                                             # full suite GREEN, no regressions
```

**Manual (deferred to Plan 05 / 15-VERIFICATION.md):**
- Umami Cloud website entry exists for jackcutrara.com; UUID captured into BaseLayout.astro before merge
- CF Web Analytics toggle enabled on portfolio Pages project (Plan 05 Task: enable + screenshot)
- Post-deploy: `curl -s https://jackcutrara.com/ | grep -c cloud.umami.is` returns `1`
- Post-deploy: `curl -s https://jackcutrara.com/ | grep -c cloudflareinsights` returns `1` (auto-inject happy path) — fallback path if `0`
- Post-deploy: preview subdomain `curl -s https://<preview>.portfolio-5wl.pages.dev/ | grep -c cloud.umami.is` returns `0`

**Requirement mapping:**
- ANAL-01 → test file assertions #1-7 + build output grep
- ANAL-02 → dashboard toggle (operational, Plan 05 verifies)
- ANAL-06 → documented: Umami + CF Web Analytics are cookie-free by vendor contract; Plan 05 DevTools cookie audit confirms
</verification>

<success_criteria>
- [ ] `tests/build/umami-tag-present.test.ts` created with 7 assertions, all GREEN
- [ ] `src/layouts/BaseLayout.astro` modified: Umami tag with `is:inline`, `defer`, `data-website-id={WEBSITE_ID}`, `data-domains="jackcutrara.com"`, `src="https://cloud.umami.is/script.js"`, inside `{import.meta.env.PROD && ...}` gate, placed between `<SEO/>` and `<Font/>` preloads
- [ ] `WEBSITE_ID` committed as `"TODO_PHASE_15_UMAMI_ID"` placeholder (Jack replaces pre-deploy)
- [ ] `pnpm build` exits 0 with zero warnings
- [ ] `pnpm test` full suite GREEN; no pre-existing test regressions
- [ ] `dist/client/*.html` contains the literal `cloud.umami.is` substring (confirms PROD build emits the tag)
- [ ] Two commits: RED test commit + GREEN implementation commit
- [ ] `user_setup.dashboard_config` captures the two operational prerequisites (Umami entry + CF toggle) so Plan 05 can cite concretely
</success_criteria>

<output>
After completion, create `.planning/phases/15-analytics-instrumentation/15-01-SUMMARY.md` documenting:
- Exact line range where Umami tag lands in BaseLayout.astro
- The `TODO_PHASE_15_UMAMI_ID` placeholder location (for Jack's pre-deploy checklist)
- Confirmation that L2 (`async` dropped per HTML spec + Umami docs), L3 (is:inline present), L4 (conditional in template position) landmines are all addressed
- D-04 fallback path status (auto-inject assumed; Plan 05 verifies post-deploy)
- Evidence: full 7-test GREEN output, `pnpm build` clean, grep on `dist/client/index.html`
</output>
