---
phase: 10-page-port
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/contact.astro
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - ABOUT-01
  - ABOUT-02

must_haves:
  truths:
    - "Homepage renders JACK CUTRARA wordmark with accent-red trailing dot"
    - "Homepage renders AVAILABLE FOR WORK status dot and EST. 2024 metadata"
    - "Homepage renders a numbered work list of 3 featured projects with real titles"
    - "Homepage renders an about preview with intro and first paragraph and READ MORE link"
    - "Homepage renders a contact section with email, GITHUB, LINKEDIN, RESUME links"
    - "About page renders intro line plus 3 body paragraphs from about.ts"
    - "About page has no skill icons, no progress bars, no narrative graphics"
    - "Contact page renders the ContactSection composite"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage with hero, work list, about preview, contact section"
      min_lines: 80
    - path: "src/pages/about.astro"
      provides: "About page with editorial structure"
      min_lines: 30
    - path: "src/pages/contact.astro"
      provides: "Contact page rendering ContactSection"
      min_lines: 15
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/primitives/WorkRow.astro"
      via: "import and composition with getCollection"
      pattern: "import WorkRow"
    - from: "src/pages/index.astro"
      to: "src/components/ContactSection.astro"
      via: "import and render in section 03"
      pattern: "import ContactSection"
    - from: "src/pages/index.astro"
      to: "src/data/about.ts"
      via: "import for about preview"
      pattern: "import.*ABOUT_INTRO.*from"
    - from: "src/pages/about.astro"
      to: "src/data/about.ts"
      via: "import for full about copy"
      pattern: "import.*ABOUT"
    - from: "src/pages/contact.astro"
      to: "src/components/ContactSection.astro"
      via: "import and render"
      pattern: "import ContactSection"
---

<objective>
Rewrite the homepage, about page, and contact page from stub placeholders into editorial compositions using Phase 9 primitives and the Plan 02 data files.

Purpose: Delivers HOME-01 through HOME-04 (homepage hero, work list, about preview, contact section), ABOUT-01 and ABOUT-02 (editorial about page with real copy), and the contact page rendering the shared composite. These three pages have no cross-dependencies -- they can be implemented as independent tasks.

Output: Three fully rewritten pages composing primitives, real content, and editorial layout per mockup.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-page-port/10-CONTEXT.md
@.planning/phases/10-page-port/10-RESEARCH.md
@mockup.html

<interfaces>
<!-- Phase 9 primitive props (READ ONLY -- do not modify) -->

From src/components/primitives/Container.astro:
```typescript
interface Props {
  as?: string;  // HTML tag, default "div"
  class?: string;
}
// Usage: <Container> or <Container as="main" class="...">
```

From src/components/primitives/SectionHeader.astro:
```typescript
interface Props {
  number: string;
  title: string;
  count?: string;
}
// Usage: <SectionHeader number="01" title="WORK" count="3 / 6" />
```

From src/components/primitives/WorkRow.astro:
```typescript
interface Props {
  number: string;
  title: string;
  stack: string;
  year: string;
  href: string;
}
// Usage: <WorkRow number="01" title="SeatWatch" stack="TYPESCRIPT . REACT . EXPRESS" year="2025" href="/projects/seatwatch" />
```

From src/components/primitives/StatusDot.astro:
```typescript
interface Props {
  label: string;
}
// Usage: <StatusDot label="AVAILABLE FOR WORK" />
```

From src/components/primitives/MetaLabel.astro:
```typescript
interface Props {
  text: string;
  color?: "ink" | "ink-muted" | "ink-faint";
  class?: string;
}
// Usage: <MetaLabel text="EST. 2024 . OAKLAND, CA" color="ink-muted" />
```

From src/components/ContactSection.astro (Plan 02):
```typescript
interface Props {
  showSectionHeader?: boolean;
}
// Usage: <ContactSection showSectionHeader={true} />
```

From src/data/contact.ts (Plan 02):
```typescript
export const CONTACT = {
  email: "jack@jackcutrara.com",
  github: "https://github.com/jackc625",
  linkedin: "https://linkedin.com/in/jackcutrara",
  x: null as string | null,
  resume: "/jack-cutrara-resume.pdf",
} as const;
```

From src/data/about.ts (Plan 02):
```typescript
export const ABOUT_INTRO = "I'm Jack...";
export const ABOUT_P1 = "I build small...";
export const ABOUT_P2 = "I reach for...";
export const ABOUT_P3 = "Right now...";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite homepage (index.astro) with hero, work list, about preview, contact</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro, mockup.html (lines 148-183 for hero, 188-436 for work section, 438-451 for about, 453-467 for contact, 259-277 for about styles, 282-309 for contact styles, 331-344 for responsive), src/components/primitives/WorkRow.astro, src/components/primitives/SectionHeader.astro, src/components/primitives/StatusDot.astro, src/components/primitives/MetaLabel.astro, src/components/primitives/Container.astro</read_first>
  <action>
Rewrite `src/pages/index.astro` from stub to full editorial homepage per HOME-01 through HOME-04.

**Frontmatter:**
```typescript
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import Container from "../components/primitives/Container.astro";
import SectionHeader from "../components/primitives/SectionHeader.astro";
import WorkRow from "../components/primitives/WorkRow.astro";
import StatusDot from "../components/primitives/StatusDot.astro";
import MetaLabel from "../components/primitives/MetaLabel.astro";
import ContactSection from "../components/ContactSection.astro";
import { ABOUT_INTRO, ABOUT_P1 } from "../data/about";

const allProjects = (await getCollection("projects")).sort(
  (a, b) => a.data.order - b.data.order
);
const featured = allProjects.filter((p) => p.data.featured);
```

**Template structure (4 sections):**

**Section 1: Hero** (HOME-01) -- transcribe mockup.html lines 364-380
- `<section class="hero">` with `<Container>`
- `.hero-grid` div (CSS Grid: 12-col on desktop, 1fr on tablet/mobile)
- `.hero-content` (grid-column 1/span 8): `<h1 class="display">JACK<br />CUTRARA<span class="accent-dot">.</span></h1>` plus `<p class="lead hero-lead">Software engineer building reliable, production-grade systems.</p>`
- `.hero-meta` (grid-column 9/span 4): `<StatusDot label="AVAILABLE FOR WORK" />` plus `<MetaLabel text="EST. 2024 . OAKLAND, CA" color="ink-muted" />`

**Section 2: Work list** (HOME-02) -- `<section class="section works">`
- `<Container>` wrapping `<SectionHeader number="01" title="WORK" count={\`${featured.length} / ${allProjects.length}\`} />`
- `.work-list` div with `{featured.map((p, i) => <WorkRow number={String(i + 1).padStart(2, "0")} title={p.data.title} stack={p.data.techStack.join(" . ").toUpperCase()} year={p.data.year} href={\`/projects/${p.id}\`} />)}`

**Section 3: About preview** (HOME-03) -- `<section class="section about">`
- `<Container>` wrapping `<SectionHeader number="02" title="ABOUT" />`
- `.about-body` div containing: `<p class="about-intro">{ABOUT_INTRO}</p>` then `<p>{ABOUT_P1}</p>` then `<a class="label-mono read-more" href="/about">READ MORE &rarr;</a>`

**Section 4: Contact** (HOME-04) -- `<section class="section contact">`
- `<Container>` wrapping `<ContactSection showSectionHeader={true} />`

**Scoped styles (transcribed from mockup.html):**
```css
/* Hero */
.hero { padding-top: 96px; padding-bottom: 0; }
.hero-grid { display: grid; grid-template-columns: repeat(12, 1fr); gap: 24px; align-items: start; }
.hero-content { grid-column: 1 / span 8; }
.hero-meta { grid-column: 9 / span 4; display: flex; flex-direction: column; gap: 8px; }
.hero-lead { margin-top: 32px; max-width: 40ch; color: var(--ink); }
.accent-dot { color: var(--accent); }

/* About preview */
.about-body { max-width: 68ch; margin-top: 24px; }
.about-intro { font-weight: 500; font-size: 1.375rem; line-height: 1.4; color: var(--ink); margin-bottom: 24px; }
.about-body p:not(.about-intro) { font-weight: 400; font-size: 1.125rem; line-height: 1.6; color: var(--ink); margin-bottom: 16px; }
.read-more { display: inline-block; margin-top: 24px; color: var(--ink-muted); text-decoration: none; }
.read-more:hover { color: var(--accent); }

/* Responsive */
@media (max-width: 1023px) {
  .hero { padding-top: 72px; }
  .hero-grid { grid-template-columns: 1fr; }
  .hero-content, .hero-meta { grid-column: auto; }
  .hero-meta { width: 100%; margin-top: 32px; }
}
@media (max-width: 767px) {
  .hero { padding-top: 48px; }
}
```

Pass `title="Jack Cutrara"` and `description="Software engineer building reliable, production-grade systems."` to BaseLayout.
  </action>
  <verify>
    <automated>grep -c "JACK" src/pages/index.astro && grep -c "WorkRow" src/pages/index.astro && grep -c "ContactSection" src/pages/index.astro && grep -c "ABOUT_INTRO" src/pages/index.astro && grep -c "getCollection" src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro contains `import { getCollection } from "astro:content"`
    - src/pages/index.astro contains `import WorkRow from "../components/primitives/WorkRow.astro"`
    - src/pages/index.astro contains `import ContactSection from "../components/ContactSection.astro"`
    - src/pages/index.astro contains `import { ABOUT_INTRO, ABOUT_P1 } from "../data/about"`
    - src/pages/index.astro contains `.filter((p) => p.data.featured)`
    - src/pages/index.astro contains `JACK` and `CUTRARA` in the display heading
    - src/pages/index.astro contains `accent-dot` class
    - src/pages/index.astro contains `StatusDot` with label "AVAILABLE FOR WORK"
    - src/pages/index.astro contains `SectionHeader` with number "01" and title "WORK"
    - src/pages/index.astro contains `SectionHeader` with number "02" and title "ABOUT"
    - src/pages/index.astro contains `showSectionHeader={true}`
    - src/pages/index.astro contains `READ MORE`
    - src/pages/index.astro contains `.hero-grid` in scoped styles
    - src/pages/index.astro contains responsive breakpoints (max-width: 1023px and max-width: 767px)
  </acceptance_criteria>
  <done>Homepage renders hero with wordmark + metadata, numbered work list of featured projects, about preview with READ MORE link, and contact section with shared composite. Responsive at all breakpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite about page (about.astro) with editorial structure per D-26</name>
  <files>src/pages/about.astro</files>
  <read_first>src/pages/about.astro, mockup.html (lines 438-451 for about section, lines 259-277 for about styles), src/data/about.ts</read_first>
  <action>
Rewrite `src/pages/about.astro` from stub to full editorial about page per ABOUT-01 and ABOUT-02.

**Frontmatter:**
```typescript
import BaseLayout from "../layouts/BaseLayout.astro";
import Container from "../components/primitives/Container.astro";
import SectionHeader from "../components/primitives/SectionHeader.astro";
import { ABOUT_INTRO, ABOUT_P1, ABOUT_P2, ABOUT_P3 } from "../data/about";
```

**Template:**
```astro
<BaseLayout title="About" description="Jack Cutrara -- junior software engineer building production-grade systems.">
  <section class="section about">
    <Container>
      <SectionHeader number="01" title="ABOUT" />
      <div class="about-body">
        <p class="about-intro">{ABOUT_INTRO}</p>
        <p>{ABOUT_P1}</p>
        <p>{ABOUT_P2}</p>
        <p>{ABOUT_P3}</p>
      </div>
    </Container>
  </section>
</BaseLayout>
```

**Scoped styles** (transcribed from mockup.html lines 259-277):
```css
.about-body { max-width: 68ch; margin-top: 24px; }
.about-intro { font-weight: 500; font-size: 1.375rem; line-height: 1.4; color: var(--ink); margin-bottom: 24px; }
.about-body p:not(.about-intro) { font-weight: 400; font-size: 1.125rem; line-height: 1.6; color: var(--ink); margin-bottom: 16px; }
.about-body p:not(.about-intro):last-child { margin-bottom: 0; }
```

No skill icons. No progress bars. No narrative graphics. Pure text composition per ABOUT-01.
  </action>
  <verify>
    <automated>grep -c "ABOUT_INTRO" src/pages/about.astro && grep -c "ABOUT_P3" src/pages/about.astro && grep -c "SectionHeader" src/pages/about.astro && grep -c "about-intro" src/pages/about.astro</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/about.astro contains `import { ABOUT_INTRO, ABOUT_P1, ABOUT_P2, ABOUT_P3 } from "../data/about"`
    - src/pages/about.astro contains `SectionHeader` with number "01" and title "ABOUT"
    - src/pages/about.astro contains `{ABOUT_INTRO}` rendered inside `.about-intro`
    - src/pages/about.astro contains `{ABOUT_P1}`, `{ABOUT_P2}`, `{ABOUT_P3}`
    - src/pages/about.astro does NOT contain any `<img`, `<svg`, `<canvas`, `progress`, or `skill` elements
    - src/pages/about.astro scoped styles contain `.about-body` with `max-width: 68ch`
  </acceptance_criteria>
  <done>About page renders editorial structure: intro line + 3 body paragraphs from about.ts. No icons, no progress bars, no graphics. ABOUT-01 and ABOUT-02 delivered.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite contact page (contact.astro) using ContactSection composite</name>
  <files>src/pages/contact.astro</files>
  <read_first>src/pages/contact.astro, src/components/ContactSection.astro</read_first>
  <action>
Rewrite `src/pages/contact.astro` from stub to standalone contact page.

**Frontmatter:**
```typescript
import BaseLayout from "../layouts/BaseLayout.astro";
import Container from "../components/primitives/Container.astro";
import SectionHeader from "../components/primitives/SectionHeader.astro";
import ContactSection from "../components/ContactSection.astro";
```

**Template:**
```astro
<BaseLayout title="Contact" description="Get in touch with Jack Cutrara.">
  <section class="section">
    <Container>
      <SectionHeader number="01" title="CONTACT" />
      <ContactSection />
    </Container>
  </section>
</BaseLayout>
```

The /contact page uses its own `<SectionHeader number="01" title="CONTACT" />` as the page-level header (since it has no section 02 or 03), and renders `<ContactSection />` without the `showSectionHeader` prop (defaults to false, so the section 03 header is omitted -- it would be redundant with the page-level header).

No additional styles needed -- ContactSection handles its own scoped styles.
  </action>
  <verify>
    <automated>grep -c "ContactSection" src/pages/contact.astro && grep -c "SectionHeader" src/pages/contact.astro && grep -c "Container" src/pages/contact.astro</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/contact.astro contains `import ContactSection from "../components/ContactSection.astro"`
    - src/pages/contact.astro contains `<ContactSection />`  (no showSectionHeader prop)
    - src/pages/contact.astro contains `<SectionHeader number="01" title="CONTACT" />`
    - src/pages/contact.astro does NOT contain inline `style=` attributes
    - src/pages/contact.astro does NOT contain form elements, SVG icons, or CTA buttons
  </acceptance_criteria>
  <done>Contact page renders the shared ContactSection composite with page-level section header. Minimal layout per CONTACT-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content collection -> HTML | Project data from MDX frontmatter rendered into page markup |
| about.ts -> HTML | Static strings rendered into page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | getCollection output | accept | Content is static MDX checked into git. Astro renders at build time. No runtime user input. |
| T-10-05 | Information Disclosure | about.ts strings | accept | Public about page content -- intentionally displayed. |
</threat_model>

<verification>
After all 3 tasks complete:
1. Homepage renders hero, work list, about preview, contact section
2. About page renders intro + 3 paragraphs from about.ts
3. Contact page renders ContactSection composite
4. No stub "redesigning" text remains in any of the 3 pages
5. `pnpm run build` succeeds
</verification>

<success_criteria>
- Homepage delivers HOME-01 (hero), HOME-02 (work list), HOME-03 (about preview), HOME-04 (contact)
- About page delivers ABOUT-01 (editorial structure) and ABOUT-02 (real copy)
- Contact page renders shared ContactSection
- All three pages compose Phase 9 primitives and Plan 02 data files
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-port/10-03-SUMMARY.md`
</output>
