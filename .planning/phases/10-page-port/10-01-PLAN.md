---
phase: 10-page-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/MASTER.md
  - .planning/REQUIREMENTS.md
  - src/content.config.ts
  - src/content/projects/clipify.mdx
  - src/content/projects/crypto-breakout-trader.mdx
  - src/content/projects/nfl-predict.mdx
  - src/content/projects/optimize-ai.mdx
  - src/content/projects/seatwatch.mdx
  - src/content/projects/solsniper.mdx
  - astro.config.mjs
autonomous: true
requirements:
  - WORK-03

must_haves:
  truths:
    - "MASTER.md section 6.1 documents the state-indicator animation carve-out"
    - "MASTER.md section 9 or 10 documents the round chat bubble exception"
    - "MASTER.md sections 5.2 and 5.8 no longer reference X in social rows"
    - "REQUIREMENTS.md CONTACT-01 lists GITHUB, LINKEDIN, RESUME without X"
    - "REQUIREMENTS.md CONTACT-02 reflects the shipped PDF wording"
    - "Every .mdx file has a year frontmatter field matching /^\\d{4}$/"
    - "content.config.ts schema includes year: z.string().regex(/^\\d{4}$/)"
    - "astro.config.mjs sets Shiki theme to github-light"
    - "pnpm run build succeeds with the new schema and all 6 MDX files"
  artifacts:
    - path: "design-system/MASTER.md"
      provides: "Amended design contract with typing-dot carve-out and chat bubble exception"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended CONTACT-01 and CONTACT-02 wording"
    - path: "src/content.config.ts"
      provides: "Updated Zod schema with year field"
    - path: "astro.config.mjs"
      provides: "Shiki github-light theme configuration"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/projects/*.mdx"
      via: "Zod schema validation at build time"
      pattern: "year: z\\.string\\(\\)\\.regex"
---

<objective>
Land the head-of-phase docs amendments and content schema changes that unblock all downstream plans.

Purpose: D-30 requires MASTER.md and REQUIREMENTS.md to be amended in a single docs commit before any code changes. D-03 requires the year field in the content schema with all 6 MDX files backfilled atomically. D-13 requires Shiki theme configuration before any MDX code blocks render.

Output: Amended MASTER.md, amended REQUIREMENTS.md, updated content.config.ts with year field, all 6 MDX files with year frontmatter, astro.config.mjs with github-light Shiki theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-page-port/10-CONTEXT.md
@.planning/phases/10-page-port/10-RESEARCH.md
@design-system/MASTER.md
@.planning/REQUIREMENTS.md
@src/content.config.ts
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend MASTER.md and REQUIREMENTS.md (head-of-phase docs commit per D-30)</name>
  <files>design-system/MASTER.md, .planning/REQUIREMENTS.md</files>
  <read_first>design-system/MASTER.md, .planning/REQUIREMENTS.md, .planning/phases/10-page-port/10-CONTEXT.md</read_first>
  <action>
Amend design-system/MASTER.md with four changes per D-30:

1. **Section 6.1 (Motion)** -- Add a new bullet to the allowed-motion list:
   "Status indicators (typing dots, loading spinners) may use looped CSS @keyframes when actively signaling state -- these are functional indicators, not entrance animations."

2. **Section 5.2 (Footer)** -- In the mobile footer social row documentation, change the four-link form `GITHUB . LINKEDIN . X . EMAIL` to three-link form `GITHUB . LINKEDIN . EMAIL`. Remove any mention of X/Twitter from the footer spec.

3. **Section 5.8 (MobileMenu)** -- In the MobileMenu social row documentation, change the four-link form `GITHUB . LINKEDIN . X . EMAIL` to three-link form `GITHUB . LINKEDIN . EMAIL`. Remove any mention of X/Twitter.

4. **Section 9 or new Section 10 (Chat Widget)** -- Add documentation stating: "The round accent chat bubble (48x48px, background: var(--accent), border-radius: 50%) is the only round surface in the editorial system. This is a deliberate exception to the flat-rectangle grammar -- the bubble serves as the accent-red beacon signaling the chatbot exists."

Amend .planning/REQUIREMENTS.md with two changes per D-25:

1. **CONTACT-01** -- Change social link list from `GITHUB . LINKEDIN . X . RESUME` to `GITHUB . LINKEDIN . RESUME`. The "no icons, no contact form, no CTA buttons" wording is preserved. Specifically update the line to read:
   `- [ ] **CONTACT-01**: Contact section is minimal: \`GET IN TOUCH\` label + email + inline social links (\`GITHUB . LINKEDIN . RESUME\`), no icons, no contact form, no CTA buttons`

2. **CONTACT-02** -- Update "placeholder PDF shipped initially" wording to reflect the shipped state. Change to:
   `- [ ] **CONTACT-02**: Resume link uses \`<a download>\` and points to \`/jack-cutrara-resume.pdf\``

Both files are amended in a single commit. After this commit, MASTER.md and REQUIREMENTS.md stay byte-locked for the rest of Phase 10.
  </action>
  <verify>
    <automated>grep -c "Status indicators" design-system/MASTER.md && grep -c "round accent chat bubble" design-system/MASTER.md && grep "CONTACT-01" .planning/REQUIREMENTS.md | grep -v "X" | grep -c "RESUME"</automated>
  </verify>
  <acceptance_criteria>
    - design-system/MASTER.md contains the string "Status indicators (typing dots, loading spinners) may use looped CSS @keyframes"
    - design-system/MASTER.md contains the string "round accent chat bubble"
    - design-system/MASTER.md section 5.2 does NOT contain the string " X " in the social row spec
    - design-system/MASTER.md section 5.8 does NOT contain the string " X " in the social row spec
    - .planning/REQUIREMENTS.md CONTACT-01 line contains "GITHUB" and "LINKEDIN" and "RESUME" but NOT "X"
    - .planning/REQUIREMENTS.md CONTACT-02 line does NOT contain "placeholder PDF shipped initially"
  </acceptance_criteria>
  <done>MASTER.md has all four amendments (section 6.1 typing-dot carve-out, section 5.2 X drop, section 5.8 X drop, section 9/10 chat bubble exception). REQUIREMENTS.md has both amendments (CONTACT-01 X drop, CONTACT-02 PDF wording). Single docs commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Add year field to content schema, backfill all 6 MDX files, and configure Shiki theme</name>
  <files>src/content.config.ts, src/content/projects/seatwatch.mdx, src/content/projects/nfl-predict.mdx, src/content/projects/solsniper.mdx, src/content/projects/optimize-ai.mdx, src/content/projects/clipify.mdx, src/content/projects/crypto-breakout-trader.mdx, astro.config.mjs</files>
  <read_first>src/content.config.ts, src/content/projects/seatwatch.mdx, src/content/projects/nfl-predict.mdx, src/content/projects/solsniper.mdx, src/content/projects/optimize-ai.mdx, src/content/projects/clipify.mdx, src/content/projects/crypto-breakout-trader.mdx, astro.config.mjs</read_first>
  <action>
**Part A: Schema change + MDX backfill (D-03)**

1. In `src/content.config.ts`, add a required `year` field to the projects schema:
   ```typescript
   year: z.string().regex(/^\d{4}$/),
   ```
   Place it after the `order` field. The field is a string (not number) per D-03 to keep template-literal output clean and tabular-aligned.

2. Add `year` frontmatter to all 6 MDX files. **The user (Jack) must supply the exact year values.** Present the file list and ask Jack to provide years before writing. Expected range is 2024-2026. Add the `year` field in each MDX file's frontmatter block, after the `order` field line:
   - `src/content/projects/seatwatch.mdx` -- year: "YYYY" (Jack supplies)
   - `src/content/projects/nfl-predict.mdx` -- year: "YYYY" (Jack supplies)
   - `src/content/projects/solsniper.mdx` -- year: "YYYY" (Jack supplies)
   - `src/content/projects/optimize-ai.mdx` -- year: "YYYY" (Jack supplies)
   - `src/content/projects/clipify.mdx` -- year: "YYYY" (Jack supplies)
   - `src/content/projects/crypto-breakout-trader.mdx` -- year: "YYYY" (Jack supplies)

3. Schema change and all 6 MDX backfills MUST land in the same commit -- if any file is missing the field, the build breaks (Zod required field).

Addresses review concern: Codex flagged "Jack supplies values" as external dependency. Mitigated by presenting the file list immediately and requiring all 6 values before any write. If Jack provides partial values, do NOT commit -- wait for all 6.

**Part B: Shiki theme configuration (D-13)**

4. Add `markdown.shikiConfig.theme` to `astro.config.mjs` per D-13. In the `defineConfig({})` object, add:

```javascript
markdown: {
  shikiConfig: {
    theme: 'github-light',
  },
},
```

Place it after the `site` field and before `adapter`. This changes the default Shiki theme from `github-dark` to `github-light` so MDX code blocks render with a light background that fits the warm off-white editorial design.

5. After committing the Shiki theme change, run `pnpm run build` to verify no visual regression -- the build must succeed and existing code blocks (if any) must now render with light backgrounds. Addresses review concern: Codex flagged that Shiki theme change may affect existing MDX/code styling globally without visual regression check.
  </action>
  <verify>
    <automated>grep -c "year: z.string().regex" src/content.config.ts && grep -l "^year:" src/content/projects/*.mdx | wc -l && grep -c "github-light" astro.config.mjs && pnpm run build 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - src/content.config.ts contains `year: z.string().regex(/^\d{4}$/)`
    - All 6 files in src/content/projects/*.mdx contain a `year:` frontmatter field with a 4-digit string value
    - grep -c 'year:' across all 6 MDX files returns 6
    - astro.config.mjs contains `theme: 'github-light'`
    - astro.config.mjs contains `shikiConfig`
    - astro.config.mjs contains `markdown:`
    - `pnpm run build` exits 0 (validates schema + Shiki theme in one pass)
  </acceptance_criteria>
  <done>Content schema has year field. All 6 MDX files have year values supplied by Jack. Shiki theme is github-light. Build passes with the new schema and theme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time content validation | Zod schema validates MDX frontmatter at build time; malformed year values caught before deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | content.config.ts schema | accept | Schema is build-time only; no runtime attack surface. Year regex `/^\d{4}$/` prevents injection. |
</threat_model>

<verification>
After both tasks complete:
1. `grep "Status indicators" design-system/MASTER.md` returns a match
2. `grep "round accent chat bubble" design-system/MASTER.md` returns a match
3. `grep "year: z.string().regex" src/content.config.ts` returns a match
4. All 6 MDX files have `year:` in frontmatter
5. `grep "github-light" astro.config.mjs` returns a match
6. `pnpm run build` succeeds (validates schema + Shiki theme + all page routes)
</verification>

<success_criteria>
- MASTER.md and REQUIREMENTS.md are amended per D-25 and D-30
- Content schema includes year field per D-03
- All 6 MDX files have year values
- Shiki theme is github-light per D-13
- Build passes (validates Shiki produces no visual regression in build output)
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-port/10-01-SUMMARY.md`
</output>
