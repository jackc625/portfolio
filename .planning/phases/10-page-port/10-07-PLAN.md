---
phase: 10-page-port
plan: 07
type: execute
wave: 6
depends_on: [03, 04, 05, 06]
files_modified: []
autonomous: false

requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - ABOUT-01
  - ABOUT-02
  - WORK-01
  - WORK-02
  - WORK-03
  - CONTACT-01
  - CONTACT-02
  - CHAT-01
  - CHAT-02

must_haves:
  truths:
    - "pnpm run build succeeds with zero errors and generates all 6 project detail routes"
    - "pnpm run lint is clean"
    - "pnpm run check (astro check) is clean"
    - "pnpm run test (vitest) is all green"
    - "Chat opens, sends, streams SSE, copies, closes on at least one page"
    - "Chat history persists across page navigation and replays without duplication"
    - "Homepage shows exactly 3 featured projects and /projects shows all 6"
    - "Homepage visually matches mockup.html at 1440px and 375px"
    - "No placeholder 'redesigning' text remains on any page"
  artifacts: []
  key_links: []
---

<objective>
Run the full Phase 10 verification gate: automated build/lint/check/test plus manual chat smoke test, persistence test, and visual parity check with specific pass/fail assertions.

Purpose: D-32 defines the Phase 10 ship gate. All 13 requirements must be verified. Automated tools catch schema errors, type errors, and lint violations. Manual tests verify chat functionality, persistence behavior, and visual parity against mockup.html.

Addresses review concern (Codex HIGH + Gemini MEDIUM): Verification gate now includes specific assertions instead of a generic checklist. Explicit checks for: 3 featured items on homepage, 6 projects on /projects, all 6 detail routes build, repeated open/close duplication, restore-after-refresh.

Output: Verified phase -- all automated gates pass, manual tests confirmed, phase ready for Phase 11 polish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-page-port/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification gate with specific route and content assertions</name>
  <files></files>
  <read_first>.planning/phases/10-page-port/10-CONTEXT.md (D-32 verification gate spec)</read_first>
  <action>
Run the 4-point automated verification gate per D-32, PLUS specific content assertions that go beyond "build passes."

**Part A: Standard automated gate**
1. `pnpm run build` -- must succeed with zero errors.
2. `pnpm run lint` -- must be clean (no ESLint errors).
3. `pnpm run check` -- must be clean (astro check, no type errors).
4. `pnpm run test` -- must be all green (vitest, existing markdown sanitization + API tests).

**Part B: Specific content assertions (addresses Codex HIGH concern)**

After build succeeds, verify the built output contains the expected content:

5. **6 project detail routes exist:** Check that `dist/projects/seatwatch/`, `dist/projects/nfl-predict/`, `dist/projects/solsniper/`, `dist/projects/optimize-ai/`, `dist/projects/clipify/`, `dist/projects/crypto-breakout-trader/` directories all exist in the build output. Run:
   ```bash
   ls dist/projects/*/index.html | wc -l
   ```
   Must return 6.

6. **Homepage has exactly 3 featured work rows:** Grep the built homepage for WorkRow links:
   ```bash
   grep -c 'href="/projects/' dist/index.html
   ```
   Must return 3 (the 3 featured projects linked from work rows). The actual project names should be real titles (SeatWatch, NFL Prediction Model, SolSniper), NOT "Project One" or placeholder text.

7. **Projects index has all 6 work rows:** Grep the built projects page:
   ```bash
   grep -c 'href="/projects/' dist/projects/index.html
   ```
   Must return 6.

8. **No placeholder text remains:** Grep across all built HTML for stub text:
   ```bash
   grep -rl "redesigning" dist/ | wc -l
   ```
   Must return 0.

9. **Privacy note is correct:** Grep the built output for the updated privacy text:
   ```bash
   grep -r "stored locally" dist/ | wc -l
   ```
   Must return >= 1 (appears on every page via ChatWidget).

10. **Resume download link present:** Grep for the download attribute:
    ```bash
    grep -r 'download.*resume\|resume.*download' dist/ | head -5
    ```
    Must show at least one match with `jack-cutrara-resume.pdf`.

Report results as a table: Check | Expected | Actual | PASS/FAIL

If any command fails, diagnose and fix the issue before proceeding to the manual gate.
  </action>
  <verify>
    <automated>pnpm run build && pnpm run lint && pnpm run check && pnpm run test && ls dist/projects/*/index.html 2>/dev/null | wc -l && grep -c 'href="/projects/' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - pnpm run build exits with code 0
    - pnpm run lint exits with code 0
    - pnpm run check exits with code 0
    - pnpm run test exits with code 0 and all tests pass
    - `ls dist/projects/*/index.html | wc -l` returns 6 (all project detail routes)
    - `grep -c 'href="/projects/' dist/index.html` returns 3 (homepage featured count per D-02)
    - `grep -c 'href="/projects/' dist/projects/index.html` returns 6 (all projects)
    - `grep -rl "redesigning" dist/ | wc -l` returns 0 (no placeholder text)
    - `grep -r "stored locally" dist/ | wc -l` returns >= 1 (privacy note updated)
  </acceptance_criteria>
  <done>All 4 automated gates pass. All 6 specific content assertions verified: 6 project routes, 3 featured on homepage, 6 on /projects, no placeholder text, correct privacy note, resume download link present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification -- chat smoke test + persistence test + visual parity</name>
  <what-built>
Complete Phase 10 page port: homepage with hero/work list/about preview/contact section, about page with editorial copy, contact page with shared composite, projects index with numbered list, project detail with editorial case study layout, chat widget restyled to editorial chrome, and chat persistence via localStorage.
  </what-built>
  <how-to-verify>
Start the dev server (`pnpm run dev`) and verify these three manual gates with specific pass/fail criteria:

**Gate 1: Chat smoke test** (on any page)
1. Click the red accent chat bubble (bottom-right) -- PASS if panel opens with flat-rectangle chrome (border-radius: 0, no shadow, 1px rule border)
2. Verify header says "ASK JACK'S AI" with close X button
3. Click a starter chip -- PASS if it sends the prompt text and chip disappears
4. Verify SSE streaming response -- PASS if text appears progressively (not all at once)
5. Verify typing dots bounce while bot is responding -- PASS if 3 dots animate with staggered delay
6. Hover over bot message -- PASS if "COPY" text label appears (not SVG icon)
7. Click COPY -- PASS if clipboard has message text and button shows "COPIED" briefly in accent color
8. Press Tab key repeatedly -- PASS if focus cycles within panel (focus trap)
9. Press Escape -- PASS if panel closes and focus returns to bubble
10. Verify bubble is the ONLY round surface -- PASS if panel, chips, textarea, send button all have sharp corners

**Gate 2: Chat persistence test** (specific assertions per review feedback)
1. Open chat and send a message (e.g., "Hello")
2. Wait for bot response to complete fully
3. Navigate to a different page (e.g., /about)
4. Open the chat panel -- PASS if previous messages (your message + bot response) appear
5. Close and re-open the panel -- PASS if messages appear exactly once (no duplication)
6. Close panel, re-open again -- PASS if still no duplication (tests repeated open/close guard)
7. Hard refresh the page (Ctrl+Shift+R) -- PASS if opening chat still shows history (localStorage survives refresh)
8. Verify privacy note says "Conversations stored locally for 24h." -- PASS if exact text visible
9. (Optional) Open devtools > Application > Local Storage -- verify "chat-history" key exists with `version: 1`, messages array, and lastActive timestamp

**Gate 3: Visual parity check** (specific pages and criteria)
1. **Homepage at 1440px:** Compare against mockup.html -- PASS if:
   - JACK CUTRARA wordmark visible with red accent dot
   - Status dot says AVAILABLE FOR WORK
   - Metadata says EST. 2024 . OAKLAND, CA
   - Section 01 WORK shows exactly 3 project rows with real titles (SeatWatch, etc.)
   - Section 02 ABOUT shows intro paragraph + READ MORE link
   - Section 03 CONTACT shows GET IN TOUCH + email + GITHUB . LINKEDIN . RESUME
2. **Homepage at 375px:** PASS if hero stacks vertically, sections compress, no horizontal scroll
3. **Project detail (/projects/seatwatch):** PASS if:
   - Mono metadata header shows YEAR . TECHSTACK
   - Large title in h1-section weight
   - Tagline in lead weight
   - MDX body with section-sign mono h2 labels
   - NextProject link at bottom
4. **About page:** PASS if intro (larger weight) + 3 body paragraphs, no icons/graphics
5. **Contact page:** PASS if GET IN TOUCH + email + social links, resume has download attribute
6. **Projects index:** PASS if 6 numbered rows with real project names

This is sanity, not Lighthouse -- Phase 11 owns the full audit.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced by verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Tampering | Full phase | mitigate | Automated gate (build + lint + check + test) catches regressions. Content assertions verify expected route count and featured project count. Manual gate verifies chat security features (focus trap, XSS sanitization via DOMPurify, rate limiting) are functional. |
</threat_model>

<verification>
All gates from D-32 plus specific assertions:
1. pnpm run build -- PASS
2. pnpm run lint -- PASS
3. pnpm run check -- PASS
4. pnpm run test -- PASS
5. 6 project detail routes in dist/ -- PASS
6. 3 featured projects on homepage -- PASS
7. 6 projects on /projects -- PASS
8. 0 placeholder text occurrences -- PASS
9. Privacy note "stored locally" present -- PASS
10. Manual chat smoke test -- PASS (human verified)
11. Manual chat persistence test with open/close/refresh -- PASS (human verified)
12. Manual visual parity check (6 specific page/viewport checks) -- PASS (human verified)
</verification>

<success_criteria>
- All 4 automated gates pass
- All 6 content assertions verified (routes, featured count, placeholder absence)
- Chat smoke test passes (open, send, SSE stream, copy, focus trap, all sharp corners except bubble)
- Chat persistence test passes with specific duplication and refresh checks
- Visual parity check passes on all 6 page/viewport combinations
- No placeholder text remains
- Phase 10 complete -- ready for Phase 11 polish
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-port/10-07-SUMMARY.md`
</output>
