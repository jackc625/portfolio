---
phase: 25-chat-knowledge-refresh-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/build/chat-context-integrity.test.ts
  - tests/build/chat-knowledge-voice.test.ts
  - tests/api/prompt-injection.test.ts
  - tests/fixtures/chat-eval-dataset.ts
autonomous: true
requirements: [CHAT-10, CHAT-11]
must_haves:
  truths:
    - "chat-context-integrity asserts 7 project slugs (incl. multi-chain-evm) and no longer bans #7 content (D-04)"
    - "chat-knowledge-voice walks experience as an array of summary/role strings, not a single string (D-09)"
    - "prompt-injection asserts the #7 topic ban directive is ABSENT and grounded anchors match the new full-time positioning (D-05)"
    - "a new education-wiring assertion pins education to WGU / May 2026 / VT transfer / LPI cert derived from education.ts (D-07)"
    - "a new experience-array presence assertion pins Holloway-first reverse-chron ordering with Balfour present (D-01, D-03, D-09)"
    - "the canonical FIRST_PERSON_LEAK regex stays byte-identical to build-chat-context.mjs and chat-voice-split.test.ts (D-13)"
  artifacts:
    - tests/build/chat-context-integrity.test.ts
    - tests/build/chat-knowledge-voice.test.ts
    - tests/api/prompt-injection.test.ts
    - tests/fixtures/chat-eval-dataset.ts
  key_links:
    - "all four files import/exercise src/data/portfolio-context.json + buildSystemPrompt — the new assertions are EXPECTED RED until 25-03 regenerates the corpus and 25-02 removes the #7 ban (mirrors the Phase 22-01 Wave-0 tests-first pattern)"
---

<objective>
Retarget the four chat-side test/fixture files that are HARD-PINNED to the pre-#7, pre-repositioning world so they define the Phase 25 target BEFORE the build-script change (25-03) and copy change (25-02) land. This is the Nyquist Wave-0 gate: the suite states the contract, then 25-02/25-03 make it green.

Purpose: Without these retargets the suite silently guards the old behavior (exactly 6 projects / "never mention multi-dex" / "entry-level" positioning / string `experience`) and would false-fail the moment #7 and the structured experience array land. Editing them first makes the target unambiguous and catches the #1 regression risk (a string-only `experience` walk that silently skips array entries).
Output: Four retargeted test/fixture files. New assertions are intentionally RED until 25-02 (removes the #7 ban, locks positioning wording) and 25-03 (regenerates the corpus as 7 projects + structured experience array + education from education.ts).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-RESEARCH.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-VALIDATION.md
@tests/build/chat-context-integrity.test.ts
@tests/build/chat-knowledge-voice.test.ts
@tests/api/prompt-injection.test.ts
@tests/fixtures/chat-eval-dataset.ts
</context>

## Artifacts this phase produces (referenced by all plans — exclude from drift/source-grounding)

This plan authors NEW test assertions that reference symbols other 25-plans create. Do NOT treat these as "undefined" — they are produced this phase:
- `education.graduation` = "May 2026", `education.transferredFrom` = "Virginia Tech", `education.certifications` = ["LPI Linux Essentials"] (emitted by 25-03 `parseEducation`).
- `experience` is an ARRAY of `{ role, company, dateRange, summary }` (emitted by 25-03 experience-collection reader), reverse-chronological (Holloway first, Balfour second).
- The `multi-chain-evm` project slug present in the corpus (25-03 lifts the exclusion).
- The new positioning anchor tokens "looking for" + "full-time" in the knowledge block (25-02 `about-chat.ts` P3 rewrite).
- The #7 topic-ban directive REMOVED from `system-prompt.ts` (25-02).

<tasks>

<!-- planner-discipline-allow: multi-chain-evm -->
<!-- planner-discipline-allow: PROJECTS_7_REGEXES -->

<task type="auto">
  <name>Task 1: Retarget chat-context-integrity — 7 slugs, drop the #7-leak block, add education + experience assertions</name>
  <files>tests/build/chat-context-integrity.test.ts</files>
  <read_first>
    - tests/build/chat-context-integrity.test.ts (full — the file being edited; note EXPECTED_SLUGS :17-24, PROJECTS_7_REGEXES :26-31, count :45-47, the #7-leak test :49-58, the D-08 key tests :101-121, Daytrade truncation :123-135)
    - 25-RESEARCH.md §7.1 (the exact assertions to change) + §3 (the emitted education shape) + §2a (the experience array shape)
    - 25-PATTERNS.md "Test edits" (chat-context-integrity analog block)
    - src/data/education.ts (the SSoT values the education-wiring assertion pins: degree "B.S. Computer Science", institution "Western Governors University", date "May 2026", transferredFrom "Virginia Tech", CREDENTIALS[0].name "LPI Linux Essentials")
  </read_first>
  <action>
    Edit tests/build/chat-context-integrity.test.ts:
    (1) Add "multi-chain-evm" to EXPECTED_SLUGS (:17-24) so the sorted array is the 7 slugs clipify, daytrade, multi-chain-evm, nfl-predict, optimize-ai, seatwatch, solsniper (keep alphabetical to match the `.sort()` on :41). Update the two test titles that say "6 expected" / "exactly 6" to "7".
    (2) Change the count assertion `expect((portfolioContext.projects ...).length).toBe(6)` (:46) to `toBe(7)`.
    (3) DELETE the PROJECTS_7_REGEXES const (:26-31) and DELETE the entire "no Projects/7 content leaks into the merged JSON" test (:49-58) — the exclusion it guards is deliberately lifted (D-04). Do not leave a dangling reference to PROJECTS_7_REGEXES.
    (4) ADD an education-wiring test: assert portfolioContext.education.graduation === "May 2026", .transferredFrom === "Virginia Tech", .school === "Western Governors University", .degree === "B.S. Computer Science", and (education.certifications as string[]).includes("LPI Linux Essentials"). These are the exact values 25-03 parseEducation emits from education.ts (D-07).
    (5) ADD an experience-array presence test: assert Array.isArray(portfolioContext.experience) and length === 2; entry[0].company === "Holloway Company" (reverse-chron: Holloway first — D-01/D-09); some entry has company === "Balfour Beatty" (D-03); every entry has a non-empty string summary (>40 chars for Holloway, >0 for Balfour). Type the entries inline as Array<{ role: string; company: string; dateRange: string; summary: string }>.
    Leave the caseStudy / extendedReference / token-floor / D-08-key / Daytrade-truncation tests unchanged.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/chat-context-integrity.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - The file loads with no TypeScript/parse error; `grep -c "multi-chain-evm" tests/build/chat-context-integrity.test.ts` >= 1 and `grep -c "PROJECTS_7_REGEXES" tests/build/chat-context-integrity.test.ts` == 0.
    - The education-wiring and experience-array tests exist and reference education.graduation/transferredFrom/certifications and experience[0].company.
    - The slug/count/education/experience assertions are EXPECTED RED now (corpus still ships 6 projects + string experience + stale education); they go GREEN after 25-03 regenerates portfolio-context.json. The unchanged tests (token floor, D-08 keys, Daytrade truncation) stay GREEN.
  </acceptance_criteria>
  <done>chat-context-integrity targets 7 slugs, the #7-content-leak block + its const are removed, and education-wiring + experience-array presence assertions exist (RED until 25-03 regen).</done>
</task>

<task type="auto">
  <name>Task 2: chat-knowledge-voice — walk experience as an array, not a string</name>
  <files>tests/build/chat-knowledge-voice.test.ts</files>
  <read_first>
    - tests/build/chat-knowledge-voice.test.ts (full — note the FIRST_PERSON_LEAK regex :39-40, the B1 self-test :42-108, the string-typed experience assertion :117-131, the per-project loop :133-148)
    - 25-RESEARCH.md §7.4 + §2b (the array walk) + §10 (regex triplication — DO NOT change the regex)
    - 25-PATTERNS.md "chat-knowledge-voice.test.ts" analog block
  </read_first>
  <action>
    Edit the "no first-person leak in about.{intro,p1,p2,p3} or experience" test (:117-131). The `fields` array currently includes `["experience", ctx.experience]` and asserts every value `toBeTypeOf("string")`. After D-09 `ctx.experience` is an ARRAY. Replace the single experience entry with a walk that flat-maps each array entry's string fields into the `fields` list: for each `e` in `ctx.experience` (guard Array.isArray), push `[`experience[${i}].summary`, e.summary]` and `[`experience[${i}].role`, e.role]`. Keep the about.intro/p1/p2/p3 entries and the `not.toMatch(FIRST_PERSON_LEAK)` + `toBeTypeOf("string")` assertions applied per-field. Do NOT touch FIRST_PERSON_LEAK (:40) — it MUST stay byte-identical to build-chat-context.mjs:93 and chat-voice-split.test.ts:33 (D-13/§10). Do NOT touch the B1 self-test or the "Jack is a junior software engineer" negative-control sample (:97) — it is a regex control, unrelated to positioning.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/chat-knowledge-voice.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - The file loads with no parse error; the experience-leak test iterates array entries (grep confirms `experience[` index syntax present) and no longer asserts `ctx.experience` is a string.
    - FIRST_PERSON_LEAK regex on :40 is unchanged (byte-identical to build-chat-context.mjs:93).
    - The B1 self-test (KNOWN_LEAKS + SAFE negative control) stays GREEN. The experience-array walk is EXPECTED RED until 25-03 emits the array; it goes GREEN after regen.
  </acceptance_criteria>
  <done>the experience-leak test walks the array's summary/role strings; FIRST_PERSON_LEAK regex is byte-identical to the build script; the B1 self-test stays GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: prompt-injection + eval-dataset — invert the #7 ban assertion, count 6->7, retarget positioning anchors</name>
  <files>tests/api/prompt-injection.test.ts, tests/fixtures/chat-eval-dataset.ts</files>
  <read_first>
    - tests/api/prompt-injection.test.ts (:16-27 imports; :171-173 the #7-ban assertion; :175-184 the grounded-anchor loop; :279-298 the "exactly 6 generated-context projects" test)
    - tests/fixtures/chat-eval-dataset.ts (:134-185 groundedQA — esp. "What does Jack do currently?" :175-179 anchors ["looking for","entry-level"]; :192-243 voiceSpotChecks — esp. :239-242; GLOBAL_BANNED_REGEXES :38-45)
    - 25-RESEARCH.md §7.2 + §7.3 + §9 (the ban is removed in 25-02; scope the negative assertion to the security directive, NOT the whole prompt, because the knowledge block now legitimately contains #7 prose)
    - 25-PATTERNS.md "prompt-injection.test.ts" + "chat-eval-dataset.ts" analog blocks
  </read_first>
  <action>
    In tests/api/prompt-injection.test.ts:
    (1) Replace the "contains the Projects/7 (D-04) banlist reinforcement" test (:171-173). Instead of asserting the ban is present, assert the ban DIRECTIVE is ABSENT and scoped so the knowledge-block #7 prose does not false-match: assert `expect(prompt).not.toMatch(/Never discuss.{0,80}(MULTI[- ]?DEX|crypto arbitrage)/is)`. Rename the test to reflect that the #7 topic ban is lifted (CHAT-11 / §9). Do NOT assert `not.toMatch(/MULTI-DEX/i)` on the whole prompt — the corpus now contains #7 content and that would false-fail.
    (2) Change `expect(projectPages.size).toBe(6)` (:293) to `toBe(7)`. Update the surrounding test title from "exactly 6" to "exactly 7". Leave the `referencedProjects ... toBeGreaterThanOrEqual(6)` assertion (:294-297) as-is (the six original project anchors still resolve).
    Leave every injection-vector, refusal-copy, drift-guard, and section-tag test unchanged (the <security> block is preserved byte-intact by 25-02).

    In tests/fixtures/chat-eval-dataset.ts:
    (3) Update the "What does Jack do currently?" groundedQA anchors (:177) from ["looking for","entry-level"] to ["looking for","full-time"] — the exact anchor tokens 25-02 locks into ABOUT_CHAT_P3. Keep the fabricationBanlist ["senior","10 years"].
    (4) ADD one groundedQA entry for #7 CHAT-11 positive coverage: question "Tell me about the Multi-Chain EVM trader", requiredAnchors ["Multi-Chain EVM"], fabricationBanlist []. ("Multi-Chain EVM Trader" is the corpus project name, so the anchor resolves once #7 lands.)
    (5) Update the voiceSpotChecks "Is Jack currently looking..." gold line (:239-242) to third-person new-grad/full-time wording for consistency (consumed by no test — cosmetic). Leave GLOBAL_BANNED_STRINGS and GLOBAL_BANNED_REGEXES unchanged (they gate attack-vector refusals, which never mention #7).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/api/prompt-injection.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - prompt-injection.test.ts: the #7-ban test now asserts absence (scoped `Never discuss ... MULTI-DEX|crypto arbitrage` regex not present); projectPages.size assertion reads toBe(7). All injection/refusal/drift-guard tests unchanged.
    - chat-eval-dataset.ts: "current" anchors are ["looking for","full-time"]; a "Multi-Chain EVM trader" groundedQA entry exists.
    - The count-7, new-anchor, and #7-groundedQA assertions are EXPECTED RED until 25-02 removes the ban + locks wording and 25-03 regenerates the corpus; the ban-absent assertion goes GREEN once 25-02 removes the directive.
  </acceptance_criteria>
  <done>prompt-injection asserts the #7 topic-ban directive is absent (scoped) + project count reads 7; the fixture carries the full-time positioning anchors and a Multi-Chain EVM groundedQA entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor -> chat API | untrusted user messages reach `buildSystemPrompt`; the prompt-injection battery is the guard these tests exercise |
| build sources -> cached system prompt | corpus + prompt template become the cached `<knowledge>`/`<security>` blocks; these tests assert the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Information disclosure | prompt-injection battery / `<security>` assertions | medium | mitigate | These tests keep every injection-vector, tiered-refusal, section-tag, and drift-guard assertion intact; ONLY the over-broad #7 topic-ban assertion is inverted. The battery still proves the preserved defenses. |
| T-25-02 | Tampering (voice break) | FIRST_PERSON_LEAK regex + experience-array walk | medium | mitigate | The array-walk retarget makes the leak sweep reach the new experience entries (closes the string-only-walk blind spot); the regex stays byte-identical across all three sites (D-13). |
| T-25-SC | Tampering (supply chain) | npm installs | low | accept | Zero packages installed this plan/phase (QA-02); no `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/build/chat-context-integrity.test.ts tests/build/chat-knowledge-voice.test.ts tests/api/prompt-injection.test.ts` — files load; unchanged tests GREEN; the newly-authored assertions are RED-by-design until 25-02/25-03 (document the RED set in the SUMMARY, mirroring 22-01).
- `grep -c "PROJECTS_7_REGEXES" tests/build/chat-context-integrity.test.ts` == 0 (the #7-leak block is fully removed).
- The FIRST_PERSON_LEAK regex on chat-knowledge-voice.test.ts:40 is byte-identical to scripts/build-chat-context.mjs:93.
</verification>

<success_criteria>
- All four files parse and the intended assertions are present (grep/read confirmed).
- The RED-until-25-03 set is explicitly enumerated in the SUMMARY so 25-03's regen has a precise green target.
- No production/source file touched (test + fixture only — D-14 allows).
</success_criteria>

<output>
Create `.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-01-SUMMARY.md` when done, listing the exact assertions changed per file and the RED-until-25-03 set.
</output>
