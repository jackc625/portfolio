---
phase: 25-chat-knowledge-refresh-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/build/chat-context-integrity.test.ts
  - tests/build/chat-knowledge-voice.test.ts
  - tests/api/prompt-injection.test.ts
  - tests/fixtures/chat-eval-dataset.ts
autonomous: true
requirements: [CHAT-10, CHAT-11]
must_haves:
  truths:
    - "chat-context-integrity asserts 7 project slugs (incl. multi-chain-evm) and no longer bans #7 content (D-04)"
    - "chat-context-integrity asserts /projects/multi-chain-evm has extendedReference.truncated === false AND contains a distinctive late-source anchor from Projects/7 — proving D-04 full untruncated sibling treatment (D-04)"
    - "chat-knowledge-voice walks ALL FOUR serialized experience string fields (role, company, dateRange, summary) — not just summary/role — and asserts no entry begins with I/My (the Balfour first-person guard Codex flagged) (D-09, D-13)"
    - "chat-knowledge-voice adds B1 self-test samples for 'I interned'/'I coordinated' — RED until 25-03 extends FIRST_PERSON_LEAK byte-identically across all three sites, closing the finite-allowlist blind spot Codex escalated to HIGH (D-13)"
    - "prompt-injection asserts the #7 topic ban directive is ABSENT (scoped so knowledge-block #7 prose does not false-match) and grounded anchors match the new full-time positioning (D-05)"
    - "prompt-injection adds a normalized <security>-block integrity assertion that permits EXACTLY the removal of the #7 ban sentence and preserves every refusal tier / attack-pattern / framing-tag anchor byte-intact (CHAT-11)"
    - "a new education-wiring assertion compares the generated education object against EDUCATION/CREDENTIALS imported from src/data/education.ts (SSoT relationship, not hard-pinned literals) (D-07)"
    - "a new experience-array presence assertion pins Holloway-first reverse-chron ordering with Balfour present (D-01, D-03, D-09)"
    - "a new skills-presence assertion pins the four additive D-08 skills (Deno, TanStack Query, Vitest, Ethers.js) in the corpus so D-08 is not human-review-only (D-08)"
    - "the fixture's #7-term refusal banlist is renamed REFUSAL_RESPONSE_BANNED_REGEXES with clarified comments — it gates refusal responses, not global chat knowledge, now that #7 is public (D-04)"
  artifacts:
    - tests/build/chat-context-integrity.test.ts
    - tests/build/chat-knowledge-voice.test.ts
    - tests/api/prompt-injection.test.ts
    - tests/fixtures/chat-eval-dataset.ts
  key_links:
    - "all four files import/exercise src/data/portfolio-context.json + buildSystemPrompt — the new assertions are EXPECTED RED until 25-03 regenerates the corpus + extends the regex and 25-02 removes the #7 ban (mirrors the Phase 22-01 Wave-0 tests-first pattern)"
---

<objective>
Retarget the four chat-side test/fixture files that are HARD-PINNED to the pre-#7, pre-repositioning world so they define the Phase 25 target BEFORE the build-script change (25-03) and copy change (25-02) land. This is the Nyquist Wave-0 gate: the suite states the contract, then 25-02/25-03 make it green.

Purpose: Without these retargets the suite silently guards the old behavior (exactly 6 projects / "never mention multi-dex" / "entry-level" positioning / string `experience`) and would false-fail the moment #7 and the structured experience array land. Editing them first makes the target unambiguous and catches the #1 regression risk (a string-only `experience` walk that silently skips array entries).
Output: Four retargeted test/fixture files. New assertions are intentionally RED until 25-02 (removes the #7 ban, locks positioning wording) and 25-03 (regenerates the corpus as 7 projects + structured experience array + education from education.ts).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-RESEARCH.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-VALIDATION.md
@tests/build/chat-context-integrity.test.ts
@tests/build/chat-knowledge-voice.test.ts
@tests/api/prompt-injection.test.ts
@tests/fixtures/chat-eval-dataset.ts
</context>

## Artifacts this phase produces (referenced by all plans — exclude from drift/source-grounding)

This plan authors NEW test assertions + one fixture rename. Symbols this plan CREATES or that other 25-plans create — do NOT treat these as "undefined", they are produced this phase:
- `REFUSAL_RESPONSE_BANNED_REGEXES` — this plan RENAMES the fixture export `GLOBAL_BANNED_REGEXES` → `REFUSAL_RESPONSE_BANNED_REGEXES` in chat-eval-dataset.ts and updates the import in prompt-injection.test.ts (the only two consumers; chat-context-integrity has its own copy).
- A new `<security>`-block golden constant authored in prompt-injection.test.ts (the current security block minus exactly the #7 ban sentence, whitespace-normalized) — the byte-intact snapshot baseline.
- A new #7 groundedQA entry ("Tell me about the Multi-Chain EVM trader", anchor "Multi-Chain EVM") in chat-eval-dataset.ts.
- The extended `FIRST_PERSON_LEAK` regex (adds `interned`/`coordinated`) — 25-03 lands it byte-identical across all three sites; this plan only adds the RED B1 self-test samples that exercise it.
- `education.graduation` = "May 2026", `education.transferredFrom` = "Virginia Tech", `education.certifications` = ["LPI Linux Essentials"] (emitted by 25-03 `parseEducation`); this plan compares them against `EDUCATION`/`CREDENTIALS` imported from src/data/education.ts.
- `experience` is an ARRAY of `{ role, company, dateRange, summary }` (emitted by 25-03 experience-collection reader), reverse-chronological (Holloway first, Balfour second).
- The `multi-chain-evm` project slug present in the corpus with `extendedReference.truncated === false` (25-03 lifts the exclusion; #7 is 2,807 words < 5,000 cap).
- The four additive D-08 skills (Deno, TanStack Query, Vitest, Ethers.js) in the corpus `skills` (25-02 static.json + 25-03 regen).
- The new positioning anchor tokens "looking for" + "full-time" in the knowledge block (25-02 `about-chat.ts` P3 rewrite).
- The #7 topic-ban directive REMOVED from `system-prompt.ts` (25-02).

<tasks>

<!-- planner-discipline-allow: multi-chain-evm -->
<!-- planner-discipline-allow: PROJECTS_7_REGEXES -->
<!-- planner-discipline-allow: GLOBAL_BANNED_REGEXES -->
<!-- planner-discipline-allow: REFUSAL_RESPONSE_BANNED_REGEXES -->

<task type="auto">
  <name>Task 1: Retarget chat-context-integrity — 7 slugs, drop the #7-leak block, add education + experience assertions</name>
  <files>tests/build/chat-context-integrity.test.ts</files>
  <read_first>
    - tests/build/chat-context-integrity.test.ts (full — the file being edited; note EXPECTED_SLUGS :17-24, PROJECTS_7_REGEXES :26-31, count :45-47, the #7-leak test :49-58, the D-08 key tests :101-121, Daytrade truncation :123-135)
    - 25-RESEARCH.md §7.1 (the exact assertions to change) + §3 (the emitted education shape) + §2a (the experience array shape) + §4 (#7 is 2,807 words < 5,000 cap → truncated:false)
    - 25-PATTERNS.md "Test edits" (chat-context-integrity analog block)
    - src/data/education.ts (the SSoT to IMPORT into the test: `export const EDUCATION` with keys degree/institution/date/transferredFrom and `export const CREDENTIALS` — the test compares the generated object against these exports, not hard-pinned literals)
    - src/data/portfolio-context.static.json (the current `skills` block — the additive D-08 skills the presence test pins: Deno, TanStack Query, Vitest, Ethers.js)
    - Projects/7 - MULTI-DEX CRYPTO TRADER.md (read the TAIL below `<!-- CASE-STUDY-END -->` and pick ONE distinctive verbatim late-source phrase — used to prove #7's extendedReference is untruncated through to the end)
  </read_first>
  <action>
    Edit tests/build/chat-context-integrity.test.ts:
    (1) Add "multi-chain-evm" to EXPECTED_SLUGS (:17-24) so the sorted array is the 7 slugs clipify, daytrade, multi-chain-evm, nfl-predict, optimize-ai, seatwatch, solsniper (keep alphabetical to match the `.sort()` on :41). Update the two test titles that say "6 expected" / "exactly 6" to "7".
    (2) Change the count assertion `expect((portfolioContext.projects ...).length).toBe(6)` (:46) to `toBe(7)`.
    (3) DELETE the PROJECTS_7_REGEXES const (:26-31) and DELETE the entire "no Projects/7 content leaks into the merged JSON" test (:49-58) — the exclusion it guards is deliberately lifted (D-04). Do not leave a dangling reference to PROJECTS_7_REGEXES.
    (4) ADD an education-wiring test that proves the SSoT RELATIONSHIP, not literals (Codex MEDIUM 25-01): `import { EDUCATION, CREDENTIALS } from "../../src/data/education.ts";` and assert portfolioContext.education deep-matches the object DERIVED from those exports — graduation === EDUCATION.date, transferredFrom === EDUCATION.transferredFrom, school === EDUCATION.institution, degree === EDUCATION.degree, and (education.certifications as string[]) equals CREDENTIALS.map(c => c.name). A build script that hard-coded the same values would still pass a literal test; comparing against the imported exports proves the chat education can never silently drift from education.ts (D-07).
    (5) ADD an experience-array presence test: assert Array.isArray(portfolioContext.experience) and length === 2; entry[0].company === "Holloway Company" (reverse-chron: Holloway first — D-01/D-09); some entry has company === "Balfour Beatty" (D-03); every entry has a non-empty string summary (>40 chars for Holloway, >0 for Balfour). Type the entries inline as Array<{ role: string; company: string; dateRange: string; summary: string }>.
    (6) ADD a full-#7-ingestion test (Codex LOW 25-01): find the project with page === "/projects/multi-chain-evm"; assert it is defined, its extendedReference.truncated === false (proves D-04 full untruncated sibling treatment through the truncation path), and its extendedReference.content contains the distinctive late-source phrase you chose from the tail of Projects/7 (proves the below-fence body is ingested end-to-end, not clipped).
    (7) ADD a skills-presence test (D-08, resolves the 25-02 LOW "D-08 is human-review-only"): flatten portfolioContext.skills to a single array of strings and assert it includes "Deno", "TanStack Query", "Vitest", and "Ethers.js" — so D-08's additive refresh is machine-verified, not review-only.
    Leave the caseStudy / extendedReference / token-floor / D-08-key / Daytrade-truncation tests unchanged.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/chat-context-integrity.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - The file loads with no TypeScript/parse error; `grep -c "multi-chain-evm" tests/build/chat-context-integrity.test.ts` >= 1 and `grep -c "PROJECTS_7_REGEXES" tests/build/chat-context-integrity.test.ts` == 0.
    - The education test imports `EDUCATION`/`CREDENTIALS` from src/data/education.ts and compares the generated object against them (no bare literal-only education assertion remains).
    - The #7-ingestion test references `extendedReference.truncated` and a distinctive late-source phrase; the skills test references "Deno"/"TanStack Query"/"Vitest"/"Ethers.js".
    - The slug/count/education/experience/#7/skills assertions are EXPECTED RED now (corpus still ships 6 projects + string experience + stale 3-field education + old skills); they go GREEN after 25-02 adds the skills and 25-03 regenerates portfolio-context.json. The unchanged tests (token floor, D-08 keys, Daytrade truncation) stay GREEN.
  </acceptance_criteria>
  <done>chat-context-integrity targets 7 slugs, removes the #7-content-leak block + const, and adds SSoT-education / experience-array / full-#7-untruncated / skills-presence assertions (RED until 25-02/25-03).</done>
</task>

<task type="auto">
  <name>Task 2: chat-knowledge-voice — walk all 4 experience fields, add the Balfour B1 counterexamples, guard against I/My leads</name>
  <files>tests/build/chat-knowledge-voice.test.ts</files>
  <read_first>
    - tests/build/chat-knowledge-voice.test.ts (full — note the FIRST_PERSON_LEAK regex :39-40, the B1 KNOWN_LEAKS self-test :42-108, the string-typed experience assertion :117-131, the per-project loop :133-148)
    - src/content/experience/balfour-beatty.mdx (the frontmatter `summary` at :9 starts "I interned…" and the body coordinates workstreams — the real first-person counterexamples the finite-allowlist regex misses)
    - 25-RESEARCH.md §7.4 + §2b (the array walk) + §10 (regex triplication) ; 25-REVIEWS.md Codex 25-01 HIGH (leak-guard blind spot) + MEDIUM (walk covers only 2 of 4 serialized fields)
    - 25-PATTERNS.md "chat-knowledge-voice.test.ts" analog block
  </read_first>
  <action>
    Edit tests/build/chat-knowledge-voice.test.ts:
    (1) Replace the "no first-person leak in about.{intro,p1,p2,p3} or experience" test (:117-131). The `fields` array currently includes `["experience", ctx.experience]` and asserts every value `toBeTypeOf("string")`. After D-09 `ctx.experience` is an ARRAY. First assert `Array.isArray(ctx.experience)` (RED until 25-03 emits the array). Then flat-map each entry's string fields into `fields` — push ALL FOUR serialized fields, not just summary/role (Codex MEDIUM: role, company, and dateRange also enter the prompt via wholesale JSON.stringify): for each `e` at index `i` push `[`experience[${i}].role`, e.role]`, `[`experience[${i}].company`, e.company]`, `[`experience[${i}].dateRange`, e.dateRange]`, `[`experience[${i}].summary`, e.summary]`. Keep the about.intro/p1/p2/p3 entries and the per-field `not.toMatch(FIRST_PERSON_LEAK)` + `toBeTypeOf("string")` assertions.
    (2) ADD a defense-in-depth structural assertion in the same test (Codex option B): every experience entry's `role`, `company`, `dateRange`, and `summary` must NOT begin with a first-person pronoun clause — assert each `.not.toMatch(/^\s*(I|My|We|Our)\b/)`. This catches ANY first-person lead even if it uses a verb the finite regex misses.
    (3) ADD two B1 self-test KNOWN_LEAKS samples that exercise the real Balfour counterexamples (Codex HIGH): add "I interned in project management" and "I coordinated deliverables" to the KNOWN_LEAKS array (:47-87). These assert `.toMatch(FIRST_PERSON_LEAK)` and are EXPECTED RED now (the current finite allowlist lacks `interned`/`coordinated`); they go GREEN when 25-03 extends the regex byte-identically across all three sites. Their presence guarantees the extension actually lands and is not silently dropped.
    Do NOT edit the FIRST_PERSON_LEAK literal on :40 in this plan — 25-03 owns the byte-identical extension across build-chat-context.mjs:93 + this file + chat-voice-split.test.ts:33 (keeping the triplication in sync at a single wave-2 commit). Do NOT touch the "Jack is a junior software engineer" SAFE negative-control (:97) — it is a regex control unrelated to positioning.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/chat-knowledge-voice.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - The file loads with no parse error; the experience-leak test asserts `Array.isArray(ctx.experience)` and walks all four fields (grep confirms `experience[` index syntax and `.company`/`.dateRange` references present).
    - The `/^\s*(I|My|We|Our)\b/` never-begins-first-person guard is applied to experience entries.
    - KNOWN_LEAKS contains "I interned…" and "I coordinated…" samples (grep confirms `interned` and `coordinated` present in the test).
    - The FIRST_PERSON_LEAK literal on :40 is unchanged in THIS plan (25-03 extends it). The array-walk + the two new B1 samples are EXPECTED RED until 25-03; the existing B1 self-test + SAFE negative control stay GREEN.
  </acceptance_criteria>
  <done>the leak test walks all four experience fields + guards I/My leads; KNOWN_LEAKS carries the Balfour interned/coordinated counterexamples (RED until 25-03 extends the regex); the pre-existing B1 self-test stays GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: prompt-injection + eval-dataset — #7 ban absent, security-block byte-intact snapshot, count 6->7, positioning anchors, fixture rename</name>
  <files>tests/api/prompt-injection.test.ts, tests/fixtures/chat-eval-dataset.ts</files>
  <read_first>
    - tests/api/prompt-injection.test.ts (:16-27 imports incl. `GLOBAL_BANNED_REGEXES`; :138-154 the tiered-refusal + attack-pattern assertions; :171-173 the #7-ban assertion; :175-184 the grounded-anchor loop; :279-298 the "exactly 6 generated-context projects" test)
    - tests/fixtures/chat-eval-dataset.ts (RESUME_REFUSAL/OFFSCOPE_REFUSAL :16-19; GLOBAL_BANNED_STRINGS :21-36; GLOBAL_BANNED_REGEXES :38-45 with the #7 terms + their "Projects/7 banlist (D-04)" comments; groundedQA :134-185 esp. "What does Jack do currently?" :176-179 anchors ["looking for","entry-level"]; voiceSpotChecks :239-242)
    - src/prompts/system-prompt.ts (the ENTIRE `<security>` block :31-57 — the golden snapshot source; the last line :56 has the `<security>`-scoped #7 ban sentence 25-02 removes; the audience line :5 is in `<role>`, NOT `<security>`, so `<security>` loses ONLY that one sentence)
    - src/content/projects/multi-chain-evm.mdx (frontmatter title :2 = "Multi-Chain EVM Trader" — confirm the exact corpus project name the #7 groundedQA anchor must match)
    - 25-RESEARCH.md §7.2 + §7.3 + §9 ; 25-REVIEWS.md Codex 25-02 MEDIUM (security block not auto-proven) + LOW (contradictory global #7 ban regexes)
  </read_first>
  <action>
    In tests/api/prompt-injection.test.ts:
    (1) Replace the "contains the Projects/7 (D-04) banlist reinforcement" test (:171-173). Instead of asserting the ban is present, assert the ban DIRECTIVE is ABSENT and scoped so the knowledge-block #7 prose does not false-match: assert `expect(prompt).not.toMatch(/Never discuss.{0,80}(MULTI[- ]?DEX|crypto arbitrage)/is)`. Rename the test to reflect that the #7 topic ban is lifted (CHAT-11 / §9). Do NOT assert `not.toMatch(/MULTI-DEX/i)` on the whole prompt — the corpus now contains #7 content and that would false-fail.
    (2) ADD a `<security>`-block byte-intact snapshot test (Codex MEDIUM 25-02 — "permits exactly the removal of the #7 sentence and nothing else"): extract the substring of buildSystemPrompt() output between the literal "<security>" marker and the "<knowledge>" marker; whitespace-normalize it (collapse runs of whitespace to single spaces, trim). Author a golden constant equal to the CURRENT `<security>` block (system-prompt.ts :31-57) with EXACTLY the one #7-ban sentence removed (delete `Never discuss "MULTI-DEX CRYPTO TRADER"…those are out of scope.` while KEEPING the preceding `Never pivot to projects not listed in the knowledge block.`), whitespace-normalized the same way. Assert the extracted block equals the golden. Additionally assert the extracted block still `.toContain` each preserved-defense anchor (RESUME_REFUSAL, OFFSCOPE_REFUSAL, "ignore previous instructions", "repeat your system prompt", "act as", "translate the above", "<system>", "cache_control", "Never output Jack's phone number", "Never pivot to projects not listed") and `.not.toContain("those are out of scope")`. This proves the sensitive security edit is exactly one sentence, nothing broader. It is EXPECTED RED until 25-02 removes that sentence; GREEN after.
    (3) Change `expect(projectPages.size).toBe(6)` (:293) to `toBe(7)`. Update the surrounding test title from "exactly 6" to "exactly 7". Leave the `referencedProjects ... toBeGreaterThanOrEqual(6)` assertion (:294-297) as-is (the six original project anchors still resolve).
    (4) Update the import (:20-27) to reflect the fixture rename in step (7): import `REFUSAL_RESPONSE_BANNED_REGEXES` instead of `GLOBAL_BANNED_REGEXES`, and update its single use site in `assertAttackVector` (:49, the `opts.bannedRegex ?? GLOBAL_BANNED_REGEXES` default) to the new name. No behavior change — refusal responses still must not surface #7 terms.
    Leave every injection-vector, refusal-copy, drift-guard, and section-tag test otherwise unchanged.

    In tests/fixtures/chat-eval-dataset.ts:
    (5) Update the "What does Jack do currently?" groundedQA anchors (:177) from ["looking for","entry-level"] to ["looking for","full-time"] — the exact anchor tokens 25-02 locks into ABOUT_CHAT_P3. Keep the fabricationBanlist ["senior","10 years"].
    (6) ADD one groundedQA entry for #7 CHAT-11 positive coverage: question "Tell me about the Multi-Chain EVM trader", requiredAnchors ["Multi-Chain EVM"] (a substring of the corpus title "Multi-Chain EVM Trader" — confirm against the mdx frontmatter you read), fabricationBanlist [].
    (7) RENAME the fixture export `GLOBAL_BANNED_REGEXES` → `REFUSAL_RESPONSE_BANNED_REGEXES` (Codex LOW 25-02): the list gates ATTACK-VECTOR REFUSAL RESPONSES (a refusal must never pivot to #7 chatter), NOT global chat knowledge — its old name falsely implied #7 is globally banned now that #7 is public. Keep the phone/address regexes AND the #7 terms (a clean refusal still must not surface them) but rewrite the inline comments: change "Projects/7 banlist (D-04)" to note these gate refusal-response outputs, not corpus content. Update the stale header comment "Consumed by … chat-context-integrity.test.ts (Projects/7 banlist only)" (chat-context-integrity no longer imports this list). Leave GLOBAL_BANNED_STRINGS unchanged (it gates structural section tags — a genuinely global concern).
    (8) Update the voiceSpotChecks "Is Jack currently looking…" gold line (:239-242) to third-person new-grad/full-time wording for consistency (consumed by no test — cosmetic).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/api/prompt-injection.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - prompt-injection.test.ts: the #7-ban test asserts absence (scoped `Never discuss … MULTI-DEX|crypto arbitrage` regex not present); a `<security>`-block snapshot test exists that compares the normalized extracted block to a golden and asserts the preserved-defense anchors are present; projectPages.size reads toBe(7); the import + use site reference `REFUSAL_RESPONSE_BANNED_REGEXES`.
    - chat-eval-dataset.ts: "current" anchors are ["looking for","full-time"]; a "Multi-Chain EVM trader" groundedQA entry exists; `grep -c "REFUSAL_RESPONSE_BANNED_REGEXES" tests/fixtures/chat-eval-dataset.ts` >= 1 and `grep -c "GLOBAL_BANNED_REGEXES" tests/fixtures/chat-eval-dataset.ts` == 0.
    - The count-7, new-anchor, #7-groundedQA, and security-snapshot assertions are EXPECTED RED until 25-02 removes the ban + locks wording and 25-03 regenerates the corpus; the ban-absent + security-snapshot assertions go GREEN once 25-02 makes the exact one-sentence edit. All injection/refusal/drift-guard tests stay GREEN (the rename is name-only).
  </acceptance_criteria>
  <done>prompt-injection asserts the #7 ban absent + a byte-intact `<security>`-block snapshot + count 7; the fixture carries full-time anchors, a Multi-Chain EVM groundedQA entry, and the renamed REFUSAL_RESPONSE_BANNED_REGEXES with corrected comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor -> chat API | untrusted user messages reach `buildSystemPrompt`; the prompt-injection battery is the guard these tests exercise |
| build sources -> cached system prompt | corpus + prompt template become the cached `<knowledge>`/`<security>` blocks; these tests assert the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Information disclosure | prompt-injection battery / `<security>` assertions | medium | mitigate | These tests keep every injection-vector, tiered-refusal, section-tag, and drift-guard assertion intact; ONLY the over-broad #7 topic-ban assertion is inverted, and a new normalized `<security>`-block snapshot proves the sensitive edit is EXACTLY the one-sentence #7-ban removal (nothing broader). The battery still proves the preserved defenses. |
| T-25-02 | Tampering (voice break) | FIRST_PERSON_LEAK regex + experience-array walk | medium | mitigate | The array-walk retarget sweeps ALL FOUR serialized experience fields (role/company/dateRange/summary) + a never-begins-I/My guard (closes the string-only-walk blind spot and the two-of-four gap); the RED B1 samples for "I interned"/"I coordinated" force 25-03 to extend FIRST_PERSON_LEAK byte-identically across all three sites, closing the finite-allowlist blind spot Codex escalated to HIGH (D-13). |
| T-25-SC | Tampering (supply chain) | npm installs | low | accept | Zero packages installed this plan/phase (QA-02); no `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/build/chat-context-integrity.test.ts tests/build/chat-knowledge-voice.test.ts tests/api/prompt-injection.test.ts` — files load; unchanged tests GREEN; the newly-authored assertions (7 slugs, SSoT education, full-#7-untruncated, skills-presence, 4-field array walk, I/My guard, the two Balfour B1 samples, #7-ban-absent, `<security>`-block snapshot, count-7, #7 groundedQA) are RED-by-design until 25-02/25-03 (document the exact RED set in the SUMMARY, mirroring 22-01).
- `grep -c "PROJECTS_7_REGEXES" tests/build/chat-context-integrity.test.ts` == 0 (the #7-leak block is fully removed); `grep -c "GLOBAL_BANNED_REGEXES" tests/fixtures/chat-eval-dataset.ts` == 0 (renamed to REFUSAL_RESPONSE_BANNED_REGEXES; import updated in prompt-injection.test.ts).
- This plan does NOT edit the FIRST_PERSON_LEAK literal on chat-knowledge-voice.test.ts:40 — it stays byte-identical to build-chat-context.mjs:93 + chat-voice-split.test.ts:33 at this wave; 25-03 extends all three together at wave 2.
</verification>

<success_criteria>
- All four files parse and the intended assertions are present (grep/read confirmed).
- The RED-until-25-03 set is explicitly enumerated in the SUMMARY so 25-03's regen has a precise green target.
- No production/source file touched (test + fixture only — D-14 allows).
</success_criteria>

<output>
Create `.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-01-SUMMARY.md` when done, listing the exact assertions changed per file and the RED-until-25-03 set.
</output>
