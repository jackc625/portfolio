---
phase: 25-chat-knowledge-refresh-milestone-verification
plan: 03
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - src/prompts/portfolio-context-types.ts
  - scripts/build-chat-context.mjs
  - src/data/portfolio-context.json
autonomous: true
requirements: [CHAT-10, CHAT-11, QA-02]
must_haves:
  truths:
    - "build-chat-context.mjs lifts the #7 exclusion at BOTH sites (slug-skip deleted; MULTI-DEX regex scoped with && slug !== 'multi-chain-evm') so #7 enters the corpus with its chatSummary + below-fence extendedReference (D-04)"
    - "the corpus experience field is a reverse-chron array of {role, company, dateRange, summary} read from src/content/experience/*.mdx, replacing the about-derived one-liner (D-09)"
    - "checkFirstPersonLeaks walks the experience array's summary/role strings and hard-fails (exit 2) on first-person leakage (D-09/D-13)"
    - "education is emitted into the merge from education.ts via a dep-free parseEducation (degree/school/graduation May 2026/transferredFrom VT/certifications LPI), single-sourced (D-07)"
    - "portfolio-context.json is regenerated + committed and build:chat-context:check reports no drift; est_tokens stays under the 60k WARN threshold (D-10)"
    - "the PortfolioContext interface types experience as an array and extends the education sub-type — a NON-gated file, so D-26/D-15 do not trigger (D-09/D-14)"
  artifacts:
    - src/prompts/portfolio-context-types.ts
    - scripts/build-chat-context.mjs
    - src/data/portfolio-context.json
  key_links:
    - "api/chat.ts consumes the corpus only via JSON.stringify(context) — the array experience shape + emitted education are serialization-safe (no gated-file change)"
---

<objective>
Change the corpus engine so the chat's grounded knowledge includes Holloway, Balfour, and project #7, single-sources education from education.ts, and structures `experience` as a real reverse-chron array — then regenerate and commit `portfolio-context.json` with the leak guard and drift gate green. This is the heart of CHAT-10 and the load-bearing half of CHAT-11.

Purpose: 25-02 authored the content; this plan wires the build to ingest it. The highest-risk item (D-09) is that a string-only leak walk silently skips array entries — the leak-guard walk extension MUST land WITH the structured-experience change (same task). The corpus regen runs AFTER all content + build-script edits (25-02 locked copy first).
Output: Updated type contract + build script + regenerated corpus. The 25-01 Wave-0 assertions go GREEN here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-RESEARCH.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-PATTERNS.md
@scripts/build-chat-context.mjs
@scripts/sync-projects.mjs
@src/prompts/portfolio-context-types.ts
@src/data/education.ts
@src/content/experience/holloway.mdx
@src/content/experience/balfour-beatty.mdx
</context>

## Artifacts this phase produces (exclude from drift/source-grounding)

Symbols this plan CREATES (newly-defined this phase — a source-grounding pass must not flag them as undefined):
- `parseEducation(sourceContent)` — dep-free per-key regex reader of education.ts (mirrors `parseAboutChatExports`).
- `EDUCATION_TS_PATH` + `EXPERIENCE_GLOB` path/glob constants.
- The experience-collection reader loop (globs `src/content/experience/*.mdx`, reads role/company/dateRange/chatSummary/startDate via `readStringField` + `sliceFrontmatter`, sorts reverse-chron).
- The structured `experience` array field: `Array<{ role, company, dateRange, summary }>` replacing the string synthesis.
- The extended `checkFirstPersonLeaks` array-walk (flatMap over summary/role).
- The emitted `education` object shape: `{ degree, school, graduation, transferredFrom, certifications }`.
- The updated `PortfolioContext.experience` (array) + `PortfolioContext.education` (extended) interface fields.

<tasks>

<!-- planner-discipline-allow: multi-chain-evm") continue -->

<task type="auto">
  <name>Task 1: Update the PortfolioContext interface (experience array + extended education)</name>
  <files>src/prompts/portfolio-context-types.ts</files>
  <read_first>
    - src/prompts/portfolio-context-types.ts (full — education sub-type :17, the experience one-liner doc + `experience: string` :46-50)
    - 25-RESEARCH.md §2c (confirmed NON-gated) + §3 (education sub-shape) + 25-PATTERNS.md portfolio-context-types block
  </read_first>
  <action>
    Edit src/prompts/portfolio-context-types.ts:
    - Change `experience: string;` (:50) to an array type: `experience: Array<{ role: string; company: string; dateRange: string; summary: string }>;` and update the doc comment (:46-49) to describe the reverse-chron role array (drop the "one-liner / prefer about" note).
    - Extend the education sub-type (:17) from `{ degree: string; school: string; graduation: string }` to `{ degree: string; school: string; graduation: string; transferredFrom: string; certifications: string[] }`.
    This file is NON-gated (confirmed §2c) — the edit does NOT trigger the D-26 battery / D-15 anchor.
  </action>
  <verify>
    <automated>pnpm exec astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec astro check` stays 0/0/0.
    - PortfolioContext.experience is an Array<{role;company;dateRange;summary}>; education carries transferredFrom + certifications.
  </acceptance_criteria>
  <done>the type contract matches the new corpus shape (experience array + extended education); astro check 0/0/0; NON-gated file (no D-26/D-15 trigger).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build engine — lift #7 exclusion, structured experience reader + leak-walk, parseEducation wiring</name>
  <files>scripts/build-chat-context.mjs</files>
  <behavior>
    - Given multi-chain-evm.mdx now has a chatSummary and source: Projects/7, the build INCLUDES it as the 7th project block (no throw at either former exclusion site).
    - Given a second MDX pointing source: at Projects/7 under a different slug, the scoped MULTI-DEX regex STILL throws (belt-and-suspenders retained).
    - Given the experience collection, `merged.experience` is a reverse-chron array [Holloway, Balfour] of {role,company,dateRange,summary}; summary = each MDX's chatSummary.
    - Given a first-person string in any experience summary/role, checkFirstPersonLeaks exits 2.
    - Given education.ts, `merged.education` = {degree "B.S. Computer Science", school "Western Governors University", graduation "May 2026", transferredFrom "Virginia Tech", certifications ["LPI Linux Essentials"]}.
  </behavior>
  <read_first>
    - scripts/build-chat-context.mjs (FULL — the file being edited: header docstring :11-17; imports :35-39; path consts :43-47; FIRST_PERSON_LEAK_RE :93; checkFirstPersonLeaks targets :104-115 + non-string skip :117-118; readStringField :158-185; buildProjectBlock :377-422; the MDX loop + slug-skip :442-449 + defensive regex :466-472 + seenSources :479-483; the experience synthesis :546-560; the merge :562-570; parseAboutChatExports :343-367 as the parseEducation analog)
    - scripts/sync-projects.mjs (readSourceField / sliceFrontmatter / wordCount signatures — already imported)
    - src/data/education.ts (EDUCATION object :25-38 keys degree/institution/date/transferredFrom; CREDENTIALS :40-42)
    - src/content/experience/holloway.mdx + balfour-beatty.mdx frontmatter (role/company/dateRange/startDate/chatSummary field names + quoting)
    - 25-RESEARCH.md §1 (both exclusion sites) + §2 (experience block) + §3 (parseEducation) + §10 (regex — DO NOT change) ; 25-PATTERNS.md all build-chat-context blocks + Don't Hand-Roll table
  </read_first>
  <action>
    Edit scripts/build-chat-context.mjs (dep-free; reuse imported helpers — do NOT hand-roll frontmatter parsing):
    (1) Lift #7 exclusion (§1): DELETE the `if (basename(mdxPath, ".mdx") === "multi-chain-evm") continue;` at :449 AND its D-15 comment block :443-448. SCOPE (do not delete) the defensive MULTI-DEX throw at :466-472 by computing `const slug = basename(mdxPath, ".mdx");` and guarding the throw with `&& slug !== "multi-chain-evm"`; update its message to state Projects/7 source is reserved for the multi-chain-evm slug. The seenSources duplicate check (:479-483) remains the independent backstop. Update the header docstring (:11-17) to state #7 is now ingested (Phase 25 / CHAT-10 lifted the skip).
    (2) Experience-collection reader (§2 / D-01/D-09): add `const EXPERIENCE_GLOB = "src/content/experience/*.mdx";` near :43. Glob those files (deterministic sort), and for each read `role`, `company`, `dateRange`, `chatSummary`, `startDate` via `readStringField(frontmatterBlock, ...)` after `sliceFrontmatter`. Build entries `{ role, company, dateRange, summary: chatSummary }`; sort by `startDate` DESC (Holloway before Balfour). Do NOT read a below-fence/extendedReference for experience. Do NOT emit startDate. If an experience MDX is missing chatSummary, throw exit 2 with a named error (parallels the projects chatSummary throw).
    (3) Structured experience field (§2a / D-09): REPLACE the string synthesis at :546-560 (`const experience = ...aboutBlock...`) with `const experience = experienceEntries;` (the reverse-chron array from step 2). Keep it dropping into the same `merged` key (:568). Remove the now-obsolete synthesis comment.
    (4) Leak-walk extension (§2b / D-09/D-13): in checkFirstPersonLeaks (:104-115), REPLACE the single `["experience", merged.experience]` entry with an Array.isArray-guarded flatMap that pushes `[`experience[${i}].summary (${e.company})`, e.summary]` and `[`experience[${i}].role`, e.role]` for each entry, falling back to `[["experience", merged.experience]]` if not an array. Do NOT change FIRST_PERSON_LEAK_RE (:93) — it stays byte-identical across all three sites (§10). The non-string skip (:118) makes the fallback free.
    (5) parseEducation + wiring (§3 / D-07): add `const EDUCATION_TS_PATH = "src/data/education.ts";` and a `parseEducation(sourceContent)` helper (mirror parseAboutChatExports's dep-free approach, but education.ts is an object literal — use per-key value regexes: degree, institution, date, transferredFrom against the EDUCATION block, and a CREDENTIALS name scan). Read education.ts in main(), build `education = { degree, school: institution, graduation: date, transferredFrom, certifications }` and ADD it to the `merged` object (:565-570) so it overrides any static key (static.json's education was removed in 25-02, so this is now the sole source). REJECT the --experimental-strip-types import path (§3).
    Do NOT touch api/chat.ts, chat.ts, BaseLayout.astro, or global.css (D-14).
  </action>
  <verify>
    <automated>node scripts/build-chat-context.mjs 2>&1 | tail -20 && pnpm exec vitest run tests/build/chat-context-integrity.test.ts tests/build/chat-knowledge-voice.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-chat-context.mjs` exits 0 (no leak, no hard-fail); stdout shows `projects=7` and multi-chain-evm in the per-project breakdown; `est_tokens` is under 60000 (WARN) and above 4096 (D-10).
    - `grep -c 'multi-chain-evm") continue' scripts/build-chat-context.mjs` == 0 (slug-skip removed); the scoped regex line contains `slug !== "multi-chain-evm"`.
    - checkFirstPersonLeaks walks the array (grep confirms `experience[` index syntax) and FIRST_PERSON_LEAK_RE on :93 is byte-identical to tests/build/chat-knowledge-voice.test.ts:40.
    - After regen, tests/build/chat-context-integrity.test.ts + tests/build/chat-knowledge-voice.test.ts (from 25-01) are GREEN (7 slugs, education wired, experience array walk).
  </acceptance_criteria>
  <done>build-chat-context.mjs ingests #7 (projects=7), emits a reverse-chron experience array + education from education.ts, and the extended leak-walk + byte-identical regex hold; the build exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate + commit corpus, run drift + quick chat tests + astro check (wave-merge sampling)</name>
  <files>src/data/portfolio-context.json</files>
  <read_first>
    - package.json scripts (build:chat-context, build:chat-context:check, test)
    - 25-VALIDATION.md Sampling Rate + Per-Task Verification Map
  </read_first>
  <action>
    Run `pnpm build:chat-context` (write mode) to regenerate src/data/portfolio-context.json with the 7-project corpus + structured experience array + education from education.ts. Commit the regenerated portfolio-context.json (it MUST be committed so the drift gate passes). Then run `pnpm build:chat-context:check` (must exit 0 — no drift). Then run the quick chat test set (`pnpm exec vitest run tests/build/chat-context-integrity.test.ts tests/build/chat-knowledge-voice.test.ts tests/api/chat-voice-split.test.ts tests/api/prompt-injection.test.ts`) and `pnpm exec astro check`. Note the new est_tokens figure in the SUMMARY (expected ~48-49k per §8). If the leak guard exits 2, the culprit copy is in 25-02's authored strings — fix at source (about-chat.ts / the offending chatSummary), do NOT hand-edit portfolio-context.json.
  </action>
  <verify>
    <automated>pnpm build:chat-context && pnpm build:chat-context:check && pnpm exec astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build:chat-context:check` exits 0 (committed portfolio-context.json matches a fresh regen — no drift).
    - The quick 4-file chat test set passes; the 25-01 retargets are now GREEN.
    - `pnpm exec astro check` 0/0/0.
    - portfolio-context.json contains 7 projects (incl. multi-chain-evm), `experience` as a 2-entry array (Holloway first), and `education` with graduation "May 2026" + transferredFrom "Virginia Tech" + certifications ["LPI Linux Essentials"].
  </acceptance_criteria>
  <done>portfolio-context.json regenerated + committed; build:chat-context:check exit 0; quick chat tests + astro check GREEN; est_tokens recorded (under WARN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build sources -> cached `<knowledge>` | corpus content (now incl. #7 + experience array + education) becomes the model-visible cached system block |
| visitor -> chat API | api/chat.ts serializes the corpus by value only (JSON.stringify) — no code change; the array shape is serialization-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-02 | Tampering (voice break) | structured experience array + regenerated corpus | medium | mitigate | The leak-walk extension lands WITH the structured-experience change (same task) so the guard reaches every new array entry; FIRST_PERSON_LEAK_RE stays byte-identical (D-13); `pnpm build:chat-context` exit 2 hard-fails the build on any leak. |
| T-25-01 | Information disclosure | corpus as injected knowledge | medium | mitigate | The #7 content is public portfolio material; api/chat.ts + system-prompt `<security>` defenses are untouched here (D-14). 25-04 re-runs the prompt-injection battery + D-26/D-15 gates. |
| T-25-04 | Denial of service (token blowup) | corpus token budget | low | accept | D-10: accept growth to ~48-49k est_tokens; existing INFO/WARN/CAP observability prints it; stays under WARN (60k) and above the 4,096 Haiku cache floor. Revisit only if a future estimate crosses WARN. |
| T-25-SC | Tampering (supply chain) | npm installs | low | accept | Zero packages installed (QA-02); parseEducation + the experience reader are dep-free Node using already-imported helpers. |
</threat_model>

<verification>
- `node scripts/build-chat-context.mjs` exits 0 with projects=7 and no first-person leak.
- `pnpm build:chat-context:check` exits 0 (committed corpus == fresh regen).
- `pnpm exec vitest run tests/build/chat-context-integrity.test.ts tests/build/chat-knowledge-voice.test.ts tests/api/chat-voice-split.test.ts tests/api/prompt-injection.test.ts` — chat build + voice + injection tests GREEN (the 25-01 retargets flip green here).
- `pnpm exec astro check` 0/0/0.
- FIRST_PERSON_LEAK_RE byte-identical across build-chat-context.mjs:93, chat-knowledge-voice.test.ts:40, chat-voice-split.test.ts:33.
</verification>

<success_criteria>
- Corpus regenerated + committed with 7 projects, structured experience array, and education single-sourced from education.ts; drift gate clean.
- Only NON-gated files touched (portfolio-context-types.ts, build-chat-context.mjs, the generated portfolio-context.json) — D-26/D-15 do not trigger.
- Zero new runtime dependencies (all edits dep-free).
</success_criteria>

<output>
Create `.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-03-SUMMARY.md` when done, recording the new est_tokens, the projects=7 breakdown, and confirmation the 25-01 retargets flipped GREEN.
</output>
