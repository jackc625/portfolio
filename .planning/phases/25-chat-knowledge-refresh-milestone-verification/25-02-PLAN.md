---
phase: 25-chat-knowledge-refresh-milestone-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/experience/holloway.mdx
  - src/content/experience/balfour-beatty.mdx
  - src/content/projects/multi-chain-evm.mdx
  - src/data/about-chat.ts
  - src/data/portfolio-context.static.json
  - src/prompts/system-prompt.ts
autonomous: false
requirements: [CHAT-10, CHAT-11]
must_haves:
  truths:
    - "holloway.mdx carries a ~150-220 word third-person chatSummary condensing the 5 strongest specifics (D-01)"
    - "multi-chain-evm.mdx carries a third-person engineering-invariants chatSummary that makes NO returns/profit claims (D-02)"
    - "balfour-beatty.mdx carries a one-line third-person chatSummary; its first-person summary field is never fed to chat (D-03)"
    - "about-chat.ts drops 'junior', reads as a new-grad with shipped production experience, and its P3 contains the locked anchor tokens 'looking for' AND 'full-time' (D-05)"
    - "personal.summary is refreshed to new-grad-with-production-experience and skills additively gain Deno / TanStack Query / Vitest / Ethers.js (D-06, D-08)"
    - "system-prompt.ts removes ONLY the #7 topic-ban sentence and re-frames the audience away from 'junior', preserving the entire <security> block byte-intact"
    - "the stale static education object is removed so education single-sources from education.ts via the 25-03 build wiring (D-07)"
  artifacts:
    - src/content/experience/holloway.mdx
    - src/content/experience/balfour-beatty.mdx
    - src/content/projects/multi-chain-evm.mdx
    - src/data/about-chat.ts
    - src/data/portfolio-context.static.json
    - src/prompts/system-prompt.ts
  key_links:
    - "the human copy-review checkpoint (Task 3) locks all wording — incl. the two positioning anchor tokens 'looking for'/'full-time' that 25-01 pins — BEFORE 25-03 regenerates the corpus"
---

<objective>
Author every piece of third-person chat copy and identity this phase adds, and lock it through Jack's copy-review checkpoint (the Phase 24 pattern). This includes the SCOPE ADDITION the upstream file list omitted: `src/prompts/system-prompt.ts` (lift the #7 topic ban + drop the "junior" audience framing) — without it the chat would refuse the very project #7 this phase ingests.

Purpose: CHAT-10/CHAT-11 require the corpus to carry accurate, third-person, voice-split-clean content for Holloway, project #7, Balfour, and the repositioned identity. All wording is "Claude drafts, Jack reviews" so nothing reputationally sensitive (the #7 no-returns discipline, the honest new-grad register) ships unreviewed.
Output: Six edited source files with locked copy, gated by a human checkpoint. 25-03 then reads these to regenerate the corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-RESEARCH.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-PATTERNS.md
@src/content/experience/holloway.mdx
@src/content/experience/balfour-beatty.mdx
@src/content/projects/multi-chain-evm.mdx
@src/data/about-chat.ts
@src/data/about.ts
@src/data/portfolio-context.static.json
@src/prompts/system-prompt.ts
</context>

## Artifacts this phase produces (exclude from drift/source-grounding)

- New `chatSummary:` frontmatter field on holloway.mdx, multi-chain-evm.mdx, balfour-beatty.mdx (all single-line double-quoted, no embedded double-quotes — read by `readStringField` which throws on `\"`).
- Rewritten `ABOUT_CHAT_INTRO`/`ABOUT_CHAT_P1`/`ABOUT_CHAT_P2`/`ABOUT_CHAT_P3` (single-line double-quoted export form preserved — `parseAboutChatExports` exits 2 on template-literal/multi-line forms).
- Refreshed `personal.summary` + additive `skills` entries in portfolio-context.static.json; the stale `education` object is removed (build emits it in 25-03).
- The #7 topic-ban directive removed from system-prompt.ts and the audience line re-framed.
- Locked positioning anchor tokens "looking for" + "full-time" (consumed by 25-01's fixture anchors).

<tasks>

<!-- planner-discipline-allow: those are out of scope -->

<task type="auto">
  <name>Task 1: Author the three third-person chatSummary frontmatter fields (Holloway / #7 / Balfour)</name>
  <files>src/content/experience/holloway.mdx, src/content/projects/multi-chain-evm.mdx, src/content/experience/balfour-beatty.mdx</files>
  <read_first>
    - src/content/experience/holloway.mdx (the first-person summary :18 + highlights :20-27 + "Themes at a glance" :76-83 — the content to condense; do NOT feed the first-person summary into chat)
    - src/content/projects/multi-chain-evm.mdx (frontmatter :1-20 has no chatSummary; the Problem/Approach body :22+ carries the eight-stage safety pipeline, per-chain MEV transport, restart-safe exits, no-double-sell state machine)
    - src/content/projects/solsniper.mdx (chatSummary line — the third-person engineering-invariants register + single-line form to mirror)
    - src/content/experience/balfour-beatty.mdx (role/company/dateRange :2-7; first-person summary :9 that must NOT be emitted)
    - 25-RESEARCH.md §4 (#7 path) + §5 (Balfour option a) + 25-PATTERNS.md holloway/multi-chain-evm/balfour analog blocks
    - src/data/about-chat.ts header (:9-15) — the FIRST_PERSON_LEAK token list every string must avoid
  </read_first>
  <action>
    Author a single-line, double-quoted `chatSummary:` frontmatter field on each of the three MDX files. Third person throughout ("Jack built", "Jack grew"); avoid every FIRST_PERSON_LEAK token; no embedded double-quotes (use single quotes or restructure — readStringField throws on `\"`). En dashes are permitted (chat is exempt from the site em-dash ban, D-13); zero em dashes (U+2014).
    - holloway.mdx: ~150-220 words condensing the 5 strongest specifics (D-01): grew the test suite from 0 to ~1,400 passing checks (money/payroll math pinned to the penny before refactor); closed a cross-tenant leak and added row-level security across all 47 entities (customer portal re-scoped server-side, 223 jobs down to 1); recovered 91 wrongly-archived production jobs; built an idempotent geofenced payroll time-clock; consolidated the data-access layer into a query-factory (killed silent 5,000-record truncations + a React Query cache-key collision). Frame the headline as solo contract engineer on Holloway Connect, a live production operations platform.
    - multi-chain-evm.mdx: engineering-invariants framing (D-02) mirroring the case study's discipline: the eight-stage token-safety pipeline with a sell-simulation hard gate, pluggable per-chain MEV transport (Flashbots / bloXroute / public RPC), restart-safe volatility-adaptive exits persisted to SQLite (WAL), an idempotent no-double-sell state machine. CARRY the explicit NO returns/profit claims (the project makes no claims about returns). No schema change needed (projects read chatSummary via readStringField, not Zod).
    - balfour-beatty.mdx: one-line third-person chatSummary (D-03) — Project Management Intern at Balfour Beatty (May 2023 - Aug 2023) who coordinated deliverables and subcontractor timelines across active construction workstreams. Do NOT emit the first-person summary field.
    These are DRAFTS for Jack's review in Task 3 — do not consider the copy locked until then.
  </action>
  <verify>
    <automated>pnpm exec astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec astro check` stays 0/0/0 (the experience schema types chatSummary as optional string — adding it validates; Balfour hasCaseStudy:false + techStack:[] still validate).
    - `grep -c 'chatSummary:' src/content/experience/holloway.mdx src/content/projects/multi-chain-evm.mdx src/content/experience/balfour-beatty.mdx` shows each file has exactly one chatSummary line.
    - No chatSummary value contains an em dash (U+2014) or an embedded double-quote; each is single-line.
    - The Holloway chatSummary is 150-220 words; the #7 chatSummary contains no profit/returns claim; neither reads in first person (spot-check against the leak-token list — the authoritative gate is 25-03's checkFirstPersonLeaks).
  </acceptance_criteria>
  <done>each of holloway.mdx, multi-chain-evm.mdx, balfour-beatty.mdx carries one single-line third-person chatSummary; astro check 0/0/0; #7 makes no returns claim.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite positioning + identity + prompt template (about-chat.ts, static.json, system-prompt.ts)</name>
  <files>src/data/about-chat.ts, src/data/portfolio-context.static.json, src/prompts/system-prompt.ts</files>
  <read_first>
    - src/data/about-chat.ts (full — INTRO :19-20 "junior", P1 :23-24, P2 :27-28, P3 :31-32 "junior or entry-level"; the single-line double-quoted export form is MANDATORY)
    - src/data/about.ts (the Phase 24 first-person site register to third-person-mirror: P1 "solo contract engineer on Holloway Connect ... recently finished my B.S.", P3 "looking for a full-time software engineering role ... correctness, reliability, and performance")
    - src/data/portfolio-context.static.json (personal.summary :6, education :8-12 to REMOVE, skills :13-18)
    - src/prompts/system-prompt.ts (audience line :5 "junior software-engineering roles"; the #7 ban sentence at the END of the <security> block :56; the ENTIRE <security> block :31-57 that must stay byte-intact apart from that one sentence)
    - 25-RESEARCH.md §6 (positioning sources) + §9 (system-prompt) + Security Domain (preserve defenses)
    - 25-PATTERNS.md about-chat / static.json / system-prompt analog blocks
  </read_first>
  <action>
    (A) src/data/about-chat.ts — full third-person positioning rewrite (D-05), keeping the exact single-line `export const NAME = "..."` form for all four constants:
    - ABOUT_CHAT_INTRO: drop "junior" — present Jack as a software engineer (new-grad register), keep the "systems that don't break at 3 a.m." voice.
    - ABOUT_CHAT_P1 / ABOUT_CHAT_P2: keep the voice-neutral craft framing; adjust to the new-grad-with-shipped-production-experience register (mirror about.ts P1: solo contract engineer on Holloway Connect, recently finished B.S. in CS).
    - ABOUT_CHAT_P3: mirror about.ts P3 — Jack is the solo contract engineer on Holloway Connect AND is currently looking for a full-time software engineering role on a team that values correctness, reliability, and performance. The P3 string MUST contain the literal tokens "looking for" and "full-time" (these are the locked anchors 25-01 pins; the human review must preserve them). Third person; avoid all FIRST_PERSON_LEAK tokens; no em dashes.
    (B) src/data/portfolio-context.static.json:
    - Refresh personal.summary (:6) to the new-grad-with-production-experience framing (D-06); keep personal.title "Software Engineer".
    - Additively extend skills (D-08) — no pruning of existing entries: add "Deno" (a runtime; place under languages or a fitting existing key), "Ethers.js" and "TanStack Query" under frameworks, "Vitest" under tools.
    - REMOVE the stale education object (:8-12) entirely — education single-sources from education.ts via the 25-03 build wiring (D-07). (25-03 emits `education` into the merge; the existing chat-context-integrity D-08-key test still requires the corpus to have an `education` property, so 25-03 MUST emit it.)
    (C) src/prompts/system-prompt.ts (§9 — SCOPE ADDITION):
    - Line 5: re-frame the audience away from "junior software-engineering roles" to the new-grad / full-time positioning (drop "junior"; match the about-chat register). Draft wording for Jack's review.
    - <security> block: remove ONLY the sentence banning the #7 topic (the `Never discuss "MULTI-DEX CRYPTO TRADER" ... those are out of scope.` sentence at the end of :56). KEEP "Never pivot to projects not listed in the knowledge block." and PRESERVE the entire rest of the <security> block byte-intact: the tiered-refusal lines, the attack-pattern list, the PII/resume tiers, and the framing-tag suppression. Do not touch <role>/<tone>/<constraints>/<knowledge>.
    These are DRAFTS for Task 3.
  </action>
  <verify>
    <automated>pnpm exec astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec astro check` stays 0/0/0.
    - about-chat.ts: all four ABOUT_CHAT_* constants remain single-line double-quoted; INTRO no longer contains "junior"; P3 contains "looking for" and "full-time"; no FIRST_PERSON_LEAK token, no em dash.
    - static.json: valid JSON (`node -e "JSON.parse(require('fs').readFileSync('src/data/portfolio-context.static.json','utf8'))"` exits 0); no top-level `education` key; skills includes Deno, TanStack Query, Vitest, Ethers.js; existing skills entries intact.
    - system-prompt.ts: the #7 topic-ban sentence is gone (`grep -c 'those are out of scope' src/prompts/system-prompt.ts` == 0); the audience line no longer contains "junior"; the <security> refusal tiers + attack-pattern list + framing-tag suppression are unchanged (diff shows only the audience line + the one removed sentence).
  </acceptance_criteria>
  <done>about-chat.ts drops "junior" + P3 keeps "looking for"/"full-time"; static.json refreshed (no education key, +4 skills); system-prompt.ts audience re-framed + #7 ban removed with the security block otherwise byte-intact; astro check 0/0/0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human copy review — lock all third-person copy before corpus regen</name>
  <files>src/content/experience/holloway.mdx, src/content/projects/multi-chain-evm.mdx, src/content/experience/balfour-beatty.mdx, src/data/about-chat.ts, src/data/portfolio-context.static.json, src/prompts/system-prompt.ts</files>
  <action>Pause execution and present the drafted copy across all six files for Jack's review. Do NOT proceed to 25-03 (corpus regen) until Jack approves. If Jack requests wording edits, apply them at the source files, keeping the "looking for" + "full-time" tokens in ABOUT_CHAT_P3, then re-present. Lock the copy on approval.</action>
  <what-built>
    Drafted third-person chat copy across six files: Holloway chatSummary (~150-220 words), #7 chatSummary (engineering-invariants + explicit no-returns), Balfour one-liner chatSummary, the about-chat.ts rewrite (drop "junior"; new-grad w/ shipped production experience; solo contract engineer on Holloway AND seeking a full-time SWE role), the personal.summary refresh + additive skills, and the system-prompt.ts audience line + #7-ban removal (security block otherwise byte-intact).
  </what-built>
  <how-to-verify>
    1. Read the drafted chatSummary on src/content/experience/holloway.mdx, src/content/projects/multi-chain-evm.mdx, src/content/experience/balfour-beatty.mdx.
    2. Read the rewritten src/data/about-chat.ts and the refreshed src/data/portfolio-context.static.json (personal.summary + skills).
    3. Read the src/prompts/system-prompt.ts audience line (:5) and confirm the <security> block is unchanged except the removed #7-ban sentence.
    4. Confirm: third-person throughout; the #7 copy makes NO returns/profit claim; the register is honest new-grad (not senior/"5+ years"); ABOUT_CHAT_P3 keeps the tokens "looking for" and "full-time"; specifics (0->~1,400 tests, RLS across 47 entities / 223->1, 91-job recovery) are accurate.
    5. Note any wording changes to apply. If you change P3, keep "looking for" + "full-time" (25-01's fixture anchors depend on them).
  </how-to-verify>
  <resume-signal>Type "approved" to lock the copy, or list the exact wording edits to apply before locking.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor -> chat API | user messages reach `buildSystemPrompt`; the system-prompt `<security>` block is the prompt-injection guard being edited here |
| authored copy -> cached `<knowledge>`/`<role>` | chatSummary + about-chat + static identity become the cached, model-visible knowledge/positioning |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Information disclosure (prompt injection) | system-prompt.ts `<security>` block | medium | mitigate | Remove ONLY the over-broad #7 topic-ban sentence (a reduction of a content restriction on public portfolio content, NOT a security weakening). Preserve the tiered-refusal block, attack-pattern list, PII/resume refusal tiers, and framing-tag suppression byte-intact. 25-01's preserved prompt-injection battery + 25-04's D-26/sse gates re-verify. No high-severity threat surfaces. |
| T-25-02 | Tampering (voice break) | Holloway/#7/Balfour chatSummary + about-chat.ts | medium | mitigate | Third-person copy authored + human-reviewed; the definitive gate is 25-03's checkFirstPersonLeaks (exit 2). Balfour's first-person summary is deliberately NOT emitted. |
| T-25-03 | Repudiation / accuracy | #7 chatSummary returns/profit claims | medium | mitigate | Carry the explicit no-returns discipline into chat; human review (Task 3) confirms no profit claim ships on the reputationally sensitive project. |
| T-25-SC | Tampering (supply chain) | npm installs | low | accept | Zero packages installed (QA-02); no `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `pnpm exec astro check` 0/0/0 after all edits.
- static.json parses as JSON with no `education` key and the four new skills present.
- system-prompt.ts: `grep -c 'those are out of scope' src/prompts/system-prompt.ts` == 0; audience line drops "junior"; `<security>` refusal tiers unchanged.
- Human checkpoint APPROVED before this plan is considered complete (autonomous:false).
- NOTE: this plan does NOT regenerate portfolio-context.json — the corpus regen + leak guard is 25-03's job (do not run `pnpm build:chat-context` write mode here; a `--check` will report expected drift).
</verification>

<success_criteria>
- All six files edited; astro check 0/0/0; copy locked via Jack's approval.
- No gated file (BaseLayout.astro / global.css / chat.ts / api/chat.ts) touched; system-prompt.ts and portfolio-context.static.json are NON-gated (D-14).
- The two positioning anchor tokens "looking for" + "full-time" are preserved in ABOUT_CHAT_P3.
</success_criteria>

<output>
Create `.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-02-SUMMARY.md` when done, recording the locked copy per file and the human-review disposition.
</output>
