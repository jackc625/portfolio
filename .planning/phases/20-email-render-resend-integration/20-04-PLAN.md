---
phase: 20-email-render-resend-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 20-03
files_modified:
  - .planning/phases/20-email-render-resend-integration/20-UAT.md
  - .planning/phases/20-email-render-resend-integration/DEPLOY-GATE.md
autonomous: false
requirements:
  - MAIL-01
  - MAIL-02
  - MAIL-03
  - MAIL-04
  - MAIL-05

must_haves:
  truths:
    - "D-02: 20-UAT.md contains exactly 6 numbered manual operator steps mapping 1:1 to Phase 20 ROADMAP success criteria + closure: Step 1 (seed live: with stale last_activity_at via wrangler kv key put --remote), Step 2 (operator flips triggers.crons to ['* * * * *'] + wrangler deploy), Step 3 (verify Gmail Inbox arrival + delivered:{sid}.dry_run=false + populated resend_message_id + Workers Logs chat.delivery.sent), Step 4 (revert triggers.crons to ['0 * * * *'] + wrangler deploy), Step 5 (backlog cleanup of test-uat-* keys), Step 6 (organic real-traffic 7-day soft cap)"
    - "DEPLOY-GATE.md mirrors Plan 17-08 template at .planning/phases/17-foundations-migration-dns-debt-sweep/DEPLOY-GATE.md; status=pending at first commit; explicit prohibition 'Executor MUST NOT push' clause; pre-deploy checklist covers all forward-defense gates (pnpm test full battery >=544+/0/2; pnpm exec astro check 0/0/0; pnpm build clean; package.json deps byte-identical; wrangler.jsonc DRY_RUN==='0' confirmed via tests/build/wrangler-dry-run-shape.test.ts; D-26 chat-surface battery PRESERVED; D-15 SSE GREEN; TEST-03 Anthropic GREEN)"
    - "Plan is autonomous: false — Task 2 is a checkpoint:human-verify pausing for operator to (a) cleanly review deploy gate, (b) execute Step 6 (or accept the 7-day soft cap proxy via scripts/resend-warmup.mjs re-execution), (c) reply 'approved — deploy gate cleared' as durable audit trail per Plan 17-08 precedent, (d) operator runs `git push origin main` themselves — executor MUST NOT push"
    - "Both files committed together as the FINAL Phase 20 metadata commit; no other source changes ride along — purely operational documentation per D-04"
    - "All 5 ROADMAP success criteria are encoded as expected-result blocks within 20-UAT.md so an auditor can trace each success criterion to its evidence block (Phase 17/18/19 precedent carried)"
  artifacts:
    - path: ".planning/phases/20-email-render-resend-integration/20-UAT.md"
      provides: "6-step manual operator UAT spec for live Resend send verification (closes Phase 20 success criteria 1-5)"
      min_lines: 250
    - path: ".planning/phases/20-email-render-resend-integration/DEPLOY-GATE.md"
      provides: "Operator-confirmation gate before git push origin main; mirrors Plan 17-08 template"
      min_lines: 100
  key_links:
    - from: "20-UAT.md Step 3"
      to: "src/lib/chat-delivery.ts DeliveredMarker.resend_message_id field + Workers Logs chat.delivery.sent event"
      via: "operator visual verification of cross-system message_id correlation (KV value + Workers Logs + Resend dashboard + Gmail Inbox)"
      pattern: "resend_message_id"
    - from: "20-UAT.md Step 4"
      to: "wrangler.jsonc triggers.crons revert + tests/build/wrangler-dry-run-shape.test.ts Invariant 2 (Pitfall 6 forward-defense)"
      via: "operator-run wrangler deploy of reverted cron + next-CI source-text test catch if forgotten"
      pattern: "0 \\* \\* \\* \\*"
    - from: "DEPLOY-GATE.md status=pending -> status=confirmed"
      to: "operator chat-reply 'approved — deploy gate cleared' as durable audit trail"
      via: "operator-controlled YAML frontmatter update + operator-controlled git push origin main"
      pattern: "status\\s*:\\s*(pending|confirmed)"
---

<objective>
Wave 3 (depends on Wave 2: Plan 20-03 substitution + wrangler flip): Author the 6-step `20-UAT.md` operator UAT spec (closes ROADMAP success criteria 1-5 via seed-then-cron + organic real-traffic) and `DEPLOY-GATE.md` (mirrors Plan 17-08 deploy-gate template; status=pending at first commit; operator-controlled status=confirmed and `git push origin main`). This is the FINAL Phase 20 metadata commit per D-04 deploy-gate posture.

Purpose: This plan is purely operational documentation. No source code touched. The two new docs encode the operator runbook (UAT) + executor-MUST-NOT-push gate (DEPLOY-GATE) so the executor can complete its work locally + commit cleanly while the operator drives the actual deploy. Phase 17 Plan 17-08 closed via this same pattern (DEPLOY-GATE.md status=confirmed by Jack Cutrara 2026-05-11 via chat-reply audit trail). Plan 20-04 inherits the pattern verbatim.

Output: 2 NEW files in the phase directory. After commit + operator UAT + operator approval + operator push, Phase 20 is COMPLETE and v1.3 milestone closes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-email-render-resend-integration/20-CONTEXT.md
@.planning/phases/20-email-render-resend-integration/20-RESEARCH.md
@.planning/phases/20-email-render-resend-integration/20-PATTERNS.md
@.planning/phases/20-email-render-resend-integration/20-VALIDATION.md
@.planning/phases/20-email-render-resend-integration/20-01-SUMMARY.md
@.planning/phases/20-email-render-resend-integration/20-02-SUMMARY.md
@.planning/phases/20-email-render-resend-integration/20-03-SUMMARY.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md
@.planning/phases/18-persistence-identity-kv-write-path-sessionid/18-UAT.md
@.planning/phases/17-foundations-migration-dns-debt-sweep/17-UAT.md
@.planning/phases/17-foundations-migration-dns-debt-sweep/DEPLOY-GATE.md
@wrangler.jsonc
@scripts/resend-warmup.mjs

<interfaces>
<!-- This plan is operational documentation only. The contracts encoded in 20-UAT.md + DEPLOY-GATE.md reference: -->
<!--   - Plan 20-03 close state (DRY_RUN=='0' in wrangler.jsonc; sendOne substitution wired; delivered marker has resend_message_id) -->
<!--   - Plan 17-08 deploy-gate template (executor-MUST-NOT-push; chat-reply audit trail) -->
<!--   - Phase 19 19-UAT.md numbered-step + expected/result block structure -->
<!--   - KV namespace IDs from wrangler.jsonc:11-17 (Production: eaa30fef259e4a6b9505b41bbf3f8f01; Preview: 115f3c1b0f8a4a1da9fee78c48dcb749) -->

UAT Step -> ROADMAP success criterion mapping (per CONTEXT.md D-02 + RESEARCH § specifics):
- Step 1 (seed) + Step 2 (cron flip + invoke) + Step 3 (verify) -> SC1 (one email lands in Inbox)
- Step 3 -> SC2 (text-only body + escape) + SC3 (adversarial unit suite is unit-tested in Plan 20-01; UAT verifies live email matches)
- Step 6 organic real-traffic -> SC4 (idempotency in the wild)
- Step 4 (cron revert) + Step 5 (backlog cleanup) -> operational hygiene
- Pre-deploy checklist in DEPLOY-GATE.md -> SC5 (zero new runtime deps)

`--remote` flag discipline (Phase 18 / Phase 19 UAT learning — defaults to --local burns hours; document explicitly in deviation paragraph)
</interfaces>

<patterns>
<!-- Primary analog (UAT spec template): .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md — 5-step structure + expected/result blocks; Plan 20-04 mirrors with 6 steps -->
<!-- Secondary analog: .planning/phases/18-persistence-identity-kv-write-path-sessionid/18-UAT.md — 8-step KV-write UAT precedent -->
<!-- Primary analog (deploy-gate template): .planning/phases/17-foundations-migration-dns-debt-sweep/DEPLOY-GATE.md — frontmatter + checklist + status=pending->confirmed flow + operator-MUST-NOT-push prohibition -->
<!-- 20-PATTERNS.md Group 7 lines 602-721 enumerates both file shapes + executor notes -->
<!-- Step 6 organic real-traffic 7-day soft cap per CONTEXT.md Discretion -->
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 20-UAT.md (6 numbered manual operator steps with expected/result blocks; status=pending at first commit) and DEPLOY-GATE.md (mirrors Plan 17-08 template; status=pending; operator-MUST-NOT-push prohibition)</name>
  <files>.planning/phases/20-email-render-resend-integration/20-UAT.md, .planning/phases/20-email-render-resend-integration/DEPLOY-GATE.md</files>
  <read_first>
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md (READ FULL — 5-step template; frontmatter shape; expected: / result: block convention; --remote flag discipline; cron-flip-then-revert Step 2/4 pattern; backlog cleanup Step 5; Plan 20-04 mirrors structurally with 6 steps)
    - .planning/phases/18-persistence-identity-kv-write-path-sessionid/18-UAT.md (READ — additional UAT precedent for wrangler kv key operator-side patterns; especially the --remote pre-flight rationale)
    - .planning/phases/17-foundations-migration-dns-debt-sweep/17-UAT.md (READ — first established the numbered-steps + expected/result convention; Plan 20-04 inherits)
    - .planning/phases/17-foundations-migration-dns-debt-sweep/DEPLOY-GATE.md (READ FULL — exact template Plan 20-04 mirrors: frontmatter (type, phase, plan, created, status, operator, gate); Pre-Deploy Checklist with numbered sections; pnpm dev smoke test section; Test-Environment Mapping table; Post-Deploy Verification section; chat-reply audit trail clause; operator-MUST-NOT-push prohibition)
    - wrangler.jsonc (READ — verify Plan 20-03 close state: DRY_RUN === "0"; triggers.crons === ["0 * * * *"]; KV namespace IDs at lines 11-17 — Production eaa30fef259e4a6b9505b41bbf3f8f01, Preview 115f3c1b0f8a4a1da9fee78c48dcb749)
    - scripts/resend-warmup.mjs (READ — Step 6 fallback proxy script reference; the warmup script is the 7-day soft cap proxy if no organic visitor arrives within the soft window)
    - .planning/phases/20-email-render-resend-integration/20-CONTEXT.md (READ <decisions> D-02 + D-04 lines 50-55 + Discretion lines 102-113 — D-02 enumerates the 6 step pattern; D-04 mandates DEPLOY-GATE.md; Discretion sets the 7-day soft cap)
    - .planning/phases/20-email-render-resend-integration/20-PATTERNS.md (READ Group 7 lines 602-721 — both file shapes; the executor notes for each)
    - .planning/phases/20-email-render-resend-integration/20-RESEARCH.md (READ § Validation Architecture lines 116-125 of 20-VALIDATION.md — the Manual-Only Verifications table enumerates the 4 manual checks Plan 20-04 documents)
  </read_first>
  <action>
    AUTHOR `.planning/phases/20-email-render-resend-integration/20-UAT.md`:

    FRONTMATTER (mirror 19-UAT.md):
    ```
    ---
    status: pending
    phase: 20-email-render-resend-integration
    source: [20-01-SUMMARY.md, 20-02-SUMMARY.md, 20-03-SUMMARY.md]
    started: <ISO-timestamp at first commit>
    updated: <will be updated post-operator-execution>
    deviation: |
      [free-form deviation log paragraph — initial: "wrangler kv key commands MUST pass --remote; defaults to --local and burns hours per Phase 18 UAT learning. KV namespace IDs from wrangler.jsonc:11-17. Operator runs `wrangler deploy` for Step 2 + Step 4 cron-flip-then-revert; executor MUST NOT run wrangler deploy per D-04 + DEPLOY-GATE.md."]
    ---
    ```

    INTRO PARAGRAPH (mirror 19-UAT.md:60-66):
    Title: `# Phase 20 UAT — Email Render + Resend Integration (live-mail toggle)`. Then a paragraph stating: this UAT closes ROADMAP Phase 20 success criteria 1-5; the 6-step sequence maps Step 1+2+3 -> SC1, Step 3 -> SC2+SC3, Step 6 -> SC4, Step 4+5 -> operational hygiene, pre-deploy checklist (DEPLOY-GATE.md) -> SC5; Step 1 + Step 2 are operator-controlled per D-04 + DEPLOY-GATE.md (executor MUST NOT run wrangler deploy); chat-reply confirmation is the durable audit trail per Plan 17-08 precedent.

    STEP 1 — Seed `live:test-uat-<sid>` with stale `last_activity_at` (closes pre-condition for SC1):
    expected: |
      Operator runs from local terminal (NOT executor — operator-controlled per DEPLOY-GATE.md):
        - Generate UUIDv4: `node -e "console.log(crypto.randomUUID())"` -> use as `<sid>` (e.g. `test-uat-deadbeef-1234-4567-8901-abcdef012345`)
        - Compose stale ChatTranscript JSON value with `last_activity_at` set to ISO timestamp >2h ago (use `date -u -d '3 hours ago' +%FT%TZ` on Linux/Mac; `(Get-Date).ToUniversalTime().AddHours(-3).ToString('o')` on PowerShell). Include msg_count: 2; truncated: false; meta with country, referrer, user_agent populated; messages[] with one user + one assistant turn (assistant turn carries cache_read_input_tokens > 0 to exercise D-09/D-10 aggregate)
        - Compose stale KV metadata: `{ "last_activity_at": "<3h-ago-iso>", "msg_count": 2, "window_started_at": "<3h-ago-iso>", "window_count": 2 }`
        - PUT to KV via `wrangler kv key put --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 "live:<sid>" '<value-json>' --metadata '<metadata-json>'`
        - Confirm via `wrangler kv key get --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 "live:<sid>"` returns the JSON
        - NOTE: --remote flag is REQUIRED — defaults to --local and burns hours of debug time per Phase 18 UAT learning
    result: |
      [populated post-UAT by operator with the actual <sid> + timestamps + KV confirmation]

    STEP 2 — Operator flips `triggers.crons` to `["* * * * *"]` + `wrangler deploy` (closes Past-Events trigger for SC1):
    expected: |
      OPERATOR-ONLY (executor MUST NOT run wrangler deploy):
        1. `git diff wrangler.jsonc` shows zero diff before starting
        2. Operator EDITS `wrangler.jsonc` line 25: change `["0 * * * *"]` to `["* * * * *"]`
        3. Operator runs `wrangler deploy`
        4. Wait up to 90 seconds for the next minute boundary
        5. Cloudflare Dashboard -> Workers -> jack-cutrara-portfolio -> Triggers -> Past Events shows >=1 invocation within 90s
        6. Workers Logs (`wrangler tail --format=pretty`) shows `chat.delivery.tick` log line within 90s
    result: |
      [populated post-UAT — Past Events screenshot + first chat.delivery.tick log line + timestamp]

    STEP 3 — Verify Gmail Inbox + `delivered:test-uat-<sid>` value + Workers Logs `chat.delivery.sent` (closes SC1 + SC2 + SC3):
    expected: |
      Within 5 minutes of Step 2 cron flip:
        1. Gmail Inbox (jackcutrara@gmail.com) shows new email from `"Portfolio Chat" <transcripts@mail.jackcutrara.com>` with subject `[Portfolio chat] 2 turns from <country> via <host>` matching the seeded transcript meta
        2. Body opens with provenance line `From: chat widget on jackcutrara.com — visitor message follows below this line.` (literal, byte-distinct from any visitor-typed content per MAIL-03 anti-impersonation)
        3. Body contains 8-line metadata header (Session, Started, Last turn (with duration), From, Referrer, User-agent, Messages, Cache: <hits>/<total> turns hit, ...) with 12-char padded label column
        4. Body contains turn markers `>>> visitor:` / `<<< bot:` on own line; raw content below; blank line between turns
        5. Body is plaintext (Gmail's "Show original" -> Content-Type: text/plain; charset=utf-8; NO text/html part) per MAIL-02
        6. KV verification: `wrangler kv key get --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 "delivered:test-uat-<sid>"` returns JSON `{ v: 1, sid: "test-uat-...", delivered_at: "<iso>", dry_run: false, msg_count: 2, truncated: false, resend_message_id: "<resend-uuid>" }` — `dry_run: false` confirms Plan 20-03 substitution fired; `resend_message_id` is populated
        7. KV cleanup verification: `wrangler kv key get --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 "live:test-uat-<sid>"` returns `null` (live: was deleted by promoteOne step 5)
        8. Workers Logs query: `wrangler tail --format=pretty --search "chat.delivery.sent"` shows a structured JSON line with `sid: "test-uat-...", resend_message_id: "<same-uuid-as-step-6>", attempt: 1`
        9. Resend Dashboard (https://resend.com/emails) shows the corresponding send under `Sent` with the same `resend_message_id` from Step 6 + Step 8
        10. NO `chat.delivery.failed` log lines for the test sid; NO `chat.delivery.retry` log lines (success on first attempt expected for the seeded transcript)
    result: |
      [populated post-UAT — Gmail screenshot + KV value blocks + Workers Logs JSON line + Resend Dashboard screenshot]

    STEP 4 — Revert `triggers.crons` to `["0 * * * *"]` + `wrangler deploy` (operational hygiene; tests/build/wrangler-dry-run-shape.test.ts Invariant 2 catches if forgotten):
    expected: |
      OPERATOR-ONLY:
        1. Operator EDITS `wrangler.jsonc` line 25: change `["* * * * *"]` back to `["0 * * * *"]`
        2. Operator runs `wrangler deploy`
        3. Cloudflare Dashboard -> Workers -> Triggers shows `0 * * * *` schedule active
        4. Workers Logs over the next 5 minutes show NO new `chat.delivery.tick` events (cron now hourly, not per-minute)
    result: |
      [populated post-UAT — `git diff wrangler.jsonc` confirming revert + Cloudflare Dashboard screenshot]

    STEP 5 — Backlog cleanup of test seeds (operational hygiene):
    expected: |
      OPERATOR-ONLY:
        1. `wrangler kv key delete --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 "delivered:test-uat-<sid>"`
        2. `wrangler kv key get --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 "delivered:test-uat-<sid>"` returns `null`
        3. (live:test-uat-<sid> already deleted by Step 3 / promoteOne — confirm via `wrangler kv key get` returns `null`)
        4. `wrangler kv key list --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 --prefix test-uat-` returns empty array
    result: |
      [populated post-UAT — kv list confirmation showing zero test-uat-* keys remain]

    STEP 6 — Wait for organic real-traffic conversation + record evidence (closes SC4 — idempotency in the wild) — 7-day soft cap:
    expected: |
      Within 7 days post-deploy:
        1. A real visitor opens the chat widget on jackcutrara.com, has a >=2-turn conversation, closes the tab
        2. >=2 hours pass with no further activity from that session
        3. Next hourly cron tick (or any subsequent tick) processes the session under DRY_RUN='0'
        4. Email arrives in Jack's Gmail Inbox (NOT Spam) within 3 hours of last activity
        5. Operator records: Resend message ID + arrival timestamp + Gmail Inbox screenshot + Workers Logs `chat.delivery.sent` JSON line screenshot

      7-DAY SOFT CAP per CONTEXT.md Discretion:
        If no organic visitor session arrives within 7 days, milestone may close on Steps 1-5 + manual `node scripts/resend-warmup.mjs` re-execution as proxy — verify the warmup script's 1-shot send still lands in Inbox (proves the wire still works post-deploy independent of cron sweep timing).
    result: |
      [populated post-UAT — either (a) organic visitor evidence within 7 days, or (b) 7-day-soft-cap fallback with warmup script proxy evidence]

    AUTHOR `.planning/phases/20-email-render-resend-integration/DEPLOY-GATE.md` mirroring Plan 17-08:

    FRONTMATTER:
    ```
    ---
    type: deploy-gate
    phase: 20-email-render-resend-integration
    plan: 04
    created: <ISO-date at first commit>
    confirmed:
    status: pending
    operator:
    gate: PENDING
    ---
    ```

    OPENING SECTION:
    `# Phase 20 Deploy Gate` + intro paragraph: "Plan 20-03 substituted sendOne with the live Resend wrapper + flipped wrangler.jsonc DRY_RUN === '0'. The next `git push origin main` is the live-mail go-live event. Per D-04, the executor MUST NOT push — operator runs UAT (20-UAT.md), confirms approval via chat-reply 'approved — deploy gate cleared' as durable audit trail (Plan 17-08 precedent), then runs `git push origin main` themselves."

    PRE-DEPLOY CHECKLIST (numbered sections per Plan 17-08):

    ### 1. Local build is clean
    - [ ] `pnpm exec vitest run` exits 0 with PASS count >= 544 / FAIL count == 0 / SKIP count == 2 (Phase 20 close baseline target per RESEARCH § specifics)
    - [ ] `pnpm exec astro check` exits 0/0/0 (Plan 17-08 / Phase 19 baseline preserved)
    - [ ] `pnpm build` exits 0 cleanly (wrangler types regen + astro check + astro build)

    ### 2. Phase 20 forward-defense gates GREEN
    - [ ] `pnpm exec vitest run tests/build/wrangler-dry-run-shape.test.ts` exits 0 — `vars.DRY_RUN === "0"` confirmed AT phase close + `triggers.crons === ["0 * * * *"]` confirmed (UAT Step 4 cron revert verified pre-push)
    - [ ] `pnpm exec vitest run tests/build/wrangler-cron-shape.test.ts` exits 0 — both updated DRY_RUN === "0" assertion + cron assertion GREEN
    - [ ] `pnpm exec vitest run tests/build/chat-delivery-send-site.test.ts` exits 0 — Plan 20-03 substitution wired + DRY_RUN='1' rollback runway preserved
    - [ ] `pnpm exec vitest run tests/api/email-render.test.ts tests/api/email-render.adversarial.test.ts tests/api/email-resend.test.ts tests/api/chat-delivery.test.ts` exits 0 — all Phase 20 unit batteries GREEN

    ### 3. Cross-phase forward-defense gates GREEN (Phase 20 touches ZERO chat-surface)
    - [ ] D-26 chat-surface regression battery byte-identical or grown (`pnpm exec vitest run` PASS count >= 498 baseline; Phase 20 added ~46 net new tests)
    - [ ] D-15 SSE byte-identical anchor: `pnpm exec vitest run tests/api/sse-snapshot.test.ts` exits 0
    - [ ] TEST-03 Anthropic prompt-cache integrity: `pnpm exec vitest run tests/api/anthropic-payload-shape.test.ts` exits 0
    - [ ] DEBT-02 cache-hit-logs: `pnpm exec vitest run tests/api/cache-hit-logs.test.ts` exits 0

    ### 4. MAIL-01 zero-new-runtime-dep lock
    - [ ] `git diff origin/main..HEAD package.json | grep -E '^\+.*":' | grep -v 'scripts'` returns empty (NO new lines under `dependencies`; the only acceptable changes are in `scripts` block — and Phase 20 should have zero `scripts` changes either)
    - [ ] `git diff --stat package.json` shows `package.json` unchanged OR only whitespace-level changes (defensive — Phase 20 should not touch package.json at all)

    ### 5. Operational state confirmed
    - [ ] `wrangler.jsonc` line 21 contains exactly `"DRY_RUN": "0"`
    - [ ] `wrangler.jsonc` line 25 contains exactly `"crons": ["0 * * * *"]` (NO leftover `* * * * *` from UAT Step 2 — Step 4 revert confirmed)
    - [ ] Wrangler secret `RESEND_API_KEY` present in production: `wrangler secret list` includes RESEND_API_KEY (set Plan 17-06)
    - [ ] Wrangler secrets `CHAT_RECIPIENT_EMAIL` + `CHAT_SENDER_EMAIL` present (set Plan 17-02)
    - [ ] `wrangler.jsonc` `vars.CHAT_REPLY_TO_EMAIL` === `jackcutrara@gmail.com` (Phase 19 default)

    POST-DEPLOY VERIFICATION (operator-only, mirrors 20-UAT.md Steps 1-6):
    Defer to `20-UAT.md` for the full 6-step runbook. The deploy-gate clears on operator confirmation that Steps 1-5 are GREEN; Step 6 (organic real-traffic) closes asynchronously within the 7-day soft cap.

    EXECUTOR-MUST-NOT-PUSH PROHIBITION (verbatim from Plan 17-08):
    > **CRITICAL — D-04 / Plan 17-08 deploy-gate posture:** The executor (Claude Code) MUST NOT run `git push origin main` for this phase. After committing Plans 20-01..20-04 locally, the executor STOPS at this gate. The operator (Jack Cutrara) runs the 6-step UAT in `20-UAT.md` against the post-push production deployment, confirms via chat-reply "approved — deploy gate cleared" (durable audit trail per Plan 17-08 precedent), updates this file's frontmatter to `status: confirmed`, `gate: CONFIRMED`, `confirmed: <date>`, `operator: Jack Cutrara`, then runs `git push origin main` themselves.

    ROLLBACK PROCEDURE (per D-03):
    > **Rollback:** A single-line revert of `wrangler.jsonc` line 21 (`"DRY_RUN": "0"` -> `"1"`) + `wrangler deploy` reverts ALL Phase 20 behavior in ~60s. The Plan 20-01 renderer + Plan 20-02 wrapper + Plan 20-03 substitution all stay in source — they're unreachable behind the dry-run gate. The DRY_RUN=='1' branch in src/lib/chat-delivery.ts sendOne IS the rollback runway (D-03 lock; tests/build/chat-delivery-send-site.test.ts Invariants D + E source-text-lock its presence). DO NOT git revert the Phase 20 commits as a rollback — that's a heavier blast radius and harder to re-ship cleanly.

    Both files committed together as the FINAL Phase 20 metadata commit. After commit + push (operator-controlled), Phase 20 is ready for operator-driven execution of the UAT runbook.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/wrangler-dry-run-shape.test.ts tests/build/chat-delivery-send-site.test.ts tests/build/wrangler-cron-shape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/20-email-render-resend-integration/20-UAT.md` exists; `wc -l` returns >= 250
    - `.planning/phases/20-email-render-resend-integration/DEPLOY-GATE.md` exists; `wc -l` returns >= 100
    - 20-UAT.md frontmatter has `status: pending`
    - 20-UAT.md contains exactly 6 numbered step headings (`### 1. ` through `### 6. `)
    - 20-UAT.md every step has both `expected: |` and `result: |` blocks
    - 20-UAT.md `--remote` flag appears at least 5 times (one per wrangler kv key invocation)
    - 20-UAT.md cites the production KV namespace ID `eaa30fef259e4a6b9505b41bbf3f8f01` at least 4 times (Steps 1, 3, 5)
    - 20-UAT.md Step 6 mentions "7-day soft cap" and references `scripts/resend-warmup.mjs` as the proxy fallback
    - 20-UAT.md mentions "executor MUST NOT" at least once (operator-only deploy clause)
    - DEPLOY-GATE.md frontmatter has `status: pending` and `gate: PENDING` and `operator:` (empty)
    - DEPLOY-GATE.md Pre-Deploy Checklist has at least 5 numbered sections
    - DEPLOY-GATE.md contains exact substring `git push origin main` in the prohibition section
    - DEPLOY-GATE.md contains exact substring `approved — deploy gate cleared` (the chat-reply audit trail token)
    - DEPLOY-GATE.md ROLLBACK PROCEDURE section references D-03 + the single-line wrangler.jsonc revert pattern
    - All Phase 20 forward-defense build tests still GREEN: `pnpm exec vitest run tests/build/wrangler-dry-run-shape.test.ts tests/build/chat-delivery-send-site.test.ts tests/build/wrangler-cron-shape.test.ts` exits 0
    - `pnpm exec astro check` exits 0/0/0 (no source changes; informational)
    - `pnpm exec vitest run` exits 0 with PASS count >= 544 (Phase 20 target — Plan 20-04 adds zero net new tests; baseline preserved)
    - `git diff --stat package.json` shows zero changes (Plan 20-04 touches no source code)
  </acceptance_criteria>
  <done>
    20-UAT.md (6 numbered steps + frontmatter + intro + deviation paragraph) and DEPLOY-GATE.md (frontmatter + checklist + prohibition + rollback) both exist. Phase 20 forward-defense build tests still GREEN. Phase 20 source code is structurally complete + locally committed. Plan 20-04 commit is the FINAL Phase 20 metadata commit per D-04. Operator can now run UAT against post-push production after operator-controlled push.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs DEPLOY-GATE.md pre-deploy checklist + 20-UAT.md UAT runbook against post-push production + replies "approved — deploy gate cleared" + updates DEPLOY-GATE.md frontmatter to status=confirmed + runs git push origin main</name>
  <what-built>
    Plan 20-04 Task 1 authored 20-UAT.md (6 numbered manual operator steps closing ROADMAP success criteria 1-5 with Step 6 organic-real-traffic + 7-day soft cap) and DEPLOY-GATE.md (mirrors Plan 17-08 template; status=pending; pre-deploy checklist + executor-MUST-NOT-push prohibition + rollback procedure).

    The full Phase 20 structural work is complete in commits from Plans 20-01..20-04:
    - 20-01: src/lib/email/render.ts pure renderer + 2 unit-test files (~19 new tests)
    - 20-02: src/lib/email/resend.ts pure REST wrapper + 1 unit-test file (~14 new tests)
    - 20-03: src/lib/chat-delivery.ts substitution + DeliveredMarker.resend_message_id additive + wrangler.jsonc DRY_RUN '1' -> '0' + extended chat-delivery.test.ts + 2 NEW build tests + 1 EDIT existing build test (~13 net new tests)
    - 20-04: 20-UAT.md + DEPLOY-GATE.md (zero new tests; documentation only)

    Phase 20 close baseline: pnpm test PASS >= 544 / FAIL 0 / SKIP 2; pnpm exec astro check 0/0/0; pnpm build clean; package.json deps byte-identical to Phase 19 close.
  </what-built>
  <how-to-verify>
    OPERATOR-ONLY (executor MUST NOT push or run wrangler deploy):

    PRE-PUSH (run from local main with all Phase 20 commits present, BEFORE git push):
    1. Open DEPLOY-GATE.md. Verify status: pending. Run each item in the Pre-Deploy Checklist (sections 1-5):
       - `pnpm exec vitest run` exits 0; verify PASS >= 544 / FAIL 0 / SKIP 2
       - `pnpm exec astro check` exits 0/0/0
       - `pnpm build` exits 0 cleanly
       - `pnpm exec vitest run tests/build/wrangler-dry-run-shape.test.ts tests/build/wrangler-cron-shape.test.ts tests/build/chat-delivery-send-site.test.ts` exits 0
       - `pnpm exec vitest run tests/api/email-render.test.ts tests/api/email-render.adversarial.test.ts tests/api/email-resend.test.ts tests/api/chat-delivery.test.ts` exits 0
       - `pnpm exec vitest run tests/api/sse-snapshot.test.ts tests/api/anthropic-payload-shape.test.ts tests/api/cache-hit-logs.test.ts` exits 0
       - `git diff origin/main..HEAD package.json | grep -E '^\+.*":' | grep -v 'scripts'` returns empty (no new dependencies)
       - `wrangler secret list` (against production) shows RESEND_API_KEY + CHAT_RECIPIENT_EMAIL + CHAT_SENDER_EMAIL present
       - `cat wrangler.jsonc | grep DRY_RUN` shows `"DRY_RUN": "0"` exactly
       - `cat wrangler.jsonc | grep crons` shows `"crons": ["0 * * * *"]` exactly

    2. If all 5 sections GREEN: operator runs `git push origin main` from local main. The push event ships all of Plans 20-01..20-04 atomically (per D-01 single-deploy commit posture; the local commits are granular for incremental rollback granularity, but the push is one event).

    POST-PUSH (UAT runbook per 20-UAT.md):
    3. Operator opens 20-UAT.md and executes Steps 1-5 in order against the live production Worker:
       - Step 1: seed live:test-uat-<sid> with stale last_activity_at via `wrangler kv key put --remote --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 "live:test-uat-<sid>" '<value>' --metadata '<metadata>'`
       - Step 2: edit wrangler.jsonc triggers.crons to `["* * * * *"]` + `wrangler deploy`; wait 90s; verify Cloudflare Dashboard Past Events + `wrangler tail` chat.delivery.tick log line
       - Step 3: verify Gmail Inbox arrival within 5 minutes; verify delivered:test-uat-<sid> JSON has dry_run: false + populated resend_message_id; verify live:test-uat-<sid> deleted; verify Workers Logs chat.delivery.sent log carries matching resend_message_id; verify Resend Dashboard shows the same id; verify body is plaintext (Show original -> Content-Type text/plain only)
       - Step 4: revert wrangler.jsonc triggers.crons to `["0 * * * *"]` + `wrangler deploy`; verify Cloudflare Dashboard cron revert
       - Step 5: backlog cleanup (`wrangler kv key delete --remote ... delivered:test-uat-<sid>`); verify zero test-uat-* keys remain

    4. Operator updates 20-UAT.md `result:` blocks for Steps 1-5 with actual evidence (sids, timestamps, Resend message IDs, Gmail screenshot references)

    5. Operator updates DEPLOY-GATE.md frontmatter: `status: confirmed`, `gate: CONFIRMED`, `confirmed: <today's date>`, `operator: Jack Cutrara`. Operator commits the metadata update to main directly OR via a follow-up commit (operator's choice).

    6. Operator replies in chat: `approved — deploy gate cleared` as the durable audit trail per Plan 17-08 precedent.

    STEP 6 ASYNC (organic real-traffic; 7-day soft cap):
    7. Within 7 days post-deploy, the first organic visitor session that ages >2h gets emailed. Operator records evidence in 20-UAT.md Step 6 result block (Resend message ID, arrival time, Gmail screenshot, Workers Logs JSON line).
    8. If no organic visitor arrives within 7 days: operator runs `node scripts/resend-warmup.mjs` once as the proxy verification (proves the wire still works post-deploy independent of cron timing); records the warmup message ID in Step 6 result.

    ROLLBACK PATH (per D-03):
    9. If anything breaks post-push (production chat.delivery.failed log spike; Gmail bounce; Resend dashboard error rate spike): operator edits wrangler.jsonc line 21 from `"DRY_RUN": "0"` back to `"1"` + runs `wrangler deploy`. ~60s recovery. The DRY_RUN=='1' branch is the runway. NO source revert needed.
  </how-to-verify>
  <resume-signal>
    Operator replies in chat with one of:
      - `approved — deploy gate cleared` (DEPLOY-GATE.md updated to status=confirmed; operator pushed; UAT Steps 1-5 GREEN; Step 6 will close asynchronously within 7 days)
      - `revert — <reason>` (rollback path executed; operator reverted wrangler.jsonc DRY_RUN to "1"; phase scope re-triaged)
      - `blocked — <reason>` (a checklist item failed pre-push; phase NOT pushed; specify which gate to unblock)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local main -> origin/main (operator's git push) | Production state changes when operator pushes; deploy-gate is the human-controlled checkpoint |
| Production wrangler.jsonc -> deployed Worker env | wrangler deploy applies the JSONC to the running Worker; cron flips + DRY_RUN values are live in seconds |
| Production KV (eaa30fef259e4a6b9505b41bbf3f8f01) -> live transcripts | Real visitor data lives here; UAT seeds test-uat-* keys to avoid contamination |
| Resend API send -> Jack's Gmail Inbox | The first real cross-system flow; Postmaster Tools enrolled Plan 17-06 monitors deliverability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Repudiation | Operator pushes without running pre-deploy checklist; later production breakage has no audit trail of what was verified | mitigate | DEPLOY-GATE.md status=pending at first commit; status=confirmed only after operator personally checks each item; chat-reply "approved — deploy gate cleared" is the durable audit trail. Plan 17-08 precedent: chat-reply preserved in conversation history |
| T-20-04-02 | Tampering | Operator runs UAT Step 2 cron flip but forgets Step 4 revert; production cron fires every minute -> 1440 ticks/day instead of 24 -> CPU/Resend rate-limit/log volume spike | mitigate | tests/build/wrangler-dry-run-shape.test.ts + tests/build/wrangler-cron-shape.test.ts both assert `triggers.crons === ["0 * * * *"]`. Next CI run on any commit catches the unreverted state. DEPLOY-GATE.md section 5 explicitly verifies cron value pre-push. Pitfall 6 inherited from Phase 19 |
| T-20-04-03 | Tampering | Operator pushes without flipping DRY_RUN to "0" (forgot Plan 20-03 substitution + flip is the live-mail trigger) | mitigate | tests/build/wrangler-dry-run-shape.test.ts Invariant 1 asserts DRY_RUN === "0". DEPLOY-GATE.md section 5 explicitly verifies. CI fails RED if reverted. Plan 20-03 substitution + flip ship in same commit per D-01 |
| T-20-04-04 | Information Disclosure | Operator emails actual visitor sid / content via Gmail screenshot (UAT result block) and the screenshot leaks via planning artifacts | accept | Test-uat-* sids are operator-controlled UUIDs (no PII); the seed transcript content for Steps 1-5 uses synthetic content. Step 6 organic real-traffic evidence may include real visitor content — operator's discretion to redact PII before recording in 20-UAT.md result block |
| T-20-04-05 | Denial of Service | Operator pushes during high-traffic window; Resend rate limit throttles live sends | accept | Resend default is 5 req/sec per team (RESEARCH § Drift item 2); portfolio site traffic at v1.3 scale is well under this threshold. Phase 19 PER_TICK_BATCH_CAP=50 + serialized loop already bounds per-tick fetch rate |
| T-20-04-06 | Repudiation | First organic visitor email lands in Spam (NOT Inbox) and Jack misses it -> Step 6 cannot close | mitigate | Plan 17-06 enrolled Postmaster Tools + warmed the domain (5/5 Inbox at warmup). DKIM/SPF/DMARC live. Step 6 7-day soft cap allows fallback to scripts/resend-warmup.mjs proxy if organic traffic doesn't materialize. If Spam classification persists, operator opens a ticket against the domain reputation (out-of-scope for Phase 20 closure) |
</threat_model>

<verification>
- 20-UAT.md exists with frontmatter status=pending + 6 numbered steps + intro + deviation paragraph
- DEPLOY-GATE.md exists with frontmatter status=pending + 5-section pre-deploy checklist + executor-MUST-NOT-push prohibition + rollback procedure
- All Phase 20 forward-defense build tests still GREEN (Plan 20-04 adds zero source changes)
- pnpm exec astro check exits 0/0/0 (Plan 20-04 touches no source)
- pnpm exec vitest run exits 0 with PASS >= 544 / FAIL 0 / SKIP 2 (Phase 20 close baseline preserved)
- git diff --stat package.json shows zero changes (MAIL-01 phase-wide lock holds)
- D-15 SSE snapshot GREEN; TEST-03 Anthropic GREEN; D-26 chat-surface battery preserved
- Task 2 checkpoint: operator UAT executed against post-push production; chat-reply "approved — deploy gate cleared" received; DEPLOY-GATE.md updated to status=confirmed
</verification>

<success_criteria>
- 2 NEW phase-end docs (20-UAT.md, DEPLOY-GATE.md) exist + committed
- 20-UAT.md encodes 6 numbered operator steps mapping 1:1 to ROADMAP success criteria 1-5 + Step 6 organic real-traffic 7-day soft cap closure
- DEPLOY-GATE.md mirrors Plan 17-08 template: frontmatter (status=pending at first commit) + 5-section pre-deploy checklist + executor-MUST-NOT-push prohibition + rollback procedure
- Plan is autonomous: false — Task 2 checkpoint pauses for operator UAT + approval + push
- After operator approval + push: Phase 20 is COMPLETE; v1.3 milestone (Phases 17-20) closes
- All cross-phase forward-defense gates GREEN at phase close: D-26 chat-surface battery preserved; D-15 SSE GREEN; TEST-03 Anthropic GREEN; package.json deps byte-identical (MAIL-01 lock); pnpm exec astro check 0/0/0; pnpm build clean
- Rollback runway preserved: DRY_RUN==='1' branch in src/lib/chat-delivery.ts intact (D-03); single-line wrangler.jsonc revert is the rollback mechanism per DEPLOY-GATE.md ROLLBACK PROCEDURE
</success_criteria>

<output>
After Task 1 completion, executor commits both files together as the FINAL Phase 20 metadata commit (per D-01 atomic-deploy posture; per D-04 deploy-gate posture).

After Task 2 operator approval + push: operator (or executor on follow-up commit) creates `.planning/phases/20-email-render-resend-integration/20-04-SUMMARY.md` documenting:
- Two new files created (20-UAT.md, DEPLOY-GATE.md) with paths + LOC
- Operator UAT outcome: which steps GREEN; Resend message IDs from Step 3 + Step 6; Gmail Inbox arrival timestamps; any deviation
- Final pnpm test PASS count (target >= 544 / 0 / 2)
- DEPLOY-GATE.md status transition: pending -> confirmed (with operator name + date)
- Phase 20 close announcement: ROADMAP success criteria 1-5 GREEN; v1.3 milestone (Phases 17-20) COMPLETE

After Phase 20 close + v1.3 milestone close, write `.planning/phases/20-email-render-resend-integration/20-RETROSPECTIVE.md` capturing milestone-wide learnings (per Phase 17/18/19 precedent).
</output>
