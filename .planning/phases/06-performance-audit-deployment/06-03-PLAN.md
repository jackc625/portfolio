---
phase: 06-performance-audit-deployment
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified: []
autonomous: false
requirements:
  - PERF-01
  - PERF-05
must_haves:
  truths:
    - "Site is live on jackcutrara.com over HTTPS with all Phase 6 optimizations deployed"
    - "CI/CD pipeline builds successfully on Cloudflare Pages after git push"
    - "Layout renders correctly at 375px (phone), 768px (tablet), and 1440px (desktop) with no overflow, broken grids, or truncated text"
    - "CLS is visually zero -- no visible layout shifts during page load at any breakpoint"
    - "All pages load and render correctly on the live site"
  artifacts:
    - path: "https://jackcutrara.com"
      provides: "Live production site"
      contains: "Jack Cutrara"
  key_links:
    - from: "git push to main"
      to: "Cloudflare Pages build"
      via: "CI/CD webhook"
      pattern: "push-to-deploy"
    - from: "jackcutrara.com"
      to: "Cloudflare Pages"
      via: "Custom domain DNS"
      pattern: "HTTPS"
---

<objective>
Deploy all Phase 6 optimizations to production, verify the live site on jackcutrara.com over HTTPS, and confirm responsive layout at all target breakpoints.

Purpose: This is the final gate before the site is production-ready. All optimizations from Plans 01-02 are pushed to Cloudflare Pages, the CI/CD pipeline is confirmed working, and responsive design is visually verified at 375px (phone), 768px (tablet), and 1440px (desktop) per D-11 and D-12. The human checkpoint confirms everything looks correct on the live site.

Output: Live site on jackcutrara.com with all optimizations, verified responsive at all breakpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-audit-deployment/06-CONTEXT.md
@.planning/phases/06-performance-audit-deployment/06-01-SUMMARY.md
@.planning/phases/06-performance-audit-deployment/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to production and verify CI/CD deployment</name>
  <files>No files modified -- git push and live site verification task</files>
  <read_first>
    - .planning/phases/06-performance-audit-deployment/06-02-SUMMARY.md (confirm all audits passed before deploying)
    - .planning/phases/06-performance-audit-deployment/06-CONTEXT.md (D-11: full deploy verification)
  </read_first>
  <action>
**1. Ensure all Phase 6 changes are committed:**

Run `rtk git status` to check for any uncommitted changes from Plans 01-02. If there are uncommitted files, commit them with an appropriate message.

**2. Push to main branch:**

Run `rtk git push origin main` to trigger Cloudflare Pages CI/CD pipeline.

**3. Monitor deployment:**

Check Cloudflare Pages build status. The CI/CD pipeline should:
- Install dependencies with pnpm
- Run `pnpm build` (which includes `astro check && astro build`)
- Deploy the `dist/` directory to the CDN

If the build fails, read the error output and fix the issue.

**4. Verify live site:**

After deployment completes (typically 1-2 minutes):
- Verify `https://jackcutrara.com` loads correctly over HTTPS
- Verify `https://jackcutrara.com/about/` loads
- Verify `https://jackcutrara.com/projects/` loads
- Verify `https://jackcutrara.com/resume/` loads
- Verify `https://jackcutrara.com/contact/` loads
- Verify at least one case study: `https://jackcutrara.com/projects/project-alpha/`

Use `curl -sI https://jackcutrara.com` to check:
- HTTP status 200
- HTTPS is active (no redirect loops)
- Content-Type is text/html

**5. Verify no console errors:**

The deployment itself should be error-free. Any build issues would be caught by `pnpm build` which runs `astro check` first.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk git status && curl -sI https://jackcutrara.com 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `git status` shows clean working tree (all changes committed and pushed)
    - `curl -sI https://jackcutrara.com` returns HTTP status 200
    - `curl -sI https://jackcutrara.com` shows HTTPS (no redirect to HTTP)
    - `curl -sI https://jackcutrara.com/about/` returns HTTP status 200
    - Cloudflare Pages build completed successfully
  </acceptance_criteria>
  <done>All Phase 6 changes deployed to production. Site live at jackcutrara.com over HTTPS. CI/CD pipeline confirmed working (git push triggers build and deploy).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Responsive design verification at target breakpoints</name>
  <files>No files modified -- human visual verification task</files>
  <action>
Human verification of the complete Phase 6 delivery. The site now has:
- GSAP lazy-loaded via dynamic imports (no longer blocking LCP)
- Mobile canvas particles reduced from 400 to 200
- CSS pre-animation states with 3-second fallback
- prefers-reduced-motion fully disables all animations and canvas
- Lighthouse 90+ scores across all categories on all pages
- LCP confirmed under 2 seconds on all tested pages
- CLS confirmed under 0.1 on all tested pages
- Both themes verified for WCAG AA contrast compliance
- All pending Phase 2 requirements formally closed
- Build output verified (self-hosted fonts, no external CDN, code-split JS)
- Deployed to Cloudflare Pages at jackcutrara.com

**Responsive layout verification (D-12) -- check at these breakpoints:**

1. **Phone (375px):** Open https://jackcutrara.com in Chrome DevTools, set viewport to 375px width
   - Check: No horizontal overflow on any page
   - Check: Navigation hamburger menu works
   - Check: Text is readable (not too small or too large)
   - Check: Canvas hero renders (with reduced particles)
   - Check: Project cards stack vertically
   - Navigate to /about/, /projects/, /resume/, /contact/

2. **Tablet (768px):** Set viewport to 768px width
   - Check: Grid layouts transition correctly (may be 2-column)
   - Check: Header navigation visible (desktop nav or hamburger)
   - Check: Content has appropriate padding
   - Check: Project cards may show 2 per row

3. **Desktop (1440px):** Set viewport to 1440px width
   - Check: Content is contained within max-w-[90rem] container
   - Check: Full desktop navigation visible
   - Check: Asymmetric grid layouts (1fr/2fr) render correctly on About page
   - Check: Project cards show in multi-column grid

**Theme check:**
4. Toggle between dark and light themes at each breakpoint
   - Check: No broken layouts during/after theme switch
   - Check: Text remains readable in both themes

**Animation check:**
5. Scroll through the home page
   - Check: Sections fade in as you scroll (GSAP loaded)
   - Check: Canvas hero animates on desktop
   - Check: No visible content "jump" (GSAP loads before content area is reached)

**Performance feel:**
6. Navigate between pages
   - Check: Page transitions feel smooth
   - Check: Initial page load feels fast (no blocking)

Type "approved" to complete Phase 6, or describe any layout/visual issues found at specific breakpoints.
  </action>
  <verify>Human visual inspection at 375px, 768px, and 1440px breakpoints across all pages and both themes.</verify>
  <done>Human confirms responsive layout is correct at all breakpoints, both themes render properly, animations work after lazy-loading, and the live site at jackcutrara.com is production-ready. Phase 6 complete.</done>
</task>

</tasks>

<verification>
1. Site live at jackcutrara.com over HTTPS
2. CI/CD working (push triggers deploy)
3. Responsive layout verified at 375px, 768px, 1440px
4. Both themes work at all breakpoints
5. Animations load and play correctly after lazy-loading
6. No visible layout shifts during page load
</verification>

<success_criteria>
- jackcutrara.com returns HTTP 200 over HTTPS
- Human-verified responsive layout at phone, tablet, and desktop breakpoints
- No horizontal overflow, broken grids, or truncated text at any breakpoint
- Both themes render correctly at all breakpoints
- Animations load correctly (GSAP lazy-loading works in production)
- Phase 6 formally complete after human approval
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-audit-deployment/06-03-SUMMARY.md`
</output>
