---
phase: 01-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - .prettierrc.mjs
  - eslint.config.mjs
  - src/env.d.ts
  - src/pages/index.astro
autonomous: true
requirements:
  - DSGN-01
user_setup: []

must_haves:
  truths:
    - "pnpm dev starts Astro dev server on localhost with zero errors"
    - "pnpm build produces a dist/ directory with index.html"
    - "TypeScript strict mode is active and astro check passes"
    - "Prettier formats .astro files correctly"
    - "ESLint runs without config errors"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies and scripts"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro configuration with Tailwind v4 vite plugin, MDX, sitemap, fonts"
      contains: "tailwindcss"
    - path: "tsconfig.json"
      provides: "TypeScript strict config extending Astro preset"
      contains: "astro/tsconfigs/strict"
    - path: ".prettierrc.mjs"
      provides: "Prettier config with astro and tailwind plugins"
      contains: "prettier-plugin-astro"
    - path: "eslint.config.mjs"
      provides: "ESLint flat config with astro plugin"
      contains: "eslint-plugin-astro"
    - path: "src/pages/index.astro"
      provides: "Minimal index page for build validation"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite.plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "astro.config.mjs"
      to: "@astrojs/mdx"
      via: "integrations array"
      pattern: "mdx\\(\\)"
    - from: "tsconfig.json"
      to: "astro/tsconfigs/strict"
      via: "extends field"
      pattern: "astro/tsconfigs/strict"
---

<objective>
Scaffold the Astro 6 project from scratch with all dependencies installed and build tooling configured.

Purpose: Create the foundation that every other plan in this phase (and every subsequent phase) builds on. Without a working Astro project with Tailwind v4 and TypeScript, nothing else can proceed.
Output: A buildable, lintable, formattable Astro 6 project with a blank index page that starts and builds cleanly.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 6 project and install all dependencies</name>
  <files>package.json</files>
  <read_first>
    .planning/phases/01-foundation-design-system/01-RESEARCH.md (Installation section, Standard Stack tables, Anti-Patterns)
    CLAUDE.md (Technology Stack section -- version constraints)
  </read_first>
  <action>
    Create the Astro 6 project using pnpm. Run:

    ```bash
    rtk pnpm create astro@latest . -- --template minimal --install --no-git --typescript strict
    ```

    If the project directory is not empty (has CLAUDE.md, PRD.md, .planning/), the scaffolder may warn. Accept overwrite for conflicting files or scaffold in a temp dir and move files.

    After scaffolding, install additional dependencies:

    ```bash
    # Tailwind CSS v4 via Vite plugin (NOT @astrojs/tailwind which is deprecated)
    rtk pnpm add tailwindcss @tailwindcss/vite

    # Content and SEO integrations
    rtk pnpm astro add mdx sitemap
    rtk pnpm add astro-seo

    # Dev tools
    rtk pnpm add -D prettier prettier-plugin-astro prettier-plugin-tailwindcss eslint eslint-plugin-astro @astrojs/check typescript

    # Test framework for validation tasks
    rtk pnpm add -D vitest
    ```

    Update package.json scripts to include:
    ```json
    {
      "scripts": {
        "dev": "astro dev",
        "build": "astro check && astro build",
        "preview": "astro preview",
        "check": "astro check",
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "lint": "eslint .",
        "test": "vitest run"
      }
    }
    ```

    Add engine constraint:
    ```json
    {
      "engines": {
        "node": ">=22"
      }
    }
    ```

    CRITICAL: Do NOT install `@astrojs/tailwind` (deprecated v3 integration). Do NOT install `zod` separately (bundled with Astro 6). Do NOT install `@astrojs/cloudflare` (not needed for static sites). Do NOT install GSAP (Phase 5).
  </action>
  <verify>
    <automated>cd "C:/Users/jackc/Code/portfolio" && rtk pnpm list astro tailwindcss @tailwindcss/vite @astrojs/mdx @astrojs/sitemap prettier eslint vitest 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"astro"` in dependencies
    - package.json contains `"tailwindcss"` and `"@tailwindcss/vite"` in dependencies
    - package.json contains `"@astrojs/mdx"` in dependencies
    - package.json contains `"@astrojs/sitemap"` in dependencies
    - package.json contains `"astro-seo"` in dependencies
    - package.json contains `"prettier"`, `"prettier-plugin-astro"`, `"prettier-plugin-tailwindcss"` in devDependencies
    - package.json contains `"eslint"` and `"eslint-plugin-astro"` in devDependencies
    - package.json contains `"vitest"` in devDependencies
    - package.json contains `"@astrojs/check"` in devDependencies
    - package.json scripts include `"build": "astro check && astro build"`
    - package.json scripts include `"dev": "astro dev"`
    - package.json scripts include `"check": "astro check"`
    - package.json scripts include `"test": "vitest run"`
    - package.json contains `"engines": { "node": ">=22" }`
    - package.json does NOT contain `@astrojs/tailwind`
    - package.json does NOT contain `@astrojs/cloudflare`
    - node_modules/ exists and pnpm install succeeded
  </acceptance_criteria>
  <done>All dependencies installed, package.json has correct scripts and engine constraint, no deprecated packages present</done>
</task>

<task type="auto">
  <name>Task 2: Configure Astro, TypeScript, Prettier, and ESLint</name>
  <files>astro.config.mjs, tsconfig.json, .prettierrc.mjs, eslint.config.mjs, src/pages/index.astro, src/env.d.ts</files>
  <read_first>
    package.json (to confirm installed dependencies)
    .planning/phases/01-foundation-design-system/01-RESEARCH.md (Pattern 4: Astro 6 Fonts API, Complete astro.config.mjs, Complete tsconfig.json, Prettier Configuration, ESLint Flat Config)
    .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Font Families table -- Instrument Serif, Instrument Sans, JetBrains Mono with specific weights and CSS variables)
  </read_first>
  <action>
    Create or update these configuration files:

    **astro.config.mjs** -- Complete configuration with Tailwind v4 Vite plugin, MDX, sitemap, and Astro Fonts API:
    ```javascript
    // @ts-check
    import { defineConfig, fontProviders } from "astro/config";
    import tailwindcss from "@tailwindcss/vite";
    import mdx from "@astrojs/mdx";
    import sitemap from "@astrojs/sitemap";

    export default defineConfig({
      site: "https://jackcutrara.com",
      integrations: [mdx(), sitemap()],
      vite: {
        plugins: [tailwindcss()],
      },
      fonts: [
        {
          provider: fontProviders.google(),
          name: "Instrument Serif",
          cssVariable: "--font-heading",
          weights: [400],
          styles: ["normal", "italic"],
        },
        {
          provider: fontProviders.google(),
          name: "Instrument Sans",
          cssVariable: "--font-sans",
          weights: [400, 600],
          styles: ["normal", "italic"],
        },
        {
          provider: fontProviders.google(),
          name: "JetBrains Mono",
          cssVariable: "--font-code",
          weights: [400],
          styles: ["normal"],
        },
      ],
    });
    ```

    **tsconfig.json** -- Strict TypeScript extending Astro preset:
    ```json
    {
      "extends": "astro/tsconfigs/strict",
      "include": [".astro/types.d.ts", "**/*"],
      "exclude": ["dist"]
    }
    ```

    **src/env.d.ts** -- Astro type reference (if not created by scaffolder):
    ```typescript
    /// <reference types="astro/client" />
    ```

    **.prettierrc.mjs** -- Prettier with Astro and Tailwind plugins:
    ```javascript
    /** @type {import("prettier").Config} */
    export default {
      semi: true,
      singleQuote: false,
      tabWidth: 2,
      trailingComma: "all",
      plugins: [
        "prettier-plugin-astro",
        "prettier-plugin-tailwindcss",
      ],
      overrides: [
        {
          files: "*.astro",
          options: {
            parser: "astro",
          },
        },
      ],
    };
    ```

    **eslint.config.mjs** -- ESLint flat config with Astro plugin:
    ```javascript
    import eslintPluginAstro from "eslint-plugin-astro";

    export default [
      ...eslintPluginAstro.configs.recommended,
      {
        rules: {},
      },
    ];
    ```

    **src/pages/index.astro** -- Minimal blank page for build validation (BaseLayout will be added in Plan 02):
    ```astro
    ---
    ---
    <html lang="en">
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Jack Cutrara</title>
      </head>
      <body>
      </body>
    </html>
    ```

    CRITICAL: Do NOT create a `tailwind.config.js` file. Tailwind v4 uses CSS-first config via `@theme` directives (created in Plan 02). The `@tailwindcss/vite` plugin in astro.config.mjs is the only Tailwind configuration needed at the framework level.

    CRITICAL: Font configuration uses exact values from UI-SPEC.md:
    - Instrument Serif: weight 400, styles normal + italic, variable --font-heading
    - Instrument Sans: weights 400 + 600, styles normal + italic, variable --font-sans
    - JetBrains Mono: weight 400, style normal, variable --font-code
  </action>
  <verify>
    <automated>cd "C:/Users/jackc/Code/portfolio" && rtk pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - astro.config.mjs contains `import tailwindcss from "@tailwindcss/vite"`
    - astro.config.mjs contains `plugins: [tailwindcss()]` inside vite config
    - astro.config.mjs contains `integrations: [mdx(), sitemap()]`
    - astro.config.mjs contains `site: "https://jackcutrara.com"`
    - astro.config.mjs contains `fontProviders.google()` for all 3 fonts
    - astro.config.mjs contains `name: "Instrument Serif"` with `cssVariable: "--font-heading"`
    - astro.config.mjs contains `name: "Instrument Sans"` with `cssVariable: "--font-sans"`
    - astro.config.mjs contains `name: "JetBrains Mono"` with `cssVariable: "--font-code"`
    - tsconfig.json contains `"extends": "astro/tsconfigs/strict"`
    - .prettierrc.mjs contains `"prettier-plugin-astro"` and `"prettier-plugin-tailwindcss"`
    - eslint.config.mjs contains `eslintPluginAstro.configs.recommended`
    - src/pages/index.astro exists with `<html lang="en">`
    - No `tailwind.config.js` file exists anywhere in the project
    - `pnpm run build` exits with code 0 and produces dist/index.html
  </acceptance_criteria>
  <done>All config files created, build succeeds producing dist/index.html, no tailwind.config.js exists, fonts configured per UI-SPEC</done>
</task>

</tasks>

<verification>
- `pnpm run build` exits 0 and produces `dist/index.html`
- `pnpm run check` (astro check) exits 0
- `pnpm run format:check` runs without config errors
- `pnpm run lint` runs without config errors
- No `tailwind.config.js` file exists
- No `@astrojs/tailwind` in node_modules
</verification>

<success_criteria>
1. Astro 6 dev server starts on localhost without errors
2. `pnpm run build` produces dist/index.html
3. TypeScript strict mode active via astro/tsconfigs/strict
4. Prettier formats .astro files (prettier-plugin-astro configured)
5. ESLint runs with astro rules (eslint-plugin-astro configured)
6. All three fonts (Instrument Serif, Instrument Sans, JetBrains Mono) configured in Astro Fonts API
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
