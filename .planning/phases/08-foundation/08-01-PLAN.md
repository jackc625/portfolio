---
phase: 08-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/MASTER.md
autonomous: true
requirements: [DSGN-05]
requirements_addressed: [DSGN-05]

must_haves:
  truths:
    - "design-system/MASTER.md exists at repo root and is 400+ lines (~8-10 pages at ~50 lines/page, matching the comprehensive scope per D-01)"
    - "The file documents the six hex tokens, the type ramp, layout grid, motion line, accent rules, anti-patterns, and Phase 9 primitive specs"
    - "A reader browsing the repo on GitHub can open design-system/MASTER.md and use it as the single source of truth for the design system"
    - "Phase 9 can implement Header, Footer, Container, SectionHeader, WorkRow, MetaLabel, StatusDot directly from the spec without re-reading mockup.html"
  artifacts:
    - path: "design-system/MASTER.md"
      provides: "The locked design contract for milestone v1.1 (tokens, typography, layout, components, motion, accent usage, anti-patterns)"
      min_lines: 400
      contains: "## Tokens"
  key_links:
    - from: "design-system/MASTER.md"
      to: "mockup.html lines 14-25 (token table)"
      via: "transcribed hex values #FAFAF7 / #0A0A0A / #52525B / #A1A1AA / #E4E4E7 / #E63946"
      pattern: "#FAFAF7"
    - from: "design-system/MASTER.md"
      to: "Phase 9 primitive specs"
      via: "Components section naming Header, Footer, Container, SectionHeader, WorkRow, MetaLabel, StatusDot"
      pattern: "WorkRow"
---

<objective>
Author `design-system/MASTER.md` as the comprehensive, locked design contract for milestone v1.1. This is Phase 8's task 1 per D-25 — no code changes happen in any later plan until this contract is captured. The file becomes the single source of truth Phase 9 implements primitives against and Phase 10 consumes when porting pages.

Purpose: Lock the design vocabulary in one authoritative file so downstream phases do not re-interpret mockup.html or drift from the locked visual system. Once this plan lands, mockup.html becomes a reference artifact only — MASTER.md is the spec.

Output: A single markdown file at `design-system/MASTER.md` (repo root, alongside `src/` and `public/`) containing all sections enumerated in D-01 through D-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-foundation/08-CONTEXT.md
@.planning/phases/08-foundation/08-RESEARCH.md
@mockup.html
@CLAUDE.md

<interfaces>
<!-- The Phase 9 primitive specs MASTER.md must document. Extracted from ROADMAP Phase 9 success criteria. -->
<!-- MASTER.md must include a Components section that names, sizes, and specs each primitive. -->

Phase 9 primitives MASTER.md must specify (names + contracts):
- Header.astro — sticky 72px-tall, JACK CUTRARA mono wordmark (0.875rem, weight 500, letter-spacing 0.12em), 3-link nav (works / about / contact), active link uses accent-red underline (text-decoration-thickness 1.5px, underline-offset 6px)
- Footer.astro — 64px-tall, copyright on left, "BUILT WITH ASTRO · TAILWIND · GEIST" mono caption on right, 96px top margin, border-top 1px solid --rule, --ink-faint colors
- Container.astro — max-width 1200px, padding-left/right: 48px (desktop), 32px (tablet <1024px), 24px (mobile <768px), margin 0 auto
- SectionHeader.astro — mono "§ NN — TITLE" label + optional count; flex justify-between, baseline-aligned, followed by 1px --rule section-rule with 16px top / 24px bottom margin
- WorkRow.astro — grid-template-columns: 56px 1fr auto, 24px gap, 28px 0 padding, border-bottom 1px solid --rule (hidden on :last-child), hover reveals accent arrow with 120ms opacity transition and adds accent-red text-decoration underline to title
- MetaLabel.astro — uppercase Geist Mono caption, 0.75rem weight 500, letter-spacing 0.12em, --ink-muted color
- StatusDot.astro — 6px circle, --accent color, 8px right margin, paired with MetaLabel text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author design-system/MASTER.md as the locked design contract</name>
  <files>design-system/MASTER.md</files>
  <read_first>
    - mockup.html (lines 14-25 for tokens, 55-99 for type ramp, 103-143 for header, 147-184 for hero, 186-310 for work/about/contact sections, 313-326 for footer, 328-348 for responsive)
    - .planning/phases/08-foundation/08-CONTEXT.md (D-01 through D-06 for MASTER.md requirements, D-13 for pragmatic motion line)
    - .planning/ROADMAP.md (Phase 9 section for primitive names and success criteria)
  </read_first>
  <action>
Create a new file at `design-system/MASTER.md` (at repo root, not under `.planning/` or `docs/`). The file must be 300+ lines and contain every section below in order. Copy actual values from mockup.html into the file — do not write "see mockup.html".

Required sections, each with a level-2 heading:

## 1. Overview
A 2-3 paragraph intro explaining that this file is the locked source of truth for the editorial redesign (milestone v1.1), that Phases 9-11 consume it without editing it (per D-03), and that mockup.html at repo root is the visual reference it transcribes from.

## 2. Tokens
Transcribe the exact six color tokens + four layout tokens from mockup.html lines 14-25 as a markdown table:

| Token | Value | Usage |
|-------|-------|-------|
| `--bg` | `#FAFAF7` | Page background (warm off-white) |
| `--ink` | `#0A0A0A` | Primary text (near-black) |
| `--ink-muted` | `#52525B` | Secondary text (grey) |
| `--ink-faint` | `#A1A1AA` | Tertiary text (light grey) |
| `--rule` | `#E4E4E7` | Dividers, borders, subtle surface tints |
| `--accent` | `#E63946` | Signal red — interactive + attention signal only |
| `--container-max` | `1200px` | Content max-width |
| `--pad-desktop` | `48px` | Horizontal page padding at ≥1024px |
| `--pad-tablet` | `32px` | Horizontal page padding at <1024px |
| `--pad-mobile` | `24px` | Horizontal page padding at <768px |

Immediately after the table add a callout: "These six hex values are the ENTIRE color palette. No `oklch()`, no dark-theme overrides, no gradients, no additional semantic tokens. If a new color is needed in Phase 9-11, it must be added to this table first."

## 3. Typography
Document the type ramp from mockup.html lines 55-99. Include every role class as a subsection with the font family, weight, size (including clamp), line-height, letter-spacing, and usage note. Transcribe exact values:

- `.display` — Geist 700, `clamp(4rem, 9vw, 8rem)`, line-height 0.92, letter-spacing -0.035em — homepage hero wordmark only
- `.h1-section` — Geist 600, `clamp(2.5rem, 5vw, 3.5rem)`, line-height 1.05, letter-spacing -0.02em — page-level section titles
- `.h2-project` — Geist 500, 1.75rem, line-height 1.2, letter-spacing -0.01em — project titles in work list
- `.lead` — Geist 400, `clamp(1.25rem, 2vw, 1.625rem)`, line-height 1.4 — homepage lead role statement, about intro line
- `.body` — Geist 400, 1.125rem, line-height 1.6, max-width 68ch — long-form paragraphs
- `.label-mono` — Geist Mono 500, 0.75rem, uppercase, letter-spacing 0.12em — section labels, nav links, wordmark
- `.meta-mono` — Geist Mono 400, 0.8125rem, letter-spacing 0.02em — metadata (dates, locations, work stack)

Note: fluid `clamp()` sizing lives INSIDE the role classes, not as CSS custom properties (per D-08).

## 4. Layout
- Container: max-width 1200px via `--container-max`, horizontal padding tiers via `--pad-desktop`/`--pad-tablet`/`--pad-mobile`
- Section rhythm: 160px top margin on desktop, 96px at <1024px, 72px at <768px (transcribed from mockup.html lines 188, 337, 343)
- Section header pattern: mono `§ NN — TITLE` label on left, count on right, followed by 1px `--rule` divider with 16px top / 24px bottom margin
- Grid: homepage hero uses 12-column grid with 24px gap, hero-content spans cols 1/8, hero-meta spans cols 9/12

## 5. Components (Phase 9 primitive specs — spec-first per D-04)
For each Phase 9 primitive, write a subsection with:
- **Name** (e.g., `Header.astro`)
- **Purpose** (1 sentence)
- **Props** (TypeScript-style signature or bullet list)
- **Sizing** (height, padding, grid)
- **Colors** (which tokens)
- **Typography** (which role class)
- **Hover/focus states** (from mockup.html)
- **Mobile behavior**
- **HTML sketch** (a minimal ASCII or HTML block showing the structure)

Primitives to document (copy the Phase 9 contract from the `<interfaces>` context block above):
1. `Header.astro` — sticky 72px, mono wordmark, 3-link nav, accent underline for active
2. `Footer.astro` — 64px, copy left, BUILT WITH mono caption right
3. `Container.astro` — responsive horizontal padding wrapper
4. `SectionHeader.astro` — `§ NN — TITLE` + count + rule
5. `WorkRow.astro` — 56px number column / title column / meta column grid row with hover underline + arrow reveal
6. `MetaLabel.astro` — uppercase mono caption
7. `StatusDot.astro` — 6px accent circle + mono label pair

Also document in a "Chat Widget Token Map" subsection the chat widget token mapping table from D-19 + D-27 + D-28 (see section 9).

## 6. Motion
Document the **pragmatic motion line** (D-13) explicitly so Phase 9 does not second-guess and strip surviving Tailwind state transitions. State:

**Dead:** GSAP, scroll-trigger animations, canvas hero, view transitions (`<ClientRouter />`, `::view-transition-*` keyframes), `[data-animate]` machinery, dark-theme `.theme-transitioning` rules, chat bubble GSAP pulse/scale-in/typing-dot bounce (chat.ts GSAP removal — see D-27).

**Allowed to survive (functional hover/focus state only):**
- Tailwind `transition-colors` utility classes (nav link color hover, etc.)
- `.nav-link-hover` underline animation (global.css, kept)
- Footer icon hover `hover:-translate-y-0.5` translate
- `.chat-copy-btn` opacity transition (global.css, kept)
- All CSS `transition` on state change (hover, focus, active) — kept
- `prefers-reduced-motion: reduce` is respected (trivially, since most motion is already gone)

## 7. Accent usage
Rules for when `--accent` (`#E63946`) is allowed. Transcribe from mockup.html behavior:
- Signal use only: active nav link underline (1.5px, offset 6px), trailing dot on homepage wordmark, status dot, work row hover underline + arrow reveal, email hover underline, contact link hover color, chat bubble background, chat focus ring
- **Never** use accent for: large surfaces, body text, icon fills for non-interactive elements, decorative flourishes, gradients

## 8. Anti-patterns
A bulleted list of things Phase 9-11 must not reintroduce, each with a 1-sentence rationale:
- No `oklch()` values — locked to flat hex per DSGN-02
- No theme toggle — single light theme per DSGN-03
- No `localStorage.theme` reads/writes — dead per DSGN-03
- No GSAP import anywhere — `npm uninstall gsap` is permanent
- No `<ClientRouter />` — view transitions removed (Phase 10 may re-evaluate for chat cross-page persistence but must reconcile with CHAT-01)
- No card layouts or shadows for project/work listings — editorial numbered rows only
- No canvas hero / WebGL / Three.js
- No skill icons, progress bars, or narrative graphics on About
- No contact form — direct links only
- No Inter or IBM Plex Mono references — Geist/Geist Mono only
- No `--token-*` CSS variable names — use `--bg`/`--ink`/`--ink-muted`/`--ink-faint`/`--rule`/`--accent`

## 9. Chat Widget Token Map
Transcribe the D-19 + D-27 + D-28 chat token mapping table verbatim so Phase 10's CHAT-02 restyle inherits a clean target:

| Old v1.0 token | New editorial token | Notes |
|----------------|---------------------|-------|
| `--token-bg-primary` | `--bg` | warm off-white |
| `--token-bg-secondary` | `--rule` | divider tint #E4E4E7 |
| `--token-text-primary` | `--ink` | black |
| `--token-text-secondary` | `--ink-muted` | grey |
| `--token-text-muted` | `--ink-faint` | light grey |
| `--token-accent` | `--accent` | signal red |
| `--token-accent-hover` | `--accent` | no separate hover variant |
| `--token-border` | `--rule` | #E4E4E7 |
| `--token-border-hover` | `--ink-muted` | stronger border on hover |
| `--token-success` | `--accent` | copy-success signal-positive |
| `--token-destructive` | `--accent` | 500-char limit (D-28) |
| `--token-warning` | `--accent` | 450-char limit (D-28) |

Note: Phase 8 only refactors chat token references. CHAT-02 (full visual restyle — no cards, no shadows, no gradients, monochrome surfaces) is Phase 10 scope. Chat loses its GSAP-driven motion in Phase 8 (D-27) — bubble pulse, panel scale-in, typing-dot bounce are gone until Phase 10 optionally restores CSS keyframe equivalents.

## 10. Changelog
Single line: "2026-04-07 — v1.1 initial lock (Phase 8)". This section exists so future edits (Phase 10 CHAT-02 restyle spec, etc.) are timestamped.

Writing rules:
- Use markdown tables, not prose, for token and type values
- Copy exact hex values from mockup.html — no "approximately" or "roughly"
- Every primitive in section 5 must have all 8 fields (Name, Purpose, Props, Sizing, Colors, Typography, States, Mobile)
- Target 400+ lines (comprehensive, not minimal per D-01; ~8-10 pages at ~50 lines/page)
- Do not edit mockup.html — only transcribe from it
  </action>
  <verify>
    <automated>test -f design-system/MASTER.md &amp;&amp; wc -l design-system/MASTER.md | awk '{exit ($1 &gt;= 400) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f design-system/MASTER.md` exits 0
    - `wc -l design-system/MASTER.md` reports ≥ 400 lines
    - `grep -c "#FAFAF7" design-system/MASTER.md` returns ≥ 1
    - `grep -c "#E63946" design-system/MASTER.md` returns ≥ 1
    - `grep -c "#0A0A0A" design-system/MASTER.md` returns ≥ 1
    - `grep -c "#52525B" design-system/MASTER.md` returns ≥ 1
    - `grep -c "#A1A1AA" design-system/MASTER.md` returns ≥ 1
    - `grep -c "#E4E4E7" design-system/MASTER.md` returns ≥ 1
    - `grep -c "Geist Mono" design-system/MASTER.md` returns ≥ 2
    - `grep -E "^## [0-9]+\." design-system/MASTER.md | wc -l` returns ≥ 10 (ten numbered top-level sections)
    - `grep -c "WorkRow" design-system/MASTER.md` returns ≥ 1
    - `grep -c "SectionHeader" design-system/MASTER.md` returns ≥ 1
    - `grep -c "Container" design-system/MASTER.md` returns ≥ 1
    - `grep -c "StatusDot" design-system/MASTER.md` returns ≥ 1
    - `grep -c "MetaLabel" design-system/MASTER.md` returns ≥ 1
    - `grep -c "Header" design-system/MASTER.md` returns ≥ 1
    - `grep -c "Footer" design-system/MASTER.md` returns ≥ 1
    - `grep -c "pragmatic motion" design-system/MASTER.md` returns ≥ 1
    - `grep -c "Anti-patterns\\|anti-pattern" design-system/MASTER.md` returns ≥ 1
    - `grep -c "ClientRouter" design-system/MASTER.md` returns ≥ 1 (documented as forbidden)
    - `grep -c "token-destructive\\|token-warning" design-system/MASTER.md` returns ≥ 1 (D-28 mapping)
  </acceptance_criteria>
  <done>
    design-system/MASTER.md exists at repo root with ≥ 400 lines, all 10 top-level sections present, all six hex tokens transcribed, all seven Phase 9 primitives specified, pragmatic motion line explicitly documented, anti-patterns listed, and the chat widget token map included. No code changes in any other file.
  </done>
</task>

</tasks>

<verification>
- `test -f design-system/MASTER.md`
- `wc -l design-system/MASTER.md` returns ≥ 400
- All 10 sections (`## 1.` through `## 10.`) present
- All six hex tokens transcribed verbatim
- All seven Phase 9 primitives have specs
- Pragmatic motion line documented
- Chat widget token map documented
- No other files modified (git status shows only `design-system/MASTER.md` as new)
</verification>

<success_criteria>
design-system/MASTER.md exists at repo root as the locked Phase 8 design contract that Phase 9 can implement primitives from and Phase 10 can port pages against, without referring back to mockup.html. DSGN-05 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation/08-01-SUMMARY.md` capturing:
- File created: `design-system/MASTER.md` (line count, section count)
- Any sections that were elaborated beyond the minimum spec
- Any ambiguities in mockup.html that required a judgment call (document the call made)
</output>
