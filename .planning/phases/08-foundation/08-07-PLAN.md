---
phase: 08-foundation
plan: 07
type: execute
wave: 5
depends_on: [04, 05, 06]
files_modified:
  - src/pages/resume.astro
  - public/resume.pdf
  - public/jack-cutrara-resume.pdf
autonomous: true
requirements: [CONTACT-03]
requirements_addressed: [CONTACT-03]

must_haves:
  truths:
    - "src/pages/resume.astro no longer exists"
    - "Visiting /resume at build time returns 404 (no static HTML generated for the route)"
    - "public/jack-cutrara-resume.pdf exists at the new filename"
    - "public/resume.pdf no longer exists"
    - "The renamed PDF is the same file (byte-for-byte) as the original resume.pdf"
  artifacts:
    - path: "public/jack-cutrara-resume.pdf"
      provides: "The canonical resume PDF at its new filename (CONTACT-02 ready)"
  key_links:
    - from: "public/jack-cutrara-resume.pdf"
      to: "Phase 10 CONTACT-02 <a download> link"
      via: "file exists at the expected URL path /jack-cutrara-resume.pdf"
      pattern: "jack-cutrara-resume.pdf"
---

<objective>
Delete the standalone `/resume` route and rename the public résumé PDF from `resume.pdf` to `jack-cutrara-resume.pdf`. After this plan, the standalone `/resume` page is 404 (per CONTACT-03) and the PDF file lives at its new canonical path ready for Phase 10's CONTACT-02 download link.

Purpose: Per D-23, D-24, and the CONTACT-03 requirement. The résumé is now a single hosted PDF, not a standalone page. The rename decouples Phase 8's deletion of the page from Phase 10's wire-up of the new contact section download link.

Output: One fewer page file, one PDF renamed in place. The file rename is a `git mv` so git history follows.

**Reconciliation note for Phase 10:** D-24 explicitly notes CONTACT-02's "placeholder PDF" wording in REQUIREMENTS.md conflicts with the fact that this is the real résumé being renamed, not a placeholder. This is a deferred concern — Phase 10 planning will either update the REQUIREMENTS.md text or acknowledge CONTACT-02 was partially satisfied in Phase 8. Plan 08's summary must mention this for downstream tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-foundation/08-CONTEXT.md
@.planning/phases/08-foundation/08-RESEARCH.md
@.planning/phases/08-foundation/08-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/pages/resume.astro and rename public/resume.pdf to jack-cutrara-resume.pdf</name>
  <files>src/pages/resume.astro, public/resume.pdf, public/jack-cutrara-resume.pdf</files>
  <read_first>
    - .planning/phases/08-foundation/08-CONTEXT.md D-23, D-24
    - .planning/phases/08-foundation/08-RESEARCH.md section 5 (/resume references map — confirms Header.astro and MobileMenu.astro navLinks are the only code refs, both cleaned in Plan 04)
  </read_first>
  <action>
**Edit A — Delete src/pages/resume.astro**

Delete the file entirely. Use `rm src/pages/resume.astro` via Bash or the file-delete tool.

Before deleting, verify no other file references the `/resume` ROUTE (as opposed to the `.pdf` file). Run `grep -rn '"/resume"' src/` — expected hits are zero since Plan 04 cleaned up both navLinks arrays in Header.astro and MobileMenu.astro. If this grep returns a hit, STOP and investigate.

Note: `grep -rn 'resume' src/` will still show hits from:
- Print stylesheet comment in global.css (currently line 238) — harmless comment, leave alone (Phase 9/10 can prune)
- The soon-to-be-renamed PDF filename if any inline href exists — should be none after Plans 04/06

**Edit B — Rename public/resume.pdf → public/jack-cutrara-resume.pdf**

Use `git mv public/resume.pdf public/jack-cutrara-resume.pdf` to preserve git history. If `git mv` fails (file not tracked or in a weird state), fall back to:
1. `mv public/resume.pdf public/jack-cutrara-resume.pdf` (plain move)
2. Stage both the deletion of `public/resume.pdf` and the creation of `public/jack-cutrara-resume.pdf` with `git add -A public/`

Verify the new file exists and is the same size as the original:
- `test -f public/jack-cutrara-resume.pdf` exits 0
- `test ! -f public/resume.pdf` exits 0

**Edit C — Check public/robots.txt for stale /resume references**

Per RESEARCH section 5, public/robots.txt was not grepped for /resume references. Run `grep -c 'resume' public/robots.txt` (or `test -f public/robots.txt && grep -c 'resume' public/robots.txt || echo 0`). If any hits exist and reference the `/resume` route (not the PDF), remove those lines. If hits only reference the PDF path, update them to `/jack-cutrara-resume.pdf`. This is a defensive check — the expected result is zero hits.

**Sitemap note:** `@astrojs/sitemap` auto-regenerates the sitemap from actual static routes at build time. Deleting `src/pages/resume.astro` automatically removes `/resume` from the next sitemap build. No manual sitemap edit needed.

**Commit notes:** All three edits (A, B, C) can land in a single commit: "feat(phase-8): remove /resume route and rename resume.pdf to jack-cutrara-resume.pdf". Git should show one deletion (src/pages/resume.astro), one rename (public/resume.pdf → public/jack-cutrara-resume.pdf), and potentially one edit (public/robots.txt) if applicable.
  </action>
  <verify>
    <automated>test ! -f src/pages/resume.astro &amp;&amp; test -f public/jack-cutrara-resume.pdf &amp;&amp; test ! -f public/resume.pdf</automated>
  </verify>
  <acceptance_criteria>
    - `test -e src/pages/resume.astro` exits 1 (deleted)
    - `test -f public/jack-cutrara-resume.pdf` exits 0 (new file exists)
    - `test -e public/resume.pdf` exits 1 (old file gone)
    - `grep -rn '"/resume"' src/` returns 0 matches
    - `grep -rn '"/resume"' public/` returns 0 matches (or only historical comments in robots.txt if any)
    - `grep -c 'resume' public/robots.txt` returns 0 OR only references `/jack-cutrara-resume.pdf`
    - `ls -la public/jack-cutrara-resume.pdf | awk '{print $5}'` returns a non-zero file size (the PDF is not empty)
    - git log shows the file move (or the delete + add pair) for public/resume.pdf → public/jack-cutrara-resume.pdf
  </acceptance_criteria>
  <done>
    src/pages/resume.astro is deleted, public/resume.pdf is renamed to public/jack-cutrara-resume.pdf (preserving byte content), robots.txt is clean, and no code references the /resume route. CONTACT-03 is structurally satisfied. Phase 10's CONTACT-02 download link has its target file already in place.
  </done>
</task>

</tasks>

<verification>
- `test ! -f src/pages/resume.astro` → success
- `test -f public/jack-cutrara-resume.pdf` → success
- `test ! -f public/resume.pdf` → success
- `grep -rn '"/resume"' src/ public/` returns 0
- The next `npm run build` will not generate a dist/resume/ directory
</verification>

<success_criteria>
CONTACT-03 is satisfied: the /resume route is removed, the résumé PDF lives at its new canonical path, and Phase 10 can wire CONTACT-02 against the new filename in the next phase.
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation/08-07-SUMMARY.md` capturing:
- Deletion: src/pages/resume.astro
- Rename: public/resume.pdf → public/jack-cutrara-resume.pdf (via git mv or add/delete pair)
- robots.txt audit result (changed / clean)
- **REQUIREMENTS.md reconciliation flag**: CONTACT-02's "placeholder PDF" wording should be reconciled in Phase 10 planning since the renamed file is the real résumé, not a placeholder (per D-24).
</output>
