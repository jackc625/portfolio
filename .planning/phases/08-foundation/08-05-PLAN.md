---
phase: 08-foundation
plan: 05
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/components/chat/ChatWidget.astro
  - src/styles/global.css
  - src/data/portfolio-context.json
autonomous: true
requirements: [DSGN-02, DSGN-04]
requirements_addressed: [DSGN-02, DSGN-04]

must_haves:
  truths:
    - "ChatWidget.astro no longer references any --token-* CSS custom property in its inline styles"
    - "ChatWidget.astro inline styles use --bg / --rule / --ink / --ink-muted / --ink-faint / --accent + flat size values"
    - "global.css .chat-* rule block uses the new token names exclusively (no --token-* or --font-code left)"
    - "src/data/portfolio-context.json no longer lists GSAP in siteStack, and any other v1.0 design references are updated or removed"
    - "The chat widget still renders visually (no build errors) but is cosmetically unchanged per D-19"
  artifacts:
    - path: "src/components/chat/ChatWidget.astro"
      provides: "Chat widget markup using new editorial token names"
    - path: "src/styles/global.css"
      provides: ".chat-* rules bound to new tokens"
    - path: "src/data/portfolio-context.json"
      provides: "Audited chat system-prompt context with no v1.0 design references"
  key_links:
    - from: "src/components/chat/ChatWidget.astro inline styles"
      to: "Plan 02 :root hex tokens"
      via: "var(--bg), var(--ink), var(--accent) etc."
      pattern: "var\\(--accent\\)"
    - from: "src/styles/global.css .chat-bot-message code"
      to: "Plan 02 --font-mono var"
      via: "font-family: var(--font-mono)"
      pattern: "font-family: var\\(--font-mono\\)"
---

<objective>
Refactor the chat widget's visual references to use the new editorial token names established in Plan 02. Phase 8 deliberately does NOT restyle the chat (CHAT-02 is Phase 10) — this plan only renames token references so the chat widget renders against the new palette with no dangling CSS variable references.

Also audit `src/data/portfolio-context.json` (the chat system-prompt context) and remove every reference to v1.0 design that no longer applies — at minimum `"GSAP"` in `siteStack`, but also any canvas hero, dark theme, project card grid, theme toggle, scroll animation, or IBM Plex Mono references in the skills/frameworks/descriptions.

Purpose: Complete the chat widget's side of the token cascade. After Plans 03 + 05, `grep -rn "token-" src/` touches zero files except possibly legacy print-stylesheet comments. Per D-19, D-20, D-22.

Output: ChatWidget.astro with new token names in all ~18 inline styles, global.css .chat-* block using new token names (~10 replacements), and portfolio-context.json audited for v1.0 references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-foundation/08-CONTEXT.md
@.planning/phases/08-foundation/08-RESEARCH.md
@.planning/phases/08-foundation/08-03-SUMMARY.md
@design-system/MASTER.md
@src/components/chat/ChatWidget.astro
@src/styles/global.css
@src/data/portfolio-context.json

<interfaces>
<!-- Chat token map (D-19 + D-27 + D-28) — the complete rename table -->

| Old token | New token | Notes |
|-----------|-----------|-------|
| `var(--token-bg-primary)` | `var(--bg)` | warm off-white |
| `var(--token-bg-secondary)` | `var(--rule)` | subtle surface tint |
| `var(--token-text-primary)` | `var(--ink)` | black |
| `var(--token-text-secondary)` | `var(--ink-muted)` | grey |
| `var(--token-text-muted)` | `var(--ink-faint)` | light grey |
| `var(--token-accent)` | `var(--accent)` | signal red |
| `var(--token-accent-hover)` | `var(--accent)` | no separate hover variant |
| `var(--token-border)` | `var(--rule)` | divider |
| `var(--token-border-hover)` | `var(--ink-muted)` | stronger hover border |
| `var(--token-success)` | `var(--accent)` | copy-success, signal-positive |
| `var(--token-destructive)` | `var(--accent)` | 500-char limit |
| `var(--token-warning)` | `var(--accent)` | 450-char limit |
| `var(--token-space-xs)` | `4px` | flatten per D-08 |
| `var(--token-space-sm)` | `8px` | |
| `var(--token-space-md)` | `16px` | |
| `var(--token-space-lg)` | `24px` | |
| `var(--token-text-sm)` | `0.875rem` | flatten per D-08 |
| `var(--token-text-base)` | `1rem` | |
| `var(--font-code)` | `var(--font-mono)` | D-12 font rename |
| `var(--font-heading)` | `var(--font-display)` | D-12 (unlikely to appear in chat) |
| `var(--font-sans)` | `var(--font-body)` | D-12 (unlikely — body was already --font-body in the @theme bridge) |

<!-- ChatWidget.astro touch list (from RESEARCH section 4) -->

Lines 14, 56, 63, 67, 87, 99, 108, 115, 124, 133, 142, 152, 163, 165, 173, 177, 184, 208 — all inline `style="..."` attributes with --token-* references. RESEARCH section 4 enumerates each.

<!-- global.css .chat-* surface (current lines 354-451) -->

- Line 370: `.typing-dot { background-color: var(--token-text-muted); }`
- Line 384: `.chat-textarea:focus { border-color: var(--token-border-hover) !important; box-shadow: 0 0 0 2px var(--token-accent); }`
- Line 398: `.chat-bot-message ul/ol { padding-left: var(--token-space-md); }`
- Line 401: `.chat-bot-message li { margin-bottom: var(--token-space-xs); }`
- Lines 404-408: `.chat-bot-message code { font-family: var(--font-code); font-size: var(--token-text-sm); background: var(--token-bg-secondary); }`
- Line 411: `.chat-bot-message a { color: var(--token-accent); }`
- Line 420-421: `.chat-starter-chip:hover { border-color: var(--token-border-hover); color: var(--token-accent); }`
- Line 426: `.hover-text-primary:hover { color: var(--token-text-primary); }`
- Line 438: `.copy-success svg { color: var(--token-success); }`

<!-- portfolio-context.json — known audit targets -->

From RESEARCH section 5: line 65 `siteStack` array contains `"GSAP"`. Additional audit targets (not individually enumerated in research — executor must read the whole file):
- Any `"canvas hero"` references
- Any `"dark theme"` or `"dark mode"` references
- Any `"project card grid"` references
- Any `"theme toggle"` references
- Any `"scroll animation"` or `"scroll trigger"` references
- Any `"IBM Plex Mono"` / `"Inter"` references in typography descriptions
- Any `"oklch"` or `"hex"` palette descriptions

Replacement rationale: this file is the chat's system-prompt context. The chat should tell visitors about the new editorial site, not the old one. Where the v1.0 feature is replaced (e.g., canvas hero → display hero), rewrite to describe the new thing. Where the v1.0 feature is removed with no replacement (e.g., dark mode), delete the reference.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ChatWidget.astro + global.css .chat-* rules to new token names</name>
  <files>src/components/chat/ChatWidget.astro, src/styles/global.css</files>
  <read_first>
    - src/components/chat/ChatWidget.astro (full file — inline styles on lines 14, 56, 63, 67, 87, 99, 108, 115, 124, 133, 142, 152, 163, 165, 173, 177, 184, 208)
    - src/styles/global.css (lines 354-451 for .chat-* rules)
    - .planning/phases/08-foundation/08-CONTEXT.md D-19, D-20, D-28
    - .planning/phases/08-foundation/08-RESEARCH.md section 4 (exact line enumeration)
    - design-system/MASTER.md section 9 (Chat Widget Token Map)
  </read_first>
  <action>
**Edit A — src/components/chat/ChatWidget.astro**

Apply the full token rename map from the `<interfaces>` context block above. Every `var(--token-*)` reference inside `style="..."` attributes MUST be replaced with the mapped new name or flat value. Do NOT leave ANY `--token-*` reference in this file after the edit.

Specific rename sites (line numbers from the current file — verify before each edit):

1. Line 14: `style="bottom: 24px; right: 24px; width: 48px; height: 48px; background: var(--token-accent);"` — replace `var(--token-accent)` with `var(--accent)`.

2. Line 56 (chat-panel style): replace `background: var(--token-bg-primary); border: 1px solid var(--token-border);` with `background: var(--bg); border: 1px solid var(--rule);`.

3. Line 63 (chat header style): replace `background: var(--token-bg-secondary); border-bottom: 1px solid var(--token-border); padding: 0 var(--token-space-md);` with `background: var(--rule); border-bottom: 1px solid var(--rule); padding: 0 16px;`.

4. Line 67 (header title span): replace `font-size: var(--token-text-sm); font-weight: 600; color: var(--token-text-primary);` with `font-size: 0.875rem; font-weight: 600; color: var(--ink);`.

5. Line 87 (close button SVG color): replace `color: var(--token-text-muted);` with `color: var(--ink-faint);`.

6. Line 99 (chat-messages padding): replace `padding: var(--token-space-md);` with `padding: 16px;`.

7. Line 108 (chat-starters gap): replace `gap: var(--token-space-sm);` with `gap: 8px;`.

8. Lines 115, 124, 133, 142 (four chat-starter-chip inline styles): replace `background: var(--token-bg-secondary); border: 1px solid var(--token-border); border-radius: 9999px; padding: 8px 12px; font-size: var(--token-text-sm); font-weight: 400; color: var(--token-text-secondary); cursor: pointer;` with `background: var(--rule); border: 1px solid var(--rule); border-radius: 9999px; padding: 8px 12px; font-size: 0.875rem; font-weight: 400; color: var(--ink-muted); cursor: pointer;`.

9. Line 152 (chat-typing): replace `border-left: 2px solid var(--token-border);` with `border-left: 2px solid var(--rule);`.

10. Line 163 (input area wrapper): replace `border-top: 1px solid var(--token-border); padding: 12px var(--token-space-md); background: var(--token-bg-primary);` with `border-top: 1px solid var(--rule); padding: 12px 16px; background: var(--bg);`.

11. Line 165 (input flex row gap): replace `gap: var(--token-space-sm);` with `gap: 8px;`.

12. Line 173 (textarea): replace `background: var(--token-bg-secondary); border: 1px solid var(--token-border); border-radius: 8px; padding: 8px 12px; font-family: var(--font-body); font-size: var(--token-text-base); color: var(--token-text-primary); outline: none;` with `background: var(--rule); border: 1px solid var(--rule); border-radius: 8px; padding: 8px 12px; font-family: var(--font-body); font-size: 1rem; color: var(--ink); outline: none;`.

13. Line 177 (chat-char-count): replace `font-size: var(--token-text-sm); color: var(--token-text-muted);` with `font-size: 0.875rem; color: var(--ink-faint);`.

14. Line 184 (chat-send button): replace `background: var(--token-accent);` with `background: var(--accent);`.

15. Line 208 (privacy note): replace `font-size: var(--token-text-sm); color: var(--token-text-muted);` with `font-size: 0.875rem; color: var(--ink-faint);`.

After these edits, `grep -c "token-" src/components/chat/ChatWidget.astro` must return 0.

**Edit B — src/styles/global.css (.chat-* rules at current lines 354-451)**

Apply the same token map to every `var(--token-*)` reference inside the `.chat-*` rule block. Specific changes:

1. `.typing-dot` (line 370): `background-color: var(--token-text-muted);` → `background-color: var(--ink-faint);`

2. `.chat-textarea:focus` (lines 383-386):
```css
.chat-textarea:focus {
  border-color: var(--ink-muted) !important;
  box-shadow: 0 0 0 2px var(--accent);
}
```

3. `.chat-bot-message ul`, `.chat-bot-message ol` (lines 395-399): `padding-left: var(--token-space-md);` → `padding-left: 16px;`

4. `.chat-bot-message li` (line 400-402): `margin-bottom: var(--token-space-xs);` → `margin-bottom: 4px;`

5. `.chat-bot-message code` (lines 403-409):
```css
.chat-bot-message code {
  font-family: var(--font-mono);
  font-size: 0.875rem;
  background: var(--rule);
  padding: 1px 4px;
  border-radius: 3px;
}
```

6. `.chat-bot-message a` (line 410-413):
```css
.chat-bot-message a {
  color: var(--accent);
  text-decoration: none;
}
```

7. `.chat-starter-chip:hover` (line 418-422):
```css
.chat-starter-chip:hover {
  border-color: var(--ink-muted) !important;
  color: var(--accent) !important;
}
```

8. `.hover-text-primary:hover` (line 424-427):
```css
.hover-text-primary:hover {
  color: var(--ink) !important;
}
```

9. `.copy-success svg` (line 437-439):
```css
.copy-success svg {
  color: var(--accent) !important;
}
```

Leave `.chat-copy-btn { opacity: 0; transition: opacity 200ms ease; }` and `.chat-message-wrapper:hover .chat-copy-btn { opacity: 1; }` unchanged — D-13 survivors.

Leave `.chat-textarea` base rule (lines 374-380) unchanged — only modifies `resize`, `min-height`, `max-height`, `overflow-y`.

Leave mobile full-screen `@media (max-width: 767px) { .chat-panel-mobile { ... } }` unchanged.

After these edits, the `.chat-*` rule block (and the entire global.css file) must have zero `--token-*` references. Run `grep -c 'token-' src/styles/global.css` — expect 0.
  </action>
  <verify>
    <automated>grep -c 'token-' src/components/chat/ChatWidget.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'token-' src/components/chat/ChatWidget.astro` returns 0
    - `grep -c 'token-' src/styles/global.css` returns 0
    - `grep -c 'var(--accent)' src/components/chat/ChatWidget.astro` returns ≥ 2 (bubble + send button)
    - `grep -c 'var(--bg)' src/components/chat/ChatWidget.astro` returns ≥ 2 (panel + input area)
    - `grep -c 'var(--rule)' src/components/chat/ChatWidget.astro` returns ≥ 5
    - `grep -c 'var(--ink)' src/components/chat/ChatWidget.astro` returns ≥ 2
    - `grep -c 'var(--ink-muted)' src/components/chat/ChatWidget.astro` returns ≥ 4
    - `grep -c 'var(--ink-faint)' src/components/chat/ChatWidget.astro` returns ≥ 3
    - `grep -c 'var(--font-mono)' src/styles/global.css` returns ≥ 1
    - `grep -c 'var(--font-code)' src/styles/global.css` returns 0
    - `grep -c 'var(--accent)' src/styles/global.css` returns ≥ 4
    - `grep -c '\.chat-' src/styles/global.css` returns ≥ 5 (chat rules intact)
    - `grep -c 'nav-link-hover' src/styles/global.css` returns ≥ 1 (D-13 survivor preserved)
    - `grep -c '\.chat-copy-btn' src/styles/global.css` returns ≥ 1 (D-13 survivor preserved)
    - `grep -c 'oklch(' src/styles/global.css` returns 0 (no stale oklch color-mix)
  </acceptance_criteria>
  <done>
    ChatWidget.astro inline styles and global.css .chat-* rules use only the new editorial tokens. Phase 8 has completed the token cascade on the chat widget. The widget is visually unchanged (D-19 stance preserved) but now sits on the new foundation. CHAT-02 (Phase 10) will restyle it to match the editorial system.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit src/data/portfolio-context.json for v1.0 design references</name>
  <files>src/data/portfolio-context.json</files>
  <read_first>
    - src/data/portfolio-context.json (full file — this is the chat system-prompt context)
    - .planning/phases/08-foundation/08-CONTEXT.md D-22
    - .planning/phases/08-foundation/08-RESEARCH.md section 5 (known audit targets)
  </read_first>
  <action>
Read `src/data/portfolio-context.json` in full. This file is the chat widget's system-prompt context — it tells the chat bot about Jack and the site. Audit every field and update every reference to v1.0 design that no longer applies.

**Known required change:**

1. In the `siteStack` array (per research, around line 65), remove the string `"GSAP"`. If the array has other stale entries like `"IBM Plex Mono"` or `"Inter"`, replace them with `"Geist"` / `"Geist Mono"` (as appropriate — or drop typography from siteStack entirely if that's cleaner). Do NOT add new entries like `"Tailwind CSS v4"` unless the field already listed the Tailwind version — only update, don't expand.

**Audit targets (search the whole file for these strings and decide per-instance):**

- `"canvas hero"`, `"generative hero"`, `"canvas"`, `"WebGL"` → remove (v1.0 feature, replaced by editorial display hero in Phase 10)
- `"dark theme"`, `"dark mode"`, `"theme toggle"`, `"light/dark"` → remove (v1.1 is single light theme per DSGN-03)
- `"project card grid"`, `"card grid"`, `"featured cards"` → remove or replace with `"editorial numbered work list"` (depending on how it's phrased)
- `"scroll animation"`, `"scroll trigger"`, `"GSAP scroll"`, `"reveal animation"` → remove (v1.1 has no motion per DSGN-04)
- `"IBM Plex Mono"` → replace with `"Geist Mono"`
- `"Inter"` (as typography reference, NOT as a regular word) → replace with `"Geist"`
- `"oklch"` → remove or replace with `"flat hex"` (describing the palette is fine; describing the color space is not)
- `"shiyunlu.com"` (the v1.0 design language source) → remove

**What NOT to change:**
- Personal details (name, email, location, education, experience)
- Project titles, descriptions, stacks (those are content, not design refs, and Phase 10 will port them)
- Skills that are real (`"TypeScript"`, `"Astro"`, `"Cloudflare Pages"`, etc.)
- The overall JSON structure and key names
- Anything under `skills.languages`, `skills.frameworks` (unless a listed framework no longer applies — only `"GSAP"` is known to be dead)
- Social links, URLs

**Validation:** After edits, the file must still be valid JSON. Run `node -e "JSON.parse(require('fs').readFileSync('src/data/portfolio-context.json', 'utf8')); console.log('valid')"` to verify. If parse fails, the edit introduced a syntax error — fix before committing.

**Scope discipline:** This is an audit-and-edit task, not a rewrite. If a field contains no v1.0 design references, do not touch it. If a field mixes design refs with content (e.g., a sentence in a project description mentions GSAP scroll animations), rewrite just the design portion of the sentence.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/data/portfolio-context.json', 'utf8'))" &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('src/data/portfolio-context.json', 'utf8'))"` exits 0 (valid JSON)
    - `grep -c '"GSAP"' src/data/portfolio-context.json` returns 0
    - `grep -c 'GSAP' src/data/portfolio-context.json` returns 0 (any casing)
    - `grep -ci 'canvas hero\|generative hero' src/data/portfolio-context.json` returns 0
    - `grep -ci 'dark mode\|dark theme\|theme toggle' src/data/portfolio-context.json` returns 0
    - `grep -ci 'IBM Plex Mono' src/data/portfolio-context.json` returns 0
    - `grep -ci 'oklch' src/data/portfolio-context.json` returns 0
    - `grep -ci 'shiyunlu' src/data/portfolio-context.json` returns 0
    - `grep -ci 'scroll trigger\|scroll animation' src/data/portfolio-context.json` returns 0
  </acceptance_criteria>
  <done>
    portfolio-context.json no longer references GSAP, canvas hero, dark theme, theme toggle, scroll animations, IBM Plex Mono, oklch, or shiyunlu.com. The file still parses as valid JSON and retains all personal/project content.
  </done>
</task>

</tasks>

<verification>
- Plan 05 touches ChatWidget.astro, global.css, portfolio-context.json — no overlap with Plan 04 (Header/MobileMenu/deletions) or Plan 06 (pages + surviving components) → parallel-safe in Wave 4
- `grep -rn 'token-' src/components/chat/ src/styles/global.css src/data/portfolio-context.json` returns 0 matches
- `grep -rn 'GSAP' src/data/portfolio-context.json` returns 0
- JSON file remains valid
</verification>

<success_criteria>
The chat widget has completed its side of the token cascade (DSGN-02 structurally satisfied for chat surface), and the chat system-prompt context no longer references v1.0 design. The chat widget is still visually unchanged per D-19 — CHAT-02 in Phase 10 will do the full restyle. Functionally it remains a zero-motion widget after Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation/08-05-SUMMARY.md` capturing:
- ChatWidget.astro edit count (~18 inline style renames)
- global.css .chat-* edit count (~10 rule renames)
- portfolio-context.json audit findings (what was changed, what was not)
- Confirmation that grep for `token-` returns 0 in all three files
- Confirmation JSON parse passes
</output>
