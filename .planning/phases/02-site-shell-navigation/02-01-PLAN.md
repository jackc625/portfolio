---
phase: 02-site-shell-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/SkipToContent.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/projects.astro
  - src/pages/resume.astro
  - src/pages/contact.astro
  - public/og-default.png
autonomous: true
requirements:
  - CNTC-02
  - SEOA-01
  - SEOA-02
  - SEOA-04
  - SEOA-06
  - SEOA-07

must_haves:
  truths:
    - "Every page has a unique <title> tag rendered in the format '{PageName} | Jack Cutrara' (except Home which is 'Jack Cutrara | Software Engineer')"
    - "Every page has Open Graph meta tags (og:title, og:type, og:image, og:url) in page source"
    - "Every page has a <footer> with GitHub, LinkedIn, and email links"
    - "Pressing Tab on any page focuses the skip-to-content link first"
    - "HTML uses semantic landmarks: <header>, <nav>, <main id='main-content'>, <footer>"
    - "All five nav target pages (/, /about, /projects, /resume, /contact) return 200 after build"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Layout shell composing SEO, SkipToContent, Header slot, main, Footer with ClientRouter"
      contains: "astro-seo"
    - path: "src/components/SkipToContent.astro"
      provides: "Visually hidden skip link targeting #main-content"
      contains: "main-content"
    - path: "src/components/Footer.astro"
      provides: "Footer with contact links (GitHub, LinkedIn, email)"
      contains: "aria-label"
    - path: "src/pages/about.astro"
      provides: "Stub about page with SEO props"
      contains: "BaseLayout"
    - path: "public/og-default.png"
      provides: "Default Open Graph image for social previews"
  key_links:
    - from: "src/pages/*.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import BaseLayout, pass title + description props"
      pattern: "title=.*description="
    - from: "src/layouts/BaseLayout.astro"
      to: "astro-seo"
      via: "SEO component in <head>"
      pattern: "import.*SEO.*astro-seo"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Footer.astro"
      via: "import and render after <main>"
      pattern: "import Footer"
---

<objective>
Build the structural site shell: rewrite BaseLayout with SEO meta infrastructure (astro-seo + ClientRouter), create SkipToContent accessibility link, create Footer with contact links, generate OG placeholder image, and create all 5 stub pages with unique SEO meta per page.

Purpose: Establishes the page wrapper that every page inherits -- SEO tags, semantic landmarks, footer contact links, skip-to-content accessibility, and view transitions. This is the foundation the Header/MobileMenu (Plan 02) plugs into.

Output: Updated BaseLayout.astro, SkipToContent.astro, Footer.astro, 5 page files with SEO meta, og-default.png placeholder.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-site-shell-navigation/02-CONTEXT.md
@.planning/phases/02-site-shell-navigation/02-RESEARCH.md
@.planning/phases/02-site-shell-navigation/02-UI-SPEC.md

<interfaces>
<!-- Existing BaseLayout interface (will be rewritten) -->
From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title?: string;
  description?: string;
}
```

<!-- Existing page pattern -->
From src/pages/index.astro:
```astro
<BaseLayout title="Jack Cutrara" description="Software engineer portfolio...">
```

<!-- Design tokens available via Tailwind utilities -->
From src/styles/global.css:
- Colors: bg-bg-primary, bg-bg-secondary, text-text-primary, text-text-secondary, text-text-muted, text-accent, text-accent-hover, border-border
- Fonts: font-display (Instrument Serif), font-body (Instrument Sans), font-mono (JetBrains Mono)
- Token vars: --token-text-sm, --token-text-base, --token-space-md, --token-space-lg, --token-space-xl

<!-- astro-seo API (installed v1.1.0) -->
```typescript
import { SEO } from "astro-seo";
// Props: title, titleTemplate, titleDefault, description, canonical
// openGraph: { basic: { title, type, image, url }, optional: { siteName, description }, image: { width, height, alt, type } }
// twitter: { card, title, description, image, imageAlt }
```

<!-- Astro View Transitions -->
```typescript
import { ClientRouter } from "astro:transitions";
// Add <ClientRouter /> to <head> for site-wide page transitions
```

<!-- astro.config.mjs site URL -->
site: "https://jackcutrara.com"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkipToContent, Footer, OG image, and rewrite BaseLayout with SEO + shell structure</name>
  <files>
    src/components/SkipToContent.astro,
    src/components/Footer.astro,
    src/layouts/BaseLayout.astro,
    public/og-default.png
  </files>
  <read_first>
    src/layouts/BaseLayout.astro,
    src/styles/global.css,
    src/pages/index.astro,
    astro.config.mjs,
    .planning/phases/02-site-shell-navigation/02-UI-SPEC.md,
    .planning/phases/02-site-shell-navigation/02-RESEARCH.md
  </read_first>
  <action>
**Create `src/components/SkipToContent.astro`** (per D-06, SEOA-06):
- Single `<a href="#main-content">` element with text "Skip to content"
- Classes: `sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:bg-bg-primary focus:text-accent focus:px-4 focus:py-2 focus:rounded focus:ring-2 focus:ring-accent focus:outline-none`
- This is the first focusable element in the page body

**Create `src/components/Footer.astro`** (per D-08, CNTC-02, SEOA-04, SEOA-07):
- `<footer>` element with `class="bg-bg-secondary py-8 px-4 md:px-6"`
- Inner flex container: `class="max-w-5xl mx-auto flex flex-col md:flex-row items-center md:justify-between gap-4"`
- Left side (column on mobile, left on desktop):
  - `<span>` with text "Jack Cutrara" in `class="font-body text-text-secondary"` at `--token-text-sm` size (use `text-[length:var(--token-text-sm)]`)
  - `<span>` with text "\u00A9 2026 Jack Cutrara" in `class="font-body text-text-muted"` at `--token-text-sm` size
- Right side: `<nav aria-label="Footer links">` containing a flex row with `gap-4`
- Three icon links inside the nav, each wrapped in `<a>` with 44x44px tap area via `class="p-2 text-text-secondary hover:text-accent transition-colors duration-200"`:
  1. GitHub: `href="https://github.com/jackc625"`, `aria-label="GitHub profile"`, inline SVG 20x20 of GitHub mark using `currentColor` fill, `aria-hidden="true"` on the SVG
  2. LinkedIn: `href="https://linkedin.com/in/jackcutrara"`, `aria-label="LinkedIn profile"`, inline SVG 20x20 of LinkedIn mark using `currentColor` fill, `aria-hidden="true"` on the SVG
  3. Email: `href="mailto:jack@jackcutrara.com"`, `aria-label="Send email"`, inline SVG 20x20 of envelope icon using `currentColor` fill/stroke, `aria-hidden="true"` on the SVG
- All links open in same tab (no target="_blank" for email; GitHub and LinkedIn can use `target="_blank" rel="noopener noreferrer"`)
- SVG paths: Use standard brand SVG paths for GitHub octicon and LinkedIn "in" logo. For email, use a simple envelope outline (rect + triangle flap).

**Create `public/og-default.png`** (per SEOA-02 research open question #1):
- Generate a 1200x630 PNG using a Node.js script or the `canvas` package. If canvas is unavailable, create a minimal valid PNG programmatically using raw bytes (a solid dark background PNG at 1200x630). The image is a placeholder -- visual design comes later.
- Alternative: Use a simple shell command with ImageMagick (`convert`) or just create a minimal 1x1 PNG and note it as a placeholder to be replaced.
- The file must exist at `public/og-default.png` so the OG image path resolves.

**Rewrite `src/layouts/BaseLayout.astro`** (per D-12, SEOA-01, SEOA-02, SEOA-04):
- Update Props interface to:
  ```typescript
  interface Props {
    title: string;
    description: string;
    ogImage?: string;
    ogType?: string;
    canonicalUrl?: string;
  }
  ```
- Destructure with defaults: `ogImage = "/og-default.png"`, `ogType = "website"`
- Compute `siteUrl` from `Astro.site?.toString().replace(/\/$/, "") ?? "https://jackcutrara.com"`
- Compute `currentUrl` from `canonicalUrl ?? \`${siteUrl}${Astro.url.pathname}\``
- In `<head>`:
  - Keep existing: charset, viewport, favicon, Font components
  - Add `import { SEO } from "astro-seo";` and render `<SEO>` with:
    - `title={title}`
    - `titleTemplate="%s | Jack Cutrara"`
    - `titleDefault="Jack Cutrara | Software Engineer"` (used when title is empty/undefined)
    - `description={description}`
    - `canonical={currentUrl}`
    - `openGraph={{ basic: { title, type: ogType, image: \`${siteUrl}${ogImage}\`, url: currentUrl }, optional: { siteName: "Jack Cutrara", description }, image: { width: 1200, height: 630, alt: \`${title} - Jack Cutrara Portfolio\`, type: "image/png" } }}`
    - `twitter={{ card: "summary_large_image", title, description, image: \`${siteUrl}${ogImage}\`, imageAlt: \`${title} - Jack Cutrara Portfolio\` }}`
  - Remove the old `<meta name="description">` and `<title>` tags (SEO component handles these)
  - Add `import { ClientRouter } from "astro:transitions";` and render `<ClientRouter />` in `<head>`
- In `<body>` (keep existing classes `bg-bg-primary text-text-primary font-body min-h-screen antialiased`):
  - Add `class="flex flex-col"` to body for sticky footer layout
  - Import and render `<SkipToContent />` as first child of body
  - Render a `<!-- Header will be added in Plan 02 -->` comment placeholder where Header goes
  - Render `<main id="main-content" class="flex-1 pt-16">` wrapping `<slot />` (pt-16 = 64px = header height offset)
  - Import and render `<Footer />` after `</main>`
- Import paths:
  - `import SkipToContent from "../components/SkipToContent.astro";`
  - `import Footer from "../components/Footer.astro";`
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SkipToContent.astro exists and contains `href="#main-content"` and text "Skip to content"
    - src/components/SkipToContent.astro contains `sr-only` and `focus:not-sr-only`
    - src/components/Footer.astro contains `<footer` and `aria-label="Footer links"`
    - src/components/Footer.astro contains `href="https://github.com/jackc625"` and `aria-label="GitHub profile"`
    - src/components/Footer.astro contains `href="https://linkedin.com/in/jackcutrara"` and `aria-label="LinkedIn profile"`
    - src/components/Footer.astro contains `href="mailto:jack@jackcutrara.com"` and `aria-label="Send email"`
    - src/components/Footer.astro contains three `<svg` elements with `aria-hidden="true"`
    - src/layouts/BaseLayout.astro contains `import { SEO } from "astro-seo"`
    - src/layouts/BaseLayout.astro contains `import { ClientRouter } from "astro:transitions"`
    - src/layouts/BaseLayout.astro contains `titleTemplate="%s | Jack Cutrara"`
    - src/layouts/BaseLayout.astro contains `<main id="main-content"`
    - src/layouts/BaseLayout.astro contains `import Footer from "../components/Footer.astro"`
    - src/layouts/BaseLayout.astro contains `import SkipToContent from "../components/SkipToContent.astro"`
    - src/layouts/BaseLayout.astro contains `openGraph=` with `og:title` equivalent
    - public/og-default.png exists (file size > 0 bytes)
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>BaseLayout renders SEO meta tags via astro-seo, includes ClientRouter for view transitions, composes SkipToContent and Footer, wraps slot in semantic main landmark, and a placeholder OG image exists at public/og-default.png.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 5 stub pages with unique SEO meta and heading</name>
  <files>
    src/pages/index.astro,
    src/pages/about.astro,
    src/pages/projects.astro,
    src/pages/resume.astro,
    src/pages/contact.astro
  </files>
  <read_first>
    src/layouts/BaseLayout.astro,
    src/pages/index.astro,
    .planning/phases/02-site-shell-navigation/02-UI-SPEC.md
  </read_first>
  <action>
**Update `src/pages/index.astro`** (Home page -- per UI-SPEC SEO Meta Copy table):
- Import BaseLayout
- Render `<BaseLayout title="" description="Software engineer portfolio showcasing projects, technical depth, and problem-solving ability.">` -- empty title triggers `titleDefault` which renders "Jack Cutrara | Software Engineer" (avoids the "Jack Cutrara | Jack Cutrara" double-wrap from Pitfall 6)
- Body: `<section class="py-[var(--token-space-section)] px-4 md:px-6"><h1 class="font-display text-[length:var(--token-text-display)]">Jack Cutrara</h1></section>`
- The `<h1>` is the only heading on this page. Content built in Phase 3.

**Create `src/pages/about.astro`** (per UI-SPEC SEO Meta Copy table):
- Import BaseLayout
- `<BaseLayout title="About" description="Learn about Jack Cutrara's background, education, and path into software engineering.">`
- Body: `<section class="py-[var(--token-space-section)] px-4 md:px-6"><h1 class="font-display text-[length:var(--token-text-display)]">About</h1></section>`

**Create `src/pages/projects.astro`** (per UI-SPEC SEO Meta Copy table):
- Import BaseLayout
- `<BaseLayout title="Projects" description="Explore software engineering projects by Jack Cutrara, from web applications to developer tools.">`
- Body: `<section class="py-[var(--token-space-section)] px-4 md:px-6"><h1 class="font-display text-[length:var(--token-text-display)]">Projects</h1></section>`

**Create `src/pages/resume.astro`** (per UI-SPEC SEO Meta Copy table):
- Import BaseLayout
- `<BaseLayout title="Resume" description="View Jack Cutrara's software engineering resume and professional experience.">`
- Body: `<section class="py-[var(--token-space-section)] px-4 md:px-6"><h1 class="font-display text-[length:var(--token-text-display)]">Resume</h1></section>`

**Create `src/pages/contact.astro`** (per UI-SPEC SEO Meta Copy table):
- Import BaseLayout
- `<BaseLayout title="Contact" description="Get in touch with Jack Cutrara via email, LinkedIn, or GitHub.">`
- Body: `<section class="py-[var(--token-space-section)] px-4 md:px-6"><h1 class="font-display text-[length:var(--token-text-display)]">Contact</h1></section>`

Each page has exactly one `<h1>` (SEOA-04). Each page passes unique title and description (SEOA-01). The titleTemplate "%s | Jack Cutrara" appends the site name to non-home pages. The home page uses titleDefault "Jack Cutrara | Software Engineer" via empty title.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro contains `description="Software engineer portfolio showcasing projects, technical depth, and problem-solving ability."`
    - src/pages/about.astro exists and contains `title="About"` and `description="Learn about Jack Cutrara`
    - src/pages/projects.astro exists and contains `title="Projects"` and `description="Explore software engineering projects`
    - src/pages/resume.astro exists and contains `title="Resume"` and `description="View Jack Cutrara`
    - src/pages/contact.astro exists and contains `title="Contact"` and `description="Get in touch`
    - Each of the 5 page files contains exactly one `<h1` tag
    - Each of the 5 page files contains `import BaseLayout`
    - `pnpm build` exits with code 0 (all 5 routes generated)
  </acceptance_criteria>
  <done>All 5 pages exist with unique SEO meta (title + description), single h1 headings, and BaseLayout integration. Build generates /index.html, /about/index.html, /projects/index.html, /resume/index.html, /contact/index.html.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with 0 errors
- Generated HTML files exist in dist/ for all 5 routes
- Each generated HTML file contains `<title>` tag with unique content
- Each generated HTML file contains `og:title` meta tag
- Each generated HTML file contains `<footer>` with GitHub, LinkedIn, email links
- Each generated HTML file contains `<a href="#main-content">Skip to content</a>`
- Each generated HTML file contains `<main id="main-content">`
</verification>

<success_criteria>
- Build produces 5 HTML pages, each with unique SEO meta (title, description, OG tags, Twitter cards)
- Footer with contact links (GitHub, LinkedIn, email) renders on every page (CNTC-02)
- Skip-to-content link is first focusable element on every page (SEOA-06)
- Semantic HTML landmarks (main, footer) present on every page (SEOA-04)
- All SVG icons have aria-hidden="true" with visible aria-label on parent link (SEOA-07)
- ClientRouter enables view transitions site-wide
- OG default image exists at public/og-default.png (SEOA-02)
- IDNV-01 note: name ("Jack Cutrara") visible via home h1 stub. Full IDNV-01 satisfaction (name + role + value prop on all pages) requires Phase 2 Plan 02 (header namemark) and Phase 3 (home hero). Phase 2 partially addresses this.
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-shell-navigation/02-01-SUMMARY.md`
</output>
