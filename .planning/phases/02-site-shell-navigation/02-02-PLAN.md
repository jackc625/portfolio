---
phase: 02-site-shell-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/Header.astro
  - src/components/MobileMenu.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - IDNV-01
  - IDNV-02
  - IDNV-03
  - SEOA-04
  - SEOA-05

must_haves:
  truths:
    - "Jack Cutrara namemark is visible in the header on every page, linking to home"
    - "Five nav links (Home, About, Projects, Resume, Contact) are visible at desktop widths and functional"
    - "Header hides when scrolling down past 100px and reappears when scrolling up"
    - "Header background transitions from transparent to semi-transparent dark with blur after scrolling 50px"
    - "Hamburger button appears below 768px width and opens a full-screen overlay menu"
    - "Mobile menu traps keyboard focus, closes on Escape, and closes when a nav link is clicked"
    - "Active page link is highlighted in accent color with aria-current='page'"
    - "All interactive elements have visible focus-visible ring indicators"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Fixed header with namemark, desktop nav, hamburger trigger, scroll-reveal behavior"
      contains: "aria-label=\"Main navigation\""
    - path: "src/components/MobileMenu.astro"
      provides: "Full-screen overlay menu with focus trap and keyboard accessibility"
      contains: "role=\"dialog\""
    - path: "src/layouts/BaseLayout.astro"
      provides: "Updated layout importing Header component"
      contains: "import Header"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Header.astro"
      via: "import and render before <main>"
      pattern: "import Header"
    - from: "src/components/Header.astro"
      to: "src/components/MobileMenu.astro"
      via: "hamburger button aria-controls targeting mobile-menu ID"
      pattern: "aria-controls=\"mobile-menu\""
    - from: "src/components/Header.astro"
      to: "scroll event listener"
      via: "vanilla JS script with astro:page-load re-init"
      pattern: "astro:page-load"
    - from: "src/components/MobileMenu.astro"
      to: "focus trap"
      via: "vanilla JS keydown listener cycling focus within menu"
      pattern: "trapFocus"
---

<objective>
Build the header with scroll-reveal navigation and full-screen mobile menu overlay. The header displays Jack's namemark linking home, 5 desktop nav links with active page indication, and a hamburger trigger for mobile. The mobile menu is a full-screen overlay with focus trap, Escape-to-close, and staggered link animation. All interactive elements have visible focus-visible indicators.

Purpose: Delivers the primary navigation system that satisfies IDNV-01 (name visible on all pages), IDNV-02 (sticky nav with labeled links), IDNV-03 (accessible mobile menu), SEOA-04 (semantic nav landmarks), and SEOA-05 (keyboard navigation with focus indicators).

Output: Header.astro, MobileMenu.astro, updated BaseLayout.astro with Header integration.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-site-shell-navigation/02-CONTEXT.md
@.planning/phases/02-site-shell-navigation/02-RESEARCH.md
@.planning/phases/02-site-shell-navigation/02-UI-SPEC.md
@.planning/phases/02-site-shell-navigation/02-01-SUMMARY.md

<interfaces>
<!-- BaseLayout structure from Plan 01 (will be modified to add Header import) -->
From src/layouts/BaseLayout.astro (after Plan 01):
```astro
import { SEO } from "astro-seo";
import { ClientRouter } from "astro:transitions";
import Font from "astro/components/Font.astro";
import SkipToContent from "../components/SkipToContent.astro";
import Footer from "../components/Footer.astro";

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
}
```
Body structure:
```html
<body class="bg-bg-primary text-text-primary font-body min-h-screen antialiased flex flex-col">
  <SkipToContent />
  <!-- Header will be added in Plan 02 -->
  <main id="main-content" class="flex-1 pt-16">
    <slot />
  </main>
  <Footer />
</body>
```

<!-- Design tokens available via Tailwind utilities -->
From src/styles/global.css:
- bg-bg-primary, bg-bg-secondary, text-text-primary, text-text-secondary, text-text-muted, text-accent, text-accent-hover
- font-display (Instrument Serif), font-body (Instrument Sans)
- --token-text-sm (13-14px fluid), --token-text-base (15-16px fluid), --token-text-heading (20-32px fluid)

<!-- UI-SPEC Component Specs -->
Header: fixed top, full width, z-50, 64px height, transparent -> bg-secondary/85 blur on scroll
Desktop nav: hidden below md, flex at md+, gap-lg (24px), uppercase text-sm, letter-spacing 0.05em
Hamburger: visible below md, 44x44px, three 20px wide 2px tall lines, animates to X
Mobile menu: fixed inset-0, z-40, bg-primary/98, role="dialog", aria-modal="true"
Active link: text-accent + aria-current="page"
Focus rings: focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header with desktop nav, hamburger button, and scroll-reveal behavior</name>
  <files>
    src/components/Header.astro,
    src/layouts/BaseLayout.astro
  </files>
  <read_first>
    src/layouts/BaseLayout.astro,
    src/styles/global.css,
    .planning/phases/02-site-shell-navigation/02-UI-SPEC.md,
    .planning/phases/02-site-shell-navigation/02-RESEARCH.md
  </read_first>
  <action>
**Create `src/components/Header.astro`** (per D-01, D-02, D-03, D-04, D-07, D-09, IDNV-01, IDNV-02, SEOA-04, SEOA-05):

**Frontmatter:**
```typescript
const currentPath = Astro.url.pathname;
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/projects", label: "Projects" },
  { href: "/resume", label: "Resume" },
  { href: "/contact", label: "Contact" },
];
```

**HTML structure:**
- `<header id="site-header" class="fixed top-0 left-0 right-0 z-50 h-16 transition-[transform,background-color] duration-300 ease-out">` -- per UI-SPEC: fixed, full width, z-50, 64px (h-16), transition on transform and background-color
- Inner container: `<nav aria-label="Main navigation" class="flex items-center justify-between h-full px-4 md:px-6 max-w-5xl mx-auto">`
- **Left: Namemark** (per D-09): `<a href="/" class="font-display text-[length:var(--token-text-base)] text-text-primary hover:text-accent transition-colors duration-200 focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none focus-visible:rounded">Jack Cutrara</a>`
- **Right: Desktop nav links** (per D-03, D-04, D-07): `<ul class="hidden md:flex items-center gap-6">` containing 5 `<li>` elements, each with an `<a>`:
  - Determine `isActive` per link: `href === "/" ? currentPath === "/" : currentPath.startsWith(href)`
  - Active class: `text-accent` (per UI-SPEC Active link color)
  - Inactive class: `text-text-secondary hover:text-text-primary`
  - All links: `class:list={["uppercase tracking-[0.05em] text-[length:var(--token-text-sm)] font-body transition-colors duration-200 focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none focus-visible:rounded px-2 py-1", isActive ? "text-accent" : "text-text-secondary hover:text-text-primary"]}`
  - Active link gets `aria-current="page"` attribute (use `aria-current={isActive ? "page" : undefined}`)
- **Right: Hamburger button** (per D-07, IDNV-03): `<button id="menu-trigger" class="md:hidden flex items-center justify-center w-11 h-11 text-text-primary focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none focus-visible:rounded" aria-label="Open menu" aria-expanded="false" aria-controls="mobile-menu">`
  - Inside: three `<span>` elements styled as horizontal lines:
    - Container: `<span class="relative w-5 h-4 flex flex-col justify-between">`
    - Three lines: `<span class="block w-full h-0.5 bg-current transition-all duration-300 origin-center"></span>` (x3)
    - CSS for animated hamburger-to-X: when `[aria-expanded="true"]` on the button:
      - Line 1: `translate-y-[7px] rotate-45` (moves to center and rotates)
      - Line 2: `opacity-0` (fades out)
      - Line 3: `-translate-y-[7px] -rotate-45` (moves to center and rotates opposite)

**CSS for hamburger animation** -- add a `<style>` block in Header.astro:
```css
#menu-trigger[aria-expanded="true"] .hamburger-line:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}
#menu-trigger[aria-expanded="true"] .hamburger-line:nth-child(2) {
  opacity: 0;
}
#menu-trigger[aria-expanded="true"] .hamburger-line:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}
```
Add class `hamburger-line` to each of the three `<span>` line elements.

**CSS for scroll states** -- add in the same `<style>` block:
```css
.header-scrolled {
  background-color: oklch(0.18 0.005 260 / 0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}
.header-hidden {
  transform: translateY(-100%);
}
```
Note: `oklch(0.18 0.005 260 / 0.85)` is `--token-bg-secondary` at 85% opacity. Hardcoded here because Tailwind `bg-bg-secondary/85` with `backdrop-blur` can be used as an alternative if preferred, but the class-toggle approach via JS is cleaner.

**Script block** (per RESEARCH.md Pattern 2, Pitfall 1):
```html
<script>
  function initHeader() {
    const header = document.getElementById("site-header");
    if (!header) return;

    let lastScrollY = 0;
    let ticking = false;

    function onScroll() {
      if (!ticking) {
        window.requestAnimationFrame(() => {
          const currentScrollY = window.scrollY;

          // Add/remove background based on scroll position (threshold: 50px per UI-SPEC)
          if (currentScrollY > 50) {
            header.classList.add("header-scrolled");
          } else {
            header.classList.remove("header-scrolled");
          }

          // Hide on scroll down, show on scroll up (threshold: 100px per UI-SPEC)
          if (currentScrollY > lastScrollY && currentScrollY > 100) {
            header.classList.add("header-hidden");
          } else {
            header.classList.remove("header-hidden");
          }

          lastScrollY = currentScrollY;
          ticking = false;
        });
        ticking = true;
      }
    }

    window.addEventListener("scroll", onScroll, { passive: true });
  }

  // Initialize on first load and after view transitions (Pitfall 1)
  document.addEventListener("astro:page-load", initHeader);
</script>
```

**Update `src/layouts/BaseLayout.astro`:**
- Add import: `import Header from "../components/Header.astro";`
- Replace the `<!-- Header will be added in Plan 02 -->` comment with `<Header />`
- The body structure becomes:
  ```html
  <SkipToContent />
  <Header />
  <main id="main-content" class="flex-1 pt-16">
    <slot />
  </main>
  <Footer />
  ```
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Header.astro exists and contains `id="site-header"`
    - src/components/Header.astro contains `aria-label="Main navigation"`
    - src/components/Header.astro contains `>Jack Cutrara</a>` (namemark link)
    - src/components/Header.astro contains `href="/"` and `href="/about"` and `href="/projects"` and `href="/resume"` and `href="/contact"` (5 nav links)
    - src/components/Header.astro contains `aria-current` attribute logic
    - src/components/Header.astro contains `id="menu-trigger"` with `aria-label="Open menu"` and `aria-expanded="false"` and `aria-controls="mobile-menu"`
    - src/components/Header.astro contains `md:hidden` on hamburger and `hidden md:flex` on desktop nav
    - src/components/Header.astro contains `astro:page-load` in script
    - src/components/Header.astro contains `header-scrolled` and `header-hidden` CSS classes
    - src/components/Header.astro contains `requestAnimationFrame` for scroll throttling
    - src/components/Header.astro contains `.hamburger-line` CSS rules for X animation
    - src/layouts/BaseLayout.astro contains `import Header from "../components/Header.astro"`
    - src/layouts/BaseLayout.astro contains `<Header />` between SkipToContent and main
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Header renders on all pages with namemark "Jack Cutrara" (IDNV-01 partial), 5 desktop nav links with active indication (IDNV-02), hamburger trigger for mobile (IDNV-03 trigger), scroll-reveal hide/show behavior (D-01, D-02), and all interactive elements have focus-visible ring indicators (SEOA-05).</done>
</task>

<task type="auto">
  <name>Task 2: Create MobileMenu with full-screen overlay, focus trap, and keyboard accessibility</name>
  <files>
    src/components/MobileMenu.astro,
    src/layouts/BaseLayout.astro
  </files>
  <read_first>
    src/components/Header.astro,
    src/layouts/BaseLayout.astro,
    .planning/phases/02-site-shell-navigation/02-UI-SPEC.md,
    .planning/phases/02-site-shell-navigation/02-RESEARCH.md
  </read_first>
  <action>
**Create `src/components/MobileMenu.astro`** (per D-05, D-06, D-07, IDNV-03, SEOA-05):

**Frontmatter:**
```typescript
const currentPath = Astro.url.pathname;
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/projects", label: "Projects" },
  { href: "/resume", label: "Resume" },
  { href: "/contact", label: "Contact" },
];
```

**HTML structure:**
- Outer element: `<div id="mobile-menu" class="fixed inset-0 z-40 hidden flex-col items-center justify-center bg-bg-primary/[0.98]" role="dialog" aria-label="Navigation menu" aria-modal="true">`
- Close button (top right, same position as hamburger): `<button id="menu-close" class="absolute top-0 right-0 m-4 flex items-center justify-center w-11 h-11 text-text-primary focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none focus-visible:rounded" aria-label="Close menu">`
  - Inside close button: SVG X icon (two lines crossing), 20x20, using `currentColor`, `aria-hidden="true"` on SVG
  - Close X SVG: `<svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><line x1="4" y1="4" x2="16" y2="16" /><line x1="16" y1="4" x2="4" y2="16" /></svg>`
- `<nav aria-label="Mobile navigation" class="flex flex-col items-center gap-12">`
  - 5 `<a>` link elements (NOT wrapped in `<ul>`/`<li>` for the dramatic centered layout):
    - Each: `class:list={["font-body font-semibold text-[length:var(--token-text-heading)] transition-colors duration-200 focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none focus-visible:rounded px-4 py-2 mobile-menu-link", isActive ? "text-accent" : "text-text-primary hover:text-accent"]}`
    - Active link gets `aria-current="page"`
    - `isActive` logic same as Header: exact match for "/" , startsWith for others
    - Each link has `style={\`--link-index: ${index}\`}` for stagger animation (set index 0-4 per link)

**CSS for stagger animation** -- `<style>` block:
```css
#mobile-menu.menu-open .mobile-menu-link {
  animation: menuLinkIn 300ms ease-out forwards;
  animation-delay: calc(var(--link-index) * 50ms);
  opacity: 0;
}
@keyframes menuLinkIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
#mobile-menu.menu-closing {
  animation: menuFadeOut 150ms ease-in forwards;
}
@keyframes menuFadeOut {
  from { opacity: 1; }
  to { opacity: 0; }
}
```

**Script block** (per RESEARCH.md Pattern 3, Pitfalls 1, 2, 5):
```html
<script>
  function initMobileMenu() {
    const trigger = document.getElementById("menu-trigger");
    const menu = document.getElementById("mobile-menu");
    const closeBtn = document.getElementById("menu-close");
    if (!trigger || !menu || !closeBtn) return;

    function openMenu() {
      menu.classList.remove("hidden");
      menu.classList.add("flex", "menu-open");
      menu.classList.remove("menu-closing");
      trigger.setAttribute("aria-expanded", "true");
      trigger.setAttribute("aria-label", "Close menu");
      document.body.style.overflow = "hidden";
      closeBtn.focus();
      document.addEventListener("keydown", handleKeyDown);
    }

    function closeMenu(returnFocus = true) {
      menu.classList.add("hidden");
      menu.classList.remove("flex", "menu-open");
      trigger.setAttribute("aria-expanded", "false");
      trigger.setAttribute("aria-label", "Open menu");
      document.body.style.overflow = "";
      document.removeEventListener("keydown", handleKeyDown);
      if (returnFocus) trigger.focus();
    }

    function handleKeyDown(e: KeyboardEvent) {
      if (e.key === "Escape") {
        closeMenu();
        return;
      }
      if (e.key !== "Tab") return;

      const focusable = menu.querySelectorAll<HTMLElement>(
        'a[href], button, [tabindex]:not([tabindex="-1"])'
      );
      const first = focusable[0];
      const last = focusable[focusable.length - 1];

      if (e.shiftKey && document.activeElement === first) {
        e.preventDefault();
        last.focus();
      } else if (!e.shiftKey && document.activeElement === last) {
        e.preventDefault();
        first.focus();
      }
    }

    // Trigger open/close
    trigger.addEventListener("click", () => {
      const isOpen = trigger.getAttribute("aria-expanded") === "true";
      if (isOpen) {
        closeMenu();
      } else {
        openMenu();
      }
    });

    // Close button
    closeBtn.addEventListener("click", () => closeMenu());

    // Close on nav link click (Pitfall 2 -- prevent stale state)
    menu.querySelectorAll("a").forEach((link) => {
      link.addEventListener("click", () => closeMenu(false));
    });

    // Reset menu state on page load (Pitfall 2 -- View Transitions)
    // This handles the case where menu was open during navigation
    menu.classList.add("hidden");
    menu.classList.remove("flex", "menu-open", "menu-closing");
    trigger.setAttribute("aria-expanded", "false");
    trigger.setAttribute("aria-label", "Open menu");
    document.body.style.overflow = "";
  }

  document.addEventListener("astro:page-load", initMobileMenu);
</script>
```

**Update `src/layouts/BaseLayout.astro`:**
- Add import: `import MobileMenu from "../components/MobileMenu.astro";`
- Add `<MobileMenu />` after `<Header />` and before `<main>`:
  ```html
  <SkipToContent />
  <Header />
  <MobileMenu />
  <main id="main-content" class="flex-1 pt-16">
    <slot />
  </main>
  <Footer />
  ```
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/MobileMenu.astro exists and contains `id="mobile-menu"`
    - src/components/MobileMenu.astro contains `role="dialog"` and `aria-label="Navigation menu"` and `aria-modal="true"`
    - src/components/MobileMenu.astro contains `id="menu-close"` with `aria-label="Close menu"`
    - src/components/MobileMenu.astro contains `aria-label="Mobile navigation"` on the nav element
    - src/components/MobileMenu.astro contains all 5 nav link hrefs: `/`, `/about`, `/projects`, `/resume`, `/contact`
    - src/components/MobileMenu.astro contains `aria-current` logic for active page
    - src/components/MobileMenu.astro contains `focus-visible:ring-2 focus-visible:ring-accent` on interactive elements
    - src/components/MobileMenu.astro script contains `trapFocus` or `handleKeyDown` function with Tab key cycling logic
    - src/components/MobileMenu.astro script contains `e.key === "Escape"` handler
    - src/components/MobileMenu.astro script contains `document.body.style.overflow = "hidden"` (body lock)
    - src/components/MobileMenu.astro script contains `astro:page-load` listener
    - src/components/MobileMenu.astro contains `@keyframes menuLinkIn` animation
    - src/layouts/BaseLayout.astro contains `import MobileMenu from "../components/MobileMenu.astro"`
    - src/layouts/BaseLayout.astro contains `<MobileMenu />` between Header and main
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Full-screen mobile menu overlay with focus trap (IDNV-03), Escape-to-close, staggered link animations, body scroll lock, aria-modal dialog role, active page indication, and View Transitions state reset. All interactive elements have focus-visible indicators (SEOA-05). Hamburger button in Header toggles between hamburger and X states.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with 0 errors
- Generated HTML for each page contains `<header id="site-header">`
- Generated HTML for each page contains `<nav aria-label="Main navigation">`
- Generated HTML contains namemark `>Jack Cutrara</a>` linking to `/`
- Generated HTML contains 5 nav links with correct hrefs
- Generated HTML contains hamburger button with `aria-expanded`, `aria-controls`, `aria-label`
- Generated HTML contains `<div id="mobile-menu"` with `role="dialog"` and `aria-modal="true"`
- Generated HTML contains focus-visible ring classes on all interactive elements
- Script blocks include `astro:page-load` initialization for View Transitions compatibility
</verification>

<success_criteria>
- Header with "Jack Cutrara" namemark visible on all pages (IDNV-01 partial -- name on all pages, role in Phase 3 hero)
- Sticky header with 5 labeled nav links functional at desktop widths (IDNV-02)
- Scroll-reveal behavior: transparent at top, background on scroll, hides down / shows up (D-01, D-02)
- Mobile hamburger menu opens full-screen overlay at breakpoints below 768px (IDNV-03, D-05, D-07)
- Focus trap in mobile menu, Escape to close, aria-expanded toggling (IDNV-03, SEOA-05)
- Active page highlighted in accent color with aria-current="page" (SEOA-04)
- All interactive elements have visible focus-visible ring indicators (SEOA-05)
- Semantic nav landmarks with aria-label distinguish Main/Mobile/Footer navigation (SEOA-04)
- View Transitions compatible: scripts re-initialize on astro:page-load (Pitfall 1, 2)
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-shell-navigation/02-02-SUMMARY.md`
</output>
