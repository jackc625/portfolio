---
phase: 21-experience-content-pipeline-collection
plan: 04
type: execute
wave: 3
depends_on: [21-01, 21-03]
files_modified:
  - .github/workflows/sync-check.yml
  - docs/CONTENT-SCHEMA.md
autonomous: true
requirements: [EXP-01]

must_haves:
  truths:
    - "CI runs `pnpm sync:experience:check` on PRs that touch `Experience/**`, `src/content/experience/**`, or `scripts/sync-experience.mjs` — an independent drift gate mirroring the projects step (SC1, D-11)"
    - "`docs/CONTENT-SCHEMA.md` documents the experience pipeline: the D-01 frontmatter fields, the shared fence contract (noting H2/word-count checks do NOT apply per D-07), the `pnpm sync:experience` author workflow, and the exit-2/exit-1 failure modes"
    - "`pnpm sync:experience:check` exits 0 against the synced repo state (proves the CI step will pass, SC1)"
  artifacts:
    - .github/workflows/sync-check.yml (experience paths + verify step)
    - docs/CONTENT-SCHEMA.md (experience section)
  key_links:
    - "The new CI step runs AFTER the existing projects verify step, using the `pnpm sync:experience:check` script from Plan 01 (D-11 — independent, legible gates)"
    - "The three added path globs mirror the projects entries so experience-only PRs trigger the gate"
---

<objective>
Wire the experience pipeline into the CI drift gate and document it, completing the manual-sync + CI-`--check` pattern (D-11) exactly as projects does. This is the last seam of SC1 (drift enforcement lives in CI, not in `build`) and closes the EXP-01 pipeline contract.

Purpose: makes the experience sync self-policing on PRs and gives contributors a documented author workflow, matching the projects pipeline's legibility.

Output: three added path globs + a "Verify Experience" step in `.github/workflows/sync-check.yml`, and an experience section in `docs/CONTENT-SCHEMA.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-experience-content-pipeline-collection/21-CONTEXT.md
@.planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md

# Analogs — self-extend both files
@.github/workflows/sync-check.yml
@docs/CONTENT-SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the CI drift gate with the experience paths + verify step (D-11)</name>
  <files>.github/workflows/sync-check.yml</files>
  <read_first>
    - .github/workflows/sync-check.yml (existing `on.pull_request.paths` block + the "Verify Projects/ <-> MDX sync is clean" step running `pnpm sync:check` — the exact parallel)
    - .planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md (the CI additions, verbatim)
  </read_first>
  <action>
    Under `on.pull_request.paths`, ADD three globs mirroring the projects entries (D-11): `"Experience/**"`, `"src/content/experience/**"`, and `"scripts/sync-experience.mjs"`. Leave every existing path entry in place.

    Add a new step immediately AFTER the existing "Verify Projects/ <-> MDX sync is clean" step: name it "Verify Experience/ <-> MDX sync is clean" and set `run: pnpm sync:experience:check` (the Plan 01 script). Keep the two gates as separate, independent steps (D-11 — do not merge them). Do not touch the setup/install steps or the projects/chat-context steps.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/sync-check.yml','utf8');for(const t of ['Experience/**','src/content/experience/**','scripts/sync-experience.mjs','pnpm sync:experience:check','Verify Experience'])if(!y.includes(t))process.exit(1)" && pnpm sync:experience:check</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/sync-check.yml `on.pull_request.paths` includes `Experience/**`, `src/content/experience/**`, and `scripts/sync-experience.mjs`
    - A step named "Verify Experience/ <-> MDX sync is clean" runs `pnpm sync:experience:check`, placed after the projects verify step
    - The existing projects verify step and portfolio-context step are unchanged
    - `pnpm sync:experience:check` exits 0 against the synced repo state (the CI step will pass)
  </acceptance_criteria>
  <done>The CI drift gate triggers on experience-path PRs and runs the experience `--check` as an independent step; it exits 0 against current synced state.</done>
</task>

<task type="auto">
  <name>Task 2: Document the experience pipeline in CONTENT-SCHEMA.md (D-07)</name>
  <files>docs/CONTENT-SCHEMA.md</files>
  <read_first>
    - docs/CONTENT-SCHEMA.md (existing sections to parallel: §1 Frontmatter Schema table, §2 Sync Contract fence rules, §3 Author Workflow, §4 Failure-Mode Matrix)
    - src/content.config.ts (the experience schema — source of truth for the field table)
    - .planning/phases/21-experience-content-pipeline-collection/21-CONTEXT.md (D-01 field semantics, D-07 dropped checks)
  </read_first>
  <action>
    Add an experience section (or parallel subsections) mirroring the existing projects documentation, keeping code-wins framing (if the doc disagrees with `src/content.config.ts` or `scripts/sync-experience.mjs`, code wins).

    Document: (1) an experience frontmatter table for the D-01 fields (role, company, location, startDate, endDate optional, dateRange, techStack — may be empty, summary, highlights max 5, engagementType enum, hasCaseStudy, chatSummary optional, source) with the same "source string-shape only, existence validated by the sync script" note; (2) the shared fence contract reusing the same markers (D-08), explicitly noting that the 5-H2-shape and 600–900-word checks do NOT apply to experience (D-07) — experience bodies are freeform; (3) an author workflow referencing `pnpm sync:experience` and the CI `pnpm sync:experience:check` gate; (4) the failure-mode rows repointed to `Experience/*.md` (exit 2 hard-fails: missing/duplicate/out-of-order fence, missing `source:`, source not found, path escape; exit 1: drift in `--check`). Do NOT restate the deleted H2/word-count warning rows for experience.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('docs/CONTENT-SCHEMA.md','utf8');for(const t of ['experience','Experience/','sync:experience','engagementType','hasCaseStudy'])if(!d.toLowerCase().includes(t.toLowerCase()))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/CONTENT-SCHEMA.md documents the experience frontmatter fields (role/company/dates/techStack/summary/highlights/engagementType/hasCaseStudy/source)
    - The doc notes the H2-shape and word-count checks do NOT apply to experience (D-07)
    - The doc references `pnpm sync:experience` / `pnpm sync:experience:check` and the `Experience/*.md` → `src/content/experience/*.mdx` flow
    - `pnpm exec astro check` still passes (doc change does not affect the build)
  </acceptance_criteria>
  <done>The experience pipeline is documented in CONTENT-SCHEMA.md with the D-01 field table, the D-07-relaxed fence contract, the author workflow, and the failure-mode matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor PR → CI drift gate | A PR may change a fenced source or MDX body; the CI `--check` step detects divergence before merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Tampering | experience sync drift on PRs | medium | mitigate | The new `Verify Experience/ <-> MDX sync is clean` CI step runs `pnpm sync:experience:check` (exit 1 on drift), blocking merges where the MDX body diverges from its fenced source. |
| T-21-SC | Tampering | npm installs | n/a | accept | Zero installs; CI + docs only (SC4). |
</threat_model>

<verification>
- `pnpm sync:experience:check` exits 0 against the synced repo state (the CI step passes).
- The YAML retains all existing projects/chat-context paths and steps (additive only).
- `pnpm exec astro check` unaffected by the doc change.
</verification>

<success_criteria>
- SC1: drift enforcement for experience lives in CI as an independent `--check` step (not in `build`), mirroring projects (D-11).
- EXP-01: the pipeline is documented and self-policing, completing the mirror of the projects content pipeline.
</success_criteria>

## Artifacts this phase produces

New symbols/files created across Phase 21 (for source-grounding drift exclusion):
- `scripts/sync-experience.mjs` (NEW) — exports `normalize`, `readSourceField`, `sliceFrontmatter`, `extractFence`; internal `syncOne`, `main`
- `tests/scripts/sync-experience.test.ts` (NEW)
- `tests/scripts/sync-experience-check.test.ts` (NEW)
- `experience` collection + extended `collections` export in `src/content.config.ts` (MODIFY)
- `src/content/experience/holloway.mdx` (NEW)
- `src/content/experience/balfour-beatty.mdx` (NEW)
- `Experience/BALFOUR_BEATTY.md` (NEW)
- `Experience/HOLLOWAY_EXPERIENCE.md` fence markers (MODIFY)
- `sync:experience` + `sync:experience:check` scripts in `package.json` (MODIFY)
- "Verify Experience/ <-> MDX sync is clean" step + experience paths in `.github/workflows/sync-check.yml` (MODIFY — this plan)
- experience section in `docs/CONTENT-SCHEMA.md` (MODIFY — this plan)

<output>
Create `.planning/phases/21-experience-content-pipeline-collection/21-04-SUMMARY.md` when done.
</output>
