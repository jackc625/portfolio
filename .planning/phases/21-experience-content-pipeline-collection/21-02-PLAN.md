---
phase: 21-experience-content-pipeline-collection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
autonomous: true
requirements: [EXP-01, EXP-06]

must_haves:
  truths:
    - "The `experience` collection is registered and exported alongside `projects` in the `collections` object (SC2, Pitfall 6)"
    - "`astro check` validates experience frontmatter at build time; an empty collection (no entries yet) passes (SC2)"
    - "The schema accepts Holloway's omitted `endDate` (optional) and Balfour's empty `techStack` — the two optionality edges that would break if copied wrong (SC2)"
    - "`highlights` is bounded `.max(5)` with NO hard minimum so a lightweight 0–2-highlight entry still validates (A1 / D-03)"
  artifacts:
    - src/content.config.ts (experience collection block + extended collections export)
  key_links:
    - "`export const collections = { projects, experience }` — EXTEND the existing object, never replace it (Pitfall 6)"
    - "`techStack: z.array(z.string())` with NO `.min(1)` — copying the projects `.min(1)` fails on Balfour's `[]` (Pitfall 4)"
    - "plain-object `schema: z.object({...})` form (not the `({ image }) =>` function form — experience has no image field, A3)"
---

<objective>
Register a rich, forward-compatible `experience` content collection in `src/content.config.ts` with the D-01 Zod schema, so Phases 22 and 25 need no later schema change. This defines the typed field contract (SC2) and the sortable-date model (SC3/EXP-06) the Experience surface renders against.

Purpose: SC2's build-time validation gate. The schema is the typed contract; the two real entries (authored in Plan 03) double as its optionality-edge fixtures (omitted `endDate`, empty `techStack`).

Output: an `experience` `defineCollection` block and an extended `collections` export.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-experience-content-pipeline-collection/21-CONTEXT.md
@.planning/phases/21-experience-content-pipeline-collection/21-RESEARCH.md
@.planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md

# Analog — the existing projects block is the self-extend template
@src/content.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the experience collection schema (D-01)</name>
  <files>src/content.config.ts</files>
  <read_first>
    - src/content.config.ts (the existing `projects` block is the self-extend template; note the `glob({ pattern, base })` loader, the `source: z.string()` script-validated convention, and the `collections` export)
    - .planning/phases/21-experience-content-pipeline-collection/21-RESEARCH.md (§Schema Field Types & Optionality Edges — the per-field disposition table)
    - .planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md (the exact experience block to add)
  </read_first>
  <action>
    Keep the existing imports and the entire `projects` block unchanged. Add a new `const experience = defineCollection({...})` using the plain-object schema form `schema: z.object({...})` (NOT the `({ image }) =>` function form — experience has no image field, A3). Loader: `glob({ pattern: "**/*.mdx", base: "./src/content/experience" })` (D-09).

    Schema fields exactly per D-01 (types are Claude's-discretion within the D-01 field list — lock these): `role: z.string()`, `company: z.string()`, `location: z.string()`, `startDate: z.coerce.date()` (D-04 sortable), `endDate: z.coerce.date().optional()` (absence ⇒ present, D-01 — omitted entirely from Holloway; never an empty string), `dateRange: z.string()` (display-only, decoupled from sort keys, D-05), `techStack: z.array(z.string())` with NO `.min(1)` (Balfour is `[]`, D-10 / Pitfall 4), `summary: z.string()` (first-person site voice), `highlights: z.array(z.string()).max(5)` with NO hard min (A1 — so the lightweight Balfour entry with 0–2 highlights validates while Holloway carries 3–5, D-03), `engagementType: z.enum(["contract", "internship"])` (exact lowercase literals, D-01/D-10), `hasCaseStudy: z.boolean()`, `chatSummary: z.string().optional()` (content deferred to Phase 25, D-02 — omitted from both entries now, stays green empty), `source: z.string()` (existence validated by the sync script, not Zod — matches the projects D-15 convention; do NOT add a filesystem refinement).

    EXTEND the export to `export const collections = { projects, experience };` — add to the existing object, do NOT overwrite or drop `projects` (Pitfall 6).
  </action>
  <verify>
    <automated>pnpm exec astro check</automated>
  </verify>
  <acceptance_criteria>
    - src/content.config.ts exports `collections` containing BOTH `projects` and `experience`
    - `grep -c "z.array(z.string()).min(1)" src/content.config.ts` returns 1 (projects keeps it; experience does NOT add a second — Pitfall 4)
    - `grep -c "engagementType: z.enum(\[\"contract\", \"internship\"\])" src/content.config.ts` returns 1
    - `grep -c "highlights: z.array(z.string()).max(5)" src/content.config.ts` returns 1 (A1 bound, no `.min`)
    - `grep -c "endDate: z.coerce.date().optional()" src/content.config.ts` returns 1
    - `pnpm exec astro check` passes (empty experience collection is valid — no entries yet)
  </acceptance_criteria>
  <done>The `experience` collection is registered with the full D-01 schema, the A1 `highlights.max(5)`-no-min and empty-`techStack` edges are in place, both collections export, and `astro check` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → build (`astro check`) | Frontmatter authored in `Experience/*.md`-derived `.mdx` files crosses into the typed render contract. Build-time validation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Tampering | `experience` Zod schema in `src/content.config.ts` | medium | mitigate | Every experience entry's frontmatter is validated by Zod at build via `astro check`; untyped/invalid frontmatter fails the build before it can reach a render surface. Optionality edges (`endDate` optional, `techStack` unbounded, `highlights.max(5)`) are exact so valid content is not falsely rejected. |
| T-21-SC | Tampering | npm installs | n/a | accept | Zero package installs this phase; uses the already-installed `astro/zod` re-export (SC4). |
</threat_model>

<verification>
- `pnpm exec astro check` is 0/0/0 with the new collection registered (empty collection valid).
- The projects block is byte-unchanged (only additive edits): `grep -c "z.array(z.string()).min(1)" src/content.config.ts` returns 1.
</verification>

<success_criteria>
- SC2: typed frontmatter contract exists and is enforced by the `astro check` build gate.
- EXP-06: `startDate` (sortable) + `dateRange` (display) + `highlights[]` (at-a-glance) fields are typed and available for the reverse-chron render contract.
</success_criteria>

## Artifacts this phase produces

New symbols/files created across Phase 21 (for source-grounding drift exclusion):
- `scripts/sync-experience.mjs` (NEW) — exports `normalize`, `readSourceField`, `sliceFrontmatter`, `extractFence`; internal `syncOne`, `main`
- `tests/scripts/sync-experience.test.ts` (NEW)
- `tests/scripts/sync-experience-check.test.ts` (NEW)
- `experience` collection + extended `collections` export in `src/content.config.ts` (MODIFY — this plan)
- `src/content/experience/holloway.mdx` (NEW)
- `src/content/experience/balfour-beatty.mdx` (NEW)
- `Experience/BALFOUR_BEATTY.md` (NEW)
- `Experience/HOLLOWAY_EXPERIENCE.md` fence markers (MODIFY)
- `sync:experience` + `sync:experience:check` scripts in `package.json` (MODIFY)
- "Verify Experience/ <-> MDX sync is clean" step + experience paths in `.github/workflows/sync-check.yml` (MODIFY)
- experience section in `docs/CONTENT-SCHEMA.md` (MODIFY)

<output>
Create `.planning/phases/21-experience-content-pipeline-collection/21-02-SUMMARY.md` when done.
</output>
