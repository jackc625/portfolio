---
phase: 21-experience-content-pipeline-collection
plan: 03
type: execute
wave: 2
depends_on: [21-01, 21-02]
files_modified:
  - Experience/BALFOUR_BEATTY.md
  - Experience/HOLLOWAY_EXPERIENCE.md
  - src/content/experience/holloway.mdx
  - src/content/experience/balfour-beatty.mdx
autonomous: true
requirements: [EXP-01, EXP-06]

must_haves:
  truths:
    - "Both experience entries exist (Holloway full deep-dive, Balfour lightweight) and their MDX bodies are synced byte-for-byte from the `Experience/*.md` fenced sources (SC1)"
    - "The synced `holloway.mdx` body opens with the `Contract engagement` lede — the fence-start sits immediately after the H1 so the blockquote + Overview + Highlights + Themes all land in the body (A2 / D-08)"
    - "`pnpm sync:experience:check` exits 0 immediately after `pnpm sync:experience` — the sync is idempotent, no drift (SC1)"
    - "`pnpm build` succeeds with the two entries wired into the collection and no new runtime dependencies (SC4)"
    - "Entries carry role, company, dates, and tech/skills as typed frontmatter validated by `astro check`; Holloway omits `endDate`, Balfour uses `techStack: []` (SC2 / EXP-06)"
  artifacts:
    - Experience/BALFOUR_BEATTY.md
    - Experience/HOLLOWAY_EXPERIENCE.md (fence markers added)
    - src/content/experience/holloway.mdx
    - src/content/experience/balfour-beatty.mdx
  key_links:
    - "`source:` frontmatter values resolve to `Experience/HOLLOWAY_EXPERIENCE.md` and `Experience/BALFOUR_BEATTY.md` (the sync exits 2 if either is missing — Pitfall 3)"
    - "Holloway fence-start immediately after the H1 line; fence-end at EOF (A2)"
    - "Balfour source must exist with its fence BEFORE the first `pnpm sync:experience` (Pitfall 3)"
---

<objective>
Populate the two experience entries end-to-end: add fence markers to the existing Holloway source, author the new lightweight Balfour source, create both `.mdx` files with D-01 frontmatter, and run the sync to fill their bodies. Proves SC1 (idempotent real-entry sync), SC2 (typed frontmatter validated), and SC4 (build green, no new deps) with the two entries doubling as the optionality-edge fixtures (omitted `endDate`, empty `techStack`).

Purpose: turns the mechanism (Plan 01) and the schema (Plan 02) into the two real entries the Phase 22 Experience page renders against.

Output: `Experience/BALFOUR_BEATTY.md` (NEW), Holloway fence markers, and `src/content/experience/{holloway,balfour-beatty}.mdx` with synced bodies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-experience-content-pipeline-collection/21-CONTEXT.md
@.planning/phases/21-experience-content-pipeline-collection/21-RESEARCH.md
@.planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md
@.planning/phases/21-experience-content-pipeline-collection/21-VALIDATION.md

# The one existing source (add fence markers) + the frontmatter shape to parallel
@Experience/HOLLOWAY_EXPERIENCE.md
@src/content/projects/seatwatch.mdx
@docs/VOICE-GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the fenced Experience sources (Balfour NEW, Holloway fence)</name>
  <files>Experience/BALFOUR_BEATTY.md, Experience/HOLLOWAY_EXPERIENCE.md</files>
  <read_first>
    - Experience/HOLLOWAY_EXPERIENCE.md (existing source — H1 on line 1, the `Contract engagement` blockquote on line 3; currently NO fence markers)
    - .planning/phases/21-experience-content-pipeline-collection/21-CONTEXT.md (Specific Ideas — authoritative Balfour resume facts + Holloway stack/dates)
    - .planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md (fence placement guidance, MDX-safety Pitfall 2)
    - docs/VOICE-GUIDE.md (first-person site voice governs the Balfour body)
  </read_first>
  <action>
    Holloway (MODIFY, D-08 / A2): insert the fence-start marker on its own line IMMEDIATELY AFTER the H1 line and BEFORE the `Contract engagement` blockquote, so the blockquote lede plus Overview, Highlights, and Themes all fall inside the fenced body (the blockquote is part of the deep-dive Phase 22 renders — A2). Insert the fence-end marker on its own line at end of file. Use the exact marker strings defined as FENCE_START/FENCE_END in scripts/sync-experience.mjs (D-08). Change nothing else in the body. Each marker must appear exactly once (the sync exits 2 on duplicates). Scan the fenced region for any bare `<` or `{` that MDX would misparse (Pitfall 2) — the existing body uses arrow glyphs and backticked identifiers, which are safe; do not introduce comparison-operator literals.

    Balfour (NEW, D-10 / Pitfall 3): create `Experience/BALFOUR_BEATTY.md` with an H1 (e.g. `# Balfour Beatty — Project Management Internship`), then a fenced block wrapping a 1–2 line first-person description drawn from the resume facts in CONTEXT Specific Ideas (tracked deliverables and subcontractor timelines across active construction workstreams; coordinated engineers, subcontractors, and clients to keep multi-phase milestones on schedule; led stakeholder meetings and prepared status reports). First-person site voice per VOICE-GUIDE.md. This file MUST exist with its fence before the first `pnpm sync:experience` or the sync exits 2 (source file not found). Author with LF line endings (Pitfall 5).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('Experience/HOLLOWAY_EXPERIENCE.md','utf8');const b=fs.readFileSync('Experience/BALFOUR_BEATTY.md','utf8');const S='CASE-STUDY-START',E='CASE-STUDY-END';const c=(s,m)=>s.split(m).length-1;if(c(h,S)!==1||c(h,E)!==1||c(b,S)!==1||c(b,E)!==1)process.exit(1);const si=h.indexOf(S),li=h.indexOf('Contract engagement');if(si===-1||li===-1||si>li)process.exit(2)"</automated>
  </verify>
  <acceptance_criteria>
    - Experience/HOLLOWAY_EXPERIENCE.md contains exactly one fence-start and one fence-end marker
    - The Holloway fence-start marker appears BEFORE the `Contract engagement` blockquote text (A2 placement)
    - Experience/BALFOUR_BEATTY.md exists with exactly one fence-start and one fence-end marker wrapping a 1–2 line first-person description
    - Neither fenced body introduces bare `<`/`{` comparison literals (MDX-safe, Pitfall 2)
  </acceptance_criteria>
  <done>Holloway carries fence markers with the lede inside the fence (A2); the new Balfour source exists with its fenced 1–2 line body; both are ready for sync.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two MDX entries, run the sync, and prove the build + idempotency</name>
  <files>src/content/experience/holloway.mdx, src/content/experience/balfour-beatty.mdx</files>
  <read_first>
    - src/content/projects/seatwatch.mdx (frontmatter shape to parallel: `---` delimiters, quoted strings, array formatting, the `source:` field)
    - src/content.config.ts (the experience schema from Plan 02 — the exact required fields to satisfy)
    - .planning/phases/21-experience-content-pipeline-collection/21-CONTEXT.md (Specific Ideas — Holloway role/company/location/stack/dates and candidate highlights; Balfour facts)
    - .planning/phases/21-experience-content-pipeline-collection/21-RESEARCH.md (Schema Field Types & Optionality Edges — omit `endDate` from Holloway; `techStack: []` for Balfour)
  </read_first>
  <action>
    Create both `.mdx` files with ONLY a frontmatter block plus a one-line placeholder body (the sync overwrites the body from the fenced source). Do NOT hand-author the bodies — they are machine-written (docs/CONTENT-SCHEMA.md forbids hand-editing MDX bodies). Each file must start with `---` and have a closing `---` so `sliceFrontmatter` can split it.

    holloway.mdx frontmatter (D-01 fields, first-person `summary` per VOICE-GUIDE.md): `role: "Software Engineer, Contract"`, `company: "The Holloway Company"`, `location: "Northern Virginia"`, `startDate: "2026-05"` (quoted month string coerces via z.coerce.date), OMIT `endDate` entirely (present role — never an empty string), `dateRange: "May 2026 – Present"` (D-05), `techStack` array from CONTEXT (JavaScript, TypeScript, Deno, React, TanStack Query, Base44, Vitest, GitHub Actions), first-person `summary`, `highlights` of 3–5 headline bullets selected from the CONTEXT candidate list (0→~1,400 tests / money math; RLS across 47 entities + 223→1 portal scoping; recovered 91 archived jobs; idempotent geofenced payroll clock; consolidated data-access layer), `engagementType: "contract"`, `hasCaseStudy: true`, `source: "Experience/HOLLOWAY_EXPERIENCE.md"`. Omit `chatSummary` (D-02).

    balfour-beatty.mdx frontmatter (D-10): `role: "Project Management Intern"`, `company: "Balfour Beatty"`, `location: "Chantilly, VA"`, `startDate: "2023-05"`, `endDate: "2023-08"`, `dateRange: "May 2023 – Aug 2023"`, `techStack: []` (empty array — non-engineering, D-10 / Pitfall 4), first-person `summary`, `highlights` of 0–2 bullets (A1 allows a light entry), `engagementType: "internship"`, `hasCaseStudy: false`, `source: "Experience/BALFOUR_BEATTY.md"`. Omit `chatSummary`.

    Then run `pnpm sync:experience` to populate both bodies from the fenced sources, run `pnpm sync:experience:check` (must report no drift, exit 0 — idempotency, SC1), and run `pnpm build` (green with the collection wired in, SC4). Verify the synced holloway.mdx body opens with the `Contract engagement` lede (A2). Confirm no new runtime dependencies were added.
  </action>
  <verify>
    <automated>pnpm sync:experience && pnpm sync:experience:check && pnpm build && node -e "const fs=require('fs');const m=fs.readFileSync('src/content/experience/holloway.mdx','utf8');const body=m.slice(m.indexOf('---',3)+3);if(!/Contract engagement/.test(body))process.exit(1);const p=require('./package.json');if(Object.keys(p.dependencies).length!==11||Object.keys(p.devDependencies).length!==12)process.exit(2)"</automated>
  </verify>
  <acceptance_criteria>
    - src/content/experience/holloway.mdx validates against the schema with `endDate` omitted and `hasCaseStudy: true`; body opens with the `Contract engagement` lede (A2, EXP-04 depth preserved for Phase 22)
    - src/content/experience/balfour-beatty.mdx validates with `techStack: []`, `hasCaseStudy: false`, `engagementType: "internship"` (D-10)
    - `pnpm sync:experience:check` exits 0 immediately after `pnpm sync:experience` (idempotent, no drift — SC1)
    - `pnpm build` succeeds (astro check 0/0/0 on the new collection — SC2/SC4)
    - package.json dependencies stay at 11 / devDependencies at 12 (no new runtime deps — SC4)
  </acceptance_criteria>
  <done>Both entries validate, sync idempotently, and the full build is green; the Holloway lede is inside the synced body; the dependency graph is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → sync script | The `source:` values in the two `.mdx` files are author-controlled paths the sync reads. Build-time only. |
| author → build (`astro check`) | The two entries' frontmatter crosses into the typed render contract at build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information Disclosure | `source:` path resolution when `pnpm sync:experience` runs over the two entries | medium | mitigate | The in-repo `source:` values point only at `Experience/*.md`; the lifted path-traversal guard (Plan 01) would exit 2 on any escape. |
| T-21-02 | Tampering | Holloway + Balfour frontmatter validated at build | medium | mitigate | `astro check` runs the Plan 02 Zod schema against both entries; the two entries exercise the real optionality edges (omitted `endDate`, empty `techStack`) so a wrong schema fails the build. |
| T-21-SC | Tampering | npm installs | n/a | accept | Zero installs; content-only. Dependency counts asserted unchanged (SC4). |
</threat_model>

<verification>
- `pnpm sync:experience:check` exits 0 (idempotent) and `pnpm build` is green with both entries.
- The synced `holloway.mdx` body opens with the `Contract engagement` lede (A2 — the one Manual-Only check in 21-VALIDATION.md, here proxied by the automated body grep).
- package.json runtime/dev dependency counts are unchanged (SC4).

<human-check>
Open `src/content/experience/holloway.mdx` after sync and confirm the body reads correctly: it opens with the "Contract engagement" lede and includes Overview → Highlights → Themes (A2 authoring judgment — a passing sync proves the fence extracted, the grep proves the lede landed, but a human confirms the intended deep-dive content is what got fenced).
</human-check>
</verification>

<success_criteria>
- SC1: both real entries sync idempotently (`--check` exits 0 after a write).
- SC2: typed frontmatter for both entries passes `astro check`.
- SC4: `pnpm build` green, no new runtime dependencies.
- EXP-06: role, company, dates, and tech/skills present as typed at-a-glance frontmatter on both entries.
</success_criteria>

## Artifacts this phase produces

New symbols/files created across Phase 21 (for source-grounding drift exclusion):
- `scripts/sync-experience.mjs` (NEW) — exports `normalize`, `readSourceField`, `sliceFrontmatter`, `extractFence`; internal `syncOne`, `main`
- `tests/scripts/sync-experience.test.ts` (NEW)
- `tests/scripts/sync-experience-check.test.ts` (NEW)
- `experience` collection + extended `collections` export in `src/content.config.ts` (MODIFY)
- `src/content/experience/holloway.mdx` (NEW — this plan)
- `src/content/experience/balfour-beatty.mdx` (NEW — this plan)
- `Experience/BALFOUR_BEATTY.md` (NEW — this plan)
- `Experience/HOLLOWAY_EXPERIENCE.md` fence markers (MODIFY — this plan)
- `sync:experience` + `sync:experience:check` scripts in `package.json` (MODIFY)
- "Verify Experience/ <-> MDX sync is clean" step + experience paths in `.github/workflows/sync-check.yml` (MODIFY)
- experience section in `docs/CONTENT-SCHEMA.md` (MODIFY)

<output>
Create `.planning/phases/21-experience-content-pipeline-collection/21-03-SUMMARY.md` when done.
</output>
