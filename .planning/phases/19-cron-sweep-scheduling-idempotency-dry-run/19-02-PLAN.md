---
phase: 19-cron-sweep-scheduling-idempotency-dry-run
plan: 02
type: tdd
wave: 1
depends_on:
  - 19-01
files_modified:
  - src/lib/chat-delivery.ts
  - tests/api/chat-delivery.test.ts
autonomous: true
requirements:
  - CRON-02
  - CRON-03
  - CRON-04
user_setup: []

must_haves:
  truths:
    - "Calling `deliverDue(env)` with mock KV containing one `live:{sid}` whose `metadata.last_activity_at` is older than 2h promotes it: PUT `delivered:{sid}` is observed BEFORE the would-be send call, and DELETE `live:{sid}` is observed AFTER the dry-run success"
    - "Under `env.DRY_RUN === \"1\"` the inner send is a single `console.log(\"chat.delivery.dry_run\", {...})` line with the locked D-05 field NAMES (`sid, to, from, reply_to, msg_count, truncated, country, referrer_host, dry_run`); no Resend POST exists in this plan and the would-be call is unreachable at runtime"
    - "A second call to `deliverDue(env)` over the same KV state (where `delivered:{sid}` is still present within its 24h TTL) reports `sessions_promoted: 0` for that sid and emits a `chat.delivery.skipped_already_delivered { sid, delivered_at_existing }` log line"
    - "Per-tick batch cap fires at 50 sessions exactly; pagination hard-cap fires at 50 pages exactly; max 3 send-attempt retries enforced; per-session try/catch isolates failures so one bad session never aborts the sweep"
    - "Structured per-tick summary log `chat.delivery.tick { sessions_seen, sessions_due, sessions_promoted, errors, pages_scanned, elapsed_ms }` is emitted once per `deliverDue` call with flat primitives only"
  artifacts:
    - path: "src/lib/chat-delivery.ts"
      provides: "deliverDue entry point + two-keyspace promotion loop + DRY_RUN-gated envelope log harness"
      exports: ["deliverDue", "INACTIVITY_THRESHOLD_MS", "PER_TICK_BATCH_CAP", "PAGINATION_PAGE_HARDCAP", "MAX_SEND_ATTEMPTS", "DELIVERED_TTL_SECONDS", "DeliveredMarker"]
      min_lines: 150
    - path: "tests/api/chat-delivery.test.ts"
      provides: "Unit test battery for deliverDue with mock KV; covers all CRON-02/03/04 invariants enumerated in 19-PATTERNS.md test table (19 cases)"
      min_lines: 350
  key_links:
    - from: "src/lib/chat-delivery.ts"
      to: "src/lib/chat-transcripts.ts"
      via: "type-only import of `ChatTranscript` + `KVMetadata` + value import of `KEY_PREFIX`"
      pattern: "import\\s+(?:type\\s+)?\\{[^}]*\\b(?:ChatTranscript|KVMetadata|KEY_PREFIX)\\b[^}]*\\}\\s+from\\s+[\"']\\./chat-transcripts[\"']"
    - from: "deliverDue promoteOne step 4"
      to: "deliverDue promoteOne step 5"
      via: "ordering invariant — PUT delivered:{sid} MUST happen BEFORE kv.delete(live:{sid})"
      pattern: "kv\\.put\\(\\s*[`'\"]delivered:[\\s\\S]*?kv\\.delete\\(\\s*[`'\"]\\$\\{KEY_PREFIX\\}|kv\\.put\\([^)]*delivered[^)]*\\)[^;]*;[\\s\\S]{0,2000}kv\\.delete"
    - from: "deliverDue env.DRY_RUN check"
      to: "sendOne envelope log emission"
      via: "strict-equals-string gate per D-02"
      pattern: "env\\.DRY_RUN\\s*===\\s*[\"']1[\"']"
---

<objective>
Create the heart of Phase 19: NEW pure module `src/lib/chat-delivery.ts` exporting `deliverDue(env, scheduledTime?)` and its companion unit test file `tests/api/chat-delivery.test.ts`. Implement the full two-keyspace promotion loop (`live:` → `delivered:`) with DRY_RUN-gated send harness, per-session try/catch isolation, 50-session batch cap, 50-page pagination hard-cap, 3-try retry harness, and the four `chat.delivery.*` structured Workers Logs.

Purpose: This plan owns the entire Phase 19 sweep contract minus the worker.ts wiring (Plan 19-03) and the cron flip (Plan 19-04). Once this plan lands, `deliverDue` is exported, type-safe, and fully tested in isolation against a mock KV — Plan 19-03 plugs it into `src/worker.ts` `scheduled()` and Plan 19-04 turns on the cron.

TDD approach: tests/api/chat-delivery.test.ts is authored BEFORE (or alongside) the module body — every test case enumerated below maps to a specific assertion in the locked invariant set. The RED→GREEN cycle proves each invariant exactly once.

Output: 1 new module (~150–250 LOC), 1 new test file (~350–500 LOC with 19 test cases), 0 file modifications. Zero new runtime dependencies. Zero chat-surface files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-CONTEXT.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-RESEARCH.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-VALIDATION.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-01-SUMMARY.md
@src/lib/chat-transcripts.ts
@tests/api/chat-transcripts.test.ts

<interfaces>
<!-- The Phase 18 sibling module `src/lib/chat-transcripts.ts` is the canonical analog -->
<!-- (19-PATTERNS.md explicitly names it). Plan 19-02 imports types from it and mirrors its file shape. -->

Type-only imports from src/lib/chat-transcripts.ts (Phase 18 — UNCHANGED in Phase 19):
- `export interface ChatTranscript { v: 1; sid: string; started_at: string; last_activity_at: string; msg_count: number; truncated: boolean; meta: { referrer?: string; user_agent?: string; country?: string; region?: string; colo?: string; }; messages: ChatMessage[]; }`
- `export interface KVMetadata { last_activity_at: string; msg_count: number; window_started_at: string; window_count: number; }`
- `export const KEY_PREFIX = "live:";`
- `export const TRANSCRIPT_TTL_SECONDS = 30 * 24 * 3600;` (Phase 18 — NOT consumed by Plan 19-02)

Type-only imports from src/worker.ts Env shape (Plan 19-01 added DRY_RUN):
- `interface Env { ASSETS: Fetcher; CHAT_KV: KVNamespace; ANTHROPIC_API_KEY: string; RESEND_API_KEY: string; CHAT_RECIPIENT_EMAIL: string; CHAT_SENDER_EMAIL: string; CHAT_RATE_LIMITER?: RateLimit; DRY_RUN: string; }`
- Plan 19-02 declares its OWN narrowed `DeliveryEnv` interface (NOT imported from worker.ts to avoid cyclic-import — structural-typing compatibility is what matters) with fields: `CHAT_KV: KVNamespace; DRY_RUN: string; CHAT_RECIPIENT_EMAIL?: string; CHAT_SENDER_EMAIL?: string;`

NEW public surface from src/lib/chat-delivery.ts (Plan 19-02 ADDS):
- `export async function deliverDue(env: DeliveryEnv, scheduledTime?: number): Promise<void>`
- `export interface DeliveredMarker { v: 1; sid: string; delivered_at: string; dry_run: boolean; msg_count: number; truncated: boolean; }`
- `export const INACTIVITY_THRESHOLD_MS = 2 * 60 * 60 * 1000`
- `export const PER_TICK_BATCH_CAP = 50`
- `export const PAGINATION_PAGE_HARDCAP = 50`
- `export const MAX_SEND_ATTEMPTS = 3`
- `export const DELIVERED_TTL_SECONDS = 24 * 3600`
- `export const BACKOFF_BASE_MS = 250` (planner's discretion per D-07; full-jitter exponential per RESEARCH OQ-2)
- `export const BACKOFF_CAP_MS = 5000`

Internal-only (NOT exported, per pure-module sibling convention in chat-transcripts.ts):
- `async function promoteOne(env, sid, scheduledAt): Promise<{ status: "promoted" | "already_delivered" | "missing_live" | "error" }>` — owns the 5-step ordering invariant (read delivered → load live → send → put delivered → delete live)
- `async function sendOne(env, transcript): Promise<void>` — DRY_RUN gate; envelope log under `"1"`; `throw new Error("send_not_implemented_in_phase_19")` under `"0"` (Phase 20 replaces this branch with real Resend POST)
- `async function retryWithBackoff<T>(fn, maxAttempts): Promise<T>` — 3-try loop with exponential full-jitter backoff
- `function hostnameOrNull(url?: string): string | null` — referrer_host extraction (try-catch around `new URL(url).hostname`; returns null for missing/malformed)

NO imports from (per 19-PATTERNS.md anti-coupling list):
- `@anthropic-ai/sdk` (no LLM surface in cron path)
- `cloudflare:workers` (Env passed via param, not virtual-module reach-in)
- `src/prompts/`, `src/data/portfolio-context.json` (TEST-03 anchor anti-coupling)
- `src/pages/`, `src/scripts/chat.ts` (D-26 chat-surface anchor)
- `src/lib/email/` (Phase 20 creates this; D-06)
</interfaces>

<patterns>
<!-- See 19-PATTERNS.md "src/lib/chat-delivery.ts (NEW)" section for the full verbatim shape -->
<!-- of the module: header docblock, locked-constants block, types, two-keyspace ordering -->
<!-- (promoteOne 5 steps), pagination loop, DRY_RUN envelope log, retry harness, tick summary. -->

<!-- The test file pattern is enumerated in 19-PATTERNS.md "tests/api/chat-delivery.test.ts (NEW)" -->
<!-- — 19 test cases mapped to invariants, MockKVNamespace extension (add delete + cursor-pagination -->
<!-- list semantics — the Phase 18 mock is single-page). -->

<!-- Sibling analog references (do NOT recopy from these; READ them for shape): -->
<!--   - src/lib/chat-transcripts.ts (Phase 18 — pure-module pattern verbatim) -->
<!--   - tests/api/chat-transcripts.test.ts (Phase 18 — MockKVNamespace pattern + console-spy pattern) -->
</patterns>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Author tests/api/chat-delivery.test.ts (RED) — 19 unit-test cases against an extended MockKVNamespace</name>
  <files>tests/api/chat-delivery.test.ts</files>
  <read_first>
    - tests/api/chat-transcripts.test.ts (READ FULL — the canonical sibling test file; the MockKVNamespace class definition, console-spy beforeEach/afterEach pattern, hard-coded `SID = "8b0f7f1c-1234-4567-8901-abcdef012345"` fixture, and import header MUST be inherited byte-for-byte where applicable)
    - src/lib/chat-transcripts.ts (READ FULL — verify the exact ChatTranscript / KVMetadata / KEY_PREFIX shapes for type-import correctness; these shapes are what the test fixtures must construct)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md (READ "tests/api/chat-delivery.test.ts (NEW)" section — the full 19-case test table at lines ~520-550 with the "Closes" column locking each test to a specific decision/requirement)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-VALIDATION.md (READ Per-Task Verification Map — the `-t "..."` test names in the automated commands ARE the test descriptions that must be authored: "filters by inactivity", "ordering: PUT delivered before send", "ordering: DELETE live after success", "envelope shape", "24h TTL", "dry_run logs envelope", "idempotency cursor skip", "batch cap 50", "pagination cap 50 pages", "retry harness 3 attempts", "per-session isolation", "tick summary log", "skipped log")
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-CONTEXT.md (READ <decisions> section A.D-01/02, B.D-05/06/07, C.D-09/10/11 to anchor each test to a specific decision ID)
  </read_first>
  <behavior>
    Author 19 test cases organized into describe blocks corresponding to the requirement groups. Each test sets up mock KV state, invokes `deliverDue(env, scheduledTime)`, then asserts behavior via storage inspection + console spy.

    Test cases (each name maps to a `-t "<name>"` filter command listed in 19-VALIDATION.md):

    GROUP A — CRON-02 list + filter:
      1. "filters by inactivity": session with `metadata.last_activity_at` 30min stale (`Date.now() - 30*60*1000`) → NOT promoted; `sessions_promoted: 0` in tick log
      2. "filters by inactivity (3h stale promoted)": session with `last_activity_at` 3h stale → IS promoted; `sessions_promoted: 1`
      3. "lists only live: prefix": MockKV records `list({prefix})` calls — assert only `prefix: "live:"` was ever passed (NOT `delivered:`)

    GROUP B — CRON-02 ordering invariant (D-09):
      4. "ordering: PUT delivered before send": instrument sendOne via dependency-injection OR by spying on console.log call order vs kv.put call order → assert `kv.put("delivered:...")` storage entry exists BEFORE the `chat.delivery.dry_run` log line is emitted (capture timestamps or use a recorded operation log on MockKV)
      5. "ordering: DELETE live after success": assert `live:{sid}` key was DELETED after `delivered:{sid}` was PUT and after the dry_run log emitted

    GROUP C — CRON-02 envelope value shape (D-09 / D-10):
      6. "envelope shape": `JSON.parse(MockKV.storage.get("delivered:" + SID).value)` deep-equals `{ v: 1, sid: SID, delivered_at: <ISO 8601 string>, dry_run: true, msg_count: <number>, truncated: <boolean> }` (no extra keys, no missing keys)
      7. "24h TTL": MockKV `put` options arg for `delivered:{sid}` key has `expirationTtl === DELIVERED_TTL_SECONDS` (= 24*3600 = 86400)
      8. "no metadata on delivered: writes" (D-11): MockKV `put` options arg for `delivered:{sid}` key has NO `metadata` field (`options.metadata === undefined`)

    GROUP D — CRON-04 DRY_RUN gate (D-01 / D-02 / D-05):
      9. "dry_run logs envelope": with `env.DRY_RUN === "1"`, console.log called with first-arg `"chat.delivery.dry_run"` and second-arg object containing the locked field names — assert exact key set `{sid, to, from, reply_to, msg_count, truncated, country, referrer_host, dry_run}` (use `Object.keys(call[1]).sort()` deep-equal); `dry_run` field must equal `true`
      10. "dry_run gate (env.DRY_RUN !== '1' throws)": with `env.DRY_RUN === "0"`, calling deliverDue with a due session causes promoteOne to log `chat.delivery.failed` (per-session try/catch catches the `send_not_implemented_in_phase_19` throw) — proves the gate is strict-equals-string per D-02

    GROUP E — CRON-02 idempotency cursor:
      11. "idempotency cursor skip": pre-seed `delivered:{sid}` with `{v:1, sid, delivered_at: "<2h ago iso>", dry_run: true, msg_count: 1, truncated: false}` → call deliverDue → `sessions_promoted: 0`, `chat.delivery.skipped_already_delivered` log emitted with `{ sid, delivered_at_existing: "<the ISO string>" }`
      12. "missing live: graceful skip": `delivered:{sid}` absent, `live:{sid}` absent (race) — should be skipped with no error, no log spam

    GROUP F — CRON-03 batch cap + pagination:
      13. "batch cap 50": seed 60 stale `live:` sessions → first deliverDue call: `sessions_promoted: 50`, second call: `sessions_promoted: 10`
      14. "pagination cap 50 pages": configure MockKV.list to always return `list_complete: false` with a fresh cursor (simulating infinite pages) → deliverDue terminates after exactly 50 pages; tick log `pages_scanned: 50`
      15. "multi-page batch drain": seed 30 stale sessions distributed across 3 pages (limit=10 per page in mock) → first call: `sessions_promoted: 30`, `pages_scanned: 3`, `list_complete: true` on page 3 → loop exits cleanly

    GROUP G — CRON-03 retry harness + isolation:
      16. "retry harness 3 attempts": inject mock that throws on attempts 1+2+3 (use vi.useFakeTimers + vi.runAllTimersAsync to skip backoff) → after 3 attempts, the would-be send raises, promoteOne catches, logs `chat.delivery.failed { sid, error_class, msg_count }`, returns `{status: "error"}`; tick `errors: 1`, `sessions_promoted: 0`
      17. "retry harness eventual success": mock throws on attempts 1+2, succeeds on 3 → session promoted normally; `errors: 0`, `sessions_promoted: 1`
      18. "per-session isolation": two due sessions, session A throws on send, session B succeeds → tick `errors: 1`, `sessions_promoted: 1`; both sessions are processed (loop did not abort after A's failure)

    GROUP H — observability (OQ-7):
      19. "tick summary log": every deliverDue call emits exactly one `console.log("chat.delivery.tick", {sessions_seen, sessions_due, sessions_promoted, errors, pages_scanned, elapsed_ms})` — assert flat-primitive types: `expect(typeof call[1].sessions_seen).toBe("number")` × 5 numeric fields; `elapsed_ms >= 0`

    Bonus invariant (RESEARCH OQ-6) — covered structurally inside Group A tests:
    - `scheduledTime` arg (when present) is used as `nowMs` for inactivity comparisons — Test 1 + 2 pass `scheduledTime: <fixed Date.now() value>` and seed `last_activity_at` relative to that value; assertions don't depend on real wall clock.

    MockKVNamespace must be EXTENDED (vs chat-transcripts.test.ts's single-page mock):
    - Add `async delete(key): Promise<void>` (this.storage.delete(key))
    - Replace `list()` with cursor-paged version: `list({prefix, cursor, limit}): {keys: [{name, metadata}], list_complete: boolean, cursor: string}` — cursor is numeric-string index into sorted entries; pageSize defaults to 1000; `list_complete = (cursor + pageSize >= total)`; default startIdx = `cursor ? parseInt(cursor) : 0`
    - Optionally add operation-log capability (`this.operations: Array<{op: "get"|"put"|"delete"|"list", key?: string, ts: number}>`) for the ordering tests (4, 5)

    Hard-coded test fixtures (sibling pattern from chat-transcripts.test.ts):
    - `const SID = "8b0f7f1c-1234-4567-8901-abcdef012345"` (Phase 18 fixture, reuse)
    - `const SCHEDULED_NOW = Date.parse("2026-05-12T12:00:00.000Z")` (deterministic — use this for all `scheduledTime` args)
    - `const STALE_3H = new Date(SCHEDULED_NOW - 3*60*60*1000).toISOString()` (older than 2h threshold → due)
    - `const FRESH_30M = new Date(SCHEDULED_NOW - 30*60*1000).toISOString()` (newer than 2h threshold → not due)
    - Mock `env: DeliveryEnv` constructed per-test: `{ CHAT_KV: mockKv, DRY_RUN: "1", CHAT_RECIPIENT_EMAIL: "to@example.com", CHAT_SENDER_EMAIL: "from@example.com" }`

    Test file MUST USE the console-spy pattern from chat-transcripts.test.ts:167-178: `beforeEach` mocks log/warn/error; `afterEach` restores; assertions use `logSpy.mock.calls.find((c) => c[0] === "chat.delivery.tick")`.

    All tests fail with module-not-found / import error initially (TDD RED). Commit message: `test(19-02): RED — add 19 chat-delivery test cases against missing module`.
  </behavior>
  <action>
    Author `tests/api/chat-delivery.test.ts` with imports matching 19-PATTERNS.md (named-imports from `../../src/lib/chat-delivery` for `deliverDue`, all 6 exported constants `INACTIVITY_THRESHOLD_MS / PER_TICK_BATCH_CAP / PAGINATION_PAGE_HARDCAP / MAX_SEND_ATTEMPTS / DELIVERED_TTL_SECONDS / BACKOFF_BASE_MS` (omit if not exporting BACKOFF_BASE_MS), type-import `DeliveredMarker`; named-imports from `../../src/lib/chat-transcripts` for `KEY_PREFIX`, type-imports `ChatTranscript`, `KVMetadata`).

    Define a top-of-file `MockKVNamespace` class with `storage: Map<string, MockKVEntry>`, `operations: Array<{op, key, ts}>`, methods `get / getWithMetadata / put / delete / list`. Hand-roll the cursor pagination per 19-PATTERNS.md lines 470-486.

    Organize the 19 cases into 8 `describe` blocks (one per Group A-H). Use `vi.useFakeTimers()` inside Group G (retry tests) and `vi.runAllTimersAsync()` to skip backoff delays without sleeping the test runner.

    Use `expect(logSpy.mock.calls).toContainEqual([<event-name>, expect.objectContaining({...})])` for log assertions to allow field ordering freedom while locking field NAMES per D-05.

    DO NOT inline source code from chat-delivery.ts inside the test file (no copy-paste of implementation logic — tests must drive against the module's public surface only). DO NOT add any `import` from `src/lib/email/*` (does not exist in Phase 19 per D-06).

    Per the TDD discipline: this file's test suite must fail with module-not-found error or import error after this task commits. The fix arrives in Task 2.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/api/chat-delivery.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|test files|tests)" || echo "RED state confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/chat-delivery.test.ts` exists; line count >= 350
    - File contains exactly 19 `it(...)` calls (matched via `grep -c "^\\s*it(" tests/api/chat-delivery.test.ts`)
    - File contains 8 `describe(...)` blocks corresponding to groups A-H
    - File imports `deliverDue` from `../../src/lib/chat-delivery` (the module does NOT exist yet — RED state expected)
    - `pnpm exec vitest run tests/api/chat-delivery.test.ts` exits NON-ZERO with "Cannot find module" OR "Failed to resolve import" in stderr — RED state confirmed
    - Full battery `pnpm test` shows the new test file failing but Phase 18 baseline (419 PASS / 0 FAIL / 2 SKIP) for OTHER tests is preserved (failing tests are localized to chat-delivery.test.ts; pre-existing tests unaffected)
    - No imports from `cloudflare:workers` virtual module
    - No imports from `src/lib/email/*` (does not exist in Phase 19)
    - Commit message starts with `test(19-02): RED — `
  </acceptance_criteria>
  <done>
    19-test RED test file committed. Vitest exits non-zero on chat-delivery.test.ts with module-not-found. Pre-existing 419 PASS baseline preserved on all other files. Ready for Task 2 (GREEN implementation).
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement src/lib/chat-delivery.ts (GREEN) — pure module: deliverDue + promoteOne + sendOne + retryWithBackoff + pagination loop</name>
  <files>src/lib/chat-delivery.ts</files>
  <read_first>
    - src/lib/chat-transcripts.ts (READ FULL — the byte-for-byte sibling shape: file header docblock with decision-ID groups, locked-constants block, public types, internal helpers, inline decision-ID citations on every algorithmic step, NO `cloudflare:workers` import. chat-delivery.ts mirrors this structure)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md (READ "src/lib/chat-delivery.ts (NEW — pure module)" section in FULL — contains the verbatim header docblock template, the public types block, the promoteOne 5-step ordering function, the pagination loop, the DRY_RUN envelope log block, the retry harness, and the tick summary log)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-RESEARCH.md (READ § Pattern 1 "Custom-Entrypoint scheduled() Handler" + § Pattern 2 "Two-Keyspace Promotion" + § Code Example 3 "Retry harness exponential full-jitter")
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-CONTEXT.md (READ <decisions> for D-01 through D-11 locks; <specifics> for the "envelope log is the operational ground truth" rationale)
    - tests/api/chat-delivery.test.ts (READ FULL — the contract this implementation must satisfy; the import list at the top of the test file defines the export surface)
  </read_first>
  <action>
    Implement `src/lib/chat-delivery.ts` per the 19-PATTERNS.md template. File structure (top-to-bottom):

    1. File header docblock (~25 lines) citing CRON-02/03/04, D-01/02/05/06/07/09/10/11, the pure-module disclaimer ("NO imports from..."), and the caller contract ("wrap deliverDue with ctx.waitUntil(...).catch(...) per RESEARCH § Pattern 1").

    2. Type-only imports: `import type { ChatTranscript, KVMetadata } from "./chat-transcripts";` Value import: `import { KEY_PREFIX } from "./chat-transcripts";` (single source-of-truth for the "live:" prefix string).

    3. Locked-constants block (per 19-PATTERNS.md lines 243-249): `INACTIVITY_THRESHOLD_MS = 2 * 60 * 60 * 1000`, `PER_TICK_BATCH_CAP = 50`, `PAGINATION_PAGE_HARDCAP = 50`, `MAX_SEND_ATTEMPTS = 3`, `BACKOFF_BASE_MS = 250`, `BACKOFF_CAP_MS = 5000`, `DELIVERED_TTL_SECONDS = 24 * 3600`. Inline comments cite STATE.md / CRON-03 / D-09 / OQ-2.

    4. Public types: `export interface DeliveredMarker { v: 1; sid: string; delivered_at: string; dry_run: boolean; msg_count: number; truncated: boolean; }` (exact shape locked by D-09); internal `interface DeliveryEnv { CHAT_KV: KVNamespace; DRY_RUN: string; CHAT_RECIPIENT_EMAIL?: string; CHAT_SENDER_EMAIL?: string; }` (narrowed structural type — NOT imported from worker.ts to avoid cyclic import).

    5. `export async function deliverDue(env: DeliveryEnv, scheduledTime?: number): Promise<void>` — the public entry. Body owns the pagination loop (per 19-PATTERNS.md lines 329-356). `const nowMs = scheduledTime ?? Date.now();` (OQ-6 tick-as-batch consistency). Initialize counters `pagesScanned, sessionsSeen, sessionsDue, sessionsPromoted, errors` = 0; `const startMs = Date.now()`. `while (pagesScanned < PAGINATION_PAGE_HARDCAP) { ... }`. Inside the loop: `await env.CHAT_KV.list<KVMetadata>({prefix: KEY_PREFIX, cursor})`, increment pagesScanned, iterate keys with the inactivity filter; for each due key call `promoteOne`, increment counters; break if `sessionsPromoted >= PER_TICK_BATCH_CAP` or `page.list_complete`; otherwise `cursor = page.cursor`. After loop: emit `console.log("chat.delivery.tick", { sessions_seen, sessions_due, sessions_promoted, errors, pages_scanned: pagesScanned, elapsed_ms: Date.now() - startMs })`.

    6. Internal `async function promoteOne(env, sid, scheduledAt): Promise<{status}>` per 19-PATTERNS.md lines 278-324. Five steps in EXACT order: (1) read `delivered:{sid}` — short-circuit if present, emit `chat.delivery.skipped_already_delivered { sid, delivered_at_existing }`; (2) load `live:{sid}` via `env.CHAT_KV.get<ChatTranscript>(KEY_PREFIX + sid, {type: "json"})` — short-circuit if null with `{status: "missing_live"}` (no log); (3) wrap `sendOne` in `retryWithBackoff` with MAX_SEND_ATTEMPTS; (4) construct `DeliveredMarker` value `{ v: 1, sid, delivered_at: new Date().toISOString(), dry_run: env.DRY_RUN === "1", msg_count: transcript.msg_count, truncated: transcript.truncated }`, then `await env.CHAT_KV.put("delivered:" + sid, JSON.stringify(value), { expirationTtl: DELIVERED_TTL_SECONDS })` — NO metadata field per D-11; (5) `await env.CHAT_KV.delete(KEY_PREFIX + sid)`. Wrap steps 3-5 in try/catch — on catch, emit `console.error("chat.delivery.failed", { sid, error_class: err instanceof Error ? err.constructor.name : "Error", msg_count: transcript.msg_count })` and return `{status: "error"}`.

    7. Internal `async function sendOne(env, transcript): Promise<void>` per 19-PATTERNS.md lines 360-378. If `env.DRY_RUN === "1"`: emit `console.log("chat.delivery.dry_run", { sid: transcript.sid, to: env.CHAT_RECIPIENT_EMAIL ?? null, from: env.CHAT_SENDER_EMAIL ?? null, reply_to: "jackcutrara@gmail.com", msg_count: transcript.msg_count, truncated: transcript.truncated, country: transcript.meta.country ?? null, referrer_host: hostnameOrNull(transcript.meta.referrer), dry_run: true })`. Return synthetic success. Else: `throw new Error("send_not_implemented_in_phase_19")` (Phase 20 replaces this branch).

    8. Internal `async function retryWithBackoff<T>(fn, maxAttempts): Promise<T>` per 19-PATTERNS.md lines 382-394. For-loop `for (let attempt = 0; attempt < maxAttempts; attempt++)`. `try { return await fn(); } catch (err) { lastErr = err; if (attempt + 1 >= maxAttempts) break; const ceiling = Math.min(BACKOFF_CAP_MS, BACKOFF_BASE_MS * 2 ** attempt); await new Promise((r) => setTimeout(r, Math.floor(Math.random() * ceiling))); }`. After loop: `throw lastErr`. Full-jitter exponential per OQ-2.

    9. Helper `function hostnameOrNull(url?: string): string | null` — try-catch around `new URL(url).hostname`; returns null if url is undefined / empty / malformed.

    Inline decision-ID citations on every algorithmic step in the form `// D-09 / D-10 — schema-versioned envelope` (matches the Phase 18 chat-transcripts.ts convention at lines 107-117).

    DO NOT import anything from `@anthropic-ai/sdk`, `cloudflare:workers`, `src/prompts/*`, `src/pages/*`, `src/scripts/chat.ts`, or `src/lib/email/*`. All five are forbidden per the pure-module disclaimer (19-PATTERNS.md lines 410-416).

    DO NOT add any `metadata` field to the `delivered:{sid}` `put` options (D-11 lock).

    DO NOT use `Date.now()` for the inactivity comparison — use `scheduledTime ?? Date.now()` so unit tests can supply a deterministic timestamp.

    Commit message: `feat(19-02): GREEN — implement deliverDue two-keyspace + DRY_RUN harness`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/api/chat-delivery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/chat-delivery.ts` exists; line count >= 150
    - File exports the 7 named symbols `deliverDue`, `INACTIVITY_THRESHOLD_MS`, `PER_TICK_BATCH_CAP`, `PAGINATION_PAGE_HARDCAP`, `MAX_SEND_ATTEMPTS`, `DELIVERED_TTL_SECONDS`, `DeliveredMarker` (and any additional constants the test file imports e.g. `BACKOFF_BASE_MS`)
    - File contains type-only import `import type { ChatTranscript, KVMetadata } from "./chat-transcripts"` — verified by source-text grep
    - File contains value import `import { KEY_PREFIX } from "./chat-transcripts"` — verified by source-text grep
    - File contains the strict-equals-string check `env.DRY_RUN === "1"` — verified by source-text grep (D-02)
    - File contains `expirationTtl: DELIVERED_TTL_SECONDS` — verified by source-text grep (D-09 24h TTL)
    - File contains the literal string `"send_not_implemented_in_phase_19"` (the Phase 20 substitution target) — verified by source-text grep
    - File does NOT import from `@anthropic-ai/sdk`, `cloudflare:workers`, `src/prompts/`, `src/pages/`, `src/scripts/`, or `src/lib/email/` — verified by source-text grep returning zero matches for any of these path fragments
    - `pnpm exec vitest run tests/api/chat-delivery.test.ts` exits 0 with 19 tests PASS / 0 FAIL — GREEN state
    - `pnpm exec astro check` exits 0 / 0 / 0 (no new ts errors; structural-type compatibility with worker.ts Env holds)
    - `pnpm test` full battery: PASS count >= 438 (419 baseline + 19 new); FAIL count = 0; SKIP count = 2
    - `tests/api/sse-snapshot.test.ts` GREEN (D-15 forward-defense)
    - `tests/api/anthropic-payload-shape.test.ts` GREEN (TEST-03 forward-defense)
    - Commit message starts with `feat(19-02): GREEN — `
  </acceptance_criteria>
  <done>
    chat-delivery.ts module committed with all 19 tests GREEN. astro check 0/0/0. Full battery >= 438/0/2. Pure-module discipline holds (forbidden-import grep returns zero matches). Module is now ready for Plan 19-03 to import and wire into `src/worker.ts`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare cron scheduler → scheduled() handler | Untrusted input is `controller.scheduledTime` (a number); platform-controlled, but treated as input |
| chat-delivery.ts → env.CHAT_KV | KV write surface; transcripts already-validated by Phase 18 KV-04 (30-turn cap + 512-char referrer/UA truncation); chat-delivery.ts only reads values authored by Phase 18 + writes flat envelope markers |
| chat-delivery.ts → console.log/warn/error → Workers Logs | Log ingestion surface; flat-primitive convention prevents accidental object-explosion DoS on Workers Logs cardinality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Spoofing | DRY_RUN env value misread | mitigate | Strict-equals-string check `env.DRY_RUN === "1"` per D-02. `Boolean(env.DRY_RUN)` rejected at decision time (would treat `"false"` as truthy). Unit test Group D case 9 + 10 lock the gate behavior. |
| T-19-02-02 | Tampering | delivered:{sid} marker overwritten | accept | Last-writer-wins per KV semantics; idempotency cursor is a hint, not a lock. Layer 2 (Phase 20 Resend `Idempotency-Key`) provides the cryptographic-grade dedupe. 24h TTL bounds blast radius. |
| T-19-02-03 | Repudiation | failed send not logged | mitigate | Every promoteOne catch path emits `chat.delivery.failed { sid, error_class, msg_count }` via console.error; per-tick summary log emits `errors` count. Workers Logs retains. Unit test Group G case 16 locks the log shape. |
| T-19-02-04 | Information Disclosure | DRY_RUN envelope log leaks PII in country/referrer fields | accept | `country` is `request.cf.country` (ISO-3166 code, e.g. "US") — not PII. `referrer_host` is hostname only via `new URL(...).hostname` — strips path/query. Phase 20 will be the surface that touches actual message content. |
| T-19-02-05 | Denial of Service | unbounded pagination loop | mitigate | Hard cap `PAGINATION_PAGE_HARDCAP = 50` per CRON-03; loop exits on `list_complete` OR `pagesScanned >= 50` OR `sessionsPromoted >= PER_TICK_BATCH_CAP`. Unit test Group F case 14 locks the cap. Per-session try/catch (T-19-02-06) prevents one bad session from exhausting the cron budget. |
| T-19-02-06 | Denial of Service | one bad session aborts entire sweep | mitigate | Per-session try/catch in promoteOne isolates errors to single sid; loop continues to next key. Unit test Group G case 18 locks the isolation. |
| T-19-02-07 | Denial of Service | retry harness amplifies inner failure to N x latency | mitigate | `MAX_SEND_ATTEMPTS = 3` cap; exponential backoff with `BACKOFF_CAP_MS = 5000` per-attempt ceiling; worst case ~15s per failing session vs ~50s cron CPU budget. Unit test Group G case 16 locks the 3-try cap. |
| T-19-02-08 | Elevation of Privilege | chat-delivery.ts reaches into chat surface | mitigate | Pure-module discipline locked by import grep in acceptance criteria. No `src/pages/`, `src/scripts/`, `src/prompts/` imports allowed. D-26 chat regression battery preserved as forward-defense. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/api/chat-delivery.test.ts` exits 0 with 19 / 19 PASS — every CRON-02/03/04 invariant locked at the unit level
- `pnpm test` full battery: PASS >= 438 (Phase 18 baseline 419 + 19 new); FAIL = 0; SKIP = 2
- `pnpm exec astro check` 0/0/0 — Plan 17-08's clean-typecheck achievement preserved
- `tests/api/sse-snapshot.test.ts` GREEN (D-15 anchor preserved — Plan 19-02 doesn't touch SSE)
- `tests/api/anthropic-payload-shape.test.ts` GREEN (TEST-03 anchor — Plan 19-02 doesn't touch Anthropic surface)
- D-26 chat regression battery GREEN (Plan 19-02 doesn't touch chat surface)
- Forbidden-import grep returns zero matches for: `@anthropic-ai/sdk`, `cloudflare:workers`, `src/prompts/`, `src/pages/`, `src/scripts/`, `src/lib/email/`
</verification>

<success_criteria>
- 2 new files (`src/lib/chat-delivery.ts` ~150-250 LOC + `tests/api/chat-delivery.test.ts` ~350-500 LOC); 0 modifications
- 19 new tests, all GREEN; full battery >= 438/0/2
- Pure-module discipline holds (no chat-surface imports; no cloudflare:workers virtual-module reach-in; no future-phase imports)
- D-09 / D-10 / D-11 schema locked: `delivered:{sid}` value matches `{v:1, sid, delivered_at, dry_run, msg_count, truncated}`; 24h TTL; NO metadata field
- D-02 strict-equals-string gate proven by Group D test 9 + 10
- D-05 envelope field NAMES locked by Group D test 9 deep-equal assertion
- Two-keyspace ordering invariant proven by Group B tests 4 + 5 (PUT delivered BEFORE send-log emission, DELETE live AFTER success)
- Idempotency cursor proven by Group E test 11
- Batch cap + pagination cap + retry cap all proven by Group F + G tests 13 / 14 / 16
- Per-session try/catch isolation proven by Group G test 18
- `chat.delivery.tick` summary log shape proven by Group H test 19
- Module ready for Plan 19-03 to import `deliverDue` and wire into `scheduled()`
</success_criteria>

<output>
After completion, create `.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-02-SUMMARY.md` documenting:
- Test count delta (419 → ~438), GREEN at every commit
- Module LOC + exports list
- Confirmation of pure-module discipline (forbidden-import grep results)
- TDD RED → GREEN commit sequence (Task 1 RED commit hash; Task 2 GREEN commit hash)
- Any Rule 3 inline deviations
- Any planner's-discretion decisions made (e.g., backoff curve choice = full-jitter exponential per OQ-2; cursor implementation in mock = numeric-string indices)
- Confirmation that Plan 19-03 is unblocked (deliverDue export structurally available + tested in isolation)
</output>
