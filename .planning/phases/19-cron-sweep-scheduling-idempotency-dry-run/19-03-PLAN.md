---
phase: 19-cron-sweep-scheduling-idempotency-dry-run
plan: 03
type: execute
wave: 2
depends_on:
  - 19-02
files_modified:
  - src/worker.ts
  - tests/build/worker-scheduled-call-site.test.ts
autonomous: true
requirements:
  - CRON-01
user_setup: []

must_haves:
  truths:
    - "`src/worker.ts` `scheduled()` handler body delegates to `deliverDue(env, controller.scheduledTime)` via `ctx.waitUntil`, with `.catch(...)` chained INSIDE the promise per Phase 18 D-09 pattern"
    - "The Phase 17 `console.warn(\"worker.scheduled.stub\", ...)` log line is REMOVED — Phase 19 replaces, not amends, per the migration semantic"
    - "A failure in `deliverDue` surfaces in Workers Logs as `worker.scheduled.failed { error_class }` via the outer `.catch` (catastrophic-only); per-session failures live INSIDE `deliverDue` per Plan 19-02"
    - "`tests/build/worker-scheduled-call-site.test.ts` source-text invariants lock the call-site shape: deliverDue import path, ctx.waitUntil wrap, .catch chained INSIDE, anti-ctx-destructure, anti-stub-log-line, Env.DRY_RUN field"
    - "`pnpm dev:cron` + `curl http://localhost:8787/__scheduled?cron=*+*+*+*+*` invokes the live `deliverDue` against local KV — handler-wiring proof BEFORE the operator-controlled production *****  UAT"
  artifacts:
    - path: "src/worker.ts"
      provides: "scheduled() handler wired to deliverDue via ctx.waitUntil(...).catch(...)"
      contains: "ctx.waitUntil(\n      deliverDue("
    - path: "tests/build/worker-scheduled-call-site.test.ts"
      provides: "Source-text forward-defense for call-site shape (6 invariants)"
      min_lines: 60
  key_links:
    - from: "src/worker.ts scheduled() body"
      to: "src/lib/chat-delivery.ts deliverDue export"
      via: "named import from './lib/chat-delivery'"
      pattern: "import\\s*\\{[^}]*\\bdeliverDue\\b[^}]*\\}\\s*from\\s*[\"']\\./lib/chat-delivery[\"']"
    - from: "ctx.waitUntil(deliverDue(...))"
      to: ".catch handler"
      via: ".catch chained INSIDE the promise per Phase 18 D-09"
      pattern: "ctx\\.waitUntil\\(\\s*deliverDue\\([\\s\\S]*?\\)\\s*\\.catch\\("
---

<objective>
Replace the `scheduled()` handler stub body in `src/worker.ts` with the live `deliverDue` call site, wired via `ctx.waitUntil(deliverDue(env, controller.scheduledTime).catch(...))` per the Phase 18 D-09 rejection-safe pattern. Add the optional-but-recommended source-text forward-defense test `tests/build/worker-scheduled-call-site.test.ts` that locks 6 call-site invariants.

Purpose: This is the wiring step — Plan 19-02 delivered an isolated, fully-tested `deliverDue`; Plan 19-03 plugs it into the Worker's `scheduled()` handler so that a cron tick actually invokes it. After this plan, the LOCAL handler wiring is proven (via `pnpm dev:cron` + `curl /__scheduled`), but `triggers.crons` is STILL `[]` — Plan 19-04 turns on the cron and ships the manual UAT.

Output: 1 file modified (`src/worker.ts`), 1 new file (`tests/build/worker-scheduled-call-site.test.ts`). Zero behavior change in the deployed Worker until `triggers.crons` flips in Plan 19-04. Pre-flight `curl /__scheduled` against local `pnpm dev:cron` proves the handler dispatches correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-CONTEXT.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-VALIDATION.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-01-SUMMARY.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-02-SUMMARY.md
@src/worker.ts
@src/lib/chat-delivery.ts
@tests/build/append-turn-call-site.test.ts
@tests/build/worker-entrypoint.test.ts

<interfaces>
<!-- Plan 19-02 (Wave 1) shipped src/lib/chat-delivery.ts exporting deliverDue. -->
<!-- Plan 19-03 imports that single function + Plan 19-01's DRY_RUN field on Env. -->

From src/lib/chat-delivery.ts (Plan 19-02 — UNCHANGED in Plan 19-03):
- `export async function deliverDue(env: DeliveryEnv, scheduledTime?: number): Promise<void>`
- The `DeliveryEnv` interface in chat-delivery.ts is NARROWER than worker.ts's `Env` (only CHAT_KV, DRY_RUN, CHAT_RECIPIENT_EMAIL?, CHAT_SENDER_EMAIL?), but `Env` is STRUCTURALLY assignable to `DeliveryEnv` because it has all those fields. TypeScript accepts `deliverDue(env, controller.scheduledTime)` with `env: Env` at the call site.

From src/worker.ts (Plan 19-01 — Env extended with `DRY_RUN: string`):
- The Env interface has `DRY_RUN: string` (Plan 19-01); `scheduled(_controller, _env, ctx)` body still runs the Phase 17 stub log line + `ctx.waitUntil(Promise.resolve())` (Plan 19-01 did NOT touch the body).
- Plan 19-03 REPLACES the body: removes the stub `console.warn("worker.scheduled.stub", {...})` block + the `ctx.waitUntil(Promise.resolve())` call; drops the underscore prefix from `_controller` + `_env` (both are now used actively); adds `ctx.waitUntil(deliverDue(env, controller.scheduledTime).catch((err: unknown) => console.error("worker.scheduled.failed", { error_class: err instanceof Error ? err.constructor.name : "Error" })))`.

Existing build test that this plan must coexist with:
- `tests/build/worker-entrypoint.test.ts` (Phase 17 FOUND-02) — lines 40-43 currently assert `src.toContain("Phase 19")` and `src.toContain("deliverDue")` against the stub forward-compat comment. After the stub is removed, both substring checks still pass via the new import + call site. Plan 19-03 should verify worker-entrypoint.test.ts stays GREEN.
- `tests/build/append-turn-call-site.test.ts` (Plan 18-07) — the canonical 5-invariant idiom Plan 19-03 mirrors. Reusable verbatim except: count assertion changes from "2 call sites" to "1 call site"; ordering-vs-controller.close assertion drops entirely.
</interfaces>

<patterns>
<!-- See 19-PATTERNS.md "tests/build/worker-scheduled-call-site.test.ts (NEW, OPTIONAL)" section -->
<!-- for the full verbatim 6-invariant test idiom (Invariants A-F). The dynamic-RegExp -->
<!-- anti-destructure pattern is critical so the test file itself doesn't self-match. -->

<!-- Sibling analog: tests/build/append-turn-call-site.test.ts is the byte-for-byte source -->
<!-- of the idiom — open it, copy structure, change identifiers, drop count + ordering -->
<!-- assertions, add Invariant E (anti-stub-log-line) + Invariant F (Env.DRY_RUN field). -->
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace src/worker.ts `scheduled()` body with `ctx.waitUntil(deliverDue(env, controller.scheduledTime).catch(...))` + drop the underscore prefixes from controller/env args</name>
  <files>src/worker.ts</files>
  <read_first>
    - src/worker.ts (READ FULL — 45 lines including the Plan 19-01 Env.DRY_RUN addition; verify exact byte position of the stub log line and ctx.waitUntil(Promise.resolve()) call before edit)
    - src/lib/chat-delivery.ts (READ the top 20 lines to confirm `deliverDue` export signature `(env: DeliveryEnv, scheduledTime?: number): Promise<void>` and that `DeliveryEnv` is structurally compatible with `Env`)
    - tests/build/worker-entrypoint.test.ts (READ FULL — lines 40-43 currently assert `Phase 19` and `deliverDue` substring presence; verify the new import + call site will keep these assertions GREEN)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md (READ "src/worker.ts (MODIFY)" section — verify the target shape at lines 113-143; READ the "Critical anti-patterns to avoid" subsection at lines 145-149)
    - .planning/phases/18-persistence-identity-kv-write-path-sessionid/18-CONTEXT.md (READ section documenting D-09 / D-10 / D-11 ctx.waitUntil .catch INSIDE the promise — verify the pattern matches what Plan 19-03 inherits)
  </read_first>
  <action>
    Edit `src/worker.ts` per 19-PATTERNS.md "src/worker.ts (MODIFY)" target shape:

    1. Add a new top-level import after the existing `import { handle } from "@astrojs/cloudflare/handler";` line: `import { deliverDue } from "./lib/chat-delivery";` — single named import; relative path matches Phase 18's `chat-transcripts` import pattern from `src/pages/api/chat.ts`.

    2. Drop the underscore prefix from BOTH `_controller` and `_env` in the `scheduled()` arg list — both args are now actively used (controller for `controller.scheduledTime`; env for the `deliverDue(env, ...)` call). The TypeScript "noUnusedParameters" rule passed previously because of the underscore convention; removing the prefix is mandatory once the args are used.

    3. Replace the ENTIRE body of `scheduled()` — delete (a) the 5-line `// WR-05 (Phase 17 review): emit a structured warn line ...` comment block (lines 30-35), (b) the `console.warn("worker.scheduled.stub", { ... })` call (lines 36-41), (c) the `ctx.waitUntil(Promise.resolve());` line, AND (d) the forward-compat `// Phase 19 will replace with: ctx.waitUntil(deliverDue(_env, _controller.scheduledTime));` comment + stub-keeping rationale paragraph at lines 27-29.

    4. Insert in their place a comment block citing Phase 18 D-09/D-10/D-11 + RESEARCH § Pattern 1 + the literal substring `Phase 19 CRON-01 — scheduled() dispatch` (so `tests/build/worker-entrypoint.test.ts:40-43` keeps matching `src.toContain("Phase 19")`), then the wired call site:
       - The wrap MUST be exactly `ctx.waitUntil(\n      deliverDue(env, controller.scheduledTime).catch((err: unknown) => {\n        console.error("worker.scheduled.failed", {\n          error_class: err instanceof Error ? err.constructor.name : "Error",\n        });\n      })\n    );`
       - The `.catch` MUST be chained INSIDE the promise BEFORE the pass to `ctx.waitUntil` (Phase 18 D-09 — if it's chained outside, the rejection is silently swallowed AND `waitUntil` returns `void` so `.catch` on it would be a type error anyway).
       - The catch handler emits `console.error` (NOT `console.warn`) per the convention that `worker.scheduled.failed` is catastrophic-only (per-session failures live inside `deliverDue` with `chat.delivery.failed`).

    5. DO NOT add a `switch (controller.cron)` branch — v1.3 has a single cron expression per CRON-01; the handler runs unconditionally per RESEARCH Pitfall 3.

    6. DO NOT destructure `ctx` (`const { waitUntil } = ctx` throws "Illegal invocation" at runtime per RESEARCH Pitfall 1 + the Phase 18 D-09 lesson).

    7. The default-export shape `satisfies ExportedHandler<Env>` MUST remain — TypeScript still type-checks the handler signature against the Cloudflare types.

    Commit message: `feat(19-03): wire scheduled() to deliverDue with ctx.waitUntil(.catch INSIDE)`.
  </action>
  <verify>
    <automated>pnpm exec astro check &amp;&amp; pnpm exec vitest run tests/build/worker-entrypoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/worker.ts` contains exact substring `import { deliverDue } from "./lib/chat-delivery"`
    - `src/worker.ts` contains regex match `ctx\.waitUntil\(\s*deliverDue\(\s*env\s*,\s*controller\.scheduledTime\s*\)\s*\.catch\(`
    - `src/worker.ts` contains exact substring `worker.scheduled.failed`
    - `src/worker.ts` contains exact substring `error_class`
    - `src/worker.ts` contains exact substring `Phase 19` (so worker-entrypoint.test.ts:40-43 stays GREEN)
    - `src/worker.ts` does NOT contain `worker.scheduled.stub` — the Plan 17 stub log line is REMOVED, not amended (greppable absence)
    - `src/worker.ts` does NOT contain `_controller` or `_env` (underscore-prefixed args removed)
    - `src/worker.ts` does NOT contain `const { waitUntil } = ctx` or any ctx destructure pattern (anti-pattern per Pitfall 1)
    - `src/worker.ts` does NOT contain `switch (controller.cron)` or `switch(controller.cron)` (anti-pattern per Pitfall 3 — single cron, runs unconditionally)
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm exec vitest run tests/build/worker-entrypoint.test.ts` exits 0 (Phase 17 FOUND-02 baseline still GREEN — `Phase 19` substring still matched via the new comment; `deliverDue` substring matched via the new import + call site)
    - `pnpm exec vitest run tests/api/chat-delivery.test.ts` still exits 0 with 19 PASS (Plan 19-02 unit tests unaffected)
    - `pnpm test` full battery: PASS count >= 438; FAIL count = 0
  </acceptance_criteria>
  <done>
    src/worker.ts scheduled() body replaced with the wired call site. astro check 0/0/0. worker-entrypoint.test.ts GREEN. chat-delivery.test.ts GREEN. Pre-existing 19-02 test count preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests/build/worker-scheduled-call-site.test.ts — 6 source-text invariants forward-defending the call-site shape</name>
  <files>tests/build/worker-scheduled-call-site.test.ts</files>
  <read_first>
    - tests/build/append-turn-call-site.test.ts (READ FULL — the canonical 5-invariant idiom; copy the structure, drop the count + ordering assertions, change identifiers from `appendTurn` to `deliverDue`, change target file from `src/pages/api/chat.ts` to `src/worker.ts`)
    - tests/build/worker-entrypoint.test.ts (READ FULL — verify the existing assertions on src/worker.ts; Plan 19-03's new file is a sibling, not a replacement)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md (READ "tests/build/worker-scheduled-call-site.test.ts (NEW, OPTIONAL)" section in FULL — verify the 6 invariants A-F at lines 581-622)
    - src/worker.ts (READ the new state from Task 1 — the test file regex anchors must match the exact line structure Task 1 wrote)
  </read_first>
  <action>
    Author `tests/build/worker-scheduled-call-site.test.ts` per 19-PATTERNS.md lines 575-618. Single describe block titled `"CRON-01: ctx.waitUntil(deliverDue(...).catch(...)) call site in src/worker.ts"`. Six `it(...)` invariants:

    - **Invariant A** — `src/worker.ts` imports `deliverDue` from `./lib/chat-delivery`. Regex: `/import\s*\{[^}]*\bdeliverDue\b[^}]*\}\s*from\s*["']\.\/lib\/chat-delivery["']/`
    - **Invariant B** — `scheduled()` body contains `ctx.waitUntil(deliverDue(...))`. Regex: `/ctx\.waitUntil\(\s*deliverDue\(/`
    - **Invariant C** — `.catch` is chained INSIDE the promise (Phase 18 D-09 inheritance). Match the full `ctx.waitUntil(deliverDue(...))` substring, then assert the match contains `.catch(`. Use the multiline match pattern `/ctx\.waitUntil\(\s*deliverDue\([\s\S]*?\)\s*\)/` then `.toContain(".catch(")` on the matched substring.
    - **Invariant D** — source does NOT destructure `ctx`. Use a dynamically-built RegExp so this test file itself doesn't self-match: `const destructurePattern = new RegExp(["const","\\s*","\\{","\\s*","waitUntil","\\s*","\\}","\\s*","=","\\s*","ctx","\\b"].join(""));` then `expect(src).not.toMatch(destructurePattern)`. (Verbatim pattern from append-turn-call-site.test.ts:69-72.)
    - **Invariant E** — `worker.scheduled.stub` log line is REMOVED (substitution semantic). `expect(src).not.toContain('"worker.scheduled.stub"');` + `expect(src).toMatch(/worker\.scheduled\.failed/);` + `expect(src).toMatch(/error_class/);`
    - **Invariant F** — `Env` interface declares `DRY_RUN: string` (Plan 19-01's contribution; locked here as a regression-test). `expect(src).toMatch(/DRY_RUN\s*:\s*string/);`

    Imports: `import { describe, it, expect } from "vitest"; import { readFileSync } from "node:fs"; import { join } from "node:path";`
    Source read pattern: `const src = readFileSync(join(process.cwd(), "src/worker.ts"), "utf8");` — single read at describe-scope.

    DO NOT inline assert specific counts (vs append-turn-call-site.test.ts which asserts 2 call sites — Plan 19-03 has 1 deliverDue call site, so the count assertion is dropped per 19-PATTERNS.md deviation note).
    DO NOT assert ordering relative to a `controller.close()` site (no such site in `scheduled()` — that was an appendTurn-specific concern per 19-PATTERNS.md deviation note).

    Commit message: `test(19-03): add worker-scheduled-call-site.test.ts source-text forward-defense`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/worker-scheduled-call-site.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/build/worker-scheduled-call-site.test.ts` exists; line count >= 60
    - File contains exactly 6 `it(...)` calls (matched via `grep -c "^\\s*it(" tests/build/worker-scheduled-call-site.test.ts`)
    - File describes the 6 invariants A through F by name in either describe block titles or it titles
    - File contains the dynamic-RegExp anti-destructure pattern (`new RegExp([...].join(""))` form) — verified by source-text grep — so this test file itself does NOT self-match Invariant D
    - `pnpm exec vitest run tests/build/worker-scheduled-call-site.test.ts` exits 0 with 6 PASS / 0 FAIL
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm test` full battery: PASS >= 444 (438 baseline from 19-02 + 6 new); FAIL = 0; SKIP = 2
    - `tests/build/worker-entrypoint.test.ts` GREEN (Phase 17 FOUND-02 anchor preserved)
    - `tests/api/chat-delivery.test.ts` GREEN (Plan 19-02 unit tests preserved)
    - `tests/api/sse-snapshot.test.ts` GREEN (D-15 anchor)
    - `tests/api/anthropic-payload-shape.test.ts` GREEN (TEST-03 anchor)
  </acceptance_criteria>
  <done>
    Source-text forward-defense test committed. 6 / 6 GREEN. Full battery >= 444 / 0 / 2. astro check 0/0/0. Plan 19-04 unblocked — the call-site shape is now locked against drift.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare cron scheduler → src/worker.ts scheduled() | Untrusted only in the sense that `controller.scheduledTime` is platform-supplied — treated as input but Cloudflare-controlled |
| scheduled() handler → ctx.waitUntil | Promise lifecycle boundary; `.catch()` chained INSIDE per Phase 18 D-09 ensures rejections surface as `worker.scheduled.failed` rather than silent swallow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Repudiation | scheduled() failure silently swallowed by ctx.waitUntil | mitigate | `.catch` chained INSIDE the promise per Phase 18 D-09 inheritance. Failure emits `console.error("worker.scheduled.failed", { error_class })` → Workers Logs ingestion. Source-text forward-defense Invariant C locks this in the test suite. |
| T-19-03-02 | Tampering | future contributor removes `.catch` or destructures ctx | mitigate | `tests/build/worker-scheduled-call-site.test.ts` Invariants C + D fail at build time. Plus the source-text comment block at the call site cites Phase 18 D-09 / D-10 / D-11 by ID for code-archaeology. |
| T-19-03-03 | DoS | deliverDue blocks the scheduled() handler past the 30s ceiling | accept | Inner per-session try/catch in Plan 19-02 + 50-session batch cap + 50-page pagination cap + retry harness cap bound the total work to ~50s worst case; well under Cloudflare's scheduled() wall-clock budget for paid plans and acceptable for Free tier (3s CPU but 30s wall after-response). Phase 19 ships under DRY_RUN where the inner send is a no-op log line — actual wall time is dominated by KV list/get/put/delete latency. |
| T-19-03-04 | Information Disclosure | worker.scheduled.failed log leaks stack details | mitigate | Only `error_class` (constructor name) is logged — not message, not stack. Per the existing Phase 18 pattern. Reasoning: failure correlation, not debugging — Workers Logs already provides full stack via its own panel; the structured log line is for grep-by-class. |
</threat_model>

<verification>
- `pnpm exec astro check` 0/0/0
- `pnpm test` PASS >= 444 (Plan 19-02's 438 + 6 new build tests); FAIL = 0; SKIP = 2
- `tests/build/worker-scheduled-call-site.test.ts` 6/6 GREEN
- `tests/build/worker-entrypoint.test.ts` GREEN (Phase 17 FOUND-02 preserved through the body replacement)
- `tests/api/chat-delivery.test.ts` 19/19 GREEN (Plan 19-02 unit tests unaffected)
- D-15 / TEST-03 / D-26 forward-defense GREEN (Plan 19-03 doesn't touch chat surface or Anthropic surface)
- Pre-flight local validation: `pnpm dev:cron` runs without crash; `curl "http://localhost:8787/__scheduled?cron=*+*+*+*+*"` returns 200 (or any non-5xx) — exercises the handler dispatch path end-to-end against local KV state
- `pnpm build` clean (wrangler types regen + astro check + astro build)
</verification>

<success_criteria>
- 1 file modified (`src/worker.ts` scheduled() body replacement + import addition + underscore-prefix drop)
- 1 new file (`tests/build/worker-scheduled-call-site.test.ts` with 6 invariants)
- Phase 17 `worker.scheduled.stub` log line REMOVED (substitution semantic — not amended)
- ctx.waitUntil(deliverDue(...).catch(...)) wired with .catch INSIDE per Phase 18 D-09
- All 6 source-text invariants A-F GREEN
- Forward-defense locked against (a) future contributor removing .catch (Invariant C), (b) future contributor destructuring ctx (Invariant D), (c) someone re-adding the stub log line (Invariant E), (d) someone removing DRY_RUN from Env (Invariant F)
- Full battery PASS >= 444; FAIL = 0; SKIP = 2; astro check 0/0/0
- Local `pnpm dev:cron` + `curl /__scheduled` proves handler dispatch (operator pre-flight; not gated on this plan's PASS but should work)
- Plan 19-04 unblocked — only the wrangler.jsonc cron flip + UAT doc remains
</success_criteria>

<output>
After completion, create `.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-03-SUMMARY.md` documenting:
- Body-replacement before/after snippet boundaries (no inline code — anchor by line ranges)
- 6-invariant test count + PASS state
- Confirmation that worker-entrypoint.test.ts (Phase 17 FOUND-02) stays GREEN after the body replacement
- Test count delta (~438 → ~444)
- Any Rule 3 inline deviations (none expected — Plan 19-01 already absorbed the typecheck concerns; chat-surface untouched)
- Confirmation that Plan 19-04 is the final wave (cron flip + UAT doc + ROADMAP/STATE/REQUIREMENTS metadata)
</output>
