---
phase: 03-core-pages
plan: 05
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/components/ResumeEntry.astro
  - src/components/CTAButton.astro
  - src/components/ContactChannel.astro
  - src/pages/resume.astro
  - src/pages/contact.astro
autonomous: true
requirements:
  - RESM-01
  - RESM-02
  - CNTC-01
must_haves:
  truths:
    - "Resume page renders viewable content on-page (styled summary of experience, education, skills)"
    - "Resume page has a PDF download button visible above the fold"
    - "Contact page displays direct email, LinkedIn, and GitHub links"
    - "Contact page shows availability status indicator"
    - "External links open in new tab with proper rel attributes"
    - "Both pages follow shiyunlu.com's design system (grid, spacing, typography, colors)"
  artifacts:
    - path: "src/pages/resume.astro"
      provides: "Resume page with styled summary and PDF download"
      contains: "resume.pdf"
      min_lines: 20
    - path: "src/pages/contact.astro"
      provides: "Contact page with email, LinkedIn, GitHub links"
      contains: "jack@jackcutrara.com"
      min_lines: 15
    - path: "src/components/ResumeEntry.astro"
      provides: "Restyled resume entry component"
      contains: "title"
    - path: "src/components/CTAButton.astro"
      provides: "Restyled CTA button component"
      contains: "href"
    - path: "src/components/ContactChannel.astro"
      provides: "Restyled contact channel component"
      contains: "href"
  key_links:
    - from: "src/pages/resume.astro"
      to: "src/components/ResumeEntry.astro"
      via: "component import"
      pattern: "import ResumeEntry"
    - from: "src/pages/resume.astro"
      to: "src/components/CTAButton.astro"
      via: "component import for PDF download"
      pattern: "import CTAButton"
    - from: "src/pages/resume.astro"
      to: "public/resume.pdf"
      via: "download link href"
      pattern: "resume.pdf"
    - from: "src/pages/contact.astro"
      to: "src/components/ContactChannel.astro"
      via: "component import"
      pattern: "import ContactChannel"
---

<objective>
Rebuild the Resume and Contact pages within shiyunlu.com's design system. Both pages get the full design system treatment (D-17) -- grid, spacing, composition -- not just a color/font swap.

Purpose: These pages complete the core page set. Resume provides credibility at a glance + downloadable PDF. Contact provides clear paths to reach Jack.

Output: Restyled ResumeEntry, CTAButton, ContactChannel components and rewritten resume.astro, contact.astro pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-RESEARCH.md
@.planning/phases/03-core-pages/03-02-SUMMARY.md

<interfaces>
<!-- Component interfaces that must be preserved -->

From src/components/ResumeEntry.astro:
```typescript
interface Props {
  title: string;
  organization: string;
  date: string;
  description?: string;
  bullets?: string[];
}
```

From src/components/CTAButton.astro:
```typescript
interface Props {
  href: string;
  label: string;
  download?: boolean;
  showDownloadIcon?: boolean;
}
```

From src/components/ContactChannel.astro:
```typescript
interface Props {
  href: string;
  label: string;
  value: string;
  icon: "github" | "linkedin" | "email";
  external?: boolean;
}
```

<!-- Resume page data (to preserve) -->
Experience entries:
- "Software Engineering Intern" at "Tech Company", "Summer 2025"
- "Freelance Web Developer" at "Self-employed", "2024 - Present"
Education:
- "B.S. Computer Science" at "University", "2025"
Skills: TypeScript, JavaScript, Python, React, Next.js, Astro, Node.js, PostgreSQL, Docker, Git, REST APIs, CI/CD

<!-- Contact page data (to preserve) -->
- Email: jack@jackcutrara.com
- LinkedIn: linkedin.com/in/jackcutrara
- GitHub: github.com/jackc625
- Availability: "Currently open to opportunities" with pulsing dot
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle resume components and rebuild resume page</name>
  <files>src/components/ResumeEntry.astro, src/components/CTAButton.astro, src/pages/resume.astro</files>
  <read_first>
    - src/pages/resume.astro (current implementation -- preserve content data)
    - src/components/ResumeEntry.astro (current implementation -- preserve Props interface)
    - src/components/CTAButton.astro (current implementation -- preserve Props interface)
    - src/layouts/BaseLayout.astro (updated shell from Plan 02)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-17, D-18)
  </read_first>
  <action>
    **Step 1: Restyle ResumeEntry.astro**

    Per D-17, rebuild within shiyunlu.com's design system, not just a color swap.

    Preserve the Props interface: `{ title, organization, date, description?, bullets? }`.

    Visual redesign:
    - Study how shiyunlu.com presents timeline or list-based content
    - Remove or simplify the card background if the design system uses a cleaner approach
    - Title: `font-body text-[length:var(--token-text-base)] font-semibold text-text-primary`
    - Organization + date: `font-mono text-[length:var(--token-text-sm)] text-text-secondary`
    - Consider a left-border accent treatment (thin `border-l-2 border-accent` with left padding) for visual hierarchy
    - Bullets: keep the dash prefix or switch to a minimal bullet, using `text-text-secondary`
    - Spacing between entries: `mb-[var(--token-space-xl)]`

    **Step 2: Restyle CTAButton.astro**

    Preserve the Props interface: `{ href, label, download?, showDownloadIcon? }`.

    Visual redesign:
    - Study button/link styling on shiyunlu.com
    - The current outline button (border-accent with hover fill) may or may not match. Options:
      - **Minimal text link with arrow:** `text-accent hover:text-accent-hover` + an arrow icon (matches many modern portfolio buttons)
      - **Solid button:** `bg-accent text-bg-primary hover:bg-accent-hover` (filled)
      - **Ghost button:** Current outline style (keep if shiyunlu.com uses it)
    - Preserve the download icon SVG for the resume PDF button
    - Keep focus-visible styles for accessibility
    - Keep `uppercase tracking-[0.05em]` if that matches the design system

    **Step 3: Rewrite resume.astro**

    Per D-18, resume hybrid approach (styled summary + PDF download) still valid, just rebuilt.

    The implementor should study how shiyunlu.com lays out content-heavy inner pages and compose the resume accordingly.

    Layout redesign:
    1. **Page header with download CTA above the fold:**
       - Large heading "Resume" using `font-display text-[length:var(--token-text-display)]`
       - CTAButton for PDF download immediately below heading
       - This ensures RESM-02 (PDF download above the fold) is satisfied

    2. **Resume content section:**
       - Remove the card wrapper (`bg-bg-secondary border border-border rounded-lg`) if the design system doesn't use cards
       - Use section headings (Experience, Education, Technical Skills) with clear visual hierarchy
       - Section headings: `font-body text-[length:var(--token-text-heading)] font-semibold` with a bottom border accent or spacing treatment matching the design system
       - ResumeEntry components for experience and education
       - Skills as a flowing paragraph or tag list

    3. **Preserve all content data:**
       - Experience: "Software Engineering Intern" / "Tech Company" / "Summer 2025" with 3 bullets
       - Experience: "Freelance Web Developer" / "Self-employed" / "2024 - Present" with 2 bullets
       - Education: "B.S. Computer Science" / "University" / "2025" with coursework description
       - Skills: TypeScript, JavaScript, Python, React, Next.js, Astro, Node.js, PostgreSQL, Docker, Git, REST APIs, CI/CD

    4. **Max content width:** `max-w-3xl` or `max-w-4xl` for readability

    5. **Section spacing:** `var(--token-space-section)` between major sections, `var(--token-space-2xl)` between resume categories
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ResumeEntry.astro contains `interface Props` with `title: string` and `organization: string`
    - src/components/CTAButton.astro contains `interface Props` with `href: string` and `label: string`
    - src/pages/resume.astro contains `import BaseLayout from`
    - src/pages/resume.astro contains `import ResumeEntry from`
    - src/pages/resume.astro contains `import CTAButton from`
    - src/pages/resume.astro contains `resume.pdf` (PDF download link)
    - src/pages/resume.astro contains "Software Engineering Intern"
    - src/pages/resume.astro contains "B.S. Computer Science"
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>Resume components restyled, resume page rebuilt with design system treatment, all content preserved, PDF download above fold, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Restyle contact component and rebuild contact page</name>
  <files>src/components/ContactChannel.astro, src/pages/contact.astro</files>
  <read_first>
    - src/pages/contact.astro (current implementation -- preserve contact data and availability dot)
    - src/components/ContactChannel.astro (current implementation -- preserve Props interface)
    - src/layouts/BaseLayout.astro (updated shell from Plan 02)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-17, D-19)
  </read_first>
  <action>
    **Step 1: Restyle ContactChannel.astro**

    Per D-17, rebuild within shiyunlu.com's design system.

    Preserve the Props interface: `{ href, label, value, icon: "github"|"linkedin"|"email", external? }`.

    Visual redesign:
    - Study how shiyunlu.com presents links or contact information
    - Remove the card treatment (`bg-bg-secondary border border-border rounded-lg`) if design system doesn't use cards
    - Options for layout:
      - **Clean row with icon:** icon + label + value in a horizontal layout with hover accent color
      - **Minimal list item:** icon + value, no card background, just spacing and borders between items
      - **Grid layout:** icon on left, content on right with proper alignment
    - Keep all SVG icons (GitHub, LinkedIn, Email) -- they can be resized or restyled
    - Keep `target="_blank" rel="noopener noreferrer"` for external links
    - Keep `aria-label` and screen reader text for external links
    - Hover state: `hover:text-accent` or `hover:border-accent` (CSS transition only, no GSAP per D-03)
    - Focus-visible ring for accessibility

    **Step 2: Rewrite contact.astro**

    Per D-19, contact page (email, LinkedIn, GitHub + availability status) still valid, rebuilt within design system.

    Layout redesign:
    1. **Page header:**
       - Large heading "Get in Touch" or "Contact" using `font-display text-[length:var(--token-text-display)]`
       - Subtext: "I'm always open to hearing about new opportunities, interesting projects, or just connecting with other engineers."

    2. **Availability indicator:**
       - Keep the pulsing green dot with "Currently open to opportunities" text
       - Preserve the `availability-dot` CSS animation (pulse-ring)
       - Preserve the `prefers-reduced-motion` media query that disables animation
       - The dot uses `var(--token-accent)` for color (will auto-update to new accent)

    3. **Contact channels:**
       - Three ContactChannel components: Email, LinkedIn, GitHub
       - Same data as current:
         - Email: href="mailto:jack@jackcutrara.com", label="Email", value="jack@jackcutrara.com"
         - LinkedIn: href="https://linkedin.com/in/jackcutrara", label="LinkedIn", value="linkedin.com/in/jackcutrara", external=true
         - GitHub: href="https://github.com/jackc625", label="GitHub", value="github.com/jackc625", external=true
       - Layout: could be vertical stack (current), or horizontal grid on desktop

    4. **Section spacing:** Generous whitespace matching design system. `var(--token-space-section)` between header and contact list.

    5. **Max width:** Center content with `max-w-xl` or `max-w-2xl` for a focused, clean presentation.
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ContactChannel.astro contains `interface Props` with `href: string`, `label: string`, `icon`
    - src/components/ContactChannel.astro contains `target="_blank"` for external links
    - src/pages/contact.astro contains `import BaseLayout from`
    - src/pages/contact.astro contains `import ContactChannel from`
    - src/pages/contact.astro contains "jack@jackcutrara.com"
    - src/pages/contact.astro contains "linkedin.com/in/jackcutrara"
    - src/pages/contact.astro contains "github.com/jackc625"
    - src/pages/contact.astro contains "availability-dot" (pulsing indicator)
    - src/pages/contact.astro contains "prefers-reduced-motion" (accessible animation)
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>Contact component restyled, contact page rebuilt with design system treatment, all links preserved, availability dot preserved, build passes</done>
</task>

</tasks>

<verification>
- `npx astro build` exits 0
- Resume page (/resume) shows styled summary with PDF download button above the fold
- Contact page (/contact) shows email, LinkedIn, GitHub with availability indicator
- External links have target="_blank" and rel="noopener noreferrer"
- Both pages use design system tokens for all visual styling
- Availability dot animation respects prefers-reduced-motion
</verification>

<success_criteria>
Resume and Contact pages are fully rebuilt within shiyunlu.com's design system (D-17). Resume shows styled summary + PDF download above fold (D-18, RESM-01, RESM-02). Contact shows email/LinkedIn/GitHub + availability status (D-19, CNTC-01). All content data preserved. Component Props interfaces unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-05-SUMMARY.md`
</output>
