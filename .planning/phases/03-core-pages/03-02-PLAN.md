---
phase: 03-core-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SkillGroup.astro
  - src/pages/about.astro
autonomous: true
requirements: [ABUT-01, ABUT-02, ABUT-03]

must_haves:
  truths:
    - "About page presents Jack's background narrative covering education, path into engineering, and interests in first person"
    - "About page displays skills grouped by domain (Languages, Frameworks, Tools, Concepts) without progress bars"
    - "About page tone is professional but human — first person, conversational, not casual"
  artifacts:
    - path: "src/components/SkillGroup.astro"
      provides: "Grouped skill card for about page"
      exports: ["Props interface"]
    - path: "src/pages/about.astro"
      provides: "Complete about page replacing stub"
      contains: "What I Work With"
  key_links:
    - from: "src/pages/about.astro"
      to: "src/components/SkillGroup.astro"
      via: "import SkillGroup"
      pattern: "import SkillGroup"
---

<objective>
Build the complete About page with editorial narrative and skills section. Create the SkillGroup component for domain-grouped skill presentation.

Purpose: The About page gives recruiters and engineers a personal, human picture of who Jack is — his journey, education, and technical breadth. Per D-07, it gets the same editorial energy as the Home page, not a plain bio wall.
Output: Complete About page, SkillGroup component
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@.planning/phases/03-core-pages/03-RESEARCH.md

<interfaces>
<!-- BaseLayout props (from src/layouts/BaseLayout.astro) -->
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
}
```

<!-- Design tokens available (from src/styles/global.css) -->
<!-- Colors: bg-primary, bg-secondary, text-primary, text-secondary, text-muted, accent, accent-hover, border, border-hover -->
<!-- Typography: --token-text-sm, --token-text-base, --token-text-heading, --token-text-display -->
<!-- Spacing: --token-space-xs, sm, md, lg, xl, 2xl, 3xl, section -->
<!-- Fonts: font-display (serif), font-body (sans), font-mono (mono) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkillGroup component</name>
  <files>src/components/SkillGroup.astro</files>
  <read_first>
    - src/styles/global.css (design token names)
    - src/components/Footer.astro (existing Tailwind class conventions)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (SkillGroup component spec and Accessibility Contract)
  </read_first>
  <action>
    Create SkillGroup.astro per UI-SPEC SkillGroup spec. Per D-08 (skills grouped by domain, NO progress bars):

    Props interface:
    ```typescript
    interface Props {
      title: string;
      skills: string[];
    }
    ```

    Container: `<div class="bg-bg-secondary border border-border rounded-lg p-[var(--token-space-lg)]">`
    Title: `<h3 class="font-mono text-[length:var(--token-text-sm)] text-text-secondary uppercase tracking-[0.05em] mb-[var(--token-space-sm)]">{title}</h3>`
    Skills list: `<ul class="space-y-1">` with each skill as `<li class="font-body text-[length:var(--token-text-base)] text-text-primary">{skill}</li>`

    Note: Use `text-text-primary` (not `text-text-secondary`) for skill items on `bg-bg-secondary` per UI-SPEC accessibility warning about contrast (4.2:1 is borderline AA for body text).
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SkillGroup.astro contains `interface Props` with `title: string` and `skills: string[]`
    - SkillGroup.astro contains `bg-bg-secondary border border-border rounded-lg`
    - SkillGroup.astro contains `font-mono` and `uppercase` and `tracking-[0.05em]` (title styling)
    - SkillGroup.astro contains `text-text-primary` for skill items (not text-text-secondary — contrast requirement)
    - SkillGroup.astro does NOT contain `progress` or `width:` percentage (no progress bars per ABUT-03)
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>SkillGroup component exists with domain title and skills list, styled per design tokens, no progress bars.</done>
</task>

<task type="auto">
  <name>Task 2: Build complete About page replacing stub</name>
  <files>src/pages/about.astro</files>
  <read_first>
    - src/pages/about.astro (current stub to replace)
    - src/components/SkillGroup.astro (just created — verify props)
    - src/layouts/BaseLayout.astro (layout props)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (About Page composition + Copywriting Contract)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-05, D-06, D-07, D-08 decisions)
  </read_first>
  <action>
    Replace the stub in about.astro with the full About page per UI-SPEC About Page composition.

    **Frontmatter block:**
    ```typescript
    import BaseLayout from "../layouts/BaseLayout.astro";
    import SkillGroup from "../components/SkillGroup.astro";
    ```

    **BaseLayout wrapper:**
    ```html
    <BaseLayout
      title="About"
      description="Learn about Jack Cutrara's background, education, and path into software engineering."
    >
    ```

    **Section 1 — Page Header** (per UI-SPEC About Page Header):
    - `<section class="py-[var(--token-space-section)] pb-[var(--token-space-2xl)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]">`
    - Inner `max-w-5xl mx-auto`
    - `<h1 class="font-display text-[length:var(--token-text-display)] tracking-[-0.03em]">About</h1>`

    **Section 2 — Narrative** (per UI-SPEC Narrative Section, ABUT-01, ABUT-02, D-05, D-06):
    - `<section aria-label="Background" class="pb-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]">`
    - Inner `max-w-2xl mx-auto` (optimal reading width)
    - 4 paragraphs in first person (D-05), professional but human (ABUT-02). Each `<p>` with `class="font-body text-[length:var(--token-text-base)] text-text-primary mb-[var(--token-space-md)]"` and `leading-[1.6]`:

    Paragraph 1: `I'm Jack Cutrara, a software engineer based in New York. I got into programming because I wanted to build things — not just read about them.`

    Paragraph 2: `I studied computer science at university, where I discovered that the best way to learn was to ship real projects. That mindset stuck. Every project in my portfolio exists because I saw a problem and decided to solve it.`

    Paragraph 3: `Outside of code, I enjoy exploring new technologies, contributing to open source, and finding creative solutions to everyday problems. I'm drawn to work that's technically interesting and meaningfully useful.`

    Paragraph 4: `I'm currently looking for a software engineering role where I can contribute to a team building something that matters.`

    Note: Per D-04, this is placeholder copy the user will revise. Use `[City]` replaced with "New York" as reasonable placeholder per copywriting contract guidance.

    **Section 3 — Skills** (per UI-SPEC Skills Section, ABUT-03, D-08):
    - `<section aria-label="Skills" class="py-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]">`
    - Inner `max-w-5xl mx-auto`
    - `<h2 class="font-body text-[length:var(--token-text-heading)] font-semibold mb-[var(--token-space-xl)]">What I Work With</h2>`
    - Grid: `<div class="grid grid-cols-1 md:grid-cols-2 gap-[var(--token-space-lg)]">`
    - Four SkillGroup components with these exact props:
      1. `title="Languages"` `skills={["TypeScript", "JavaScript", "Python", "SQL", "HTML", "CSS"]}`
      2. `title="Frameworks"` `skills={["React", "Next.js", "Astro", "Node.js", "Express"]}`
      3. `title="Tools"` `skills={["Git", "Docker", "PostgreSQL", "VS Code", "Linux"]}`
      4. `title="Concepts"` `skills={["REST APIs", "CI/CD", "Testing", "Responsive Design", "Accessibility"]}`
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/about.astro contains `import SkillGroup from`
    - about.astro contains `<h1` with "About" in `font-display text-[length:var(--token-text-display)]`
    - about.astro contains `aria-label="Background"` (narrative section)
    - about.astro contains `I'm Jack Cutrara` (first person tone per D-05)
    - about.astro contains `I got into programming` (journey narrative per D-06/ABUT-01)
    - about.astro contains `currently looking for a software engineering role` (interests/goals per ABUT-01)
    - about.astro contains `aria-label="Skills"` (skills section)
    - about.astro contains `What I Work With` (h2 heading)
    - about.astro contains `grid-cols-1 md:grid-cols-2` (responsive skills grid)
    - about.astro contains `title="Languages"` and `title="Frameworks"` and `title="Tools"` and `title="Concepts"` (4 skill groups)
    - about.astro does NOT contain `progress` (no progress bars per ABUT-03)
    - about.astro contains `max-w-2xl` (narrative reading width)
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>About page presents first-person narrative covering background, education, path into engineering, and interests (ABUT-01, ABUT-02). Skills grouped by domain in 2-column grid without progress bars (ABUT-03). Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds with no errors
- About page has three semantic sections: header, narrative (aria-label="Background"), skills (aria-label="Skills")
- Narrative uses first person tone with 4 paragraphs
- Skills section has 4 groups in a responsive grid, no progress bars
</verification>

<success_criteria>
- About page narrative covers education, journey into engineering, and interests in first person (ABUT-01, ABUT-02)
- Skills displayed in 4 domain groups (Languages, Frameworks, Tools, Concepts) without progress bars (ABUT-03)
- Editorial design treatment with proper typography tokens (D-07)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-02-SUMMARY.md`
</output>
