---
phase: 03-core-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/ResumeEntry.astro
  - src/components/ContactChannel.astro
  - src/pages/resume.astro
  - src/pages/contact.astro
  - public/resume.pdf
autonomous: true
requirements: [RESM-01, RESM-02, CNTC-01]

must_haves:
  truths:
    - "Resume page renders styled summary with Experience, Education, and Technical Skills sections on-page"
    - "Resume page has a 'Download PDF' button above the fold linking to /resume.pdf"
    - "Contact page displays email, LinkedIn, and GitHub links with correct URLs"
    - "Contact page shows 'Currently open to opportunities' availability indicator"
  artifacts:
    - path: "src/components/ResumeEntry.astro"
      provides: "Experience/education entry for resume page"
      exports: ["Props interface"]
    - path: "src/components/ContactChannel.astro"
      provides: "Contact card with icon for contact page"
      exports: ["Props interface"]
    - path: "src/pages/resume.astro"
      provides: "Complete resume page replacing stub"
      contains: "Download PDF"
    - path: "src/pages/contact.astro"
      provides: "Complete contact page replacing stub"
      contains: "Currently open to opportunities"
    - path: "public/resume.pdf"
      provides: "Placeholder PDF for download button"
  key_links:
    - from: "src/pages/resume.astro"
      to: "src/components/CTAButton.astro"
      via: "import CTAButton"
      pattern: "import CTAButton"
    - from: "src/pages/resume.astro"
      to: "src/components/ResumeEntry.astro"
      via: "import ResumeEntry"
      pattern: "import ResumeEntry"
    - from: "src/pages/resume.astro"
      to: "public/resume.pdf"
      via: "href /resume.pdf download"
      pattern: 'href="/resume.pdf"'
    - from: "src/pages/contact.astro"
      to: "src/components/ContactChannel.astro"
      via: "import ContactChannel"
      pattern: "import ContactChannel"

user_setup:
  - service: resume-pdf
    why: "PDF download button on resume page"
    env_vars: []
    dashboard_config:
      - task: "Replace placeholder resume.pdf with actual resume"
        location: "public/resume.pdf in project root"
---

<objective>
Build the Resume and Contact pages with supporting components. Resume has a styled summary with PDF download; Contact has channel cards with availability indicator.

Purpose: Resume gives recruiters a quick scan of highlights with easy PDF access (per D-09 hybrid approach). Contact makes it dead simple to reach Jack with an availability signal (per D-13).
Output: Complete Resume page, Contact page, ResumeEntry component, ContactChannel component, placeholder PDF
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@.planning/phases/03-core-pages/03-RESEARCH.md
@.planning/phases/03-core-pages/03-01-SUMMARY.md

<interfaces>
<!-- CTAButton props (from Plan 01, src/components/CTAButton.astro) -->
```typescript
interface Props {
  href: string;
  label: string;
  download?: boolean;
  showDownloadIcon?: boolean;
}
```

<!-- BaseLayout props (from src/layouts/BaseLayout.astro) -->
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
}
```

<!-- Footer SVG icons (from src/components/Footer.astro) — reuse at 24x24 for ContactChannel -->
<!-- GitHub: <svg viewBox="0 0 24 24" fill="currentColor"> path d="M12 0C5.37..." -->
<!-- LinkedIn: <svg viewBox="0 0 24 24" fill="currentColor"> path d="M20.447 20.452..." -->
<!-- Email: <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> rect + polyline -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResumeEntry, ContactChannel components and placeholder PDF</name>
  <files>src/components/ResumeEntry.astro, src/components/ContactChannel.astro, public/resume.pdf</files>
  <read_first>
    - src/components/Footer.astro (SVG icons to reuse for ContactChannel at 24x24)
    - src/styles/global.css (design token names)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (ResumeEntry and ContactChannel component specs + Contact interaction contracts)
    - .planning/phases/03-core-pages/03-RESEARCH.md (Pattern 5: CSS Pulse Animation, Pitfall 4: Contrast)
  </read_first>
  <action>
    **ResumeEntry.astro** — Experience/education entry per UI-SPEC ResumeEntry spec:

    Props interface:
    ```typescript
    interface Props {
      title: string;
      organization: string;
      date: string;
      description?: string;
      bullets?: string[];
    }
    ```

    Layout:
    - Header row: `<div class="flex flex-col md:flex-row md:items-center md:justify-between gap-[var(--token-space-xs)]">`
      - Left: `<h3 class="font-body text-[length:var(--token-text-base)] font-semibold text-text-primary">{title}</h3>`
      - Right: `<span class="font-mono text-[length:var(--token-text-sm)] text-text-secondary">{organization} · {date}</span>`
    - Description (if provided): `<p class="font-body text-[length:var(--token-text-base)] text-text-secondary mt-[var(--token-space-md)]">{description}</p>`
    - Bullets (if provided): `<ul class="mt-[var(--token-space-md)] space-y-1">` with each `<li class="font-body text-[length:var(--token-text-base)] text-text-secondary">` prefixed with `— ` (em dash, not bullet point, for editorial feel)
    - Entry spacing: `mb-[var(--token-space-xl)]` on outer `<div>`

    Note: Use `text-text-primary` for title (on bg-secondary background) and `text-text-secondary` for description/org/date per UI-SPEC. The title at font-semibold is large enough for secondary to be acceptable on the org line.

    **ContactChannel.astro** — Contact card per UI-SPEC ContactChannel spec:

    Props interface:
    ```typescript
    interface Props {
      href: string;
      label: string;
      value: string;
      icon: "github" | "linkedin" | "email";
      external?: boolean;
    }
    ```

    Element: `<a>` wrapping entire card.
    - `href={href}`
    - If `external`: add `target="_blank" rel="noopener noreferrer"`
    - If `external`: append visually hidden `<span class="sr-only"> (opens in new tab)</span>` per accessibility contract
    - Container classes: `flex items-center gap-[var(--token-space-md)] bg-bg-secondary border border-border rounded-lg p-[var(--token-space-lg)] w-full transition-colors duration-200 hover:border-border-hover focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none`
    - Icon: 24x24 SVG in `text-text-secondary flex-shrink-0`. Use the SAME SVG paths from Footer.astro but at `width="24" height="24"`:
      - `github`: `<svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12Z"/></svg>`
      - `linkedin`: `<svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286ZM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065Zm1.782 13.019H3.555V9h3.564v11.452ZM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003Z"/></svg>`
      - `email`: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="4" width="20" height="16" rx="2"/><polyline points="22,4 12,13 2,4"/></svg>`
    - Text group: `<div>`
      - Label: `<span class="block font-mono text-[length:var(--token-text-sm)] text-text-muted uppercase">{label}</span>`
      - Value: `<span class="block font-body text-[length:var(--token-text-base)] text-text-primary">{value}</span>`

    **public/resume.pdf** — Create a minimal placeholder PDF. Use a simple command to generate a one-page PDF with text "Jack Cutrara - Resume - Placeholder". If PDF generation tools are not available, create a minimal valid PDF file with raw bytes (a valid PDF is ~200 bytes minimum). The user will replace this with their real resume per D-10. Document this in the summary.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ResumeEntry.astro contains `interface Props` with `title: string` and `organization: string` and `date: string`
    - ResumeEntry.astro contains `font-semibold text-text-primary` (title styling)
    - ResumeEntry.astro contains `font-mono` (org/date styling)
    - ResumeEntry.astro contains `mb-[var(--token-space-xl)]` (entry spacing)
    - src/components/ContactChannel.astro contains `interface Props` with `href: string` and `icon: "github" | "linkedin" | "email"`
    - ContactChannel.astro contains `bg-bg-secondary border border-border rounded-lg`
    - ContactChannel.astro contains `hover:border-border-hover`
    - ContactChannel.astro contains `target="_blank"` (for external links)
    - ContactChannel.astro contains `sr-only` (visually hidden "opens in new tab")
    - ContactChannel.astro contains `aria-hidden="true"` (on SVG icons)
    - public/resume.pdf exists and is a valid file (size > 0 bytes)
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>ResumeEntry and ContactChannel components exist with typed props and correct styling. Placeholder resume PDF exists in public/. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build complete Resume and Contact pages replacing stubs</name>
  <files>src/pages/resume.astro, src/pages/contact.astro</files>
  <read_first>
    - src/pages/resume.astro (current stub to replace)
    - src/pages/contact.astro (current stub to replace)
    - src/components/CTAButton.astro (from Plan 01 — verify props for download button)
    - src/components/ResumeEntry.astro (just created — verify props)
    - src/components/ContactChannel.astro (just created — verify props)
    - src/layouts/BaseLayout.astro (layout props)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (Resume Page + Contact Page compositions, Copywriting Contract, Interaction Contracts)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-09, D-10, D-11, D-12, D-13, D-14 decisions)
  </read_first>
  <action>
    **Resume page (resume.astro)** — Replace stub with full page per UI-SPEC Resume Page composition. Per D-09 (hybrid summary + PDF download):

    Frontmatter:
    ```typescript
    import BaseLayout from "../layouts/BaseLayout.astro";
    import CTAButton from "../components/CTAButton.astro";
    import ResumeEntry from "../components/ResumeEntry.astro";
    ```

    BaseLayout: `<BaseLayout title="Resume" description="View and download Jack Cutrara's software engineering resume.">`

    **Section 1 — Page Header with Download CTA** (RESM-02):
    - `<section class="py-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]">`
    - Inner `max-w-5xl mx-auto`
    - `<h1 class="font-display text-[length:var(--token-text-display)] tracking-[-0.03em]">Resume</h1>`
    - CTA below heading: `<div class="mt-[var(--token-space-lg)]">` containing `<CTAButton href="/resume.pdf" label="Download PDF" download={true} showDownloadIcon={true} />`

    **Section 2 — Styled Summary** (RESM-01):
    - `<section aria-label="Resume summary" class="pb-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]">`
    - Inner `max-w-3xl mx-auto`
    - Container: `<div class="bg-bg-secondary border border-border rounded-lg p-[var(--token-space-lg)] md:p-[var(--token-space-xl)]">`

    **Experience subsection:**
    - `<h2 class="font-body text-[length:var(--token-text-heading)] font-semibold border-b border-border pb-[var(--token-space-sm)] mb-[var(--token-space-lg)]">Experience</h2>`
    - ResumeEntry 1: `title="Software Engineering Intern"` `organization="Tech Company"` `date="Summer 2025"` `bullets={["Built and deployed internal tool reducing manual data processing by 40%", "Collaborated with senior engineers on REST API design and implementation", "Wrote unit and integration tests achieving 85% code coverage"]}`
    - ResumeEntry 2: `title="Freelance Web Developer"` `organization="Self-employed"` `date="2024 — Present"` `bullets={["Designed and built responsive websites for local businesses using modern frameworks", "Managed full project lifecycle from requirements gathering to deployment"]}`

    **Education subsection:**
    - `<h2 class="font-body text-[length:var(--token-text-heading)] font-semibold border-b border-border pb-[var(--token-space-sm)] mb-[var(--token-space-lg)] mt-[var(--token-space-2xl)]">Education</h2>`
    - ResumeEntry: `title="B.S. Computer Science"` `organization="University"` `date="2025"` `description="Relevant coursework: Data Structures, Algorithms, Database Systems, Software Engineering, Operating Systems"`

    **Technical Skills subsection:**
    - `<h2 class="font-body text-[length:var(--token-text-heading)] font-semibold border-b border-border pb-[var(--token-space-sm)] mb-[var(--token-space-lg)] mt-[var(--token-space-2xl)]">Technical Skills</h2>`
    - Simple text: `<p class="font-body text-[length:var(--token-text-base)] text-text-primary">TypeScript, JavaScript, Python, React, Next.js, Astro, Node.js, PostgreSQL, Docker, Git, REST APIs, CI/CD</p>`

    Note: All resume content is placeholder per D-04 / D-10. User will revise.

    ---

    **Contact page (contact.astro)** — Replace stub with full page per UI-SPEC Contact Page composition. Per D-12 (dedicated page), D-13 (availability status), D-14 (email, LinkedIn, GitHub):

    Frontmatter:
    ```typescript
    import BaseLayout from "../layouts/BaseLayout.astro";
    import ContactChannel from "../components/ContactChannel.astro";
    ```

    BaseLayout: `<BaseLayout title="Contact" description="Get in touch with Jack Cutrara via email, LinkedIn, or GitHub.">`

    **Section 1 — Page Header** (D-12):
    - `<section class="py-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]">`
    - Inner `max-w-5xl mx-auto`
    - `<h1 class="font-display text-[length:var(--token-text-display)] tracking-[-0.03em]">Get in Touch</h1>`
    - Subtext: `<p class="font-body text-[length:var(--token-text-base)] text-text-secondary mt-[var(--token-space-md)] max-w-2xl">I'm always open to hearing about new opportunities, interesting projects, or just connecting with other engineers.</p>`

    **Availability Badge** (D-13):
    - `<div class="flex items-center gap-[var(--token-space-sm)] mt-[var(--token-space-lg)] mb-[var(--token-space-2xl)]">`
    - Pulsing dot: `<span class="availability-dot" aria-hidden="true"></span>`
    - Status text: `<span class="font-body text-[length:var(--token-text-sm)] text-text-secondary">Currently open to opportunities</span>`

    **Section 2 — Contact Channels** (CNTC-01, D-14):
    - `<section aria-label="Contact information" class="pb-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]">`
    - Inner `max-w-lg mx-auto` (narrow for card stack)
    - `<div class="flex flex-col gap-[var(--token-space-md)]">`
    - ContactChannel 1: `href="mailto:jack@jackcutrara.com"` `label="Email"` `value="jack@jackcutrara.com"` `icon="email"`
    - ContactChannel 2: `href="https://linkedin.com/in/jackcutrara"` `label="LinkedIn"` `value="linkedin.com/in/jackcutrara"` `icon="linkedin"` `external={true}`
    - ContactChannel 3: `href="https://github.com/jackc625"` `label="GitHub"` `value="github.com/jackc625"` `icon="github"` `external={true}`

    **Scoped pulse animation CSS** — Add `<style>` block at the bottom of contact.astro (before closing `</BaseLayout>`):
    ```css
    <style is:global>
      .availability-dot {
        position: relative;
        width: 8px;
        height: 8px;
        border-radius: 50%;
        background-color: var(--token-accent);
        flex-shrink: 0;
      }
      .availability-dot::before {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: 50%;
        background-color: var(--token-accent);
        animation: pulse-ring 2s ease-out infinite;
      }
      @keyframes pulse-ring {
        0% { transform: scale(1); opacity: 1; }
        100% { transform: scale(1.5); opacity: 0; }
      }
      @media (prefers-reduced-motion: reduce) {
        .availability-dot::before {
          animation: none;
          display: none;
        }
      }
    </style>
    ```
    Use `is:global` because Astro's scoping may interfere with pseudo-element animations.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/resume.astro contains `import CTAButton from` and `import ResumeEntry from`
    - resume.astro contains `<h1` with "Resume" in `font-display text-[length:var(--token-text-display)]`
    - resume.astro contains `href="/resume.pdf"` and `download={true}` and `showDownloadIcon={true}`
    - resume.astro contains `Download PDF` (CTA label)
    - resume.astro contains `aria-label="Resume summary"` (summary section)
    - resume.astro contains `Experience` and `Education` and `Technical Skills` (three subsection h2s)
    - resume.astro contains `bg-bg-secondary border border-border rounded-lg` (container styling)
    - resume.astro contains `max-w-3xl` (summary max width)
    - src/pages/contact.astro contains `import ContactChannel from`
    - contact.astro contains `<h1` with "Get in Touch" in `font-display text-[length:var(--token-text-display)]`
    - contact.astro contains `Currently open to opportunities` (availability text per D-13)
    - contact.astro contains `availability-dot` (pulsing dot class)
    - contact.astro contains `aria-label="Contact information"` (channels section)
    - contact.astro contains `mailto:jack@jackcutrara.com` (email link)
    - contact.astro contains `linkedin.com/in/jackcutrara` (LinkedIn)
    - contact.astro contains `github.com/jackc625` (GitHub)
    - contact.astro contains `@keyframes pulse-ring` (animation)
    - contact.astro contains `prefers-reduced-motion` (a11y)
    - contact.astro contains `max-w-lg` (channel stack width)
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>Resume page shows styled summary with Experience/Education/Skills sections and PDF download button above fold (RESM-01, RESM-02). Contact page displays email, LinkedIn, GitHub channels with availability badge (CNTC-01). Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds with no errors
- Resume page has download button linking to /resume.pdf with download attribute
- Resume page has three content sections (Experience, Education, Technical Skills) inside styled container
- Contact page has three channel cards with correct URLs
- Contact page has availability badge with pulse animation and reduced-motion fallback
- All external links have target="_blank" and rel="noopener noreferrer"
</verification>

<success_criteria>
- Resume page renders styled summary on-page (RESM-01)
- PDF download button visible above the fold with download icon (RESM-02)
- Contact page at /contact (dedicated page per D-12) shows email, LinkedIn, GitHub (CNTC-01, D-14)
- Availability status indicator present (D-13)
- All pages responsive on mobile and desktop
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-03-SUMMARY.md`
</output>
