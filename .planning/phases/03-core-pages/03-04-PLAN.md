---
phase: 03-core-pages
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified: []
autonomous: false
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, ABUT-01, ABUT-02, ABUT-03, RESM-01, RESM-02, CNTC-01]

must_haves:
  truths:
    - "All four core pages render correctly on desktop and mobile with no visual defects"
    - "All navigation links between pages work correctly"
    - "Featured projects on home page display from content collection"
    - "Resume PDF download works"
    - "Contact links open correct destinations"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all four core pages (Home, About, Resume, Contact) across desktop and mobile viewports.

Purpose: Visual/functional verification that cannot be automated — editorial design quality, typography rendering, responsive layout, and interactive states all need human eyes.
Output: Verification confirmation or list of issues to fix
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All four core pages: Home (hero + featured projects + about teaser + quick links), About (narrative + skills grid), Resume (styled summary + PDF download), Contact (channel cards + availability badge). Built with editorial typography, design tokens, and responsive layout.</what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev` (or it may already be running)
    2. Open http://localhost:4321/ in your browser

    **Home page (/):**
    - Verify hero: "Jack Cutrara" in large serif, "SOFTWARE ENGINEER" role line, tagline text, "View Projects" outline button
    - Verify "Selected Work" section shows 3 featured projects as editorial list rows (not cards)
    - Hover over project rows — titles should shift to accent color
    - Verify about teaser text with "About me" link
    - Verify "Resume" and "Get in Touch" text links at bottom
    - Click "View Projects" — should navigate to /projects

    **About page (/about):**
    - Verify "About" heading in display serif font
    - Verify narrative text in first person ("I'm Jack Cutrara...")
    - Verify 4 skill groups in grid: Languages, Frameworks, Tools, Concepts
    - Verify no progress bars anywhere
    - Verify skills grid collapses to 1 column on mobile

    **Resume page (/resume):**
    - Verify "Resume" heading with "Download PDF" button with download icon
    - Click "Download PDF" — should download resume.pdf (placeholder is fine)
    - Verify styled summary card with Experience, Education, Technical Skills sections
    - Verify resume entry layout (title left, org/date right on desktop)

    **Contact page (/contact):**
    - Verify "Get in Touch" heading with subtext
    - Verify pulsing green dot with "Currently open to opportunities"
    - Verify 3 contact channel cards: Email, LinkedIn, GitHub
    - Click email — should open mailto:
    - Click LinkedIn — should open in new tab
    - Click GitHub — should open in new tab
    - Hover over cards — border should lighten

    **Responsive (resize browser or use DevTools):**
    - All 4 pages render correctly at mobile width (~375px)
    - Featured project rows stack on mobile
    - Skills grid goes single column on mobile
    - Resume entries stack on mobile

    **Navigation:**
    - All nav links in header work (Home, About, Projects, Resume, Contact)
    - Footer links still work
    - Back/forward browser navigation works (View Transitions)
  </how-to-verify>
  <resume-signal>Type "approved" if all pages look good, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all four pages at desktop and mobile viewports.
</verification>

<success_criteria>
- User confirms all four pages render correctly
- User confirms editorial design quality meets expectations
- User confirms responsive layout works on mobile
- User confirms all interactive elements function (links, hover states, download)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-04-SUMMARY.md`
</output>
