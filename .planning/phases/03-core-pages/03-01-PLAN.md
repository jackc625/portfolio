---
phase: 03-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CTAButton.astro
  - src/components/FeaturedProjectItem.astro
  - src/content/projects/placeholder-devtools.mdx
  - src/content/projects/placeholder-api.mdx
  - src/pages/index.astro
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04]

must_haves:
  truths:
    - "Home page displays hero with Jack's name, role 'SOFTWARE ENGINEER', tagline, and 'View Projects' CTA linking to /projects"
    - "Home page shows 2-3 featured projects from content collection in an editorial list format, each linking to a project page"
    - "Home page has an about teaser section with link to /about"
    - "Home page has prominent links to /resume and /contact"
  artifacts:
    - path: "src/components/CTAButton.astro"
      provides: "Reusable CTA button used on Home hero and Resume download"
      exports: ["Props interface"]
    - path: "src/components/FeaturedProjectItem.astro"
      provides: "Editorial project row for home page featured section"
      exports: ["Props interface"]
    - path: "src/pages/index.astro"
      provides: "Complete home page replacing stub"
      contains: "getCollection"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/content/projects/*.mdx"
      via: "getCollection('projects', featured === true)"
      pattern: "getCollection.*projects.*featured"
    - from: "src/pages/index.astro"
      to: "src/components/CTAButton.astro"
      via: "import CTAButton"
      pattern: "import CTAButton"
    - from: "src/pages/index.astro"
      to: "src/components/FeaturedProjectItem.astro"
      via: "import FeaturedProjectItem"
      pattern: "import FeaturedProjectItem"
---

<objective>
Build the complete Home page with bold typographic hero, featured projects section from content collection, about teaser, and resume/contact links. Also create the shared CTAButton component and additional sample MDX files so the featured projects section has 2-3 items.

Purpose: The Home page is the first thing recruiters see. It must be editorial and non-generic (per D-02), with typography doing the heavy lifting (per D-01), and featured projects woven into the design as an editorial list (per D-03).
Output: Complete Home page, CTAButton component, FeaturedProjectItem component, 2 additional sample MDX files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@.planning/phases/03-core-pages/03-RESEARCH.md

<interfaces>
<!-- BaseLayout props (from src/layouts/BaseLayout.astro) -->
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
}
```

<!-- Content collection schema (from src/content.config.ts) -->
```typescript
z.object({
  title: z.string(),
  tagline: z.string().max(80),
  description: z.string(),
  techStack: z.array(z.string()).min(1),
  featured: z.boolean().default(false),
  status: z.enum(["completed", "in-progress"]),
  githubUrl: z.string().url().optional(),
  demoUrl: z.string().url().optional(),
  thumbnail: image(),
  category: z.enum(["web-app", "cli-tool", "library", "api", "other"]),
  order: z.number().int().min(1),
})
```

<!-- Existing _sample.mdx frontmatter pattern -->
```yaml
title: "Sample Project"
tagline: "A brief project summary goes here"
techStack: ["Astro", "TypeScript", "Tailwind CSS"]
featured: true
status: "completed"
githubUrl: "https://github.com/example/sample-project"
thumbnail: "../../assets/images/sample-thumbnail.png"
category: "web-app"
order: 1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CTAButton, FeaturedProjectItem components and sample MDX files</name>
  <files>src/components/CTAButton.astro, src/components/FeaturedProjectItem.astro, src/content/projects/placeholder-devtools.mdx, src/content/projects/placeholder-api.mdx</files>
  <read_first>
    - src/components/Footer.astro (SVG icon patterns and Tailwind class conventions)
    - src/styles/global.css (design token names and values)
    - src/content/projects/_sample.mdx (frontmatter schema pattern)
    - src/content.config.ts (Zod schema for validation)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (component specs)
  </read_first>
  <action>
    **CTAButton.astro** — Create reusable CTA anchor component per UI-SPEC CTAButton spec and RESEARCH code example. Per D-01 (bold typographic hero CTA):

    Props interface:
    ```typescript
    interface Props {
      href: string;
      label: string;
      download?: boolean;
      showDownloadIcon?: boolean;
    }
    ```

    Element: `<a>` (NOT `<button>` — these are navigation links).
    Classes: `inline-flex items-center gap-[var(--token-space-sm)] border border-accent text-accent px-[var(--token-space-lg)] py-[var(--token-space-sm)] font-body text-[length:var(--token-text-base)] font-semibold uppercase tracking-[0.05em] rounded transition-colors duration-200 hover:bg-accent hover:text-bg-primary focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none`

    When `download` is true, add `download` attribute and `aria-label="Download {label}"`.
    When `showDownloadIcon` is true, render a 20x20 inline SVG download arrow icon (arrow pointing down into tray) after the label text with `ml-[var(--token-space-sm)]` gap:
    ```svg
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
      <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
      <polyline points="7 10 12 15 17 10" />
      <line x1="12" y1="15" x2="12" y2="3" />
    </svg>
    ```

    **FeaturedProjectItem.astro** — Editorial project row for home page per UI-SPEC FeaturedProjectItem spec. Per D-03 (projects woven into design, not card grid):

    Props interface:
    ```typescript
    interface Props {
      title: string;
      tagline: string;
      techStack: string[];
      slug: string;
    }
    ```

    Element: Outer `<a>` wrapping full row, href=`/projects/{slug}`.
    Layout: Flex row on desktop (`md:flex-row md:items-center md:justify-between`), flex column on mobile.
    - Left: Title in `font-display text-[length:var(--token-text-heading)] font-normal text-text-primary`
    - Right (desktop) / Below (mobile): Tagline in `font-body text-[length:var(--token-text-sm)] text-text-secondary`
    - Below both: Tech stack in `font-mono text-[length:var(--token-text-sm)] text-text-muted` comma-separated
    - Bottom border: `border-b border-border`
    - Padding: `py-[var(--token-space-lg)]` (24px)
    - Hover: Title shifts to `text-accent`, border shifts to `border-border-hover`. Use `group` on the `<a>` and `group-hover:text-accent` on title, `group-hover:border-border-hover` on the container.
    - Transition: `transition-colors duration-200`
    - Focus: `focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-4 focus-visible:rounded`

    **placeholder-devtools.mdx** — Second featured project sample. Frontmatter:
    ```yaml
    title: "DevTools Dashboard"
    tagline: "Real-time developer productivity metrics"
    description: "A dashboard for tracking developer workflow metrics. Placeholder for Phase 4 case study."
    techStack: ["React", "Node.js", "PostgreSQL"]
    featured: true
    status: "completed"
    githubUrl: "https://github.com/example/devtools-dashboard"
    thumbnail: "../../assets/images/sample-thumbnail.png"
    category: "web-app"
    order: 2
    ```
    Body: `# DevTools Dashboard\n\nPlaceholder content for schema validation. Will be replaced with real case study in Phase 4.`

    **placeholder-api.mdx** — Third featured project sample. Frontmatter:
    ```yaml
    title: "REST API Toolkit"
    tagline: "Type-safe API client generator for TypeScript"
    description: "A CLI tool that generates type-safe API clients from OpenAPI specs. Placeholder for Phase 4 case study."
    techStack: ["TypeScript", "Node.js", "Zod"]
    featured: true
    status: "completed"
    githubUrl: "https://github.com/example/api-toolkit"
    thumbnail: "../../assets/images/sample-thumbnail.png"
    category: "cli-tool"
    order: 3
    ```
    Body: `# REST API Toolkit\n\nPlaceholder content for schema validation. Will be replaced with real case study in Phase 4.`
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CTAButton.astro contains `interface Props` with `href: string` and `label: string` and `download?: boolean` and `showDownloadIcon?: boolean`
    - CTAButton.astro uses `<a` element (not `<button`)
    - CTAButton.astro contains `hover:bg-accent hover:text-bg-primary`
    - CTAButton.astro contains `focus-visible:ring-2 focus-visible:ring-accent`
    - src/components/FeaturedProjectItem.astro contains `interface Props` with `title: string` and `slug: string`
    - FeaturedProjectItem.astro contains `group-hover:text-accent`
    - FeaturedProjectItem.astro contains `border-b border-border`
    - FeaturedProjectItem.astro contains `font-display`
    - src/content/projects/placeholder-devtools.mdx contains `featured: true` and `order: 2`
    - src/content/projects/placeholder-api.mdx contains `featured: true` and `order: 3`
    - `npx astro build` exits 0 (Zod validates all MDX frontmatter)
  </acceptance_criteria>
  <done>CTAButton and FeaturedProjectItem components exist with typed props and correct styling. Three MDX files with featured: true exist for content collection query. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build complete Home page replacing stub</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current stub to replace)
    - src/components/CTAButton.astro (just created — verify props)
    - src/components/FeaturedProjectItem.astro (just created — verify props)
    - src/layouts/BaseLayout.astro (layout props and structure)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (Home Page Compositions section + Copywriting Contract)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-01, D-02, D-03, D-04 decisions)
  </read_first>
  <action>
    Replace the stub in index.astro with the full Home page per UI-SPEC Home Page composition. Per D-02 (NON-GENERIC editorial design) and D-01 (bold typographic hero):

    **Frontmatter block:**
    ```typescript
    import BaseLayout from "../layouts/BaseLayout.astro";
    import { getCollection } from "astro:content";
    import CTAButton from "../components/CTAButton.astro";
    import FeaturedProjectItem from "../components/FeaturedProjectItem.astro";

    const featuredProjects = (
      await getCollection("projects", ({ data }) => data.featured === true)
    ).sort((a, b) => a.data.order - b.data.order);
    ```

    **BaseLayout wrapper:**
    ```html
    <BaseLayout
      title=""
      description="Software engineer who builds real projects. Explore my work, background, and how to get in touch."
    >
    ```
    Note: `title=""` uses titleDefault "Jack Cutrara | Software Engineer" per UI-SPEC Meta Copy.

    **Section 1 — Hero** (per UI-SPEC Hero Section, HOME-01):
    - `<section aria-label="Introduction">` with `min-h-[calc(100vh-4rem)] flex flex-col justify-center px-[var(--token-space-md)] md:px-[var(--token-space-lg)]`
    - Inner `max-w-5xl mx-auto w-full`
    - `<h1>` "Jack Cutrara" in `font-display text-[length:var(--token-text-display)] tracking-[-0.03em]`
    - Role line: `<p>` "SOFTWARE ENGINEER" in `font-body text-[length:var(--token-text-heading)] font-normal text-text-secondary uppercase tracking-[0.1em] mt-[var(--token-space-lg)]`
    - Tagline: `<p>` "I build things that work. Full-stack developer with a bias for shipping real projects and learning by doing." in `font-body text-[length:var(--token-text-base)] text-text-secondary mt-[var(--token-space-md)] max-w-2xl`
    - CTA: `<div class="mt-[var(--token-space-xl)]">` containing `<CTAButton href="/projects" label="View Projects" />`

    **Section 2 — Featured Projects** (per UI-SPEC Featured Projects Section, HOME-02):
    - `<section aria-label="Selected work">` with `py-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]`
    - Inner `max-w-5xl mx-auto`
    - `<h2>` "Selected Work" in `font-body text-[length:var(--token-text-heading)] font-semibold mb-[var(--token-space-xl)]`
    - Conditional rendering: if `featuredProjects.length > 0`, map over `featuredProjects` rendering `<FeaturedProjectItem>` for each with props: `title={project.data.title}`, `tagline={project.data.tagline}`, `techStack={project.data.techStack}`, `slug={project.id}`
    - Empty state: `<p class="text-text-secondary font-body text-[length:var(--token-text-base)]">Projects coming soon. Check back for updates on what I'm building.</p>`

    **Section 3 — About Teaser** (per UI-SPEC About Teaser Section, HOME-03):
    - `<section aria-label="About">` with `py-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]`
    - Inner `max-w-2xl mx-auto` (reading width)
    - `<p>` with text: `I'm a software engineer who got here by building things — not by waiting for permission. Curious about the full story?` in `font-body text-[length:var(--token-text-base)] text-text-secondary`
    - Inline link: ` ` then `<a href="/about" class="text-accent hover:text-accent-hover transition-colors duration-200">About me</a>`

    **Section 4 — Resume/Contact Links** (per UI-SPEC Resume/Contact Links Section, HOME-04):
    - `<section aria-label="Quick links">` with `py-[var(--token-space-section)] px-[var(--token-space-md)] md:px-[var(--token-space-lg)]`
    - Inner `max-w-5xl mx-auto`
    - Two text links in a flex row with gap: `flex flex-wrap gap-[var(--token-space-xl)]`
    - "Resume" link: `<a href="/resume" class="font-body text-[length:var(--token-text-heading)] font-semibold text-text-primary hover:text-accent transition-colors duration-200">Resume</a>`
    - "Get in Touch" link: `<a href="/contact" class="font-body text-[length:var(--token-text-heading)] font-semibold text-text-primary hover:text-accent transition-colors duration-200">Get in Touch</a>`
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && rtk npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro contains `import { getCollection } from "astro:content"`
    - index.astro contains `import CTAButton from` and `import FeaturedProjectItem from`
    - index.astro contains `getCollection("projects"` with `featured === true`
    - index.astro contains `aria-label="Introduction"` (hero section)
    - index.astro contains `<h1` with "Jack Cutrara"
    - index.astro contains `SOFTWARE ENGINEER` (role line)
    - index.astro contains `View Projects` (CTA label)
    - index.astro contains `aria-label="Selected work"` (featured projects section)
    - index.astro contains `Selected Work` (h2 heading)
    - index.astro contains `aria-label="About"` (about teaser section)
    - index.astro contains `href="/about"` (about link)
    - index.astro contains `aria-label="Quick links"` (resume/contact section)
    - index.astro contains `href="/resume"` and `href="/contact"`
    - index.astro contains `min-h-[calc(100vh-4rem)]` (hero viewport height)
    - index.astro contains `tracking-[-0.03em]` (display letter spacing)
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>Home page displays hero with name/role/tagline/CTA, featured projects editorial list from content collection, about teaser with link, and resume/contact text links. All four HOME requirements satisfied. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds with no errors
- Built output includes index.html with all four sections (hero, featured projects, about teaser, quick links)
- Content collection query returns 3 featured projects sorted by order
- All links (/projects, /about, /resume, /contact) are present in the home page HTML
</verification>

<success_criteria>
- Home page hero displays "Jack Cutrara", "SOFTWARE ENGINEER", tagline, and "View Projects" CTA linking to /projects (HOME-01)
- Featured projects section shows 2-3 items from content collection with editorial list layout (HOME-02)
- About teaser with "About me" link to /about exists (HOME-03)
- "Resume" and "Get in Touch" text links to /resume and /contact exist (HOME-04)
- CTAButton component is reusable (will be consumed by Resume page in Plan 03)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-01-SUMMARY.md`
</output>
