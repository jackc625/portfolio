---
phase: 03-core-pages
plan: 06
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-04
  - 03-05
files_modified: []
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - ABUT-01
  - ABUT-02
  - ABUT-03
  - RESM-01
  - RESM-02
  - CNTC-01
must_haves:
  truths:
    - "All four core pages visually match shiyunlu.com's design language"
    - "Canvas hero renders generative art on home page"
    - "Navigation works on desktop and mobile across all pages"
    - "All content is readable and properly laid out at 375px, 768px, and 1280px"
    - "Site builds with zero errors"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete Phase 3 rebuild. Human confirms that all four core pages match shiyunlu.com's design language and are responsive across breakpoints.

Purpose: Claude cannot visually verify design fidelity. This checkpoint ensures the site actually looks like shiyunlu.com before moving to Phase 4. Previous Phase 3 attempts failed because no visual verification was done.

Output: Human approval or list of issues to address.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-03-SUMMARY.md
@.planning/phases/03-core-pages/03-04-SUMMARY.md
@.planning/phases/03-core-pages/03-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification of complete design overhaul</name>
  <files>none (visual verification only)</files>
  <action>
    Present the completed site to the user for visual verification. Start the dev server with `pnpm dev` and provide the verification checklist below. Wait for user approval before marking Phase 3 complete.

    Complete visual rebuild of the portfolio site to clone shiyunlu.com's design language:
    - Updated design tokens (colors, fonts, spacing) matching shiyunlu.com
    - Rewritten site shell (header nav, footer, mobile menu)
    - Home page with generative canvas hero (simplex-noise flow field)
    - About page with narrative and skills in new design system
    - Resume page with styled summary and PDF download
    - Contact page with links and availability indicator

    Verification checklist for the user:

    Step 1: Start dev server — run `pnpm dev` and open http://localhost:4321

    Step 2: Side-by-side comparison at 1280px desktop
    Open shiyunlu.com in another browser tab. Compare at 1280px width:
    1. HOME (/) — Does the spatial layout, typography hierarchy, and color treatment match shiyunlu.com? Does the canvas hero render generative art?
    2. ABOUT (/about) — Does it feel like it belongs in shiyunlu.com's design system? Is the narrative readable? Are skills presented cleanly?
    3. RESUME (/resume) — Is the PDF download button visible above the fold? Does the styled summary look professional within the design system?
    4. CONTACT (/contact) — Are all three links visible? Does the availability dot pulse? Do external links open in new tabs?

    Step 3: Navigation check
    - Click through all nav links on desktop — do they work?
    - Check active state highlighting on the current page link

    Step 4: Mobile check at 375px
    - Use DevTools responsive mode at 375px width
    - Open the hamburger menu — does it match shiyunlu.com's mobile pattern?
    - Check all four pages are readable at mobile width
    - Verify the canvas hero performs acceptably (no major lag)

    Step 5: Tablet check at 768px
    - Quick pass at 768px to verify no layout breakage

    Step 6: Accessibility quick-check
    - Tab through the page — are focus indicators visible?
    - Escape key closes mobile menu?

    Step 7: Build verification
    Run `npx astro build` — should exit 0 with no errors.

    Resume signal: Type "approved" to proceed to Phase 4, or describe specific issues to fix.
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <done>Human confirms site visually matches shiyunlu.com's design language across all pages and breakpoints, or provides specific issues to address</done>
</task>

</tasks>

<verification>
Human visual verification at desktop (1280px), mobile (375px), and tablet (768px). Side-by-side comparison with shiyunlu.com. Navigation, accessibility, and build verification.
</verification>

<success_criteria>
Human confirms:
- Site visually matches shiyunlu.com's design language (colors, fonts, spacing, layout)
- All four core pages render correctly at all breakpoints
- Canvas hero works and looks good
- Navigation works on desktop and mobile
- Build passes
- No blocking visual issues
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-06-SUMMARY.md`
</output>
