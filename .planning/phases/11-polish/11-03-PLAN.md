---
phase: 11-polish
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - CLAUDE.md
autonomous: false
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06

must_haves:
  truths:
    - "feat/ui-redesign branch is merged to main with --no-ff merge commit"
    - "Build passes on main branch after merge"
    - "Site is deployed to Cloudflare Pages at jackcutrara.com with the editorial design"
    - "Production site verified: homepage, project detail, chat functionality, OG meta"
    - "PROJECT.md reflects v1.1 completion with updated tech stack"
    - "CLAUDE.md has real Conventions and Architecture sections"
    - "REQUIREMENTS.md marks QUAL-01..06 as complete, 25/25 coverage"
    - "STATE.md records milestone v1.1 as complete"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated current state, validated requirements, v1.1 key decisions"
    - path: "CLAUDE.md"
      provides: "Updated Conventions and Architecture sections reflecting v1.1 stack"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QUAL-01..06 marked complete, 25/25 coverage"
    - path: ".planning/STATE.md"
      provides: "Milestone v1.1 recorded as complete"
  key_links:
    - from: "feat/ui-redesign"
      to: "main"
      via: "git merge --no-ff"
      pattern: "merge"
    - from: "main"
      to: "Cloudflare Pages"
      via: "git push origin main (auto-deploy)"
      pattern: "push"
---

<objective>
Merge the editorial redesign branch to main with pre-merge safety checks, deploy to Cloudflare Pages with production verification, and update all project documentation to close out milestone v1.1.

Purpose: Ship the redesigned portfolio to production and update all tracking artifacts so the project reflects its current state accurately. Addresses review concerns about merge safety, weak production verification, and missing rollback strategy.
Output: Live site at jackcutrara.com with editorial design. All project docs updated. Milestone v1.1 complete.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-polish/11-CONTEXT.md
@.planning/phases/11-polish/11-AUDIT.md
@.planning/phases/11-polish/11-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-merge safety checks and merge feat/ui-redesign to main (D-17)</name>
  <files></files>
  <read_first>
    - .planning/phases/11-polish/11-AUDIT.md (confirm all 6 QUAL requirements show PASS before merging)
    - .planning/phases/11-polish/11-02-SUMMARY.md (confirm audit plan completed successfully)
  </read_first>
  <action>
**Addresses review concern (Codex, HIGH): Pre-merge checks for clean merge path.** Both Codex and Gemini raised concerns about assuming a clean merge. This task adds explicit pre-merge validation.

**Step 1: Verify audit gate passed.**

Before ANY merge activity, confirm the audit is complete:
```bash
grep "QUAL-01.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-02.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-03.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-04.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-05.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-06.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "quality gate PASSED" .planning/phases/11-polish/11-AUDIT.md
```
All 7 greps must return matches. If any is missing or shows FAIL, do NOT proceed -- go back to Plan 02.

**Step 2: Pre-merge safety checks (addresses Codex HIGH concern).**

```bash
# Ensure we're on the feature branch
git branch --show-current
# Expected: feat/ui-redesign

# Sync main to latest remote state
git fetch origin main

# Check for divergence between local main and remote main
git log main..origin/main --oneline
# Expected: empty (no commits on remote main that we don't have locally)
# If non-empty: run `git checkout main && git pull origin main && git checkout feat/ui-redesign`

# Preview merge stat to check for unexpected changes
git diff main...feat/ui-redesign --stat | tail -5
# Review: confirm expected file count, no unexpected deletions

# Dry-run merge to check for conflicts
git merge-tree $(git merge-base main feat/ui-redesign) main feat/ui-redesign 2>&1 | head -20
# If conflicts appear, document them and present to user before proceeding
```

If any conflicts are detected, STOP and present them to the user with resolution options. Do not force-merge.

**Addresses review concern (project memory): Merge audit per feedback_merge_safety.md.** The project has a history of destructive merge commits. Audit the diff stat before proceeding.

**Step 3: Commit any uncommitted Phase 11 work on feature branch.**

```bash
git status
```

If there are uncommitted changes from Plan 01/02 execution, commit them first:
```bash
git add .planning/phases/11-polish/ src/styles/global.css src/layouts/BaseLayout.astro src/components/JsonLd.astro src/components/primitives/Container.astro src/components/primitives/WorkRow.astro src/components/ContactSection.astro src/components/primitives/Footer.astro astro.config.mjs
git commit -m "feat(11): phase 11 polish -- focus rings, contrast fix, tech debt, audit

- Add comprehensive focus-visible rings on all interactive elements (QUAL-03)
- Fix accent contrast for chat bot links (QUAL-05)
- Fix lightning-css warnings via @source not directives (D-05)
- Add BaseLayout noindex prop (D-06)
- Fix JsonLd is:inline + Container Props hints (D-07)
- Delete mockup.html and /dev/primitives (D-15, D-16)
- Complete Lighthouse + WCAG AA + responsive + a11y audit (QUAL-01..06)

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

**Step 4: Merge to main.**

```bash
git checkout main
git merge feat/ui-redesign --no-ff -m "$(cat <<'EOF'
Merge feat/ui-redesign: v1.1 Editorial Redesign

Phases 8-11: Foundation -> Primitives -> Page Port -> Polish
- Editorial design system with Geist/Geist Mono typography
- Warm off-white #FAFAF7 + signal red #E63946 hex palette
- Single light theme (dark mode removed)
- Numbered work list replacing card grid
- Restrained motion (GSAP/scroll animations removed)
- Chat widget restyled to editorial chrome
- Lighthouse 90+ all categories, WCAG AA contrast verified
- Full keyboard accessibility with visible focus rings
- Responsive at 375/768/1024/1440px

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

**Step 5: Verify build passes on main.**

```bash
pnpm run build
```

If build fails on main, do NOT push. Document the failure and investigate. The merge can be reset with `git reset --hard HEAD~1` if needed (but only after presenting the issue to the user).

**Step 6: Push to trigger deployment.**

```bash
git push origin main
```

Cloudflare Pages is configured to auto-deploy from main. The editorial site will go live at jackcutrara.com.

**Step 7: Return to feature branch for documentation updates.**

```bash
git checkout feat/ui-redesign
git merge main
```
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && git log main --oneline -1 | grep -i "editorial\|redesign\|v1.1" && echo "Merge: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `git log main --oneline -1` shows the --no-ff merge commit containing "Editorial Redesign"
    - `pnpm run build` exited 0 on main branch before push
    - `git push origin main` completed successfully
    - No merge conflicts were encountered (or they were resolved and documented)
    - Current branch is feat/ui-redesign for documentation updates
  </acceptance_criteria>
  <done>feat/ui-redesign merged to main with --no-ff merge commit after pre-merge safety checks. Build verified on main. Pushed to origin to trigger Cloudflare Pages deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production deployment at jackcutrara.com (D-18)</name>
  <read_first>
    - .planning/phases/11-polish/11-AUDIT.md (reference for expected behavior)
  </read_first>
  <action>
**Addresses review concern (both reviewers, MEDIUM-HIGH): Expanded production verification.** Both Gemini and Codex flagged the original checkpoint as too weak. This expanded checklist includes chat functionality, metadata, and specific visual confirmations.

**What was built:** The editorial redesign has been merged to main and pushed to trigger Cloudflare Pages deployment. The site should now be live at jackcutrara.com with the new editorial design.

**Production verification checklist:**

**Visual design (homepage):**
- [ ] Visit https://jackcutrara.com
- [ ] Warm off-white (#FAFAF7) background visible
- [ ] JACK CUTRARA wordmark in mono font at top
- [ ] Numbered work list (not card grid) visible
- [ ] Geist typography rendering correctly
- [ ] No dark mode toggle anywhere
- [ ] Signal red accent dot visible next to "AVAILABLE FOR WORK"

**Page navigation:**
- [ ] Click "works" nav link -- /projects loads with numbered work list
- [ ] Click first project -- /projects/[id] loads with editorial case study layout
- [ ] Click "about" -- /about loads with editorial structure
- [ ] Click "contact" -- /contact loads with minimal contact layout
- [ ] Footer visible on all pages with BUILT WITH ASTRO . TAILWIND . GEIST

**Chat functionality (addresses Codex concern: chat must work in production, not just dev):**
- [ ] Chat bubble visible in bottom-right corner (red circle)
- [ ] Click bubble -- chat panel opens
- [ ] Type a message and send -- SSE streaming response appears (this confirms the Cloudflare Workers API route is working)
- [ ] Close chat, navigate to another page, reopen -- conversation preserved
- [ ] If chat API fails: note the error (may need ANTHROPIC_API_KEY env var in Cloudflare dashboard)

**Metadata (addresses Gemini suggestion: check robots.txt and sitemap on live site):**
- [ ] Visit https://jackcutrara.com/robots.txt -- verify it exists and contains `Disallow: /dev/`
- [ ] Visit https://jackcutrara.com/sitemap-index.xml -- verify it exists and does NOT contain `/dev/primitives`

**Addresses review concern (Codex): Rollback/hold rule.**
If ANY of the following fail, do NOT mark milestone complete:
- Chat API returns errors (Cloudflare Workers issue)
- Pages return 404 or 500
- Visual design does not match editorial system
Instead, describe the issue and we will investigate before proceeding to documentation.

**Resume signal:** Type "approved" if all checks pass, or describe specific issues found. If chat API fails due to missing env var, that is an infrastructure issue -- note it but it does not block the visual deployment approval.
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <acceptance_criteria>
    - User confirms editorial design is live at jackcutrara.com
    - User confirms page navigation works (homepage, projects, about, contact)
    - User confirms chat widget works in production (or documents API infrastructure issue)
    - User confirms robots.txt and sitemap are correct
    - User types "approved"
  </acceptance_criteria>
  <done>Production deployment verified at jackcutrara.com. Editorial design live. Chat functionality confirmed (or infrastructure issue documented). Metadata verified.</done>
</task>

<task type="auto">
  <name>Task 3: Update project documentation for milestone closeout (D-19, D-20, D-21, D-22)</name>
  <files>.planning/PROJECT.md, CLAUDE.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (current state at lines 1-50: still references GSAP and v1.0)
    - CLAUDE.md (search for "## Conventions" and "## Architecture" sections -- currently contain placeholder text)
    - .planning/REQUIREMENTS.md (QUAL-01..06 at lines 51-56 marked with [ ], traceability at lines 100-127)
    - .planning/STATE.md (current position, status field, progress counters)
    - .planning/phases/11-polish/11-AUDIT.md (audit results to reference in decisions)
  </read_first>
  <action>
**D-19: Update PROJECT.md.**

In `.planning/PROJECT.md`:

1. Line 5 -- change the "What This Is" description from:
```
A polished multi-page personal portfolio website for Jack Cutrara built with Astro 6, Tailwind CSS v4, and GSAP.
```
to:
```
A polished multi-page personal portfolio website for Jack Cutrara built with Astro 6, Tailwind CSS v4, and Geist typography.
```

2. Line 13 -- change the "Shipped" line from:
```
**Shipped:** v1.0 MVP (2026-03-31)
```
to:
```
**Shipped:** v1.1 Editorial Redesign (2026-04-13)
```

3. Line 15 -- change the "Tech stack" line from:
```
**Tech stack:** Astro 6 + Tailwind CSS v4 + TypeScript + GSAP + MDX + Cloudflare Workers (SSR)
```
to:
```
**Tech stack:** Astro 6 + Tailwind CSS v4 + TypeScript + MDX + Cloudflare Workers (SSR) + Geist/Geist Mono typography
```

4. In the "Requirements > Validated" section, add after line 47 (after the v1.0 list):
```
- ✓ Editorial design system with locked MASTER.md contract -- v1.1
- ✓ Geist + Geist Mono typography via Astro 6 Fonts API -- v1.1
- ✓ Warm off-white #FAFAF7 + signal red #E63946 hex palette -- v1.1
- ✓ Single light theme (dark mode removed) -- v1.1
- ✓ Numbered editorial work list replacing card grid -- v1.1
- ✓ Restrained motion (GSAP/scroll animations removed) -- v1.1
- ✓ Chat widget restyled to editorial chrome -- v1.1
- ✓ Lighthouse 90+ maintained after redesign -- v1.1
- ✓ WCAG AA contrast verified -- v1.1
- ✓ Full keyboard accessibility with visible focus rings -- v1.1
```

5. Update v1.0 items that are no longer true -- change these lines to include "(replaced in v1.1)" suffix:
```
- ✓ Home page with generative canvas hero -- v1.0 (replaced in v1.1 by editorial display hero)
- ✓ Dark/light theme with persistent toggle -- v1.0 (removed in v1.1)
- ✓ GSAP scroll animations and page transitions -- v1.0 (removed in v1.1)
- ✓ Project card grid with 6 projects -- v1.0 (replaced in v1.1 by numbered work list)
- ✓ Resume page with PDF download -- v1.0 (replaced in v1.1 by inline download link)
```

**D-20: Update CLAUDE.md Conventions and Architecture sections.**

In the root `CLAUDE.md`, find the `## Conventions` section (currently contains "Conventions not yet established"). Replace the entire section content with:

```
## Conventions

- Editorial design system contract at `design-system/MASTER.md` -- all visual decisions reference this spec
- Six hex color tokens only: --bg (#FAFAF7), --ink (#0A0A0A), --ink-muted (#52525B), --ink-faint (#A1A1AA), --rule (#E4E4E7), --accent (#E63946)
- Geist + Geist Mono via Astro 6 Fonts API (self-hosted, no external CDN)
- Primitives in `src/components/primitives/` use scoped `<style>`, not Tailwind utilities
- Single light theme -- no dark mode, no theme switching
- Restrained motion -- only functional hover/focus transitions, no scroll animations
- Focus rings: `outline: 2px solid var(--accent); outline-offset: 2px` on all interactive elements
- Chat widget: Phase 7 architecture preserved (SSE streaming, focus trap, XSS sanitization, rate limiting)
- Content collections: Zod schemas + MDX in `src/content/projects/`
- Type role classes in global.css: `.display`, `.h1-section`, `.h2-project`, `.lead`, `.body`, `.label-mono`, `.meta-mono`
```

Find the `## Architecture` section (currently contains "Architecture not yet mapped"). Replace the entire section content with:

```
## Architecture

- **Framework:** Astro 6 (static site + per-route SSR for chat API endpoint)
- **Styling:** Tailwind CSS v4 (Oxide engine) via `@tailwindcss/vite` + CSS custom properties for design tokens in `src/styles/global.css`
- **Layout:** `BaseLayout.astro` wraps all pages; primitives in `src/components/primitives/` compose into page templates
- **Content:** Astro Content Collections with Zod schemas + MDX for project case studies in `src/content/projects/`
- **Chat:** Client-side widget (`src/scripts/chat.ts`) + Cloudflare Workers SSR API (`src/pages/api/chat.ts`) + Anthropic Claude Haiku
- **Deployment:** Cloudflare Pages (static pages) + Cloudflare Workers (SSR API route) at jackcutrara.com
- **Fonts:** Astro 6 Fonts API -> self-hosted Geist/Geist Mono with `--font-display-src`/`--font-body-src`/`--font-mono-src` CSS variable bridge
- **Design system:** `design-system/MASTER.md` is the locked visual contract; tokens/typography/layout/components/motion/accent rules
```

**D-21: Update REQUIREMENTS.md.**

In `.planning/REQUIREMENTS.md`:

1. Change each QUAL requirement checkbox from `[ ]` to `[x]` (lines 51-56):
```
- [x] **QUAL-01**: Lighthouse scores remain >=90 on Performance, Accessibility, Best Practices, and SEO across all pages
- [x] **QUAL-02**: LCP < 2s, CLS < 0.1 maintained on the homepage and project detail pages
- [x] **QUAL-03**: Full keyboard accessibility with visible focus rings on all interactive elements
- [x] **QUAL-04**: `prefers-reduced-motion` respected (trivially satisfied -- there is effectively no motion to reduce)
- [x] **QUAL-05**: WCAG AA color contrast (4.5:1 normal text, 3:1 large text) verified on all text/background combinations
- [x] **QUAL-06**: Responsive across breakpoints (375, 768, 1024, 1440) with no horizontal scroll and readable type at every width
```

2. In the Traceability table (lines 121-126), change "Pending" to "Complete" for QUAL-01 through QUAL-06.

3. Update the coverage line (line 98) from:
```
Coverage: 25 / 25 requirements mapped to Phases 8-11 ✓
```
to:
```
Coverage: 25 / 25 requirements -- all complete ✓
```

**D-22: Update STATE.md.**

In `.planning/STATE.md`, update the frontmatter and body:

1. Frontmatter changes:
   - `status: complete` (was: `executing`)
   - `stopped_at: Milestone v1.1 complete` (was: `Phase 11 context gathered`)
   - `last_updated:` set to current ISO timestamp
   - `last_activity: 2026-04-13 -- Milestone v1.1 complete`
   - `progress.completed_phases: 4` (was: `3`)
   - `progress.completed_plans:` increment by 3 (the 3 Phase 11 plans)
   - `progress.percent: 100`

2. "Current Position" section:
```
Phase: 11
Plan: Complete
Status: Milestone v1.1 complete
Last activity: 2026-04-13 -- All QUAL requirements verified, merged to main, deployed
Branch: main
```

3. Phase Map table -- update Phase 11 row:
```
| 11 | Polish | QUAL-01..06 | Complete |
```

4. Add to "Accumulated Context > Decisions":
```
- [Phase 11]: Lighthouse CLI JSON reports captured for homepage + /projects/seatwatch per D-01/D-02 (representative sampling documented)
- [Phase 11]: WCAG AA contrast verified with full inventory table -- --ink-faint (~3.2:1) documented as tertiary text, --accent restricted to hover/UI per D-04
- [Phase 11]: Focus-visible rings added globally (a, button, textarea, input, select, [role=button], summary) + component-scoped rules per D-12
- [Phase 11]: Chat bot message links changed from accent to ink text color with accent underline for WCAG AA normal-text compliance
- [Phase 11]: lightning-css warnings fixed via @source not directives for .planning/ and design-system/ per D-05
- [Phase 11]: BaseLayout noindex prop added as clean replacement for slot-based injection per D-06
- [Phase 11]: JsonLd is:inline + Container type Props fixes per D-07
- [Phase 11]: mockup.html deleted after Phase 10 parity sign-off per D-15
- [Phase 11]: /dev/primitives page deleted, sitemap filter cleaned per D-16
- [Phase 11]: feat/ui-redesign merged to main with pre-merge safety checks, deployed to Cloudflare Pages per D-17/D-18
- [Phase 11]: OG/social meta tags verified in production (Gemini review suggestion)
```
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && grep "v1.1 Editorial Redesign" .planning/PROJECT.md && grep "\[x\].*QUAL-01" .planning/REQUIREMENTS.md && grep "\[x\].*QUAL-06" .planning/REQUIREMENTS.md && grep "all complete" .planning/REQUIREMENTS.md && grep "complete" .planning/STATE.md | head -3 && grep "Conventions" CLAUDE.md | head -1 && grep "Architecture" CLAUDE.md | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains "v1.1 Editorial Redesign" in Shipped line
    - `.planning/PROJECT.md` tech stack line does NOT contain "GSAP"
    - `.planning/PROJECT.md` tech stack line contains "Geist"
    - `.planning/PROJECT.md` "What This Is" line does NOT contain "GSAP"
    - `.planning/REQUIREMENTS.md` contains `[x] **QUAL-01**` through `[x] **QUAL-06**` (all 6 checked)
    - `.planning/REQUIREMENTS.md` traceability table shows "Complete" for QUAL-01 through QUAL-06
    - `.planning/REQUIREMENTS.md` contains "25 / 25 requirements -- all complete"
    - `.planning/STATE.md` frontmatter contains `status: complete`
    - `.planning/STATE.md` contains "Milestone v1.1 complete"
    - `.planning/STATE.md` progress shows `percent: 100`
    - `CLAUDE.md` "## Conventions" section contains "editorial design system" and "MASTER.md"
    - `CLAUDE.md` "## Conventions" section contains "Six hex color tokens"
    - `CLAUDE.md` "## Architecture" section contains "Astro 6" and "Cloudflare Pages"
    - `CLAUDE.md` "## Architecture" section contains "Geist/Geist Mono"
  </acceptance_criteria>
  <done>All project documentation updated to reflect v1.1 completion. PROJECT.md shows v1.1 shipped. REQUIREMENTS.md shows 25/25 complete. STATE.md shows milestone complete. CLAUDE.md has real Conventions and Architecture sections. Milestone v1.1 closeout complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git merge | Merging feature branch to main -- could introduce regressions if not pre-checked |
| Cloudflare Pages deployment | Pushing to production -- public-facing site |
| Production chat API | Cloudflare Workers endpoint -- requires ANTHROPIC_API_KEY env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | T (Tampering) | Git merge to main | mitigate | Pre-merge safety checks: fetch latest main, check divergence, dry-run merge conflict detection, diff stat audit, build verification on main before push. --no-ff preserves merge history for audit trail. |
| T-11-08 | D (Denial of Service) | Cloudflare Pages deployment | accept | Cloudflare handles DDoS protection. Static site has minimal attack surface. Portfolio traffic is low. Rollback is a git revert + push. |
| T-11-09 | I (Information Disclosure) | .planning/ files in public repo | accept | Planning artifacts contain no secrets. Repo is already public. .planning/ is excluded from Tailwind detection via @source not. |
| T-11-10 | E (Elevation of Privilege) | Chat API in production | accept | Rate limiting (5/60s) and input validation already implemented in Phase 7. ANTHROPIC_API_KEY stored in Cloudflare Workers secrets, not in code. |
</threat_model>

<verification>
- `git log main --oneline -1` shows merge commit with "Editorial Redesign"
- Site accessible at jackcutrara.com with editorial design
- Chat widget works in production (or infrastructure issue documented)
- robots.txt and sitemap verified on production
- PROJECT.md, REQUIREMENTS.md, STATE.md, CLAUDE.md all updated
- All 25 requirements marked complete in REQUIREMENTS.md
</verification>

<success_criteria>
1. Pre-merge safety checks completed (fetch, divergence check, dry-run, diff audit)
2. feat/ui-redesign merged to main with --no-ff and descriptive merge commit
3. Build passes on main branch before push
4. Site deployed to Cloudflare Pages (push triggers auto-deploy)
5. Production verified: homepage, project detail, chat, robots.txt, sitemap
6. PROJECT.md reflects v1.1 completion (no GSAP references, Geist in tech stack)
7. CLAUDE.md has real Conventions (tokens, fonts, focus rings) and Architecture (Astro 6, CF Pages)
8. REQUIREMENTS.md shows 25/25 complete
9. STATE.md shows milestone v1.1 complete with 100% progress
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish/11-03-SUMMARY.md`
</output>
