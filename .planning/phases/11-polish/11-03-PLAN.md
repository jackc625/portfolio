---
phase: 11-polish
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - CLAUDE.md
autonomous: false
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06

must_haves:
  truths:
    - "feat/ui-redesign branch is merged to main"
    - "Site is deployed to Cloudflare Pages at jackcutrara.com with the editorial design"
    - "PROJECT.md reflects v1.1 completion"
    - "CLAUDE.md Technology Stack section reflects actual v1.1 stack"
    - "REQUIREMENTS.md marks QUAL-01..06 as complete"
    - "STATE.md records milestone v1.1 as complete"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated current state, validated requirements, v1.1 key decisions"
    - path: "CLAUDE.md"
      provides: "Updated Technology Stack reflecting v1.1 (no GSAP, no dark mode, editorial design)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QUAL-01..06 marked complete, 25/25 coverage"
    - path: ".planning/STATE.md"
      provides: "Milestone v1.1 recorded as complete"
  key_links:
    - from: "feat/ui-redesign"
      to: "main"
      via: "git merge"
      pattern: "merge"
---

<objective>
Merge the editorial redesign branch to main, deploy to Cloudflare Pages, and update all project documentation to close out milestone v1.1.

Purpose: Ship the redesigned portfolio to production and update all tracking artifacts so the project reflects its current state accurately.
Output: Live site at jackcutrara.com with editorial design. All project docs updated. Milestone v1.1 complete.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-polish/11-CONTEXT.md
@.planning/phases/11-polish/11-AUDIT.md
@.planning/phases/11-polish/11-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge feat/ui-redesign to main and deploy (D-17, D-18)</name>
  <files></files>
  <read_first>
    - .planning/phases/11-polish/11-AUDIT.md (confirm all QUAL requirements PASS before merging)
    - .planning/phases/11-polish/11-02-SUMMARY.md (confirm audit plan completed successfully)
  </read_first>
  <action>
**Pre-merge verification:**

Before merging, verify the audit is complete and all QUAL requirements passed:
```bash
grep "QUAL-01.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-02.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-03.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-04.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-05.*PASS" .planning/phases/11-polish/11-AUDIT.md
grep "QUAL-06.*PASS" .planning/phases/11-polish/11-AUDIT.md
```

All 6 must return matches. If any is missing or FAIL, do NOT proceed — go back to Plan 02.

**D-17: Merge feat/ui-redesign to main.**

```bash
git checkout main
git merge feat/ui-redesign --no-ff -m "Merge feat/ui-redesign: v1.1 Editorial Redesign

Phases 8-11: Foundation → Primitives → Page Port → Polish
- Editorial design system with Geist/Geist Mono typography
- Warm off-white #FAFAF7 + signal red #E63946 hex palette
- Single light theme (dark mode removed)
- Numbered work list replacing card grid
- Restrained motion (GSAP/scroll animations removed)
- Chat widget restyled to editorial chrome
- Lighthouse 90+ all categories, WCAG AA contrast verified
- Full keyboard accessibility with visible focus rings
- Responsive at 375/768/1024/1440px

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

After merge, verify build passes on main:
```bash
pnpm run build
```

**D-18: Deploy to Cloudflare Pages.**

Push main to trigger Cloudflare Pages deployment:
```bash
git push origin main
```

Cloudflare Pages is configured to auto-deploy from main. The editorial site will go live at jackcutrara.com.

After pushing, check deployment status:
```bash
npx wrangler pages deployment list --project-name=portfolio 2>/dev/null || echo "Check Cloudflare dashboard for deployment status"
```

Switch back to the feature branch for documentation updates:
```bash
git checkout feat/ui-redesign
git merge main
```
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && git log main --oneline -1 | grep -i "editorial\|redesign\|v1.1" && echo "Merge: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `git log main --oneline -1` shows the merge commit message containing "Editorial Redesign"
    - `pnpm run build` exits 0 on the main branch
    - `git push origin main` completed successfully (or user confirmed push)
    - Current branch is back on feat/ui-redesign (or main) for doc updates
  </acceptance_criteria>
  <done>feat/ui-redesign merged to main with descriptive merge commit. Build passes. Pushed to origin to trigger Cloudflare Pages deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production deployment at jackcutrara.com (D-18)</name>
  <what-built>
    The editorial redesign has been merged to main and pushed to trigger Cloudflare Pages deployment. The site should now be live at jackcutrara.com with the new editorial design.
  </what-built>
  <how-to-verify>
    1. Visit https://jackcutrara.com in a browser
    2. Verify the editorial design is live:
       - Homepage shows JACK CUTRARA wordmark with warm off-white background
       - Numbered work list (not card grid)
       - Geist typography
       - No dark mode toggle
    3. Click through to /projects/seatwatch — verify editorial case study layout
    4. Open chat widget — verify it works (send a message, see streaming response)
    5. Check /about and /contact pages load correctly
  </how-to-verify>
  <resume-signal>
    Type "approved" if the site is live and looking correct, or describe any issues.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update project documentation (D-19, D-20, D-21, D-22)</name>
  <files>.planning/PROJECT.md, CLAUDE.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (current state reflects v1.0, needs v1.1 update)
    - CLAUDE.md (Technology Stack section lists GSAP, dark mode — needs v1.1 update)
    - .planning/REQUIREMENTS.md (QUAL-01..06 marked Pending, need to be marked Complete)
    - .planning/STATE.md (current position needs milestone completion update)
    - .planning/phases/11-polish/11-AUDIT.md (audit results to reference)
  </read_first>
  <action>
**D-19: Update PROJECT.md.**

In `.planning/PROJECT.md`, update the "Current State" section:

Change the "Shipped" line from:
```
**Shipped:** v1.0 MVP (2026-03-31)
```
to:
```
**Shipped:** v1.1 Editorial Redesign (2026-04-13)
```

Change the "Tech stack" line from:
```
**Tech stack:** Astro 6 + Tailwind CSS v4 + TypeScript + GSAP + MDX + Cloudflare Workers (SSR)
```
to:
```
**Tech stack:** Astro 6 + Tailwind CSS v4 + TypeScript + MDX + Cloudflare Workers (SSR) + Geist/Geist Mono typography
```

In the "Requirements > Validated" section, add v1.1 items after the v1.0 list:
```
- ✓ Editorial design system with locked MASTER.md contract — v1.1
- ✓ Geist + Geist Mono typography via Astro 6 Fonts API — v1.1
- ✓ Warm off-white #FAFAF7 + signal red #E63946 hex palette — v1.1
- ✓ Single light theme (dark mode removed) — v1.1
- ✓ Numbered editorial work list replacing card grid — v1.1
- ✓ Restrained motion (GSAP/scroll animations removed) — v1.1
- ✓ Chat widget restyled to editorial chrome — v1.1
- ✓ Lighthouse 90+ maintained after redesign — v1.1
- ✓ WCAG AA contrast verified — v1.1
- ✓ Full keyboard accessibility with visible focus rings — v1.1
```

Update the v1.0 items that are no longer true to reflect their v1.1 replacement:
- Change "Home page with generative canvas hero" to strikethrough or remove (replaced by editorial hero)
- Change "Dark/light theme with persistent toggle" to strikethrough or remove (removed in v1.1)
- Change "GSAP scroll animations and page transitions" to strikethrough or remove (removed in v1.1)
- Change "Project card grid with 6 projects" to strikethrough or remove (replaced by work list)
- Change "Resume page with PDF download" to strikethrough or remove (replaced by inline download link)

**D-20: Update CLAUDE.md Technology Stack section.**

In the root `CLAUDE.md`, find the `## Technology Stack` section (currently lists GSAP, dark mode references). Update it to reflect the actual v1.1 stack:

The `## Recommended Stack` section should be left as-is (it is reference documentation). But ensure the "What NOT to Use" and "Alternatives Considered" sections are accurate. No changes needed there since they already reflect v1.1 decisions.

If there is a "Conventions" section that says "Conventions not yet established", update it to:
```
## Conventions

- Editorial design system contract at `design-system/MASTER.md` — all visual decisions reference this spec
- Six hex color tokens only: --bg (#FAFAF7), --ink (#0A0A0A), --ink-muted (#52525B), --ink-faint (#A1A1AA), --rule (#E4E4E7), --accent (#E63946)
- Geist + Geist Mono via Astro 6 Fonts API (self-hosted, no external CDN)
- Primitives in `src/components/primitives/` use scoped <style>, not Tailwind utilities
- Single light theme — no dark mode, no theme switching
- Restrained motion — only functional hover/focus transitions, no scroll animations
- Focus rings: `outline: 2px solid var(--accent); outline-offset: 2px` on all interactive elements
- Chat widget: Phase 7 architecture preserved (SSE streaming, focus trap, XSS sanitization, rate limiting)
```

If there is an "Architecture" section that says "Architecture not yet mapped", update it to:
```
## Architecture

- **Framework:** Astro 6 (static site + per-route SSR for API endpoints)
- **Styling:** Tailwind CSS v4 (Oxide engine) + CSS custom properties for design tokens
- **Layout:** BaseLayout.astro wraps all pages; primitives compose into page templates
- **Content:** Astro Content Collections with Zod schemas + MDX for project case studies
- **Chat:** Client-side widget (src/scripts/chat.ts) + Cloudflare Workers SSR API (src/pages/api/chat.ts) + Anthropic Claude Haiku
- **Deployment:** Cloudflare Pages (static) + Cloudflare Workers (SSR API routes)
- **Fonts:** Astro 6 Fonts API → self-hosted Geist/Geist Mono with CSS variable bridge
```

**D-21: Update REQUIREMENTS.md.**

In `.planning/REQUIREMENTS.md`, change each QUAL requirement from `[ ]` to `[x]`:

```
- [x] **QUAL-01**: Lighthouse scores remain >=90 ...
- [x] **QUAL-02**: LCP < 2s, CLS < 0.1 ...
- [x] **QUAL-03**: Full keyboard accessibility ...
- [x] **QUAL-04**: `prefers-reduced-motion` respected ...
- [x] **QUAL-05**: WCAG AA color contrast ...
- [x] **QUAL-06**: Responsive across breakpoints ...
```

In the Traceability table at the bottom, update QUAL-01..06 rows:
- Change "Pending" to "Complete" in the Status column

Update the coverage line to:
```
Coverage: 25 / 25 requirements — all complete ✓
```

**D-22: Update STATE.md.**

In `.planning/STATE.md`, update:

1. `status:` from `verifying` to `complete`
2. `stopped_at:` to `Milestone v1.1 complete`
3. `progress.completed_phases:` to `4` (all 4 phases complete)
4. `Current Position` section:
```
Phase: 11
Plan: Complete
Status: Milestone v1.1 complete
Last activity: 2026-04-13
Branch: main
```

5. Update the Phase Map table to show Phase 11 as complete:
```
| 11 | Polish | QUAL-01..06 | Complete |
```

6. Add to `Accumulated Context > Decisions`:
```
- [Phase 11]: Lighthouse CLI JSON reports captured for homepage + /projects/seatwatch per D-01/D-02
- [Phase 11]: WCAG AA contrast verified — all text/background combinations meet minimum ratios per D-04
- [Phase 11]: Focus-visible rings added globally (a:focus-visible, button:focus-visible) + component-scoped rules per D-12
- [Phase 11]: lightning-css warnings fixed via @source not directives for .planning/ and design-system/ per D-05
- [Phase 11]: BaseLayout noindex prop added as clean replacement for slot-based injection per D-06
- [Phase 11]: mockup.html deleted after Phase 10 parity sign-off per D-15
- [Phase 11]: /dev/primitives page deleted, sitemap filter cleaned per D-16
- [Phase 11]: feat/ui-redesign merged to main, deployed to Cloudflare Pages per D-17/D-18
```
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/portfolio && grep "v1.1 Editorial Redesign" .planning/PROJECT.md && grep "\[x\].*QUAL-01" .planning/REQUIREMENTS.md && grep "\[x\].*QUAL-06" .planning/REQUIREMENTS.md && grep "complete" .planning/STATE.md && grep "Conventions" CLAUDE.md | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains "v1.1 Editorial Redesign" in Shipped line
    - `.planning/PROJECT.md` tech stack line does NOT contain "GSAP"
    - `.planning/PROJECT.md` tech stack line contains "Geist"
    - `.planning/REQUIREMENTS.md` contains `[x] **QUAL-01**` through `[x] **QUAL-06**` (all 6 checked)
    - `.planning/REQUIREMENTS.md` traceability table shows "Complete" for QUAL-01..06
    - `.planning/REQUIREMENTS.md` contains "25 / 25 requirements — all complete"
    - `.planning/STATE.md` contains `status: complete`
    - `.planning/STATE.md` contains "Milestone v1.1 complete"
    - `CLAUDE.md` contains "## Conventions" with non-placeholder content
    - `CLAUDE.md` contains "## Architecture" with non-placeholder content
    - `CLAUDE.md` Conventions section mentions "editorial design system" or "MASTER.md"
  </acceptance_criteria>
  <done>All project documentation updated to reflect v1.1 completion. PROJECT.md shows v1.1 shipped. REQUIREMENTS.md shows 25/25 complete. STATE.md shows milestone complete. CLAUDE.md has real Conventions and Architecture sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git merge | Merging feature branch to main — could introduce regressions |
| Cloudflare Pages deployment | Pushing to production — public-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | T (Tampering) | Git merge to main | mitigate | Build verified on main after merge before push; --no-ff preserves merge history for audit |
| T-11-07 | D (Denial of Service) | Cloudflare Pages deployment | accept | Cloudflare handles DDoS protection. Static site has minimal attack surface. Portfolio traffic is low. |
| T-11-08 | I (Information Disclosure) | .planning/ files in public repo | accept | Planning artifacts contain no secrets. Repo is already public per PROJECT.md. |
</threat_model>

<verification>
- `git log main --oneline -1` shows merge commit
- Site accessible at jackcutrara.com with editorial design
- PROJECT.md, REQUIREMENTS.md, STATE.md, CLAUDE.md all updated
- All 25 requirements marked complete in REQUIREMENTS.md
</verification>

<success_criteria>
1. feat/ui-redesign merged to main with descriptive merge commit
2. Build passes on main branch
3. Site deployed to Cloudflare Pages (push triggers auto-deploy)
4. Production site verified at jackcutrara.com
5. PROJECT.md reflects v1.1 completion
6. CLAUDE.md has real Conventions and Architecture sections
7. REQUIREMENTS.md shows 25/25 complete
8. STATE.md shows milestone v1.1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish/11-03-SUMMARY.md`
</output>
