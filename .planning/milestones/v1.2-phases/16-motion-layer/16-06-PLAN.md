---
phase: 16
plan: 06
type: execute
wave: 3
depends_on:
  - "16-01"
files_modified:
  - src/components/primitives/WorkRow.astro
autonomous: true
requirements:
  - MOTN-03
tags:
  - motion
  - primitive
  - phase-16
  - tdd-green

must_haves:
  truths:
    - "src/components/primitives/WorkRow.astro `.work-arrow` rule upgraded: transition adds `transform 180ms ease-out` alongside existing `opacity 120ms ease`."
    - "Hover/focus rule adds `transform: translateX(4px)` alongside existing `opacity: 1`."
    - "Existing `opacity: 0` resting state preserved on `.work-arrow`."
    - "Existing `color: var(--accent)` preserved on `.work-arrow` (accent reservation list intact)."
    - "Existing accent title underline rules at `.work-row:hover .work-title, .work-row:focus-visible .work-title` byte-identical (MASTER.md §7.1 affordance)."
    - "Scoped `<style>` block contains paired `@media (prefers-reduced-motion: reduce)` override that resets `.work-arrow` transform + opacity to 0 on hover/focus (UI-SPEC MOTN-03 row reduced-motion contract)."
    - "tests/build/work-arrow-motion.test.ts ALL GREEN."
    - "No other primitives modified — WorkRow is single-file scope."
    - "No global.css edits — primitive layout stays scoped per MASTER.md §5 / D-14 (per-primitive scoped CSS rule)."
  artifacts:
    - path: "src/components/primitives/WorkRow.astro"
      provides: "Arrow upgrade — opacity + transform transition + paired reduce override"
      max_lines_delta: 15
  key_links:
    - from: "WorkRow.astro .work-arrow"
      to: "browser CSS transition engine"
      via: "transition: opacity + transform"
      pattern: "transition:\\s*opacity\\s+120ms\\s+ease,\\s*transform\\s+180ms\\s+ease-out"
    - from: "WorkRow.astro hover/focus arrow"
      to: "translateX(4px)"
      via: "transform property"
      pattern: "transform:\\s*translateX\\(4px\\)"

---

<objective>
Land Plan 01's RED WorkRow assertions as GREEN by upgrading the arrow CSS in `src/components/primitives/WorkRow.astro` (current lines 81-98 scoped style block) with a transform addition and a paired reduced-motion override.

Purpose: Ship MOTN-03 — WorkRow arrow opacity + 4px translateX slide-in on hover/focus, replacing the v1.1 opacity-only fade. Keep all existing affordance rules (color: var(--accent), title underline on hover/focus) byte-identical.

Output: 1 modified Astro primitive (`src/components/primitives/WorkRow.astro`, surgical diff inside the scoped `<style>` block — 4 lines changed, ~6 lines added). Zero edits anywhere else.

This plan runs in parallel with Plan 05 (no file overlap — Plan 05 owns chat.ts + global.css; this plan owns WorkRow.astro).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-motion-layer/16-CONTEXT.md
@.planning/phases/16-motion-layer/16-PATTERNS.md
@.planning/phases/16-motion-layer/16-UI-SPEC.md
@design-system/MOTION.md

<interfaces>
<!-- WorkRow.astro current state at HEAD — the entire scoped <style> block lines 81-104 -->

```css
.work-arrow {
  color: var(--accent);
  opacity: 0;
  transition: opacity 120ms ease;
}

.work-row:hover .work-title,
.work-row:focus-visible .work-title {
  text-decoration: underline;
  text-decoration-color: var(--accent);
  text-decoration-thickness: 1.5px;
  text-underline-offset: 4px;
}

.work-row:hover .work-arrow,
.work-row:focus-visible .work-arrow {
  opacity: 1;
}

.work-row:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
```

The `.work-arrow` rule (lines 81-85) gains `transform: translateX(0)` resting state + extended transition list.
The hover/focus rule (lines 95-98) gains `transform: translateX(4px)`.
A new `@media (prefers-reduced-motion: reduce)` block is appended to the scoped style.
The accent title underline rule (lines 87-93) and the focus ring rule (lines 100-103) stay byte-identical.

From .planning/phases/16-motion-layer/16-UI-SPEC.md MOTN-03 row:
- Trigger: `.work-row:hover` / `.work-row:focus-visible`
- From → To: `.work-arrow` opacity 0→1 + translateX 0→4px
- Duration: 180 ms (transform); opacity stays 120ms per MASTER.md §6.2 floor
- Easing: ease-out (transform); ease (opacity, inherited)
- Reduced-motion: with reduce, arrow stays opacity 0 (hidden); accent title underline still appears as the affordance (color-only state change, allowed unconditionally)

From tests/build/work-arrow-motion.test.ts (the contract — every assertion):
- `.work-arrow` declares `transition: opacity 120ms ease, transform 180ms ease-out`
- `.work-arrow` keeps `opacity: 0`
- `.work-arrow` keeps `color: var(--accent)`
- Hover/focus rule has `transform: translateX(4px)` AND `opacity: 1`
- `@media (prefers-reduced-motion: reduce)` block resets transform to translateX(0) AND opacity 0 on .work-arrow / hover / focus
- Title underline rules preserved
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade WorkRow.astro scoped style block — arrow transition + hover translateX + paired reduce override</name>
  <files>src/components/primitives/WorkRow.astro</files>

  <read_first>
    - src/components/primitives/WorkRow.astro FULL FILE — but focus on lines 75-104 (the scoped `<style>` block end)
    - tests/build/work-arrow-motion.test.ts (the contract)
    - .planning/phases/16-motion-layer/16-PATTERNS.md section "src/components/primitives/WorkRow.astro:81–98 (MODIFIED — arrow upgrade in place)"
    - .planning/phases/16-motion-layer/16-UI-SPEC.md MOTN-03 row in "Animation specs" table + the "Spacing Scale → Motion-specific spacing values" table (4px translateX confirmed)
    - design-system/MOTION.md §5 MOTN-03 row
    - design-system/MASTER.md §6.2 (allowed-to-survive transitions — sub-200ms hover/focus state changes)
  </read_first>

  <behavior>
    - `.work-arrow` declares `transition: opacity 120ms ease, transform 180ms ease-out` (extended from current `transition: opacity 120ms ease`)
    - `.work-arrow` retains `color: var(--accent)` and `opacity: 0`
    - `.work-arrow` adds resting `transform: translateX(0)` (explicit baseline for the transition)
    - Hover/focus rule sets `opacity: 1` AND `transform: translateX(4px)`
    - `@media (prefers-reduced-motion: reduce)` block neutralizes: `.work-arrow { transition: none; transform: translateX(0); opacity: 0; }` AND on hover/focus stays `transform: translateX(0); opacity: 0;`
    - Title underline hover/focus rule (color-only state change, sub-200ms allowed) preserved byte-identical
    - `.work-row:focus-visible` outline ring rule preserved byte-identical
  </behavior>

  <action>
Make TWO surgical edits to `src/components/primitives/WorkRow.astro` scoped `<style>` block (current lines 81-104).

**Edit 1 — Replace lines 81-85 (`.work-arrow` rule).** Current:
```css
.work-arrow {
  color: var(--accent);
  opacity: 0;
  transition: opacity 120ms ease;
}
```

Replace with:
```css
.work-arrow {
  color: var(--accent);
  opacity: 0;
  transform: translateX(0);
  transition: opacity 120ms ease, transform 180ms ease-out;
}
```

Two changes:
- Added `transform: translateX(0);` (explicit resting baseline so the transition has a starting state to interpolate from)
- Extended `transition:` to include `transform 180ms ease-out` per MOTN-03 spec

**Edit 2 — Replace lines 95-98 (hover/focus arrow rule).** Current:
```css
.work-row:hover .work-arrow,
.work-row:focus-visible .work-arrow {
  opacity: 1;
}
```

Replace with:
```css
.work-row:hover .work-arrow,
.work-row:focus-visible .work-arrow {
  opacity: 1;
  transform: translateX(4px);
}
```

One change: added `transform: translateX(4px);` per MOTN-03 spec.

**Edit 3 — APPEND new `@media (prefers-reduced-motion: reduce)` block AFTER line 103 (the `.work-row:focus-visible` outline ring rule).** Add as new content before the closing `</style>` tag:

```css

  /* Phase 16 MOTN-03 / D-23 / D-24: paired reduce override.
     With reduce, the arrow stays opacity 0 (hidden); the accent title underline
     above (color-only state change, sub-200ms — MASTER.md §6.2) still appears
     as the affordance. */
  @media (prefers-reduced-motion: reduce) {
    .work-arrow {
      transition: none;
      transform: translateX(0);
      opacity: 0;
    }
    .work-row:hover .work-arrow,
    .work-row:focus-visible .work-arrow {
      transform: translateX(0);
      opacity: 0;
    }
  }
```

The 2-space indentation matches the existing CSS block style inside the scoped `<style>` (read lines 75-103 to confirm indentation — Astro scoped CSS uses 2-space indent in this file).

**Critical preservation invariants:**

1. The accent title underline rule (current lines 87-93) — `text-decoration: underline`, `text-decoration-color: var(--accent)`, `text-decoration-thickness: 1.5px`, `text-underline-offset: 4px` — stays BYTE-IDENTICAL. Test asserts these are preserved.

2. The `.work-row:focus-visible` outline ring rule (current lines 100-103) — `outline: 2px solid var(--accent); outline-offset: 2px;` — stays BYTE-IDENTICAL. QUAL-03 focus ring contract.

3. The component-scoped `<style>` discipline — no Tailwind utilities, no `:global()`, all rules stay scoped. Per MASTER.md §5 / D-03 / D-14.

4. **No edit to `global.css`** — the WorkRow arrow CSS lives entirely in the scoped style block. Plan 04 + Plan 05 own all global.css edits in this phase.

5. **No edit to `WorkRow.astro` template/HTML** — only the `<style>` block. The `.work-arrow` markup at the top of the file stays unchanged.

After editing, run:
- `pnpm vitest run tests/build/work-arrow-motion.test.ts --reporter=dot` — ALL tests GREEN (was RED at end of Plan 01)
- `pnpm test --reporter=dot` — full suite; net result ~290 GREEN (Plan 06 closes the last RED stubs from Plan 01)
- `pnpm check` 0/0/0
- `pnpm build` clean end-to-end

Verify NO regression in WorkRow rendering by spot-checking one prerendered page:
```
grep -o 'class="work-arrow"' dist/client/index.html | wc -l
```
Should return 6 (the homepage work list has 6 rows; the arrow appears once per row).
  </action>

  <verify>
    <automated>pnpm vitest run tests/build/work-arrow-motion.test.ts --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'transition: opacity 120ms ease, transform 180ms ease-out' src/components/primitives/WorkRow.astro` returns 1
    - `grep -c 'transform: translateX(4px)' src/components/primitives/WorkRow.astro` returns 1
    - `grep -c 'transform: translateX(0)' src/components/primitives/WorkRow.astro` returns >= 3 (resting state on .work-arrow + 2 inside the reduce-override block: .work-arrow + hover/focus)
    - `grep -c 'opacity: 0' src/components/primitives/WorkRow.astro` returns >= 3 (resting + 2 inside reduce-override)
    - `grep -c 'opacity: 1' src/components/primitives/WorkRow.astro` returns 1 (hover/focus state preserved)
    - `grep -c 'color: var(--accent)' src/components/primitives/WorkRow.astro` returns >= 1 (accent reservation list preserved)
    - `grep -c '@media (prefers-reduced-motion: reduce)' src/components/primitives/WorkRow.astro` returns 1
    - `grep -c 'text-decoration: underline' src/components/primitives/WorkRow.astro` returns 1 (title underline rule preserved)
    - `grep -c 'text-decoration-color: var(--accent)' src/components/primitives/WorkRow.astro` returns 1
    - `grep -c 'outline: 2px solid var(--accent)' src/components/primitives/WorkRow.astro` returns 1 (QUAL-03 focus ring preserved)
    - `git diff src/styles/global.css | wc -l` returns 0 (no global.css edit)
    - `pnpm vitest run tests/build/work-arrow-motion.test.ts --reporter=dot` exits 0; ALL tests GREEN (was RED at end of Plan 01)
    - `pnpm check` 0 errors / 0 warnings / 0 hints
    - `pnpm build` clean end-to-end; all 11 routes prerender
    - Spot-check rendered HTML: `grep -o 'class="work-arrow"' dist/client/index.html | wc -l` returns 6 (no behavioral regression in markup)
  </acceptance_criteria>

  <done>WorkRow.astro arrow upgraded: transition adds transform 180ms ease-out; hover/focus adds translateX(4px); paired reduce override neutralizes both. Title underline + focus ring rules byte-identical. tests/build/work-arrow-motion.test.ts ALL GREEN. No global.css edit. pnpm check / build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser CSS engine → user agent | Standard CSS transition; no JS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering | Affordance regression — arrow doesn't reveal on hover | mitigate | tests/build/work-arrow-motion.test.ts asserts hover/focus rule has both opacity 1 + translateX(4px); regression fires CI |
| T-16-06-02 | Tampering | Title underline accent affordance silently removed | mitigate | tests/build/work-arrow-motion.test.ts asserts title underline rules preserved |
| T-16-06-03 | Information Disclosure | n/a | accept | No data flows in CSS-only motion |
| T-16-06-04 | Denial-of-Access | Focus ring hidden under reduce | mitigate | The `.work-row:focus-visible` outline rule (lines 100-103) is OUTSIDE any media query — focus ring shows unconditionally; reduce only neutralizes the arrow translation |
| T-16-06-05 | Repudiation | git history | mitigate | Single atomic commit; commit message references MOTN-03 |
| T-16-06-06 | Spoofing / Elevation | n/a | accept | No auth/privilege surface |

ASVS L1: not applicable — pure CSS, no JS, no input validation surface.
</threat_model>

<verification>
After the task completes:
- WorkRow.astro scoped style block has the arrow transition + hover translateX + paired reduce override
- tests/build/work-arrow-motion.test.ts ALL GREEN (last RED stubs from Plan 01)
- Title underline + focus ring rules preserved byte-identical
- No edits anywhere else
- `pnpm test` shows ~292+ GREEN (full Phase 16 test suite GREEN; Plan 07 phase gate is the final verification)
- `pnpm build` clean
- `pnpm check` 0/0/0
</verification>

<success_criteria>
- MOTN-03 closed in REQUIREMENTS.md
- WorkRow arrow upgrade lands without disturbing accent reservation list, title underline affordance, or QUAL-03 focus ring
- Plan completes within ~10% context (~5-10 minutes — single primitive, surgical CSS diff)
</success_criteria>

<output>
After completion, create `.planning/phases/16-motion-layer/16-06-SUMMARY.md` with:
- Files changed (1 modified WorkRow.astro) and LOC delta
- tests/build/work-arrow-motion.test.ts RED → GREEN flip
- Confirmation that title underline + focus ring rules byte-identical
- Confirmation no global.css edit (`git diff src/styles/global.css | wc -l` = 0)
- pnpm check / build / test final state
- Pointer to next plan (16-07 phase gate — Lighthouse + final test sweep + close-out)
- MOTN-03 marked Complete in REQUIREMENTS.md
</output>
