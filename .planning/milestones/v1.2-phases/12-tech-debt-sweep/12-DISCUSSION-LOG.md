# Phase 12: Tech Debt Sweep - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-15
**Phase:** 12-tech-debt-sweep
**Areas discussed:** Copy-button parity, Build/lint warnings, MobileMenu inert, Accepted trade-off documentation

---

## Copy-button parity (DEBT-04)

### Which markup should become canonical for both live and replay copy buttons?

| Option | Description | Selected |
|--------|-------------|----------|
| COPY text label | Mono 'COPY'/'COPIED' label with accent color flip — already the editorial aesthetic; replay already behaves this way. | ✓ |
| SVG clipboard icon | Live already uses an SVG; replay adopts it. More conventional for chat UIs but less consistent with the editorial 'mono label' vocabulary. | |
| Shared renderCopyButton() helper | Extract one function both paths call. Implementation neutral on SVG-vs-text. | |

**User's choice:** COPY text label (Recommended).
**Notes:** The shared helper is a natural consequence of making the two paths identical — captured as D-02 in CONTEXT.md even though it wasn't the primary selection.

### Hover-reveal vs always-visible copy button on bot messages?

| Option | Description | Selected |
|--------|-------------|----------|
| Always-visible | Small mono 'COPY' sitting in message meta — matches replay today, consistent with editorial restraint, works on touch. | ✓ |
| Hover-reveal only | Hide by default, show on hover/focus. More minimal but creates touch-device discoverability issue. | |
| You decide | Capture as Claude's Discretion. | |

**User's choice:** Always-visible (Recommended).

---

## Build / lint warnings (DEBT-01)

### DEBT-01 scope pivot — actual warnings are Wrangler `rate_limits`, not lightning-css

| Option | Description | Selected |
|--------|-------------|----------|
| Upgrade + verify schema | Bump Wrangler to 4.83+, re-run build. If recognized, warning vanishes. If not, relocate or remove. Root-cause fix. | ✓ |
| Remove the `rate_limits` block | Delete from `wrangler.jsonc` if not enforcing. Risk: lose declarative platform rate-limiting intent. | |
| Suppress / annotate without fixing | Wrap build or comment around warning. Violates DEBT-01 zero-warning criterion. | |

**User's choice:** Upgrade + verify schema (Recommended).

### Should the stale `px-[var(--token-*)]` literal in global.css:34 comment be cleaned up?

| Option | Description | Selected |
|--------|-------------|----------|
| Clean it up | Rewrite comment without bracket syntax — prevents regression if `@source not` exclusion ever changes. | ✓ |
| Leave as-is | No warnings today; low priority. | |

**User's choice:** Clean it up (Recommended).

### How to handle the 3 ESLint `unused-disable-directive` warnings?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete stale comment + ignore generated file | Remove dead disable at Container.astro:18; add worker-configuration.d.ts to ESLint ignores (generated by `wrangler types`). | ✓ |
| Auto-fix with `--fix` | Simplest; but fixes in generated file get wiped on next `wrangler types` run. | |
| Regenerate types + then fix | Re-run `wrangler types` first to see whether newer Wrangler stops emitting the directives. | |

**User's choice:** Delete stale comment + ignore generated file (Recommended).

---

## MobileMenu inert strategy (DEBT-02)

### ChatWidget is a body-level sibling and not currently inert'd. Extend the pattern?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — inert the ChatWidget too | Add `setAttribute('inert','')` on the chat widget root in openMenu/closeMenu. Triggers D-26 chat regression battery as verification. | ✓ |
| No — leave ChatWidget tabbable | Argue chat bubble is separate from 'page UI'. Avoids regression overhead but contradicts 'all background UI inert' success criterion. | |
| You decide | Claude's Discretion. | |

**User's choice:** Yes — inert the ChatWidget too (Recommended).

### Keep the focus-trap keydown handler (now partly redundant with inert)?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both — belt-and-suspenders | inert for modern browsers; focus trap as fallback for old mobile Safari/Firefox. Preserves D-08 invariant. | ✓ |
| Remove the trap, rely on inert | Cleaner code; 95%+ browser support for inert. Removes a documented invariant. | |

**User's choice:** Keep both — belt-and-suspenders (Recommended).

### How to verify the middle-element tab-order bug is actually gone?

| Option | Description | Selected |
|--------|-------------|----------|
| Scripted keyboard-cycle test | Open menu on /projects (many bg elements), Tab 30+ times watching activeElement, assert it stays in dialog subtree. Documented manual step. | ✓ |
| Automated Playwright/Puppeteer test | Headless test. Adds a test-runner dep not currently in the project. | |
| Manual smoke test only | Fast but misses the 'many background elements' edge case. | |

**User's choice:** Scripted keyboard-cycle test (Recommended).

---

## Accepted trade-off documentation (DEBT-05, DEBT-06)

### Where should `--ink-faint` contrast exception and print-only `#666` be documented?

| Option | Description | Selected |
|--------|-------------|----------|
| New §2.4 Accepted token exceptions | Dedicated subsection after §2.3 Lock contract. Symmetrical with §10 Chat Bubble Exception. Single grep'able home. | ✓ |
| Extend §2.3 Lock contract | Append inline as 'carve-outs'. Dilutes lock-contract messaging. | |
| Inline at each usage site | CSS comments at each usage. Duplicated, drift-prone, not MASTER.md-discoverable. | |

**User's choice:** New §2.4 Accepted token exceptions (Recommended).

### Inline CSS comments at the usage sites too, as belt-and-suspenders?

| Option | Description | Selected |
|--------|-------------|----------|
| MASTER.md only, no inline | Single source of truth; avoids drift. | ✓ |
| MASTER.md + short inline pointer | `/* accepted trade-off — MASTER.md §2.4 */` at each site. Helps grep discovery. | |

**User's choice:** MASTER.md only, no inline (Recommended).

### Are these trade-offs permanent-accepted or to revisit in a future milestone?

| Option | Description | Selected |
|--------|-------------|----------|
| Permanent-accepted, documented with rationale | Status: accepted — will not be addressed in v1.x. Re-evaluation deferred to future design-system shift. | ✓ |
| Accepted for v1.2, revisit in v1.3 | Forces a re-decision that likely just re-confirms. Adds churn. | |

**User's choice:** Permanent-accepted (Recommended).

### DEBT-03 (OG URL builder) — also needs documentation?

| Option | Description | Selected |
|--------|-------------|----------|
| Verify-in-production only, no new docs | resolveOg fix already shipped. OG crawler run against 5 page types on prod; results recorded in VALIDATION.md. | ✓ |
| Also document the guard in MASTER.md | Adds implementation detail to design contract — probably overkill. | |
| Re-open — different approach for DEBT-03 | (Not selected.) | |

**User's choice:** Verify-in-production only (Recommended).

---

## Claude's Discretion

- Exact helper signature / naming for the shared copy-button renderer.
- Precise syntax for excluding `worker-configuration.d.ts` from ESLint (flat-config `ignores` vs legacy `ignorePatterns`).
- Whether inert also applies to the chat panel (not just the bubble) if somehow open when the mobile menu opens.

## Deferred Ideas

None — discussion stayed within phase scope.
