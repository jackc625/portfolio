# Milestone v1.4: Professional Experience

**Status:** ✅ SHIPPED 2026-07-14
**Phases:** 21-25
**Total Plans:** 22 (53 tasks)
**Audit:** [v1.4-MILESTONE-AUDIT.md](v1.4-MILESTONE-AUDIT.md) — 19/19 requirements satisfied · 5/5 phases verified · 6/6 integration flows wired · 5/5 Nyquist-compliant · `status: passed`

## Overview

Add Jack's real professional experience to the site and reposition him from "student with side projects" to a new-grad engineer with shipped production work — raising recruiter credibility while giving engineers a deep technical read.

Built a typed `experience` content pipeline (mirroring the projects pipeline) feeding a dedicated Experience surface: the Holloway contract engagement as a scannable summary opening into a full deep-dive case study, with the Balfour Beatty 2023 internship as a lightweight earlier-work entry. Reconciled the project set against the résumé — synced the Multi-Chain EVM trader (#7) and introduced a featured tier (SeatWatch / Multi-Chain EVM / NFL Prediction) with the remaining four kept accessible below. Shifted positioning to an honest new-grad-with-production-experience register across Core Value, About, education status, and metadata, with a Home teaser surfacing the Holloway highlight for the 30-second recruiter scan. Refreshed the chat's grounded knowledge to ingest the new experience and project #7 in third person (CHAT-06 voice split held), then verified every cross-cutting quality gate green before ship.

Zero new runtime dependencies. The four D-14 chat-surface files and `dependencies` stayed byte-identical phase-wide; the production-edge Lighthouse gate is owned by `/gsd-ship` (D-11).

## Phases

### Phase 21: Experience Content Pipeline & Collection

**Goal**: A typed, Zod-validated `experience` content collection exists, fed from `Experience/*.md` through a sync pipeline mirroring `scripts/sync-projects.mjs`, carrying the fields and ordering contract the Experience surface will render.
**Depends on**: Nothing new (builds on the live v1.3 site and its established content-collection + sync-pipeline pattern)
**Requirements**: EXP-01, EXP-06
**Plans**: 4 plans

Plans:

- [x] 21-01: Sync mechanism — `sync-experience.mjs` (lift + D-07 strip) + Wave 0 tests-first suite + npm scripts (shipped `sortExperienceEntries` reverse-chron ordering helper as the Phase-22-consumable contract)
- [x] 21-02: `experience` Zod schema registered in `content.config.ts` (D-01 fields, A1 highlights bound; techStack unbounded, highlights.max(5) with no hard min so lightweight entries validate)
- [x] 21-03: Author fenced sources (Holloway fence + new Balfour) + both MDX entries + run sync + build gate (two entries double as schema-optionality fixtures — Holloway omits endDate/present role, Balfour techStack: [])
- [x] 21-04: CI drift gate step + `docs/CONTENT-SCHEMA.md` experience documentation

### Phase 22: Experience Page & Holloway Case Study

**Goal**: Visitors can reach a dedicated Experience page from the primary nav that presents Holloway as a scannable summary opening into a full deep-dive, alongside a lightweight Balfour Beatty entry — all rendered from the Phase 21 collection.
**Depends on**: Phase 21
**Requirements**: EXP-02, EXP-03, EXP-04, EXP-05
**Plans**: 5 plans (UI phase)

Plans:

- [x] 22-01: Wave 0 validation suite — experience-nav / experience-summary / experience-detail / experience-voice-em-dash tests (3 RED until Wave 2)
- [x] 22-02: Primary nav entry `experience` FIRST in Header.astro + MobileMenu.astro (D-03) with `startsWith('/experience')` active branch; focus-trap/inert script untouched (D-26)
- [x] 22-03: `/experience` two-tier listing (Holloway featured with accent CTA vs Balfour non-linked mono "Earlier" entry; highlights as hairline ledger) + D-08 company normalization to "Holloway Company"
- [x] 22-04: `/experience/[id]` Holloway deep-dive (`getStaticPaths` `hasCaseStudy` filter D-01 → only Holloway builds, Balfour structurally excluded; D-02 persistent-arrow back links; D-11 scroll sentinels; page-scoped prose CSS)
- [x] 22-05: Phase gate — full suite (D-26 battery) + build route set + drift + QA-02 dep lock + frontend-design human visual sign-off (SC5d APPROVED)

### Phase 23: Projects Reconciliation & Featured Tier

**Goal**: The résumé-aligned project set is reconciled — the Multi-Chain EVM trader is synced onto the site, three projects are featured in a top tier, and the remaining four stay accessible below, with one data-model distinction driving ordering everywhere.
**Depends on**: Nothing new (reuses `sync-projects.mjs` and the `featured`/`order` schema fields already in `src/content.config.ts`)
**Requirements**: PROJ-01, PROJ-02, PROJ-03, PROJ-04
**Plans**: 4 plans (UI phase)

Plans:

- [x] 23-01: #7 sync + fenced case study + D-15 chat-skip (explicit multi-chain-evm slug-continue before the hard-fails; MULTI-DEX regex retained dormant) + featured/order reconciliation (SolSniper → featured:false order 4; exactly 3 featured, order contiguous 1-7) + SC4 ordering gate
- [x] 23-02: Content-gate coverage — add #7 to the six site-side pinned arrays (D-16 exhaustiveness); chat-side pins left unchanged so chat stays at 6 until Phase 25
- [x] 23-03: Two-tier featured UI — WorkRow optional `tagline` prop (byte-identical when absent), projects.astro FEATURED/MORE WORK, Home taglines + See-all-work link; global.css/BaseLayout untouched; SC2 render gate parses built HTML for exactly 3 `.work-tagline` elements
- [x] 23-04: Phase gate — full `pnpm build` + `pnpm test` + drift + dep lock (QA-02) + human sign-off (#7 copy D-02/D-03 + frontend-design 6-pillar SC5), both APPROVED

### Phase 24: Positioning Shift & Home Teaser

**Goal**: The site reads as a new-grad engineer with shipped production experience — Core Value framing, About narrative, education status, and metadata all updated in an honest new-grad (not senior) register — with a Home teaser surfacing the Holloway highlight for the 30-second recruiter scan.
**Depends on**: Phase 22 (the Home teaser links through to the Experience page)
**Requirements**: POS-01, POS-02, POS-03, POS-04, HOME-01
**Plans**: 4 plans (UI phase)

Plans:

- [x] 24-01: `src/data/education.ts` SSoT (facts + DERIVED alumniOf/hasCredential fragments, no re-hardcoded WGU/VT) + Gate D (education module) + strengthened Gate E (chat-surface tripwire, 11 anchors) + phase-start invariant baseline (`24-BASELINE.json` protected-file hashes + deps + og placeholder)
- [x] 24-02: Home `index.astro` — `01 EXPERIENCE` Holloway teaser (frontend-design, id-guarded query reusing experience.astro's) + renumber 01/02/03/04 (incl. ContactSection literal + strip its em-dash comments) + enrich personSchema (jobTitle/alumniOf/hasCredential) + sharpen SEO description + Gate B/C (home-teaser render + JSON-LD parse)
- [x] 24-03: About — `about.ts` copy revision (intro/P1/P3, P2 verbatim at plan time) + `/about` Education block (frontend-design) fed by `education.ts` + description sharpen + `about-education-render` POS-03 regression test
- [x] 24-04: Real 1200×630 `og-default.png` (frontend-design true-Geist card, deterministic PNG-signature/size/hash-diff verifier) + Gate A (site-copy em-dash/register banlist across all five data/page copy surfaces) + capstone gate (build + full suite incl D-26/sse-snapshot + astro-check 0/0/0 + baseline comparison) + human sign-off (copy + 6-pillar at 375px/1440px + Schema Markup Validator)

> **24-UAT deviation:** removed About `ABOUT_P2` per Jack's copy review (reverses D-06); coupled about.ts + about.astro + about-data.test.ts edits; chat about block sourced from `about-chat.ts` so `portfolio-context.json` stayed byte-identical.

### Phase 25: Chat Knowledge Refresh & Milestone Verification

**Goal**: The chat's grounded knowledge includes the new Experience content and project #7 in third-person voice, and the milestone's cross-cutting quality gates are verified green end-to-end before ship.
**Depends on**: Phase 22, Phase 23, Phase 24
**Requirements**: CHAT-10, CHAT-11, QA-01, QA-02
**Plans**: 5 plans

Plans:

- [x] 25-00: Phase-25 invariant baseline — `verify-phase25-invariants.mjs` + `25-BASELINE.json` hashing the four D-14 gated files + normalized deps at phase start (committed-drift proof for QA-01/QA-02); added in the --reviews replan
- [x] 25-01: Wave-0 test retargets — 7 slugs + 4-field array-experience walk + Balfour B1 leak samples + SSoT education + #7 ban-invert + `<security>` byte-intact snapshot + fixture rename (14 RED-by-design / 77 GREEN)
- [x] 25-02: Chat copy authoring + human review — Holloway/#7/Balfour chatSummary, about-chat + static.json positioning, system-prompt.ts #7-ban lift; cut chat `ABOUT_CHAT_P2` to match the Phase 24 site P2 removal
- [x] 25-03: ATOMIC build-engine migration — lift #7 exclusion + recursive fail-closed experience array + `parseEducation` (unit-tested) + 4-field leak-walk + regex extension ×3; regenerate corpus (projects=7 untruncated, est_tokens=48735)
- [x] 25-04: Milestone verification capstone — build/test/astro-check/drift + the 25-00 hash verifier for the phase-wide untouched proof + D-26/D-15 + live chat UAT incl. security probe (APPROVED by Jack)

---

## Milestone Summary

**Decimal Phases:** none.

**Key Decisions:**

- Reuse the projects sync pipeline for experience (EXP-01) — parallel `Experience/*.md → experience` collection + `sync-experience.mjs`, not a new mechanism. ✓ Good — idempotent `--check` drift gate, Zod-validated.
- `hasCaseStudy` filter drives detail-route generation (D-01) — Holloway builds `/experience/holloway`; Balfour structurally excluded from any detail route. ✓ Good — EXP-05 lightweight entry enforced at the `getStaticPaths` layer, not by convention.
- Single `featured`/`order` data-model distinction drives ordering on both `/projects` and Home (PROJ-04) — schema already carried the fields; reconciliation applied them consistently. ✓ Good — both surfaces number via `order`, no array-index desync.
- `education.ts` single source of truth (D-10) — DERIVED alumniOf/hasCredential fragments feed both the JSON-LD Person schema and the chat education object; VT rendered as a transfer sub-note, never a credential. ✓ Good — one edit updates every education surface.
- Honest new-grad register, no seniority cosplay (POS-01/02) — Gate A banlist enforces junior/senior/"5+ years" absence across all five site copy surfaces. ✓ Good — closed the MDX-only voice-gate scope gap.
- Deliberate D-15 → D-04 lift for chat corpus (CHAT-10/PROJ-01) — #7 held out of the chat corpus through Phase 23, then the `build-chat-context.mjs` exclusion lifted in Phase 25. ✓ Good — corpus stayed at 6 until the atomic Phase 25 migration, then went to 7 untruncated.
- CHAT-06 voice split held for new content — third-person chatSummary/about-chat variants; `checkFirstPersonLeaks` 4-field walk hard-fails the build on leakage. ✓ Good — leak guard green with #7 + experience ingested.
- Per-phase invariant baselines (`24-BASELINE.json` / `25-BASELINE.json`) — hash protected files + deps at phase start so the capstone proves phase-wide byte-identity against committed drift, not just a working-tree diff. ✓ Good — QA-01/QA-02 proven committed-drift-safe.

**Issues Resolved:**

- Positioning drift — site no longer reads as "a student building side projects"; new-grad-with-production-experience framing across every surface.
- Off-résumé project set reconciled without deletion — 3 featured, 4 kept accessible.
- Chat knowledge staleness — corpus regenerated with Experience + project #7 in third person.
- Voice-gate scope gap (em dashes slipping into non-MDX About/data copy) closed by Gate A.

**Issues Deferred:**

- **EXP-FUT-01** — full case-study treatment for the Balfour Beatty internship (non-engineering; lightweight entry sufficient for now).
- **EXP-FUT-02** — metrics/impact visualizations for experience highlights (0→~1,400 tests, 223→1 scoping); defer until the text case study proves the format.
- **QA-02 · production-edge Lighthouse** — the one ship-time gate; runs during `/gsd-ship` (a production Cloudflare edge must exist first), per D-11.

**Technical Debt Incurred (non-blocking, from the milestone audit):**

- Phase 22 WR-01 — copied scroll-depth sentinels also fire `scroll_depth` analytics on `/experience/holloway` (the tracker comment scopes it to project detail pages); analytics scope drift, not a functional break.
- Phase 22 WR-02 — the ~360-char first-person Holloway `summary` is reused verbatim as the detail-page meta/OG/Twitter description; truncates awkwardly in search results (SEO polish).
- Phase 23 WR-02 — tautological wordcount test (`expect(true).toBe(true)`, soft `console.warn`); intentional per D-16 (600–900-word floor enforced by human sign-off, not automation).
- Milestone-wide — SUMMARY `requirements-completed` frontmatter under-populated for Phase 22 (only EXP-02) and Phase 23 (none); coverage proven by each VERIFICATION.md + the integration check. Documentation hygiene only.

---

_For current project status, see .planning/ROADMAP.md_
