---
phase: 22-experience-page-holloway-case-study
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/build/experience-nav.test.ts
  - tests/content/experience-summary.test.ts
  - tests/content/experience-detail.test.ts
  - tests/content/experience-voice-em-dash.test.ts
autonomous: true
requirements: [EXP-02, EXP-03, EXP-04, EXP-05]
must_haves:
  truths:
    - "Running vitest collects four new experience test files with no syntax/collection errors"
    - "experience-nav.test.ts fails (RED) until Header.astro and MobileMenu.astro carry /experience first"
    - "experience-summary.test.ts fails (RED) on the D-08 company normalization and the /experience listing source until those ship"
    - "experience-detail.test.ts asserts only holloway has hasCaseStudy and fails (RED) on the /experience/[id] source until it ships"
    - "experience-voice-em-dash.test.ts is GREEN now (no em dashes in experience mdx bodies) and guards future edits + new page copy"
  artifacts:
    - tests/build/experience-nav.test.ts
    - tests/content/experience-summary.test.ts
    - tests/content/experience-detail.test.ts
    - tests/content/experience-voice-em-dash.test.ts
  key_links:
    - "experience-nav.test.ts asserts index of href /experience < index of href /projects in both nav primitives"
    - "experience-detail.test.ts filter contract (hasCaseStudy === true) resolves to exactly the id set [holloway]"
---

<objective>
Author the Wave 0 tests-first validation suite for the Experience surface (Nyquist Dimension 8 / the RESEARCH.md Validation Architecture). These four test files encode the observable contracts for SC1-SC5e BEFORE the pages, nav entry, and D-08 normalization exist, so the Wave 2 implementation plans (22-02, 22-03, 22-04) turn them GREEN and the Wave 3 gate (22-05) confirms them.

Purpose: Make the phase's success criteria machine-checkable and give every downstream task an `<automated>` verify to sample against. Realizes RESEARCH.md "Wave 0 Gaps" and phase constraint #7 (Wave 0 must add/mirror the validation tests; analogs `case-studies-shape`, `voice-em-dash`, `sync-experience*`).
Output: 3 new RED/guard test files + 1 em-dash guard file, all mirroring existing in-repo test idioms; zero framework install (Vitest 4.1.0 already present).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-experience-page-holloway-case-study/22-RESEARCH.md
@.planning/phases/22-experience-page-holloway-case-study/22-PATTERNS.md
@.planning/phases/22-experience-page-holloway-case-study/22-VALIDATION.md

# Test analogs to mirror
@tests/content/case-studies-shape.test.ts
@tests/content/voice-em-dash.test.ts
@tests/content/projects-collection.test.ts

# The data under test (already shipped in Phase 21)
@src/content/experience/holloway.mdx
@src/content/experience/balfour-beatty.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav-shape test (experience-nav.test.ts) — SC1 / EXP-02</name>
  <files>tests/build/experience-nav.test.ts</files>
  <read_first>
    - tests/content/case-studies-shape.test.ts (the readFile + normalize + string-assertion idiom to copy)
    - src/components/primitives/Header.astro (navLinks lines 23-27, isActive lines 29-35 — the source shape being asserted)
    - src/components/primitives/MobileMenu.astro (navLinks lines 38-42, isActive lines 52-57)
  </read_first>
  <action>
    Create tests/build/experience-nav.test.ts using the vitest describe/it + node:fs `readFile` idiom from case-studies-shape.test.ts (normalize CRLF with .replace(/\r\n/g,"\n")). For BOTH src/components/primitives/Header.astro AND src/components/primitives/MobileMenu.astro, read the source text and assert: (1) the substring `href: "/experience"` appears and its index is LESS than the index of `href: "/projects"` (D-03 experience-FIRST ordering); (2) the source contains an isActive branch matching /experience via startsWith — assert the source includes both `"/experience"` and `startsWith("/experience")`. These assertions are RED until plan 22-02 edits the nav. Do NOT assert on the focus-trap script region. No fenced literal that a later negative-grep depends on.
  </action>
  <acceptance_criteria>
    - tests/build/experience-nav.test.ts exists and is collected by `pnpm exec vitest run tests/build/experience-nav.test.ts` with no syntax/collection error
    - The test asserts, for Header.astro AND MobileMenu.astro, that `href: "/experience"` precedes `href: "/projects"` and that `startsWith("/experience")` is present
    - The test is currently RED (Header/MobileMenu do not yet contain /experience) — RED is the expected pre-implementation state
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/build/experience-nav.test.ts</automated>
  </verify>
  <done>Nav-shape test authored; fails only because the /experience nav entry is not yet added (turned GREEN by 22-02).</done>
</task>

<task type="auto">
  <name>Task 2: Holloway summary + D-08 test (experience-summary.test.ts) — SC2 / EXP-03 / D-08</name>
  <files>tests/content/experience-summary.test.ts</files>
  <read_first>
    - tests/content/case-studies-shape.test.ts (frontmatter/body split idiom at lines 29-32: indexOf("\n---\n",4) then slice)
    - src/content/experience/holloway.mdx (frontmatter lines 1-30: company line 3, role, dateRange, techStack, 5 highlights)
    - .planning/phases/22-experience-page-holloway-case-study/22-RESEARCH.md (D-08 normalization + Validation SC2 row)
  </read_first>
  <action>
    Create tests/content/experience-summary.test.ts. Read src/content/experience/holloway.mdx, split frontmatter from body via the case-studies-shape idiom. Assert on the frontmatter block: company equals exactly `Holloway Company` (D-08 — the leading "The" is dropped; this is RED until 22-03 normalizes it), and that role, dateRange, and a non-empty techStack are present, and that the highlights list contains exactly 5 items. REVIEWS 22-01 (LOW): Holloway's `highlights` is MULTILINE flow-style YAML (a `[` on its own line, one quoted string per line, then a closing `]`) — NOT a single-line `[a, b, c]` array — so write the count to tolerate that: slice the block from the `highlights:` key up to the next top-level key (`engagementType:`) and count the quoted string entries (e.g. lines that begin, after whitespace, with a double-quote), never assuming a single-line array shape. Then read src/pages/experience.astro source and assert it references `sortExperienceEntries` and contains a hyperlink whose href starts with `/experience/` (the D-05 "Read the full case study" deep-dive link) — this portion is RED until 22-03 ships the page. Guard the page-source assertions so a missing file yields a clear RED failure, not a thrown ENOENT that aborts the suite.
  </action>
  <acceptance_criteria>
    - tests/content/experience-summary.test.ts asserts holloway.mdx company === "Holloway Company" (no "The")
    - The test asserts exactly 5 highlight entries, counting the MULTILINE flow-style YAML block (REVIEWS 22-01 — not assuming a single-line array), plus role/dateRange/non-empty techStack present
    - The test asserts src/pages/experience.astro uses sortExperienceEntries and links to an `/experience/` detail path
    - Test is RED pre-implementation (company still "The Holloway Company" and experience.astro absent)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/content/experience-summary.test.ts</automated>
  </verify>
  <done>Summary/D-08 test authored; RED until 22-03 normalizes company and ships the listing page.</done>
</task>

<task type="auto">
  <name>Task 3: Detail filter test (experience-detail.test.ts) — SC3 / SC4 / EXP-04 / EXP-05</name>
  <files>tests/content/experience-detail.test.ts</files>
  <read_first>
    - tests/content/projects-collection.test.ts (readdir + per-file frontmatter regex idiom, e.g. /^featured:\s*true\b/m)
    - src/content/experience/holloway.mdx (hasCaseStudy: true, line 28) and src/content/experience/balfour-beatty.mdx (hasCaseStudy: false, line 16)
    - .planning/phases/22-experience-page-holloway-case-study/22-PATTERNS.md (Wave 0 analog note: replicate the hasCaseStudy filter over the collection)
  </read_first>
  <action>
    Create tests/content/experience-detail.test.ts. First assert the CONTENT contract (the structural EXP-05 guarantee): readdir src/content/experience, for each .mdx parse the `hasCaseStudy:` boolean from frontmatter, and assert the set of ids where hasCaseStudy is true equals exactly `["holloway"]` and that `balfour-beatty` is NOT in that set. This mirrors the filter `entries.filter((e) => e.data.hasCaseStudy)` that getStaticPaths will use. Second, read src/pages/experience/[id].astro source and assert it contains a getStaticPaths filter on `hasCaseStudy` (assert the source includes `hasCaseStudy` inside a `.filter(`) and EXACTLY TWO `href="/experience"` occurrences (the D-02 top AND bottom back links — REVIEWS 22-01: count them, do not assert merely one; counting the route source is safe because the site nav lives in Header/MobileMenu, not in this route file, so only the two back links contribute to the count) — RED until 22-04 ships the route; guard the missing-file case to fail RED cleanly rather than throw.
  </action>
  <acceptance_criteria>
    - The content-contract assertion (ids with hasCaseStudy true === ["holloway"]; balfour-beatty excluded) passes against the current collection
    - The source-shape assertion requires src/pages/experience/[id].astro to filter on hasCaseStudy in getStaticPaths and to carry EXACTLY TWO `href="/experience"` occurrences (the D-02 top + bottom back links — REVIEWS 22-01, not just one)
    - Test is RED pre-implementation on the source-shape half (route not yet built)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/content/experience-detail.test.ts</automated>
  </verify>
  <done>Detail-filter test authored; the content contract passes, the source-shape assertion RED until 22-04 ships /experience/[id].astro.</done>
</task>

<task type="auto">
  <name>Task 4: Experience em-dash guard (experience-voice-em-dash.test.ts) — SC5e</name>
  <files>tests/content/experience-voice-em-dash.test.ts</files>
  <read_first>
    - tests/content/voice-em-dash.test.ts (the paragraph-split + `—` count idiom to mirror; note it hardcodes the 6 project slugs — do NOT edit that locked file)
    - src/content/experience/holloway.mdx, src/content/experience/balfour-beatty.mdx (bodies must stay em-dash-free)
  </read_first>
  <action>
    Create a NEW file tests/content/experience-voice-em-dash.test.ts (do not modify the locked project voice-em-dash.test.ts noted as cross-phase-fragile in STATE). Mirror the paragraph-split idiom: for each of src/content/experience/holloway.mdx and balfour-beatty.mdx, slice the body, split on blank-line paragraph boundaries, skip `## ` headings, and assert zero `—` occurrences per paragraph. Additionally, when src/pages/experience.astro and src/pages/experience/[id].astro exist, read their source and assert zero `—` in the file (catches an em dash slipping into a new meta-description string — the UI-SPEC copy table line 103 shows an em dash that is a transcription error superseded by the zero-em-dash rule; the correct meta uses an en dash). Skip the page assertions gracefully if the files are not present yet so this file is GREEN in Wave 1 and tightens once pages ship.
  </action>
  <acceptance_criteria>
    - tests/content/experience-voice-em-dash.test.ts asserts zero `—` in both experience mdx bodies (GREEN against current content)
    - When the two new page files exist, the test also asserts zero `—` in their source
    - The existing tests/content/voice-em-dash.test.ts is unchanged
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/content/experience-voice-em-dash.test.ts</automated>
  </verify>
  <done>Em-dash guard authored as a new file; GREEN now, guards the experience mdx bodies and future page meta copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → repo source | Tests only READ source files; they mutate nothing at runtime. No boundary crossed with untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | New test files assert wrong contract (false GREEN) | low | mitigate | Assertions transcribed from the locked D-01..D-11 decisions and SC map; each test is RED pre-implementation, proving it discriminates. |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — Vitest 4.1.0 already present (QA-02 byte-identical lock). No install task; legitimacy gate N/A. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/build/experience-nav.test.ts tests/content/experience-summary.test.ts tests/content/experience-detail.test.ts tests/content/experience-voice-em-dash.test.ts` collects all four files with no syntax/collection errors.
- experience-voice-em-dash.test.ts is GREEN; the other three are RED only on the not-yet-built contracts (expected pre-implementation state).
- No new dependency added (package.json unchanged); the existing tests/content/voice-em-dash.test.ts is untouched.
- These source-shape tests are intentionally BACKED by build-output HTML assertions at the 22-05 gate (all 5 Holloway highlights visible on the built listing, no `experience/balfour-beatty` link, two `Back to experience` affordances on the built deep-dive), so a page cannot false-green the source guards (REVIEWS 22-01).
</verification>

<success_criteria>
Four test files exist mirroring the named analogs, encoding SC1 (nav), SC2+D-08 (summary/company), SC3+SC4 (detail filter / Balfour exclusion), and SC5e (em-dash). Their pass/fail state precisely tracks whether Wave 2 has shipped the corresponding artifact.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files this plan creates (exclude from drift/source-grounding verification — they do not exist before this plan):
- `tests/build/experience-nav.test.ts`
- `tests/content/experience-summary.test.ts`
- `tests/content/experience-detail.test.ts`
- `tests/content/experience-voice-em-dash.test.ts`

<output>
Create `.planning/phases/22-experience-page-holloway-case-study/22-01-SUMMARY.md` when done.
</output>
