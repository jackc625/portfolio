---
phase: 21-experience-content-pipeline-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync-experience.mjs
  - src/lib/experience.ts
  - tests/scripts/sync-experience.test.ts
  - tests/scripts/sync-experience-check.test.ts
  - tests/scripts/sync-experience-idempotency.test.ts
  - package.json
autonomous: true
requirements: [EXP-01, EXP-06]

must_haves:
  truths:
    - "`node scripts/sync-experience.mjs --check` exits 0 on a clean tree and exits 1 when a fenced source is mutated but the MDX is not (SC1)"
    - "A second `pnpm sync:experience` write-mode run over an unchanged entry does NOT rewrite the file — the idempotency test asserts `secondContents === firstContents` AND `secondStat.mtimeMs === firstStat.mtimeMs` (SC1, review finding #1 — a `--check` drift test alone would not catch a script that rewrites identical bytes every run)"
    - "The lifted pure functions (normalize, readSourceField, sliceFrontmatter, extractFence) are importable and behave per the mirrored unit tests (SC1, D-06)"
    - "A `source:` path escaping the project root exits 2 — the path-escape test captures the thrown process status and asserts `err.status === 2` in addition to matching the `escapes project root` message (V12 path-traversal guard; review finding #2 — verbatim mirror asserted only the message, so exit 2 was not actually proven)"
    - "The reverse-chronological ordering is proven by importing the REAL `sortExperienceEntries()` helper from `src/lib/experience.ts` and asserting it sorts Holloway (2026) before Balfour (2023) — a reusable query-ordering contract Phase 22 consumes, not a throwaway inline comparator (SC3 / EXP-06, D-04; review finding #3, option a)"
    - "`pnpm sync:experience` and `pnpm sync:experience:check` npm scripts exist and invoke the new script (D-11)"
  artifacts:
    - scripts/sync-experience.mjs
    - src/lib/experience.ts (sortExperienceEntries helper)
    - tests/scripts/sync-experience.test.ts
    - tests/scripts/sync-experience-check.test.ts
    - tests/scripts/sync-experience-idempotency.test.ts
    - package.json (sync:experience + sync:experience:check scripts)
  key_links:
    - "CLI guard `if (process.argv[1] === fileURLToPath(import.meta.url)) await main()` keeps main() from running on import so the pure functions stay testable"
    - "MDX_GLOB constant points at `src/content/experience/*.mdx` (the ONLY glob change from the projects analog)"
    - "FENCE_START/FENCE_END reuse the exact `<!-- CASE-STUDY-START -->` / `<!-- CASE-STUDY-END -->` strings (D-08)"
    - "`sortExperienceEntries` is a pure generic `<T extends { data: { startDate: Date } }>` sort (no `astro:content` import) so it is Vitest-testable in node env AND consumable by Phase 22 as `sortExperienceEntries(await getCollection(\"experience\"))`"
---

<objective>
Stand up the experience sync mechanism: a parallel `scripts/sync-experience.mjs` that mirrors `scripts/sync-projects.mjs` (fenced-block extraction, byte-for-byte frontmatter preservation, LF normalization, path-traversal guard, idempotent diff-then-write, `--check` drift mode) with the two projects-specific validations (5-H2 shape + 600–900 word target) stripped per D-07. Also ships the reusable reverse-chronological ordering helper `src/lib/experience.ts` (`sortExperienceEntries`) that makes SC3 a concrete, consumable contract for Phase 22. Ships with the Wave 0 tests-first suite (unit + path-traversal + `--check` drift + write-mode idempotency + ordering) and the two npm scripts.

Purpose: This is SC1's engine and SC3's ordering contract at the unit level. Everything downstream (schema, content, CI) depends on this script existing and being import-safe.

Review-incorporation summary (see 21-REVIEWS.md): finding #1 (add the write-mode idempotency test), finding #2 (assert exit `2` — not just the message — in the path-escape test), and finding #3 (SC3 helper, option a — ship `sortExperienceEntries` rather than reword SC3) are all incorporated in this plan.

Output: `scripts/sync-experience.mjs` (exports `normalize`, `readSourceField`, `sliceFrontmatter`, `extractFence`; internal `syncOne`/`main`), `src/lib/experience.ts` (exports `sortExperienceEntries`), three Vitest files, and the `sync:experience` / `sync:experience:check` package.json scripts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-experience-content-pipeline-collection/21-RESEARCH.md
@.planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md
@.planning/phases/21-experience-content-pipeline-collection/21-VALIDATION.md

# Analog sources — copy from these, do not reconstruct from memory
@scripts/sync-projects.mjs
@tests/scripts/sync-projects.test.ts
@tests/scripts/sync-projects-check.test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Wave 0 tests-first suite (RED)</name>
  <files>tests/scripts/sync-experience.test.ts, tests/scripts/sync-experience-check.test.ts, tests/scripts/sync-experience-idempotency.test.ts</files>
  <read_first>
    - tests/scripts/sync-projects.test.ts (the unit + path-traversal analog to mirror; note the path-escape test at :98-128 asserts ONLY `threwWithExpectedMessage` and never inspects `err.status` — review finding #2 is exactly this gap)
    - tests/scripts/sync-projects-check.test.ts (the --check drift analog to mirror; note the drift case at :97-106 already captures `err.status` and asserts `status === 1` — reuse that status-capture idiom for the exit-2 path-escape assertion)
    - tests/scripts/sync-projects-idempotency.test.ts (the OMITTED third analog — review finding #1; note the "second run does not rewrite" mtime assertion at :103-112: `secondContents === firstContents` AND `secondStat.mtimeMs === firstStat.mtimeMs`)
    - .planning/phases/21-experience-content-pipeline-collection/21-VALIDATION.md (Wave 0 requirements + SC→test map)
    - .planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md (test import block + scaffold guidance)
  </read_first>
  <action>
    Create all three Vitest files by mirroring the projects analogs, repointing every `sync-projects` reference to `sync-experience`. These are authored BEFORE the script + helper exist (tests-first / Wave 0 per 21-VALIDATION.md), so they fail to import until Task 2 lands them — that is the intended RED state.

    In `sync-experience.test.ts`: import `readSourceField`, `sliceFrontmatter`, `extractFence`, and `normalize` from `../../scripts/sync-experience.mjs`. DROP the `wordCount` import and its describe block (that function is deleted per D-07). Mirror the `readSourceField`, `sliceFrontmatter`, and `extractFence` describe blocks verbatim (same fence markers `<!-- CASE-STUDY-START -->` / `<!-- CASE-STUDY-END -->` per D-08, same assertions). For the S3 path-traversal integration test, mirror the analog and repoint the tmpdir mdx directory to `src/content/experience`, the bad `.mdx` frontmatter to experience fields (e.g. `role: Bad`), and the spawned script path to `scripts/sync-experience.mjs` — but STRENGTHEN it per review finding #2: in addition to matching the `escapes project root` message, capture the thrown process status inside the `catch` (`(err as { status?: number }).status`) and assert it `=== 2` (the exit-2 contract at scripts/sync-projects.mjs:233-235). Both the message match AND `status === 2` must be asserted — a verbatim mirror would prove only the message and leave the T-21-01 exit-2 mitigation claim unbacked.

    For the SC3 ordering describe block (review finding #3, option a — helper, not inline comparator): import `sortExperienceEntries` from `../../src/lib/experience` (created in Task 2) and assert that calling it over two mock objects `{ data: { startDate: new Date("2023-05") } }` (Balfour) and `{ data: { startDate: new Date("2026-05") } }` (Holloway) returns them Holloway-first (`result[0].data.startDate` is the 2026 date, `result[1]` the 2023 date) — proving the REAL reusable helper (not a throwaway inline comparator) implements D-04 reverse-chron. Do NOT re-define the comparator inline; the test must exercise the exported helper so SC3 is a verifiable, consumable contract.

    In `sync-experience-check.test.ts`: mirror the `scaffold()` + `afterEach` cleanup pattern, but repoint `Projects` → `Experience` and `src/content/projects` → `src/content/experience`, set `syncScript` to `scripts/sync-experience.mjs`, and REPLACE the 5-H2 `body` array with freeform experience prose (no `## Problem`/`## Approach & Architecture`/etc. — that shape is not validated per D-07) containing a stable needle string the drift test can mutate. The scaffolded mdx frontmatter must carry experience fields (`role`, `company`, `startDate`, `dateRange`, `summary`, `techStack`, `highlights`, `engagementType`, `hasCaseStudy`, `source: "Experience/1 - SAMPLE.md"` or equivalent tmp source path). Mirror the two `--check` cases verbatim: exit 0 when source fence matches the MDX body, exit 1 (assert `status === 1`) when the fenced source is mutated but the MDX is not.

    In `sync-experience-idempotency.test.ts` (NEW — review finding #1, mirrors tests/scripts/sync-projects-idempotency.test.ts): scaffold a tmpdir with `Experience/` + `src/content/experience/` and a matching source `.md` (fenced with `<!-- CASE-STUDY-START -->` / `<!-- CASE-STUDY-END -->`) + `.mdx`. Use FREEFORM experience prose for the fenced body (a short paragraph — NOT the 5-H2 project shape, since D-07 removed it). Run the sync in WRITE mode once via `execFileSync("node", [syncScript], { cwd: tmp })`, capture `stat().mtimeMs` and file contents; run it a SECOND time and assert `secondContents === firstContents` AND `secondStat.mtimeMs === firstStat.mtimeMs` — proving the diff-then-write path does not rewrite an unchanged file (a guarantee the `--check` drift test does not provide). Mirror the analog's `afterEach` tmp cleanup.
  </action>
  <verify>
    <automated>grep -l "sync-experience.mjs" tests/scripts/sync-experience.test.ts tests/scripts/sync-experience-check.test.ts tests/scripts/sync-experience-idempotency.test.ts && grep -c "escapes project root" tests/scripts/sync-experience.test.ts && grep -c "sortExperienceEntries" tests/scripts/sync-experience.test.ts && grep -c "mtimeMs" tests/scripts/sync-experience-idempotency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/scripts/sync-experience.test.ts imports readSourceField, sliceFrontmatter, extractFence, normalize from ../../scripts/sync-experience.mjs
    - tests/scripts/sync-experience.test.ts imports `sortExperienceEntries` from ../../src/lib/experience and asserts it returns Holloway (2026) before Balfour (2023) — no inline comparator re-definition (finding #3)
    - tests/scripts/sync-experience.test.ts path-escape test asserts BOTH the `escapes project root` message AND the captured thrown `err.status === 2` (finding #2 — not message-only)
    - tests/scripts/sync-experience.test.ts contains NO reference to wordCount or a 5-H2 body shape
    - tests/scripts/sync-experience-check.test.ts contains one case expecting exit 0 (no drift) and one expecting status === 1 (drift)
    - tests/scripts/sync-experience-idempotency.test.ts asserts a second write-mode run leaves `secondContents === firstContents` AND `secondStat.mtimeMs === firstStat.mtimeMs`, using freeform (non-5-H2) experience prose (finding #1)
  </acceptance_criteria>
  <done>All three test files exist, reference `scripts/sync-experience.mjs`, and cover the Wave 0 behaviors (pure fns, path-traversal exit 2 with status assertion, --check exit 0/1, write-mode idempotency, SC3 ordering via the real helper), carrying no projects-specific H2/word-count assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/sync-experience.mjs (lift + strip) and the src/lib/experience.ts ordering helper (GREEN)</name>
  <files>scripts/sync-experience.mjs, src/lib/experience.ts</files>
  <read_first>
    - scripts/sync-projects.mjs (the verbatim-lift source — every line disposition is called out in 21-PATTERNS.md)
    - .planning/phases/21-experience-content-pipeline-collection/21-PATTERNS.md (line-by-line LIFT-vs-DELETE map for this file; §"z.coerce.date() + descending sort" for the ordering contract)
    - .planning/phases/21-experience-content-pipeline-collection/21-RESEARCH.md (§Sync Mechanism Replication D-06/D-07)
  </read_first>
  <action>
    Create the SC3 ordering helper `src/lib/experience.ts` (review finding #3, option a) FIRST so the Task 1 ordering test goes green: export a pure function `sortExperienceEntries` typed as a generic `<T extends { data: { startDate: Date } }>(entries: T[]): T[]` that returns a NEW array (do not mutate the argument — copy then sort) ordered by `startDate` DESCENDING using `b.data.startDate.valueOf() - a.data.startDate.valueOf()` (D-04 reverse-chron). Import NOTHING from `astro:content` (structural generic only) so the helper stays Vitest-testable in node env and is still consumable by Phase 22 as `sortExperienceEntries(await getCollection("experience"))`. This is the reusable ordering contract SC3 promises — no page, route, or `getCollection` call lives here (that render/query wiring is Phase 22 per 21-CONTEXT).

    Then copy `scripts/sync-projects.mjs` to `scripts/sync-experience.mjs` and apply exactly these changes per D-06/D-07 (21-PATTERNS.md carries the line numbers):

    Constants: change `MDX_GLOB` to `"src/content/experience/*.mdx"` (the ONLY glob change). Keep `FENCE_START`/`FENCE_END` verbatim (D-08). DELETE the `EXPECTED_H2S` array, `WORD_TARGET_MIN`, and `WORD_TARGET_MAX`.

    Pure functions: LIFT `normalize`, `readSourceField`, `sliceFrontmatter`, and `extractFence` verbatim. Add the `export` keyword to `normalize` (the projects analog leaves it a bare const; the test file imports it). DELETE the `wordCount` and `checkH2Shape` functions entirely (D-07).

    `syncOne`: LIFT verbatim EXCEPT remove the two lines `const words = wordCount(newBody); checkH2Shape(newBody, slug);` and drop the `words` property from all three returned objects so they become `{ slug, changed: false }`, `{ slug, changed: true, drift: true }`, and `{ slug, changed: true }`. Preserve the path-traversal guard block verbatim (`if (!absSource.startsWith(PROJECT_ROOT + sep) && absSource !== PROJECT_ROOT) throw ...`) — this is the V12 File Handling control (T-21-01); do NOT drop it during the strip. Preserve the `access` existence check, the `newMdx = frontmatterBlock + "\n" + newBody + "\n"` assembly, and the diff-then-write / CHECK_MODE drift branch verbatim.

    `main`: LIFT verbatim EXCEPT remove the `wordTag` computation and simplify the stdout line to `${r.slug}.mdx: ${verb}`. Keep the `errorCount > 0 → exit 2`, `CHECK_MODE && driftFound → exit 1`, else `exit 0` logic verbatim.

    CLI guard: LIFT verbatim (`if (process.argv[1] === fileURLToPath(import.meta.url)) await main();`) so the pure functions remain importable without running `main`.

    Header docblock: rewrite to reference `Experience/*.md` → `src/content/experience/*.mdx`; delete the "600-900 word" mention and the word-count line. Keep the exit-code contract lines (0 success/no-drift, 1 drift in --check, 2 hard failure). Do NOT restate any deleted-validation literal in prose that would confuse future readers.
  </action>
  <verify>
    <automated>pnpm test tests/scripts/sync-experience.test.ts tests/scripts/sync-experience-check.test.ts tests/scripts/sync-experience-idempotency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/sync-experience.mjs exports normalize, readSourceField, sliceFrontmatter, extractFence
    - src/lib/experience.ts exports `sortExperienceEntries`, sorts by `startDate` descending, returns a new (non-mutated) array, and imports nothing from `astro:content` (finding #3, option a)
    - `grep -c "MDX_GLOB = \"src/content/experience/\*.mdx\"" scripts/sync-experience.mjs` returns 1
    - `grep -c "checkH2Shape\|wordCount\|EXPECTED_H2S\|WORD_TARGET" scripts/sync-experience.mjs` returns 0 (D-07 strip complete)
    - `grep -c "escapes project root" scripts/sync-experience.mjs` returns 1 (V12 guard preserved)
    - `pnpm test tests/scripts/sync-experience.test.ts tests/scripts/sync-experience-check.test.ts tests/scripts/sync-experience-idempotency.test.ts` passes (all three files green — self-bootstrapped tmpdir fixtures; the ordering test exercises the real helper)
  </acceptance_criteria>
  <done>The stripped script + the ordering helper exist, the full Wave 0 suite (including the new idempotency test and the exit-2 path-escape assertion) is green, the path-traversal guard is intact, and no H2/word-count code survives the strip.</done>
</task>

<task type="auto">
  <name>Task 3: Add the sync:experience npm scripts (D-11)</name>
  <files>package.json</files>
  <read_first>
    - package.json (existing `sync:projects` / `sync:check` scripts at lines 24-25 are the exact parallel)
  </read_first>
  <action>
    Add two scripts to the `scripts` block, parallel to the existing `sync:projects` and `sync:check` (D-11): `"sync:experience": "node scripts/sync-experience.mjs"` and `"sync:experience:check": "node scripts/sync-experience.mjs --check"`. Do NOT add the sync to the `build` script — the projects sync is not in `build` either; it stays a manual `pnpm sync:*` + CI drift gate (anti-pattern per 21-RESEARCH.md). Touch ONLY the `scripts` object: the `dependencies` and `devDependencies` blocks must remain byte-identical (SC4 no-new-deps).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['sync:experience']||!p.scripts['sync:experience:check']) process.exit(1); if(Object.keys(p.dependencies).length!==11||Object.keys(p.devDependencies).length!==12) process.exit(2); if(p.scripts.build.includes('sync-experience')) process.exit(3)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts.`sync:experience` === "node scripts/sync-experience.mjs"
    - package.json scripts.`sync:experience:check` === "node scripts/sync-experience.mjs --check"
    - package.json dependencies has exactly 11 keys and devDependencies exactly 12 keys (no new runtime deps, SC4)
    - the `build` script does NOT reference sync-experience (D-11: manual sync + CI gate, not build-time)
  </acceptance_criteria>
  <done>Both npm scripts run the new sync in write/check modes; the dependency graph is unchanged; the sync is not wired into `build`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → sync script | The `source:` frontmatter value is author-controlled and is resolved to a filesystem path the script reads. Build-time only; no network, no auth, no runtime user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information Disclosure | `syncOne` `source:` path resolution in `scripts/sync-experience.mjs` | medium | mitigate | Lift the path-traversal guard verbatim (`!absSource.startsWith(PROJECT_ROOT + sep)` → exit 2). Do not drop during the D-07 strip. Covered by the S3 path-traversal integration test, which asserts BOTH the `escapes project root` message AND the captured `err.status === 2` (review finding #2) — so this exit-2 mitigation claim is actually backed by the test, not merely by the message match. |
| T-21-02 | Tampering | `readSourceField` / `extractFence` frontmatter + fence parsing | medium | mitigate | `readSourceField` rejects mismatched quotes (returns null → exit 2); `extractFence` rejects missing/duplicate/out-of-order markers (exit 2). Covered by the mirrored unit tests. |
| T-21-SC | Tampering | npm installs | n/a | accept | Zero package installs this phase — no third-party code added (SC4). No `[ASSUMED]`/`[SUS]` packages; legitimacy gate N/A. |
</threat_model>

<verification>
- `pnpm test tests/scripts/sync-experience.test.ts tests/scripts/sync-experience-check.test.ts tests/scripts/sync-experience-idempotency.test.ts` is green.
- The full suite (`pnpm test`) still passes (no regression in the projects sync tests — this is an additive parallel script).
- `scripts/sync-experience.mjs` contains no `wordCount`/`checkH2Shape`/`EXPECTED_H2S`/`WORD_TARGET` symbols and one intact path-traversal guard.
- The path-escape test asserts the captured `err.status === 2`, and the idempotency test asserts the second write-mode run leaves mtime + contents unchanged (review findings #1, #2).
</verification>

<success_criteria>
- SC1 (mechanism): the script's pure functions, path-traversal guard (exit 2, status-asserted), `--check` exit-0/exit-1 drift contract, AND write-mode idempotency (second run does not rewrite) are proven by the Wave 0 suite.
- SC3 (ordering): the reusable `sortExperienceEntries()` helper in `src/lib/experience.ts` is unit-tested to return Holloway (2026) before Balfour (2023) — a concrete, Phase-22-consumable ordering contract (finding #3, option a), not a throwaway inline comparator.
- SC4 (no new deps): package.json dependency counts unchanged (11 runtime / 12 dev).
</success_criteria>

## Artifacts this phase produces

New symbols/files created across Phase 21 (for source-grounding drift exclusion):
- `scripts/sync-experience.mjs` (NEW) — exports `normalize`, `readSourceField`, `sliceFrontmatter`, `extractFence`; internal `syncOne`, `main`; constants `MDX_GLOB`, `FENCE_START`, `FENCE_END` (this plan)
- `src/lib/experience.ts` (NEW) — exports `sortExperienceEntries` (reverse-chron ordering helper, SC3 finding #3) — this plan
- `tests/scripts/sync-experience.test.ts` (NEW — this plan)
- `tests/scripts/sync-experience-check.test.ts` (NEW — this plan)
- `tests/scripts/sync-experience-idempotency.test.ts` (NEW — write-mode idempotency, finding #1) — this plan
- `experience` collection + extended `collections` export in `src/content.config.ts` (MODIFY)
- `src/content/experience/holloway.mdx` (NEW)
- `src/content/experience/balfour-beatty.mdx` (NEW)
- `Experience/BALFOUR_BEATTY.md` (NEW)
- `Experience/HOLLOWAY_EXPERIENCE.md` fence markers (MODIFY)
- experience source-existence block added to `tests/content/source-files-exist.test.ts` (MODIFY, finding #5 — owned by 21-03)
- `sync:experience` + `sync:experience:check` scripts in `package.json` (MODIFY)
- "Verify Experience/ <-> MDX sync is clean" step + experience paths in `.github/workflows/sync-check.yml` (MODIFY)
- experience section in `docs/CONTENT-SCHEMA.md` (MODIFY)

<output>
Create `.planning/phases/21-experience-content-pipeline-collection/21-01-SUMMARY.md` when done.
</output>
