---
phase: 25-chat-knowledge-refresh-milestone-verification
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase25-invariants.mjs
  - .planning/phases/25-chat-knowledge-refresh-milestone-verification/25-BASELINE.json
autonomous: true
requirements: [QA-01, QA-02]
must_haves:
  truths:
    - "a Phase-25 invariant baseline (25-BASELINE.json) is captured as the FIRST plan of the phase (Wave 1, plan 00 — with worktrees off, same-wave plans serialize in plan-number order so it runs before the test/copy mutations; and the four gated files + deps are never mutated by any Phase-25 plan, so the captured bytes equal the phase-start bytes), recording the SHA-256 of the four D-14 gated files (BaseLayout.astro, global.css, chat.ts, api/chat.ts) and the normalized package.json dependencies (QA-01/QA-02/D-14)"
    - "scripts/verify-phase25-invariants.mjs exists (Node built-ins only, zero new deps) and its PROTECTED_FILES set is EXACTLY the four gated files — build-chat-context.mjs / about-chat.ts / portfolio-context.json (all legitimately edited this phase) are NOT in the protected set (D-14)"
    - "verify-phase25-invariants.mjs exits 0 immediately after --record (fresh baseline matches current tree) and is the authoritative capstone proof 25-04 runs against committed drift, not a working-tree git diff (QA-01)"
  artifacts:
    - scripts/verify-phase25-invariants.mjs
    - .planning/phases/25-chat-knowledge-refresh-milestone-verification/25-BASELINE.json
  key_links:
    - "this baseline is captured as the first plan of the phase (the four gated files + deps are never mutated by any Phase-25 plan, so it equals the phase-start bytes) so 25-04's capstone hash comparison catches drift COMMITTED in any task of 25-01..25-03, not just uncommitted edits — mirrors the Phase 24 verify-phase24-invariants.mjs / 24-BASELINE.json pattern (review fix #4, Codex mandatory correction #4)"
---

<objective>
Capture the authoritative Phase-25 invariant baseline as the phase's FIRST plan (Wave 1, plan 00 — runs before the test/copy mutations because same-wave plans serialize in plan-number order with worktrees off; and the protected set is never mutated by any Phase-25 plan, so the captured bytes are the phase-start bytes either way), and author the Node-built-ins verifier that 25-04 runs at the capstone. This replaces 25-04's original placeholder `git diff <phase-start-ref>` (which no plan captured and which misses committed drift) with a concrete SHA-256 fingerprint of the four D-14 gated files plus the normalized `package.json` dependencies.

Purpose: QA-01 requires proof that this phase never touched `BaseLayout.astro` / `global.css` / `chat.ts` / `api/chat.ts`, and QA-02 requires proof of zero new runtime dependencies. A working-tree `git diff` at the capstone cannot prove "byte-identical across the whole phase" because a file committed-then-touched in an earlier task looks clean in the final working tree. A phase-start hashed baseline + a deterministic verifier is the Phase 24 pattern that closes this gap.
Output: `scripts/verify-phase25-invariants.mjs` (verify + `--record` modes, dep-free) and a committed `25-BASELINE.json`. 25-04 imports/runs the verifier as the phase-wide untouched proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-RESEARCH.md
@scripts/verify-phase24-invariants.mjs
@package.json
</context>

## Artifacts this phase produces (exclude from drift/source-grounding)

Symbols this plan CREATES (newly-defined this phase — a source-grounding pass must not flag them as undefined):
- `scripts/verify-phase25-invariants.mjs` — a new dep-free Node verifier mirroring `scripts/verify-phase24-invariants.mjs`.
- Exports: `PROTECTED_FILES` (the four D-14 gated files), `sha256File`, `currentDependencies`, `verifyInvariants`, `recordBaseline`.
- `.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-BASELINE.json` — the phase-start fingerprint `{ protected_file_hashes: {relPath: sha256}, dependencies: {...} }`.

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/verify-phase25-invariants.mjs (verify + --record modes, dep-free)</name>
  <files>scripts/verify-phase25-invariants.mjs</files>
  <read_first>
    - scripts/verify-phase24-invariants.mjs (FULL — the analog to clone: PROTECTED_FILES :35-44, sha256File :49-53, currentDependencies :56-59, normDeps + verifyInvariants :76-121, CLI entry :124-135; note the Phase-24 protected set includes files Phase 25 legitimately EDITS — do NOT copy that set verbatim)
    - package.json (the `dependencies` block — the object hashed for QA-02; confirm `scripts` has no wrangler/astro/vitest binding that would need protection)
    - 25-RESEARCH.md §7 "QA-02 dep-lock — how it's verified" (the Phase 24 baseline precedent) + Security Domain
  </read_first>
  <action>
    Create scripts/verify-phase25-invariants.mjs using Node built-ins only (node:fs, node:crypto, node:path, node:url) — zero new dependencies (QA-02). Mirror verify-phase24-invariants.mjs structure with these Phase-25-specific differences:
    - `PROTECTED_FILES` = EXACTLY the four D-14 gated files: "src/layouts/BaseLayout.astro", "src/styles/global.css", "src/scripts/chat.ts", "src/pages/api/chat.ts". Do NOT include about-chat.ts / portfolio-context.static.json / portfolio-context.json / build-chat-context.mjs (the Phase-24 verifier protects those, but Phase 25 deliberately edits them — including them would false-fail the capstone). This narrower set is the entire point of the D-14 discipline for this phase.
    - `BASELINE_PATH` points at .planning/phases/25-chat-knowledge-refresh-milestone-verification/25-BASELINE.json.
    - Keep `sha256File(relPath)`, `currentDependencies()`, and a `normDeps(deps)` that sorts keys before JSON.stringify (so a benign dependency reorder does not false-fail; QA-02 detects ADDED deps, not key order — same rationale as verify-phase24-invariants.mjs:106-111).
    - Add a `recordBaseline()` that writes `{ protected_file_hashes: { <rel>: sha256File(rel) }, dependencies: currentDependencies() }` to BASELINE_PATH (pretty-printed + trailing newline).
    - `verifyInvariants()` returns `{ ok, mismatches }` comparing current protected-file hashes + normalized dependencies against the baseline, with a named mismatch string per drifted file and for a dependencies change (mirror verify-phase24-invariants.mjs:76-121).
    - CLI entry: `--record` calls recordBaseline() and exits 0; default runs verifyInvariants() and exits 1 on any mismatch, 0 when clean (mirror the :124-135 CLI guard, adapted to the record flag).
    Do NOT touch any of the four gated files, package.json, or any src/ runtime file — this task only creates the verifier script.
  </action>
  <verify>
    <automated>node -e "import('./scripts/verify-phase25-invariants.mjs').then(m => { const four = ['src/layouts/BaseLayout.astro','src/styles/global.css','src/scripts/chat.ts','src/pages/api/chat.ts']; if (JSON.stringify([...m.PROTECTED_FILES].sort()) !== JSON.stringify([...four].sort())) throw new Error('PROTECTED_FILES must be exactly the four gated files'); if (m.PROTECTED_FILES.includes('scripts/build-chat-context.mjs')) throw new Error('build-chat-context.mjs must NOT be protected this phase'); console.log('PROTECTED_FILES OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase25-invariants.mjs imports with no error; `PROTECTED_FILES` is exactly the four D-14 gated files and excludes build-chat-context.mjs / about-chat.ts / the portfolio-context JSONs.
    - The module imports only node: built-ins (`grep -c "from \"node:" scripts/verify-phase25-invariants.mjs` >= 3; no non-node import) — QA-02 dep-free.
    - It exports `verifyInvariants`, `recordBaseline`, `PROTECTED_FILES`, `sha256File`, `currentDependencies`.
  </acceptance_criteria>
  <done>verify-phase25-invariants.mjs exists, dep-free, with PROTECTED_FILES = the four gated files and both --record and verify modes.</done>
</task>

<task type="auto">
  <name>Task 2: Record + commit the Phase-25 baseline; sanity-verify exit 0</name>
  <files>.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-BASELINE.json</files>
  <read_first>
    - scripts/verify-phase25-invariants.mjs (the script authored in Task 1)
    - .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json (the analog baseline shape, if present — protected_file_hashes + dependencies keys)
  </read_first>
  <action>
    Run `node scripts/verify-phase25-invariants.mjs --record` to write 25-BASELINE.json capturing the four gated-file SHA-256 hashes + the current normalized dependencies from the clean tree at the start of the phase (with worktrees off this plan-00 runs first in Wave 1; regardless, the four gated files + deps are never mutated by any Phase-25 plan, so these are the phase-start bytes). Then run `node scripts/verify-phase25-invariants.mjs` (verify mode) and confirm it exits 0 (a freshly-recorded baseline must match the current tree). Commit BOTH scripts/verify-phase25-invariants.mjs and 25-BASELINE.json so the capstone (25-04) can compare committed drift. Record the four captured hashes + the dependency count in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/verify-phase25-invariants.mjs --record && node scripts/verify-phase25-invariants.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 25-BASELINE.json exists and parses as JSON with a `protected_file_hashes` object containing all four gated-file paths and a `dependencies` object.
    - `node scripts/verify-phase25-invariants.mjs` exits 0 immediately after --record (no drift on a freshly-recorded baseline).
    - The baseline captured the phase-start bytes of the protected set (which no Phase-25 plan mutates) — noted in the SUMMARY.
  </acceptance_criteria>
  <done>25-BASELINE.json committed with the four gated-file hashes + dependencies; verifier exits 0 against the clean tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-start tree -> committed baseline | the hashed fingerprint is the trust anchor 25-04 compares against; it must be captured before any mutation |
| build tooling -> gated runtime files | the four D-14 gated files (chat surface + SSE) must not change; this baseline is the tamper-evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-00 | Tampering | gated chat-surface files (BaseLayout/global.css/chat.ts/api/chat.ts) | medium | mitigate | Phase-start SHA-256 baseline + capstone verifier catches ANY committed drift across 25-01..25-03, closing the working-tree-diff blind spot the reviewers flagged (Codex mandatory #4). |
| T-25-SC | Tampering (supply chain) | package.json dependencies | low | mitigate | The baseline records the normalized dependencies object; the capstone asserts byte-identity (QA-02 zero-new-dep lock). Zero packages installed this plan; Node built-ins only. |
</threat_model>

<verification>
- `node scripts/verify-phase25-invariants.mjs --record` writes 25-BASELINE.json with four gated-file hashes + dependencies.
- `node scripts/verify-phase25-invariants.mjs` exits 0 against the clean phase-start tree.
- PROTECTED_FILES is exactly the four gated files (not the Phase-24 eight).
</verification>

<success_criteria>
- The verifier + baseline are committed as the phase's first plan, giving 25-04 a deterministic phase-wide untouched proof.
- Zero new runtime dependencies (Node built-ins only, QA-02).
- No gated file, package.json, or src/ runtime file touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-00-SUMMARY.md` when done, recording the four captured gated-file hashes, the dependency count, and confirmation the verifier exits 0 against the phase-start tree.
</output>
