---
phase: 25-chat-knowledge-refresh-milestone-verification
plan: 03
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - src/prompts/portfolio-context-types.ts
  - scripts/build-chat-context.mjs
  - src/data/portfolio-context.json
  - tests/build/parse-education.test.ts
  - tests/build/chat-knowledge-voice.test.ts
  - tests/api/chat-voice-split.test.ts
autonomous: true
requirements: [CHAT-10, CHAT-11, QA-02]
must_haves:
  truths:
    - "build-chat-context.mjs lifts the #7 exclusion at BOTH sites (slug-skip deleted; the existing `const slug` is moved above the reservation regex and reused so there is NO duplicate declaration; the MULTI-DEX regex is scoped with && slug !== 'multi-chain-evm') so #7 enters the corpus with its chatSummary + below-fence extendedReference (D-04)"
    - "the corpus experience field is a reverse-chron array of {role, company, dateRange, summary} read RECURSIVELY from src/content/experience/**/*.mdx (parity with content.config.ts + sync-experience.mjs), replacing the about-derived one-liner (D-09)"
    - "the experience reader validates EVERY schema-required field (role/company/dateRange/chatSummary/startDate) with named errors and fails CLOSED via the existing per-file catch/errorCount/exit-2 mechanism (NOT a raw throw) BEFORE the artifact is written (D-09)"
    - "checkFirstPersonLeaks walks ALL FOUR serialized experience string fields (role, company, dateRange, summary) via a generic object-string walker with experience[i].<field> labels, and hard-fails (exit 2) on first-person leakage (D-09/D-13)"
    - "FIRST_PERSON_LEAK_RE is EXTENDED to catch 'I interned'/'I coordinated' (the real Balfour counterexamples) and the change lands byte-identically across all three sites in this one wave-2 change (build-chat-context.mjs, chat-knowledge-voice.test.ts, chat-voice-split.test.ts) (D-13)"
    - "education is emitted into the merge from education.ts via a dep-free, exported, unit-tested parseEducation (degree/school/graduation May 2026/transferredFrom VT/certifications LPI), single-sourced; parseEducation, parseExperienceEntry, and the source-reservation helper are unit-tested with malformed/missing-field cases (D-07)"
    - "the migration is ATOMIC: the PortfolioContext interface (experience array + extended education, NON-gated), the producer, and the regenerated portfolio-context.json go green together — astro check is asserted only AFTER regeneration, and the artifact is regenerated + committed in the same task (no premature write in a verify step) (D-09/D-14)"
    - "portfolio-context.json is regenerated + committed and build:chat-context:check reports no drift; est_tokens stays under the 60k WARN threshold (D-10)"
  artifacts:
    - src/prompts/portfolio-context-types.ts
    - scripts/build-chat-context.mjs
    - src/data/portfolio-context.json
  key_links:
    - "api/chat.ts consumes the corpus only via JSON.stringify(context) — the array experience shape + emitted education are serialization-safe (no gated-file change)"
---

<objective>
Change the corpus engine so the chat's grounded knowledge includes Holloway, Balfour, and project #7, single-sources education from education.ts, and structures `experience` as a real reverse-chron array — then regenerate and commit `portfolio-context.json` with the leak guard and drift gate green. This is the heart of CHAT-10 and the load-bearing half of CHAT-11.

Purpose: 25-02 authored the content; this plan wires the build to ingest it. Task 1 lands the pure, unit-tested helpers (parseEducation + fail-closed field validators + the #7 reservation predicate); Task 2 is ONE ATOMIC migration (Codex mandatory #2) — the interface flip, the producer edits, and the regenerated artifact go green together, with astro check asserted only AFTER regeneration so the new types are never checked against the stale JSON. The leak-guard walk now sweeps ALL FOUR experience fields and the FIRST_PERSON_LEAK regex is EXTENDED (byte-identical ×3) to catch the real "I interned"/"I coordinated" Balfour counterexamples (Codex HIGH). The corpus regen runs AFTER all content + build-script edits (25-02 locked copy first).
Output: Updated type contract + build script + regenerated corpus + unit-tested helpers. The 25-01 Wave-0 assertions + the two Balfour B1 samples go GREEN here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-RESEARCH.md
@.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-PATTERNS.md
@scripts/build-chat-context.mjs
@scripts/sync-projects.mjs
@src/prompts/portfolio-context-types.ts
@src/data/education.ts
@src/content/experience/holloway.mdx
@src/content/experience/balfour-beatty.mdx
</context>

## Artifacts this phase produces (exclude from drift/source-grounding)

Symbols this plan CREATES (newly-defined this phase — a source-grounding pass must not flag them as undefined):
- `parseEducation(sourceContent)` — dep-free, EXPORTED, unit-tested per-key regex reader of education.ts (mirrors `parseAboutChatExports`); per-key regexes use the `m` flag and are bounded to the EDUCATION/CREDENTIALS export blocks so they never match comments/fragments elsewhere (Antigravity LOW).
- `parseExperienceEntry(frontmatterBlock, slug)` — EXPORTED, unit-tested pure helper that validates every schema-required experience field (role/company/dateRange/chatSummary/startDate) and throws a named error on any missing field.
- `isReservedProjects7Source(sourceRel, slug)` — EXPORTED, unit-tested pure helper: true when `source:` matches the MULTI-DEX regex AND the slug is not `multi-chain-evm` (the belt-and-suspenders #7 reservation).
- `tests/build/parse-education.test.ts` — NEW unit-test file exercising the three helpers above with valid + malformed/missing-field cases.
- `EDUCATION_TS_PATH` + `EXPERIENCE_GLOB` (= `"src/content/experience/**/*.mdx"`, RECURSIVE — parity with content.config.ts:26 + sync-experience.mjs:35) path/glob constants.
- The experience-collection reader loop (globs recursively, reads via `parseExperienceEntry`, fails closed via the existing catch/errorCount/exit-2 mechanism, sorts reverse-chron by startDate DESC).
- The structured `experience` array field: `Array<{ role, company, dateRange, summary }>` replacing the string synthesis.
- The extended `checkFirstPersonLeaks` object-string walker over ALL FOUR experience fields (role, company, dateRange, summary).
- The EXTENDED `FIRST_PERSON_LEAK_RE` (adds `interned` + `coordinated` to the verb alternation) — byte-identical across build-chat-context.mjs, chat-knowledge-voice.test.ts, chat-voice-split.test.ts.
- The emitted `education` object shape: `{ degree, school, graduation, transferredFrom, certifications }`.
- The updated `PortfolioContext.experience` (array) + `PortfolioContext.education` (extended) interface fields.

<tasks>

<!-- planner-discipline-allow: multi-chain-evm") continue -->

<task type="auto" tdd="true">
  <name>Task 1: Pure, exported, unit-tested helpers — parseEducation, parseExperienceEntry, isReservedProjects7Source</name>
  <files>scripts/build-chat-context.mjs, tests/build/parse-education.test.ts</files>
  <behavior>
    - parseEducation(<education.ts source>) returns { degree, school, graduation, transferredFrom, certifications } DEEP-EQUAL to the object derived from the imported EDUCATION/CREDENTIALS exports (degree=EDUCATION.degree, school=EDUCATION.institution, graduation=EDUCATION.date, transferredFrom=EDUCATION.transferredFrom, certifications=CREDENTIALS.map(c=>c.name)).
    - parseEducation on a source with the `date:` key removed throws a NAMED error (mentions education.ts + the missing key), not a silent undefined.
    - parseExperienceEntry(<Holloway frontmatter>, "holloway") returns { role, company, dateRange, summary, startDate } with summary = the chatSummary.
    - parseExperienceEntry on a frontmatter missing chatSummary throws a named error mentioning chatSummary; missing role throws a named error mentioning role.
    - isReservedProjects7Source("Projects/7 - MULTI-DEX CRYPTO TRADER.md", "other-slug") === true; (same source, "multi-chain-evm") === false; ("Projects/1 - SEATWATCH.md", "seatwatch") === false.
  </behavior>
  <read_first>
    - scripts/build-chat-context.mjs (parseAboutChatExports :343-367 as the exported-dep-free-regex analog; readStringField :158-185; the projects catch/errorCount/exit-2 mechanism :509-515; path consts :43-47; the defensive MULTI-DEX regex :466-472 as the reservation predicate source)
    - src/data/education.ts (EDUCATION object literal :25-38 keys degree/institution/date/transferredFrom; CREDENTIALS :40-42 — the SSoT the unit test imports and compares against)
    - src/content.config.ts (experience schema required fields :27-42: role/company/location/startDate/dateRange/summary/highlights + optional chatSummary — parseExperienceEntry validates role/company/dateRange/chatSummary/startDate)
    - src/content/experience/holloway.mdx + balfour-beatty.mdx frontmatter (exact field names + quoting the helper reads)
    - 25-RESEARCH.md §3 (parseEducation mechanism; REJECT --experimental-strip-types) + §5 (Balfour) ; 25-REVIEWS.md Codex 25-03 MEDIUM (fail-closed validation + unit-test the helpers)
  </read_first>
  <action>
    RED first: create tests/build/parse-education.test.ts. Import `parseEducation`, `parseExperienceEntry`, `isReservedProjects7Source` from scripts/build-chat-context.mjs (all undefined initially → RED) and import `EDUCATION`, `CREDENTIALS` from src/data/education.ts. Author the assertions in <behavior> (SSoT deep-equal for education; malformed/missing-field throws for education + experience; the three reservation cases). Read education.ts + the two experience MDX with readFileSync/sliceFrontmatter inside the test.
    GREEN: implement + EXPORT the three helpers in scripts/build-chat-context.mjs, dep-free, reusing imported helpers (do NOT hand-roll frontmatter parsing):
    - `parseEducation(sourceContent)`: add `EDUCATION_TS_PATH = "src/data/education.ts"` near the path consts. Bound the extraction to the `export const EDUCATION = { … } as const;` block and the `export const CREDENTIALS … ];` block (slice the source to those blocks first) so per-key regexes with the `m` flag never match comments or fragments elsewhere (Antigravity LOW). Read degree/institution/date/transferredFrom; scan CREDENTIALS for each `name:`. Throw a named error per missing key. Return { degree, school: institution, graduation: date, transferredFrom, certifications }.
    - `parseExperienceEntry(frontmatterBlock, slug)`: read role/company/dateRange/chatSummary/startDate via readStringField; for EACH missing required field throw `Error(\`${slug}.mdx: missing <field>\`)`. Return { role, company, dateRange, summary: chatSummary, startDate }.
    - `isReservedProjects7Source(sourceRel, slug)`: return `/MULTI[- ]?DEX|multi[- ]?dex/i.test(sourceRel) && slug !== "multi-chain-evm"`.
    Do NOT wire these into main() yet (Task 2 wires + regenerates) — main() is unchanged here, so the committed portfolio-context.json and astro check are unaffected by Task 1.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/parse-education.test.ts --reporter=dot && node scripts/build-chat-context.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - tests/build/parse-education.test.ts passes: parseEducation deep-equals the object derived from imported EDUCATION/CREDENTIALS; malformed education + missing experience fields throw named errors; the three isReservedProjects7Source cases hold.
    - The three helpers are `export`ed from scripts/build-chat-context.mjs.
    - `node scripts/build-chat-context.mjs --check` still exits 0 (helpers added but unused → the artifact is unchanged; no premature regen in this task).
  </acceptance_criteria>
  <done>parseEducation / parseExperienceEntry / isReservedProjects7Source are exported + unit-tested (valid + malformed/missing-field cases); main() and portfolio-context.json unchanged.</done>
</task>


<task type="auto" tdd="true">
  <name>Task 2: ATOMIC engine migration — interface + #7 lift + recursive fail-closed experience reader + 4-field leak-walk + regex extension ×3 + parseEducation wiring + regenerate + commit</name>
  <files>src/prompts/portfolio-context-types.ts, scripts/build-chat-context.mjs, tests/build/chat-knowledge-voice.test.ts, tests/api/chat-voice-split.test.ts, src/data/portfolio-context.json</files>
  <behavior>
    - Given multi-chain-evm.mdx now has a chatSummary and source: Projects/7, the build INCLUDES it as the 7th project block (no throw at either former exclusion site) with extendedReference.truncated === false.
    - Given a second MDX pointing source: at Projects/7 under a different slug, isReservedProjects7Source returns true and the build STILL throws exit 2 (belt-and-suspenders retained).
    - Given the experience collection read recursively, `merged.experience` is a reverse-chron array [Holloway, Balfour] of {role,company,dateRange,summary}; summary = each MDX's chatSummary.
    - Given an experience MDX missing ANY required field (role/company/dateRange/chatSummary/startDate), the build FAILS CLOSED with exit 2 (a named per-file error via the errorCount mechanism) BEFORE writing the artifact.
    - Given a first-person string in ANY of the four experience fields (incl. "I interned"/"I coordinated"), checkFirstPersonLeaks exits 2 — the extended FIRST_PERSON_LEAK_RE now catches those verbs.
    - Given education.ts, `merged.education` = {degree "B.S. Computer Science", school "Western Governors University", graduation "May 2026", transferredFrom "Virginia Tech", certifications ["LPI Linux Essentials"]}.
    - astro check is 0/0/0 and the drift gate is clean ONLY AFTER regeneration (type + producer + artifact go green together).
  </behavior>
  <read_first>
    - scripts/build-chat-context.mjs (FULL — header docstring :11-17; imports :35-39; path consts :43-47; FIRST_PERSON_LEAK_RE :93; checkFirstPersonLeaks targets :104-115 + non-string skip :117-118; the MDX loop; the slug-skip `if (basename(mdxPath, ".mdx") === "multi-chain-evm") continue;` + its D-15 comment; the defensive MULTI-DEX regex; the EXISTING `const slug = basename(mdxPath, ".mdx");` declared LATER at :475 inside the try; seenSources :479-483; the projects catch/errorCount/exit-2 :509-515; the experience synthesis :546-560; the merge :562-570; and the Task-1 helpers parseEducation/parseExperienceEntry/isReservedProjects7Source)
    - src/prompts/portfolio-context-types.ts (education sub-type :17; the experience one-liner doc + `experience: string` :46-50)
    - tests/build/chat-knowledge-voice.test.ts:40 + tests/api/chat-voice-split.test.ts:33 (the two byte-identical FIRST_PERSON_LEAK copies to extend in lockstep with :93) — CHANGE ONLY the regex literal line; leave 25-01's other edits in chat-knowledge-voice.test.ts untouched
    - src/content.config.ts:26 + scripts/sync-experience.mjs:35 (both use `**/*.mdx` — the recursive glob to match) ; src/data/education.ts ; holloway/balfour frontmatter
    - 25-RESEARCH.md §1 (both exclusion sites) + §2 (experience block) + §2c (interface NON-gated) + §3 + §10 (regex triplication) ; 25-REVIEWS.md Codex 25-03 HIGH (task ordering) + MEDIUM (glob/dup-slug/fail-closed/leak-walk) ; 25-PATTERNS.md build-chat-context blocks + Don't Hand-Roll
  </read_first>
  <action>
    This is ONE atomic task by design (Codex mandatory #2): the interface, the producer, and the regenerated artifact must go green together, and astro check is asserted only AFTER regeneration — so the type flip cannot be verified against the stale JSON. Edit dep-free; reuse the Task-1 helpers + imported helpers (do NOT hand-roll frontmatter parsing):
    (A) Interface (§2c, NON-gated): src/prompts/portfolio-context-types.ts — change `experience: string;` (:50) to `experience: Array<{ role: string; company: string; dateRange: string; summary: string }>;` (update the :46-49 doc comment to the reverse-chron role array; drop the "prefer about" note). Extend the education sub-type (:17) to `{ degree: string; school: string; graduation: string; transferredFrom: string; certifications: string[] }`. This file is NOT in the D-14 gated set — no D-26/D-15 trigger.
    (B) Lift #7 exclusion (§1): DELETE the `if (basename(mdxPath, ".mdx") === "multi-chain-evm") continue;` slug-skip AND its D-15 comment block. MOVE the existing `const slug = basename(mdxPath, ".mdx");` declaration (currently at :475, inside the try) UP to just before the defensive MULTI-DEX regex and REUSE that single declaration — there must be exactly ONE `const slug` in the loop body (Codex MEDIUM: a new declaration would collide with the existing one). Replace the blanket throw guard with `if (isReservedProjects7Source(sourceRel, slug))` (the Task-1 helper); keep the reserved-slug error message. The seenSources check (:479-483) remains the independent backstop. Update the header docstring (:11-17) to state #7 is now ingested (Phase 25 / CHAT-10 lifted the skip).
    (C) Recursive fail-closed experience reader (§2 / D-01/D-09, Codex MEDIUM): add `const EXPERIENCE_GLOB = "src/content/experience/**/*.mdx";` (RECURSIVE — parity with content.config.ts:26 + sync-experience.mjs:35, NOT a single-level `*.mdx`). Glob (deterministic sort); for each file `sliceFrontmatter` then call `parseExperienceEntry(frontmatterBlock, slug)` INSIDE a try/catch that on error writes a named `ERROR <file>: <msg>` to stderr and increments an errorCount, then `if (errorCount > 0) process.exit(2)` BEFORE the artifact is written — mirroring the projects loop at :509-515 (a raw `throw` does NOT guarantee exit 2; the catch/count/exit does). Sort entries by startDate DESC (Holloway before Balfour); build `{ role, company, dateRange, summary }` (drop startDate from the emitted object). Do NOT read a below-fence/extendedReference for experience.
    (D) Structured experience field (§2a / D-09): REPLACE the string synthesis at :546-560 with `const experience = experienceEntries;`; keep the same `merged` key (:568); remove the obsolete synthesis comment.
    (E) 4-field leak-walk (§2b / D-09/D-13, Codex MEDIUM): in checkFirstPersonLeaks (:104-115) REPLACE the single `["experience", merged.experience]` entry with an Array.isArray-guarded generic object-string walker that pushes an entry for EACH of role, company, dateRange, AND summary of each array item, labeled `experience[${i}].<field>` (include the company in the label for the summary/role for debuggability), falling back to `[["experience", merged.experience]]` when not an array. The non-string skip (:118) keeps the fallback free.
    (F) Extend the regex (§10 / D-13, Codex HIGH): add `interned` and `coordinated` to the `I\s+(?:…)` verb alternation of FIRST_PERSON_LEAK_RE (:93). Apply the IDENTICAL literal edit to tests/build/chat-knowledge-voice.test.ts:40 and tests/api/chat-voice-split.test.ts:33 so all three stay byte-identical at this single wave-2 change. This flips 25-01's two RED B1 samples ("I interned"/"I coordinated") to GREEN. Change ONLY the regex literal line in the two test files.
    (G) parseEducation wiring (§3 / D-07): read education.ts (EDUCATION_TS_PATH) in main(), build the education object via `parseEducation` (the Task-1 helper), and ADD it to `merged` (:565-570) so it overrides any static key (static.json's education was removed in 25-02, so this is the sole source). REJECT the --experimental-strip-types import path.
    (H) Regenerate + verify IN ORDER: run `pnpm build:chat-context` (write mode) to regenerate portfolio-context.json; THEN `pnpm exec astro check` (now 0/0/0 — the regenerated JSON matches the new types); THEN `pnpm build:chat-context:check` (exit 0, no drift); THEN the quick 4-file chat test set. COMMIT portfolio-context.json + all edited files together. If the leak guard exits 2, fix at the copy SOURCE (25-02's about-chat.ts / the offending chatSummary), never hand-edit portfolio-context.json. Record the new est_tokens (~48-49k per §8) in the SUMMARY.
    Do NOT touch api/chat.ts, chat.ts, BaseLayout.astro, or global.css (D-14).
  </action>
  <verify>
    <automated>pnpm build:chat-context && pnpm exec astro check 2>&1 | tail -3 && pnpm build:chat-context:check && pnpm exec vitest run tests/build/chat-context-integrity.test.ts tests/build/chat-knowledge-voice.test.ts tests/api/chat-voice-split.test.ts tests/api/prompt-injection.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build:chat-context` regenerates portfolio-context.json exit 0 (no leak, no hard-fail); stdout shows `projects=7` with multi-chain-evm in the breakdown; est_tokens under 60000 (WARN) and above 4096 (D-10).
    - `grep -c 'multi-chain-evm") continue' scripts/build-chat-context.mjs` == 0 (slug-skip removed); the build runs with NO duplicate-declaration SyntaxError — the existing loop-body slug binding is MOVED above the reservation regex and REUSED (not re-declared, so the main() MDX loop declares it exactly once); the reservation guard calls isReservedProjects7Source.
    - EXPERIENCE_GLOB is the recursive `src/content/experience/**/*.mdx`; the reader fails closed (exit 2) on a missing required field via the errorCount mechanism (proven by the Task-1 parseExperienceEntry unit test + a manual missing-field spot check).
    - FIRST_PERSON_LEAK_RE on build-chat-context.mjs:93 is byte-identical to chat-knowledge-voice.test.ts:40 and chat-voice-split.test.ts:33 AND now matches "I interned"/"I coordinated" (25-01's two B1 samples are GREEN).
    - AFTER regen: `pnpm exec astro check` 0/0/0; `pnpm build:chat-context:check` exit 0; the 25-01 retargets (7 slugs, SSoT education, full-#7-untruncated, skills-presence, 4-field walk, #7-ban-absent, `<security>` snapshot, count-7, #7 groundedQA) are GREEN. Committed portfolio-context.json has `experience` as a 2-entry array (Holloway first) + extended education.
  </acceptance_criteria>
  <done>the interface + producer + regenerated corpus land atomically green: #7 ingested (projects=7, untruncated), recursive fail-closed experience array, 4-field leak-walk, regex extended byte-identical ×3, education single-sourced; astro check + drift clean AFTER regen; corpus committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build sources -> cached `<knowledge>` | corpus content (now incl. #7 + experience array + education) becomes the model-visible cached system block |
| visitor -> chat API | api/chat.ts serializes the corpus by value only (JSON.stringify) — no code change; the array shape is serialization-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-02 | Tampering (voice break) | structured experience array + regenerated corpus | medium | mitigate | The 4-field leak-walk lands WITH the structured-experience change (same atomic task) so the guard reaches EVERY serialized field of every entry; FIRST_PERSON_LEAK_RE is EXTENDED to catch interned/coordinated and stays byte-identical across all three sites (D-13); the recursive reader fails CLOSED (exit 2) on any missing required field; `pnpm build:chat-context` exit 2 hard-fails the build on any leak. |
| T-25-01 | Information disclosure | corpus as injected knowledge | medium | mitigate | The #7 content is public portfolio material; api/chat.ts + system-prompt `<security>` defenses are untouched here (D-14). 25-04 re-runs the prompt-injection battery + D-26/D-15 gates. |
| T-25-04 | Denial of service (token blowup) | corpus token budget | low | accept | D-10: accept growth to ~48-49k est_tokens; existing INFO/WARN/CAP observability prints it; stays under WARN (60k) and above the 4,096 Haiku cache floor. Revisit only if a future estimate crosses WARN. |
| T-25-SC | Tampering (supply chain) | npm installs | low | accept | Zero packages installed (QA-02); parseEducation + the experience reader are dep-free Node using already-imported helpers. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/build/parse-education.test.ts` GREEN (helpers unit-tested: SSoT education + malformed/missing-field fail-closed + reservation predicate).
- `pnpm build:chat-context` regenerates + exits 0 with projects=7 and no first-person leak; est_tokens recorded (under WARN).
- Order matters: `pnpm exec astro check` 0/0/0 is asserted AFTER regeneration (not before); `pnpm build:chat-context:check` exits 0 (committed corpus == fresh regen).
- `pnpm exec vitest run tests/build/chat-context-integrity.test.ts tests/build/chat-knowledge-voice.test.ts tests/api/chat-voice-split.test.ts tests/api/prompt-injection.test.ts` — the 25-01 retargets + the two Balfour B1 samples flip GREEN here.
- The EXTENDED FIRST_PERSON_LEAK_RE (now matching interned/coordinated) is byte-identical across build-chat-context.mjs:93, chat-knowledge-voice.test.ts:40, chat-voice-split.test.ts:33.
</verification>

<success_criteria>
- Corpus regenerated + committed with 7 projects (incl. untruncated #7), a recursive fail-closed reverse-chron experience array, and education single-sourced from education.ts; drift gate clean; astro check 0/0/0 AFTER regen.
- Only NON-gated files touched (portfolio-context-types.ts, build-chat-context.mjs, the generated portfolio-context.json, and the three test files parse-education.test.ts / chat-knowledge-voice.test.ts / chat-voice-split.test.ts — tests are permitted by D-14) — D-26/D-15 do not trigger.
- FIRST_PERSON_LEAK_RE extended byte-identical across all three sites; parseEducation/parseExperienceEntry/isReservedProjects7Source exported + unit-tested.
- Zero new runtime dependencies (all edits dep-free).
</success_criteria>

<output>
Create `.planning/phases/25-chat-knowledge-refresh-milestone-verification/25-03-SUMMARY.md` when done, recording the new est_tokens, the projects=7 breakdown, and confirmation the 25-01 retargets flipped GREEN.
</output>
