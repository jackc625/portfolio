---
phase: 24-positioning-shift-home-teaser
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/pages/index.astro
  - src/components/ContactSection.astro
  - tests/build/home-teaser-render.test.ts
autonomous: true
requirements: [HOME-01, POS-01, POS-04]
must_haves:
  truths:
    - "The Home page opens with a concise Holloway EXPERIENCE teaser (role/company/dates + one metric + a link to /experience) for the 30-second recruiter scan (HOME-01, D-01..D-05)"
    - "Home section numbers read 01 EXPERIENCE / 02 WORK / 03 ABOUT / 04 CONTACT, and ContactSection.astro is fully em-dash-clean (comments included) so the Gate A whole-source scan cannot fail for an unplanned reason (D-01; review fix #3)"
    - "The teaser selects the Holloway entry by an explicit id === 'holloway' guard (mirroring experience.astro), not merely the first hasCaseStudy entry (review fix #6)"
    - "The Home JSON-LD Person schema carries jobTitle + alumniOf + hasCredential and the SEO description signals production/contract experience (POS-04, D-14, D-15)"
    - "The Home hero lead ('Software engineer building reliable, production-grade systems.') stays UNCHANGED (D-08) -- asserted by an exact-match Gate B DOM regression check on `.hero-lead` (WARNING 1) -- and the sharpened Home description= (D-15) is a DISTINCT string from it"
    - "Gate B/C (home-teaser-render) is AUTHORED in this plan and GREEN at this plan's boundary, so every task <verify> exits 0 (review fix #1)"
  artifacts:
    - src/pages/index.astro
    - src/components/ContactSection.astro
    - tests/build/home-teaser-render.test.ts
  key_links:
    - "index.astro imports alumniOfSchema + hasCredentialSchema from src/data/education.ts (24-01) and spreads them into personSchema rendered via JsonLd.astro"
    - "The teaser reuses getCollection('experience') + sortExperienceEntries + the id==='holloway' guard -- no data duplication (D-05); grep -c 'sortExperienceEntries' on index.astro is 2 (one import + one call, exactly like experience.astro:6,10) -- NOT 1 (review fix #2)"
    - "The CONTACT §-number lives as a hardcoded literal in ContactSection.astro:28, NOT a SectionHeader prop (RESEARCH Pitfall 3); its three doc comments carrying U+2014 are stripped this plan (review fix #3)"
    - "Gate B/C (tests/build/home-teaser-render.test.ts) is authored HERE (moved from 24-01 per review fix #1)"
---

<objective>
Rebuild the Home page for the positioning shift: add the new `01 EXPERIENCE` Holloway teaser placed FIRST (HOME-01), renumber the existing section headers, enrich the JSON-LD Person schema, and sharpen the SEO description -- then fix the CONTACT §-number literal (and strip its em-dash comments) so the whole Home sequence reads 01/02/03/04 and ContactSection.astro is em-dash-clean. Finally author the Gate B/C render test HERE, where the implementation makes it GREEN.

Purpose: put the strongest new-grad credibility signal (shipped production experience) at the top of the 30-second scan and make the metadata reflect the software-engineer-with-production-experience positioning (POS-01, POS-04). Every task <verify> exits 0 at its own boundary (review fix #1): the teaser and metadata edits are self-checked by source greps, and Gate B/C is authored and run GREEN only after all Home implementation is in place.

Output: a modified `src/pages/index.astro` (teaser section + renumbered WORK/ABOUT headers + enriched `personSchema` + sharpened `description`), a renumbered + em-dash-stripped `src/components/ContactSection.astro`, and a new GREEN `tests/build/home-teaser-render.test.ts` (Gates B + C).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md
@.planning/phases/24-positioning-shift-home-teaser/24-PATTERNS.md
@.planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md

# The teaser structure + reduced-motion + id-guard + render-gate analogs
@src/pages/experience.astro
@src/content/experience/holloway.mdx
@src/components/JsonLd.astro
@src/components/ContactSection.astro
@tests/build/featured-tier-render.test.ts
@design-system/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design + build the 01 EXPERIENCE Home teaser (frontend-design routed, D-20)</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md §"Teaser internal structure" (lines 72-86) and §Copywriting Contract (the teaser summary + metric drafts, lines 190-196) and §Motion (line 227)
    - design-system/MASTER.md (six-token palette, type-role classes, page-scoped CSS, §7 accent-signal rule, restrained-motion contract) -- the D-20 contract the frontend-design skill validates against
    - src/pages/experience.astro (lines 6, 10-37 = collection query + hasCaseStudy find guard + the id==='holloway' assertion to reuse verbatim with an index.astro throw message; lines 51-69 = the `.featured` article structure to mirror trimmed; lines 141-169 + 222-233 = the `.deep-link` reduced-motion contract)
    - src/pages/index.astro (lines 2-16 frontmatter imports; lines 72-75 + 114-129 = the in-file `.see-all-work` deep-link idiom + reduced-motion CSS to clone under a teaser-distinct selector)
    - src/content/experience/holloway.mdx (frontmatter role/company/dateRange/summary + highlights[0] the teaser draws from)
  </read_first>
  <action>
    First route the teaser's visual form through the frontend-design skill against design-system/MASTER.md and the UI-SPEC teaser table -- confirm the compact `.featured` mirror (eyebrow / title / summary / ONE highlight / deep-link), the six-token palette, the accent reserved only for the link (MASTER §7), and the reduced-motion contract before writing CSS. Then in src/pages/index.astro add a new `<section>` placed FIRST (above the existing WORK section), labelled with `<SectionHeader number="01" title="EXPERIENCE" id="section-experience" />` and NO count prop. In the frontmatter, reuse the experience collection query: import sortExperienceEntries from ../lib/experience, run `sortExperienceEntries(await getCollection("experience"))`, then reuse the experience.astro guard set VERBATIM adapted to index.astro (review fix #6): find the entry where hasCaseStudy is true, throw an index.astro-prefixed Error if none, AND throw an index.astro-prefixed Error if `holloway.id !== "holloway"` -- so the teaser can never point at a route the build did not generate. Do NOT inline a `.sort()` (sortExperienceEntries orders by startDate desc; anti-pattern per RESEARCH line 202). Render the teaser article as a trimmed clone of experience.astro's `.featured`: eyebrow `{role} · {dateRange}` (.meta-mono, --ink-muted), title `{company}` (.h2-project, --ink), OMIT the tech-stack line (D-05), summary (.lead, --ink) using the UI-SPEC teaser sentence draft, and exactly ONE metric highlight (.body) carrying the `1,400` test-suite-growth metric (D-02/D-03) -- either holloway.data.highlights[0] or the trimmed UI-SPEC draft; both are covered by Gate A. Add the link `See the experience →` targeting `href="/experience"` (the listing, D-04), cloning the in-file `.see-all-work` markup + CSS under a teaser-distinct selector (e.g. `.experience-link` / `.experience-arrow`) so it does not collide with the WORK section's `.see-all-work`; keep the color-change affordance + arrow-opacity reveal + focus ring `outline: 2px solid var(--accent); outline-offset: 2px` and the `prefers-reduced-motion` override that drops the reveal but keeps the color change. All teaser CSS is page-scoped in index.astro's `<style>` -- do NOT touch global.css or BaseLayout.astro (D-19). Keep every teaser string em-dash-free (U+2014); en dash permitted (the reused dateRange is `May 2026 – Present`).
  </action>
  <verify>
    <automated>pnpm build && test "$(grep -c sortExperienceEntries src/pages/index.astro)" -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 with the new EXPERIENCE section in place (the render gate itself is authored in Task 3; this task's build proves the teaser compiles + the id guard does not throw)
    - `grep -c "sortExperienceEntries" src/pages/index.astro` returns 2 -- exactly one import line and one call line, mirroring experience.astro:6 and :10 (review fix #2: the earlier "must equal 1" criterion was mathematically impossible)
    - index.astro contains the hasCaseStudy find guard AND the `holloway.id !== "holloway"` throw guard (review fix #6); no inline `.sort()` on experience
    - The teaser contains `<a href="/experience">` (exactly the listing route, not /experience/holloway) and the `1,400` metric substring; the tech-stack line is OMITTED (D-05)
    - index.astro `<style>` contains a `prefers-reduced-motion: reduce` block for the teaser link that keeps the color change and drops the arrow opacity reveal
    - `grep -c $'—' src/pages/index.astro` returns 0 (no em dash in the new teaser strings; self-check for the Gate A scope that lands in 24-04)
    - No edit was made to src/styles/global.css or src/layouts/BaseLayout.astro (D-19)
  </acceptance_criteria>
  <done>Home opens with the frontend-design-routed 01 EXPERIENCE teaser sourced from the live collection via the id-guarded query, linking to /experience with the 1,400 metric; the build compiles and the source self-checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Renumber Home headers + enrich personSchema + sharpen SEO description + fix ContactSection (renumber + strip em dashes)</name>
  <files>src/pages/index.astro, src/components/ContactSection.astro</files>
  <read_first>
    - src/pages/index.astro (line 60 WORK `number="01"`; line 82 ABOUT `number="02"`; lines 21-32 the current personSchema object; line 35 the BaseLayout `description=` prop; line 12 the about import)
    - src/components/ContactSection.astro (line 28 the hardcoded `&sect; 03 &middot; CONTACT` literal -- NOT a SectionHeader prop; AND the U+2014 em dashes in the doc comments at lines 3, 5, and 9 that review fix #3 requires stripping)
    - src/data/education.ts (the alumniOfSchema + hasCredentialSchema exports to import and spread -- created in 24-01)
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md §Layout renumber table (lines 62-67) + the sharpened SEO description draft (line 210)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Pattern 3 (personSchema enrichment, lines 182-199) and Pitfall 3 (the CONTACT literal, lines 233-237)
  </read_first>
  <action>
    In src/pages/index.astro renumber the two SectionHeader props: WORK `number="01"` becomes `number="02"` (keep its existing count prop) and ABOUT `number="02"` becomes `number="03"` -- prop-value edits only; do NOT edit SectionHeader.astro. Enrich the personSchema object (lines 21-32): import alumniOfSchema and hasCredentialSchema from ../data/education, add `jobTitle: "Software Engineer"` (D-07/D-14), and add `alumniOf: alumniOfSchema` + `hasCredential: hasCredentialSchema` (D-14); leave the render path unchanged (`<JsonLd schema={personSchema} />`) -- do NOT hand-roll a `<script>` tag (V5.3). Sharpen the BaseLayout `description=` prop on the Home page to the D-15 draft signalling production/contract experience (UI-SPEC line 210) -- this is a per-page prop, no BaseLayout.astro edit; keep it em-dash-free (U+2014) and DISTINCT from the unchanged hero lead (D-08). Finally, in src/components/ContactSection.astro (i) edit the hardcoded section-header literal at line 28 from `&sect; 03 &middot; CONTACT` to `&sect; 04 &middot; CONTACT` (D-01); (ii) update the two `§ 03` doc-comment references (lines 3 and 9) to `§ 04`; and (iii) STRIP every U+2014 em dash from the three doc comments at lines 3, 5, and 9, replacing each with an en dash, a hyphen, or a restructured phrase (review fix #3 -- these em dashes would otherwise fail the whole-source Gate A scan that 24-04 authors over ContactSection.astro; this matches the known project voice-gate scope gap where em dashes in non-MDX source slip past the MDX-only checks). This literal renders only on Home (showSectionHeader default false on /contact), so the number change is Home-only in effect.
  </action>
  <verify>
    <automated>pnpm build && test "$(grep -c '—' src/components/ContactSection.astro)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c $'—' src/components/ContactSection.astro` returns 0 (all three comment em dashes stripped -- review fix #3) and `grep -c $'—' src/pages/index.astro` returns 0
    - `grep -c "&sect; 04" src/components/ContactSection.astro` returns 1 and `grep -c "&sect; 03" src/components/ContactSection.astro` returns 0 (literal + both comment refs renumbered)
    - `grep -c "jobTitle" src/pages/index.astro` returns 1 and index.astro imports alumniOfSchema + hasCredentialSchema from ../data/education
    - index.astro still contains the hero `.lead` string "Software engineer building reliable, production-grade systems." UNCHANGED (D-08)
    - The Home `description=` prop text differs from the current placeholder, is a DISTINCT string from the hero lead (D-15), and contains zero U+2014
    - `pnpm build` exits 0; `pnpm exec astro check` stays 0/0/0
  </acceptance_criteria>
  <done>Home headers read 01/02/03/04, personSchema carries jobTitle + alumniOf + hasCredential, the SEO description is sharpened, and ContactSection is renumbered AND fully em-dash-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Author Gate B/C (home-teaser-render) -- GREEN at this boundary</name>
  <files>tests/build/home-teaser-render.test.ts</files>
  <read_first>
    - tests/build/featured-tier-render.test.ts (clone the `// @vitest-environment jsdom` header, `existsSync` dist guard + "run `pnpm build` first" message, and the DOMParser idiom)
    - src/components/primitives/SectionHeader.astro (renders `§ {number} · {title}` -- the exact label shape Gate B asserts)
    - src/components/JsonLd.astro (the escaping serializer -- Gate C must JSON.parse the script textContent, NOT substring-match; the `<`→`<` escapes are valid JSON, per RESEARCH Pitfall 5)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Gate B/C specs (lines 395-403)
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md §"Teaser internal structure" (the one-highlight / no-stack / eyebrow contract Gate B asserts)
  </read_first>
  <action>
    Create tests/build/home-teaser-render.test.ts as a jsdom render gate (`// @vitest-environment jsdom`; read dist/client/index.html; `existsSync` guard that surfaces "run `pnpm build` first"). Author it AFTER Tasks 1-2 so it is GREEN at this boundary (review fix #1). Assertions:

    Gate B (section numbering + teaser contract, strengthened per review fix #6): select the section-label spans (`.section-header .label-mono`, covering both SectionHeader output and the ContactSection literal), map trimmed/whitespace-normalized textContent, and assert the exact ordered sequence `["§ 01 · EXPERIENCE", "§ 02 · WORK", "§ 03 · ABOUT", "§ 04 · CONTACT"]` (D-01). Scope the remaining teaser assertions to `section[aria-labelledby="section-experience"]`: assert exactly ONE highlight line (`.teaser-highlight`, or the executor's chosen single-highlight class) inside that section; assert the eyebrow carries role + dates and the title carries the company (role/company/dates present); assert ZERO tech-stack elements render inside the section (no `.featured-stack` / stack line -- D-05); and assert an `<a href="/experience">` (exactly the listing route, NOT /experience/holloway -- D-04) whose text or a sibling contains the metric substring `1,400` (D-03).

    Gate C (JSON-LD, same file): select `script[type="application/ld+json"]`, `JSON.parse` its textContent (do NOT substring-match the escaped bytes), and assert `@type === "Person"`, `jobTitle === "Software Engineer"`, `alumniOf` names include "Western Governors University" AND "Virginia Tech", `hasCredential` includes an entry named "LPI Linux Essentials", and NO hasCredential entry references "Virginia Tech" (D-10).

    Also assert the rendered `<meta name="description">` (review fix #6): its content is non-empty, contains zero U+2014, and is NOT byte-identical to the hero lead "Software engineer building reliable, production-grade systems." (D-08/D-15 -- the sharpened description is distinct).

    Additionally assert the D-08 hero-lead INVARIANT (WARNING 1): select the hero lead element `.hero-lead` and assert its trimmed textContent equals EXACTLY "Software engineer building reliable, production-grade systems." -- a persistent automated regression guard so any future accidental edit to that LOCKED line trips this gate, not merely the distinct-from-description check. (Confirm the exact hero-lead string is unchanged in src/pages/index.astro line 47 before relying on it; a boundary-safe source alternative is `test "$(grep -c 'Software engineer building reliable, production-grade systems.' src/pages/index.astro)" -ge 1`.)
  </action>
  <verify>
    <automated>pnpm build && pnpm exec vitest run tests/build/home-teaser-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm exec vitest run tests/build/home-teaser-render.test.ts` exits 0 (Gates B + C GREEN at this boundary)
    - Gate B asserts the four-item ordered label sequence ending `§ 04 · CONTACT`, exactly one teaser highlight, zero stack elements in the experience section, role/company/dates present, and the `<a href="/experience">` + `1,400` substring
    - Gate C JSON.parses the ld+json and asserts Person / jobTitle / alumniOf (WGU+VT) / hasCredential (LPI) / VT-excluded-from-hasCredential
    - The rendered `<meta name="description">` is asserted present, em-dash-free, and distinct from the hero lead (review fix #6)
    - Gate B asserts the hero `.hero-lead` element's trimmed textContent equals EXACTLY "Software engineer building reliable, production-grade systems." -- an automated regression guard for the LOCKED D-08 hero-lead invariant (WARNING 1)
    - With no fresh dist/ present the gate degrades cleanly via the existsSync guard (no opaque file-read error)
  </acceptance_criteria>
  <done>Gate B/C authored HERE and GREEN; it proves the numbering, the concise-teaser contract (one highlight, no stack, role/company/dates, /experience link + 1,400), the enriched JSON-LD, the distinct rendered meta description, and the UNCHANGED D-08 hero lead (WARNING 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time collection -> static HTML | Holloway teaser fields come from the content collection at build; no runtime input |
| education fragments -> `ld+json` script | schema data reaches a `<script type="application/ld+json">` via JsonLd.astro |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02 | Tampering (XSS) | personSchema -> JsonLd.astro | low | mitigate | All schema data flows through the existing JsonLd.astro serializer that escapes `<`/`>`/`&`/U+2028/U+2029; this plan spreads author-controlled constants and does not hand-roll a script tag (V5.3 output encoding) |
| T-24-03 | Tampering (invariant break) | chat-surface via BaseLayout.astro / global.css | low | mitigate | D-19: all teaser + schema + description edits are page-scoped in index.astro / a per-page prop / ContactSection.astro; Gate E asserts BaseLayout anchors intact and the 24-04 baseline hash comparison proves BaseLayout/global.css unchanged across the phase (review fix #4) |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero package installs (D-19); dep-lock gate at 24-04 asserts package.json dependencies byte-identical to the baseline |
</threat_model>

<verification>
- `pnpm build` succeeds; `pnpm exec vitest run tests/build/home-teaser-render.test.ts` exits 0 (Gates B + C GREEN, authored this plan)
- `grep -c $'—' src/pages/index.astro` and `grep -c $'—' src/components/ContactSection.astro` both return 0 (self-check for the 24-04 Gate A scope)
- `grep -c "sortExperienceEntries" src/pages/index.astro` returns 2 (import + call; review fix #2)
- `pnpm exec astro check` is 0/0/0
- No change to src/layouts/BaseLayout.astro or src/styles/global.css (D-19; proved phase-wide by the 24-04 baseline comparison)
</verification>

<success_criteria>
- Home surfaces a concise Holloway teaser linking to /experience via the id-guarded query (HOME-01, SC5)
- Home reads 01 EXPERIENCE / 02 WORK / 03 ABOUT / 04 CONTACT; ContactSection is em-dash-clean (D-01, review fix #3)
- JSON-LD Person schema reflects the job title + education; SEO description reflects the positioning and is distinct from the hero lead (POS-04, SC4)
- Gate B/C authored HERE and GREEN; no new runtime dependencies; no chat-surface file touched
</success_criteria>

## Review Findings Addressed (Phase 24 cross-AI review, codex gpt-5.6-sol)

- **#1 (HIGH) Task 1 runs a test file that includes Task 2 requirements**: Gate B/C is now AUTHORED in this plan (Task 3), after the teaser + numbering + schema land, so it is GREEN at its boundary. Tasks 1-2 self-verify via source greps + `pnpm build`, not the full render gate.
- **#2 (HIGH) impossible `grep -c "sortExperienceEntries"` == 1**: corrected to expect 2 (one import line + one call line, matching experience.astro:6,10).
- **#3 (HIGH) ContactSection.astro em-dash assumption**: Task 2 now explicitly strips U+2014 from the three ContactSection comments (lines 3/5/9) while renumbering, so the whole-source Gate A scan (24-04) passes for the intended reason.
- **#6 (MEDIUM) teaser query has no ID guard**: Task 1 reuses the experience.astro `holloway.id !== "holloway"` throw guard, not merely the first hasCaseStudy entry.
- **#6 (MEDIUM) Gate B under-tests the concise-teaser contract**: Gate B now asserts exactly one highlight, role/company/dates, zero stack elements inside `section[aria-labelledby="section-experience"]`, plus a rendered `<meta name="description">` assertion (present, em-dash-free, distinct from the hero lead).

## Artifacts this phase produces

New symbols introduced by THIS plan (exclude from source-drift verification):
- The Home `#section-experience` teaser section and its page-scoped classes (`.experience-teaser` / `.featured-teaser` / teaser eyebrow/title/summary/highlight + `.experience-link` / `.experience-arrow`, final names at executor discretion per frontend-design)
- New `personSchema` fields: `jobTitle`, `alumniOf`, `hasCredential`
- `tests/build/home-teaser-render.test.ts` (Gates B + C; moved here from 24-01 per review fix #1)
- Consumes (created in 24-01): `alumniOfSchema`, `hasCredentialSchema` from `src/data/education.ts`

<output>
Create `.planning/phases/24-positioning-shift-home-teaser/24-02-SUMMARY.md` when done.
</output>
