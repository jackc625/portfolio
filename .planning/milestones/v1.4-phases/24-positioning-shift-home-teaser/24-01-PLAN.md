---
phase: 24-positioning-shift-home-teaser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/education.ts
  - tests/content/education-module.test.ts
  - tests/build/chat-surface-untouched.test.ts
  - scripts/verify-phase24-invariants.mjs
  - .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json
autonomous: true
requirements: [POS-03, POS-04]
must_haves:
  truths:
    - "src/data/education.ts is the single source of truth for education display facts AND the JSON-LD schema fragments, with alumniOfSchema + hasCredentialSchema DERIVED from EDUCATION + CREDENTIALS (no re-hardcoded WGU/VT strings) so display and schema can never drift (POS-03, POS-04, D-11; review fix: LOW single-source)"
    - "Gate D (education-module) and Gate E (chat-surface-untouched, strengthened) are GREEN at this plan's boundary; every task <verify> exits 0 (review fix #1: no intentionally-RED task-boundary gate is authored here)"
    - "A phase-start baseline (SHA-256 hashes of all D-19/D-17 protected files + the package.json dependencies object + the og-default.png placeholder hash + the phase-base commit SHA) is captured so the 24-04 capstone can prove phase-wide invariants across committed tasks, not only the working tree (review fix #4)"
  artifacts:
    - src/data/education.ts
    - tests/content/education-module.test.ts
    - tests/build/chat-surface-untouched.test.ts
    - scripts/verify-phase24-invariants.mjs
    - .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json
  key_links:
    - "education.ts exports alumniOfSchema + hasCredentialSchema (DERIVED from EDUCATION/CREDENTIALS) consumed by index.astro personSchema (24-02); EDUCATION + CREDENTIALS consumed by about.astro block (24-03)"
    - "24-BASELINE.json + scripts/verify-phase24-invariants.mjs are consumed by the 24-04 capstone to compare protected-file hashes and the dependencies object against phase start (review fix #4)"
    - "Gate A (site-copy em-dash/register) is authored in 24-04 and Gate B/C (home-teaser-render) in 24-02 -- the plans that make them GREEN -- so no gate is left intentionally RED at a task boundary (review fix #1)"
---

<objective>
Establish the Phase 24 foundation: the shared `src/data/education.ts` module (education facts + schema.org fragments DERIVED from those facts), the two validation gates that are GREEN at this plan's own boundary (Gate D education-module + Gate E chat-surface tripwire), and the phase-start invariant baseline that the capstone (24-04) compares against.

Purpose: education.ts is the single source of truth (D-11) that both the `/about` education block (24-03) and the Home `personSchema` JSON-LD (24-02) read, preventing drift. This plan owns ONLY gates that pass at their own task boundary -- the executor treats every `<verify>` as a hard gate (execute-plan.md), so a task that leaves automated verification RED cannot complete. Per the cross-AI review (finding #1), the two gates whose GREEN state depends on later plans are authored in the plan that makes them green: Gate A (site-copy em-dash/register, scans files owned by BOTH 24-02 and 24-03) is authored in the 24-04 capstone; Gate B/C (home-teaser-render) is authored in 24-02. This plan additionally captures the phase-start baseline (protected-file hashes + dependencies + og placeholder hash + phase-base SHA) so the capstone can prove the D-19/D-17 "untouched across the phase" invariants across committed tasks, not just the working tree (finding #4).

Output: 1 new data module (schema derived, not re-hardcoded) + 1 GREEN unit gate (Gate D) + 1 GREEN strengthened source tripwire (Gate E) + 1 committed baseline JSON + 1 phase-scoped invariant-verify script. No intentionally-RED gate is authored here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md
@.planning/phases/24-positioning-shift-home-teaser/24-VALIDATION.md
@.planning/phases/24-positioning-shift-home-teaser/24-PATTERNS.md

# Analog source for the module shape and the test idioms
@src/data/contact.ts
@src/layouts/BaseLayout.astro
@tests/content/experience-voice-em-dash.test.ts
@tests/build/featured-tier-render.test.ts
@tests/client/about-data.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/data/education.ts (facts + DERIVED schema fragments) + Gate D unit test</name>
  <files>src/data/education.ts, tests/content/education-module.test.ts</files>
  <read_first>
    - src/data/contact.ts (the data-module SSoT convention to clone: doc header naming consumers, typed `as const`, voice-neutral facts, zero em dashes)
    - src/data/about.ts (existing escape convention: curly quotes, non-breaking space, en dash for date ranges, zero U+2014)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Pattern 2 (lines 135-180 -- the recommended module shape) and Assumptions A1/A2 (schema field caveats)
    - tests/client/about-data.test.ts (the import-and-assert unit-gate analog for Gate D)
  </read_first>
  <behavior>
    - EDUCATION.degree === "B.S. Computer Science"; EDUCATION.institution === "Western Governors University"; EDUCATION.date === "May 2026"; EDUCATION.transferredFrom === "Virginia Tech"; EDUCATION.degreeSchemaName === "Bachelor of Science in Computer Science"; EDUCATION.dateISO === "2026-05"
    - CREDENTIALS contains an entry named "LPI Linux Essentials" with issuer "Linux Professional Institute"
    - DERIVATION (review fix, LOW single-source): alumniOfSchema[0].name === EDUCATION.institution and one alumniOfSchema entry name === EDUCATION.transferredFrom (fragments read from EDUCATION/CREDENTIALS; they do NOT re-hardcode the WGU/VT literals)
    - alumniOfSchema DOES include EDUCATION.transferredFrom (VT; attended = honest per D-10) and EDUCATION.institution (WGU)
    - hasCredentialSchema: every entry @type === "EducationalOccupationalCredential"; the degree entry name === EDUCATION.degreeSchemaName, recognizedBy.name === EDUCATION.institution, validFrom === EDUCATION.dateISO; NO entry recognizedBy.name equals EDUCATION.transferredFrom (VT is not a credential -- D-10); cert entries map 1:1 from CREDENTIALS
  </behavior>
  <action>
    Create src/data/education.ts following the src/data/contact.ts SSoT convention: a doc header naming its consumers (about.astro education block [24-03], index.astro personSchema [24-02], and the deferred Phase 25 chat consumer), then typed exports. Shape: a `Credential` interface (`name: string; issuer?: string`); an `EDUCATION` object marked `as const` carrying the DISPLAY facts (degree "B.S. Computer Science", institution "Western Governors University", date "May 2026", transferredFrom "Virginia Tech") PLUS two schema-only fields so the fragments derive from one source without expanding abbreviations at read time: degreeSchemaName "Bachelor of Science in Computer Science" and dateISO "2026-05"; a `CREDENTIALS: Credential[]` array holding one entry name "LPI Linux Essentials" issuer "Linux Professional Institute". Then build the two schema.org fragments BY DERIVING them from EDUCATION and CREDENTIALS (review fix, LOW single-source -- do NOT paste "Western Governors University"/"Virginia Tech" literals a second time): `alumniOfSchema` = CollegeOrUniversity objects whose names read `EDUCATION.institution` and `EDUCATION.transferredFrom`; `hasCredentialSchema` = an EducationalOccupationalCredential of credentialCategory "degree" whose name reads `EDUCATION.degreeSchemaName`, recognizedBy Organization name `EDUCATION.institution`, validFrom `EDUCATION.dateISO`, followed by `...CREDENTIALS.map(...)` certificate entries (credentialCategory "certificate", name c.name, recognizedBy c.issuer when present). VT appears ONLY in alumniOfSchema, never in hasCredentialSchema (D-10 -- do not imply a VT credential). All strings voice-neutral facts; zero em dash (U+2014); en dash permitted. Then author tests/content/education-module.test.ts as an import-and-assert unit gate (node env) mirroring tests/client/about-data.test.ts, asserting every item in the <behavior> block above INCLUDING the derivation-linkage assertions.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/content/education-module.test.ts && test "$(grep -c '—' src/data/education.ts)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run tests/content/education-module.test.ts` exits 0 (Gate D GREEN at this boundary)
    - `pnpm exec astro check` remains 0 errors / 0 warnings / 0 hints with education.ts added
    - src/data/education.ts contains zero U+2014 code points, asserted automatically at this boundary via `test "$(grep -c '—' src/data/education.ts)" -eq 0` (capture form exits 0 when clean; Gate A in 24-04 re-scans the file too; WARNING 3 -- earlier Nyquist sampling replaces the by-hand D-18 check)
    - Gate D asserts VT appears in alumniOfSchema but no hasCredentialSchema entry carries recognizedBy.name equal to "Virginia Tech" (VT is alumniOf only -- D-10)
    - Gate D asserts the derivation linkage (alumniOfSchema[0].name === EDUCATION.institution; degree name === EDUCATION.degreeSchemaName; validFrom === EDUCATION.dateISO), proving the fragments are not independently hardcoded (review fix, LOW single-source)
    - Module exports exactly: `Credential` (type), `EDUCATION`, `CREDENTIALS`, `alumniOfSchema`, `hasCredentialSchema`
  </acceptance_criteria>
  <done>education.ts exists as the SSoT with facts + DERIVED schema fragments; Gate D is GREEN; astro check stays 0/0/0.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen Gate E (chat-surface tripwire) + capture the phase-start invariant baseline</name>
  <files>tests/build/chat-surface-untouched.test.ts, scripts/verify-phase24-invariants.mjs, .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json</files>
  <read_first>
    - src/layouts/BaseLayout.astro (lines 20-140 -- the canonical anchors Gate E asserts still present: `ogImage = "/og-default.png"`, `titleDefault="Jack Cutrara | Software Engineer"`, the OG `width: 1200,` / `height: 630,` block, the `import ChatWidget from "../components/chat/ChatWidget.astro"` + `<ChatWidget />` render, the `pageswap` handler line `e.viewTransition?.finished?.catch`, and the three client-script imports `../scripts/analytics.ts` / `../scripts/scroll-depth.ts` / `../scripts/motion.ts`)
    - tests/content/experience-voice-em-dash.test.ts (the readFileSync source-scan idiom to clone for Gate E)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Gate E spec (lines 410-411) and Pitfall 4 (portfolio-context.json byte-identical from unchanged inputs)
    - package.json (the `dependencies` object to snapshot verbatim into the baseline; QA-02 no-new-deps)
  </read_first>
  <action>
    Two deliverables.

    (a) Create tests/build/chat-surface-untouched.test.ts (Gate E, D-19 tripwire) as a node-env readFileSync source guard over src/layouts/BaseLayout.astro. STRENGTHEN it beyond the four SEO anchors the review flagged as under-testing (finding #6, Gate E weak tripwire): assert the file still contains ALL of -- `ogImage = "/og-default.png"`, `titleDefault="Jack Cutrara | Software Engineer"`, the OG `width: 1200` and `height: 630` declarations, the `import ChatWidget` line and the `<ChatWidget />` render, the `pageswap` listener with `.viewTransition?.finished?.catch`, and each of the three client-script import specifiers (`../scripts/analytics.ts`, `../scripts/scroll-depth.ts`, `../scripts/motion.ts`). This makes an accidental edit to the chat widget / pageswap handler / client-script wiring fail the tripwire, not just an OG/title change. Note in the file's doc header that Gate E is a fast per-plan tripwire and the AUTHORITATIVE phase-wide D-19/D-17 proof is the baseline hash comparison run at the 24-04 capstone (deliverable b).

    (b) Capture the phase-start baseline and its verifier. Create scripts/verify-phase24-invariants.mjs (Node built-ins only -- `node:fs`, `node:crypto`, `node:child_process`; ZERO new deps, QA-02) that: reads .planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json; recomputes the SHA-256 of each protected file listed under protected_file_hashes and reports any whose current hash != the recorded baseline; reads the current package.json `dependencies` object and reports if JSON.stringify(current) != the recorded baseline dependencies (byte-for-byte, no new/removed/renamed deps); and exits nonzero with a per-mismatch report if ANY drift is found, exits 0 otherwise. The protected set is the D-19 + D-17 files: src/layouts/BaseLayout.astro, src/styles/global.css, src/scripts/chat.ts, src/pages/api/chat.ts, src/data/about-chat.ts, src/data/portfolio-context.json, src/data/portfolio-context.static.json, scripts/build-chat-context.mjs. The script must NOT include public/og-default.png in the unchanged set (that asset is deliberately replaced in 24-04); instead expose an exported helper (`ogPlaceholderHash()` reading baseline.og_placeholder_hash + `currentOgHash()`) the capstone can call to assert the current og hash DIFFERS from baseline. Then GENERATE 24-BASELINE.json now by hashing each protected file at its phase-start state, hashing public/og-default.png into og_placeholder_hash, snapshotting the package.json dependencies object verbatim, and recording phase_base_sha via `git rev-parse HEAD`. Because this task runs in Wave 1 before any 24-02/24-03 edit, these hashes are the true phase-start state.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/chat-surface-untouched.test.ts && node scripts/verify-phase24-invariants.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run tests/build/chat-surface-untouched.test.ts` exits 0 (Gate E GREEN); Gate E asserts the OG/title anchors AND the ChatWidget import+render, the pageswap `.finished?.catch` handler, and all three client-script import specifiers
    - `node scripts/verify-phase24-invariants.mjs` exits 0 at this boundary (protected files match the just-captured baseline; dependencies match)
    - 24-BASELINE.json exists and contains: `phase_base_sha`, `protected_file_hashes` for all eight D-19/D-17 files, `og_placeholder_hash`, and a verbatim `dependencies` snapshot
    - scripts/verify-phase24-invariants.mjs imports only Node built-ins (no new dependency; QA-02); `pnpm exec astro check` stays 0/0/0
  </acceptance_criteria>
  <done>Gate E is strengthened + GREEN; the phase-start baseline + its verifier exist and pass; both are consumed by the 24-04 capstone (review fix #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author content -> build-time module | education.ts strings are author-controlled constants compiled at build; no user input crosses here |
| module -> JSON-LD `<script>` | education fragments will be serialized into a `ld+json` script (in 24-02) via the existing JsonLd.astro escaper |
| repo state -> capstone gate | 24-BASELINE.json records the phase-start invariant fingerprint; the capstone trusts it to prove no protected file / dependency drifted across the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering (XSS) | education.ts strings reaching a `ld+json` script | low | mitigate | Fragments are consumed via JsonLd.astro's `<`/`>`/`&`/U+2028/U+2029 escaping (24-02); this plan only defines the constants, never hand-rolls a `<script>` tag (V5.3 output encoding) |
| T-24-06 | Tampering (invariant break) | protected chat-surface / dependency files across the phase | low | mitigate | The baseline hashes (this plan) + `verify-phase24-invariants.mjs` (run at 24-04) detect any BaseLayout / global.css / chat / dependency edit committed anywhere in 24-01..03, closing the working-tree-only blind spot (review fix #4) |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs ZERO packages (D-19: no new runtime deps); vitest + jsdom already present; verify-phase24-invariants.mjs uses Node built-ins only. The capstone dep-lock gate (24-04) asserts package.json dependencies byte-identical to the baseline |
</threat_model>

<verification>
- `pnpm exec vitest run tests/content/education-module.test.ts` exits 0 (Gate D GREEN)
- `pnpm exec vitest run tests/build/chat-surface-untouched.test.ts` exits 0 (Gate E GREEN, strengthened)
- `node scripts/verify-phase24-invariants.mjs` exits 0 (baseline captured; nothing drifted yet)
- `pnpm exec astro check` is 0/0/0 with education.ts added
- No file under src/layouts/ or src/styles/global.css was modified by this plan (D-19)
</verification>

<success_criteria>
- src/data/education.ts exists as the single source of truth (facts + DERIVED alumniOfSchema + hasCredentialSchema), VT in alumniOf only (D-10)
- Gate D + Gate E are GREEN at this plan's boundary (no intentionally-RED task-boundary gate; review fix #1)
- 24-BASELINE.json + scripts/verify-phase24-invariants.mjs capture the phase-start invariant fingerprint for the capstone (review fix #4)
- Zero new runtime dependencies added
</success_criteria>

## Review Findings Addressed (Phase 24 cross-AI review, codex gpt-5.6-sol)

- **#1 (HIGH) intentionally-RED task-boundary verify**: this plan no longer authors Gate A or Gate B/C. Gate B/C moves to 24-02 (its own boundary makes it GREEN); Gate A moves to the 24-04 capstone (the only point where all five scanned files -- owned across the parallel 24-02 + 24-03 -- are clean). Both gates that stay here (D, E) are GREEN at this boundary.
- **#4 (HIGH) capstone cannot prove phase-wide invariants after commits**: added the phase-start baseline (24-BASELINE.json) + scripts/verify-phase24-invariants.mjs; the capstone compares current protected-file hashes and the dependencies object against this fingerprint instead of relying on `git diff`/`git status --porcelain` of the working tree.
- **#6 (MEDIUM) Gate E weak tripwire**: Gate E strengthened to also assert the ChatWidget import+render, the pageswap `.finished?.catch` handler, and the three client-script import specifiers -- not only the four SEO anchors.
- **LOW single-source duplication**: alumniOfSchema + hasCredentialSchema now derive from EDUCATION/CREDENTIALS (with degreeSchemaName + dateISO on EDUCATION) rather than re-hardcoding WGU/VT; Gate D asserts the derivation linkage.

## Artifacts this phase produces

New symbols created by THIS plan (exclude from source-drift verification):
- `src/data/education.ts` and its exports: `Credential` (interface), `EDUCATION` (now including `degreeSchemaName` + `dateISO`), `CREDENTIALS`, `alumniOfSchema`, `hasCredentialSchema`
- `tests/content/education-module.test.ts` (Gate D)
- `tests/build/chat-surface-untouched.test.ts` (Gate E, D-19 tripwire, strengthened)
- `scripts/verify-phase24-invariants.mjs` (phase-scoped baseline verifier; Node built-ins only) and its exports `ogPlaceholderHash` / `currentOgHash`
- `.planning/phases/24-positioning-shift-home-teaser/24-BASELINE.json` (phase-start invariant fingerprint)

<output>
Create `.planning/phases/24-positioning-shift-home-teaser/24-01-SUMMARY.md` when done.
</output>
