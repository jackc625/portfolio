---
phase: 24-positioning-shift-home-teaser
plan: 03
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/data/about.ts
  - src/pages/about.astro
  - tests/build/about-education-render.test.ts
autonomous: true
requirements: [POS-01, POS-02, POS-03]
must_haves:
  truths:
    - "The About copy presents Jack as a software engineer with shipped production experience in an honest new-grad register -- no self-diminishing label, no senior/5+ years framing (POS-01, POS-02, D-06, D-07, D-12, D-13)"
    - "The /about page shows a dedicated compact Education/credentials block (WGU B.S. CS May 2026 primary, Virginia Tech transfer sub-note, LPI Linux Essentials) fed by education.ts (POS-03, D-09, D-10)"
    - "POS-03 has a persistent rendered-output regression test (tests/build/about-education-render.test.ts) asserting all four visible education facts from dist/client/about/index.html -- not just a source-copy gate + acceptance statement (review fix #6)"
    - "The revised copy propagates to the Home ABOUT preview because about.ts is the shared source of truth (D-06)"
    - "Every task <verify> exits 0 at its own boundary -- Task 1 runs about.ts-scoped checks only, not the cross-file Gate A (review fix #1)"
  artifacts:
    - src/data/about.ts
    - src/pages/about.astro
    - tests/build/about-education-render.test.ts
  key_links:
    - "about.astro imports EDUCATION + CREDENTIALS from src/data/education.ts (24-01) to render the block"
    - "index.astro (24-02) renders ABOUT_INTRO + ABOUT_P1 from about.ts -- the revision propagates to Home without editing index.astro's about copy"
    - "ABOUT_P2 stays VERBATIM (D-06); about-data.test.ts caps P1/P2/P3 at <=80 words"
    - "tests/build/about-education-render.test.ts (POS-03 render regression) is authored HERE; the cross-file Gate A is authored in 24-04 where all five scanned files are clean (review fix #1)"
---

<objective>
Revise the first-person About copy for the positioning shift and add the dedicated Education/credentials block to `/about`, then author the POS-03 rendered-output regression test.

Purpose: make the About narrative read as a software engineer with production experience and a recent degree in an honest new-grad register (POS-01, POS-02), and surface the completed education on the visible site (POS-03). Because `src/data/about.ts` is the shared source of truth, the revised intro + first paragraph also propagate to the Home ABOUT preview. Every task <verify> exits 0 at its own boundary (review fix #1): Task 1 runs only the about.ts word-count gate + an about.ts-scoped em-dash/register self-check (NOT the cross-file Gate A, which also scans index.astro/ContactSection.astro owned by the parallel 24-02); Task 2 authors the POS-03 render test and runs it GREEN after the block is built.

Output: a revised `src/data/about.ts` (intro / P1 / P3 reworked, P2 verbatim), a modified `src/pages/about.astro` (education block + sharpened description), and a new GREEN `tests/build/about-education-render.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md
@.planning/phases/24-positioning-shift-home-teaser/24-PATTERNS.md
@.planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md

# The education block divider idiom, the module it reads, and the render-gate analog
@src/data/about.ts
@src/pages/experience.astro
@tests/build/featured-tier-render.test.ts
@tests/client/about-data.test.ts
@design-system/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise src/data/about.ts copy (intro / P1 / P3; P2 verbatim)</name>
  <files>src/data/about.ts</files>
  <!-- planner-discipline-allow: junior -->
  <!-- planner-discipline-allow: senior -->
  <read_first>
    - src/data/about.ts (the existing convention to match: curly quotes, non-breaking space, per-string `/* Verified */` comment, zero U+2014; ABOUT_INTRO line 8 and ABOUT_P3 line 20 currently carry the self-applied seniority-qualifier word to drop)
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md §"Draft copy for Jack's review" (ABOUT_INTRO line 199, ABOUT_P1 lines 201-202, ABOUT_P3 lines 206-207) -- the drafts to use
    - .planning/phases/24-positioning-shift-home-teaser/24-CONTEXT.md D-06, D-07, D-12, D-13 (the copy contract)
    - tests/client/about-data.test.ts (the <=80-word cap on ABOUT_P1/P2/P3 -- RESEARCH Pitfall 2)
  </read_first>
  <action>
    Revise src/data/about.ts using the UI-SPEC drafts, matching the file's existing escape convention exactly (curly quotes, non-breaking space, en dash where needed; zero em dash U+2014). Rework ABOUT_INTRO to the D-07 draft that drops the self-applied seniority-qualifier word while keeping the memorable "systems that don't break at 3 a.m." line. Rework ABOUT_P1 to carry the production-experience positioning + present-tense Holloway framing (D-12) + a light recent-grad nod (finished B.S. in Computer Science), ending on the on-message "something I'd be comfortable handing off to a team" tail. Keep ABOUT_P2 completely VERBATIM (D-06) -- do not touch it. Rework ABOUT_P3 to the availability close: currently the solo contract engineer on Holloway Connect AND looking for a full-time software engineering role on a team that cares about correctness, reliability, and performance (D-13) -- drop the "entry-level" self-label per D-07. Keep ABOUT_P1 and ABOUT_P3 each at or under 80 words (the UI-SPEC drafts are ~57 and ~41 words; re-count if you adjust wording). The banned register words (the seniority-qualifier word, "senior", "5+ years") must not survive anywhere in the file, INCLUDING any code comment -- do not echo the word being removed into a `/* */` note. These strings are DRAFTS pending Jack's copy review at the 24-04 gate.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/client/about-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run tests/client/about-data.test.ts` exits 0 (all four exports truthy; P1/P2/P3 each <=80 words)
    - `grep -c $'—' src/data/about.ts` returns 0 (zero em dashes); `grep -ci $'\\bjunior\\b\\|\\bsenior\\b\\|5+ years' src/data/about.ts` returns 0 (about.ts-scoped register self-check; the cross-file Gate A lands in 24-04)
    - ABOUT_P2 is byte-identical to its pre-revision value (D-06 verbatim)
    - POSITIVE claims (review fix, LOW positive assertions): ABOUT_P1 mentions the present Holloway contract framing AND a completed B.S. in Computer Science; ABOUT_P3 mentions both the contract work and seeking a full-time role
    - `pnpm exec astro check` stays 0/0/0
  </acceptance_criteria>
  <done>about.ts intro/P1/P3 carry the honest new-grad-with-production-experience positioning (P2 verbatim); about-data + the about.ts-scoped em-dash/register self-check are GREEN; copy is a reviewable draft.</done>
</task>

<task type="auto">
  <name>Task 2: Add the /about Education block (frontend-design routed, D-20) + sharpen description + author the POS-03 render gate</name>
  <files>src/pages/about.astro, tests/build/about-education-render.test.ts</files>
  <!-- planner-discipline-allow: junior -->
  <read_first>
    - src/pages/about.astro (the 4-paragraph render + page-scoped `<style>`; line 8 the `description=` prop currently carrying the seniority-qualifier word to drop)
    - src/data/education.ts (EDUCATION + CREDENTIALS exports to read -- created in 24-01)
    - src/pages/experience.astro (lines 170-189 the `.earlier-divider` / `.earlier-rule` hairline `--rule` divider idiom to clone for the block divider)
    - tests/build/featured-tier-render.test.ts (the jsdom dist-render gate idiom to clone for the POS-03 render test)
    - .planning/phases/24-positioning-shift-home-teaser/24-UI-SPEC.md §"/about education block" (lines 88-97), §Typography, §Color, and the education-block label strings (line 217)
    - .planning/phases/24-positioning-shift-home-teaser/24-RESEARCH.md Code Examples (the /about education block render, lines 269-286)
    - design-system/MASTER.md (six-token palette, type roles, page-scoped CSS, §8 no narrative graphics on About) -- the D-20 contract
  </read_first>
  <action>
    First route the education block's form through the frontend-design skill against design-system/MASTER.md and the UI-SPEC block section (label grouping, VT sub-note styling, spacing, type scale -- D-09 discretion) before writing CSS. Then in src/pages/about.astro add a dedicated compact Education block AFTER the four About paragraphs, INSIDE the existing Container, with page-scoped CSS. Read EDUCATION + CREDENTIALS from ../data/education. Structure: a quiet `.label-mono` group label `EDUCATION` (color --ink) followed by a 1px `--rule` hairline divider cloned from the experience.astro `.earlier-divider` / `.earlier-rule` idiom; then the primary degree line leading with the completed degree -- `B.S. Computer Science`, `Western Governors University`, `May 2026` (degree --ink, institution + date .meta-mono --ink-muted); a light VT sub-note `Transferred from Virginia Tech` (.meta-mono, --ink-faint) that does NOT imply a VT credential (D-10); and the credential line `LPI Linux Essentials` on its own line (.meta-mono, --ink-muted). NO GPA, no honors, no skill icons or progress bars (MASTER §8). The block is non-interactive: NO accent anywhere in it (MASTER §7). All CSS page-scoped in about.astro -- do NOT touch global.css or BaseLayout.astro (D-19). Also sharpen the about.astro `description=` prop (line 8) to drop the self-applied seniority-qualifier word per D-07 (optionally the UI-SPEC line 214 reinforcement); keep it em-dash-free (U+2014). Keep every new string em-dash-free and free of the banned register words, including in comments.

    Then author tests/build/about-education-render.test.ts as a jsdom render gate (clone tests/build/featured-tier-render.test.ts: `// @vitest-environment jsdom`, read dist/client/about/index.html, `existsSync` guard + "run `pnpm build` first"). Assert the rendered About HTML contains all FOUR visible education facts (review fix #6, POS-03 render regression): "Western Governors University", "May 2026", "Transferred from Virginia Tech", and "LPI Linux Essentials"; assert the education block carries no accent affordance (no interactive/accent element inside the block); and add a positive POS-01/02 assertion that the rendered About body contains the completed-degree phrase and the full-time-search phrase. Author it AFTER the block is built so it is GREEN at this boundary (review fix #1).
  </action>
  <verify>
    <automated>pnpm build && pnpm exec vitest run tests/build/about-education-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm exec vitest run tests/build/about-education-render.test.ts` exits 0 (POS-03 render gate GREEN at this boundary)
    - The render gate asserts all four visible facts from dist/client/about/index.html: "Western Governors University", "May 2026", "Transferred from Virginia Tech", "LPI Linux Essentials" (review fix #6)
    - The render gate asserts the education block carries no accent color (non-interactive) and that the About body renders the completed-degree + full-time-search positive claims (review fix, LOW positive assertions)
    - `grep -c $'—' src/pages/about.astro` returns 0 and `grep -ci $'\\bjunior\\b\\|\\bsenior\\b\\|5+ years' src/pages/about.astro` returns 0 (about.astro-scoped self-check; cross-file Gate A lands in 24-04)
    - The education block reads EDUCATION + CREDENTIALS from ../data/education (no hardcoded duplicate of the facts); the divider uses a 1px `var(--rule)` hairline
    - No edit was made to src/styles/global.css or src/layouts/BaseLayout.astro (D-19); `pnpm exec astro check` stays 0/0/0
  </acceptance_criteria>
  <done>/about shows the frontend-design-routed compact Education block fed by education.ts, the description drops the self-label, and the new POS-03 render gate is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author copy -> static HTML | About strings + education facts are build-time constants; no user input, no runtime fetch on this surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Information disclosure (accuracy) | education facts on /about | low | mitigate | Facts sourced from education.ts (D-11) and the resume ground-truth (WGU B.S. CS May 2026, VT transfer, LPI cert); VT rendered as a transfer sub-note only, never as a credential (D-10) -- keeps the claim honest; the render gate asserts the exact visible facts |
| T-24-03 | Tampering (invariant break) | chat-surface via BaseLayout.astro / global.css | low | mitigate | D-19: education block + description edits are page-scoped in about.astro / a per-page prop; Gate E + the 24-04 baseline hash comparison prove BaseLayout/global.css unchanged |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero package installs (D-19); dep-lock gate at 24-04 asserts package.json dependencies byte-identical to the baseline |
</threat_model>

<verification>
- `pnpm build` succeeds and dist/client/about/index.html renders the Education block
- `pnpm exec vitest run tests/build/about-education-render.test.ts` exits 0 (POS-03 render regression GREEN)
- `pnpm exec vitest run tests/client/about-data.test.ts` exits 0
- `grep -c $'—' src/data/about.ts src/pages/about.astro` returns 0 for each (self-check; cross-file Gate A at 24-04)
- `pnpm exec astro check` is 0/0/0
- No change to src/layouts/BaseLayout.astro or src/styles/global.css (D-19; proved phase-wide by the 24-04 baseline comparison)
</verification>

<success_criteria>
- About copy presents Jack as a software engineer with production experience in an honest new-grad register (POS-01, POS-02, SC1, SC2)
- /about surfaces the completed WGU degree, the VT transfer, and the LPI cert, proven by a persistent render regression test (POS-03, SC3; review fix #6)
- ABOUT_P2 unchanged; no new runtime dependencies; no chat-surface file touched
</success_criteria>

## Review Findings Addressed (Phase 24 cross-AI review, codex gpt-5.6-sol)

- **#1 (HIGH) Task 1 cannot pass its verification command**: Task 1 no longer runs the cross-file site-copy Gate A (which also scans index.astro/about.astro literals owned across parallel plans and still said "junior" at that point). It runs the about-data word-count gate + an about.ts-scoped em-dash/register self-grep -- both GREEN once about.ts is revised. The comprehensive Gate A is authored in 24-04.
- **#6 (MEDIUM) POS-03 visible render not covered by a regression test**: added tests/build/about-education-render.test.ts asserting all four visible education facts from dist/client/about/index.html.
- **LOW register checks mostly negative**: added positive assertions (completed B.S. + present Holloway contract + full-time search) in both the about.ts acceptance and the rendered About body.

## Artifacts this phase produces

New symbols introduced by THIS plan (exclude from source-drift verification):
- The `/about` Education block markup + page-scoped classes (`.education` / `.education-label` / `.education-rule` / `.education-degree` / `.education-org` / `.education-transfer` / `.education-cred`, final names at executor discretion per frontend-design)
- Revised copy strings `ABOUT_INTRO`, `ABOUT_P1`, `ABOUT_P3` (values changed; symbol names unchanged)
- `tests/build/about-education-render.test.ts` (POS-03 render regression)
- Consumes (created in 24-01): `EDUCATION`, `CREDENTIALS` from `src/data/education.ts`

<output>
Create `.planning/phases/24-positioning-shift-home-teaser/24-03-SUMMARY.md` when done.
</output>
