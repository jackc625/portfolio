---
phase: 03-core-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/MobileMenu.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - HOME-01
  - HOME-04
must_haves:
  truths:
    - "Header displays site name and navigation links matching shiyunlu.com's nav pattern"
    - "Navigation works on both desktop and mobile"
    - "Footer matches shiyunlu.com's footer pattern with social links"
    - "Mobile menu opens, closes, navigates, and traps focus correctly"
    - "BaseLayout wraps all pages with updated shell structure"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Rewritten navigation matching shiyunlu.com pattern"
      contains: "nav"
      min_lines: 20
    - path: "src/components/Footer.astro"
      provides: "Rewritten footer matching shiyunlu.com design system"
      contains: "footer"
      min_lines: 10
    - path: "src/components/MobileMenu.astro"
      provides: "Rewritten mobile menu matching shiyunlu.com pattern"
      contains: "mobile-menu"
      min_lines: 30
    - path: "src/layouts/BaseLayout.astro"
      provides: "Updated layout shell for new design"
      contains: "BaseLayout"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Header.astro"
      via: "import and render"
      pattern: "import Header"
    - from: "src/components/MobileMenu.astro"
      to: "src/components/Header.astro"
      via: "menu-trigger button ID shared"
      pattern: "menu-trigger"
    - from: "src/components/Header.astro"
      to: "astro:page-load"
      via: "script re-initialization for View Transitions"
      pattern: "astro:page-load"
---

<objective>
Rewrite the site shell (Header, Footer, MobileMenu) and update BaseLayout to match shiyunlu.com's navigation and layout patterns. The Phase 2 scroll-reveal sticky nav is replaced per D-08.

Purpose: The site shell wraps every page. This must match shiyunlu.com's visual language before any page content is built. D-08 and D-09 explicitly require the nav/footer/mobile menu rework.

Output: Rewritten Header.astro, Footer.astro, MobileMenu.astro, and updated BaseLayout.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-RESEARCH.md
@.planning/phases/03-core-pages/03-01-SUMMARY.md

<interfaces>
<!-- Current component interfaces that must be preserved or adapted -->

From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
}
```
BaseLayout imports: Font (astro:assets), SEO (astro-seo), ClientRouter (astro:transitions), SkipToContent, Header, MobileMenu, Footer, global.css.
Body classes: `bg-bg-primary text-text-primary font-body min-h-screen antialiased flex flex-col`
Main element: `<main id="main-content" class="flex-1 pt-16">`

From src/components/Header.astro:
- navLinks array: [{href, label}] for Home, About, Projects, Resume, Contact
- Scroll-reveal script with .header-scrolled / .header-hidden classes (TO BE REMOVED per D-08)
- Hamburger trigger button with id="menu-trigger", aria-controls="mobile-menu"

From src/components/MobileMenu.astro:
- References menu-trigger, mobile-menu, menu-close IDs
- Focus trap implementation in handleKeyDown
- astro:page-load re-initialization pattern
- Staggered link animation with --link-index CSS variable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Header with shiyunlu.com nav pattern</name>
  <files>src/components/Header.astro</files>
  <read_first>
    - src/components/Header.astro (current implementation -- understand what to replace)
    - src/layouts/BaseLayout.astro (how Header is imported and rendered)
    - src/components/MobileMenu.astro (shared IDs: menu-trigger, mobile-menu)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-08: rework header to match shiyunlu.com)
    - .planning/phases/03-core-pages/03-RESEARCH.md (Pattern 4: Navigation Pattern)
  </read_first>
  <action>
    Per D-08, completely rewrite Header.astro to match shiyunlu.com's navigation pattern. The implementor MUST open https://shiyunlu.com in a browser and use DevTools to inspect:
    - The header element's positioning (fixed? sticky? static?)
    - The navigation grid/flex layout (column template, alignment, spacing)
    - How the site name/logo is styled and positioned
    - How navigation links are arranged and styled
    - The visual treatment (background, borders, blur effects)
    - How the hamburger trigger appears on mobile
    - Whether there is any scroll behavior (shiyunlu.com may or may not hide on scroll)

    **Requirements for the rewrite:**

    1. **Remove the old scroll-reveal logic entirely.** Delete the `.header-scrolled` and `.header-hidden` CSS classes and the scroll event listener script. Per D-08, the Phase 2 scroll-reveal sticky nav is replaced.

    2. **Keep these elements that MobileMenu.astro depends on:**
       - `id="menu-trigger"` on the hamburger button
       - `aria-expanded`, `aria-controls="mobile-menu"`, `aria-label` attributes on the hamburger button
       - The hamburger line spans (3 `<span>` elements with class `hamburger-line`) and their CSS transform animation for the X state

    3. **Keep the navLinks data array** (same 5 links: Home, About, Projects, Resume, Contact) and the `isActive` logic with `aria-current="page"`.

    4. **Preserve accessibility:** `aria-label="Main navigation"` on the `<nav>` element, focus-visible ring styles on all interactive elements.

    5. **Preserve the astro:page-load pattern** if any script initialization is needed. If the new header has NO client-side behavior (no scroll listener), the script block can be removed entirely.

    6. **Use Tailwind utility classes referencing the design token system.** All colors via `text-text-primary`, `bg-bg-primary`, etc. All spacing via `px-[var(--token-space-*)]`, etc. Font sizes via `text-[length:var(--token-text-*)]`.

    7. **Match shiyunlu.com's actual nav layout** as observed in DevTools. The research suggests an asymmetric grid (`grid grid-cols-[auto_1fr_auto]`) but the actual layout MUST be confirmed by inspecting the live site.
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Header.astro does NOT contain "header-scrolled" or "header-hidden"
    - src/components/Header.astro does NOT contain "window.addEventListener.*scroll"
    - src/components/Header.astro contains `id="menu-trigger"`
    - src/components/Header.astro contains `aria-label="Main navigation"`
    - src/components/Header.astro contains all 5 nav links: "Home", "About", "Projects", "Resume", "Contact"
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>Header rewritten to match shiyunlu.com's nav pattern, scroll-reveal removed, mobile trigger preserved, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite MobileMenu and Footer</name>
  <files>src/components/MobileMenu.astro, src/components/Footer.astro</files>
  <read_first>
    - src/components/MobileMenu.astro (current implementation -- preserve focus trap and accessibility)
    - src/components/Footer.astro (current implementation)
    - src/components/Header.astro (just rewritten in Task 1 -- check shared IDs)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-09: mobile menu rework, D-08: footer rework)
    - .planning/phases/03-core-pages/03-RESEARCH.md (Pattern 4: Navigation Pattern)
  </read_first>
  <action>
    **MobileMenu.astro rewrite (per D-09):**

    The implementor MUST open https://shiyunlu.com on a mobile viewport (DevTools responsive mode, 375px width) and inspect:
    - How the mobile menu appears (full-screen overlay? slide-in drawer? bottom sheet?)
    - The link layout (centered? left-aligned? stacked?)
    - Background treatment (solid? blur? transparent?)
    - Any additional elements in the mobile menu beyond nav links

    Rewrite MobileMenu.astro to match shiyunlu.com's mobile menu pattern while preserving these CRITICAL functional elements:

    1. **Keep `id="mobile-menu"`** (referenced by Header's aria-controls)
    2. **Keep `id="menu-close"`** button (referenced by script)
    3. **Keep the full script block** with `initMobileMenu()` function, including:
       - openMenu/closeMenu toggle logic
       - Focus trap (handleKeyDown with Tab cycling)
       - Escape key handling
       - body overflow lock
       - `astro:page-load` re-initialization
    4. **Keep `role="dialog"`, `aria-label`, `aria-modal="true"`** accessibility attributes
    5. **Keep the same 5 navLinks** with `isActive` logic and `aria-current="page"`

    Update the VISUAL treatment:
    - Background, typography, spacing, link styling to match shiyunlu.com's mobile menu
    - Link animation can be preserved or simplified (CSS transitions/hovers only per D-03, no GSAP)
    - Use design token Tailwind classes for all colors/spacing

    **Footer.astro rewrite:**

    The implementor MUST inspect shiyunlu.com's footer in DevTools:
    - Layout (centered? grid? flex?)
    - Content (what text, links, social icons appear)
    - Spacing and typography

    Rewrite Footer.astro to match shiyunlu.com's footer pattern while preserving:
    1. GitHub link (https://github.com/jackc625)
    2. LinkedIn link (https://linkedin.com/in/jackcutrara)
    3. Email link (mailto:jack@jackcutrara.com)
    4. `aria-label="Footer links"` on the nav element
    5. `target="_blank" rel="noopener noreferrer"` on external links
    6. Copyright notice (2026 Jack Cutrara)
    7. SVG icons for GitHub, LinkedIn, Email (can reuse existing SVGs or adapt to match shiyunlu.com's icon style)
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/MobileMenu.astro contains `id="mobile-menu"`
    - src/components/MobileMenu.astro contains `initMobileMenu` function
    - src/components/MobileMenu.astro contains `astro:page-load` event listener
    - src/components/MobileMenu.astro contains `role="dialog"`
    - src/components/MobileMenu.astro contains focus trap logic (handleKeyDown or equivalent)
    - src/components/Footer.astro contains `github.com/jackc625`
    - src/components/Footer.astro contains `linkedin.com/in/jackcutrara`
    - src/components/Footer.astro contains `jack@jackcutrara.com`
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>MobileMenu and Footer rewritten to match shiyunlu.com patterns, accessibility and functionality preserved, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Update BaseLayout for new shell structure</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (current layout)
    - src/components/Header.astro (just rewritten -- check if positioning changed from fixed)
    - src/components/Footer.astro (just rewritten)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-08: shell rework)
  </read_first>
  <action>
    Update BaseLayout.astro to work with the rewritten shell components. The key changes depend on what was observed on shiyunlu.com:

    1. **Update `<main>` padding-top.** The current `pt-16` compensates for the fixed header's height. If the rewritten header is still fixed, adjust `pt-16` to match the new header height. If the header is no longer fixed (or has a different height), adjust or remove `pt-16` accordingly. The implementor must check the rewritten Header.astro to determine the correct value.

    2. **Preserve all head content:** SEO component, Font components, ClientRouter, favicon links, meta viewport. These are NOT part of the visual rework.

    3. **Preserve the Props interface** (`title`, `description`, `ogImage`, `ogType`, `canonicalUrl`). This API is used by all page files.

    4. **Preserve the SkipToContent component** (accessibility requirement).

    5. **Preserve the `<slot />` pattern** inside `<main>`.

    6. **Check if BaseLayout needs additional wrapper divs or grid structure** to match shiyunlu.com's overall page composition. Some designs wrap content in an additional grid container.

    7. **Body classes** may need adjustment. Current: `bg-bg-primary text-text-primary font-body min-h-screen antialiased flex flex-col`. Update if shiyunlu.com uses a different overall page layout structure (e.g., CSS grid instead of flexbox column).
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/BaseLayout.astro contains `import Header from`
    - src/layouts/BaseLayout.astro contains `import Footer from`
    - src/layouts/BaseLayout.astro contains `import MobileMenu from`
    - src/layouts/BaseLayout.astro contains `<slot />`
    - src/layouts/BaseLayout.astro contains `<SkipToContent />`
    - src/layouts/BaseLayout.astro contains `<SEO` component
    - src/layouts/BaseLayout.astro contains `<ClientRouter />`
    - src/layouts/BaseLayout.astro contains `<Font cssVariable="--font-heading" />`
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>BaseLayout updated to work with rewritten shell components, all head content and accessibility features preserved, build passes</done>
</task>

</tasks>

<verification>
- `npx astro build` exits 0
- All 5 pages render (/, /about, /projects, /resume, /contact) without build errors
- Header no longer has scroll-reveal behavior
- Mobile menu still functional (open/close/focus trap/escape key)
- Footer has all 3 social links
</verification>

<success_criteria>
The site shell (header, footer, mobile menu, base layout) is rebuilt to match shiyunlu.com's design language. Navigation works on desktop and mobile. The old scroll-reveal header is gone. All accessibility features (focus trap, skip-to-content, aria attributes, keyboard navigation) are preserved. The shell is ready for page content to be built in Plans 03-05.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-02-SUMMARY.md`
</output>
