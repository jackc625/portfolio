---
phase: 03-core-pages
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/components/SkillGroup.astro
  - src/pages/about.astro
autonomous: true
requirements:
  - ABUT-01
  - ABUT-02
  - ABUT-03
must_haves:
  truths:
    - "About page presents Jack's background narrative in first-person conversational tone"
    - "Skills are grouped by context (Languages, Frameworks, Tools, Concepts) without progress bars"
    - "Page layout follows shiyunlu.com's design system (grid, spacing, typography, colors)"
    - "Page is responsive across mobile, tablet, and desktop"
  artifacts:
    - path: "src/components/SkillGroup.astro"
      provides: "Restyled skill group component within new design system"
      contains: "skills"
      min_lines: 10
    - path: "src/pages/about.astro"
      provides: "About page with narrative and skills in new design system"
      contains: "BaseLayout"
      min_lines: 30
  key_links:
    - from: "src/pages/about.astro"
      to: "src/components/SkillGroup.astro"
      via: "component import and render"
      pattern: "import SkillGroup"
    - from: "src/pages/about.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "import BaseLayout"
---

<objective>
Rebuild the About page within shiyunlu.com's design system. The page is composed freely (D-14) using the design system rules (fonts, colors, grid, spacing) but not a direct clone of any specific shiyunlu.com inner page.

Purpose: The about page gives recruiters and hiring managers a sense of who Jack is beyond his code. The first-person conversational tone (D-16) is preserved while the visual design matches the new system.

Output: Restyled SkillGroup.astro and rewritten about.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-RESEARCH.md
@.planning/phases/03-core-pages/03-02-SUMMARY.md

<interfaces>
<!-- Current SkillGroup interface -->
From src/components/SkillGroup.astro:
```typescript
interface Props {
  title: string;
  skills: string[];
}
```

<!-- Current about page skill data -->
4 skill groups used:
- Languages: TypeScript, JavaScript, Python, SQL, HTML, CSS
- Frameworks: React, Next.js, Astro, Node.js, Express
- Tools: Git, Docker, PostgreSQL, VS Code, Linux
- Concepts: REST APIs, CI/CD, Testing, Responsive Design, Accessibility

<!-- BaseLayout Props -->
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SkillGroup component</name>
  <files>src/components/SkillGroup.astro</files>
  <read_first>
    - src/components/SkillGroup.astro (current implementation)
    - src/styles/global.css (current token values after Plan 01 update)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-14, D-15: design system rules, flexible layout)
  </read_first>
  <action>
    Restyle SkillGroup.astro to match shiyunlu.com's design system while preserving the component's Props interface (`title: string`, `skills: string[]`).

    The implementor MUST look at how shiyunlu.com presents grouped lists or skill-like content (if any) and adopt that visual treatment. If shiyunlu.com doesn't have a direct analog, compose within the design system rules:

    **Visual changes:**
    1. **Remove the card treatment** (`bg-bg-secondary border border-border rounded-lg`) if shiyunlu.com doesn't use cards for this type of content. Replace with a cleaner, more minimal presentation -- possibly just a heading + inline list or a heading + tag-like chips.

    2. **Update typography:** The title should use the mono font at small size (keep the `font-mono text-[length:var(--token-text-sm)] uppercase tracking-[0.05em]` pattern -- it already matches a common shiyunlu.com pattern for labels).

    3. **Update the skills list layout.** Options based on design system:
       - **Inline comma-separated** (minimal): `skills.join(', ')` as a paragraph
       - **Flex-wrap tags** (modern): each skill as an inline-block with border/subtle background
       - **Simple list** (current): vertical list with no bullets
       Choose whichever matches shiyunlu.com's visual density most closely.

    4. **Use token-based spacing.** All padding/margin via `var(--token-space-*)` references.

    5. **Keep the Props interface unchanged** (`title: string`, `skills: string[]`). The about page passes these props.

    Per D-15, the skills presentation is flexible. If a completely different layout (e.g., a single flat grid of all skills without category headings) works better, that's acceptable. But the component interface should stay the same to avoid breaking the about page contract.
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SkillGroup.astro contains `interface Props` with `title: string` and `skills: string[]`
    - src/components/SkillGroup.astro uses Tailwind token classes (contains `text-text-` or `bg-bg-` or `font-`)
    - src/components/SkillGroup.astro does NOT contain progress bars or percentage values
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>SkillGroup restyled within new design system, Props interface preserved, no progress bars, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite About page within design system</name>
  <files>src/pages/about.astro</files>
  <read_first>
    - src/pages/about.astro (current implementation -- preserve narrative content)
    - src/components/SkillGroup.astro (just restyled in Task 1)
    - src/layouts/BaseLayout.astro (updated shell from Plan 02)
    - .planning/phases/03-core-pages/03-CONTEXT.md (D-14, D-15, D-16)
    - .planning/phases/03-core-pages/03-RESEARCH.md (about page composition notes)
  </read_first>
  <action>
    Rewrite `src/pages/about.astro` to compose a new layout within shiyunlu.com's design system rules (D-14). Not a clone of a specific shiyunlu.com inner page -- a free composition using the design system's grid, spacing, typography, and color.

    The implementor SHOULD study shiyunlu.com's inner pages (about, work, etc.) for layout inspiration -- how they use whitespace, grid asymmetry, and typography hierarchy -- then compose the about page within those patterns.

    **Content to preserve (D-16: conversational tone stays):**

    The narrative paragraphs should preserve this voice (update wording if needed, but keep first-person casual-professional tone):
    - Intro: "I'm Jack Cutrara, a software engineer based in New York. I got into programming because I wanted to build things -- not just read about them."
    - Education: "I studied computer science at university, where I discovered that the best way to learn was to ship real projects. That mindset stuck."
    - Interests: "Outside of code, I enjoy exploring new technologies, contributing to open source, and finding creative solutions to everyday problems."
    - CTA: "I'm currently looking for a software engineering role where I can contribute to a team building something that matters."

    **Layout composition:**

    1. **Page header:** Large display-sized heading. Could be "About" or "About Me" or something more personal per design feel. Use `font-display text-[length:var(--token-text-display)]`.

    2. **Narrative section:** The 4 paragraphs above. Use generous whitespace. Consider:
       - A wider layout (max-w-3xl or max-w-4xl) for readability
       - Larger body text for this section (text-[length:var(--token-text-heading)] for a statement feel, or keep at base for readability)
       - Asymmetric layout: narrative on one side, whitespace or decorative element on the other (only if shiyunlu.com does this)

    3. **Skills section:** Use the SkillGroup component with the same 4 groups (Languages, Frameworks, Tools, Concepts) and same skill arrays as current. Layout options:
       - 2-column grid on desktop, 1-column on mobile (current pattern -- may keep)
       - 3-column or 4-column grid if design system supports it
       - Flat inline list if SkillGroup was restyled that way

    4. **Section spacing:** Use `var(--token-space-section)` between major sections. Use `var(--token-space-lg)` or `var(--token-space-xl)` for internal spacing.

    5. **Requirements mapping:**
       - ABUT-01: Background narrative covering education, path into engineering, interests -- satisfied by the 4 paragraphs
       - ABUT-02: Professional but human tone -- satisfied by first-person conversational writing
       - ABUT-03: Skills grouped by context, not progress bars -- satisfied by SkillGroup component

    Per D-15, if the skills section doesn't fit the page flow naturally, it can be reshaped (e.g., moved to a different position, presented differently, or even condensed). But it must exist somewhere on the about page.
  </action>
  <verify>
    <automated>npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/about.astro contains `import BaseLayout from`
    - src/pages/about.astro contains `import SkillGroup from`
    - src/pages/about.astro contains "Jack Cutrara" in the narrative text
    - src/pages/about.astro contains "software engineer" (case-insensitive) in the narrative
    - src/pages/about.astro contains at least 3 SkillGroup components (Languages, Frameworks, Tools or equivalent)
    - src/pages/about.astro does NOT contain progress bars or percentage values
    - src/pages/about.astro uses `font-display` or `font-body` Tailwind classes
    - `npx astro build` exits 0
  </acceptance_criteria>
  <done>About page rewritten with design system composition, first-person narrative preserved, skills grouped by context, build passes</done>
</task>

</tasks>

<verification>
- `npx astro build` exits 0
- About page (/about) renders with narrative and skills sections
- First-person conversational tone preserved in narrative
- Skills grouped by context (not progress bars)
- Page uses design system tokens for all colors, fonts, spacing
</verification>

<success_criteria>
The About page tells Jack's story in a visually compelling layout that follows shiyunlu.com's design system. The conversational tone is preserved (D-16). Skills are presented by context without progress bars (ABUT-03). The layout is freely composed within the design rules (D-14), not a direct clone of a specific shiyunlu.com page.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-04-SUMMARY.md`
</output>
