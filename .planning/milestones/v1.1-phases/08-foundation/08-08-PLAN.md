---
phase: 08-foundation
plan: 08
type: execute
wave: 6
depends_on: [07]
files_modified: []
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, CONTACT-03]
requirements_addressed: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, CONTACT-03]

must_haves:
  truths:
    - "design-system/MASTER.md exists at repo root and documents tokens, typography, layout, components, motion, accent usage, and anti-patterns"
    - "The six hex tokens (#FAFAF7, #0A0A0A, #52525B, #A1A1AA, #E4E4E7, #E63946) are the only color source — no oklch tokens, no theme variables, no dark mode classes remain"
    - "Geist and Geist Mono are loaded via Astro 6 Fonts API (self-hosted) and applied as the default sans/mono families — Inter and IBM Plex Mono references removed"
    - "Dark mode is dead: ThemeToggle.astro deleted, BaseLayout.astro stripped of theme transition logic and FOUC scripts, prefers-color-scheme handling removed, no theme persistence in localStorage"
    - "Motion is dead: CanvasHero.astro deleted, GSAP imports removed from all pages including chat.ts, scroll-trigger animations gone, view transitions removed — only functional hover/focus color transitions remain"
    - "All 8 dead components removed: CTAButton.astro, FeaturedProjectItem.astro, ProjectCard.astro, SkillGroup.astro, CanvasHero.astro, ResumeEntry.astro, CaseStudySection.astro, ThemeToggle.astro"
    - "`/resume` route returns 404 — src/pages/resume.astro deleted from the project"
    - "npm run build && npm run lint && npm run check && npm run test all succeed, and the chat widget (Phase 7) still functions when the dev server is started — Phase 7 functionality is the regression gate for this phase"
  artifacts:
    - path: ".planning/phases/08-foundation/08-08-SUMMARY.md"
      provides: "The Phase 8 sign-off record with all four gate results + manual smoke test checklist"
  key_links:
    - from: "Phase 8 gate"
      to: "Phase 7 chat regression surface"
      via: "manual smoke test covers SSE, focus trap, markdown rendering"
      pattern: "SSE stream"
---

<objective>
Run the Phase 8 verification gate (D-26). Phase 8 ships only when ALL FOUR gates are green AND the manual chat smoke test passes. This plan consolidates the gate into:

1. **Task 1 (auto):** Run `npm run build && npm run lint && npm run check && npm run test` and record the results. Also run grep-based smoke assertions for DSGN-02, DSGN-03, DSGN-04 (static design-system regressions).

2. **Task 2 (checkpoint:human-verify):** Manual chat widget smoke test. Claude cannot run an interactive chat session against the Anthropic API in a headless env, so the user runs `npm run dev` and follows the checklist from RESEARCH section "Smoke test fingerprint" (16 items). The checkpoint blocks Phase 8 completion until the user confirms.

Purpose: Prove Phase 8 shipped a clean foundation without breaking the Phase 7 chat regression gate. Per D-26.

Output: A SUMMARY file at `.planning/phases/08-foundation/08-08-SUMMARY.md` with all four automated gate results, all static grep assertions, and the user's smoke-test verdict. Phase 8 is marked complete only after this plan's checkpoint passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-foundation/08-CONTEXT.md
@.planning/phases/08-foundation/08-RESEARCH.md
@.planning/phases/08-foundation/08-VALIDATION.md
@.planning/phases/08-foundation/08-01-SUMMARY.md
@.planning/phases/08-foundation/08-02-SUMMARY.md
@.planning/phases/08-foundation/08-03-SUMMARY.md
@.planning/phases/08-foundation/08-04-SUMMARY.md
@.planning/phases/08-foundation/08-05-SUMMARY.md
@.planning/phases/08-foundation/08-06-SUMMARY.md
@.planning/phases/08-foundation/08-07-SUMMARY.md
@package.json
@design-system/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated gate (build + lint + check + test) and static design-system grep assertions</name>
  <files></files>
  <read_first>
    - package.json (scripts: build, lint, check, test)
    - .planning/phases/08-foundation/08-CONTEXT.md D-26
    - .planning/phases/08-foundation/08-RESEARCH.md section 8 (gate mechanics — use `npm run check` not `tsc --noEmit`)
    - .planning/phases/08-foundation/08-VALIDATION.md (Nyquist contract)
  </read_first>
  <action>
**Gate Step 1 — `npm run build`**

Run `npm run build` (which chains `wrangler types && astro check && astro build && node scripts/pages-compat.mjs`). Capture the exit code and the last ~30 lines of output.

- **Pass:** Exit code 0, "Completed in X.XXs" message, dist/ directory populated
- **Fail:** Exit code non-zero, any TypeScript errors from astro check, any build errors from astro build

If the build fails, STOP and file the failures in the SUMMARY under "Gate Failures". Do NOT proceed to the manual checkpoint. Common failure modes to diagnose:
- Unresolved `import.*gsap` → Plan 03 GSAP removal incomplete
- `Cannot find module "./ThemeToggle.astro"` → Plan 04 Header.astro edit incomplete
- `Cannot find module "./CanvasHero.astro"` → Plan 06 page stub replacement incomplete
- Unresolved CSS var (`--token-*`) → Plan 02 or Plan 05 rename incomplete
- Astro content collection schema error → projects/[id].astro stub is missing `getStaticPaths`
- MDX rendering failure in projects collection → Plan 06 stub replacement incorrect

**Gate Step 2 — `npm run lint`**

Run `npm run lint` (eslint). Capture exit code.

- **Pass:** Exit code 0, no errors, warnings OK but note them in SUMMARY
- **Fail:** Exit code non-zero → fix violations and re-run

**Gate Step 3 — `npm run check`**

Run `npm run check` (astro check, same as `astro check` standalone). This is the canonical typecheck per RESEARCH section 8 — supersedes the bare `tsc --noEmit` in D-26's v1 wording.

- **Pass:** Exit code 0, "0 errors" output
- **Fail:** Exit code non-zero → report .astro file type errors in SUMMARY

**Gate Step 4 — `npm run test`**

Run `npm run test` (vitest run). Capture all test file results.

Expected passing tests (per RESEARCH Validation Architecture):
- `tests/api/chat.test.ts` — validation, CORS, SSE format, sanitization
- `tests/api/validation.test.ts` — zod schemas
- `tests/api/security.test.ts` — security
- `tests/client/markdown.test.ts` — markdown + XSS, imports renderMarkdown from chat.ts (compile canary — if chat.ts is broken, this test file fails to load)

- **Pass:** All non-`it.todo()` tests green, no failures
- **Fail:** Any test failure, or chat.ts import error in markdown.test.ts → report in SUMMARY

**Gate Step 5 — Static design-system grep assertions**

Run these greps to prove the design-system rebrand is complete. Each must return 0 hits OR the documented exceptions. Capture output:

| Assertion | Command | Expected |
|-----------|---------|----------|
| No --token-* CSS vars anywhere in src/ except possibly print comments | `grep -rEn "var\(--token-" src/` | 0 matches |
| No v1.0 Tailwind color class names | `grep -rEn "(bg-bg-primary\|bg-bg-secondary\|text-text-primary\|text-text-secondary\|text-text-muted\|border-border)" src/` | 0 matches |
| No gsap imports | `grep -rEn "from ['\"]gsap['\"]\|import\(['\"]gsap" src/` | 0 matches |
| No gsap dependency | `grep -c '"gsap"' package.json` | 0 |
| No [data-theme="light"] block in CSS | `grep -rEn 'data-theme' src/styles/global.css` | 0 matches |
| No localStorage.theme reads | `grep -rEn 'localStorage\.(theme\|getItem\("theme")' src/` | 0 matches |
| No ClientRouter imports or usages | `grep -rEn 'ClientRouter' src/` | 0 matches |
| No view transition keyframes | `grep -rEn 'view-transition' src/styles/global.css` | 0 matches |
| No [data-animate] stubs | `grep -rEn '\[data-animate\]\|data-animate="' src/` | 0 matches |
| No Inter or IBM Plex Mono references | `grep -rEn '"Inter"\|"IBM Plex Mono"\|font-heading\|font-sans\|font-code' src/ astro.config.mjs` | 0 matches |
| Hex tokens present in :root | `grep -c '#FAFAF7\|#E63946' src/styles/global.css` | ≥ 2 |
| MASTER.md exists | `test -f design-system/MASTER.md && echo OK` | "OK" |
| /resume route deleted | `test ! -f src/pages/resume.astro && echo OK` | "OK" |
| PDF renamed | `test -f public/jack-cutrara-resume.pdf && test ! -f public/resume.pdf && echo OK` | "OK" |
| Chat widget still imports marked + DOMPurify | `grep -c 'import.*marked\|import.*DOMPurify' src/scripts/chat.ts` | ≥ 2 |
| Chat widget has DOMContentLoaded fallback | `grep -c 'DOMContentLoaded' src/scripts/chat.ts` | 1 |
| MobileMenu has DOMContentLoaded fallback (D-30) | `grep -c 'DOMContentLoaded' src/components/MobileMenu.astro` | 1 |
| No transition:persist anywhere (D-29) | `grep -rEn 'transition:persist' src/` | 0 matches |

Record the result of each assertion in the SUMMARY file. Any failing assertion = gate failure, must be fixed before the manual smoke test checkpoint.

**Build output path verification (CONTACT-03):**

After `npm run build` succeeds, verify `/resume` returns 404 by checking the build output:
- `test ! -d dist/resume && echo "OK: /resume route not built"`
- OR `test ! -f dist/resume/index.html && echo "OK"`

If a `dist/resume/` directory exists, the deletion in Plan 07 did not propagate — investigate.

**SUMMARY file draft for this task:**

Write a draft at `.planning/phases/08-foundation/08-08-SUMMARY.md` containing:
- Gate Step 1 (build): PASS/FAIL + exit code
- Gate Step 2 (lint): PASS/FAIL
- Gate Step 3 (check): PASS/FAIL
- Gate Step 4 (test): PASS/FAIL + test counts
- Gate Step 5 (static assertions): table of assertions with OK/FAIL
- Build output /resume check: OK/FAIL
- Manual smoke test: PENDING (to be updated after Task 2)

Do NOT commit yet — wait for the manual smoke test in Task 2.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run check &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - `npm run check` exits 0
    - `npm run test` exits 0
    - `grep -rEn "var\(--token-" src/` returns 0 matches (or only comment lines in print stylesheet, documented)
    - `grep -rEn "from ['\"]gsap['\"]" src/` returns 0 matches
    - `grep -c '"gsap"' package.json` returns 0
    - `grep -rEn 'data-theme' src/styles/global.css` returns 0 matches
    - `grep -rEn 'ClientRouter' src/` returns 0 matches
    - `grep -rEn 'view-transition' src/styles/global.css` returns 0 matches
    - `grep -rEn 'transition:persist' src/` returns 0 matches
    - `grep -rEn '"Inter"\|"IBM Plex Mono"\|font-heading\|font-sans\|font-code' src/ astro.config.mjs` returns 0 matches
    - `test -f design-system/MASTER.md` exits 0
    - `test ! -f src/pages/resume.astro` exits 0
    - `test -f public/jack-cutrara-resume.pdf` exits 0
    - `test ! -f public/resume.pdf` exits 0
    - `test ! -d dist/resume` exits 0 after the build
    - `grep -c 'DOMContentLoaded' src/components/MobileMenu.astro` returns 1
    - Draft `.planning/phases/08-foundation/08-08-SUMMARY.md` exists with all gate results filled in (manual smoke marked PENDING)
  </acceptance_criteria>
  <done>
    All four automated gates are green, all static design-system grep assertions pass, the build output confirms /resume is not generated, and a draft 08-08-SUMMARY.md is written with gate results. Ready for manual smoke test checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual chat widget smoke test (D-26 gate step 4)</name>
  <what-built>
    Phase 8 has been automated:
    - MASTER.md authored
    - Tokens + fonts swapped
    - Dark mode + GSAP + motion + ClientRouter demolished
    - Chat widget refactored to new token names, motion kept motionless
    - Dead components deleted
    - 5 pages replaced with stubs
    - /resume deleted, PDF renamed
    - All 4 automated gates passed (build + lint + check + test)

    The manual chat smoke test is the regression fingerprint for Phase 7 chat functionality (per STATE.md Phase 7 carry-over decisions). vitest cannot cover SSE round-trip, visual rendering, focus trap, or keyboard flow end-to-end — only the user can verify these interactively.

    **Known expected changes since v1.0:**
    - Chat bubble no longer pulses (GSAP removed — D-27)
    - Chat panel no longer scales in on open (just `display: flex` — D-27)
    - Typing dots no longer bounce (CSS opacity fade only — D-27)
    - Cross-page chat persistence regresses — do NOT navigate between pages during the test (D-29)
    - Chat widget visuals are UNCHANGED in colors/shadows (still red bubble, still card layout — full restyle is CHAT-02 in Phase 10)
  </what-built>
  <how-to-verify>
**Prerequisite — before starting:**

1. Confirm `ANTHROPIC_API_KEY` is set in `.dev.vars` or via Cloudflare Wrangler secret store. Without this, the /api/chat endpoint returns an API error and the smoke test is inconclusive.
   - Check: `test -f .dev.vars && grep -c 'ANTHROPIC_API_KEY' .dev.vars`
   - Or confirm via `wrangler secret list` in the project directory

2. Kill any existing `npm run dev` instance (to ensure a fresh start with the new foundation).

**Start dev server:**

1. Run `npm run dev` in the project root
2. Wait for "Local: http://localhost:4321/" (or similar port) in the output
3. Confirm no errors or warnings in dev server startup

**Smoke test checklist — complete in order on localhost:4321/ (home stub page):**

- [ ] 1. Home stub page renders. URL `http://localhost:4321/`. Page shows `"Home — redesigning"` heading and `"This page is being redesigned. Check back soon."` paragraph.
- [ ] 2. Background is warm off-white `#FAFAF7`. Text is near-black `#0A0A0A`. (Inspect with browser devtools if unsure — the computed background-color of body should match.)
- [ ] 3. Header renders at the top of the page with the "Jack Cutrara" wordmark and the nav links Home / About / Projects / Contact (no Resume). The header is still using Plan 04's interim styling, not Phase 9's editorial rewrite — that's expected.
- [ ] 4. Floating red chat bubble is visible at the bottom-right of the viewport (48px square, background `#E63946`).
- [ ] 5. Click the chat bubble. Panel opens INSTANTLY (no scale-in animation — this is the expected regression per D-27). Starter chips render in Geist body font.
- [ ] 6. Click a starter chip OR type "show me a TypeScript snippet" and press Enter.
- [ ] 7. Typing indicator appears (3 dots with static opacity fade — no GSAP bounce, this is expected per D-27).
- [ ] 8. SSE stream starts — bot message appends tokens in real-time to a bot message bubble. No spinner-only behavior; tokens should arrive incrementally.
- [ ] 9. When the stream completes, the bot message contains at least one `<code>...</code>` block. The code block renders in **Geist Mono** (visually distinct from the Geist body text of the surrounding prose). If you're not sure, inspect the element and confirm `font-family` computed value includes `"Geist Mono"`.
- [ ] 10. Hover over the bot message — the copy button fades in (opacity transition — D-13 survivor). Click it — the message text is copied to clipboard.
- [ ] 11. Press Tab in the chat panel. Focus cycles through: starter chips (if still visible) / input field / send button / close button. Focus does NOT escape the panel (this is the focus trap — Phase 7 carry-over).
- [ ] 12. Press Shift-Tab — focus cycles backwards through the same elements.
- [ ] 13. Type a long message — character count appears below the input. At 450+ chars, the counter color changes to accent red `#E63946`. At 500 chars, typing is prevented.
- [ ] 14. Press Escape. The panel closes cleanly. Focus returns to the chat bubble (not lost to `<body>`).
- [ ] 15. Reopen the chat panel by clicking the bubble. Previous messages from the same session are still visible (in-memory state preserved on a single page — expected).
- [ ] 16. **DO NOT navigate to another page.** Cross-page persistence is a known Phase 8 regression scoped to Phase 10 per D-29. If you navigate, the chat resets — that is expected and NOT a smoke test failure, but it is not part of this test.

**Also verify the resume route is 404:**

- [ ] 17. Visit `http://localhost:4321/resume` — should return a 404 page (Astro's default 404 or your configured 404). The route does not exist.

**Font rendering verification:**

- [ ] 18. View the home page at `http://localhost:4321/`. Open devtools. The `<h1>` element's computed `font-family` should include `"Geist"`. The `<p>` element's computed `font-family` should include `"Geist"` (same family for display and body per D-11 / D-12).

**If all 18 checks pass:** The chat widget regression gate is green, the new foundation renders correctly, and Phase 8 ships.

**If ANY check fails:** Record the failure number and description in the SUMMARY. Do NOT type "approved" until the failure is diagnosed and fixed. Common failure modes:
- Chat bubble invisible → Plan 05 token rename incomplete
- Chat panel opens but stays blank → Plan 03 chat.ts GSAP removal broke initChat
- Tab escapes panel → focus trap broken (Phase 7 carry-over lost)
- SSE stream doesn't start → ANTHROPIC_API_KEY missing or /api/chat route broken
- Geist font not loading → Plan 02 astro.config.mjs + BaseLayout.astro fonts mismatch
- Warm off-white not applied → Plan 02 :root tokens not propagating OR Plan 03 global.css surgery broke body styles

**After all checks pass:**

Update `.planning/phases/08-foundation/08-08-SUMMARY.md` to mark the manual smoke test as PASS with a brief note (the user types a confirmation like "all 18 checks passed" or lists any minor observations that are not blockers).
  </how-to-verify>
  <resume-signal>Type "approved" if all 18 checks passed OR describe which checks failed and their symptoms. If checks failed, Phase 8 is blocked and you should coordinate with the planner to diagnose which plan(s) need a revision pass.</resume-signal>
</task>

</tasks>

<verification>
- Task 1 captures all 4 automated gate results + all static grep assertions in the SUMMARY
- Task 2 is the blocking manual checkpoint — Phase 8 is not complete until the user approves the smoke test
- On approval, the SUMMARY file is finalized with PASS on all 5 gate steps (4 automated + 1 manual)
</verification>

<success_criteria>
Phase 8 is complete when:
- `npm run build` exits 0
- `npm run lint` exits 0
- `npm run check` exits 0
- `npm run test` exits 0
- All static design-system grep assertions pass
- The manual chat widget smoke test passes all 18 checks
- All 6 Phase 8 requirement IDs (DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, CONTACT-03) are verifiable as satisfied

Phase 9 is unblocked.
</success_criteria>

<output>
After completion, the final `.planning/phases/08-foundation/08-08-SUMMARY.md` should include:
- Task 1 (automated gate): all 4 gates PASS + all static assertions OK
- Task 2 (manual smoke): user verdict + checklist (all 18 items)
- Phase 8 requirement coverage: DSGN-01..05 + CONTACT-03 each mapped to the plan(s) that satisfied it and the verification evidence
- Deferred items flagged for Phase 10:
  - REQUIREMENTS.md CONTACT-02 "placeholder PDF" wording reconciliation (per D-24 / Plan 07)
  - REQUIREMENTS.md CHAT-01 cross-page persistence reconciliation (per D-29 / Plan 03)
  - Chat motion restoration via CSS @keyframes (per D-27 / Plan 03)
- Milestone progress marker — Phase 8 of 4 (v1.1) complete, Phase 9 unblocked
</output>
