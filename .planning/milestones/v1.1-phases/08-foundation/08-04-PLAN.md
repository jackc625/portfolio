---
phase: 08-foundation
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/components/CTAButton.astro
  - src/components/FeaturedProjectItem.astro
  - src/components/ProjectCard.astro
  - src/components/SkillGroup.astro
  - src/components/CanvasHero.astro
  - src/components/ResumeEntry.astro
  - src/components/CaseStudySection.astro
  - src/components/ThemeToggle.astro
  - src/components/Header.astro
  - src/components/MobileMenu.astro
  - package.json
  - package-lock.json
autonomous: true
requirements: [DSGN-04]
requirements_addressed: [DSGN-04]

must_haves:
  truths:
    - "The 8 dead components listed in D-15 no longer exist"
    - "Header.astro has no ThemeToggle import, no <ThemeToggle /> usage, no /resume navLinks entry, and its inline Tailwind token classes have been renamed to the new token scheme"
    - "MobileMenu.astro has no /resume navLinks entry and its inline Tailwind token classes have been renamed to the new token scheme (DOMContentLoaded fallback from Plan 03 is preserved)"
    - "No file in src/ imports any of the deleted components"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Header component stripped of ThemeToggle and /resume, ready for Phase 9 full rewrite"
    - path: "src/components/MobileMenu.astro"
      provides: "MobileMenu with /resume removed, token classes renamed, DOMContentLoaded fallback preserved"
  key_links:
    - from: "src/components/Header.astro"
      to: "removed ThemeToggle component"
      via: "zero imports or usages after this plan"
      pattern: "ThemeToggle"
    - from: "src/components/Header.astro navLinks"
      to: "removed /resume route"
      via: "navLinks array no longer contains { href: \"/resume\" ... }"
      pattern: "/resume"
---

<objective>
Delete the 8 dead components listed in D-15, and surgically clean up the 2 surviving nav components (Header.astro and MobileMenu.astro) so they no longer reference the deleted components or the soon-to-be-deleted `/resume` route. Also rename the inline Tailwind token utility classes in Header/MobileMenu to the new token scheme so they render correctly against the new `--bg` / `--ink` tokens established in Plan 02.

Purpose: Remove the v1.0 component zoo that the new editorial system will not use, and land the minimum surgery on Header + MobileMenu needed for the site to render without deleted imports. Per D-15, D-17, D-25 wave 4.

Output: Eight fewer component files in `src/components/`, Header.astro stripped of ThemeToggle + /resume nav and using new token classes, MobileMenu.astro with /resume removed and token classes renamed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-foundation/08-CONTEXT.md
@.planning/phases/08-foundation/08-RESEARCH.md
@.planning/phases/08-foundation/08-03-SUMMARY.md
@src/components/Header.astro
@src/components/MobileMenu.astro

<interfaces>
<!-- Deletion list (D-15) -->

All 8 must be deleted from src/components/:
1. CTAButton.astro
2. FeaturedProjectItem.astro
3. ProjectCard.astro
4. SkillGroup.astro
5. CanvasHero.astro
6. ResumeEntry.astro
7. CaseStudySection.astro
8. ThemeToggle.astro

External imports of deleted components (per RESEARCH section 5 — all in pages that Plan 06 replaces with stubs):
- CTAButton used in src/pages/resume.astro (deleted in Plan 07)
- FeaturedProjectItem + ProjectCard used in src/pages/projects.astro (replaced by stub in Plan 06)
- SkillGroup used in src/pages/about.astro (replaced by stub in Plan 06)
- CanvasHero used in src/pages/index.astro (replaced by stub in Plan 06)
- ResumeEntry used in src/pages/resume.astro (deleted in Plan 07)
- CaseStudySection — no external importers (verified via grep in RESEARCH)
- ThemeToggle used in src/components/Header.astro (this plan removes)

None of the surviving components (Footer, NextProject, ArticleImage, SkipToContent, ContactChannel, JsonLd, MobileMenu, ChatWidget) import any of the 8 dead components. Verified in RESEARCH.md section 5.

<!-- Header.astro current state (from BaseLayout.astro wire-up) -->

Header.astro currently:
- Line 2: `import ThemeToggle from "./ThemeToggle.astro";`
- Line 5-11: navLinks array including `{ href: "/resume", label: "Resume" }`
- Line 16: class `border-border/40`
- Line 18: class `bg-bg-primary/80`
- Line 25: classes `font-display text-text-primary hover:text-accent focus-visible:ring-accent`
- Line 25: `text-[length:var(--token-text-base)]`
- Line 44: classes `font-body focus-visible:ring-accent` + `text-[length:var(--token-text-sm)]`
- Line 46-47: `text-text-primary` / `text-text-muted hover:text-text-primary nav-link-hover`
- Line 57: `<li><ThemeToggle /></li>` (desktop)
- Line 60: `<div class="md:hidden"><ThemeToggle /></div>` (mobile)
- Line 64: `class="text-text-primary focus-visible:ring-accent ..."`

MobileMenu.astro currently:
- Line 3-9: navLinks array including `{ href: "/resume", label: "Resume" }`
- Line 14: class `bg-bg-primary`
- Line 22: classes `text-text-primary focus-visible:ring-accent`
- Line 54-57: classes `font-display focus-visible:ring-accent mobile-menu-link text-[length:var(--token-text-heading)]` + `text-text-primary` / `text-text-muted hover:text-text-primary`
- The DOMContentLoaded fallback added by Plan 03 at the end of the script block — PRESERVE

<!-- Token class rename map (from Plan 02 + mockup semantics) -->

| Old Tailwind class | New Tailwind class | Notes |
|--------------------|--------------------|-------|
| `bg-bg-primary` | `bg-bg` | warm off-white bg |
| `bg-bg-primary/80` | `bg-bg/80` | translucent bg |
| `bg-bg-secondary` | `bg-rule` | subtle surface tint |
| `text-text-primary` | `text-ink` | black text |
| `text-text-secondary` | `text-ink-muted` | grey text |
| `text-text-muted` | `text-ink-faint` | light grey text |
| `text-accent` | `text-accent` | unchanged |
| `hover:text-accent` | `hover:text-accent` | unchanged |
| `hover:text-accent-hover` | `hover:text-accent` | no separate hover variant |
| `hover:text-text-primary` | `hover:text-ink` | black hover |
| `border-border` | `border-rule` | divider border |
| `border-border/40` | `border-rule/40` | translucent border |
| `focus-visible:ring-accent` | `focus-visible:ring-accent` | unchanged |
| `text-[length:var(--token-text-sm)]` | `text-sm` | Tailwind default replacement (0.875rem) |
| `text-[length:var(--token-text-base)]` | `text-base` | Tailwind default replacement (1rem) |
| `text-[length:var(--token-text-heading)]` | `text-3xl` | ~1.875rem, Tailwind default heading size — best v1.0-to-Tailwind-default swap until Phase 9 rebuilds mobile menu |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 8 dead components from src/components/</name>
  <files>src/components/CTAButton.astro, src/components/FeaturedProjectItem.astro, src/components/ProjectCard.astro, src/components/SkillGroup.astro, src/components/CanvasHero.astro, src/components/ResumeEntry.astro, src/components/CaseStudySection.astro, src/components/ThemeToggle.astro</files>
  <read_first>
    - .planning/phases/08-foundation/08-CONTEXT.md D-15
    - .planning/phases/08-foundation/08-RESEARCH.md section 5 (import graph — confirms no other importers)
  </read_first>
  <action>
Delete each of these 8 files:
1. `src/components/CTAButton.astro`
2. `src/components/FeaturedProjectItem.astro`
3. `src/components/ProjectCard.astro`
4. `src/components/SkillGroup.astro`
5. `src/components/CanvasHero.astro`
6. `src/components/ResumeEntry.astro`
7. `src/components/CaseStudySection.astro`
8. `src/components/ThemeToggle.astro`

Use `rm` via the Bash tool for each, or the Delete operation in the file tool. No replacements.

**Before committing this task:** Run `grep -rn "CTAButton\|FeaturedProjectItem\|ProjectCard\|SkillGroup\|CanvasHero\|ResumeEntry\|CaseStudySection\|ThemeToggle" src/` and verify the only remaining hits are:
- `src/components/Header.astro` — ThemeToggle imports/usages (Task 2 of this plan removes those in the same commit)
- `src/pages/index.astro`, `about.astro`, `projects.astro`, `resume.astro`, `projects/[id].astro` — Plan 06/07 replace those with stubs

No other hits should exist. If grep finds a hit in an unexpected file, STOP and investigate — do not silently proceed.

These deletions CAN land in a commit separate from Task 2 of this plan if that simplifies review, but the executor SHOULD bundle them with Task 2 for a single "dead components removed" commit (clean git history).

**simplex-noise uninstall (moved from Plan 03 Edit F — WRN-2 resolution):** After CanvasHero.astro is deleted, run `npm uninstall simplex-noise` in the working directory. This modifies `package.json` (removes `"simplex-noise"` from dependencies) AND `package-lock.json`. Both files must be staged with the CanvasHero deletion in the same commit. Reason for ordering: simplex-noise is consumed only by CanvasHero.astro, so uninstalling it before CanvasHero is deleted creates a momentarily unbuildable state. Uninstalling it in the same commit as the CanvasHero deletion keeps every intermediate state buildable.

After `npm uninstall simplex-noise` completes, verify:
- `grep -c '"simplex-noise"' package.json` returns 0
- `grep -rn "simplex-noise" src/` returns 0 (no stragglers in any surviving file)
  </action>
  <verify>
    <automated>test ! -f src/components/CTAButton.astro &amp;&amp; test ! -f src/components/FeaturedProjectItem.astro &amp;&amp; test ! -f src/components/ProjectCard.astro &amp;&amp; test ! -f src/components/SkillGroup.astro &amp;&amp; test ! -f src/components/CanvasHero.astro &amp;&amp; test ! -f src/components/ResumeEntry.astro &amp;&amp; test ! -f src/components/CaseStudySection.astro &amp;&amp; test ! -f src/components/ThemeToggle.astro</automated>
  </verify>
  <acceptance_criteria>
    - `test -e src/components/CTAButton.astro` exits 1 (deleted)
    - `test -e src/components/FeaturedProjectItem.astro` exits 1
    - `test -e src/components/ProjectCard.astro` exits 1
    - `test -e src/components/SkillGroup.astro` exits 1
    - `test -e src/components/CanvasHero.astro` exits 1
    - `test -e src/components/ResumeEntry.astro` exits 1
    - `test -e src/components/CaseStudySection.astro` exits 1
    - `test -e src/components/ThemeToggle.astro` exits 1
    - Remaining references to these names in src/ (excluding pages replaced by Plan 06/07 and Header.astro which Task 2 cleans) must be 0 after Task 2 lands
    - `grep -c '"simplex-noise"' package.json` returns 0 (dependency uninstalled after CanvasHero.astro deletion)
    - `grep -rn 'simplex-noise' src/` returns 0 matches
  </acceptance_criteria>
  <done>
    Eight component files are deleted from src/components/. Only pages that Plans 06/07 handle still reference them (those pages are replaced in the same wave). simplex-noise has been uninstalled in the same commit as the CanvasHero.astro deletion (moved from Plan 03 Edit F per WRN-2 — uninstalling before CanvasHero deletion would have left Wave 3 unbuildable).
  </done>
</task>

<task type="auto">
  <name>Task 2: Header.astro + MobileMenu.astro surgery — strip ThemeToggle, /resume, rename token classes</name>
  <files>src/components/Header.astro, src/components/MobileMenu.astro</files>
  <read_first>
    - src/components/Header.astro (current file — full read)
    - src/components/MobileMenu.astro (current file — lines 1-60 for nav markup, lines 225-235 for Plan 03 fallback to preserve)
    - .planning/phases/08-foundation/08-CONTEXT.md D-17
    - .planning/phases/08-foundation/08-RESEARCH.md section 5
    - design-system/MASTER.md section 5 Components
  </read_first>
  <action>
**Header.astro edits:**

1. **Delete line 2:** Remove `import ThemeToggle from "./ThemeToggle.astro";` — no replacement.

2. **Edit navLinks (lines 5-11):** Remove the `{ href: "/resume", label: "Resume" }` entry. The new navLinks array is:
```ts
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/projects", label: "Projects" },
  { href: "/contact", label: "Contact" },
];
```

3. **Rename inline Tailwind token classes throughout the file:**
- Line 16 `class="border-border/40 fixed top-0 right-0 left-0 z-50 border-b"` → `class="border-rule/40 fixed top-0 right-0 left-0 z-50 border-b"`
- Line 18 `class="bg-bg-primary/80 absolute inset-0 backdrop-blur-md"` → `class="bg-bg/80 absolute inset-0 backdrop-blur-md"`
- Line 25 class string: replace `font-display text-text-primary hover:text-accent focus-visible:ring-accent text-[length:var(--token-text-base)]` with `font-display text-ink hover:text-accent focus-visible:ring-accent text-base`
- Line 44 class string: replace `font-body focus-visible:ring-accent px-1 py-1 text-[length:var(--token-text-sm)]` with `font-body focus-visible:ring-accent px-1 py-1 text-sm`
- Line 46-47 class:list: replace `text-text-primary` with `text-ink` and `text-text-muted hover:text-text-primary nav-link-hover` with `text-ink-muted hover:text-ink nav-link-hover`
- Line 64 class string: replace `text-text-primary focus-visible:ring-accent` with `text-ink focus-visible:ring-accent`

4. **Delete `<li><ThemeToggle /></li>` at current line 57** — no replacement. The closing `</ul>` for the nav list follows directly after the last `navLinks.map(...)` `)}` expression.

5. **Delete `<div class="md:hidden"><ThemeToggle /></div>` at current line 60** — no replacement.

6. Leave everything else (the `<style>` block with hamburger animation, the button markup, the grid template) untouched. Phase 9 rewrites Header.astro entirely — this plan only does minimum surgery.

**MobileMenu.astro edits:**

1. **Edit navLinks (lines 3-9):** Remove the `{ href: "/resume", label: "Resume" }` entry. The new navLinks array is:
```ts
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/projects", label: "Projects" },
  { href: "/contact", label: "Contact" },
];
```

2. **Rename inline Tailwind token classes:**
- Line 14 `class="bg-bg-primary fixed inset-0 z-[60] hidden flex-col"` → `class="bg-bg fixed inset-0 z-[60] hidden flex-col"`
- Line 22 class string: replace `text-text-primary focus-visible:ring-accent` with `text-ink focus-visible:ring-accent`
- Line 54 class string: replace `font-display focus-visible:ring-accent mobile-menu-link text-[length:var(--token-text-heading)]` with `font-display focus-visible:ring-accent mobile-menu-link text-3xl`
- Line 56-57 class:list: replace `text-text-primary` with `text-ink` and `text-text-muted hover:text-text-primary` with `text-ink-muted hover:text-ink`

3. **Leave the DOMContentLoaded fallback block that Plan 03 added UNTOUCHED.** That is the D-30 fix and must survive this plan. Verify by running `grep -c DOMContentLoaded src/components/MobileMenu.astro` after edits — must return 1.

4. **Leave the `<style>` block and all other JavaScript** (the `initMobileMenu` function body, the openMenu/closeMenu logic, the hamburger CSS) untouched.
  </action>
  <verify>
    <automated>grep -c ThemeToggle src/components/Header.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ThemeToggle' src/components/Header.astro` returns 0
    - `grep -c 'import ThemeToggle' src/components/Header.astro` returns 0
    - `grep -c '/resume' src/components/Header.astro` returns 0
    - `grep -c '/resume' src/components/MobileMenu.astro` returns 0
    - `grep -c 'label: "Resume"' src/components/Header.astro` returns 0
    - `grep -c 'label: "Resume"' src/components/MobileMenu.astro` returns 0
    - `grep -c 'bg-bg-primary' src/components/Header.astro` returns 0
    - `grep -c 'bg-bg-primary' src/components/MobileMenu.astro` returns 0
    - `grep -c 'text-text-primary' src/components/Header.astro` returns 0
    - `grep -c 'text-text-primary' src/components/MobileMenu.astro` returns 0
    - `grep -c 'text-text-muted' src/components/Header.astro` returns 0
    - `grep -c 'text-text-muted' src/components/MobileMenu.astro` returns 0
    - `grep -c 'border-border' src/components/Header.astro` returns 0
    - `grep -c -- '--token-text-' src/components/Header.astro` returns 0
    - `grep -c -- '--token-text-' src/components/MobileMenu.astro` returns 0
    - `grep -c 'text-ink' src/components/Header.astro` returns ≥ 1
    - `grep -c 'bg-bg' src/components/Header.astro` returns ≥ 1
    - `grep -c 'border-rule' src/components/Header.astro` returns ≥ 1
    - `grep -c 'text-ink' src/components/MobileMenu.astro` returns ≥ 1
    - `grep -c 'bg-bg' src/components/MobileMenu.astro` returns ≥ 1
    - `grep -c 'DOMContentLoaded' src/components/MobileMenu.astro` returns 1 (Plan 03 fallback preserved)
    - `grep -c 'initMobileMenu' src/components/MobileMenu.astro` returns ≥ 2 (function + listener binding)
    - `grep -E "\{ href: \"/[a-z]+\", label: " src/components/Header.astro | wc -l` returns 4 (home, about, projects, contact)
    - `grep -E "\{ href: \"/[a-z]+\", label: " src/components/MobileMenu.astro | wc -l` returns 4
  </acceptance_criteria>
  <done>
    Header.astro has no ThemeToggle import/usage, no /resume navLinks entry, and all inline Tailwind token classes renamed to the new scheme. MobileMenu.astro has no /resume entry, inline classes renamed, and the Plan 03 DOMContentLoaded fallback preserved. The Header grid/style/hamburger block is untouched (Phase 9 rewrites Header entirely).
  </done>
</task>

</tasks>

<verification>
- `grep -rn "ThemeToggle" src/components/` returns 0 (after Task 1 + Task 2)
- `grep -rn "/resume" src/components/` returns 0 (both navLinks arrays clean)
- `grep -c "DOMContentLoaded" src/components/MobileMenu.astro` returns 1 (Plan 03 fallback intact)
- All 8 deleted files are gone (`ls src/components/CTAButton.astro` fails, etc.)
- No parallel Plan 05 or Plan 06 file overlap: Plan 04 touches Header.astro + MobileMenu.astro + deletes components. Plan 05 touches ChatWidget.astro + global.css .chat-* section + portfolio-context.json. Plan 06 touches pages + Footer/NextProject/ArticleImage/SkipToContent/ContactChannel. No shared files — all three can run in parallel.
</verification>

<success_criteria>
The v1.0 component zoo is deleted, Header + MobileMenu no longer import ThemeToggle or reference /resume, and their inline class attributes use the new Tailwind token utilities. DSGN-04 (motion/component cleanup) is structurally satisfied. Phase 9's full Header rewrite is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation/08-04-SUMMARY.md` capturing:
- 8 files deleted
- Header.astro diff: import removed, navLinks entry removed, 2 ThemeToggle usages removed, N class strings renamed
- MobileMenu.astro diff: navLinks entry removed, N class strings renamed, Plan 03 fallback preserved
- Confirmation that all three parallel Wave 4 plans (04, 05, 06) do not touch shared files
</output>
