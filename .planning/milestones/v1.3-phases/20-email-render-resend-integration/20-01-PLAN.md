---
phase: 20-email-render-resend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email/render.ts
  - tests/api/email-render.test.ts
  - tests/api/email-render.adversarial.test.ts
autonomous: true
requirements:
  - MAIL-02
  - MAIL-03
  - MAIL-04
  - MAIL-05
user_setup: []

must_haves:
  truths:
    - "renderEmail(env, transcript) is a pure function — calling it twice with the same ChatTranscript returns deeply equal ResendPayload (no Date.now / no crypto.randomUUID inside the renderer)"
    - "Subject derivation honors D-05 (null country -> literal `unknown`), D-06 (null referrer -> literal `direct`), D-07 (country pinned to /^[A-Z]{2}$/, referrer-host derived via new URL().hostname then /^[a-z0-9.-]+$/), D-08 (truncated suffix is trailing space + `(truncated)` parenthetical at end)"
    - "Body composition honors D-11 (8-line metadata header — D-11 enumerated 7 lines pre-D-09; D-09 hoisted the Cache aggregate as the 8th header line — with padded label column, then blank line, then provenance line, then blank line, then turn markers) and D-12 (markers `>>> visitor:` / `<<< bot:` on own line, raw content below, blank line between turns)"
    - "Cache aggregate one-liner format `Cache: {hits}/{total} turns hit, {read,localized} read / {created,localized} created` lives at line 8 of the metadata header (D-09 + D-10); hits = count of assistant turns with cache_read_input_tokens > 0; thousands separators via Number.toLocaleString(\"en-US\")"
    - "Defense-in-depth sanitizer ordering is fixed: stripControlChars -> stripCrLf (subject only) -> stripBidiOverrides -> htmlEscape (Landmine 6); every dynamic field (visitor content, bot content, referrer, user-agent, country) flows through this pipeline"
    - "Adversarial-payload it.each suite covers all 6 MAIL-05 classes (`<script>`, `</p><img onerror>`, `javascript:`, RTL/bidi `U+202A..U+202E` + `U+2066..U+2069`, null bytes, social-engineering provenance prefix) with verbatim assertions on the rendered text body"
    - "Provenance line carries the literal `From: chat widget on jackcutrara.com — visitor message follows below this line.` byte-distinct from any visitor-typable content; structural anti-impersonation defense holds"
  artifacts:
    - path: "src/lib/email/render.ts"
      provides: "Pure ChatTranscript -> ResendPayload renderer with subject + body composition + 4-stage sanitizer pipeline"
      exports: ["renderEmail", "ResendPayload", "RenderEnv"]
      min_lines: 150
    - path: "tests/api/email-render.test.ts"
      provides: "Happy-path + edge-case unit suite for renderer (subject + body + cache aggregate + HTML escape + CR/LF strip + determinism)"
      min_lines: 250
    - path: "tests/api/email-render.adversarial.test.ts"
      provides: "MAIL-05 closure: it.each over 6 locked payload classes asserting safe rendering"
      min_lines: 80
  key_links:
    - from: "src/lib/email/render.ts renderEmail"
      to: "src/lib/chat-transcripts.ts ChatTranscript type"
      via: "type-only import"
      pattern: "import type \\{[^}]*ChatTranscript"
    - from: "tests/api/email-render.test.ts"
      to: "src/lib/email/render.ts"
      via: "named import of renderEmail + ResendPayload"
      pattern: "from\\s+['\"]\\.\\./\\.\\./src/lib/email/render['\"]"
    - from: "tests/api/email-render.adversarial.test.ts"
      to: "src/lib/email/render.ts"
      via: "named import of renderEmail"
      pattern: "from\\s+['\"]\\.\\./\\.\\./src/lib/email/render['\"]"
---

<objective>
Wave 1 (parallel with 20-02): Author the pure email renderer module `src/lib/email/render.ts` and its two unit-test files. The renderer transforms a `ChatTranscript` into a `ResendPayload` ready for the wrapper in 20-02 to POST. Pure module — zero I/O, zero env reads beyond the threaded `RenderEnv`, zero `Date.now()` / `crypto.randomUUID()`, fully deterministic.

Purpose: This plan closes MAIL-02 (plaintext text body composition with metadata header + turn markers + provenance line), MAIL-03 (HTML-escape + CR/LF + bidi-strip + null-byte strip on every dynamic field), MAIL-04 (server-controlled subject + locked envelope literals), and MAIL-05 (adversarial-payload unit suite). The Result is consumed by 20-03's `sendOne` substitution.

Output: 1 NEW source module (`src/lib/email/render.ts`), 2 NEW test files (`tests/api/email-render.test.ts`, `tests/api/email-render.adversarial.test.ts`). No existing files touched. Renderer purity is the load-bearing invariant for Phase 20's idempotency story (Landmine 5/9 — non-deterministic body breaks Resend's Idempotency-Key).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-email-render-resend-integration/20-CONTEXT.md
@.planning/phases/20-email-render-resend-integration/20-RESEARCH.md
@.planning/phases/20-email-render-resend-integration/20-PATTERNS.md
@.planning/phases/20-email-render-resend-integration/20-VALIDATION.md
@src/lib/chat-transcripts.ts
@src/lib/chat-delivery.ts

<interfaces>
<!-- Type contracts the renderer reads (type-only imports). Extracted from src/lib/chat-transcripts.ts at Phase 19 close. -->
<!-- Renderer creates: ResendPayload + RenderEnv. Renderer reads: ChatTranscript + StoredTurn. -->

From src/lib/chat-transcripts.ts (Phase 18 lock — type-only import surface):
- `export interface StoredTurn { role: "user" | "assistant"; content: string; ts: string; cache_read_input_tokens?: number; cache_creation_input_tokens?: number; }`
- `export interface ChatTranscript { v: 1; sid: string; started_at: string; last_activity_at: string; msg_count: number; truncated: boolean; meta: { referrer: string | null; user_agent: string | null; country: string | null; region: string | null; colo: string | null; }; messages: StoredTurn[]; }`
- KV-04 has already truncated `referrer` and `user_agent` to <=512 chars at WRITE TIME (`chat-transcripts.ts:97-101 truncate` helper). Renderer reads already-truncated values — DO NOT re-truncate.

From src/lib/chat-delivery.ts:108-115 (existing helper to mirror, NOT re-export):
- `function hostnameOrNull(url?: string | null): string | null { if (!url) return null; try { return new URL(url).hostname; } catch { return null; } }` — Phase 20 renderer uses the SAME pattern (try/catch around `new URL().hostname`) inline in `deriveReferrerHostToken`; planner chose duplication over re-export to keep render.ts pure (no chat-delivery import surface coupling).

NEW types Plan 20-01 creates (consumed by Plan 20-02's wrapper + Plan 20-03's wiring):
- `export interface RenderEnv { CHAT_RECIPIENT_EMAIL: string; CHAT_SENDER_EMAIL: string; CHAT_REPLY_TO_EMAIL: string; }` — narrowed env shape; consumed by `renderEmail`
- `export interface ResendPayload { from: string; to: string; reply_to: string; subject: string; text: string; idempotency_key: string; }` — return shape; consumed by Plan 20-02's `sendEmail(env, payload)`
- `export function renderEmail(env: RenderEnv, transcript: ChatTranscript): ResendPayload` — public API
</interfaces>

<patterns>
<!-- Primary analog (renderer pure-module shape): src/lib/chat-transcripts.ts:1-101 -->
<!--   Header banner with decision-ID citations + locked constants block + types + file-local helpers + public API. -->
<!-- Secondary analog (URL-extraction with try/catch): src/lib/chat-delivery.ts:108-115 (hostnameOrNull) -->
<!-- Tertiary analog (test fixture builder): tests/api/chat-delivery.test.ts:174-205 (buildTranscript helper) -->
<!-- Adversarial test row shape: tests/api/chat-voice-split.test.ts:30-61 (single-fixture regex assertion) -->
<!-- Sanitizer pipeline ordering reference: 20-RESEARCH.md § Code Examples 1-3 (stripControlChars -> stripCrLf -> stripBidiOverrides -> htmlEscape) -->
<!-- Subject derivation reference: 20-RESEARCH.md § Code Example 2 (deriveCountryToken + deriveReferrerHostToken + composeSubject) -->
<!-- Body composition reference: 20-RESEARCH.md § Code Example 4 (composeBody + LABEL_WIDTH=12 + PROVENANCE literal) -->
<!-- Cache aggregate reference: 20-RESEARCH.md § Code Example 3 (deriveCacheLine using Number.toLocaleString("en-US")) -->
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 scaffold — create test files with failing-RED stubs covering ALL Per-Task Verification Map rows for Plan 20-01</name>
  <files>tests/api/email-render.test.ts, tests/api/email-render.adversarial.test.ts</files>
  <read_first>
    - .planning/phases/20-email-render-resend-integration/20-VALIDATION.md (READ Per-Task Verification Map rows 53-72 + lines 90 — every test name in the map is a `pnpm exec vitest run ... -t "<name>"` filter; the test file structure must EMIT each `it("<name>", ...)` so the map's commands actually resolve)
    - .planning/phases/20-email-render-resend-integration/20-PATTERNS.md (READ Group 4 lines 305-410 — fixture-builder pattern from tests/api/chat-delivery.test.ts:174-205; describe/it shape; adversarial it.each row shape; Code Example 5 lines 1044-1110 of 20-RESEARCH.md)
    - .planning/phases/20-email-render-resend-integration/20-RESEARCH.md (READ § Validation Architecture lines 302-376 — all locked test command -t "<name>" filters; § Implementation Landmines 5-6 lines 419-440 — determinism + sanitize ordering acceptance criteria; § Code Example 5 lines 1044-1110 — adversarial test row shape with mustContainEntities + mustNotContain expectation shape)
    - tests/api/chat-delivery.test.ts (READ lines 174-289 — fixture-builder helper buildTranscript(opts) pattern + console-spy beforeEach pattern; renderer tests do NOT need console-spy but MUST replicate the buildTranscript shape with new optional fields cacheReadTokens?: number[] and cacheCreationTokens?: number[])
    - tests/api/chat-voice-split.test.ts (READ lines 30-61 — regex-based assertion against rendered text shape; adversarial test borrows this assertion style with `expect(result.text).toContain(...)` and `expect(result.text).not.toContain(...)`)
  </read_first>
  <behavior>
    - Both test files COMPILE under `pnpm exec astro check` (TypeScript types resolve from src/lib/email/render and src/lib/chat-transcripts)
    - Test stubs FAIL RED on `pnpm exec vitest run` because src/lib/email/render.ts does not exist yet — the failure mode is "Cannot find module" or "renderEmail is not a function", NOT a TypeScript compile error
    - Each test name listed in 20-VALIDATION.md Per-Task Verification Map for plans 20-01 has a corresponding `it("<name>", ...)` block (use `it.skip` or `it.todo` if implementation is deferred to Task 2)
  </behavior>
  <action>
    Create `tests/api/email-render.test.ts` with the following structure: (a) header banner identical in spirit to tests/api/chat-delivery.test.ts:1-49 with citation to Plan 20-01 + decision IDs D-05..D-12 + Landmine 5/6; (b) imports `renderEmail`, `ResendPayload`, `RenderEnv` from `../../src/lib/email/render` (will fail RED); imports `type ChatTranscript`, `type StoredTurn` from `../../src/lib/chat-transcripts`; (c) constants block at file top: `const SID = "8b0f7f1c-1234-4567-8901-abcdef012345"`, `const STARTED_AT = "2026-05-12T14:23:08.000Z"`, `const LAST_ACTIVITY_AT = "2026-05-12T14:31:42.000Z"`, `const ENV: RenderEnv = { CHAT_RECIPIENT_EMAIL: "jackcutrara@gmail.com", CHAT_SENDER_EMAIL: '"Portfolio Chat" <transcripts@mail.jackcutrara.com>', CHAT_REPLY_TO_EMAIL: "jackcutrara@gmail.com" }`; (d) `function buildTranscript(opts: { sid?, msgCount?, truncated?, country?, referrer?, userAgent?, region?, cacheReadTokens?: number[], cacheCreationTokens?: number[] }): ChatTranscript` returning a ChatTranscript with sensible defaults — when cacheReadTokens / cacheCreationTokens supplied, distribute to assistant turns by index; (e) `describe` blocks grouping by concern: "subject derivation (D-05/D-06/D-07/D-08)", "body composition (D-11/D-12)", "cache aggregate (D-09/D-10)", "HTML escape (MAIL-03)", "CR/LF strip on subject (MAIL-03)", "renderer purity (Landmine 5)"; (f) every `it("<name>", ...)` from Per-Task Verification Map rows in 20-VALIDATION.md gets a stub — exact names: "subject happy path", "subject null country", "subject null referrer", "subject country regex", "subject referrer regex", "subject truncated suffix", "body metadata header shape", "cache aggregate", "provenance placement", "turn marker shape", "html escape", "crlf strip subject", "renderer purity"; (g) stubs MUST be implementable in Task 2 — use `it.todo("<name>", () => { /* TODO Task 2 */ })` for stubs OR write a real assertion that depends on the not-yet-existing renderEmail (preferred — RED state is informative).

    Create `tests/api/email-render.adversarial.test.ts` mirroring 20-RESEARCH.md § Code Example 5 lines 1044-1110: (a) header banner citing Plan 20-01 + MAIL-05 + Landmine 6; (b) imports `renderEmail` + `type ChatTranscript`; (c) `const ENV` fixture (same as render.test.ts); (d) `function buildTranscript(visitorContent: string): ChatTranscript` with hardcoded sid + a single user-turn fixture carrying the visitor content; (e) `describe("email-render adversarial payloads (MAIL-05)", ...)` with `it.each` rows for each of the 6 locked payload classes — exact `it.each` row labels: "script tag", "img onerror", "javascript url", "rtl bidi", "null bytes", "social engineering provenance" — using the literal payloads from 20-RESEARCH.md Code Example 5; (f) two additional standalone `it()` rows: "bidi strip" (covers all `U+202A..U+202E` + `U+2066..U+2069` codepoints — assert ZERO occurrences in output via `/[‪-‮⁦-⁩]/u.test(result.text)` returns false) and "null byte strip" (assert ZERO `\0` bytes); (g) for each `it.each` row, expect `result.text` to contain the entity-encoded form (e.g. `&lt;script&gt;`) and NOT contain the raw injection vector (e.g. `<script>`); (h) social-engineering payload assertion verifies the visitor's typed text appears UNDER a `>>> visitor:` marker AND that the AUTHENTIC provenance line `From: chat widget on jackcutrara.com — visitor message follows below this line.` appears EXACTLY ONCE in the body output (defense against impersonation).

    NO renderer implementation in Task 1. Task 2 lands the implementation — these tests are the TDD RED battery.
  </action>
  <verify>
    <automated>pnpm exec astro check</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/email-render.test.ts` exists; `tests/api/email-render.adversarial.test.ts` exists
    - `pnpm exec astro check` exits 0/0/0 (test files typecheck cleanly)
    - `pnpm exec vitest run tests/api/email-render.test.ts` exits non-zero with errors of class "Cannot find module '../../src/lib/email/render'" OR equivalent module-not-found (RED state)
    - `pnpm exec vitest run tests/api/email-render.adversarial.test.ts` exits non-zero with same module-not-found pattern (RED state)
    - `grep -c 'it.todo\|it(\|it.each' tests/api/email-render.test.ts` returns >= 13 (covers the 13 distinct test names from VALIDATION.md plus determinism)
    - `grep -c 'it.each\|it(' tests/api/email-render.adversarial.test.ts` returns >= 6 (one row per locked payload class plus standalone rows)
    - `grep -F 'From: chat widget on jackcutrara.com — visitor message follows below this line.' tests/api/email-render.adversarial.test.ts | wc -l` returns >= 1 (provenance literal cited verbatim)
    - `grep -F 'cacheReadTokens' tests/api/email-render.test.ts` returns at least 1 line (fixture-builder supports cache-token injection)
    - The full test suite still passes WITHOUT these new tests being included — `pnpm exec vitest run --exclude tests/api/email-render.test.ts --exclude tests/api/email-render.adversarial.test.ts` exits 0 (498 PASS / 0 FAIL / 2 SKIP baseline preserved)
  </acceptance_criteria>
  <done>
    Two test files exist with stubs for every Per-Task Verification Map row covered by Plan 20-01. astro check 0/0/0. The two new files fail RED with module-not-found (intentional — Task 2 lands the renderer). Existing 498/0/2 baseline preserved when excluding the new files. Plan 20-02 + Plan 20-03 can run in parallel without depending on this plan's Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author src/lib/email/render.ts pure renderer turning Wave 0 RED tests GREEN</name>
  <files>src/lib/email/render.ts, tests/api/email-render.test.ts, tests/api/email-render.adversarial.test.ts</files>
  <read_first>
    - tests/api/email-render.test.ts (READ — own Task 1 output; see exactly which test names + assertions must turn GREEN)
    - tests/api/email-render.adversarial.test.ts (READ — own Task 1 output)
    - src/lib/chat-transcripts.ts (READ FULL — the pure-module template Plan 20-01's render.ts mirrors structurally; lines 1-101 are the file-banner + locked constants + types pattern; lines 94-101 are the file-local truncate helper pattern that informs the inline sanitizer helpers)
    - src/lib/chat-delivery.ts (READ lines 1-37 + 108-115 — file-banner pattern + hostnameOrNull URL-extraction pattern; renderer mirrors hostnameOrNull inline as deriveReferrerHostToken)
    - .planning/phases/20-email-render-resend-integration/20-CONTEXT.md (READ <decisions> D-05..D-12 lines 56-92 + Claude's Discretion lines 102-113 — locked decisions for subject + body + cache-aggregate + provenance-line placement)
    - .planning/phases/20-email-render-resend-integration/20-RESEARCH.md (READ § Code Examples 1-4 lines 906-1042 — the 5-helper sanitizer pipeline, subject derivation, cache aggregate, body composition; § Implementation Landmines 5 + 6 + 9 lines 419-466)
    - .planning/phases/20-email-render-resend-integration/20-PATTERNS.md (READ Group 1 second analog lines 130-200 — render.ts module shape with locked constants block + file-local helpers + public API)
  </read_first>
  <behavior>
    - All `it("<name>", ...)` from Task 1 transition RED -> GREEN
    - `renderEmail(env, transcript)` is a pure function: `renderEmail(ENV, t)` and `renderEmail(ENV, t)` return deeply equal objects (Landmine 5)
    - Subject `[Portfolio chat] N turns from <country> via <referrer-host>` for all happy-path inputs
    - Subject substitutes literal `unknown` when meta.country is null OR fails /^[A-Z]{2}$/ regex
    - Subject substitutes literal `direct` when meta.referrer is null OR new URL() throws OR resulting hostname fails /^[a-z0-9.-]+$/
    - Subject ends with literal ` (truncated)` when transcript.truncated === true
    - Body emits 8-line metadata header (Session, Started, Last turn (with duration), From, Referrer, User-agent, Messages, Cache) with 12-char padded label column
    - Body emits provenance line `From: chat widget on jackcutrara.com — visitor message follows below this line.` separated by blank lines from the header above and the turn markers below
    - Turn markers `>>> visitor:` (role==="user") / `<<< bot:` (role==="assistant") on own line; raw-but-escaped content below; blank line between turns
    - Cache aggregate emits format `{hits}/{total} turns hit, {read,localized} read / {created,localized} created` with thousands separators via `Number.toLocaleString("en-US")`
    - Adversarial payload `<script>alert(1)</script>` renders as `&lt;script&gt;alert(1)&lt;/script&gt;` (HTML-escaped); raw `<script>` substring NOT present
    - Adversarial RTL/bidi codepoints in `U+202A..U+202E` + `U+2066..U+2069` ranges stripped (zero occurrences in output)
    - Adversarial null bytes (`\0`) stripped (zero occurrences in output)
    - Sanitizer pipeline ordering: stripControlChars -> stripCrLf (subject only) -> stripBidiOverrides -> htmlEscape (Landmine 6)
  </behavior>
  <action>
    Create `src/lib/email/render.ts` with the following structure:

    HEADER BANNER (mirror src/lib/chat-transcripts.ts:1-29 + src/lib/chat-delivery.ts:1-37): file purpose paragraph; Phase 20 contract list (MAIL-02 / MAIL-03 / MAIL-04 / MAIL-05); decision IDs honored block citing D-05 through D-12 + D-17 (no replayed variant) + Landmine 5/6/9; "Pure module. NO imports from..." block listing @anthropic-ai/sdk, cloudflare:workers, src/prompts/, src/pages/, src/scripts/chat.ts, src/lib/chat-delivery.ts (renderer is upstream of chat-delivery; reverse direction violates module dependency).

    LOCKED CONSTANTS BLOCK: `LABEL_WIDTH = 12`; `PROVENANCE = "From: chat widget on jackcutrara.com — visitor message follows below this line."`; `COUNTRY_PATTERN = /^[A-Z]{2}$/` per D-07; `HOST_PATTERN = /^[a-z0-9.-]+$/` per D-07; `COUNTRY_FALLBACK = "unknown"` per D-05; `REFERRER_HOST_FALLBACK = "direct"` per D-06; `TRUNCATED_SUFFIX = " (truncated)"` per D-08.

    PUBLIC TYPES: `export interface RenderEnv { CHAT_RECIPIENT_EMAIL: string; CHAT_SENDER_EMAIL: string; CHAT_REPLY_TO_EMAIL: string; }` and `export interface ResendPayload { from: string; to: string; reply_to: string; subject: string; text: string; idempotency_key: string; }`. Type-only import `import type { ChatTranscript } from "../chat-transcripts";` — NO value imports from chat-transcripts (KEY_PREFIX etc. not needed).

    FILE-LOCAL SANITIZER HELPERS (Landmine 6 ordering): `stripControlChars(s)` per 20-RESEARCH.md Code Example 1 — strips C0 controls except `\t \n \r` via `/[\x00-\x08\x0B-\x0C\x0E-\x1F\x7F]/g`; `stripCrLf(s)` strips `[\r\n]` (subject only); `stripBidiOverrides(s)` strips `[‪-‮⁦-⁩]`; `htmlEscape(s)` per Code Example 1 — & first, then <, >, ", ' to entities; composed `escapeBodyField(raw: string | null): string` (control + bidi + escape); `escapeSubjectComponent(raw: string | null): string` (control + crlf + bidi + escape).

    FILE-LOCAL SUBJECT HELPERS (D-05/D-06/D-07/D-08 per 20-RESEARCH.md Code Example 2): `deriveCountryToken(country: string | null): string` returns country if it matches COUNTRY_PATTERN else COUNTRY_FALLBACK; `deriveReferrerHostToken(referrer: string | null): string` returns REFERRER_HOST_FALLBACK if !referrer OR new URL throws OR hostname fails HOST_PATTERN; mirror src/lib/chat-delivery.ts:108-115 hostnameOrNull try/catch shape; `composeSubject(transcript: ChatTranscript): string` returns `[Portfolio chat] ${msg_count} turns from ${country} via ${host}${truncated ? TRUNCATED_SUFFIX : ""}`.

    FILE-LOCAL CACHE-AGGREGATE HELPER (D-09/D-10 per Code Example 3): `deriveCacheLine(transcript: ChatTranscript): string` returns format `${hits}/${total} turns hit, ${totalRead.toLocaleString("en-US")} read / ${totalCreated.toLocaleString("en-US")} created`; hits = `assistantTurns.filter(m => (m.cache_read_input_tokens ?? 0) > 0).length`; totalRead/totalCreated via reduce-sum over assistant turns.

    FILE-LOCAL DURATION HELPER: `formatDuration(durationMs: number): string` returning `Xm Ys` form (e.g. `8m 34s`); used by metadata header line 3.

    FILE-LOCAL BODY HELPER (D-11/D-12 per Code Example 4): `composeBody(transcript: ChatTranscript): string` builds 8-line headerLines array with `pad(label).padEnd(LABEL_WIDTH)` at each label position — labels exactly: `Session:`, `Started:`, `Last turn:`, `From:`, `Referrer:`, `User-agent:`, `Messages:`, `Cache:`. Then turnLines via flatMap producing `[marker, escapedContent, ""]` per turn. Final body = `[...headerLines, "", PROVENANCE, "", ...turnLines].join("\n").trimEnd() + "\n"`.

    PUBLIC API: `export function renderEmail(env: RenderEnv, transcript: ChatTranscript): ResendPayload` returns `{ from: env.CHAT_SENDER_EMAIL, to: env.CHAT_RECIPIENT_EMAIL, reply_to: env.CHAT_REPLY_TO_EMAIL, subject: composeSubject(transcript), text: composeBody(transcript), idempotency_key: \`transcript/${transcript.sid}\` }`.

    PURITY GUARDRAILS: NO `Date.now()`, NO `crypto.randomUUID()`, NO `process.env`, NO env reads beyond the threaded RenderEnv. Adding any time-of-render value would violate Landmine 5 + break Resend Idempotency-Key.

    Then update Task 1 stubs to real assertions (if originally written as it.todo, replace with concrete `expect(...)` calls). Run `pnpm exec vitest run tests/api/email-render.test.ts tests/api/email-render.adversarial.test.ts` until ALL tests GREEN.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/api/email-render.test.ts tests/api/email-render.adversarial.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/email/render.ts` exists; `wc -l src/lib/email/render.ts` returns >= 150
    - `pnpm exec vitest run tests/api/email-render.test.ts` exits 0; all 13+ test names from VALIDATION.md pass
    - `pnpm exec vitest run tests/api/email-render.adversarial.test.ts` exits 0; all 6 it.each rows + bidi strip + null byte strip pass
    - `grep -c 'export function renderEmail' src/lib/email/render.ts` returns 1
    - `grep -c 'export interface ResendPayload' src/lib/email/render.ts` returns 1
    - `grep -c 'export interface RenderEnv' src/lib/email/render.ts` returns 1
    - `grep -E 'Date\.now|crypto\.randomUUID|process\.env' src/lib/email/render.ts | grep -v '^ \*\| \*//\|//' | wc -l` returns 0 (purity guardrail — no time-of-render values, no random, no env reads)
    - `grep -F 'From: chat widget on jackcutrara.com — visitor message follows below this line.' src/lib/email/render.ts` returns >= 1 (PROVENANCE literal exact)
    - `grep -E 'COUNTRY_PATTERN\s*=\s*/\^\[A-Z\]\{2\}\$/' src/lib/email/render.ts` returns at least 1 line (D-07 country regex)
    - `grep -E 'HOST_PATTERN\s*=\s*/\^\[a-z0-9.-\]\+\$/' src/lib/email/render.ts` returns at least 1 line (D-07 host regex)
    - `grep -F 'toLocaleString("en-US")' src/lib/email/render.ts` returns >= 2 (D-10 thousands separators on read + created counts)
    - `grep -F '@anthropic-ai/sdk' src/lib/email/render.ts | grep -v '^ *//\|^ *\*' | wc -l` returns 0 (no Anthropic import)
    - `grep -F 'cloudflare:workers' src/lib/email/render.ts | grep -v '^ *//\|^ *\*' | wc -l` returns 0 (no cloudflare import)
    - `grep -F "import type { ChatTranscript } from " src/lib/email/render.ts` matches 1 (type-only import of ChatTranscript)
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm exec vitest run tests/api/sse-snapshot.test.ts` exits 0 (D-15 anchor preserved)
    - `pnpm exec vitest run tests/api/anthropic-payload-shape.test.ts` exits 0 (TEST-03 anchor preserved)
    - `git diff --stat package.json` shows no changes to dependencies (MAIL-01 zero-new-runtime-dep lock)
  </acceptance_criteria>
  <done>
    Pure renderer module shipped. All 13+ render tests + 6+ adversarial tests GREEN. astro check 0/0/0. Renderer is deterministic — `renderEmail(t)` deeply equals `renderEmail(t)` (Landmine 5). Plan 20-03's substitution can import `renderEmail`, `ResendPayload`, `RenderEnv` from `./email/render`. Cross-phase forward-defense: D-15 SSE GREEN, TEST-03 Anthropic GREEN, package.json deps byte-identical.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor input -> ChatTranscript.messages[i].content | Untrusted user content; truncated at write-time (Phase 18 KV-04) but otherwise verbatim |
| visitor input -> ChatTranscript.meta.referrer / user_agent / country | Untrusted header values; truncated to 512 chars at Phase 18 write time; renderer trusts the truncation |
| ChatTranscript -> rendered email body (text field) | All escape/strip happens here; output flows to Resend Plan 20-02 then to Gmail |
| ChatTranscript.meta.country / referrer-host -> Subject header | Subject regex-validated to /^[A-Z]{2}$/ + /^[a-z0-9.-]+$/ at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Spoofing | Visitor types `From: chat widget on jackcutrara.com — visitor message follows below this line.` to spoof provenance | mitigate | Renderer NEVER echoes visitor content into the metadata header block or pre-conversation area; visitor content always lives under a `>>> visitor:` marker AFTER the AUTHENTIC provenance line. Adversarial test "social engineering provenance" asserts the visitor's literal text appears under marker AND the authentic line appears once with the byte-distinct prefix |
| T-20-01-02 | Tampering | Visitor injects `<script>` / `</p><img onerror>` / `javascript:` URL into chat content hoping for HTML rendering when Gmail eventually shows the body | mitigate | renderEmail HTML-escapes every dynamic field even though Resend `text` field is plaintext-only (MAIL-02 + MAIL-03 defense-in-depth for v1.4+ HTML migration). Adversarial test rows "script tag", "img onerror", "javascript url" assert entity-encoded form in output and absence of raw injection vectors |
| T-20-01-03 | Tampering | Visitor injects bidi-override Unicode (`U+202A..U+202E`, `U+2066..U+2069`) to reverse-display visible text in Gmail's plaintext renderer | mitigate | Renderer's stripBidiOverrides helper removes all 9 bidi codepoints from every dynamic field. Adversarial test "rtl bidi" + "bidi strip" assert zero occurrences in output via /[‪-‮⁦-⁩]/u |
| T-20-01-04 | Tampering | Visitor injects null bytes (`\0`) hoping to truncate downstream string parsing (Gmail header parser, log shipper, etc.) | mitigate | stripControlChars strips `\x00` + other C0 control chars except `\t \n \r`. Adversarial test "null bytes" + "null byte strip" assert zero `\0` in output |
| T-20-01-05 | Tampering | Visitor injects CR/LF in referrer (or `country` in extreme cases) to inject extra Subject header lines (RFC 5322 header injection) | mitigate | escapeSubjectComponent applies stripCrLf (additional layer beyond the regex validators that already only accept `[A-Z]{2}` and `[a-z0-9.-]+`). Test "crlf strip subject" verifies |
| T-20-01-06 | Information Disclosure | Visitor types email address / phone / SSN as visitor content; renderer faithfully includes it in body sent to Jack's Gmail | accept | Verbatim transcript reproduction is the product requirement (MAIL-02 — Jack reads every conversation in entirety). No PII filtering planned at v1.3. Subject contains country + referrer-host (operational metadata; NOT visitor content). |
| T-20-01-07 | Tampering | Future contributor adds `Date.now()` to renderer body to insert "rendered at: <timestamp>" line | mitigate | Test "renderer purity" deeply equals `renderEmail(t) === renderEmail(t)` — ANY non-deterministic value (timestamp, UUID) breaks the assertion. Source-text guardrail: grep finds zero `Date.now` / `crypto.randomUUID` / `process.env` references in src/lib/email/render.ts. (Landmine 5 + 9 — non-determinism breaks Resend Idempotency-Key — Plan 20-02 dependency) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/api/email-render.test.ts` exits 0 — all renderer happy + edge tests GREEN
- `pnpm exec vitest run tests/api/email-render.adversarial.test.ts` exits 0 — all MAIL-05 adversarial rows GREEN
- `pnpm exec astro check` exits 0/0/0 — no typecheck regression
- `pnpm exec vitest run tests/api/sse-snapshot.test.ts` exits 0 — D-15 anchor (Phase 20 touches zero chat-surface; informational)
- `pnpm exec vitest run tests/api/anthropic-payload-shape.test.ts` exits 0 — TEST-03 anchor
- `pnpm exec vitest run` exits 0 with PASS count >= 498 + 19 new = 517+ (498 baseline + ~13 render + ~6 adversarial)
- `git diff --stat package.json` shows no changes to dependencies field (MAIL-01 zero-new-runtime-dep lock)
</verification>

<success_criteria>
- 1 NEW source file (`src/lib/email/render.ts`) exists; 2 NEW test files exist
- Renderer is pure: `renderEmail(env, transcript)` returns deeply-equal output across two invocations of the same input (Landmine 5)
- Subject derivation honors D-05, D-06, D-07, D-08 per Per-Task Verification Map
- Body composition honors D-09, D-10, D-11, D-12 per Per-Task Verification Map
- HTML escape + CR/LF strip + bidi strip + null byte strip applied to every dynamic field per MAIL-03
- All 6 MAIL-05 adversarial payload classes covered by it.each rows
- Provenance line `From: chat widget on jackcutrara.com — visitor message follows below this line.` placed below metadata header, separated by blank lines
- `package.json` `dependencies` byte-identical (zero new npm deps)
- D-15 SSE snapshot GREEN; TEST-03 Anthropic payload-shape GREEN; astro check 0/0/0
- Plan 20-02 and Plan 20-03 are unblocked: ResendPayload + RenderEnv types ready for import
</success_criteria>

<output>
After completion, create `.planning/phases/20-email-render-resend-integration/20-01-SUMMARY.md` documenting:
- New file: src/lib/email/render.ts (LOC; key functions: renderEmail, composeSubject, composeBody, deriveCacheLine, sanitizer pipeline)
- New tests: tests/api/email-render.test.ts (count) + tests/api/email-render.adversarial.test.ts (count)
- Test suite drift: PASS/FAIL/SKIP counts before vs after
- Verification of cross-phase anchors: D-15 SSE, TEST-03 Anthropic, package.json deps byte-identical
- Confirmation that ResendPayload + RenderEnv types are exported and ready for Plan 20-02 (sendEmail wrapper) + Plan 20-03 (sendOne substitution) to consume
- Any deviation from sanitizer-pipeline ordering or fixture-builder shape (Rule 3)
</output>
