---
phase: 20-email-render-resend-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email/resend.ts
  - tests/api/email-resend.test.ts
autonomous: true
requirements:
  - MAIL-01
user_setup: []

must_haves:
  truths:
    - "sendEmail(env, payload) is a pure HTTP wrapper around POST https://api.resend.com/emails — no Node deps, no Anthropic, no cloudflare:workers, no chat-delivery import; testable with mocked global fetch"
    - "D-14: explicitly retired by D-17 — no `replayed` Result variant; no `chat.delivery.idempotency_replay` log event. Resend's `idempotency_replay: true` response body flag is unsupported per RESEARCH Drift §1; Layer 1 KV cursor (`delivered:{sid}`) is the sole application-side replay detector via Phase 19's existing `chat.delivery.skipped_already_delivered` log emission."
    - "ResendResult is a 3-variant discriminated union per D-17: { status: 'sent', message_id, attempt } | { status: 'failed_transient', http_status?, error_class?, attempt } | { status: 'failed_terminal', http_status, resend_error?, attempt } — NO replayed variant"
    - "Headers set on every fetch: Authorization: Bearer ${env.RESEND_API_KEY}; Content-Type: application/json; Idempotency-Key: <payload.idempotency_key>; User-Agent: jack-cutrara-portfolio/1.0 (Landmine 4 defense against Resend 403/1010)"
    - "AbortController fires at 10s per attempt; thrown DOMException with name === 'AbortError' caught at the wrapper's catch branch (Landmine 1 — DOMException, NOT plain Error)"
    - "clearTimeout in finally so the dangling setTimeout never escapes the success or error path (Landmine 2)"
    - "Three structured log events emitted per D-16+D-17: chat.delivery.sent (2xx), chat.delivery.retry (5xx + 429 + AbortError), chat.delivery.failed (4xx-except-429); flat-primitive fields only; second arg JSON-parseable by wrangler tail"
    - "Status taxonomy honors D-13: 2xx -> sent; 5xx + 429 -> failed_transient; 4xx-except-429 (incl. 409 Idempotency-Conflict per Landmine 10) -> failed_terminal; AbortError -> failed_transient with error_class=AbortError"
    - "Body shape `text` field present + `html` field ABSENT per MAIL-02; body JSON keys ordered { from, to, reply_to, subject, text } literal so retries within 24h produce byte-identical body for Resend Idempotency-Key (Landmine 9)"
  artifacts:
    - path: "src/lib/email/resend.ts"
      provides: "Pure REST wrapper sendEmail(env, payload) with AbortController + 3-variant Result + 3-event structured logging"
      exports: ["sendEmail", "ResendEnv", "ResendResult"]
      min_lines: 100
    - path: "tests/api/email-resend.test.ts"
      provides: "Unit suite for sendEmail with mocked global fetch + fake timers for AbortController + 3-variant Result coverage + 4xx it.each + header literal assertions"
      min_lines: 200
  key_links:
    - from: "src/lib/email/resend.ts sendEmail"
      to: "https://api.resend.com/emails REST endpoint"
      via: "global fetch() with method POST + Authorization Bearer + Idempotency-Key + User-Agent + JSON body"
      pattern: "fetch\\(['\"]https://api\\.resend\\.com/emails['\"]"
    - from: "src/lib/email/resend.ts ResendResult"
      to: "Plan 20-03 sendOne in chat-delivery.ts"
      via: "discriminated union consumed by wiring layer; Plan 20-03 imports ResendResult + sendEmail"
      pattern: "export type ResendResult"
    - from: "src/lib/email/resend.ts emits chat.delivery.sent / .failed / .retry"
      to: "Workers Logs (operational query: wrangler tail --search 'chat.delivery.sent')"
      via: "console.log/console.error with flat-primitive second-arg object"
      pattern: "console\\.(log|error)\\(['\"]chat\\.delivery\\.(sent|failed|retry)['\"]"
---

<objective>
Wave 1 (parallel with 20-01): Author the pure Resend REST wrapper module `src/lib/email/resend.ts` and its mocked-fetch unit-test file. The wrapper accepts an already-rendered `ResendPayload` (from Plan 20-01's renderer), POSTs it to Resend, parses the response into a 3-variant discriminated `ResendResult`, and emits one of three structured Workers Logs events per outcome. Pure module — zero npm deps, zero coupling to chat-delivery, testable with mocked global fetch.

Purpose: This plan closes MAIL-01 (thin fetch wrapper to Resend with Authorization + Idempotency-Key + retry-with-same-key on 5xx + AbortController + zero new runtime deps). The Result is consumed by Plan 20-03's `sendOne` substitution. D-17 supersedes D-14/D-16: 3 Result variants, 3 log events, NO replayed variant, NO chat.delivery.idempotency_replay event — Layer 1 (`delivered:{sid}` cursor in chat-delivery.ts) is the sole application-side replay detector.

Output: 1 NEW source module (`src/lib/email/resend.ts`), 1 NEW test file (`tests/api/email-resend.test.ts`). No existing files touched. The `scripts/resend-warmup.mjs` (Plan 17-06 commit `0b9d5c5`) is the wire-shape oracle — the new wrapper is structurally a Workers-runtime port with AbortController + Result type + structured logging + User-Agent header added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-email-render-resend-integration/20-CONTEXT.md
@.planning/phases/20-email-render-resend-integration/20-RESEARCH.md
@.planning/phases/20-email-render-resend-integration/20-PATTERNS.md
@.planning/phases/20-email-render-resend-integration/20-VALIDATION.md
@scripts/resend-warmup.mjs
@src/lib/chat-delivery.ts

<interfaces>
<!-- Plan 20-02 reads ResendPayload type-only from Plan 20-01's render.ts. Both plans run parallel in Wave 1; -->
<!-- Plan 20-02 must NOT take a runtime dependency on Plan 20-01 — only a type-import. The two test files mock -->
<!-- their fixtures independently. -->

From src/lib/email/render.ts (Plan 20-01 contract — type-only import):
- `export interface ResendPayload { from: string; to: string; reply_to: string; subject: string; text: string; idempotency_key: string; }`

NEW types Plan 20-02 creates (consumed by Plan 20-03):
- `export interface ResendEnv { RESEND_API_KEY: string; }` — narrowed env shape
- `export type ResendResult = { status: "sent"; message_id: string; attempt: number } | { status: "failed_transient"; http_status?: number; error_class?: string; attempt: number } | { status: "failed_terminal"; http_status: number; resend_error?: string; attempt: number };` — 3-variant discriminated union per D-17
- `export async function sendEmail(env: ResendEnv, payload: ResendPayload, attempt?: number): Promise<ResendResult>` — public API; `attempt` defaults to 1; threaded by caller through retryWithBackoff iterations

Wire-shape oracle (scripts/resend-warmup.mjs:41-64):
- POST `https://api.resend.com/emails`
- Headers: `Authorization: Bearer ${apiKey}`, `Content-Type: application/json`, `Idempotency-Key: warmup/${sessionId}` (Phase 20 uses `transcript/{sid}` instead via payload.idempotency_key)
- Body JSON keys (literal-order ES2015): `{ from, to, reply_to, subject, text }`
- Success body shape: `{ id: string }` (canonical Resend message id)
- Error body shape: `{ name?: string; message?: string; statusCode?: number }`
- Status taxonomy per Resend docs: 2xx success; 400/401/403/409/422 terminal; 429 + 5xx transient
</interfaces>

<patterns>
<!-- Primary analog (wire-shape oracle): scripts/resend-warmup.mjs:41-64 — fetch URL, method, header keys, body field set -->
<!-- Secondary analog (pure-module shape): src/lib/chat-transcripts.ts:1-101 — file-banner + locked constants + types + helpers + public API -->
<!-- Reference implementation guide: 20-RESEARCH.md § Pattern 2 lines 634-735 — sendEmail signature + AbortController + 3-variant Result + structured-log emission -->
<!-- Console-spy test pattern: tests/api/cache-hit-logs.test.ts:14-105 — vi.spyOn(console, "log/error") + restoreAllMocks; tests/api/chat-delivery.test.ts:264-289 -->
<!-- Fake-timer pattern for AbortController test: tests/api/chat-delivery.test.ts:565-572 — vi.useFakeTimers + vi.runAllTimersAsync -->
<!-- Adversarial AbortError mock: 20-RESEARCH.md § Landmine 1 lines 384-398 — `throw new DOMException("aborted", "AbortError")` NOT `throw new Error(...)` -->
<!-- Status code it.each table: 20-PATTERNS.md Group 4 third sub-section — it.each([400, 401, 403, 409, 422]) -->
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 scaffold — create tests/api/email-resend.test.ts with failing-RED stubs covering ALL Per-Task Verification Map rows for Plan 20-02</name>
  <files>tests/api/email-resend.test.ts</files>
  <read_first>
    - .planning/phases/20-email-render-resend-integration/20-VALIDATION.md (READ Per-Task Verification Map rows 73-82 — 10 distinct test name filters; the test file MUST emit `it("<name>", ...)` for each so the locked `pnpm exec vitest run ... -t "<name>"` commands resolve)
    - .planning/phases/20-email-render-resend-integration/20-PATTERNS.md (READ Group 4 third sub-section lines 414-466 — vi.mock + console-spy + fake-timer pattern; 10 mocked-fetch test cases enumerated)
    - .planning/phases/20-email-render-resend-integration/20-RESEARCH.md (READ § Implementation Landmines 1, 2, 4, 9, 10 — DOMException AbortError mock; clearTimeout finally; User-Agent header assertion; deterministic JSON body key ordering; 409 distinct logging)
    - tests/api/cache-hit-logs.test.ts (READ lines 1-105 — vi.spyOn(console, "log/error") pattern; mock-Anthropic vs mock-fetch differences; structured-log event assertion via spy.mock.calls.find)
    - tests/api/chat-delivery.test.ts (READ lines 1-50 + lines 174-205 + lines 264-289 + lines 565-572 — header banner shape; buildTranscript fixture-builder; console-spy beforeEach/afterEach; vi.useFakeTimers + vi.runAllTimersAsync pattern for AbortController test)
    - scripts/resend-warmup.mjs (READ FULL — wire-shape oracle; lines 41-64 are the fetch shape Plan 20-02's wrapper mirrors; tests assert the wrapper's fetch.mock.calls[0] payload byte-shape matches this oracle)
  </read_first>
  <behavior>
    - File COMPILES under `pnpm exec astro check` (TypeScript types resolve from src/lib/email/resend AND src/lib/email/render — Plan 20-01 in parallel)
    - Test stubs FAIL RED on `pnpm exec vitest run` because src/lib/email/resend.ts does not exist yet — failure mode is "Cannot find module" or "sendEmail is not a function"
    - Each test name listed in 20-VALIDATION.md Per-Task Verification Map for Plan 20-02 has an `it("<name>", ...)` block (use `it.todo` if implementation is deferred to Task 2)
  </behavior>
  <action>
    Create `tests/api/email-resend.test.ts` with the following structure:

    HEADER BANNER mirroring tests/api/chat-delivery.test.ts:1-49 — file purpose paragraph; cite Plan 20-02 + MAIL-01 + decision IDs D-13 + D-15 + D-17 (3-variant collapsed) + Landmine 1/2/4/9/10. Note that the module under test does NOT yet exist (RED state).

    IMPORTS: `vitest` (describe, it, expect, vi, beforeEach, afterEach), `sendEmail`, `type ResendEnv`, `type ResendResult` from `../../src/lib/email/resend`, `type ResendPayload` from `../../src/lib/email/render` (will resolve once Plan 20-01 Task 2 lands; Wave-1 parallel coordination via the shared interface).

    CONSTANTS BLOCK: `const SID = "8b0f7f1c-1234-4567-8901-abcdef012345"`; `const ENV: ResendEnv = { RESEND_API_KEY: "test-resend-key" }`; `function buildPayload(overrides?: Partial<ResendPayload>): ResendPayload` returning `{ from: '"Portfolio Chat" <transcripts@mail.jackcutrara.com>', to: "jackcutrara@gmail.com", reply_to: "jackcutrara@gmail.com", subject: "[Portfolio chat] 7 turns from US via direct", text: "From: chat widget on jackcutrara.com — visitor message follows below this line.\n\n>>> visitor:\nHello\n", idempotency_key: \`transcript/${SID}\`, ...overrides }`.

    GLOBAL FETCH MOCK SETUP: `let fetchMock: ReturnType<typeof vi.fn>; beforeEach(() => { fetchMock = vi.fn(); globalThis.fetch = fetchMock as unknown as typeof globalThis.fetch; logSpy = vi.spyOn(console, "log").mockImplementation(() => {}); errorSpy = vi.spyOn(console, "error").mockImplementation(() => {}); }); afterEach(() => { vi.restoreAllMocks(); vi.useRealTimers(); });`. Helper `function mockResolved(status: number, body: unknown): void { fetchMock.mockResolvedValue({ ok: status >= 200 && status < 300, status, json: async () => body }); }`. Helper `function findLog(spy, eventName)` mirrors the pattern from tests/api/chat-delivery.test.ts.

    DESCRIBE GROUPS + IT STUBS (one per Per-Task Verification Map row — exact `it("<name>", ...)` titles):
    - "200 sent": mock 200 + `{ id: "test-msg-id" }`; assert `result.status === "sent"`, `result.message_id === "test-msg-id"`, `result.attempt === 1`; assert `findLog(logSpy, "chat.delivery.sent")` defined with `resend_message_id: "test-msg-id"` field
    - "5xx transient": mock 500; assert `result.status === "failed_transient"`, `result.http_status === 500`; assert `findLog(logSpy, "chat.delivery.retry")` defined
    - "429 transient": mock 429; assert `result.status === "failed_transient"`, `result.http_status === 429`; assert `chat.delivery.retry` log
    - "4xx terminal": `it.each([400, 401, 403, 409, 422])` — mock each status with body `{ name: "validation_error" }`; assert `result.status === "failed_terminal"`, `result.http_status === <code>`, `result.resend_error === "validation_error"`; assert `findLog(errorSpy, "chat.delivery.failed")` defined with `http_status: <code>` (Landmine 10 — 409 logged with `http_status: 409` so it's grep-distinguishable)
    - "abort timeout": use `vi.useFakeTimers()`; `fetchMock.mockImplementation(() => new Promise((_, reject) => setTimeout(() => reject(new DOMException("aborted", "AbortError")), 12000)))`; call `sendEmail` then `await vi.advanceTimersByTimeAsync(11000)`; assert resulting `result.status === "failed_transient"`, `result.error_class === "AbortError"` (Landmine 1 — DOMException not generic Error)
    - "idempotency key header": mock 200; call sendEmail with `payload.idempotency_key = "transcript/${SID}"`; assert `fetchMock.mock.calls[0][1].headers["Idempotency-Key"] === \`transcript/${SID}\``
    - "bearer auth header": assert `fetchMock.mock.calls[0][1].headers["Authorization"] === \`Bearer ${ENV.RESEND_API_KEY}\``
    - "user-agent header" (Landmine 4): assert `fetchMock.mock.calls[0][1].headers["User-Agent"] === "jack-cutrara-portfolio/1.0"`
    - "text field only" (MAIL-02): mock 200; call sendEmail; assert `JSON.parse(fetchMock.mock.calls[0][1].body)` has key `text`, does NOT have key `html`, has keys `{ from, to, reply_to, subject, text }` (5 keys exactly per Landmine 9 — assert `Object.keys(parsedBody).length === 5`)

    Stubs MUST be implementable in Task 2 — write real assertions that depend on the not-yet-existing sendEmail, OR use `it.todo` for the heaviest cases (preferred to write real RED assertions for the wire-shape tests).
  </action>
  <verify>
    <automated>pnpm exec astro check</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/email-resend.test.ts` exists; `wc -l tests/api/email-resend.test.ts` returns >= 200
    - `pnpm exec astro check` exits 0/0/0 (test file typechecks against not-yet-existing render.ts AND resend.ts — both Wave 1 plans coordinate via type-only imports)
    - `pnpm exec vitest run tests/api/email-resend.test.ts` exits non-zero with "Cannot find module" or "sendEmail is not a function" failure mode (RED state)
    - `grep -E 'it\.todo|it\(|it\.each' tests/api/email-resend.test.ts | wc -l` returns >= 10 (one per VALIDATION.md row)
    - `grep -c 'it\.each' tests/api/email-resend.test.ts` returns >= 1 (locks the 4xx it.each row exists — defends 4xx sub-case coverage from being silently weakened to a single it() loop)
    - `grep -E 'status:\s*(400|401|403|409|422)' tests/api/email-resend.test.ts | wc -l` returns >= 5 (locks the 5 4xx sub-case status codes are present in the test file source — structurally locks 4xx coverage instead of merely counting it.each invocations)
    - `grep -F 'DOMException("aborted", "AbortError")' tests/api/email-resend.test.ts` returns >= 1 (Landmine 1 — DOMException mock not Error mock)
    - `grep -F 'jack-cutrara-portfolio/1.0' tests/api/email-resend.test.ts` returns >= 1 (Landmine 4 — User-Agent assertion)
    - `grep -F 'Idempotency-Key' tests/api/email-resend.test.ts` returns >= 2 (header literal assertion + payload field reference)
    - `grep -F 'globalThis.fetch' tests/api/email-resend.test.ts` returns >= 1 (mock-fetch global stub pattern)
    - The full test suite passes WITHOUT this new file — `pnpm exec vitest run --exclude tests/api/email-resend.test.ts` exits 0 (498/0/2 baseline preserved)
  </acceptance_criteria>
  <done>
    Test file exists with stubs for all 10 Per-Task Verification Map rows for Plan 20-02. astro check 0/0/0. File fails RED with module-not-found (intentional). Plan 20-01 Task 2 + Plan 20-03 unblocked.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author src/lib/email/resend.ts pure REST wrapper turning Wave 0 RED tests GREEN</name>
  <files>src/lib/email/resend.ts, tests/api/email-resend.test.ts</files>
  <read_first>
    - tests/api/email-resend.test.ts (READ — own Task 1 output; see exactly which test names + assertions must turn GREEN)
    - scripts/resend-warmup.mjs (READ FULL — wire-shape oracle; lines 41-64 enumerate URL + method + header keys + body JSON keys to mirror byte-identically except for: AbortController added; idempotency_key value `transcript/{sid}` not `warmup/{sid}`; User-Agent added)
    - src/lib/chat-delivery.ts (READ lines 1-37 — file-banner pattern; lines 47-53 — locked constants block style; lines 108-115 — helper try/catch shape; this is the structural template for the new pure module)
    - src/lib/chat-transcripts.ts (READ lines 1-50 — alternative banner shape; the renderer + wrapper share the "Pure module. NO imports from..." block convention)
    - .planning/phases/20-email-render-resend-integration/20-CONTEXT.md (READ <decisions> D-13 + D-15 + D-16 [REVISED] + D-17 lines 94-100 — 3-class HTTP taxonomy, 10s AbortController, 3-event log family per D-17)
    - .planning/phases/20-email-render-resend-integration/20-RESEARCH.md (READ § Pattern 2 lines 634-735 — full reference sendEmail implementation; § Implementation Landmines 1, 2, 3, 4, 9, 10 lines 384-477 — DOMException test mock; clearTimeout finally; replayed variant DROPPED; User-Agent header; deterministic JSON body keys; 409 grep-distinguishable via http_status field)
    - .planning/phases/20-email-render-resend-integration/20-PATTERNS.md (READ Group 1 first sub-section lines 33-127 — wrapper module shape, executor notes for D-17 + Landmines 1/2/4/5/7/9; importantly the "3-variant" Result, NOT 4)
  </read_first>
  <behavior>
    - All `it("<name>", ...)` from Task 1 transition RED -> GREEN (10 distinct test names + 1 it.each row producing 5 sub-cases)
    - sendEmail returns `{ status: "sent", message_id: data.id, attempt }` on 2xx
    - sendEmail returns `{ status: "failed_transient", http_status: status, error_class?, attempt }` on 5xx + 429 + AbortError
    - sendEmail returns `{ status: "failed_terminal", http_status: status, resend_error?: errorBody.name, attempt }` on 400/401/403/409/422 (4xx-except-429)
    - sendEmail emits `console.log("chat.delivery.sent", {...})` on success
    - sendEmail emits `console.log("chat.delivery.retry", {...})` on transient (5xx + 429 + AbortError)
    - sendEmail emits `console.error("chat.delivery.failed", {...})` on terminal (4xx-except-429)
    - AbortController fires at 10s exactly per attempt; thrown DOMException name === "AbortError" caught and converted to `{ status: "failed_transient", error_class: "AbortError", attempt }`
    - clearTimeout invoked in `finally` block — no dangling setTimeout escapes
    - Headers literal: Authorization Bearer + Content-Type application/json + Idempotency-Key (from payload) + User-Agent jack-cutrara-portfolio/1.0
    - Body JSON: 5 keys exactly { from, to, reply_to, subject, text }; html field ABSENT
  </behavior>
  <action>
    Create `src/lib/email/resend.ts` with the following structure:

    HEADER BANNER (mirror src/lib/chat-transcripts.ts:1-29 + src/lib/chat-delivery.ts:1-37): file purpose paragraph naming Phase 20 contract scope; "Owns the entire Phase 20 contract:" block listing MAIL-01; decision IDs honored block citing D-13 (3-class HTTP taxonomy), D-15 (AbortController 10s), D-16 (event family) **as REVISED by D-17 (3 variants, 3 events)**, Landmine 1/2/4/9/10 acknowledgements; "Pure module. NO imports from..." block listing @anthropic-ai/sdk, cloudflare:workers, src/prompts/, src/pages/, src/scripts/chat.ts, src/lib/chat-delivery.ts (wrapper is upstream of chat-delivery; no reverse dep).

    LOCKED CONSTANTS BLOCK (Plan 20-02 exports for tests + Plan 20-03 reference): `export const RESEND_URL = "https://api.resend.com/emails";`; `export const FETCH_TIMEOUT_MS = 10_000;` (D-15); `export const USER_AGENT = "jack-cutrara-portfolio/1.0";` (Landmine 4).

    PUBLIC TYPES: `export interface ResendEnv { RESEND_API_KEY: string; }`; `export type ResendResult = { status: "sent"; message_id: string; attempt: number } | { status: "failed_transient"; http_status?: number; error_class?: string; attempt: number } | { status: "failed_terminal"; http_status: number; resend_error?: string; attempt: number };` (3 variants per D-17). Type-only `import type { ResendPayload } from "./render";` (Plan 20-01 type contract).

    FILE-LOCAL HELPER: `function extractSidFromIdempotencyKey(key: string): string` — splits on `/` returning the second segment; logged in every event so Workers Logs queries are session-scoped.

    PUBLIC API: `export async function sendEmail(env: ResendEnv, payload: ResendPayload, attempt = 1): Promise<ResendResult>`. Implementation per 20-RESEARCH.md § Pattern 2 lines 634-735 with these exact specifics:
    - Setup: `const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), FETCH_TIMEOUT_MS);`
    - Body destructure: `const { idempotency_key, ...body } = payload;` to extract idempotency_key for header use without leaking into body
    - Fetch call: POST `RESEND_URL`; headers literal `{ "Authorization": \`Bearer ${env.RESEND_API_KEY}\`, "Content-Type": "application/json", "Idempotency-Key": idempotency_key, "User-Agent": USER_AGENT }`; body `JSON.stringify(body)`; signal `controller.signal`
    - Response parsing: if `response.ok` -> read `data: { id: string } = await response.json()` -> emit `console.log("chat.delivery.sent", { sid, resend_message_id: data.id, attempt })` -> return `{ status: "sent", message_id: data.id, attempt }`
    - Best-effort error body parse: `let errorClass: string | undefined; try { const errBody = await response.json() as { name?: string }; errorClass = errBody.name; } catch {}`
    - Transient (status === 429 || status >= 500): emit `console.log("chat.delivery.retry", { sid, http_status: response.status, attempt, backoff_ms: null })` -> return `{ status: "failed_transient", http_status: response.status, error_class: errorClass, attempt }`
    - Terminal (else): emit `console.error("chat.delivery.failed", { sid, http_status: response.status, error_class: errorClass ?? "unknown", attempt })` -> return `{ status: "failed_terminal", http_status: response.status, resend_error: errorClass, attempt }`
    - Catch block: `if (err instanceof DOMException && err.name === "AbortError")` -> emit `chat.delivery.retry` with `http_status: null, error_class: "AbortError"` -> return `{ status: "failed_transient", error_class: "AbortError", attempt }`. Else network error: emit `chat.delivery.retry` with `error_class: err instanceof Error ? err.constructor.name : "Error"` -> return `{ status: "failed_transient", error_class, attempt }` (network errors also transient per D-15)
    - Finally block (Landmine 2): `clearTimeout(timeoutId);`

    PURITY GUARDRAILS: NO `Date.now()` (no timestamp emission — attempt counter is the lifecycle indicator), NO env reads beyond ResendEnv, NO crypto. NO chat-delivery import (the wrapper is upstream).

    Then update Task 1 stubs to real assertions if any were left as it.todo. Run `pnpm exec vitest run tests/api/email-resend.test.ts` until ALL tests GREEN.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/api/email-resend.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/email/resend.ts` exists; `wc -l src/lib/email/resend.ts` returns >= 100
    - `pnpm exec vitest run tests/api/email-resend.test.ts` exits 0; all 10+ test names pass; 4xx it.each produces 5 sub-passes
    - `grep -c 'export async function sendEmail' src/lib/email/resend.ts` returns 1
    - `grep -c 'export type ResendResult' src/lib/email/resend.ts` returns 1
    - `grep -c 'export interface ResendEnv' src/lib/email/resend.ts` returns 1
    - `grep -c 'replayed' src/lib/email/resend.ts` returns 0 (D-17 — replayed variant DROPPED)
    - `grep -c 'idempotency_replay' src/lib/email/resend.ts` returns 0 (D-17 — chat.delivery.idempotency_replay event DROPPED)
    - `grep -c 'chat\.delivery\.sent' src/lib/email/resend.ts` returns >= 1
    - `grep -c 'chat\.delivery\.failed' src/lib/email/resend.ts` returns >= 1
    - `grep -c 'chat\.delivery\.retry' src/lib/email/resend.ts` returns >= 1
    - `grep -c 'jack-cutrara-portfolio/1.0' src/lib/email/resend.ts` returns >= 1 (Landmine 4)
    - `grep -E 'finally\s*\{[^}]*clearTimeout' src/lib/email/resend.ts | wc -l` returns >= 1 (Landmine 2 — clearTimeout in finally block)
    - `grep -E 'instanceof\s+DOMException' src/lib/email/resend.ts | wc -l` returns >= 1 (Landmine 1 — DOMException check)
    - `grep -E 'AbortController' src/lib/email/resend.ts | wc -l` returns >= 1
    - `grep -F 'cloudflare:workers' src/lib/email/resend.ts | grep -v '^ *//\|^ *\*' | wc -l` returns 0 (no cloudflare import)
    - `grep -F '@anthropic-ai/sdk' src/lib/email/resend.ts | grep -v '^ *//\|^ *\*' | wc -l` returns 0
    - `grep -F 'src/lib/chat-delivery' src/lib/email/resend.ts | wc -l` returns 0 (wrapper is upstream of chat-delivery)
    - `grep -F 'html:' src/lib/email/resend.ts | wc -l` returns 0 (MAIL-02 — no html field anywhere)
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm exec vitest run tests/api/sse-snapshot.test.ts` exits 0 (D-15 anchor preserved)
    - `pnpm exec vitest run tests/api/anthropic-payload-shape.test.ts` exits 0 (TEST-03 anchor preserved)
    - `git diff --stat package.json` shows no changes to dependencies (MAIL-01 zero-new-runtime-dep lock — REST via fetch)
  </acceptance_criteria>
  <done>
    Pure REST wrapper shipped. All 10+ resend tests GREEN. astro check 0/0/0. Wrapper exposes `sendEmail`, `ResendEnv`, `ResendResult` for Plan 20-03's substitution. Cross-phase forward-defense: D-15 SSE GREEN, TEST-03 Anthropic GREEN, package.json deps byte-identical (zero new npm deps — REST via global fetch). 3-variant Result + 3-event family per D-17 (NOT 4).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEND_API_KEY (Wrangler secret) -> Authorization header | Secret read from env; never logged |
| ResendPayload (rendered by Plan 20-01) -> HTTP request body | Body trusted as already-sanitized by renderer (defense-in-depth assumption); wrapper does NOT re-sanitize |
| Resend API (api.resend.com) -> response body parsing | Untrusted JSON; best-effort parse with try/catch fallback to `errorClass = undefined` |
| AbortController signal -> fetch cancellation | Internal to wrapper; 10s timeout per attempt bounds wall-clock per session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | Information Disclosure | RESEND_API_KEY accidentally logged via console.log of headers object | mitigate | Wrapper NEVER logs the headers object as a whole. Each `chat.delivery.{sent,failed,retry}` event logs only flat-primitive fields { sid, resend_message_id?, http_status?, error_class?, attempt, backoff_ms? }. Source-text test: grep for `console\.(log\|error)` followed by `headers\|RESEND_API_KEY\|Authorization` finds zero matches |
| T-20-02-02 | Tampering | Future contributor adds `idempotencyKey` field to body alongside the `Idempotency-Key` header | mitigate | Wrapper destructures `const { idempotency_key, ...body } = payload;` so idempotency_key NEVER reaches the body JSON. Test "text field only" asserts body has exactly 5 keys { from, to, reply_to, subject, text } — adding idempotencyKey would push count to 6 and fail the assertion |
| T-20-02-03 | Denial of Service | Hung Resend connection consumes the entire 30s scheduled-handler budget on one session, starves the rest of the batch | mitigate | AbortController fires at 10s per attempt (D-15). 3 retries × 10s = 30s worst-case wall-clock per session. Per-session try/catch in promoteOne (Plan 20-03 / Phase 19 inherited) isolates one bad session from the rest of the 50-session batch. Test "abort timeout" verifies the controller fires at 10s |
| T-20-02-04 | Repudiation | Visitor session arrives in Inbox without distinguishing audit trail in Workers Logs | mitigate | Every successful send emits `chat.delivery.sent` with `sid` + `resend_message_id` so wrangler tail can trace each email back to its session. Test "200 sent" asserts the event payload includes both fields |
| T-20-02-05 | Tampering | Resend returns 200 with malformed body (no `id` field, e.g., bug or upstream proxy injection) | accept | Wrapper does `await response.json() as { id: string }`. If `data.id` is undefined, `result.message_id === undefined` propagates to Plan 20-03 which writes `delivered:{sid}.resend_message_id: undefined`. Layer 1 KV cursor still prevents duplicate sends. v1.3 acceptable. Re-evaluate if this materializes in production. |
| T-20-02-06 | Tampering | Future contributor reads CONTEXT.md D-15 conflated "30s ceiling" and adds a global timeout / batch abort at 30s, breaking per-session isolation (Landmine 8) | mitigate | This file's `FETCH_TIMEOUT_MS = 10_000` constant is per-attempt. NO batch-level abort exists. Header-banner comment block cites Landmine 8 explicitly. Acceptance criterion: grep `setTimeout.*abortBatch` in src/lib/email/resend.ts returns 0 |
| T-20-02-07 | Information Disclosure | 409 Idempotency-Conflict (renderer non-determinism bug) gets logged as plain `chat.delivery.failed` indistinguishable from validation 422 | mitigate | Per Landmine 10, the `failed_terminal` Result includes `http_status: 409`. Workers Logs filter via `wrangler tail --search "chat.delivery.failed" | grep '"http_status":409'`. Test 4xx it.each row for status 409 asserts log carries http_status: 409 |
</threat_model>

<verification>
- `pnpm exec vitest run tests/api/email-resend.test.ts` exits 0 — all 10+ wrapper tests GREEN
- `pnpm exec astro check` exits 0/0/0 — no typecheck regression
- `pnpm exec vitest run tests/api/sse-snapshot.test.ts` exits 0 — D-15 anchor (Phase 20 touches zero chat-surface; informational)
- `pnpm exec vitest run tests/api/anthropic-payload-shape.test.ts` exits 0 — TEST-03 anchor
- `pnpm exec vitest run` exits 0 with PASS count >= 498 (Phase 19 baseline) + 19 (Plan 20-01) + 14 (this plan) = ~531+
- `git diff --stat package.json` shows no changes to dependencies (MAIL-01 zero-new-runtime-dep lock)
</verification>

<success_criteria>
- 1 NEW source file (`src/lib/email/resend.ts`) exists; 1 NEW test file exists
- ResendResult is a 3-variant discriminated union (sent / failed_transient / failed_terminal) per D-17 — NO replayed variant
- 3 structured Workers Logs events emitted: chat.delivery.sent / chat.delivery.failed / chat.delivery.retry — NO chat.delivery.idempotency_replay
- 3-class HTTP taxonomy honored: 2xx -> sent; 5xx + 429 -> failed_transient; 4xx-except-429 (incl. 409) -> failed_terminal
- AbortController fires at 10s per attempt; thrown DOMException with name === AbortError caught (Landmine 1)
- clearTimeout invoked in finally block (Landmine 2)
- User-Agent header `jack-cutrara-portfolio/1.0` set on every fetch (Landmine 4)
- Body has exactly 5 keys { from, to, reply_to, subject, text }; html field ABSENT (MAIL-02)
- Wire shape byte-identical to scripts/resend-warmup.mjs:41-64 except for AbortController + Result type + structured logging + User-Agent
- `package.json` `dependencies` byte-identical (MAIL-01 zero-new-runtime-dep lock — REST via global fetch, no SDK install)
- D-15 SSE snapshot GREEN; TEST-03 Anthropic payload-shape GREEN; astro check 0/0/0
- Plan 20-03 unblocked: sendEmail + ResendEnv + ResendResult exported and ready for substitution
</success_criteria>

<output>
After completion, create `.planning/phases/20-email-render-resend-integration/20-02-SUMMARY.md` documenting:
- New file: src/lib/email/resend.ts (LOC; key exports: sendEmail, ResendEnv, ResendResult, RESEND_URL, FETCH_TIMEOUT_MS, USER_AGENT)
- New tests: tests/api/email-resend.test.ts (count; including 4xx it.each sub-cases)
- Test suite drift: PASS/FAIL/SKIP counts before vs after
- Verification of cross-phase anchors: D-15 SSE, TEST-03 Anthropic, package.json deps byte-identical
- Confirmation that 3-variant Result + 3-event family per D-17 ships (NO replayed variant, NO chat.delivery.idempotency_replay event)
- Wire-shape oracle reference: byte-identical to scripts/resend-warmup.mjs:41-64 except for documented additions
- Any deviation from D-17 spec (Rule 3)
</output>
