---
phase: 20-email-render-resend-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
files_modified:
  - src/lib/chat-delivery.ts
  - wrangler.jsonc
  - tests/api/chat-delivery.test.ts
  - tests/build/chat-delivery-send-site.test.ts
  - tests/build/wrangler-dry-run-shape.test.ts
  - tests/build/wrangler-cron-shape.test.ts
autonomous: true
requirements:
  - MAIL-01
  - MAIL-02
  - MAIL-03
  - MAIL-04
  - MAIL-05

must_haves:
  truths:
    - "src/lib/chat-delivery.ts sendOne substitutes the Phase 19 throw stub at line 183 with: render -> sendEmail wrapper call -> Result-translation (sent -> return message_id; failed_transient -> throw for retryWithBackoff; failed_terminal -> throw for terminal-bubble per RESEARCH § Pattern 3)"
    - "DRY_RUN=\"1\" branch in sendOne stays BYTE-IDENTICAL to Phase 19 — rollback runway per D-03; the chat.delivery.dry_run envelope log emission preserved exactly; comment block added explaining 'rollback runway; do not remove as dead code'"
    - "DeliveredMarker interface (lines 66-73) extends additively with `resend_message_id: string` field; schema v: 1 unchanged (Phase 19 D-09/D-10 additive-extension lock); promoteOne step-4 PUT (lines 268-281) populates the new field on the value object"
    - "promoteOne step-4 kv.put options stay { expirationTtl: DELIVERED_TTL_SECONDS } EXACTLY — NO metadata field (Landmine 7 / D-11 lock; idempotency cursor is a hint, not a list-surface)"
    - "wrangler.jsonc vars.DRY_RUN flips '1' -> '0' as the canonical CRON-04 -> MAIL live-toggle per D-01 atomic single-deploy commit; no other wrangler.jsonc keys touched"
    - "tests/build/wrangler-cron-shape.test.ts existing assertion of `vars.DRY_RUN === \"1\"` is REWRITTEN to `=== \"0\"` AT THE SAME COMMIT as the wrangler.jsonc flip — otherwise the existing build test goes RED"
    - "NEW tests/build/chat-delivery-send-site.test.ts source-text guard locks: (a) sendEmail import wired, (b) renderEmail import wired, (c) Phase 19 throw stub send_not_implemented_in_phase_19 GONE, (d) DRY_RUN=='1' branch STILL PRESENT (rollback runway), (e) chat.delivery.dry_run envelope log STILL PRESENT (rollback runway log)"
    - "NEW tests/build/wrangler-dry-run-shape.test.ts focused Phase 20 attribution test locks: (a) vars.DRY_RUN === '0' at phase close, (b) triggers.crons === ['0 * * * *'] (forward-defense against UAT Step 4 operator forgetting to revert the * * * * * UAT cron flip — Pitfall 6 inherited from Phase 19)"
    - "EXTENDED tests/api/chat-delivery.test.ts adds new test cases per D-17 collapsed Result wiring: (1) DRY_RUN='1' envelope byte-identical, (2) DRY_RUN='0' calls sendEmail, (3) sent -> delivered:{sid}.resend_message_id populated + chat.delivery.sent emitted, (4) failed_transient -> retry harness fires 3x, (5) failed_terminal -> chat.delivery.failed emitted + live: NOT deleted + delivered: NOT written, (6) pre-existing skipped_already_delivered test still GREEN (Layer 1 IS the application-side replay detector)"
  artifacts:
    - path: "src/lib/chat-delivery.ts"
      provides: "Phase 20 wiring: sendOne substitution + DeliveredMarker.resend_message_id additive extension + promoteOne step-4 PUT site populates new field"
      contains: "import { sendEmail"
    - path: "wrangler.jsonc"
      provides: "Production live-mail toggle: vars.DRY_RUN === '0'"
      contains: "\"DRY_RUN\": \"0\""
    - path: "tests/api/chat-delivery.test.ts"
      provides: "Extended unit suite covering Phase 20 sendOne wiring per D-17 collapsed 3-variant Result"
      contains: "vi.mock(\"../../src/lib/email/resend\""
    - path: "tests/build/chat-delivery-send-site.test.ts"
      provides: "Source-text forward-defense locking sendEmail wiring + rollback runway preservation"
      min_lines: 30
    - path: "tests/build/wrangler-dry-run-shape.test.ts"
      provides: "Source-text guard locking DRY_RUN='0' phase-close state + cron revert (Pitfall 6 defense)"
      min_lines: 25
  key_links:
    - from: "src/lib/chat-delivery.ts sendOne"
      to: "src/lib/email/resend.ts sendEmail"
      via: "named import + call: const result = await sendEmail(env, payload)"
      pattern: "import\\s*\\{[^}]*\\bsendEmail\\b[^}]*\\}\\s*from\\s*['\"]\\./email/resend['\"]"
    - from: "src/lib/chat-delivery.ts sendOne"
      to: "src/lib/email/render.ts renderEmail"
      via: "named import + call: const payload = renderEmail(env, transcript)"
      pattern: "import\\s*\\{[^}]*\\brenderEmail\\b[^}]*\\}\\s*from\\s*['\"]\\./email/render['\"]"
    - from: "src/lib/chat-delivery.ts promoteOne step-4 PUT site (lines 268-281)"
      to: "DeliveredMarker.resend_message_id field on the value JSON"
      via: "value.resend_message_id = sendResult.message_id (return value from sendOne extended to { message_id })"
      pattern: "resend_message_id\\s*:"
    - from: "wrangler.jsonc vars.DRY_RUN"
      to: "tests/build/wrangler-cron-shape.test.ts AND tests/build/wrangler-dry-run-shape.test.ts source-text guard"
      via: "build-time JSONC parse + literal value assertion"
      pattern: "DRY_RUN[^\"]*\"\\s*:\\s*\"0\""
---

<objective>
Wave 2 (depends on Wave 1: Plan 20-01 renderer + Plan 20-02 wrapper): Wire the renderer + wrapper into the cron sweep by substituting the Phase 19 throw stub at `src/lib/chat-delivery.ts` line 183 with a render -> wrapper-call -> Result-translation flow. Flip `wrangler.jsonc vars.DRY_RUN` from `"1"` to `"0"` (the locked single-line edit per D-01 atomic-deploy commit). Extend `tests/api/chat-delivery.test.ts` with the 6 new wiring test cases per D-17 collapsed Result. Add two source-text forward-defense build tests (`chat-delivery-send-site.test.ts`, `wrangler-dry-run-shape.test.ts`) per Claude's Discretion + RESEARCH OQ-2 recommendation. Update existing `tests/build/wrangler-cron-shape.test.ts` from `DRY_RUN === "1"` to `=== "0"` at the same commit as the wrangler flip (otherwise it goes RED).

Purpose: This is the integration plan that turns Phase 19's DRY_RUN dry-run sweep into Phase 20's live-mail flow. After this plan + the deploy gate (Plan 20-04), the next cron tick against any KV session whose `last_activity_at` is older than 2h will: (a) call sendOne with DRY_RUN now `"0"`, (b) renderEmail composes payload, (c) sendEmail POSTs to Resend with Idempotency-Key, (d) on success delivered:{sid}.resend_message_id captures the Resend id, (e) live:{sid} gets deleted. The DRY_RUN=`"1"` branch stays in source as the rollback runway per D-03 (one-line revert of wrangler.jsonc reverts the entire Phase 20 behavior; no code revert needed).

Output: 2 EDIT files (src/lib/chat-delivery.ts, wrangler.jsonc); 1 EXTEND test (tests/api/chat-delivery.test.ts); 2 NEW build tests; 1 EDIT existing build test (tests/build/wrangler-cron-shape.test.ts updated DRY_RUN expectation). The atomic git commit graph during execution stays granular for incremental verification + rollback granularity, but the operator's eventual `git push origin main` (Plan 20-04 deploy gate) ships them as one push event per D-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-email-render-resend-integration/20-CONTEXT.md
@.planning/phases/20-email-render-resend-integration/20-RESEARCH.md
@.planning/phases/20-email-render-resend-integration/20-PATTERNS.md
@.planning/phases/20-email-render-resend-integration/20-VALIDATION.md
@.planning/phases/20-email-render-resend-integration/20-01-SUMMARY.md
@.planning/phases/20-email-render-resend-integration/20-02-SUMMARY.md
@src/lib/chat-delivery.ts
@src/lib/email/render.ts
@src/lib/email/resend.ts
@wrangler.jsonc
@tests/api/chat-delivery.test.ts
@tests/build/wrangler-cron-shape.test.ts
@tests/build/append-turn-call-site.test.ts

<interfaces>
<!-- Plan 20-03 imports the contracts shipped by Plan 20-01 (renderer) + Plan 20-02 (wrapper). -->
<!-- Wave 2 depends on Wave 1's source files existing — typecheck would fail otherwise. -->

From src/lib/email/render.ts (Plan 20-01 close):
- `export function renderEmail(env: RenderEnv, transcript: ChatTranscript): ResendPayload`
- `export interface RenderEnv { CHAT_RECIPIENT_EMAIL: string; CHAT_SENDER_EMAIL: string; CHAT_REPLY_TO_EMAIL: string; }`
- `export interface ResendPayload { from: string; to: string; reply_to: string; subject: string; text: string; idempotency_key: string; }`

From src/lib/email/resend.ts (Plan 20-02 close):
- `export async function sendEmail(env: ResendEnv, payload: ResendPayload, attempt?: number): Promise<ResendResult>`
- `export interface ResendEnv { RESEND_API_KEY: string; }`
- `export type ResendResult = { status: "sent"; message_id: string; attempt: number } | { status: "failed_transient"; ... } | { status: "failed_terminal"; ... };`

Phase 20-03 EDITS to src/lib/chat-delivery.ts:
- (a) ADD imports `import { renderEmail } from "./email/render";` and `import { sendEmail } from "./email/resend";` at the top of the file (alongside existing chat-transcripts type-only import at line 39)
- (b) WIDEN DeliveryEnv interface (lines 84-95) to ALSO carry RESEND_API_KEY (read by sendEmail wrapper). Plan 19-02 has CHAT_RECIPIENT_EMAIL?, CHAT_SENDER_EMAIL?, CHAT_REPLY_TO_EMAIL? as optional — Plan 20-03 keeps them optional in the type but the live path requires them populated. Add `RESEND_API_KEY?: string` for parity. Actually: per RenderEnv contract, those three are REQUIRED (string, not string | undefined) — narrowing happens at sendOne entry: throw a `failed_terminal`-class error if any is undefined under DRY_RUN=="0".
- (c) ADD `resend_message_id: string` field to the DeliveredMarker interface (lines 66-73). Schema `v: 1` UNCHANGED (additive extension per Phase 19 D-09 lock).
- (d) WIDEN sendOne return type from `Promise<void>` to `Promise<{ message_id: string }>` so promoteOne can read the message_id.
- (e) REWRITE sendOne body per RESEARCH § Pattern 3 lines 743-806:
  - DRY_RUN==="1" branch BYTE-IDENTICAL to Phase 19 lines 167-181 EXCEPT for: return type widening means a sentinel return `{ message_id: "dry-run-no-id" }` after the `console.log("chat.delivery.dry_run", ...)` — replaces the Phase 19 bare `return;`
  - DRY_RUN!=="1" branch: `const payload = renderEmail(env as RenderEnv, transcript); const result = await sendEmail(env as ResendEnv, payload);` → on `result.status === "sent"` return `{ message_id: result.message_id }`; on `failed_transient` throw `new Error(\`resend_transient_${result.http_status ?? result.error_class ?? "unknown"}\`)` (caught by retryWithBackoff at line 264); on `failed_terminal` throw `new Error(\`resend_terminal_${result.http_status}\`)` (also bubbles through retryWithBackoff; net 3x log noise on terminal — acceptable per RESEARCH refinement note line 806)
- (f) UPDATE promoteOne step-4 PUT site (lines 268-281) so the value object includes `resend_message_id: sendResult.message_id` — capture sendResult by changing `await retryWithBackoff(...)` at line 264 to `const sendResult = await retryWithBackoff(...)`. The `kv.put` OPTIONS stay byte-identical (`{ expirationTtl: DELIVERED_TTL_SECONDS }` — NO metadata field per Landmine 7 / D-11)
</interfaces>

<patterns>
<!-- Substitution shape reference: 20-RESEARCH.md § Pattern 3 lines 743-806 — sendOne return type widening + Result-translation -->
<!-- Wrangler edit reference: 20-PATTERNS.md Group 3 lines 282-300 — single-line value flip; no other keys touched -->
<!-- DeliveredMarker additive extension reference: 20-PATTERNS.md Group 2 lines 235-265 — schema v:1 unchanged -->
<!-- Test extension reference: 20-PATTERNS.md Group 5 lines 469-514 — vi.mock("../../src/lib/email/resend", ...) -->
<!-- Source-text guard reference: 20-PATTERNS.md Group 6 lines 519-559 (chat-delivery-send-site) + lines 562-597 (wrangler-dry-run-shape) -->
<!-- Existing wrangler-cron-shape.test.ts (line ~49) DRY_RUN === "1" assertion: 20-PATTERNS.md note lines 591-593 — Option A: UPDATE in place -->
<!-- Existing append-turn-call-site.test.ts (Plan 18-07): readFileSync + regex invariant pattern — chat-delivery-send-site.test.ts mirrors structurally -->
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 scaffold — extend tests/api/chat-delivery.test.ts with new wiring test stubs + author NEW tests/build/chat-delivery-send-site.test.ts and tests/build/wrangler-dry-run-shape.test.ts as RED</name>
  <files>tests/api/chat-delivery.test.ts, tests/build/chat-delivery-send-site.test.ts, tests/build/wrangler-dry-run-shape.test.ts</files>
  <read_first>
    - tests/api/chat-delivery.test.ts (READ FULL — see existing 685+ LOC battery; identify GROUP D pattern lines 493-583 to understand the existing dry_run gate test that gets REWRITTEN; identify the buildTranscript helper lines 174-205 + buildEnv helper + console-spy beforeEach pattern lines 264-272; the new test cases EXTEND this file in-place by appending new it() rows in a new describe block GROUP F)
    - tests/build/append-turn-call-site.test.ts (READ FULL — Plan 18-07 source-text forward-defense pattern; readFileSync + join + describe + it with regex matchers; chat-delivery-send-site.test.ts mirrors this structurally)
    - tests/build/wrangler-cron-shape.test.ts (READ FULL — Plan 19-04 source-text guard; the existing assertion at ~line 49 `cfg.vars.DRY_RUN === "1"` will be UPDATED to `=== "0"` in Task 2 alongside the wrangler.jsonc flip; the new wrangler-dry-run-shape.test.ts is the focused Phase 20 attribution sibling)
    - tests/build/_helpers/parse-jsonc.ts (READ — string-literal-aware JSONC tokenizer; both wrangler shape tests use this helper)
    - .planning/phases/20-email-render-resend-integration/20-VALIDATION.md (READ Per-Task Verification Map rows 83-89 + 91 — the 6 wiring test names + 2 build-defense names; locked vitest -t filters)
    - .planning/phases/20-email-render-resend-integration/20-PATTERNS.md (READ Group 5 lines 469-514 — chat-delivery test extension shape; Group 6 lines 519-597 — both source-text guards)
    - .planning/phases/20-email-render-resend-integration/20-RESEARCH.md (READ § Validation Architecture lines 343-353 — wiring test rows; § Implementation Landmine 7 lines 441-447 — KV.put metadata acceptance criterion)
  </read_first>
  <behavior>
    - tests/api/chat-delivery.test.ts gains a new describe block ("GROUP F — Phase 20 sendOne substitution wiring (D-17 collapsed Result)") with 6 new it() stubs covering the 6 wiring rows from VALIDATION.md
    - The PHASE 19 test at lines 493-583 GROUP D's `dry_run gate (env.DRY_RUN !== '1' throws)` test name MUST be RENAMED + REWRITTEN — the throw stub is GONE under Plan 20-03; new behavior is "DRY_RUN='0' calls sendEmail wrapper". For Task 1: ADD a TODO comment above the existing test marking it for rewrite in Task 2 — do NOT delete the test in Task 1 to avoid losing GREEN coverage during Wave 0
    - vi.mock setup at the top of tests/api/chat-delivery.test.ts mocks `../../src/lib/email/resend` to return a controlled `sendEmail` spy: `vi.mock("../../src/lib/email/resend", () => ({ sendEmail: vi.fn() }))`. Each new it() configures `vi.mocked(sendEmail).mockResolvedValue({...})` per scenario
    - tests/build/chat-delivery-send-site.test.ts NEW file with 5 invariants per Plan 20-PATTERNS.md Group 6 first sub-section
    - tests/build/wrangler-dry-run-shape.test.ts NEW file with 2 invariants per 20-PATTERNS.md Group 6 second sub-section (asserts `cfg.vars.DRY_RUN === "0"` and `cfg.triggers.crons === ["0 * * * *"]`)
    - Build-test stubs FAIL RED because: (a) chat-delivery.ts not yet substituted (still has throw stub `send_not_implemented_in_phase_19` per Plan 19); (b) wrangler.jsonc still has DRY_RUN === "1"
    - tests/api/chat-delivery.test.ts new wiring stubs FAIL RED because the substitution hasn't landed
  </behavior>
  <action>
    EXTEND `tests/api/chat-delivery.test.ts` in-place:
    - At top of file (alongside existing imports lines 34-49) add `vi.mock("../../src/lib/email/resend", () => ({ sendEmail: vi.fn() }));` AND `vi.mock("../../src/lib/email/render", () => ({ renderEmail: vi.fn().mockReturnValue({ from: "from", to: "to", reply_to: "rt", subject: "subj", text: "text", idempotency_key: "transcript/test-sid" }) }));` — vi.mock is hoisted; placement at file top is mandatory
    - Import the mocked functions: `import { sendEmail } from "../../src/lib/email/resend";` (now the spy) and `import { renderEmail } from "../../src/lib/email/render";`
    - Add a TODO comment above the existing GROUP D `dry_run gate (env.DRY_RUN !== '1' throws)` test (lines ~493) marking it for rewrite in Task 2: `// PHASE 20 Plan 20-03 Task 2: this test is REWRITTEN to assert sendEmail call instead of throw stub. Do NOT delete in Task 1 (RED coverage protection).`
    - APPEND a new describe block at end of file: `describe("GROUP F — Phase 20 sendOne substitution wiring (D-17 collapsed 3-variant Result)", () => { ... })`. Inside, console-spy beforeEach/afterEach mirroring lines 264-272. Then 6 new it() stubs:
      - `it("dry_run preserves runway", ...)` — set DRY_RUN='1'; seed live: with stale; run deliverDue; assert chat.delivery.dry_run log byte-identical to Phase 19 envelope shape (sid, to, from, reply_to, msg_count, truncated, country, referrer_host, dry_run: true); assert sendEmail spy NOT called; assert delivered:{sid} written with dry_run: true (forward-defense for the rollback runway per D-03)
      - `it("live calls sendEmail", ...)` — set DRY_RUN='0'; mock sendEmail to return { status: "sent", message_id: "test-msg-id", attempt: 1 }; seed live: with stale; run deliverDue; assert vi.mocked(sendEmail).mock.calls.length === 1; assert the payload arg equals the renderEmail mock return value
      - `it("delivered marker resend_message_id", ...)` — set DRY_RUN='0'; mock sendEmail to return sent; seed + run; assert kv.get(`delivered:${sid}`) returns DeliveredMarker with `dry_run: false` AND `resend_message_id: "test-msg-id"` AND `v: 1` (additive extension confirmed); assert chat.delivery.sent log emitted from inside sendEmail mock context (the wrapper's log emission tested in Plan 20-02; here we test the wiring captures message_id correctly)
      - `it("failed_transient retries", ...)` — set DRY_RUN='0'; mock sendEmail to return { status: "failed_transient", http_status: 500, attempt: 1 } on EVERY call; use vi.useFakeTimers + vi.runAllTimersAsync (per existing pattern lines 565-572); run deliverDue; assert vi.mocked(sendEmail).mock.calls.length === MAX_SEND_ATTEMPTS (3); assert delivered:{sid} NOT written; assert chat.delivery.failed eventually emitted (after 3 failed attempts the catch at promoteOne lines 288-296 emits chat.delivery.failed); use vi.useRealTimers afterward
      - `it("failed_terminal logs and skips", ...)` — set DRY_RUN='0'; mock sendEmail to return { status: "failed_terminal", http_status: 422, resend_error: "validation_error", attempt: 1 }; seed + run; assert delivered:{sid} NOT written; assert live:{sid} STILL exists in mockKV (NOT deleted — Plan 20-03 substitution preserves Phase 19 catch behavior); assert chat.delivery.failed log emitted with http_status: 422 (from sendOne's translation throw caught by promoteOne)
      - `it("delivered marker no metadata" Landmine 7)` — set DRY_RUN='0'; mock sendEmail to return sent; spy on mockKV.put using vi.spyOn; seed + run; assert mockKV.put third-arg equals exactly `{ expirationTtl: DELIVERED_TTL_SECONDS }` (no metadata key — D-11 / Landmine 7 lock); the existing Phase 19 test "promoteOne PUT options no metadata" should also still pass byte-identical
    - All 6 stubs FAIL RED in Task 1 because src/lib/chat-delivery.ts still has the throw stub.

    CREATE `tests/build/chat-delivery-send-site.test.ts` mirroring tests/build/append-turn-call-site.test.ts:
    - HEADER: imports describe/it/expect/readFileSync/join; `const src = readFileSync(join(process.cwd(), "src/lib/chat-delivery.ts"), "utf8");`
    - DESCRIBE: `"Plan 20-03 forward-defense: src/lib/chat-delivery.ts sendOne substitution wired + DRY_RUN='1' rollback runway preserved (D-03)"`
    - 5 invariants (per 20-PATTERNS.md Group 6 first sub-section):
      1. `it("Invariant A: sendOne imports sendEmail from ./email/resend", ...)` — `expect(src).toMatch(/import\s*\{[^}]*\bsendEmail\b[^}]*\}\s*from\s*['\"]\.\/email\/resend['\"]/)`
      2. `it("Invariant B: sendOne imports renderEmail from ./email/render", ...)` — same shape with renderEmail + ./email/render
      3. `it("Invariant C: Phase 19 throw stub is GONE", ...)` — `expect(src).not.toContain('send_not_implemented_in_phase_19')`
      4. `it("Invariant D (D-03 rollback runway): DRY_RUN === '1' branch STILL PRESENT", ...)` — `expect(src).toMatch(/if\s*\(\s*env\.DRY_RUN\s*===\s*['\"]1['\"]\s*\)/)`
      5. `it("Invariant E (D-03 rollback runway): chat.delivery.dry_run envelope log STILL PRESENT", ...)` — `expect(src).toContain("chat.delivery.dry_run")`
    - All 5 FAIL RED in Task 1 (the throw stub still exists; sendEmail not yet imported)

    CREATE `tests/build/wrangler-dry-run-shape.test.ts` mirroring tests/build/wrangler-cron-shape.test.ts:
    - HEADER: imports describe/it/expect/readFileSync/join + `parseJsonc` from `./_helpers/parse-jsonc`
    - `const cfg = parseJsonc(readFileSync(join(process.cwd(), "wrangler.jsonc"), "utf8")) as Record<string, unknown>;`
    - DESCRIBE: `"Plan 20-03 forward-defense: wrangler.jsonc DRY_RUN='0' phase-close + cron revert (D-01 + Pitfall 6)"`
    - 2 invariants:
      1. `it("D-01 / D-17: vars.DRY_RUN === '0' (Phase 20 live-mail toggle)", ...)` — `expect((cfg.vars as { DRY_RUN: string }).DRY_RUN).toBe("0")`
      2. `it("Pitfall 6: triggers.crons === ['0 * * * *'] (operator forgot to revert UAT * * * * * cron flip catches here)", ...)` — `expect((cfg.triggers as { crons: string[] }).crons).toEqual(["0 * * * *"])`
    - Both FAIL RED in Task 1 (wrangler.jsonc still has DRY_RUN === "1" — flip happens in Task 2)
  </action>
  <verify>
    <automated>pnpm exec astro check</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/chat-delivery.test.ts` contains the new describe block GROUP F with 6 new it() rows; existing 19+ test rows unchanged in count
    - `tests/build/chat-delivery-send-site.test.ts` exists; `wc -l` returns >= 30
    - `tests/build/wrangler-dry-run-shape.test.ts` exists; `wc -l` returns >= 25
    - `pnpm exec astro check` exits 0/0/0
    - `grep -c 'GROUP F' tests/api/chat-delivery.test.ts` returns 1
    - `grep -E 'vi\.mock\([\"'\'']\.\.\/\.\.\/src\/lib\/email\/resend' tests/api/chat-delivery.test.ts | wc -l` returns >= 1 (resend mock setup)
    - `grep -E 'vi\.mock\([\"'\'']\.\.\/\.\.\/src\/lib\/email\/render' tests/api/chat-delivery.test.ts | wc -l` returns >= 1 (render mock setup)
    - `grep -F 'send_not_implemented_in_phase_19' tests/build/chat-delivery-send-site.test.ts` returns >= 1 (Invariant C asserts this string is NOT present in src/lib/chat-delivery.ts after Task 2)
    - `grep -F 'chat.delivery.dry_run' tests/build/chat-delivery-send-site.test.ts` returns >= 1 (Invariant E rollback runway log presence)
    - `grep -F 'parseJsonc' tests/build/wrangler-dry-run-shape.test.ts` returns >= 1
    - `pnpm exec vitest run tests/build/chat-delivery-send-site.test.ts` exits non-zero (RED — invariants C+D+E or A+B fail because sendOne not yet substituted)
    - `pnpm exec vitest run tests/build/wrangler-dry-run-shape.test.ts` exits non-zero (RED — wrangler.jsonc still has DRY_RUN === "1"; both assertions fail)
    - `pnpm exec vitest run tests/api/chat-delivery.test.ts` — pre-existing 19+ tests STILL PASS (regression-safe scaffolding; new GROUP F stubs RED but pre-existing battery preserved)
  </acceptance_criteria>
  <done>
    Test scaffolding for Plan 20-03 is in place. astro check 0/0/0. New build tests + new GROUP F wiring tests fail RED (correct — the substitution hasn't landed). Pre-existing 498 PASS / 0 FAIL / 2 SKIP baseline NOT regressed (the existing GROUP D dry_run-gate test still passes — it's the throw-stub assertion which Task 2 will rewrite).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Substitute sendOne in src/lib/chat-delivery.ts + extend DeliveredMarker + populate resend_message_id in promoteOne step-4 + flip wrangler.jsonc DRY_RUN '1' -> '0' + update existing wrangler-cron-shape.test.ts assertion to '0' + rewrite GROUP D pre-existing test for new contract</name>
  <files>src/lib/chat-delivery.ts, wrangler.jsonc, tests/build/wrangler-cron-shape.test.ts, tests/api/chat-delivery.test.ts</files>
  <read_first>
    - src/lib/chat-delivery.ts (READ FULL — current Phase 19 state; identify line 39 import block insertion point; line 66-73 DeliveredMarker definition; lines 84-95 DeliveryEnv definition; lines 163-184 sendOne body to substitute; line 264 retryWithBackoff call; lines 268-281 promoteOne step-4 PUT site)
    - src/lib/email/render.ts (READ — Plan 20-01 close output; verify renderEmail signature + RenderEnv field set)
    - src/lib/email/resend.ts (READ — Plan 20-02 close output; verify sendEmail signature + ResendResult discriminator + ResendEnv field set)
    - tests/api/chat-delivery.test.ts (READ — own Task 1 output; verify GROUP F mocks setup; identify the GROUP D test at lines ~493 that needs rewrite)
    - tests/build/chat-delivery-send-site.test.ts (READ — own Task 1 output; identify the 5 invariants this Task 2 substitution must satisfy)
    - tests/build/wrangler-dry-run-shape.test.ts (READ — own Task 1 output; identify the 2 invariants the wrangler flip must satisfy)
    - tests/build/wrangler-cron-shape.test.ts (READ — Plan 19-04 source; identify the existing line that asserts `cfg.vars.DRY_RUN === "1"` — single-line update to `=== "0"` in this Task)
    - wrangler.jsonc (READ FULL — current line 21 has `"DRY_RUN": "1"`; this Task changes the value to `"0"` only; all other keys UNCHANGED)
    - .planning/phases/20-email-render-resend-integration/20-CONTEXT.md (READ <decisions> D-01 + D-03 + D-13 + D-17 lines 49-100 — atomic-deploy single commit; rollback runway preservation; 3-class HTTP taxonomy; collapsed 3-variant Result)
    - .planning/phases/20-email-render-resend-integration/20-RESEARCH.md (READ § Pattern 3 lines 743-806 — full reference sendOne implementation with sentinel return for DRY_RUN branch + Result-translation in else branch)
    - .planning/phases/20-email-render-resend-integration/20-PATTERNS.md (READ Group 2 lines 202-279 — substitution shape + executor notes; CRITICAL note lines 591-593 about updating tests/build/wrangler-cron-shape.test.ts in-place at the same commit as the wrangler flip — Option A recommended)
  </read_first>
  <behavior>
    - All 6 new GROUP F tests + the rewritten GROUP D test in tests/api/chat-delivery.test.ts transition RED -> GREEN
    - Both new build tests (chat-delivery-send-site.test.ts + wrangler-dry-run-shape.test.ts) transition RED -> GREEN
    - Existing tests/build/wrangler-cron-shape.test.ts STAYS GREEN after the line-update (because wrangler.jsonc DRY_RUN flips to "0" at the same time as the test expectation flip)
    - All Phase 19 pre-existing chat-delivery.test.ts tests STAY GREEN — including the skipped_already_delivered test (Layer 1 cursor still works exactly the same; the cursor short-circuits BEFORE sendOne is called)
    - DRY_RUN='1' branch in sendOne still emits chat.delivery.dry_run with byte-identical envelope shape (rollback runway preserved per D-03)
    - DRY_RUN='0' branch invokes renderEmail then sendEmail; on sent returns { message_id }; on failed_transient throws (caught by retryWithBackoff for retry); on failed_terminal throws (bubbles through retryWithBackoff to promoteOne catch)
    - DeliveredMarker.resend_message_id field populated on sent path; DeliveredMarker.dry_run flips to false when env.DRY_RUN === "0" (existing line 272 logic correctly evaluates)
    - kv.put options on delivered:{sid} writes stay { expirationTtl: DELIVERED_TTL_SECONDS } EXACTLY (no metadata field — Landmine 7)
  </behavior>
  <action>
    EDIT `src/lib/chat-delivery.ts` per RESEARCH § Pattern 3 lines 743-806 + PATTERNS Group 2 lines 202-279:

    Edit 1 — IMPORT additions (after existing line 39 import of types from chat-transcripts; before line 42 locked constants block): Add two new value imports `import { renderEmail, type RenderEnv } from "./email/render";` and `import { sendEmail, type ResendEnv } from "./email/resend";`. Update the file-banner "NO imports from..." block (lines 28-37) by REMOVING the `// • src/lib/email/             — does NOT exist in Phase 19 (D-06; Phase 20)` line — that constraint is now satisfied (Phase 20 created the directory).

    Edit 2 — DeliveryEnv interface extension (lines 84-95): Add `RESEND_API_KEY?: string;` after `CHAT_REPLY_TO_EMAIL?: string;`. Optional in the type so existing tests (mockEnv shape) don't need to add the field; runtime narrowing for the four required-under-DRY_RUN==='0' env vars (RESEND_API_KEY + CHAT_RECIPIENT_EMAIL + CHAT_SENDER_EMAIL + CHAT_REPLY_TO_EMAIL) is AUTHORED in Edit 4 as an explicit guard at sendOne entry — it throws `resend_terminal_env_missing` BEFORE the unsafe `as` cast so missing env vars surface as a structured `chat.delivery.failed` log (not a raw TypeError). Add inline comment: `// Phase 20 — sourced from Wrangler secret (set Plan 17-06); read by sendEmail wrapper under DRY_RUN='0' live path. Runtime narrowing guard at sendOne entry (Edit 4) emits chat.delivery.failed before the as-cast on missing values.`

    Edit 3 — DeliveredMarker interface extension (lines 66-73): APPEND `resend_message_id: string;` field after `truncated: boolean;`. Update inline JSDoc to remove "Phase 20 will additively append..." sentence (replace with "Phase 20 ADDED `resend_message_id` per D-09 additive lock + 20-03 close.")

    Edit 4 — sendOne substitution + return-type widening (lines 163-184): REPLACE the entire function body per 20-RESEARCH.md § Pattern 3 lines 745-792. Specifically:
    - Signature: `async function sendOne(env: DeliveryEnv, transcript: ChatTranscript): Promise<{ message_id: string }>` (return type widens from Promise<void> to Promise<{ message_id: string }>)
    - DRY_RUN === "1" branch: BYTE-IDENTICAL to current Phase 19 console.log envelope (D-05 — preserve all 9 field NAMES + ORDER) BUT replace the bare `return;` with `return { message_id: "dry-run-no-id" };` sentinel. Add a multi-line comment block above this branch: `// D-03 ROLLBACK RUNWAY — DO NOT DELETE this branch as "dead code". // The DRY_RUN="1" path is the instant-rollback mechanism: a single-line // wrangler.jsonc revert to "DRY_RUN": "1" reverts ALL Phase 20 behavior // without source code edit. Operator runs wrangler deploy; ~60s recovery. // The sentinel "dry-run-no-id" message_id flows through promoteOne where // the line-272 dry_run discriminator (env.DRY_RUN === "1") flags the value // as dry_run: true so this sentinel only ever appears in dry-run-flagged // delivered: markers — not in real sends.`
    - DRY_RUN !== "1" branch (Phase 20 live-send substitution): FIRST emit an explicit env-narrowing guard ABOVE the renderEmail call (closes the unsafe `as` cast gap — without this guard, missing env vars at runtime under DRY_RUN==="0" surface as a raw TypeError instead of a structured `chat.delivery.failed` log). The guard literal: `if (env.DRY_RUN === "0") { if (!env.RESEND_API_KEY || !env.CHAT_RECIPIENT_EMAIL || !env.CHAT_SENDER_EMAIL || !env.CHAT_REPLY_TO_EMAIL) { console.warn("chat.delivery.failed", { sid: transcript.sid, http_status: null, error_class: "resend_terminal_env_missing", attempt: 0 }); throw new Error("resend_terminal_env_missing"); } }`. The thrown Error is caught by promoteOne's existing try/catch (lines 287-313 in chat-delivery.ts per CONTEXT) — the `chat.delivery.failed` log emission lives at the throw site so we get structured failure visibility BEFORE the as-cast. THEN: `const payload = renderEmail(env as RenderEnv, transcript); const result = await sendEmail(env as ResendEnv, payload); if (result.status === "sent") { return { message_id: result.message_id }; } if (result.status === "failed_transient") { throw new Error(\`resend_transient_${result.http_status ?? result.error_class ?? "unknown"}\`); } throw new Error(\`resend_terminal_${result.http_status}\`);` — note the trailing throw is for the failed_terminal branch (TypeScript narrows after the two `if` returns; no else needed). Add comment block per RESEARCH § Pattern 3 lines 783-792 explaining the 3x log noise on terminal trade-off.

    Edit 5 — promoteOne step-3 capture sendResult (line 264): CHANGE `await retryWithBackoff(() => sendOne(env, transcript!), MAX_SEND_ATTEMPTS);` to `const sendResult = await retryWithBackoff(() => sendOne(env, transcript!), MAX_SEND_ATTEMPTS);`. Now sendResult.message_id is in scope for step 4.

    Edit 6 — promoteOne step-4 PUT additive (lines 268-281): The value object literal gets one new field appended. Current Phase 19 shape is `{ v: 1, sid, delivered_at, dry_run, msg_count, truncated }` — Phase 20 appends `resend_message_id: sendResult.message_id`. The kv.put OPTIONS stay BYTE-IDENTICAL `{ expirationTtl: DELIVERED_TTL_SECONDS }` — DO NOT add metadata field (Landmine 7 / D-11). Update the inline comment at line 278-280 to add reference to Plan 20-03 alongside D-11.

    EDIT `wrangler.jsonc` (single-line value flip per D-01 atomic-deploy commit + D-03 rollback runway lock):
    - Line 21: change `"DRY_RUN": "1"` to `"DRY_RUN": "0"`. NO other keys touched. Update the inline comment on line 18 from "Phase 20 flips to '0' alongside the Resend POST landing" to "Phase 20 flipped 2026-05-12 alongside the Resend POST landing (Plan 20-03). Rollback = single-line revert to '1' per D-03." — historical breadcrumb form.

    EDIT `tests/build/wrangler-cron-shape.test.ts` (single-line update at the existing DRY_RUN assertion):
    - Find the line that asserts `(cfg.vars as { DRY_RUN: string }).DRY_RUN).toBe("1")` (per 20-PATTERNS.md note line 591); change `"1"` to `"0"`. Update the `it("...")` test name from "vars.DRY_RUN === '1'" to "vars.DRY_RUN === '0' (Phase 20 live-mail toggle per D-01)" — descriptive update aligned to phase-close state. NO other assertion in the file touched.

    EDIT `tests/api/chat-delivery.test.ts` GROUP D pre-existing test rewrite (the test marked TODO in Task 1):
    - Find `it("dry_run gate (env.DRY_RUN !== '1' throws)"` at line ~493; RENAME to `it("dry_run gate (env.DRY_RUN !== '1' calls sendEmail wrapper per Plan 20-03)"`; REWRITE the body: under DRY_RUN='0' with sendEmail mocked to return sent, `await deliverDue(buildEnv(kv, { DRY_RUN: "0", RESEND_API_KEY: "test-key" }), SCHEDULED_NOW)` succeeds without throwing; assert `vi.mocked(sendEmail).mock.calls.length >= 1`; assert no `Error: send_not_implemented_in_phase_19` thrown. The Phase 19 throw assertion is REPLACED by the Plan 20-03 substitution-call assertion. Update the buildEnv helper if needed to support RESEND_API_KEY pass-through (existing helper structure should already support extra env fields via the spread).

    All test edits + source edits + wrangler edit must commit AS ONE atomic commit per D-01 (the executor ensures git add includes all 4 modified files in a single commit).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/api/chat-delivery.test.ts tests/build/chat-delivery-send-site.test.ts tests/build/wrangler-dry-run-shape.test.ts tests/build/wrangler-cron-shape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run tests/api/chat-delivery.test.ts` exits 0; ALL tests GREEN — the rewritten GROUP D test + 6 new GROUP F tests + 19+ pre-existing Phase 19 tests
    - `pnpm exec vitest run tests/build/chat-delivery-send-site.test.ts` exits 0; all 5 invariants GREEN
    - `pnpm exec vitest run tests/build/wrangler-dry-run-shape.test.ts` exits 0; both invariants GREEN
    - `pnpm exec vitest run tests/build/wrangler-cron-shape.test.ts` exits 0; the updated DRY_RUN assertion + the cron assertion both GREEN
    - `grep -c 'send_not_implemented_in_phase_19' src/lib/chat-delivery.ts` returns 0 (Phase 19 throw stub GONE)
    - `grep -c "import { sendEmail" src/lib/chat-delivery.ts` returns >= 1
    - `grep -c "import { renderEmail" src/lib/chat-delivery.ts` returns >= 1
    - `grep -c "DRY_RUN === \"1\"" src/lib/chat-delivery.ts` returns >= 2 (the DRY_RUN gate in sendOne PLUS the line-272 dry_run discriminator in promoteOne — both intact)
    - `grep -c "chat.delivery.dry_run" src/lib/chat-delivery.ts` returns >= 1 (rollback runway envelope log preserved)
    - `grep -c "ROLLBACK RUNWAY" src/lib/chat-delivery.ts` returns >= 1 (the new comment block per Edit 4)
    - `grep -c 'resend_terminal_env_missing' src/lib/chat-delivery.ts` returns >= 2 (env-narrowing guard literal — the throw + the console.warn structured-log error_class — closes the unsafe `as` cast gap surfaced by checker)
    - `grep -n 'env\.RESEND_API_KEY' src/lib/chat-delivery.ts | wc -l` returns >= 1 (env null-check for RESEND_API_KEY exists in sendOne BEFORE the sendEmail call; defends against missing-env runtime TypeError)
    - `grep -c "resend_message_id" src/lib/chat-delivery.ts` returns >= 2 (DeliveredMarker field + step-4 PUT site value-literal)
    - `grep -c "resend_message_id" src/lib/chat-delivery.ts | sort -u` produces a count consistent with at least one usage per Edit 3 (interface) + Edit 6 (PUT site value)
    - `grep -c '"DRY_RUN": "0"' wrangler.jsonc` returns 1
    - `grep -c '"DRY_RUN": "1"' wrangler.jsonc` returns 0 (flip complete)
    - `grep -F '"crons": ["0 * * * *"]' wrangler.jsonc` returns 1 (cron unchanged)
    - `grep -E 'metadata\s*:' src/lib/chat-delivery.ts | grep -v 'KVMetadata\|metadata\.last_activity\|metadata\.msg_count\|metadata\?\|const metadata\|file-local\|^ *\*\|^ *//' | wc -l` — the kv.put on delivered: still has NO metadata key (Landmine 7); manual visual confirms via reading the step-4 PUT site
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm exec vitest run tests/api/sse-snapshot.test.ts` exits 0 (D-15 anchor preserved — Phase 20 touches zero chat-surface)
    - `pnpm exec vitest run tests/api/anthropic-payload-shape.test.ts` exits 0 (TEST-03 anchor preserved)
    - `pnpm exec vitest run` (full suite) exits 0 with PASS count >= 498 (Phase 19 baseline) + 19 (Plan 20-01) + 14 (Plan 20-02) + ~13 net new (Plan 20-03: 6 GROUP F + 5 chat-delivery-send-site + 2 wrangler-dry-run-shape) = ~544+ PASS / 0 FAIL / 2 SKIP
    - `git diff --stat package.json` shows zero changes to dependencies (MAIL-01 phase-wide lock holds)
    - `pnpm build` exits 0 cleanly (wrangler types regen + astro check + astro build all clean)
  </acceptance_criteria>
  <done>
    Substitution complete. wrangler.jsonc DRY_RUN === "0". All wiring tests + build-defense tests GREEN. Phase 19 pre-existing battery preserved. astro check 0/0/0. pnpm build clean. package.json deps byte-identical. Phase 20 live-mail flow ready: next cron tick against any 2h-stale session under DRY_RUN='0' will renderEmail -> sendEmail -> populate delivered:{sid}.resend_message_id -> delete live:{sid}. Rollback runway preserved: a single-line wrangler.jsonc revert to "DRY_RUN": "1" reverts all Phase 20 behavior without source revert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrangler.jsonc -> deployed Worker env.DRY_RUN | Operator-controlled deploy-time toggle; production state changes when `wrangler deploy` runs |
| sendOne substitution -> live Resend POST | First time Phase 20 contacts Resend in production; rollback runway is the safety net |
| DeliveredMarker.resend_message_id -> KV value | New field flows to Workers Logs `chat.delivery.sent` + persisted in delivered:{sid} for 24h audit trail |
| RESEND_API_KEY env -> sendEmail wrapper | Wrangler secret (Plan 17-06); accessed only when DRY_RUN='0' branch fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Tampering | Future contributor deletes the DRY_RUN === "1" branch in sendOne as "dead code unreachable in production" | mitigate | tests/build/chat-delivery-send-site.test.ts Invariant D + E lock the rollback runway: D asserts `if (env.DRY_RUN === "1")` regex still present; E asserts `chat.delivery.dry_run` log still present. Inline comment block per Edit 4 above the branch explicitly documents "ROLLBACK RUNWAY — DO NOT DELETE". A future cleanup PR removing either fails the build test |
| T-20-03-02 | Tampering | Future contributor adds `metadata` field to the kv.put on delivered:{sid} writes (Landmine 7) | mitigate | The new GROUP F test "delivered marker no metadata" asserts `mockKV.put.calls[0][2]` deep-equals `{ expirationTtl: DELIVERED_TTL_SECONDS }` exactly. Adding any `metadata: {...}` argument fails the equality check |
| T-20-03-03 | Tampering | Operator forgets to revert the UAT cron flip (`* * * * *` -> `0 * * * *`) after Plan 20-04 Step 4 | mitigate | tests/build/wrangler-dry-run-shape.test.ts Invariant 2 + tests/build/wrangler-cron-shape.test.ts both assert `cfg.triggers.crons === ["0 * * * *"]`. Next CI run on any commit catches the unreverted state. Pitfall 6 inherited from Phase 19 |
| T-20-03-04 | Tampering | Operator accidentally pushes `wrangler.jsonc` with DRY_RUN === "1" (forgetting Plan 20-03 flip) and the build tests don't catch because they pre-date Plan 20-03 | mitigate | tests/build/wrangler-dry-run-shape.test.ts Invariant 1 asserts DRY_RUN === "0" — fails RED in CI if the flip is reverted accidentally. Plan 20-04 DEPLOY-GATE.md additionally requires operator confirmation that vars.DRY_RUN === "0" pre-push |
| T-20-03-05 | Information Disclosure | resend_message_id field accidentally logged into the chat.delivery.tick summary (which is sampled by Workers Logs) | accept | resend_message_id is the canonical Resend send id (UUID-shape opaque). Not PII; not a secret. Even if it does appear in logs (which it does via chat.delivery.sent inside the wrapper at Plan 20-02 anyway), there's no harm — the message id is shared between Resend dashboard + Workers Logs as the audit anchor |
| T-20-03-06 | Repudiation | Production Resend send happens but no audit trail correlates the Workers Logs entry to the Resend dashboard entry | mitigate | DeliveredMarker.resend_message_id PLUS chat.delivery.sent log line (emitted by sendEmail in Plan 20-02) both carry the same Resend `data.id`. wrangler tail + Resend dashboard cross-check is the operational forensics path. Plan 20-04 UAT Step 3 explicitly verifies this correlation |
| T-20-03-07 | Denial of Service | sendEmail returns failed_transient on every attempt for one session; the 3-retry burn × 50-session batch cap means up to 150 fetch attempts per cron tick | accept | Phase 19 PER_TICK_BATCH_CAP=50 + per-session try/catch isolation already bounds blast radius. Resend rate limit is 5 req/sec per team (RESEARCH § Drift item 2); 50 sessions × 3 retries = 150 attempts spread over the cron tick wall-clock (~25-30s typical) is well within rate limit. AbortController 10s/attempt prevents hung connections from starving the batch (Plan 20-02 contract) |
</threat_model>

<verification>
- `pnpm exec vitest run tests/api/chat-delivery.test.ts` exits 0 — all GROUP F (6 new) + GROUP D (rewritten) + Phase 19 pre-existing tests GREEN
- `pnpm exec vitest run tests/build/chat-delivery-send-site.test.ts` exits 0 — all 5 invariants GREEN
- `pnpm exec vitest run tests/build/wrangler-dry-run-shape.test.ts` exits 0 — both invariants GREEN
- `pnpm exec vitest run tests/build/wrangler-cron-shape.test.ts` exits 0 — updated DRY_RUN === "0" assertion + cron assertion GREEN
- `pnpm exec astro check` exits 0/0/0
- `pnpm build` exits 0 cleanly (wrangler types regen + astro check + astro build)
- `pnpm exec vitest run tests/api/sse-snapshot.test.ts` exits 0 (D-15 anchor preserved)
- `pnpm exec vitest run tests/api/anthropic-payload-shape.test.ts` exits 0 (TEST-03 anchor preserved)
- `pnpm exec vitest run` exits 0 with PASS count ~544+ / 0 FAIL / 2 SKIP
- `git diff --stat package.json` shows zero changes to dependencies (MAIL-01 phase-wide lock)
</verification>

<success_criteria>
- src/lib/chat-delivery.ts sendOne substitution lands; throw stub gone; renderEmail + sendEmail imports wired
- DeliveredMarker interface extended with resend_message_id field; schema v: 1 unchanged
- promoteOne step-4 PUT site populates resend_message_id from sendOne return value
- promoteOne step-4 kv.put options stay { expirationTtl: DELIVERED_TTL_SECONDS } EXACTLY (Landmine 7)
- DRY_RUN === "1" branch byte-identical envelope log preserved (rollback runway per D-03)
- wrangler.jsonc vars.DRY_RUN flipped "1" -> "0" (single-line atomic edit per D-01)
- Existing tests/build/wrangler-cron-shape.test.ts DRY_RUN assertion updated to "0" at the same commit
- Two new build tests (chat-delivery-send-site.test.ts + wrangler-dry-run-shape.test.ts) GREEN — forward-defense for D-03 rollback runway + D-01 phase-close state + Pitfall 6 cron revert
- 6 new GROUP F wiring tests in tests/api/chat-delivery.test.ts GREEN per D-17 collapsed Result
- All Phase 19 pre-existing chat-delivery.test.ts tests STAY GREEN (Layer 1 cursor + skipped_already_delivered behavior preserved exactly — Layer 1 IS the application-side replay detector per D-17)
- D-15 SSE snapshot GREEN; TEST-03 Anthropic payload-shape GREEN; astro check 0/0/0; pnpm build clean
- package.json dependencies byte-identical (MAIL-01 zero-new-runtime-dep lock)
- Plan 20-04 unblocked: substitution + flip ready for deploy-gate authorship + operator UAT
</success_criteria>

<output>
After completion, create `.planning/phases/20-email-render-resend-integration/20-03-SUMMARY.md` documenting:
- Surgical edit map: src/lib/chat-delivery.ts (6 distinct edits — import block, DeliveryEnv, DeliveredMarker, sendOne substitution + return widen, promoteOne step-3 capture, promoteOne step-4 value-additive); wrangler.jsonc (1-line flip); tests/build/wrangler-cron-shape.test.ts (1-line assertion update); tests/api/chat-delivery.test.ts (1 test rewritten + 6 new it() rows + 2 vi.mock setups)
- New files: tests/build/chat-delivery-send-site.test.ts (LOC; 5 invariants); tests/build/wrangler-dry-run-shape.test.ts (LOC; 2 invariants)
- Test suite drift: PASS/FAIL/SKIP before vs after (target ~544+/0/2)
- Verification of all cross-phase anchors: D-15 SSE, TEST-03 Anthropic, package.json deps
- Confirmation that the rollback runway is preserved structurally: comment block + tests/build/chat-delivery-send-site.test.ts Invariants D + E
- Any deviation from RESEARCH § Pattern 3 substitution shape (Rule 3)
- Note that Phase 20 is now structurally LIVE — only the deploy-gate (Plan 20-04) + operator push remain
</output>
