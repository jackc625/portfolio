---
phase: 17-foundations-migration-dns-debt-sweep
plan: 07
type: execute
wave: 7
depends_on: []
gap_closure: true
files_modified:
  - src/data/about-chat.ts
  - src/content/projects/clipify.mdx
  - src/content/projects/daytrade.mdx
  - src/content/projects/nfl-predict.mdx
  - src/content/projects/optimize-ai.mdx
  - src/content/projects/seatwatch.mdx
  - src/content/projects/solsniper.mdx
  - scripts/build-chat-context.mjs
  - src/data/portfolio-context.json
  - src/prompts/system-prompt.ts
  - tests/build/chat-knowledge-voice.test.ts
  - tests/api/chat-voice-split.test.ts
  - tests/content/roadmap-amendment.test.ts
  - .github/workflows/sync-check.yml
autonomous: true
requirements:
  - DEBT-02
  - TEST-01
tags:
  - chat
  - voice-split
  - gap-closure
  - phase-17

must_haves:
  truths:
    - "Phase 17 was CLOSED at Plan 17-06; this plan is a gap-closure addendum re-opening Phase 17 to ship the four UAT-blockers found post-close (Wave 7-10 serial). STATE.md must be updated to reflect total_plans 6 -> 10 at the metadata commit (M7)."
    - "Chat widget addresses VISITORS (recruiters/engineers) in second person and refers to Jack in third person — never greets the visitor as Jack and never describes the visitor AS Jack (CHAT-06 voice-split contract)"
    - "src/data/about.ts first-person prose UNCHANGED — site surface (homepage, about page) keeps its first-person voice"
    - "src/content/projects/*.mdx case-study bodies UNCHANGED above the existing CASE-STUDY-END fence — site case-study pages keep their first-person voice"
    - "src/data/portfolio-context.json (the chat-knowledge artifact) carries third-person variants of about.{intro,p1,p2,p3}, experience, and per-project chat summaries — first-person prose no longer leaks into the chat <knowledge> block"
    - "scripts/build-chat-context.mjs runs a BROADENED first-person-leak regex sweep (B1) over chat-bound fields and exits non-zero on any match — future regressions fail the build (and the DEBT-03 sync-check.yml CI job)"
    - "Chat-API regression test asserts response to a synthetic 'hi' message contains neither 'Hey Jack' nor 'you're Jack' nor 'you are Jack' — the test that would have caught Test 4's failure"
    - "Pre-existing tests/content/roadmap-amendment.test.ts failure FIXED in Wave-0 (M8 option a) so `pnpm test` exit-0 assertions are honest gates, not workarounds for known failures"
    - "D-26 chat regression battery 145/145+ GREEN at every commit on this plan (TEST-01) — no chat-surface regression"
    - "D-15 SSE byte-identical anchor preserved (TEST-02 sse-snapshot 3/3 GREEN) — system-prompt.ts edits add NO new SSE frame types"
    - "sync-check.yml CI workflow path triggers cover the new src/data/about-chat.ts file (M1) — PRs touching the third-person variant trigger the leak-guard job"
  artifacts:
    - path: "src/data/about-chat.ts"
      provides: "Hand-authored third-person variants of ABOUT_INTRO/P1/P2/P3 — sibling exports consumed only by build-chat-context.mjs"
      contains: "ABOUT_CHAT_INTRO"
    - path: "src/content/projects/clipify.mdx"
      provides: "Frontmatter chatSummary field (third-person summary of the project for the chat <knowledge> block)"
      contains: "chatSummary:"
    - path: "src/content/projects/daytrade.mdx"
      provides: "Frontmatter chatSummary field"
      contains: "chatSummary:"
    - path: "src/content/projects/nfl-predict.mdx"
      provides: "Frontmatter chatSummary field"
      contains: "chatSummary:"
    - path: "src/content/projects/optimize-ai.mdx"
      provides: "Frontmatter chatSummary field"
      contains: "chatSummary:"
    - path: "src/content/projects/seatwatch.mdx"
      provides: "Frontmatter chatSummary field"
      contains: "chatSummary:"
    - path: "src/content/projects/solsniper.mdx"
      provides: "Frontmatter chatSummary field"
      contains: "chatSummary:"
    - path: "scripts/build-chat-context.mjs"
      provides: "Reads about-chat.ts + per-MDX chatSummary; substitutes them for first-person variants when populating the chat-knowledge JSON; runs a BROADENED first-person-leak regex sweep (B1) that exits 2 on match"
      contains: "ABOUT_CHAT_INTRO"
    - path: "src/data/portfolio-context.json"
      provides: "Generated artifact — about.* + experience + per-project caseStudy fields now contain third-person prose; first-person markers absent from chat-bound fields"
      contains: "Jack"
    - path: "src/prompts/system-prompt.ts"
      provides: "Defense-in-depth: <role> instruction reinforced with 'translate first-person citations to third person' callout (Hypothesis 3 mitigation per debug session)"
      contains: "translate"
    - path: "tests/build/chat-knowledge-voice.test.ts"
      provides: "Build-time source-text test asserting portfolio-context.json contains no first-person leading-clause patterns in chat-bound fields. Includes a self-test asserting the regex catches the CURRENT pre-fix portfolio-context.json (B1 self-test)."
    - path: "tests/api/chat-voice-split.test.ts"
      provides: "Chat-API regression: mocked Anthropic response for synthetic 'hi' message asserts response does NOT contain 'Hey Jack' / 'you're Jack' / 'you are Jack'"
    - path: "tests/content/roadmap-amendment.test.ts"
      provides: "Pre-existing failing test FIXED (M8 option a) — annotated or skipped per RETROSPECTIVE.md guidance so `pnpm test` exits 0"
    - path: ".github/workflows/sync-check.yml"
      provides: "Path triggers updated to include src/data/about-chat.ts (M1) so PRs touching the third-person variant trigger the leak-guard job"
      contains: "about-chat"
  key_links:
    - from: "scripts/build-chat-context.mjs"
      to: "src/data/about-chat.ts"
      via: "parseAboutChatExports() reads ABOUT_CHAT_INTRO/P1/P2/P3"
      pattern: "ABOUT_CHAT_INTRO"
    - from: "scripts/build-chat-context.mjs"
      to: "src/content/projects/*.mdx frontmatter chatSummary"
      via: "readStringField(frontmatterBlock, 'chatSummary')"
      pattern: "chatSummary"
    - from: "src/data/portfolio-context.json"
      to: "src/prompts/system-prompt.ts <knowledge> block"
      via: "JSON.stringify(context, null, 2) embeds the merged JSON verbatim"
      pattern: "JSON\\.stringify"
    - from: "tests/build/chat-knowledge-voice.test.ts"
      to: "src/data/portfolio-context.json"
      via: "readFileSync + JSON.parse + broadened regex sweep over about.* / experience / projects[].caseStudy"
      pattern: "first-person|I'?m|I built|I architected|I like|My favorite"
---

<objective>
Close UAT Gap #1 (BLOCKER): the chat widget greeted a visitor with "Hey Jack" and answered "who am i" with "You're Jack Cutrara." The voice-split contract (CHAT-06: site = first person, chat = third person) regressed silently when Phase 14-02 merged first-person prose from src/data/about.ts and src/content/projects/*.mdx into the chat <knowledge> block. ROOT cause per debug session chat-voice-split-regression.md: ~30KB of first-person prose in the <knowledge> block dominates the third-person <role>/<tone> instruction by signal volume.

**Phase 17 status reminder:** Phase 17 was CLOSED at Plan 17-06 per STATE.md (`status: completed`, `total_plans: 6`, `completed_plans: 6`). This is a gap-closure addendum (Plans 17-07..10) re-opening the phase to ship four UAT-blockers found post-close. The metadata-update task (M7) bumps STATE.md / ROADMAP.md / REQUIREMENTS.md to reflect the new plan count. The serial wave structure (M-iter2 wave correction): 17-07 (Wave 7) -> 17-09 (Wave 8) -> 17-10 (Wave 9) -> 17-08 (Wave 10). Per the wave rule (Wave = max(deps) + 1), each plan in the serial chain occupies its OWN wave so a wave-batching orchestrator cannot accidentally parallelize chat-surface mutations. This enforces clean D-26 attribution per CONTEXT.md D-10 since ALL FOUR plans touch chat-surface files and parallel chat-surface mutations would muddy the regression battery (B3).

Fix shape (per gap_closure planning_directives):
1. Author hand-written third-person variants of the about block in a NEW file `src/data/about-chat.ts` (sibling exports — beats in-file overload of about.ts; about.ts stays the site surface source-of-truth and KEEPS its first-person voice).
2. Add a `chatSummary:` frontmatter field to each of the 6 project MDX files — third-person summary the chat consumes instead of the first-person caseStudy body. The MDX bodies stay first-person (correct for the site /projects/[slug] case-study pages).
3. Extend scripts/build-chat-context.mjs to (a) read about-chat.ts and substitute its values into the chat-knowledge JSON's about.* and experience fields, (b) read each MDX's chatSummary frontmatter and substitute it for the caseStudy field in the merged JSON, (c) run a BROADENED first-person-leak regex sweep over the chat-bound fields that exits 2 on match (build/CI fail-fast). The regex MUST cover present-tense "I build", "I like", "My favorite", contractions ("I'd", "I'll", "I've", "I'm", "I haven't"), and "I wonder" — see B1 below for the canonical shape.
4. Defense-in-depth: append a "translate first-person citations to third person" instruction to system-prompt.ts <role> block (per debug session "instruction-only mitigation is fragile but cheap").
5. Add a chat-API regression test asserting response to synthetic 'hi' contains none of {'Hey Jack', "you're Jack", 'you are Jack'} (the test that would have caught this regression — pattern from tests/api/cache-hit-logs.test.ts).
6. Update .github/workflows/sync-check.yml path triggers to include `src/data/about-chat.ts` (M1) so PRs touching the new third-person variant trigger the existing `pnpm build:chat-context:check` job.

DO NOT modify src/data/about.ts (correct for site surface — homepage hero + about page). DO NOT modify the case-study bodies above the existing `<!-- CASE-STUDY-END -->` fence (correct for /projects/[slug] pages). The voice split is per-surface, not per-source.

Wave 7 — depends on nothing in the gap-closure set (it's the first plan in the new serial chain, depends_on: []). Honors memory note `project_voice_split.md` ("Don't repeat my Plan-05 mistake"). The cache-knowledge JSON regenerates from the new sources — a one-shot cache miss is expected on first deploy after this plan; subsequent requests cache normally per Phase 14 cache contract.

Output: 1 new src file (about-chat.ts), 6 modified MDX frontmatters, 1 modified generator (build-chat-context.mjs), 1 regenerated artifact (portfolio-context.json), 1 hardened system prompt, 2 new test files, 1 fixed pre-existing failing test (M8), 1 modified CI workflow (M1), and a metadata commit (M7) updating STATE.md / ROADMAP.md / REQUIREMENTS.md. D-15 SSE byte-identical preserved (no api/chat.ts edits). D-26 GREEN at every commit per CONTEXT.md D-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-foundations-migration-dns-debt-sweep/17-CONTEXT.md
@.planning/phases/17-foundations-migration-dns-debt-sweep/17-UAT.md
@.planning/phases/17-foundations-migration-dns-debt-sweep/17-RETROSPECTIVE.md
@.planning/debug/chat-voice-split-regression.md

@src/data/about.ts
@src/data/portfolio-context.json
@src/data/portfolio-context.static.json
@src/prompts/system-prompt.ts
@src/prompts/chat-request-shape.ts
@src/pages/api/chat.ts
@scripts/build-chat-context.mjs
@scripts/sync-projects.mjs
@src/content/projects/seatwatch.mdx
@src/content/projects/solsniper.mdx
@src/content/projects/clipify.mdx
@src/content/projects/daytrade.mdx
@src/content/projects/nfl-predict.mdx
@src/content/projects/optimize-ai.mdx
@tests/api/cache-hit-logs.test.ts
@tests/api/chat.test.ts
@tests/content/roadmap-amendment.test.ts
@.github/workflows/sync-check.yml

<interfaces>
<!-- Existing third-person <role> block (src/prompts/system-prompt.ts:4-6) — the instruction that the new defense-in-depth callout extends -->

Current <role> opens with: "You are a third-person biographer for Jack Cutrara, a software engineer. ... You are not Jack; you speak ABOUT Jack. You are addressing technical recruiters, hiring managers, and senior engineers..."

The new sentence appended to <role> (or to a new <knowledge_voice_translation> block before <knowledge>) MUST instruct: "the knowledge below was authored by Jack in first person for his website — when you cite or paraphrase it, rewrite first-person ('I built X', 'I'm Jack') as third-person ('Jack built X', 'Jack is...')."

<!-- Existing build-chat-context.mjs surfaces the new code extends -->

parseAboutExports(sourceContent) at lines 200-228 — current pattern that reads ABOUT_INTRO/P1/P2/P3 from about.ts. The new parseAboutChatExports() mirrors this shape but reads ABOUT_CHAT_INTRO/P1/P2/P3 from about-chat.ts.

readStringField(frontmatterBlock, fieldName) at lines 68-82 — already supports quoted + unquoted single-line strings. Use this verbatim to read the new chatSummary frontmatter field. Multi-line chatSummary is OUT OF SCOPE for this plan; keep each chatSummary as a single-line YAML string ≤ 600 chars (about a paragraph).

buildProjectBlock() at lines 238-269 currently builds a project entry with `caseStudy: body.trimEnd()`. The new behavior reads chatSummary from frontmatter; if present, use it as the caseStudy field; if absent, fail with a named error (all 6 MDX files MUST have chatSummary by the end of this plan).

main() at lines 367-405 currently composes `aboutBlock = { intro: parsed.ABOUT_INTRO, ... }` from about.ts and `experience = ...` from concatenating ABOUT_INTRO/P1/P3. The new code reads from about-chat.ts and uses ABOUT_CHAT_INTRO/P1/P3 for the experience synthesis.

<!-- BROADENED first-person leak regex (B1 fix) -->

The original regex `/\b(I'?m |I built |I architected |I chose |I wanted |I reach |I read |My approach )\b/` MISSES the following first-person tokens that are CURRENTLY present in src/data/portfolio-context.json lines 178-184:
  - "I build" (present-tense, not "built")
  - "I like" / "I liked"
  - "My favorite"
  - "I haven't" / "I'd be" (apostrophe contractions beyond just "I'm")
  - "I wonder"
  - "I read" / "I reach" without trailing space artifacts

If the hand-authored about-chat.ts retains any of these by copy-paste residue (e.g., the developer paraphrases "I build small services" -> "Jack builds small services" but accidentally leaves a "I like tests" token), the original leak guard would green despite the contract violation.

**The CANONICAL regex (use this verbatim in all 3 locations: 17-07-PLAN.md interfaces, build-chat-context.mjs leakGuard, and tests/build/chat-knowledge-voice.test.ts FIRST_PERSON_LEAK constant):**

```regex
/\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i
```

Token coverage:
- `I'm` / `I'd` / `I'll` / `I've` / `I're` (typo-form harmless) / `I am` (with space)
- `I build` / `I built` / `I like` / `I liked` / `I wonder` / `I wanted` / `I reach` / `I reached` / `I read` / `I architected` / `I chose` / `I haven` (matches `I haven't` since apostrophe terminates the word boundary) / `I wrote` / `I run` / `I set` / `I shipped` / `I added` / `I prefer` / `I care` / `I watch` / `I track` / `I love` / `I hate`
- `My approach` / `My favorite` / `My projects` / `My code` / `My work` / `My background` / `My stack` / `My version` / `My first`

Apply ONLY to: about.intro, about.p1, about.p2, about.p3, experience, projects[].caseStudy. Other fields (e.g., projects[].extendedReference.content from below-the-fence Projects/*.md) are OUT OF SCOPE — those are technical reference material, not voice-bearing prose.

**Self-test (B1):** tests/build/chat-knowledge-voice.test.ts MUST include a unit test that runs the canonical regex against a SNAPSHOT of the CURRENT (pre-fix) portfolio-context.json content (or against the literal string `"I build small services. I like tests. My favorite bug reports."`) and asserts at least N matches (proves the regex catches what it should). The same test then asserts ZERO matches against the post-fix artifact.

<!-- Chat-API mock pattern (mirror cache-hit-logs.test.ts) -->

cache-hit-logs.test.ts at tests/api/cache-hit-logs.test.ts demonstrates the canonical pattern:
  - vi.mock("cloudflare:workers", () => ({ env: { ANTHROPIC_API_KEY: "test-key" } }))
  - Mock @anthropic-ai/sdk default class with messages.create returning an async generator
  - buildRequest() builds a Request with Origin: https://jackcutrara.com and JSON body { messages: [...] }
  - drain(response) consumes the SSE body end-to-end

For chat-voice-split.test.ts, the mock returns a stream that yields content_block_delta events with text content "Hey Jack. What would you like to know?" — the test asserts the SSE body fragments are NOT echoed verbatim (because the API streams whatever Anthropic returns; the assertion is at the contract layer, NOT the model behavior layer). The REAL semantic test is the BUILD-TIME leak sweep — chat-knowledge-voice.test.ts is the binding gate.

The chat-voice-split.test.ts test serves as a REGRESSION TRIPWIRE for the system-prompt + portfolio-context contract: it asserts buildChatRequestArgs(portfolioContext, [{ role: 'user', content: 'hi' }]) — the actual call shape — produces a system block whose serialized text contains no first-person leading clauses (re-uses the same canonical regex applied to the live system block, not just the JSON file). This catches a regression where someone bypasses build-chat-context.mjs and edits portfolio-context.json directly.

<!-- B2: JSON loading without `as any` cast -->

In the chat-voice-split.test.ts test (Task 3 step 3), DO NOT use `import portfolioContext from "../../src/data/portfolio-context.json"` with `as any` — that requires `tsconfig.resolveJsonModule` and masks shape drift. Use the same `readFileSync + JSON.parse` pattern that the build-time test uses:

```typescript
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

const projectRoot = resolve(__dirname, "../..");
const portfolioContext = JSON.parse(
  readFileSync(resolve(projectRoot, "src/data/portfolio-context.json"), "utf8")
);
```

This is type-system-independent and the test fails LOUD if the file is missing or malformed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave-0 prerequisite, M8): Fix the pre-existing tests/content/roadmap-amendment.test.ts failure</name>
  <files>tests/content/roadmap-amendment.test.ts</files>
  <read_first>
    - tests/content/roadmap-amendment.test.ts (full file — current failure mode + assertion shape)
    - .planning/phases/17-foundations-migration-dns-debt-sweep/17-RETROSPECTIVE.md (carry-forward debt note for this test — likely a 2-line annotation fix per Plan 17-01 retrospective)
    - .planning/STATE.md "deferred-items.md" reference (the failing test is logged there as carry-forward debt from Plan 17-01)
    - .planning/phases/17-foundations-migration-dns-debt-sweep/deferred-items.md (if it exists — the canonical place this debt is tracked)
  </read_first>
  <behavior>
    - tests/content/roadmap-amendment.test.ts either passes (preferred) or is `it.skip(...)` with a clear comment pointing to the carry-forward debt entry. Either way, `pnpm test` exits 0 cleanly.
    - The fix is the minimal change consistent with the failure mode. If the test asserts roadmap content that has since been amended, update the assertion to match the current ROADMAP.md content. If the test is fundamentally outdated and a deeper fix is needed, mark it as `it.skip` with a comment referencing the deferred-items.md entry and a plan to revisit.
  </behavior>
  <action>
    **1. Diagnose the failure first.** Run `pnpm test tests/content/roadmap-amendment.test.ts` and capture the error message. The test was carried forward from Plan 17-01 per RETROSPECTIVE.md and has been failing for 6 plans (per STATE.md "383 PASS / 1 FAIL pre-existing").

    **2. Apply the minimal fix.** Two paths:

    Path A (preferred — assertion update): If the test asserts a specific ROADMAP.md substring that has since been amended (e.g., the test was written for a Phase-X wording that Phase-17 amendments superseded), update the assertion to match the current text. Typically a 1-2 line change.

    Path B (acceptable — skip with annotation): If the test asserts a structural invariant that no longer holds (e.g., a section header was removed in good faith and the test was not updated), mark as `it.skip` with this exact comment shape:
    ```typescript
    // SKIPPED Phase 17 Plan 17-07 (M8 from checker): pre-existing failure carried
    // forward from Plan 17-01. Test asserts {what} which no longer holds because
    // {why}. Tracked in .planning/phases/17-foundations-migration-dns-debt-sweep/deferred-items.md
    // for revisit in Phase 18 first-plan absorption.
    it.skip("...");
    ```

    **3. Verify the full suite is now GREEN.**
    ```bash
    pnpm test
    # Expected: ALL pass (was 383 PASS / 1 FAIL; now 384 PASS / 0 FAIL OR 383 PASS / 1 SKIP).
    ```

    If Path B is chosen, ALSO update `.planning/phases/17-foundations-migration-dns-debt-sweep/deferred-items.md` to note the skip (or create the file if it doesn't exist with the appropriate header).
  </action>
  <acceptance_criteria>
    - `pnpm test` exits 0 cleanly. Verify: `pnpm test` returns code 0; output shows 0 FAIL.
    - tests/content/roadmap-amendment.test.ts is either passing or marked `it.skip` (NOT `it.todo` — skip is observable in test output, todo is silent). Verify: `grep -cE "it\.(skip|todo)" tests/content/roadmap-amendment.test.ts` outputs `0` if the test passes, OR outputs `1` if Path B was chosen.
    - If Path B chosen: deferred-items.md updated with the skip rationale.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    Pre-existing tests/content/roadmap-amendment.test.ts failure is closed (passes or annotated-skip). Subsequent `pnpm test` exit-0 assertions in this plan and the rest of the gap-closure set are honest gates, not workarounds for known failures. Single commit named `fix(17-07): close pre-existing roadmap-amendment.test.ts failure (M8 prerequisite)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Author about-chat.ts (third-person about exports) + add chatSummary to all 6 MDX frontmatters</name>
  <files>src/data/about-chat.ts, src/content/projects/clipify.mdx, src/content/projects/daytrade.mdx, src/content/projects/nfl-predict.mdx, src/content/projects/optimize-ai.mdx, src/content/projects/seatwatch.mdx, src/content/projects/solsniper.mdx</files>
  <read_first>
    - src/data/about.ts (full file — first-person source-of-truth; the new third-person variants are paraphrases of the same content, NOT translations of unrelated text)
    - src/content/projects/seatwatch.mdx (frontmatter shape — title, tagline, description, techStack, source — the new chatSummary field slots in alongside these)
    - src/content/projects/solsniper.mdx (same — verify the frontmatter pattern is consistent)
    - src/content/projects/clipify.mdx, daytrade.mdx, nfl-predict.mdx, optimize-ai.mdx (same — all 6 MDX files must gain the field)
    - .planning/debug/chat-voice-split-regression.md "Affected artifacts" + "Missing pieces" sections (the spec for what voice the chat surface needs)
    - design-system/MASTER.md if it has a voice-guide section (verify third-person tone matches established conventions; if absent, defer to debug session guidance)
    - .planning/research/VOICE-GUIDE.md or similar (per STATE.md Phase 13 mention) if it exists (Voice contract reference)
  </read_first>
  <behavior>
    - About-chat.ts exports four constants ABOUT_CHAT_INTRO, ABOUT_CHAT_P1, ABOUT_CHAT_P2, ABOUT_CHAT_P3 — each a third-person paraphrase of the corresponding about.ts export ("I'm Jack" -> "Jack is", "I build" -> "Jack builds", "I reach" -> "Jack reaches", "Right now I'm looking" -> "Jack is currently looking", "I like tests" -> "Jack likes tests", "My favorite" -> "His favorite", "I wonder" -> "Jack wonders", etc.).
    - Each MDX file's frontmatter contains a `chatSummary:` field — a single-line YAML string (quoted, ≤ 600 chars) that summarizes the project in third-person past tense ("Jack architected SeatWatch as a Turborepo monorepo with four services...", NOT "I architected SeatWatch...").
    - No regex match for the BROADENED canonical regex (B1, see interfaces) in any of the new strings — Task 3's leak sweep validates this at build time.
  </behavior>
  <action>
    **1. Create `src/data/about-chat.ts`** — mirror about.ts shape but third-person voice. Use exactly the same Unicode escape conventions (`—` em-dash, ` ` non-breaking-space, `“`/`”` curly quotes) as about.ts so the JSON output is consistent. Each constant is a single-line double-quoted string literal (parseAboutExports regex requires this shape — see scripts/build-chat-context.mjs:200-228; about-chat.ts MUST follow the same constraint).

    Recommended content (paraphrases of about.ts; you have authoring discretion to refine wording, but each constant MUST be third-person and MUST avoid the BROADENED leak regex tokens — re-read the canonical regex token list in <interfaces> before authoring):

    ```typescript
    /**
     * About-page copy in THIRD PERSON for the chat widget surface (CHAT-06).
     * Consumed ONLY by scripts/build-chat-context.mjs when generating
     * src/data/portfolio-context.json. The site surface (homepage, about page)
     * reads from src/data/about.ts (first-person) — DO NOT swap them.
     *
     * Voice rule: third-person, present tense for ongoing state, past tense for
     * shipped work. Never first person — see .planning/debug/chat-voice-split-regression.md
     * for the regression these constants close.
     */

    /* Verified: <today> */
    export const ABOUT_CHAT_INTRO =
      "Jack is a junior software engineer who likes building systems that don't break at 3 a.m.";

    /* Verified: <today> */
    export const ABOUT_CHAT_P1 =
      "Jack builds small, production-grade services and the plumbing around them: caches, compilers, sync engines, APIs. Most of his projects start as “what actually happens here” and end as something he’d be comfortable handing off to a team.";

    /* Verified: <today> */
    export const ABOUT_CHAT_P2 =
      "Jack reaches for the boring tool first. He reads the spec before the blog post. He likes tests that fail loudly and code-review comments that start with “why.” His favorite bug reports are the ones that come with a reproduction.";

    /* Verified: <today> */
    export const ABOUT_CHAT_P3 =
      "Jack is currently looking for a junior or entry-level role on a team that cares about correctness, reliability, and performance — ideally one that will push him to get better at the parts of the stack he hasn’t touched yet.";
    ```

    Replace `<today>` with the date stamp matching about.ts convention. The wording above is a SUGGESTION — refine if a more natural third-person phrasing is available, but DO NOT add or remove identity claims (junior SWE, looking for entry-level role, etc.) and DO NOT introduce any token that matches the BROADENED leak regex. Specifically watch for "His favorite" (correct) vs "My favorite" (LEAK), "He likes" (correct) vs "I like" (LEAK).

    **2. Add `chatSummary:` to each MDX frontmatter.** Insert AFTER the `description:` field and BEFORE `featured:` so the YAML stays predictable. The summary MUST be:
    - Third-person past tense for shipped work, present tense for ongoing state
    - A single quoted string ≤ 600 chars (one paragraph, ~80-100 words)
    - Synthesized from the existing first-person caseStudy body — substantively the same content, voice flipped
    - Free of leak-regex tokens (canonical broadened regex from <interfaces>)

    Example for `src/content/projects/seatwatch.mdx` (insert at line 5, after `description:`):
    ```yaml
    chatSummary: "Jack architected SeatWatch as a Turborepo monorepo with four Railway-deployed services — a React 19 SPA, an Express REST API, a BullMQ worker, and a Next.js 16 SEO site. The booking engine is dual-strategy: a sub-second HTTP path that falls back to Patchright headless Chrome on recoverable errors. Concurrent safety rests on Redis SET NX EX distributed locks with UUID nonces, a per-user circuit breaker that survives worker restarts, and a 26-profile browser identity pool with deterministic per-user assignment. Stripe powers four billing tiers with transactional plan enforcement."
    ```

    Author the equivalent paragraph for each of the other 5 MDX files (clipify, daytrade, nfl-predict, optimize-ai, solsniper). Source material: each file's existing `## Approach & Architecture` + `## Outcome` sections. Voice flip "I" -> "Jack", "my" -> "his", "I built" -> "Jack built", etc.

    Voice rule for each chatSummary:
    - PROJECT NAMES are third-person neutral — keep "SeatWatch", "SolSniper", etc. exactly as-is
    - JACK'S ACTIONS are third-person past — "Jack chose", "Jack architected", "Jack shipped"
    - SYSTEM BEHAVIOR stays present tense — "the bot subscribes to ...", "the API endpoint serves ..."
    - NUMBERS over adjectives per system-prompt.ts <tone> banlist (39 test files, 6-step ladder, 26-profile pool — keep concrete counts)

    **3. Local verification (no build yet — that's Task 3):**
    ```bash
    pnpm tsx -e "import('./src/data/about-chat.ts').then(m => console.log(Object.keys(m)))"
    # Expected: ABOUT_CHAT_INTRO ABOUT_CHAT_P1 ABOUT_CHAT_P2 ABOUT_CHAT_P3 (all 4 named exports)

    grep -L 'chatSummary:' src/content/projects/*.mdx
    # Expected: empty output (every MDX file has the field)
    ```
  </action>
  <acceptance_criteria>
    - File `src/data/about-chat.ts` exists and exports exactly 4 named constants matching `^export const ABOUT_CHAT_(INTRO|P1|P2|P3)\s*=`. Verify: `grep -cE '^export const ABOUT_CHAT_(INTRO|P1|P2|P3)\s*=' src/data/about-chat.ts` outputs `4`.
    - Each export in about-chat.ts is a single-line double-quoted string literal (parseAboutChatExports regex compatibility). Verify: `grep -cE '^export const ABOUT_CHAT_\w+\s*=\s*"' src/data/about-chat.ts` outputs `4`.
    - All 6 MDX files have a `chatSummary:` frontmatter field. Verify: `grep -lE '^chatSummary:' src/content/projects/*.mdx | wc -l` outputs `6`.
    - No first-person leak in chatSummary lines, using the BROADENED canonical regex. Verify (must output 0):
      ```bash
      node -e "const fs=require('fs'); const re=/\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i; const files=['clipify','daytrade','nfl-predict','optimize-ai','seatwatch','solsniper']; for (const f of files) { const src=fs.readFileSync('src/content/projects/'+f+'.mdx','utf8'); const fm=src.split(/^---$/m)[1]; const cs=(fm.match(/^chatSummary:.*$/m)||[''])[0]; if (re.test(cs)) { console.error('LEAK in '+f+':',cs.slice(0,120)); process.exit(1); } } console.log('OK');"
      ```
    - No first-person leak in about-chat.ts (excluding comments). Verify (must output 0):
      ```bash
      node -e "const fs=require('fs'); const re=/\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i; const ac=fs.readFileSync('src/data/about-chat.ts','utf8'); const stripped=ac.split('\n').filter(l=>!/^\s*(\/\*|\*|\/\/)/.test(l)).join('\n'); if (re.test(stripped)) { console.error('LEAK in about-chat.ts'); process.exit(1); } console.log('OK');"
      ```
    - src/data/about.ts is BYTE-IDENTICAL to its pre-plan state. Verify: `git diff src/data/about.ts` shows no changes.
    - MDX bodies (below the frontmatter `---` closing fence) BYTE-IDENTICAL to pre-plan state. Verify: `git diff src/content/projects/*.mdx` shows ONLY frontmatter changes (chatSummary insertion); no body changes.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/content/projects/clipify.mdx','src/content/projects/daytrade.mdx','src/content/projects/nfl-predict.mdx','src/content/projects/optimize-ai.mdx','src/content/projects/seatwatch.mdx','src/content/projects/solsniper.mdx']; const re=/\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i; for (const f of files) { const src=fs.readFileSync(f,'utf8'); const fm=src.split(/^---$/m)[1]||''; if (!/^chatSummary:/m.test(fm)) { console.error('MISSING chatSummary:',f); process.exit(1); } const cs=fm.match(/^chatSummary:.*$/m)[0]; if (re.test(cs)) { console.error('LEAK in chatSummary:',f); process.exit(1); } } const ac=fs.readFileSync('src/data/about-chat.ts','utf8'); if (!/^export const ABOUT_CHAT_INTRO\s*=/m.test(ac)) { console.error('MISSING ABOUT_CHAT_INTRO'); process.exit(1); } const acStripped=ac.split('\n').filter(l=>!/^\s*(\/\*|\*|\/\/)/.test(l)).join('\n'); if (re.test(acStripped)) { console.error('LEAK in about-chat.ts'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    about-chat.ts exists with 4 third-person exports. All 6 MDX frontmatters have a chatSummary field. about.ts unchanged; MDX bodies unchanged. No BROADENED leak-regex matches in any of the new strings. Single commit named `feat(17-07): add third-person chat-voice variants (about-chat.ts + MDX chatSummary)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend build-chat-context.mjs with BROADENED leak guard + update sync-check.yml path triggers; regenerate portfolio-context.json</name>
  <files>scripts/build-chat-context.mjs, src/data/portfolio-context.json, .github/workflows/sync-check.yml</files>
  <read_first>
    - scripts/build-chat-context.mjs (full file — focus on parseAboutExports L200-228, buildProjectBlock L238-269, main() L271-457; the new code mirrors parseAboutExports for ABOUT_CHAT_* and adds a chatSummary read in buildProjectBlock)
    - src/data/about-chat.ts (Task 1 output — the source the new parseAboutChatExports reads from)
    - src/content/projects/*.mdx (Task 1 output — the chatSummary frontmatter the generator now reads)
    - src/data/portfolio-context.json (current generated artifact; will be REGENERATED by this task — confirm pre-state for the diff)
    - .github/workflows/sync-check.yml (current path-trigger list — verify whether `src/data/about-chat.ts` is covered by the existing patterns; M1 fix below)
    - .planning/debug/chat-voice-split-regression.md "Missing pieces" #3 (the leak-regex spec)
  </read_first>
  <behavior>
    - parseAboutChatExports() reads ABOUT_CHAT_INTRO/P1/P2/P3 from about-chat.ts using the same regex shape as parseAboutExports (single-line double-quoted string literal); throws named error on any missing export.
    - buildProjectBlock() reads chatSummary from frontmatter; if absent, throws a named error pointing at the MDX file (hard fail — all 6 MDX files MUST have it).
    - main() composes aboutBlock from about-chat.ts (NOT about.ts), composes experience from ABOUT_CHAT_INTRO + ABOUT_CHAT_P1 + ABOUT_CHAT_P3 (NOT first-person variants), substitutes chatSummary for caseStudy in each project block.
    - leakGuard(merged) walks about.intro / about.p1 / about.p2 / about.p3 / experience / projects[].caseStudy and matches against the BROADENED canonical regex (B1). Any match -> process.stderr.write a named error and process.exit(2).
    - portfolio-context.json regenerates: about.* + experience contain Jack-prefixed third-person prose; each project.caseStudy contains the chatSummary string; no first-person leak in chat-bound fields.
    - extendedReference.content (below-fence Projects/*.md) is UNCHANGED from current shape — that's technical reference material, not voice-bearing prose, and it remains first-person if the source is. DO NOT touch the leak-guard scope.
    - .github/workflows/sync-check.yml path triggers include `src/data/about-chat.ts` (M1 fix) so PRs touching the new file trigger the leak-guard CI job.
  </behavior>
  <action>
    **1. Add `parseAboutChatExports(sourceContent)` near parseAboutExports (around line 228).** Mirror the existing function exactly, just with a different name list:

    ```javascript
    /**
     * Extract ABOUT_CHAT_INTRO, ABOUT_CHAT_P1..P3 from src/data/about-chat.ts.
     * Mirrors parseAboutExports — same regex shape, same error contract.
     * The source file is the third-person variant the chat widget consumes.
     * See .planning/debug/chat-voice-split-regression.md for the gap this closes.
     */
    export function parseAboutChatExports(sourceContent) {
      const names = ["ABOUT_CHAT_INTRO", "ABOUT_CHAT_P1", "ABOUT_CHAT_P2", "ABOUT_CHAT_P3"];
      const result = {};
      for (const name of names) {
        const re = new RegExp(
          `export const ${name}\\s*=\\s*("(?:[^"\\\\]|\\\\.)*")`,
          "m"
        );
        const m = re.exec(sourceContent);
        if (!m) {
          const existsAtAll = new RegExp(`export const ${name}\\b`, "m").test(sourceContent);
          if (!existsAtAll) {
            throw new Error(
              `src/data/about-chat.ts: missing export const ${name} — add it to about-chat.ts`
            );
          }
          throw new Error(
            `src/data/about-chat.ts: export const ${name} not in single-line double-quoted form — ` +
              `either normalize back to a \`"..."\` literal OR extend parseAboutChatExports() to handle template literals`
          );
        }
        result[name] = JSON.parse(m[1]);
      }
      return result;
    }
    ```

    Add `const ABOUT_CHAT_TS_PATH = "src/data/about-chat.ts";` near `ABOUT_TS_PATH` at line 41.

    **2. Modify `buildProjectBlock()` (around line 238).** Read chatSummary from frontmatter; substitute for body in caseStudy:

    ```javascript
    const chatSummary = readStringField(frontmatterBlock, "chatSummary");
    if (!chatSummary) {
      throw new Error(
        `${basename(mdxPath)}: missing chatSummary: frontmatter field — required for chat-voice-split (CHAT-06). See .planning/debug/chat-voice-split-regression.md`
      );
    }
    ```

    Then in the block construction:
    ```javascript
    const block = {
      name: title,
      description,
      tech,
      page: `/projects/${slug}`,
      caseStudy: chatSummary, // CHAT-06 voice-split: third-person summary for chat (was body.trimEnd() — first-person leaked into knowledge)
      extendedReference: {
        content,
        truncated,
      },
    };
    ```

    Update the per-project log line to clarify what's now in caseStudy (use `block.caseStudy` for wordCount).

    **3. Modify `main()` step 4 (around line 367-380)** to read about-chat.ts INSTEAD OF about.ts:

    ```javascript
    let aboutBlock;
    try {
      const aboutChatRaw = normalize(await readFile(ABOUT_CHAT_TS_PATH, "utf8"));
      const parsed = parseAboutChatExports(aboutChatRaw);
      aboutBlock = {
        intro: parsed.ABOUT_CHAT_INTRO,
        p1: parsed.ABOUT_CHAT_P1,
        p2: parsed.ABOUT_CHAT_P2,
        p3: parsed.ABOUT_CHAT_P3,
      };
    } catch (err) {
      process.stderr.write(`ERROR ${ABOUT_CHAT_TS_PATH}: ${err.message}\n`);
      process.exit(2);
    }
    ```

    The experience composition naturally produces a third-person string since aboutBlock now sources from the chat variant.

    **4. Add `checkFirstPersonLeaks(merged)` after main()'s step 6 merge but BEFORE step 7 token budget (around line 408).** Use the BROADENED canonical regex (B1):

    ```javascript
    /**
     * First-person leak guard — exits 2 if any chat-bound field contains a
     * first-person leading clause. Closes UAT Gap #1
     * (.planning/debug/chat-voice-split-regression.md): the chat surface MUST
     * speak ABOUT Jack, never AS Jack. extendedReference.content is OUT OF
     * SCOPE — that's technical reference material from below-the-fence
     * Projects/*.md, not voice-bearing prose authored for either surface.
     *
     * BROADENED regex per Plan 17-07 revision (B1): covers present-tense "I build",
     * "I like", "My favorite", "I haven't" / "I'd be" contractions, and "I wonder".
     * The original regex missed several tokens currently present in the pre-fix
     * portfolio-context.json. The same canonical regex is used in
     * tests/build/chat-knowledge-voice.test.ts and tests/api/chat-voice-split.test.ts.
     */
    function checkFirstPersonLeaks(merged) {
      const leakRe = /\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i;
      const targets = [
        ["about.intro", merged.about?.intro],
        ["about.p1", merged.about?.p1],
        ["about.p2", merged.about?.p2],
        ["about.p3", merged.about?.p3],
        ["experience", merged.experience],
        ...merged.projects.map((p, i) => [`projects[${i}].caseStudy (${p.page})`, p.caseStudy]),
      ];
      const leaks = [];
      for (const [field, value] of targets) {
        if (typeof value !== "string") continue;
        const m = leakRe.exec(value);
        if (m) {
          leaks.push({ field, match: m[0], excerpt: value.slice(Math.max(0, m.index - 20), m.index + 60) });
        }
      }
      if (leaks.length > 0) {
        process.stderr.write(
          `ERROR first-person voice leak in chat-knowledge JSON (CHAT-06 contract violation):\n`
        );
        for (const { field, match, excerpt } of leaks) {
          process.stderr.write(`  ${field}: matched "${match}" near "...${excerpt}..."\n`);
        }
        process.stderr.write(
          `Fix: edit src/data/about-chat.ts or the matching MDX chatSummary frontmatter; voice MUST be third person ("Jack built X", "Jack chose Y"). See .planning/debug/chat-voice-split-regression.md.\n`
        );
        process.exit(2);
      }
    }
    ```

    Call `checkFirstPersonLeaks(merged);` immediately after the `merged` object is composed (after line 406 `about: aboutBlock,` but BEFORE the token-budget block).

    **5. Update `.github/workflows/sync-check.yml` path triggers (M1 fix).** Verify the current trigger list (read at top of task). Currently the list covers:
    - `Projects/**`
    - `src/content/projects/**`
    - `src/data/about.ts`
    - `src/data/portfolio-context.static.json`
    - `src/data/portfolio-context.json`
    - `scripts/sync-projects.mjs`
    - `scripts/build-chat-context.mjs`

    Add `src/data/about-chat.ts` to the list. Use the Edit tool with these exact strings:

    - **old_string:**
      ```yaml
            - "src/data/about.ts"
            - "src/data/portfolio-context.static.json"
      ```
    - **new_string:**
      ```yaml
            - "src/data/about.ts"
            - "src/data/about-chat.ts"
            - "src/data/portfolio-context.static.json"
      ```

    **6. Regenerate the artifact:**
    ```bash
    pnpm build:chat-context
    # Expected: src/data/portfolio-context.json: written
    # If leak guard fires: investigate and fix Task 1 strings, re-run.
    ```

    Per m1: the post-fix est_tokens may drop substantially. Pre-compute via the printed `est_tokens` line. If `est_tokens < 5500`, consider bumping the chatSummary cap (current ≤ 600 chars) or adding a hard-fail gate. Initial Phase 17 baseline target: est_tokens ≥ 4096 (Haiku cache minimum). If est_tokens drops below 4096, the cache silently disables — flag in SUMMARY but do NOT pad content; the static identity + extendedReference.content combined still exceed MIN_TOKEN_FLOOR.

    **7. Local verification:**
    ```bash
    pnpm build:chat-context:check    # Must exit 0 — no drift between source and artifact
    grep -E '"intro"\s*:\s*"Jack' src/data/portfolio-context.json | head -1
    grep -E '"caseStudy"\s*:\s*"I ' src/data/portfolio-context.json
    grep -E '"experience"\s*:\s*"I ' src/data/portfolio-context.json
    ```

    If the build fails with the leak guard, the diagnostic message points at the offending field — fix Task 1's authored strings and re-run.
  </action>
  <acceptance_criteria>
    - `pnpm build:chat-context:check` exits 0 — no drift between source and generated artifact.
    - File `src/data/portfolio-context.json` exists and parses as valid JSON.
    - portfolio-context.json `about.intro` starts with "Jack". Verify: `node -e "const j=require('./src/data/portfolio-context.json'); if (!/^Jack/.test(j.about.intro)) { console.error('about.intro not third-person:', j.about.intro); process.exit(1); }"` exits 0.
    - portfolio-context.json contains no first-person leak in chat-bound fields per BROADENED regex. Verify (must exit 0):
      ```bash
      node -e "const j=require('./src/data/portfolio-context.json'); const re=/\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i; const fields=[['about.intro',j.about.intro],['about.p1',j.about.p1],['about.p2',j.about.p2],['about.p3',j.about.p3],['experience',j.experience],...j.projects.map(p=>['caseStudy '+p.page,p.caseStudy])]; for (const [k,v] of fields) { if (re.test(v)) { console.error('LEAK in '+k+':',v.slice(0,80)); process.exit(1); } }"
      ```
    - scripts/build-chat-context.mjs imports from about-chat.ts. Verify: `grep -c 'about-chat' scripts/build-chat-context.mjs` outputs `>= 2`.
    - scripts/build-chat-context.mjs contains the leak-guard function. Verify: `grep -c 'checkFirstPersonLeaks' scripts/build-chat-context.mjs` outputs `>= 2`.
    - scripts/build-chat-context.mjs reads chatSummary from MDX frontmatter. Verify: `grep -c 'chatSummary' scripts/build-chat-context.mjs` outputs `>= 1`.
    - `.github/workflows/sync-check.yml` includes `src/data/about-chat.ts` in path triggers. Verify: `grep -c 'src/data/about-chat.ts' .github/workflows/sync-check.yml` outputs `>= 1`.
    - Generated portfolio-context.json est_tokens (printed by build) is ≥ 4096 (Haiku cache minimum).
    - `pnpm test` exits 0 — D-26 chat regression battery preserved (Task 0 closed the pre-existing failure).
  </acceptance_criteria>
  <verify>
    <automated>pnpm build:chat-context:check && pnpm test</automated>
  </verify>
  <done>
    build-chat-context.mjs reads about-chat.ts and chatSummary frontmatter; substitutes them for first-person originals; BROADENED leak guard exits 2 on any chat-bound first-person regression. portfolio-context.json regenerated and contains only third-person prose in chat-bound fields. sync-check.yml path triggers cover the new file (M1). est_tokens ≥ 4096. D-26 GREEN. Single commit named `feat(17-07): build-chat-context broadened leak guard + about-chat sources + sync-check.yml triggers (closes Gap #1 root cause)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Defense-in-depth — system-prompt.ts <role> hardening + chat-voice-split regression test + build-time leak test (with B1 self-test, B2 readFileSync pattern)</name>
  <files>src/prompts/system-prompt.ts, tests/build/chat-knowledge-voice.test.ts, tests/api/chat-voice-split.test.ts</files>
  <read_first>
    - src/prompts/system-prompt.ts (full file — the <role> block at lines 4-6 receives a single-sentence callout; <knowledge> block at lines 57-59 unchanged in shape)
    - src/prompts/chat-request-shape.ts (the buildChatRequestArgs signature — the test imports it directly to inspect the system block byte stream)
    - tests/api/cache-hit-logs.test.ts (the canonical mocking pattern — vi.mock cloudflare:workers, mock Anthropic SDK, drain SSE)
    - tests/api/anthropic-payload-shape.test.ts (TEST-03 forward-defense pattern — direct buildChatRequestArgs invocation + JSON.stringify on system block)
    - tests/build/project-md-debt-01.test.ts (build-time test pattern — readFileSync + grep-style regex assertions)
    - .planning/debug/chat-voice-split-regression.md "Closure path" step 3 (defense-in-depth instruction)
    - tests/api/sse-snapshot.test.ts (D-15 anchor — must remain GREEN after system-prompt.ts edit)
  </read_first>
  <behavior>
    - system-prompt.ts <role> block contains a single new sentence (or short paragraph) instructing the model to translate first-person citations to third-person.
    - tests/build/chat-knowledge-voice.test.ts: 4 tests including a B1 SELF-TEST that asserts the regex catches known first-person tokens (proves the regex catches what it should, not just what's currently in the file).
    - tests/api/chat-voice-split.test.ts: 2 tests using `readFileSync + JSON.parse` to load portfolio-context.json (B2: NO `as any` cast).
    - D-15 sse-snapshot test remains GREEN — system-prompt content does not flow into SSE response bytes.
    - D-26 chat regression battery remains GREEN.
  </behavior>
  <action>
    **1. Edit `src/prompts/system-prompt.ts`** — append a single paragraph to the <role> block (between current line 5 and line 6):

    ```
    The knowledge below was authored by Jack in first-person voice for his website surface (homepage, project pages). When you cite, paraphrase, or quote from it, REWRITE first-person ("I built X", "I'm Jack", "my approach") as third-person ("Jack built X", "Jack is...", "Jack's approach"). Never echo first-person prose verbatim — that would address the visitor as if they were Jack, which is the opposite of your role.
    ```

    Use the Edit tool. The diff is purely additive within the existing `<role>...</role>` opening tag — no new XML blocks, no reordering.

    **2. Author `tests/build/chat-knowledge-voice.test.ts` with B1 self-test:**

    ```typescript
    /**
     * UAT Gap #1 (BLOCKER) — chat-knowledge JSON voice-split regression.
     *
     * Asserts the GENERATED artifact (src/data/portfolio-context.json) contains
     * only third-person prose in chat-bound fields. Mirrors the leak-guard in
     * scripts/build-chat-context.mjs (defense in depth).
     *
     * BROADENED regex per Plan 17-07 revision (B1): covers present-tense "I build",
     * "I like", "My favorite", "I haven't" / "I'd be" contractions, and "I wonder".
     * Includes a B1 SELF-TEST that proves the regex catches known first-person
     * tokens — not just whatever happens to be in the post-fix artifact.
     *
     * See .planning/debug/chat-voice-split-regression.md for full diagnosis.
     */
    import { describe, it, expect } from "vitest";
    import { readFileSync } from "node:fs";
    import { resolve } from "node:path";

    const projectRoot = resolve(__dirname, "../..");
    const ctx = JSON.parse(
      readFileSync(resolve(projectRoot, "src/data/portfolio-context.json"), "utf8")
    );

    // BROADENED canonical regex (Plan 17-07 revision B1). Covers:
    //   - I'm / I'd / I'll / I've / I am
    //   - I build / built / like / liked / wonder / wanted / reach / reached / read /
    //     architected / chose / haven / wrote / run / set / shipped / added / prefer /
    //     care / watch / track / love / hate
    //   - My approach / favorite / projects / code / work / background / stack / version / first
    const FIRST_PERSON_LEAK = /\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i;

    describe("UAT Gap #1: B1 self-test — regex catches known first-person tokens", () => {
      // SELF-TEST: prove the regex catches what it SHOULD catch, not just whatever
      // happens to be in the post-fix portfolio-context.json. If a future regex
      // weakening lets these tokens through, this test fails before the artifact
      // sweep tests have a chance to silently pass.
      const KNOWN_LEAKS = [
        "I'm Jack",
        "I built SeatWatch",
        "I architected the system",
        "I chose Postgres",
        "I wanted a faster path",
        "I reach for the boring tool",
        "I read the spec",
        "I build small services",
        "I like tests that fail loudly",
        "I wonder how that actually works",
        "I haven't touched that yet",
        "I'd be comfortable handing off",
        "I'll push to main",
        "I've shipped 6 projects",
        "My approach is",
        "My favorite bug reports",
      ];

      for (const sample of KNOWN_LEAKS) {
        it(`regex catches: "${sample}"`, () => {
          expect(sample).toMatch(FIRST_PERSON_LEAK);
        });
      }

      it("regex does NOT match third-person variants (negative control)", () => {
        const SAFE = [
          "Jack is a junior software engineer",
          "Jack builds small services",
          "Jack likes tests that fail loudly",
          "His favorite bug reports",
          "Jack's approach is",
          "Jack reaches for the boring tool",
        ];
        for (const sample of SAFE) {
          expect(sample, `false-positive for "${sample}"`).not.toMatch(FIRST_PERSON_LEAK);
        }
      });
    });

    describe("UAT Gap #1: chat-knowledge JSON voice contract (CHAT-06)", () => {
      it("about.intro is third-person (starts with 'Jack' or other third-person noun, not 'I')", () => {
        expect(ctx.about?.intro).toBeTypeOf("string");
        expect(ctx.about.intro).not.toMatch(/^I[' ]/);
        expect(ctx.about.intro).toMatch(/^Jack/);
      });

      it("no first-person leak in about.{intro,p1,p2,p3} or experience", () => {
        const fields = [
          ["about.intro", ctx.about?.intro],
          ["about.p1", ctx.about?.p1],
          ["about.p2", ctx.about?.p2],
          ["about.p3", ctx.about?.p3],
          ["experience", ctx.experience],
        ];
        for (const [name, value] of fields) {
          expect(value, `${name} should be a string`).toBeTypeOf("string");
          expect(value, `${name} contains first-person leak`).not.toMatch(FIRST_PERSON_LEAK);
        }
      });

      it("every project has a non-empty caseStudy and no first-person leak", () => {
        expect(Array.isArray(ctx.projects)).toBe(true);
        expect(ctx.projects.length).toBeGreaterThan(0);
        for (const p of ctx.projects) {
          expect(p.caseStudy, `${p.page}.caseStudy is empty`).toBeTruthy();
          expect(typeof p.caseStudy).toBe("string");
          expect(p.caseStudy.length, `${p.page}.caseStudy too short`).toBeGreaterThan(40);
          expect(p.caseStudy, `${p.page}.caseStudy contains first-person leak`).not.toMatch(FIRST_PERSON_LEAK);
        }
      });
    });
    ```

    Run: `pnpm test tests/build/chat-knowledge-voice.test.ts` — expect ALL GREEN.

    **3. Author `tests/api/chat-voice-split.test.ts` using readFileSync + JSON.parse (B2 fix — NO `as any`):**

    ```typescript
    /**
     * UAT Gap #1 (BLOCKER) — chat-API voice-split regression tripwire.
     *
     * Asserts the LIVE system block (output of buildChatRequestArgs) contains
     * no first-person leak. Distinct from tests/build/chat-knowledge-voice.test.ts
     * (which asserts the JSON artifact alone) — this test exercises the actual
     * call shape Anthropic receives, including the <role> instruction context.
     *
     * B2 fix: load portfolio-context.json via readFileSync + JSON.parse rather
     * than `import ... as any` — type-system independent + fails loud if the
     * file is missing/malformed.
     *
     * See .planning/debug/chat-voice-split-regression.md for full diagnosis.
     */
    import { describe, it, expect } from "vitest";
    import { readFileSync } from "node:fs";
    import { resolve } from "node:path";
    import { buildChatRequestArgs } from "../../src/prompts/chat-request-shape";

    const projectRoot = resolve(__dirname, "../..");
    const portfolioContext = JSON.parse(
      readFileSync(resolve(projectRoot, "src/data/portfolio-context.json"), "utf8")
    );

    // Same BROADENED canonical regex as tests/build/chat-knowledge-voice.test.ts.
    const FIRST_PERSON_LEAK = /\b(I(?:'|\s)(?:m\b|d\b|ll\b|ve\b|re\b|am\b)|I\s+(?:build|built|like|liked|wonder|wanted|reach|reached|read|architected|chose|haven|wrote|run|set|shipped|added|prefer|care|watch|track|love|hate)|My\s+(?:approach|favorite|projects|code|work|background|stack|version|first))\b/i;

    describe("UAT Gap #1: chat-API system block voice-split tripwire (CHAT-06)", () => {
      const args = buildChatRequestArgs(portfolioContext, [
        { role: "user", content: "hi" },
      ]);

      it("system block (full serialized payload) contains no first-person leading clauses", () => {
        const systemText = JSON.stringify(args.system);
        const m = FIRST_PERSON_LEAK.exec(systemText);
        if (m) {
          const idx = systemText.indexOf(m[0]);
          const excerpt = systemText.slice(Math.max(0, idx - 40), idx + 80);
          throw new Error(
            `First-person leak in system block: matched "${m[0]}" near "${excerpt}"`
          );
        }
        expect(systemText).not.toMatch(FIRST_PERSON_LEAK);
      });

      it("system block contains the third-person biographer instruction (defense-in-depth)", () => {
        const systemText = JSON.stringify(args.system);
        // <role> block opens with this canonical phrase — guards against accidental deletion.
        expect(systemText).toContain("third-person biographer");
        // Defense-in-depth callout from Task 3 step 1 — guards against accidental deletion.
        expect(systemText).toContain("rewrite first-person");
      });
    });
    ```

    Run: `pnpm test tests/api/chat-voice-split.test.ts` — expect 2/2 GREEN.

    **4. Run the D-15 + D-26 anchor checks:**
    ```bash
    pnpm test tests/api/sse-snapshot.test.ts
    pnpm test
    ```
  </action>
  <acceptance_criteria>
    - File `src/prompts/system-prompt.ts` contains the new defense-in-depth instruction. Verify: `grep -c 'rewrite first-person' src/prompts/system-prompt.ts | grep -v "^0$"` exits 0.
    - File `tests/build/chat-knowledge-voice.test.ts` exists with B1 self-test + 3 artifact tests. Verify: `grep -c "^  it(" tests/build/chat-knowledge-voice.test.ts` outputs `>= 4` (some tests are inside the for-loop self-test describe block; total it() count includes the loop expansion).
    - tests/build/chat-knowledge-voice.test.ts uses `readFileSync + JSON.parse`. Verify: `grep -c 'readFileSync' tests/build/chat-knowledge-voice.test.ts` outputs `>= 1`.
    - tests/api/chat-voice-split.test.ts uses `readFileSync + JSON.parse` (B2 fix; NO `as any`). Verify (must exit 0): `grep -c 'readFileSync' tests/api/chat-voice-split.test.ts` outputs `>= 1` AND `grep -c 'as any' tests/api/chat-voice-split.test.ts` outputs `0`.
    - File `tests/api/chat-voice-split.test.ts` exists with 2 tests. Verify: `grep -c "^  it(" tests/api/chat-voice-split.test.ts` outputs `2`.
    - `pnpm test tests/build/chat-knowledge-voice.test.ts` exits 0 (B1 self-test catches known leaks; artifact tests pass).
    - `pnpm test tests/api/chat-voice-split.test.ts` exits 0 (2/2 GREEN).
    - `pnpm test tests/api/sse-snapshot.test.ts` exits 0 (D-15 anchor preserved).
    - `pnpm test` exits 0 (full suite GREEN, since Task 0 closed the pre-existing failure).
    - `pnpm exec astro check` exits 0 — no new typecheck errors introduced.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    Defense-in-depth instruction added to <role> block. Build-time test includes B1 self-test (regex catches known leaks) + 3 artifact contract tests. API-surface test uses readFileSync + JSON.parse (B2 fix). D-15 sse-snapshot GREEN. D-26 GREEN. Single commit named `test(17-07): chat-voice-split regression tests + B1 regex self-test + B2 readFileSync (no as-any cast)`.

    After all 3 tasks: post-deploy manual UAT — open chat on https://jackcutrara.com (after next deploy), send "hi", verify response addresses visitor in second person and refers to Jack in third person.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (M7 metadata commit): Update STATE.md / ROADMAP.md / REQUIREMENTS.md to reflect re-opened Phase 17 with 4 gap-closure plans (Wave 7-10)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (current state — `total_plans: 6`, `completed_plans: 6`, `status: completed` for milestone v1.3 / Phase 17)
    - .planning/ROADMAP.md (Phase 17 row — needs to show wave 7-10 added (M-iter2 wave correction); gap-closure section)
    - .planning/REQUIREMENTS.md (any new acceptance criteria from the gap diagnoses — UAT-GAP-01..04)
    - .planning/phases/17-foundations-migration-dns-debt-sweep/17-UAT.md (the 4 gaps with diagnoses — source for new acceptance criteria)
  </read_first>
  <behavior>
    - STATE.md `progress.total_plans` updated 6 -> 10. `completed_plans` stays at 6 until plans 17-07..10 commit-close (orchestrator updates it via gsd-sdk during execute-plan).
    - STATE.md `status` updated from "completed" to "in_progress" or similar (re-opening the phase).
    - STATE.md `stopped_at` + `last_activity` updated to note Plan 17-07 (gap closure Wave 7) is the current position.
    - STATE.md notes that FOUND-03 Pages retirement is still pending and NOW WAITS for these gap closures to deploy (cannot retire Pages while production chat panel is broken — current production works only because old chat.ts hasn't been deployed; deploying without Plan 17-08 breaks production).
    - ROADMAP.md Phase 17 row updated to show waves 7, 8, 9, 10 added (M-iter2 corrected wave layout); new gap-closure section listing plans 17-07..10 with their corrected wave assignments.
    - REQUIREMENTS.md gains UAT-GAP-01..04 acceptance criteria sourced from 17-UAT.md gap diagnoses.
  </behavior>
  <action>
    **1. Update `.planning/STATE.md`** frontmatter:

    - Change `status: completed` -> `status: in_progress` (or whatever the schema uses — check `gsd-sdk query frontmatter.validate` for STATE.md schema).
    - Update `progress.total_plans: 6` -> `progress.total_plans: 10`.
    - Update `progress.percent` if needed (10/10 of plans for Phase 17 is still 25% of overall 4 phases; recompute if denominator differs).
    - Update `stopped_at` and `last_activity` strings to note the gap-closure re-open and the 4-blocker UAT diagnoses.

    Add a new section in the body documenting the re-open:
    ```markdown
    ### Phase 17 Re-Opened (Gap Closure — 2026-05-XX)

    Phase 17 was CLOSED at Plan 17-06 with all 14 requirements GREEN per design.
    Subsequent UAT (17-UAT.md) surfaced 4 blockers + 1 RELEASE BLOCKER on local main
    (23 commits ahead of origin/main):
    - Gap #1 (BLOCKER): chat voice-split regression — chat addresses visitor as Jack
    - Gap #2 (BLOCKER + RELEASE BLOCKER): #chat-panel does not open on `pnpm dev`
    - Gap #3 (major): COPY button has no visible COPIED transition
    - Gap #4 (major): cross-document nav surfaces AbortError in DevTools console

    Gap-closure plans 17-07..10 (Wave 7-10 serial, M-iter2 corrected) re-open Phase 17
    to ship the four fixes. FOUND-03 Pages retirement now waits for these to deploy —
    production "works" today only because origin/main is 23 commits behind local main;
    deploying DEBT-05 (commit 1c148c9) without Plan 17-08 breaks production chat panel-open.
    Plan 17-08 is THE prerequisite for next `git push origin main`.
    ```

    **2. Update `.planning/ROADMAP.md`** Phase 17 row:

    - Add a new sub-section "Wave 7-10 Gap-Closure Addendum (post-UAT, M-iter2 corrected)":
      ```markdown
      Wave 7: 17-07 (chat voice-split)
      Wave 8: 17-09 (COPY button)        — depends_on 17-07
      Wave 9: 17-10 (pageswap handler)   — depends_on 17-09
      Wave 10: 17-08 (chat panel inline display fix — RELEASE BLOCKER, runs LAST) — depends_on 17-10

      All four plans run serially per CONTEXT.md D-10 (chat-surface mutations cannot
      run parallel without muddying D-26 attribution). Per the wave rule
      (Wave = max(deps) + 1), each plan in the chain occupies its OWN wave so a
      wave-batching orchestrator cannot accidentally parallelize chat-surface
      mutations (M-iter2 wave correction). Plan 17-08 is gated last as the deploy
      gate (see DEPLOY-GATE.md).
      ```

    - Update plan list:
      ```
      Plans:
      - [ ] 17-07-PLAN.md — Chat voice-split regression (UAT Gap #1)            — Wave 7
      - [ ] 17-08-PLAN.md — Chat panel inline display:none fix (UAT Gap #2 / RELEASE BLOCKER) — Wave 10
      - [ ] 17-09-PLAN.md — COPY button feedback window (UAT Gap #3)             — Wave 8
      - [ ] 17-10-PLAN.md — pageswap AbortError handler (UAT Gap #4)             — Wave 9
      ```

    **3. Update `.planning/REQUIREMENTS.md`** with new UAT-GAP-* acceptance criteria. Add a new section:

    ```markdown
    ### UAT Gap Closure Requirements (Phase 17 Wave 7-10)

    **UAT-GAP-01** — Chat voice-split (CHAT-06) holds at the chat surface:
      - chat-knowledge JSON contains zero first-person leading clauses in about.* / experience / projects[].caseStudy (verified by tests/build/chat-knowledge-voice.test.ts B1-broadened regex)
      - Live system block contains zero first-person leading clauses (verified by tests/api/chat-voice-split.test.ts)
      - Defense-in-depth instruction present in system-prompt.ts <role> block

    **UAT-GAP-02** — #chat-panel opens on `pnpm dev` AND `pnpm preview` AND production:
      - ChatWidget.astro inline style attribute contains no `display:` declaration (verified by tests/build/no-inline-display-on-chat-panel.test.ts)
      - chat-panel-display.test.ts fixture mirrors real ChatWidget.astro markup
      - DEPLOY-GATE.md operator confirmation cleared before `git push origin main`

    **UAT-GAP-03** — COPY button visibly transitions to COPIED for ~1500ms:
      - .chat-copy-btn.copy-success CSS rule wired (opacity:1 + color:var(--accent))
      - chat.ts uses single COPY_FEEDBACK_MS = 1500 constant for both timeout windows
      - Inline color writes removed from chat.ts (M3 — class toggle is single source of truth)

    **UAT-GAP-04** — Cross-document navigation produces no AbortError in DevTools console:
      - BaseLayout.astro head registers pageswap handler with .finished.catch
      - Handler is is:inline AND in head (registers before first link click)
      - MOTION.md §5 MOTN-01 documents the rejection-handling contract
    ```

    **4. Verify the metadata commit:**
    ```bash
    pnpm test  # full suite GREEN — metadata changes don't affect runtime tests
    ```

    Update SUMMARY at end of phase to note this metadata task closed.
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` frontmatter `total_plans` updated to 10. Verify: `grep -c 'total_plans: 10' .planning/STATE.md` outputs `1`.
    - `.planning/STATE.md` body contains "Phase 17 Re-Opened" section. Verify: `grep -c 'Phase 17 Re-Opened' .planning/STATE.md` outputs `1`.
    - `.planning/ROADMAP.md` lists all 4 gap-closure plans. Verify: `grep -cE '17-(07|08|09|10)-PLAN\.md' .planning/ROADMAP.md` outputs `>= 4`.
    - `.planning/ROADMAP.md` records the corrected per-plan wave assignments (7, 8, 9, 10). Verify: `grep -cE 'Wave (7|8|9|10)' .planning/ROADMAP.md` outputs `>= 4`.
    - `.planning/REQUIREMENTS.md` contains UAT-GAP-01..04 acceptance criteria. Verify: `grep -c 'UAT-GAP-0' .planning/REQUIREMENTS.md` outputs `>= 4`.
    - `pnpm test` exits 0 (no test impact from metadata-only changes).
  </acceptance_criteria>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    STATE.md / ROADMAP.md / REQUIREMENTS.md reflect the re-opened Phase 17 with 4 gap-closure plans and the M-iter2 corrected wave layout (7 → 8 → 9 → 10). UAT-GAP-* requirements documented. Single commit named `docs(17-07): re-open Phase 17 metadata for Wave 7-10 gap closure (M7 + M-iter2)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| about-chat.ts + MDX chatSummary -> portfolio-context.json | Build-time data flow; new authored content crosses into the chat-knowledge surface |
| portfolio-context.json -> Anthropic system block | Run-time data flow; voice-split contract violations here become user-visible chat regressions |
| Anthropic response -> chat client UI | Unchanged surface — DOMPurify + textContent already cover XSS at the rendering layer (Phase 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07-A | Tampering | First-person prose re-leaks into portfolio-context.json after this fix | mitigate | Task 2 BROADENED leak guard in build-chat-context.mjs exits 2 on any first-person leading-clause match (B1 expanded coverage); CI fails (DEBT-03 sync-check.yml runs `pnpm build:chat-context:check`); Task 3 build-time test includes B1 self-test (proves regex catches known tokens) + artifact contract tests; Task 3 API-surface test (chat-voice-split.test.ts) is the third gate at the live system-block layer using readFileSync + JSON.parse (B2). M1 ensures sync-check.yml triggers fire when about-chat.ts changes. Four independent tripwires + CI path coverage. |
| T-17-07-B | Information Disclosure | Defense-in-depth <role> instruction inadvertently echoes verbatim in chat responses | mitigate | system-prompt.ts <security> block at lines 29-55 already forbids echoing instructions. Existing prompt-injection battery (tests/api/prompt-injection.test.ts) covers this. No new mitigation needed. |
| T-17-07-C | Tampering | Future MDX edit accidentally drops chatSummary frontmatter | mitigate | Task 2 buildProjectBlock throws a named error if chatSummary is absent — `pnpm build:chat-context` fails fast. DEBT-03 sync-check.yml CI job runs on every PR touching the path-trigger list (now including about-chat.ts per M1). |
| T-17-07-D | Spoofing | Phase 14 prompt-cache invalidation cost on first deploy after this plan | accept | One-shot cache miss expected on first request after deploy; subsequent requests cache normally. DEBT-02 cache-hit observability (Plan 17-05) makes the cache-warming visible. |
</threat_model>

<verification>
1. After Task 0 (M8): pre-existing roadmap-amendment.test.ts failure closed; `pnpm test` exits 0 cleanly.
2. After Task 1: about-chat.ts + 6 MDX chatSummary fields; about.ts + MDX bodies unchanged; BROADENED leak regex shows zero matches in new strings.
3. After Task 2: portfolio-context.json regenerated; about.intro starts with "Jack"; no first-person leak (BROADENED regex); est_tokens ≥ 4096; build:chat-context:check exits 0; sync-check.yml triggers cover about-chat.ts (M1); D-26 GREEN.
4. After Task 3: B1 self-test catches known leaks; B2 readFileSync pattern (no as-any cast); 5+ new tests across 2 files all GREEN; D-15 sse-snapshot GREEN; D-26 GREEN; pnpm exec astro check clean.
5. After Task 4 (M7 + M-iter2): STATE.md / ROADMAP.md / REQUIREMENTS.md reflect re-opened Phase 17 with the corrected wave layout (7 → 8 → 9 → 10).
6. End of plan: 5 commits cover the fix surface + metadata. Post-deploy manual UAT confirms chat addresses visitor in second person and Jack in third person.
</verification>

<success_criteria>
- UAT Gap #1 closure: chat-knowledge JSON contains only third-person prose; first-person prose in about.ts and MDX bodies UNCHANGED. Four independent tripwires (B1-broadened build-chat-context.mjs leak guard, build-time vitest with B1 self-test, API-surface vitest using readFileSync, CI path triggers covering about-chat.ts) lock the contract forward.
- TEST-01 (D-26): chat regression battery 145/145+ GREEN at every commit (Task 0 closes the pre-existing failure that polluted previous baselines).
- TEST-02 (D-15): SSE byte-identical anchor preserved.
- DEBT-02 inheritance: server-side `chat.cache_metrics` Workers Logs continue emitting.
- M7 metadata: STATE.md / ROADMAP.md / REQUIREMENTS.md reflect re-opened Phase 17 with 4 gap-closure plans.
- M8 baseline: pnpm test now exits 0 honestly — subsequent plans' exit-0 assertions are real gates.
- M1 CI coverage: sync-check.yml triggers fire when about-chat.ts changes.
- B1 broadened regex catches present-tense + contractions + "My favorite" + "I wonder" tokens that the original regex missed.
- B2 NO `as any` cast — readFileSync + JSON.parse pattern is type-system independent.
- M-iter2 wave correction: serial chain occupies waves 7 → 8 → 9 → 10 (one wave per plan) so a wave-batching orchestrator cannot parallelize chat-surface mutations.
- Memory note `project_voice_split.md` honored.
</success_criteria>

<output>
After completion, create `.planning/phases/17-foundations-migration-dns-debt-sweep/17-07-SUMMARY.md` documenting:
- Voice-split fix shape: about-chat.ts (new) + 6 MDX chatSummary frontmatters + build-chat-context.mjs extension + BROADENED leak guard (B1) + system-prompt.ts hardening + 5+ new tests across 2 files (including B1 self-test) + sync-check.yml path triggers updated (M1)
- Pre-existing test fix (M8): tests/content/roadmap-amendment.test.ts now passes or is annotated-skip; `pnpm test` exits 0 cleanly for the first time in 6 plans
- Metadata commit (M7 + M-iter2): STATE.md / ROADMAP.md / REQUIREMENTS.md updated to reflect re-opened Phase 17 with the corrected per-plan wave layout (Wave 7 → 8 → 9 → 10)
- Token-budget delta: pre-plan vs post-plan est_tokens; confirmation est_tokens ≥ 4096
- D-26 battery count at each commit (must be GREEN at every step; Task 0 finally retires the 1-FAIL baseline)
- D-15 sse-snapshot GREEN at every commit
- Post-deploy manual UAT result
- Confirmation that about.ts is byte-identical and MDX bodies are byte-identical above the CASE-STUDY-END fence
- Note any wording adjustments made to the suggested ABOUT_CHAT_* defaults during authoring
- B1 self-test results: list of known leak tokens the regex caught
- m1 token-budget observation: post-fix est_tokens; consider chatSummary cap raise if < 5500
</output>
</content>
</invoke>