---
phase: 19-cron-sweep-scheduling-idempotency-dry-run
plan: 04
type: execute
wave: 3
depends_on:
  - 19-03
files_modified:
  - wrangler.jsonc
  - tests/build/wrangler-cron-shape.test.ts
  - tests/build/wrangler-shape.test.ts
  - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md
autonomous: false
requirements:
  - CRON-01
  - CRON-02
  - CRON-03
  - CRON-04
user_setup: []

must_haves:
  truths:
    - "`wrangler.jsonc` `triggers.crons` is `[\"0 * * * *\"]` exactly — hourly cron is wired and ready for the operator-controlled `*****` UAT in 19-UAT.md Step 1"
    - "`tests/build/wrangler-cron-shape.test.ts` (or tightened existing wrangler-shape.test.ts) source-text-locks `triggers.crons === [\"0 * * * *\"]` AND `vars.DRY_RUN === \"1\"` — Pitfall 6 defense (operator forgets to revert `*****` UAT flip)"
    - "`19-UAT.md` exists with 5 numbered manual operator steps mapping 1:1 to ROADMAP success criteria 1-4 + cleanup, matching the Phase 17 / Phase 18 numbered-steps + `expected:` / `result:` precedent"
    - "Operator can run Step 1 PRE-FLIGHT (`pnpm dev:cron` + `curl /__scheduled`) locally to prove handler dispatch BEFORE the operator-controlled production `*****` verification"
    - "After operator-completed UAT, all 4 ROADMAP success criteria can be marked CLOSED — Phase 19 is shippable + ready for Phase 20 to flip DRY_RUN to `\"0\"`"
  artifacts:
    - path: "wrangler.jsonc"
      provides: "Hourly cron trigger active in deployed Worker"
      contains: "\"crons\": [\"0 * * * *\"]"
    - path: "tests/build/wrangler-cron-shape.test.ts"
      provides: "Source-text guard locking triggers.crons + vars.DRY_RUN values (Pitfall 6 defense)"
      min_lines: 30
    - path: ".planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md"
      provides: "5-step manual UAT spec for operator to close CRON-01..04 success criteria"
      min_lines: 100
  key_links:
    - from: "wrangler.jsonc triggers.crons"
      to: "Cloudflare Workers cron trigger registration"
      via: "wrangler deploy applies the array to the deployed Worker"
      pattern: "\"crons\":\\s*\\[\\s*\"0 \\* \\* \\* \\*\"\\s*\\]"
    - from: "19-UAT.md Step 1 *****  flip"
      to: "wrangler.jsonc triggers.crons revert to ['0 * * * *']"
      via: "operator-controlled revert (DEPLOY-GATE.md posture); build-time test catches if forgotten"
      pattern: "operator.*revert"
---

<objective>
The final Phase 19 wave: flip `wrangler.jsonc` `triggers.crons` from `[]` to `["0 * * * *"]` (CRON-01 ship), add the source-text forward-defense test that locks both `triggers.crons` AND `vars.DRY_RUN` values, and author `19-UAT.md` with 5 numbered manual operator steps mapping 1:1 to the ROADMAP success criteria.

Purpose: This plan turns on hourly cron in source-of-truth, locks the configuration shape against drift, and hands the operator a checkpointed verification spec. The plan is NON-autonomous: Task 3 is a `checkpoint:human-verify` that pauses for the operator to execute the 5-step UAT against preview + production (the deploy itself is operator-controlled per DEPLOY-GATE.md — the executor MUST NOT run `wrangler deploy`).

Output: 1 file modified (`wrangler.jsonc`), 1 new build test file, 1 file modified (tighten existing `tests/build/wrangler-shape.test.ts` cron assertion per 19-PATTERNS.md recommendation), 1 new UAT doc, and 1 operator checkpoint. After operator UAT, Phase 19 is COMPLETE and Phase 20 (Email Render + Resend Integration) is unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-CONTEXT.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-VALIDATION.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-01-SUMMARY.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-02-SUMMARY.md
@.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-03-SUMMARY.md
@.planning/phases/18-persistence-identity-kv-write-path-sessionid/18-UAT.md
@.planning/phases/17-foundations-migration-dns-debt-sweep/17-UAT.md
@.planning/phases/17-foundations-migration-dns-debt-sweep/DEPLOY-GATE.md
@wrangler.jsonc
@tests/build/wrangler-shape.test.ts

<interfaces>
<!-- After Plans 19-01, 19-02, 19-03 land: -->
<!--   - wrangler.jsonc has vars.DRY_RUN = "1" (Plan 19-01) but triggers.crons is still [] (this plan flips it) -->
<!--   - src/worker.ts scheduled() body delegates to deliverDue (Plan 19-03) -->
<!--   - src/lib/chat-delivery.ts owns the full sweep contract under DRY_RUN (Plan 19-02) -->
<!--   - All forward-defense tests are GREEN -->
<!-- Plan 19-04 is the ship gate. -->

From wrangler.jsonc (Plan 19-01 state):
- `"vars": { "DRY_RUN": "1" }` block exists (added Plan 19-01)
- `"triggers": { "crons": [] }` (Plan 19-04 flips this to `["0 * * * *"]`)

From tests/build/wrangler-shape.test.ts (Phase 17 FOUND-04 baseline):
- Lines 47-52 currently assert only `Array.isArray(cfg.triggers.crons)`. The test file's comment at line 48 explicitly invites Phase 19 to tighten this assertion. Plan 19-04 chooses Option 1 from 19-PATTERNS.md ("Tighten wrangler-shape.test.ts in-place") AND adds the new `tests/build/wrangler-cron-shape.test.ts` for clear CRON-01 / D-01 attribution — both can coexist (the new file is a focused CRON-01 attribution test; the tightened wrangler-shape.test.ts retains FOUND-04 anchor).
</interfaces>

<patterns>
<!-- See 19-PATTERNS.md "tests/build/wrangler-cron-shape.test.ts (NEW, OPTIONAL)" section -->
<!-- for the parseJsonc helper + the 2 shape assertions. -->

<!-- See 19-PATTERNS.md "19-UAT.md (NEW — at phase close)" section for the front-matter -->
<!-- pattern, 5-step structure, and result-block convention. -->

<!-- Sibling analog: -->
<!--   - 18-UAT.md is the closest precedent (same wrangler kv operator surface) -->
<!--   - 17-UAT.md established the numbered-steps + result-blocks convention -->
<!--   - DEPLOY-GATE.md establishes the executor-MUST-NOT-deploy posture for Step 1 -->
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip wrangler.jsonc triggers.crons to ["0 * * * *"] + add tests/build/wrangler-cron-shape.test.ts + tighten existing wrangler-shape.test.ts cron assertion</name>
  <files>wrangler.jsonc, tests/build/wrangler-cron-shape.test.ts, tests/build/wrangler-shape.test.ts</files>
  <read_first>
    - wrangler.jsonc (READ FULL — verify current state has Plan 19-01's vars.DRY_RUN block + triggers.crons is still `[]`; verify the JSONC line comment from Plan 19-01 is intact)
    - tests/build/wrangler-shape.test.ts (READ FULL — verify the current FOUND-04 assertions at lines 14-52; the comment at line 48 explicitly invites Plan 19-04 to tighten the array-shape assertion)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md (READ "tests/build/wrangler-cron-shape.test.ts (NEW, OPTIONAL)" section lines 627-672 — verify the parseJsonc helper + the 2 shape assertions; verify the "Recommended: Option 1 (tighten in place)" note)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-CONTEXT.md (READ <decisions> D-01 / D-02 / D-12 / D-14 for cron expression value + Pitfall 6 rationale)
    - .planning/research/PITFALLS.md (READ Critical Pitfall 0 + Moderate Pitfall F + the operator-forgets-revert rationale per RESEARCH OQ-3)
  </read_first>
  <action>
    Three coordinated edits:

    Edit 1 — `wrangler.jsonc`: change `"crons": []` to `"crons": ["0 * * * *"]`. Preserve the existing 2-space indent inside the triggers block. Add an inline JSONC trailing comment on the same line: `// Phase 19 CRON-01 — hourly cron; flip to ["* * * * *"] for Step 1 UAT then REVERT (Pitfall 6: wrangler-cron-shape.test.ts catches unreverted state)`.

    Edit 2 — author `tests/build/wrangler-cron-shape.test.ts` per 19-PATTERNS.md lines 640-668. Single describe block `"CRON-01 + D-01: wrangler.jsonc cron + DRY_RUN shape"`. Two `it(...)`:
    - `"CRON-01: triggers.crons is exactly ['0 * * * *'] (Pitfall 6 anti-*****-leak)"` → `expect((cfg.triggers as { crons: string[] }).crons).toEqual(["0 * * * *"])` — exact-array-equality, NOT contains; this catches the operator's `*****` UAT flip not being reverted.
    - `"D-01 / D-02: vars.DRY_RUN === '1'"` → `expect(cfg.vars).toBeDefined();` + `expect((cfg.vars as { DRY_RUN: string }).DRY_RUN).toBe("1");`
    Include the `parseJsonc` helper verbatim from `tests/build/wrangler-shape.test.ts` (the comment-stripping regex pair) so JSONC line comments don't break `JSON.parse`. Imports: `{ describe, it, expect } from "vitest"; { readFileSync } from "node:fs"; { join } from "node:path"`.

    Edit 3 — tighten `tests/build/wrangler-shape.test.ts:47-52`. Current assertion is `Array.isArray(cfg.triggers.crons)` with comment `// Phase 17: empty array is correct. Phase 19 sets ["0 * * * *"] — when that change lands, update this assertion accordingly.` REPLACE with `expect(cfg.triggers.crons).toEqual(["0 * * * *"])` and update the comment to read `// Phase 19 CRON-01 (Plan 19-04) — locked to hourly cron. tests/build/wrangler-cron-shape.test.ts is the focused CRON-01 attribution; this assertion is the FOUND-04 anchor's lockstep tighten.` Preserve every other line in the file BYTE-IDENTICALLY (the rest of FOUND-04 anchors must not regress).

    Commit message: `feat(19-04): flip triggers.crons to hourly + lock vars.DRY_RUN/cron shape in build tests`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/build/wrangler-cron-shape.test.ts tests/build/wrangler-shape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `wrangler.jsonc` contains exact substring `"crons": ["0 * * * *"]` (with optional trailing comment)
    - `wrangler.jsonc` still contains `"DRY_RUN": "1"` (Plan 19-01's vars block survives byte-identically)
    - `wrangler.jsonc` parses as valid JSONC (re-running `pnpm exec wrangler types` exits 0)
    - `tests/build/wrangler-cron-shape.test.ts` exists; line count >= 30
    - File contains the `parseJsonc` helper (regex pair `/\/\*[\s\S]*?\*\//g` + `/(^|[^:"])\/\/.*$/gm` per 19-PATTERNS.md line 647-649)
    - File contains exactly 2 `it(...)` calls
    - `tests/build/wrangler-shape.test.ts` modification: line that previously asserted `Array.isArray(cfg.triggers.crons)` now asserts `cfg.triggers.crons).toEqual(["0 * * * *"])` — verify via grep
    - `tests/build/wrangler-shape.test.ts` other assertions UNCHANGED (FOUND-04 anchors preserved): `name === "jack-cutrara-portfolio"`, `main === "./src/worker.ts"`, `kv_namespaces` shape, `assets` shape — verify all still GREEN
    - `pnpm exec vitest run tests/build/wrangler-cron-shape.test.ts` exits 0 with 2 PASS / 0 FAIL
    - `pnpm exec vitest run tests/build/wrangler-shape.test.ts` exits 0 (FOUND-04 preserved with tightened cron assertion)
    - `pnpm exec astro check` exits 0/0/0
    - `pnpm test` full battery: PASS count >= 446 (Plan 19-03 baseline ~444 + 2 new); FAIL count = 0; SKIP count = 2
    - `tests/api/chat-delivery.test.ts` GREEN (Plan 19-02 unaffected)
    - `tests/build/worker-scheduled-call-site.test.ts` GREEN (Plan 19-03 unaffected)
    - D-15 / TEST-03 / D-26 forward-defense GREEN
  </acceptance_criteria>
  <done>
    Cron trigger array flipped to hourly. Two build tests lock the value (focused CRON-01 file + tightened FOUND-04 anchor). astro check 0/0/0. Full battery PASS >= 446. Ready for Task 2 (UAT doc) + Task 3 (operator checkpoint).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 19-UAT.md with 5 numbered manual operator steps mapping 1:1 to ROADMAP success criteria 1-4 + cleanup</name>
  <files>.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md</files>
  <read_first>
    - .planning/phases/18-persistence-identity-kv-write-path-sessionid/18-UAT.md (READ FULL — closest precedent for `wrangler kv` operator surface + Workers Builds branch-preview deviation block; Plan 19-04 inherits the front-matter shape + deviation discipline)
    - .planning/phases/17-foundations-migration-dns-debt-sweep/17-UAT.md (READ FULL — established the numbered-steps + `expected:` / `result:` / `prior_result:` block convention)
    - .planning/phases/17-foundations-migration-dns-debt-sweep/DEPLOY-GATE.md (READ FULL — establishes the executor-MUST-NOT-deploy posture that anchors D-12 / 19-UAT.md Step 1)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-PATTERNS.md (READ "19-UAT.md (NEW — at phase close)" section lines 676-770 in FULL — the 5-step template is locked there with command sequences for Steps 1-5)
    - .planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-CONTEXT.md (READ <decisions> D-12 / D-13 / D-14 — locked 5-step structure + executor-must-not-deploy posture + pre-flight `pnpm dev:cron` mechanic)
    - wrangler.jsonc (READ FULL — extract the KV namespace IDs `eaa30fef259e4a6b9505b41bbf3f8f01` PROD + `115f3c1b0f8a4a1da9fee78c48dcb749` PREVIEW for the UAT command blocks)
  </read_first>
  <action>
    Author `19-UAT.md` per 19-PATTERNS.md "19-UAT.md (NEW — at phase close)" template. The file structure:

    1. YAML front-matter:
       ```
       status: in-progress
       phase: 19-cron-sweep-scheduling-idempotency-dry-run
       source: [19-01-SUMMARY.md, 19-02-SUMMARY.md, 19-03-SUMMARY.md]
       started: <ISO 8601 timestamp at file authoring>
       updated: <same>
       deviation: |
         Phase 18 UAT learning carries forward — Workers Builds branch previews may bind to PROD KV id (eaa30fef259e4a6b9505b41bbf3f8f01) rather than the preview_id (115f3c1b0f8a4a1da9fee78c48dcb749) depending on the build environment. Operator should verify which KV the preview deployment is reading from before seeding test-uat-* keys. If PROD KV is bound, use the test-uat-* prefix discipline + Step 5 cleanup to avoid audit-debt in production KV.
       ```

    2. Title `# Phase 19 UAT — Cron Sweep (Scheduling + Idempotency under DRY_RUN)`. Opening paragraph citing D-12 (Step 1 operator-controlled per DEPLOY-GATE.md), the KV namespace IDs from wrangler.jsonc, and the preview/production URL patterns.

    3. Five Step sections — each with `expected:` block, `result: pending` initial state, `notes:` placeholder. Operator fills `result:` to `pass` or `issue` and populates `notes:` post-execution.

       **Step 1: CRON-01 `*****` Past-Events verification (closes SC1)**
       - PRE-FLIGHT (executor-runnable): `pnpm dev:cron` in one terminal; in a second terminal `curl "http://localhost:8787/__scheduled?cron=*+*+*+*+*"`; expect `chat.delivery.tick { sessions_seen: 0, sessions_due: 0, sessions_promoted: 0, errors: 0, pages_scanned: 1, elapsed_ms: ... }` log line in the first terminal (or sessions_seen > 0 if any test-uat-* keys are still seeded from previous attempts).
       - PRODUCTION (operator-controlled per DEPLOY-GATE.md): operator edits `wrangler.jsonc` `triggers.crons` to `["* * * * *"]`, runs `wrangler deploy`, waits 90s, opens Cloudflare Dashboard → Workers → jack-cutrara-portfolio → Cron → Past Events, confirms ≥1 successful invocation captured, screenshots the Past Events tab, REVERTS `wrangler.jsonc` to `["0 * * * *"]`, runs `wrangler deploy` again.
       - REVERT CHECK: `git diff wrangler.jsonc` returns empty (no unreverted `*****`); `pnpm exec vitest run tests/build/wrangler-cron-shape.test.ts` exits 0 (build-time defense).
       - expected: Past Events tab shows ≥1 invocation within 90s of `*****` deploy; revert confirmed.

       **Step 2: Seed-and-sweep end-to-end (closes SC2)**
       - Operator command sequence (per 19-PATTERNS.md lines 727-735):
         ```
         SID="test-uat-$(uuidgen)"
         STALE_TS=$(date -u -d '3 hours ago' +"%Y-%m-%dT%H:%M:%S.%3NZ")
         wrangler kv key put "live:${SID}" '<JSON ChatTranscript value with stale started_at + last_activity_at>' --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 --metadata '{"last_activity_at":"<STALE_TS>","msg_count":2,"window_started_at":"<STALE_TS>","window_count":2}' --remote
         ```
       - In a separate terminal: `wrangler tail --format pretty --search chat.delivery`
       - Wait for next top-of-hour OR use Step 1's `*****` flip (operator-controlled).
       - expected:
         - `chat.delivery.dry_run { sid: ${SID}, to, from, reply_to, msg_count: 2, truncated: false, country: "US", referrer_host: "example.com", dry_run: true }` appears in tail
         - `chat.delivery.tick { sessions_seen, sessions_due ≥ 1, sessions_promoted ≥ 1, errors: 0, pages_scanned, elapsed_ms }` appears in tail
         - `wrangler kv key get "delivered:${SID}" --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 --remote` returns the `{v:1, sid, delivered_at, dry_run:true, msg_count:2, truncated:false}` envelope (D-09 shape)
         - `wrangler kv key get "live:${SID}" --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 --remote` returns null (key DELETED post-promote)

       **Step 3: Idempotency double-tap (closes SC3)**
       - Operator re-seeds same `live:${SID}` (stale timestamp) — `delivered:${SID}` is still present from Step 2 (within 24h TTL).
       - Invoke cron (wait for top of hour OR temporary `*****` flip).
       - expected: `chat.delivery.skipped_already_delivered { sid: ${SID}, delivered_at_existing: <Step 2 ISO timestamp> }` in tail; `chat.delivery.tick.sessions_promoted: 0` for the re-seeded session.

       **Step 4: Pagination / batch-cap stress (closes SC4)**
       - Operator bash loop seeds 60 stale `live:test-uat-batch-*` keys (each with unique sid).
       - Invoke cron.
       - expected: first-tick `sessions_due: 60` but `sessions_promoted: 50` (PER_TICK_BATCH_CAP enforced).
       - Re-invoke cron; expected: second-tick processes remaining 10.
       - Verify: `wrangler kv key list --prefix delivered:test-uat-batch- --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 --remote` returns 60 keys after both ticks.

       **Step 5: Backlog cleanup (operational hygiene)**
       - Operator deletes all `live:test-uat-*` + `delivered:test-uat-*` keys via `wrangler kv key delete --bulk-delete` (or per-key delete if bulk is unsupported).
       - expected: `wrangler kv key list --prefix test-uat- --namespace-id eaa30fef259e4a6b9505b41bbf3f8f01 --remote` returns empty.
       - Rationale: no UAT audit-debt left in production KV (the test-uat-* prefix discipline keeps it greppable).

    4. Closing section: "Phase Exit Gates" — checklist of forward-defense tests + roadmap success criteria 1-4 status. Operator fills in checks post-UAT.

    Result-block convention (per 19-PATTERNS.md lines 760-769):
    ```
    ### N. [Step name]
    expected: |
      [multi-line expected behavior]
    result: [pending | pass | issue]
    prior_result: [populated only if a re-test happened]
    notes: |
      [optional — operator captures observations, command output, screenshot file paths]
    ```

    DO NOT include a browser/client tier verification step — Phase 19 touches zero chat-surface files (the forward-defense `pnpm test` battery already validates D-26 / D-15 / TEST-03 anchors automatically per 19-PATTERNS.md deviation note for 19-UAT.md).

    Commit message: `docs(19-04): add 19-UAT.md with 5 manual operator steps (D-12, D-14)`.
  </action>
  <verify>
    <automated>node -e "const fm=require('fs').readFileSync('.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md','utf8'); const steps=fm.match(/^###?\\s+(?:Step|\\d+\\.?)\\s+/gm)||[]; process.exit(steps.length>=5?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `19-UAT.md` exists; line count >= 100
    - YAML front-matter contains the 5 required keys: `status`, `phase`, `source`, `started`, `updated`, `deviation` (at minimum)
    - File contains exactly 5 Step headers — verified by grep on `^### Step` or `^## Step` (count >= 5)
    - Step 1 mentions BOTH the PRE-FLIGHT `pnpm dev:cron` invocation AND the PRODUCTION operator-controlled `*****` deploy
    - Step 1 explicitly cites that the executor MUST NOT run `wrangler deploy` (per D-12 / DEPLOY-GATE.md posture)
    - Steps 2-4 each include at least one `wrangler kv key` command (put / get / list / delete) targeting the production KV namespace id `eaa30fef259e4a6b9505b41bbf3f8f01`
    - Step 5 explicitly addresses cleanup of `test-uat-*` keys (operational hygiene)
    - File contains the literal substring "CRON-01" or "SC1" (Step 1 → success criterion 1 mapping)
    - File contains the literal substring "CRON-02" or "SC2" (Step 2 → SC2)
    - File contains the literal substring "CRON-03" or "SC4" (Step 4 → SC4 batch cap)
    - File contains the literal substring "DRY_RUN" (cron-under-DRY_RUN context)
    - Each Step has a `result: pending` initial state and an `expected:` block (manual visual review — operator fills `result:` post-execution)
    - File commits to git
    - `pnpm test` full battery: PASS >= 446 (Task 1's baseline); FAIL = 0; SKIP = 2 (UAT doc adds no tests)
  </acceptance_criteria>
  <done>
    19-UAT.md committed with 5 numbered manual operator steps. Each step maps 1:1 to a ROADMAP success criterion (or operational hygiene). Operator can now execute the UAT to close SC1-4.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: OPERATOR — execute 19-UAT.md 5-step manual UAT against preview + production; populate result: blocks; close CRON-01..04 success criteria</name>
  <what-built>
    Plans 19-01 through 19-04 (Task 1 + Task 2) have shipped:
    - `wrangler.jsonc` declares `vars.DRY_RUN = "1"` + `triggers.crons = ["0 * * * *"]`
    - `src/worker.ts` scheduled() handler delegates to `deliverDue(env, controller.scheduledTime)` via ctx.waitUntil(...).catch(...)
    - `src/lib/chat-delivery.ts` exports `deliverDue` with the full two-keyspace promotion loop (under DRY_RUN gate)
    - Forward-defense build tests lock all source-text invariants
    - Unit tests prove all CRON-02/03/04 invariants in isolation against mock KV (19 cases)
    - `19-UAT.md` exists with 5 numbered manual operator steps

    Phase 19 is now in operator-controlled UAT phase.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-UAT.md` and execute the 5 steps in order:

    **Step 1: CRON-01 `*****` Past-Events verification**
    1. PRE-FLIGHT (local, executor-runnable): in terminal 1 run `pnpm dev:cron`; in terminal 2 run `curl "http://localhost:8787/__scheduled?cron=*+*+*+*+*"`; observe `chat.delivery.tick` log in terminal 1. If this fails, STOP — handler wiring is broken; fix Plan 19-03 issue before proceeding.
    2. PRODUCTION (operator-controlled): edit `wrangler.jsonc` `triggers.crons` to `["* * * * *"]`; run `wrangler deploy` (operator-only per DEPLOY-GATE.md — executor MUST NOT run this command); wait 90s; open Cloudflare Dashboard → Workers & Pages → jack-cutrara-portfolio → Cron → Past Events; capture screenshot of ≥1 successful invocation within the 90s window.
    3. REVERT: edit `wrangler.jsonc` `triggers.crons` back to `["0 * * * *"]`; run `wrangler deploy` again; verify `git diff wrangler.jsonc` returns empty; verify `pnpm exec vitest run tests/build/wrangler-cron-shape.test.ts` exits 0.
    4. Fill Step 1 `result:` to `pass` (or `issue` with notes) in 19-UAT.md.

    **Step 2: Seed-and-sweep end-to-end** — Follow the exact `wrangler kv key put` command sequence in 19-UAT.md Step 2. Verify all four expected outcomes (dry_run log, tick log, delivered: envelope, live: deleted). Fill `result:`.

    **Step 3: Idempotency double-tap** — Re-seed the same sid; invoke cron; verify `skipped_already_delivered` log and `sessions_promoted: 0`. Fill `result:`.

    **Step 4: Pagination / batch-cap stress** — Bash-loop seed 60 stale keys; invoke cron twice; verify the 50/10 split. Fill `result:`.

    **Step 5: Backlog cleanup** — Delete all `test-uat-*` keys; verify empty list. Fill `result:`.

    **Final**: Update 19-UAT.md front-matter `status` to `complete` (or `blocked` if any step failed); update `updated` timestamp.

    Then reply with one of:
    - `approved — all 5 steps PASS, CRON-01..04 closed`
    - `issue — Step <N> failed: <description>` (executor will plan Wave 4 gap-closure)
  </how-to-verify>
  <resume-signal>Type `approved — all 5 steps PASS, CRON-01..04 closed` to mark Phase 19 complete; OR type `issue — Step <N> failed: <description>` to trigger gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → wrangler.jsonc | Pre-deploy config edit + revert (Step 1 of UAT); the build-time test is the safety net |
| operator → production KV via wrangler CLI | Seeding `test-uat-*` keys; cleanup via Step 5; production KV stays clean post-UAT |
| Cloudflare cron scheduler → deployed Worker | Hourly invocations after this plan ships; under DRY_RUN, every invocation logs envelope-only and writes `delivered:` markers — no Resend POST yet (Phase 20) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | operator forgets to revert `*****` after Step 1 UAT, leaving cron running every minute | mitigate | `tests/build/wrangler-cron-shape.test.ts` (Task 1) source-text-locks `triggers.crons === ["0 * * * *"]` exactly — any unreverted state fails the build test on next CI run + on next local `pnpm test`. Operator-side: Step 1 explicitly includes a REVERT CHECK (`git diff wrangler.jsonc` returns empty). |
| T-19-04-02 | Tampering | DRY_RUN accidentally set to `"0"` before Phase 20 ships | mitigate | `tests/build/wrangler-cron-shape.test.ts` Invariant 2 source-text-locks `vars.DRY_RUN === "1"`. Any flip to `"0"` fails the build test. Phase 20 commits the flip as an explicit single-line edit alongside the renderer + adversarial-payload suite — visible in PR diff. |
| T-19-04-03 | Information Disclosure | `test-uat-*` keys left in production KV after UAT | mitigate | Step 5 of 19-UAT.md is operational hygiene: operator deletes all `live:test-uat-*` + `delivered:test-uat-*` keys via `wrangler kv key delete`. The `test-uat-*` prefix discipline ensures key visibility via `wrangler kv key list --prefix test-uat-`. |
| T-19-04-04 | DoS | `*****` UAT flip not reverted → 60× cron load on Free tier | mitigate | Cloudflare Workers Free tier limit is 5,000 cron invocations/day; `*****` = 1,440/day, well under cap but burns the budget. The revert is operator-controlled + build-test-locked. Worst case: 1-day overrun before next CI/PR catches the unreverted state. |
| T-19-04-05 | Repudiation | Phase 19 cron-runs are silent in production logs | accept | The `chat.delivery.tick` log line emits on every cron firing (visible in Workers Logs / `wrangler tail`). Until Phase 20 ships and DRY_RUN flips, this is the only operator-visible signal that the cron is healthy — which IS the design intent (DRY_RUN validates mechanics before email goes live). |
</threat_model>

<verification>
- Task 1 acceptance criteria all GREEN: `wrangler.jsonc` flipped + `wrangler-cron-shape.test.ts` 2/2 + `wrangler-shape.test.ts` tightened cron assertion GREEN
- Task 2 acceptance criteria all GREEN: `19-UAT.md` with 5 numbered Step sections committed
- Task 3 operator checkpoint: 5/5 result blocks marked `pass` (or any `issue` triggers Wave 4 gap-closure planning)
- `pnpm test` full battery PASS >= 446 / FAIL = 0 / SKIP = 2 at every commit
- `pnpm exec astro check` 0/0/0 at every commit
- D-15 / TEST-03 / D-26 forward-defense GREEN at every commit (Phase 19 touched zero chat-surface files)
- Phase 19 ROADMAP success criteria 1-4 all marked CLOSED in REQUIREMENTS.md after operator UAT
</verification>

<success_criteria>
- `wrangler.jsonc` `triggers.crons` = `["0 * * * *"]` exactly
- `tests/build/wrangler-cron-shape.test.ts` 2/2 GREEN
- `tests/build/wrangler-shape.test.ts` tightened cron assertion + all other FOUND-04 anchors GREEN
- `19-UAT.md` exists with 5 Step sections, each with `expected:` + `result:` blocks
- Operator UAT completed (Task 3 checkpoint): 5/5 steps PASS or known-good `issue:` notes recorded
- CRON-01..04 closed in REQUIREMENTS.md traceability table
- Full battery PASS >= 446; FAIL = 0; SKIP = 2; astro check 0/0/0
- Phase 19 is shippable; Phase 20 (Email Render + Resend Integration) is UNBLOCKED — only Phase 20 work remaining for v1.3
</success_criteria>

<output>
After completion, create `.planning/phases/19-cron-sweep-scheduling-idempotency-dry-run/19-04-SUMMARY.md` documenting:
- The cron-flip edit (wrangler.jsonc line before/after)
- The 2 new build tests + the 1 tightened test
- 19-UAT.md operator execution results (per-step `result:` summaries)
- Workers Logs evidence (paste 1-2 sample `chat.delivery.tick` log lines if captured during UAT)
- Phase 19 final test count delta (Phase 18 baseline 419 → Phase 19 close ~446+, depending on chat-delivery.test.ts final case count)
- Confirmation of Phase 19 closure — CRON-01..04 GREEN per design; ROADMAP success criteria 1-4 marked complete
- Any Rule 3 inline deviations
- Carry-forward items for Phase 20: which file Phase 20 must EDIT (wrangler.jsonc DRY_RUN flip); which file Phase 20 must CREATE (src/lib/email/resend.ts); the substitution target inside chat-delivery.ts (`throw new Error("send_not_implemented_in_phase_19")`)

Also: update `.planning/REQUIREMENTS.md` traceability table — change CRON-01..04 status from `Pending` to `Complete (Plan 19-04 / 19-UAT.md)`. Update `.planning/STATE.md` Current Position to reflect Phase 19 closure + Phase 20 readiness. Update `.planning/ROADMAP.md` Phase 19 entry to mark plans 1-4 complete with summary links.
</output>
