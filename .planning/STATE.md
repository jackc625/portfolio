---
gsd_state_version: 1.0
milestone: v1.3
milestone_name: Chat Visibility
status: executing
stopped_at: "Plan 17-09 (Wave 8 -- gap-closure for UAT Gap #3 COPY button feedback window) COMPLETE. 2 atomic commits: dcf597b (Task 1 -- src/styles/global.css new rule .chat-copy-btn.copy-success { opacity: 1; color: var(--accent); } added immediately after :focus-visible block, wires the previously-dead .copy-success class set by chat.ts copyToClipboard for 2000ms; tests/client/chat-copy-button.test.ts REPLACED 5-test old-contract assertions with 10-test new contract -- 4 M4-isolated CSS-cascade tests + 1 vi.useFakeTimers lifecycle test + 5 retargeted createCopyButton helper tests asserting post-M3 contract; 9/10 GREEN at end of Task 1, 1 RED was the M3-contract test waiting for Task 2's deletion of the inline style.color writes), b35ad94 (Task 2 -- src/scripts/chat.ts new module-scope const COPY_FEEDBACK_MS = 1500 + copyToClipboard setTimeout literal 2000 -> COPY_FEEDBACK_MS + createCopyButton click handler M3 fix: deleted both `copyBtn.style.color = 'var(--accent)'` and `copyBtn.style.color = 'var(--ink-faint)'` inline writes, replaced 1000 -> COPY_FEEDBACK_MS, added M3 rationale comment block; CSS class is now SOLE source of truth for color during feedback window; M3-contract test turned GREEN). Verification: chat-copy-button.test.ts 10/10 GREEN; no-imperative-display-flip.test.ts 3/3 GREEN; sse-snapshot.test.ts 3/3 GREEN (D-15 anchor preserved); pnpm test full suite = 409 PASS / 0 FAIL / 2 SKIP (was 404/0/2 baseline; +5 net new tests). pnpm exec astro check = 2 errors (both PRE-EXISTING in tests/client/listener-dedup.test.ts from Plan 17-03; logged in deferred-items.md; Plan 17-09 introduced 0 new typecheck errors). pnpm exec astro build = clean (10 routes prerendered). Manual UAT (10 steps requiring browser interaction at pnpm preview) DEFERRED TO USER -- agent cannot drive a real browser to evaluate post-click visibility behavior outside the JS event loop; the automated suite locks the contract structurally with vi.useFakeTimers proving the 1500ms window. Phase 17 progress: 8/10 plans complete (was 7/10). Next: Plan 17-10 (Wave 9 -- view-transition AbortError handler) per serial chain 17-07 -> 17-09 -> 17-10 -> 17-08."
last_updated: "2026-05-11T15:00:00.000Z"
last_activity: 2026-05-11 -- Plan 17-09 (Wave 8) gap-closure complete; Plan 17-10 next
progress:
  total_phases: 4
  completed_phases: 0
  total_plans: 10
  completed_plans: 8
  percent: 80
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-09)

**Core value:** Recruiters and hiring managers who visit this site should immediately see Jack as someone worth interviewing
**Current focus:** Phase 17 — foundations-migration-dns-debt-sweep

## Current Position

Phase: 17 (foundations-migration-dns-debt-sweep) — EXECUTING (gap-closure addendum, Wave 7-10)
Plan: 8 of 10 complete; Plan 17-10 next per serial chain (17-07 → 17-09 → 17-10 → 17-08)
Status: Executing Phase 17 gap-closure plans
Last activity: 2026-05-11 -- Plan 17-09 (Wave 8) gap-closure complete; Plan 17-10 next

### Phase 17 Re-Opened (Gap Closure — 2026-05-11)

Phase 17 was CLOSED at Plan 17-06 (2026-05-11) with all 14 requirements GREEN per design.
Subsequent UAT (17-UAT.md) surfaced 4 blockers + 1 RELEASE BLOCKER on local main
(23 commits ahead of origin/main):
- Gap #1 (BLOCKER): chat voice-split regression — chat addresses visitor as Jack [Plan 17-07: CLOSED 2026-05-11]
- Gap #2 (BLOCKER + RELEASE BLOCKER): #chat-panel does not open on `pnpm dev` [Plan 17-08: pending — runs LAST as deploy gate]
- Gap #3 (major): COPY button has no visible COPIED transition [Plan 17-09: CLOSED 2026-05-11]
- Gap #4 (major): cross-document nav surfaces AbortError in DevTools console [Plan 17-10: pending]

Gap-closure plans 17-07..10 (Wave 7-10 serial, M-iter2 corrected) re-opened Phase 17
to ship the four fixes. FOUND-03 Pages retirement now waits for these to deploy —
production "works" today only because origin/main is 23+ commits behind local main;
deploying DEBT-05 (commit 1c148c9) without Plan 17-08 would break production chat
panel-open. Plan 17-08 is THE prerequisite for next `git push origin main`.

## Roadmap Snapshot

| Phase | Name | Requirements | Depends on |
|-------|------|--------------|------------|
| 17 | Foundations — Migration + DNS + Debt Sweep | FOUND-01..04, DNS-01..02, DEBT-01..05, TEST-01, TEST-02, TEST-03 | Phase 16 (v1.2 shipped) |
| 18 | Persistence + Identity — KV Write Path + sessionId | KV-01..04, IDENT-01..02, META-01..02, TEST-01, TEST-03 | Phase 17 |
| 19 | Cron Sweep — Scheduling + Idempotency (DRY_RUN) | CRON-01..04 | Phase 18 |
| 20 | Email Render + Resend Integration | MAIL-01..05 | Phase 19 |

**Coverage:** 31 / 31 requirements mapped (28 v1.3 requirements + 3 cross-phase TEST gates).

## Accumulated Context

### Decisions

All milestone-level decisions logged in PROJECT.md Key Decisions table. Full plan-level decision history retained in `.planning/milestones/v1.2-phases/*/SUMMARY.md` and `.planning/RETROSPECTIVE.md`.

v1.3 architectural decisions (locked at `/gsd-new-milestone` 2026-05-09):

- **Storage = Cloudflare KV** (not D1) — no aggregation/search needed; Jack reads every transcript in entirety
- **Delivery = Resend transactional email** — justified new runtime dep; v1.2 "zero new runtime deps preferred" rule overridden when there's real need
- **Cadence = hourly cron + 2-hour inactivity threshold** — generous; conversations virtually guaranteed not to fragment across emails
- **Posture = silent logging** — no in-UI disclosure; data never leaves Cloudflare → Gmail
- **Filter = none** — site traffic low enough to read every conversation; no recruiter-vs-visitor classification

v1.3 phase-shape decisions (locked at `/gsd-roadmap-phase` 2026-05-09):

- **4 phases (17-20)** — derived from natural delivery boundaries: Foundations (migration + DNS + debt) → Persistence + Identity (KV writes + sessionId) → Cron Sweep (scheduling under DRY_RUN) → Email Render (Resend + adversarial-payload hardening, DRY_RUN flipped off)
- **Phase 17 absorbs all 5 DEBT items** alongside the migration — they touch the same files (`chat.ts`, `api/chat.ts`, `BaseLayout.astro`, `global.css`), so coupling them with the migration minimizes the number of times we open the chat regression risk surface
- **Phase 21 (Observability + Hardening)** explicitly DEFERRED to v1.4+ — webhook + per-IP rate limiting + Analytics Engine all out of scope for this milestone
- **Pages → Workers Static Assets** chosen over "separate sweeper Worker" fallback (Option a vs Option b in research SUMMARY.md) — single deploy target; matches Cloudflare's documented forward path; Astro is now Cloudflare-owned

Plan 17-01 execution decisions (2026-05-10):

- **Day-1 D-04/D-11 ordering enforced at commit level**, not just plan level. Fixture committed as a standalone commit (`d6c2f0e`) BEFORE the test commit (`a4d5db6`). Fixture exists in git history independently of the test that consumes it.
- **Mock `cloudflare:workers` virtual module at the test seam**, not by refactoring `src/pages/api/chat.ts`. First time this seam is exercised in vitest; pattern documented in `.planning/phases/17-foundations-migration-dns-debt-sweep/17-01-SUMMARY.md` for reuse.
- **Canonical SSE bytes are 38, not 36** — plan arithmetic miscount corrected. `data: {"text":"Hello"}\n\n` is 24 bytes; `data: [DONE]\n\n` is 14 bytes. Fixture matches server output byte-for-byte; test asserts byte-equality against fixture file (no hardcoded length). Logged as Rule 1 deviation in SUMMARY.

Plan 17-02 execution decisions (2026-05-10) — Pages → Workers Static Assets migration:

- **Account-subdomain-scoped CORS captured AFTER first deploy.** WORKERS_PREVIEW_SUFFIX = `.jackcutrara.workers.dev` (NOT generic `.workers.dev`). The `jackcutrara` account subdomain is load-bearing — foreign Cloudflare accounts cannot register a colliding Worker name on the same suffix and bypass the allow-list. Future account-rename runbook must update `src/lib/validation.ts:WORKERS_PREVIEW_SUFFIX` constant in lockstep.
- **Parking-page CNAME pitfall caught inline (Rule 3 - Blocking).** Leftover `www → parkingpage.namecheap.com` CNAME from Namecheap's parking page blocked the `www.jackcutrara.com` Custom Domain attach with "domain already in use" error AND caused initial SSL 525 handshake fail. Deleted the parking-page CNAME → www attached cleanly → Cloudflare auto-provisioned SSL within seconds. **Future-migration pattern: BEFORE initiating any Cloudflare Custom Domain attach, audit ALL existing CNAMEs at apex AND every subdomain being flipped.** Parking-page residues are a known class of pitfall when migrating from registrars (Namecheap, GoDaddy, etc.) that enable parking pages by default.
- **Astro auto-provisions a SESSION KV namespace at build time** — `5d7c7a5749e24383a4eb256dd39a4ff4`, declared in `dist/server/wrangler.json` by the `@astrojs/cloudflare` adapter for its session-driver feature. NOT in our checked-in `wrangler.jsonc`. This is adapter-internal and invisible to FOUND-04 verification. **Future plans MUST NOT name their own KV binding `SESSION`** — would conflict with the adapter's auto-injected binding. CHAT_KV (Phase 18 KV-01) is safe; pattern: prefix custom bindings with `CHAT_*`.
- **Custom Worker entrypoint pattern (src/worker.ts) replaces `@astrojs/cloudflare/entrypoints/server`** — the bundled adapter entrypoint exports only `{ fetch }` and could not host a `scheduled()` handler. Phase 19 cron-sweep was blocked without this file. src/worker.ts re-exports Astro's `handle()` for fetch + Phase 19-ready scheduled() stub with `ctx.waitUntil(Promise.resolve())` placeholder + comment naming the `deliverDue` substitution target.
- **Pages-as-warm-rollback per D-02 — gated check, NOT a timer per CONTEXT.md A3.** Custom Domains detached from Pages FIRST, attached to Worker SECOND. Pages deployment stays live (domain-less) for 24h gated window starting ~2026-05-10 22:00 UTC. User retires manually after clean window AND no open regressions.
- **Cloudflare Access policy audit (Q2 RESOLVED) confirmed assumption** — Zero Trust was never enabled on this account, so 0 Access policies on `portfolio-5wl.pages.dev`. Worker preview URLs are public per Cloudflare default, matching prior Pages posture byte-identical. No policy replication needed before Custom Domain flip.
- **RESEND_API_KEY explicitly NOT re-added in Plan 17-02 per plan D-05.** Three secrets re-added: ANTHROPIC_API_KEY (re-keyed from Pages — secrets do NOT migrate per RESEARCH Pitfall 9), CHAT_RECIPIENT_EMAIL (`jackcutrara@gmail.com`), CHAT_SENDER_EMAIL (`"Portfolio Chat" <transcripts@mail.jackcutrara.com>`). RESEND_API_KEY lives in Plan 17-06 alongside Resend account creation.
- **Sharpened security.test.ts beyond plan minimum.** Added explicit `https://attacker.workers.dev REJECTED` test (Rule 2 - Missing Critical) guarding the foreign-account-subdomain attack vector (T-17-02). Logic unchanged (rename only per D-14); test surface now stronger than pre-migration.
- **Plan 17-06 DNS triage open: pre-existing Resend records on send.jackcutrara.com.** When auditing DNS pre-flip, observed existing records on the `send.` subdomain (MX `send.`, TXT `_dmarc`, TXT `resend._domainkey`, TXT `send "v=spf1 include:amazon..."`) — pre-existing Resend SES setup. But Plan 17-06 D-06 spec'd `mail.jackcutrara.com` as the canonical sending subdomain. **Mismatch flagged for triage at Plan 17-06 execution time** (re-use existing `send.*` records vs add fresh `mail.*` records). Out of scope for Plan 17-02.

Plan 17-03 execution decisions (2026-05-10) — DEBT-04 + DEBT-05 chat-surface tech debt closure:

- **Idempotent astro:page-load listener pattern adopted across the chat surface.** Three modules (`src/scripts/analytics.ts`, `src/scripts/scroll-depth.ts`, `src/scripts/chat.ts`) now use remove-then-add at the document level instead of module-level `*Bootstrapped` flags. Browser's internal (target, type, handler) registry dedups by reference equality, so `removeEventListener` BEFORE `addEventListener` is a safe identity operation that converges to "this handler reference is in the registry exactly once." `typeof document !== "undefined"` guard added to chat.ts for HMR/test parity with the other two files. **Future chat-surface modules that register an astro:page-load listener MUST follow this pattern** — the listener-dedup test extends trivially via the `CHAT_SURFACE_MODULES` constant + it.each. This is the canonical idiom per RESEARCH §"Pattern 3 — Don't Hand-Roll".
- **CSS-only chat-panel display state machine adopted (DEBT-05).** `src/styles/global.css` now declares `display: none` on the base `#chat-panel` rule and `display: flex` on `#chat-panel.is-open` — both outside the `@media (prefers-reduced-motion: no-preference)` block, so the display contract applies equally to reduce-motion users. The keyframe `chat-panel-scale-in` rule is unchanged. `animatePanelOpen` / `animatePanelClose` in chat.ts are no-op async stubs (`async function animatePanel*(_panel: HTMLElement): Promise<void> { /* no-op */ }`) — call sites that `await` them for keyframe-completion timing do not change shape. **Future view-toggle UI in this codebase (mobile menu, modal overlay, etc.) should follow the same shape**: base-rule display:none + `.is-open` display:flex, animation gated under the no-preference media query. The Phase 7 chat panel was the last imperative-display holdout in the codebase.
- **Source-text anti-regression test pattern adopted.** `tests/build/no-imperative-display-flip.test.ts` greps `src/scripts/chat.ts` for forbidden imperative patterns (`panel.style.display = "flex"` / `"none"`). Cheap to write; catches regressions that would pass behavioral tests but reintroduce the imperative path. Already used at `tests/api/chat.test.ts:259-289` for prompt-cache integrity; now also for DEBT-05.
- **chat-pulse-coordination.test.ts assertion rewrite was unavoidable (Rule 1 deviation).** The Phase 7 "display toggle preserved" suite (2 tests) asserted `panel.style.display === "flex"` / `"none"` — the exact behavior DEBT-05 removes. Leaving the assertions untouched would have failed the TEST-01 D-26 GREEN gate. Rewrote the assertions to the new contract (`.is-open` class toggle + `panel.style.display !== "flex"`). **Future plan-authoring pattern: any plan that removes an imperative behavior must audit existing tests that assert the imperative behavior and list them in `files_modified` at plan-time.** The 17-03 plan frontmatter under-counted the test files by 1.
- **Plan-spec'd `vi.resetModules() + re-import N` behavioral test approach has a fundamental vitest-jsdom constraint that surfaced cleanly during Task 1.** Each re-evaluation creates a new handler reference; the new evaluation's `removeEventListener` does not remove the prior evaluation's registration because the references differ. Strengthened the test surface beyond the plan minimum with source-level pattern assertions (Rule 2 deviation) — the canonical anti-regression invariant is the source-text pattern, not the runtime behavioral consequence. Production behavior is unchanged because Astro re-runs astro:page-load WITHOUT re-evaluating the module (stable handler reference from the original evaluation).

Plan 17-04 execution decisions (2026-05-10) — DEBT-01 + DEBT-03 docs/CI tech debt closure:

- **Build-time source-text test on canonical decision-bearing docs adopted (DEBT-01).** `tests/build/project-md-debt-01.test.ts` readFileSync's `.planning/PROJECT.md` and asserts: locked-decision wording (`Free-tier acceptable`) is present; stale framing (`carry-forward gap`) is absent within a 600-char window from the first `indexOf("CHAT_RATE_LIMITER")`; Workers Paid / v1.4 upgrade-path reference is present within the same window; renamed heading `**Known issues / tech debt:**` is present (without "carried into v1.3"); 4 Phase-17-closed DEBT bullets are absent from the Known issues block. Mirrors the pattern used at `tests/build/no-imperative-display-flip.test.ts` (Plan 17-03) for chat.ts source-text invariants. Cheap to write; catches future doc edits that accidentally revert the framing. **Future plan-authoring pattern: when a milestone-shape lock changes the wording of a doc, add a build-time test asserting the new wording is present and the stale wording is absent.**
- **Test anchor breadth caught a doc inconsistency at PROJECT.md:85 (Rule 1 deviation).** The build-time test slices a 600-char window from `src.indexOf("CHAT_RATE_LIMITER")`. The FIRST occurrence is in the v1.3 milestone Target features list (line 85), NOT the Known issues block (lines 115-122). The plan-spec'd action focused on lines 115-122 alone, but the test would have failed without updating line 85 too. Fixed by appending the locked-decision qualifier inline next to the line 85 CHAT_RATE_LIMITER mention. **Future plan-authoring pattern: when a build-time test searches for a token via `indexOf` and slices a window from the FIRST occurrence, the plan's action steps must enumerate ALL occurrence sites that need to fall within the slice (or anchor the test on a stricter section pattern, e.g. search within the Known issues block).**
- **Single-job two-step CI workflow pattern adopted for sync-check.yml (DEBT-03)** per RESEARCH §"DEBT-03 CI Job Shape" recommendation. The existing `pnpm sync:check` step and the new `pnpm build:chat-context:check` step run sequentially in the same `check` job. Second step only runs if first passes (failure surfaced via job-level GitHub Actions failure indicator). Saves the second `actions/checkout@v4` + `pnpm install --frozen-lockfile` cycle (~30s cold-run cost) vs splitting into separate parallel jobs. **Future plan-authoring pattern: when a new check is cheap (~5s on warm node_modules cache) and shares the same prerequisites as an existing check, append it as a sequential step on the existing job rather than splitting into a parallel job.**
- **Path-trigger surface for generated-artifact drift checks includes BOTH source files AND the generated artifact itself (DEBT-03).** Expanded from 3 entries to 7: `Projects/**`, `src/content/projects/**`, `src/data/about.ts` (NEW), `src/data/portfolio-context.static.json` (NEW), `src/data/portfolio-context.json` (NEW — the generated artifact itself), `scripts/sync-projects.mjs`, `scripts/build-chat-context.mjs` (NEW). **A PR that modified only the generated portfolio-context.json (e.g., manually edited) would otherwise slip past source-only triggers; including the generated path catches drift in either direction.**

Plan 17-05 execution decisions (2026-05-10) — DEBT-02 chat cache-hit observability + TEST-03 Anthropic payload-shape forward-defense:

- **Seams INLINED into the actual cache-touching files, NOT factored into new modules.** The original REQUIREMENTS.md DEBT-02 wording referenced `src/lib/chat-cache.ts` + `src/lib/content-snapshot.ts` as the seam locations — but the planner verified during 17-PATTERNS that neither file exists in the repo (Phase 14 chat-knowledge cache is in-server only — `system: [{ cache_control: { type: "ephemeral" } }]` block in `buildChatRequestArgs()` at src/prompts/chat-request-shape.ts is the only "cache surface"; content snapshot lives implicitly in `src/data/portfolio-context.json`). Per CLAUDE.md "Don't add abstractions beyond what the task requires" — DEBT-02 inlined a single `else if (event.type === "message_start")` branch (~10 LOC) into the existing SSE stream loop in src/pages/api/chat.ts, and a single finally-block DEV-only log line into src/scripts/chat.ts streamChat(). Creating empty wrapper modules would have been over-abstraction. **Future plan-authoring pattern: when a plan's `files_modified` lists a file that doesn't exist, the planner MUST resolve the gap at plan-time (either create the file as a new artifact OR reconcile the file reference to an existing inline seam) — leaving it to executor discretion risks scope drift.**
- **D-15 SSE byte-identical anchor proved its design value during Task 1.** The plan's Test 3 explicitly asserted no `cache_*` substring appears in the SSE response body. Combined with the standalone `tests/api/sse-snapshot.test.ts` byte-identical fixture (TEST-02), the contract is double-locked: the seam test catches the substring leak; the snapshot test catches any single-byte addition to the SSE frame stream. Both GREEN at every Task 1 verification. **Pattern: server-side structured logs route to Workers Logs / wrangler tail, NEVER to the SSE response body. The SSE body is consumer contract (chat client UI rendering); logs are operator contract (observability + debugging). The two surfaces have different consumers, different lifecycles, different SLAs — keeping them mechanically separate is non-negotiable.**
- **Client-side seam emits `chat.response_metrics_client` (distinct event name from server's `chat.cache_metrics`), not the same name.** The plan locked this in Task 2 spec because the client cannot read `cache_read_input_tokens` through the SSE stream (D-15 anchor — server doesn't send it). So the client seam logs `elapsed_ms` as a cache-hit PROXY (faster TTFB / total-stream → likely cache hit). Using the same event name for both surfaces would have created ambiguity when filtering Workers Logs (which only sees the server emissions) vs DevTools Console (which only sees the client emissions). Distinct names → unambiguous grep / filter behavior. **Pattern: when two observability seams in different tiers report related-but-distinct measurements, use distinct event names. Same event name implies same measurement; ambiguity costs more than the 3-token name length.**
- **`finally`-block placement for the client log seam (instead of duplicated `onDone()` site insertions).** Task 2 spec'd "after the final frame is read OR the stream ends" — streamChat() has 4 distinct exit paths (status 429 onError, !response.ok onError, !response.body onError, [DONE] frame return, natural loop end, error path catch). Inserting the log line at all 5 sites would have been error-prone (forget one → biased metrics). A single `finally` block at the outer try/catch covers all paths — by language contract, finally runs on every exit from the try/catch chain. The cost is one closure capture of `t0` outside the try; the benefit is correctness by construction. **Pattern: when an instrumentation seam must fire on EVERY exit path of a function with N early returns + catch blocks, hoist the timer + sink to a `finally` block, not inline at each return site.**
- **TEST-03 snapshot test uses TWO call args (different messages) and asserts byte-equality on `args.system`, not just absence of a sessionId literal.** The plan called for "no sessionId literal in system" — that's the necessary condition. The sufficient condition (and the actual cache-hit predicate Anthropic evaluates) is that the entire `system` array serializes to identical bytes across calls with different messages payloads. A future regression could thread sessionId in a non-obvious way (e.g., concatenating it into the system prompt text via a template) — the byte-equality test catches that even when the literal `sessionId` substring doesn't appear. **Pattern: when defending a contract that downstream evaluates as byte-equality, the test must also evaluate byte-equality. Substring-presence checks are a strictly weaker assertion.**
- **Rule 3 auto-fix bundled into Task 2's commit (typecheck-annotate Task 1's test file).** Task 1's `tests/api/cache-hit-logs.test.ts` introduced 2 `ts(7006)` errors under `astro check` (implicit-any on the `find()` callback param `(c)`). Pre-existing similar errors in `tests/client/listener-dedup.test.ts` from Plan 17-03 commit `0ad77b3` were already polluting `astro check` on `main` (4 total errors at HEAD~1 of Plan 17-05). Per the Git Safety Protocol "Always create NEW commits rather than amending" — the typecheck fix to Task 1's test file (one-line annotation `(c: unknown[])`) was bundled into Task 2's commit as a Rule 3 deviation (blocking issue directly caused by Task 1). The 2 listener-dedup.test.ts errors are out-of-scope (pre-existing) and logged in `.planning/phases/17-foundations-migration-dns-debt-sweep/deferred-items.md`. **Pattern: when a TDD test file introduces a new typecheck error, fix it inline rather than committing the error and following up — but if the test is already in a previous commit, do NOT amend; create a new commit and bundle with the next logical task.**
- **No plan since Plan 17-03 (commit `0ad77b3`, 2026-05-10) has run `pnpm build` / `astro check` on `main`.** The 2 pre-existing implicit-any errors in `tests/client/listener-dedup.test.ts` have been on `main` for 3 plan close-outs (17-03, 17-04, 17-05 baseline). `pnpm test` (vitest) doesn't run `astro check`; `pnpm build` does. The plans since 17-03 either ran tests only (`pnpm test`) or built a specific entry (`pnpm build:chat-context:check`), so the typecheck regression silently accumulated. **Pattern: phase-end close-out gates should include `pnpm exec astro check` (zero-error policy) explicitly, not just `pnpm test`. Phase 17 should retire `astro check` to zero before the Plan 17-06 metadata commit closes the phase — that's a 5-minute fix logged in deferred-items.md.**

Plan 17-06 execution decisions (2026-05-11) — DNS-01 + DNS-02 Resend domain authored, warmed, and Phase 17 closed:

- **Resend account was pre-existing, not freshly created in Phase 17.** User had a Resend account from a previous setup attempt that left the send.* DNS records (flagged in Plan 17-02 SUMMARY's "Issues Encountered"). Phase 17 only ADDED the `mail.jackcutrara.com` domain to the existing account — not a fresh signup. The send.* dust is the artifact of the prior abandoned setup. **Future-pattern: when a CONTEXT.md user_setup step references "Create X account," the planner should NOT assume the account doesn't exist. Verify at plan-time whether the account exists; if it does, the operation reduces to "add domain X to existing account Y."**
- **Used `mail.jackcutrara.com` (not `send.jackcutrara.com`) for v1.3 transactional sending despite send.* records existing on the account.** CONTEXT.md D-06 locked `mail.*` as the canonical sending subdomain; switching mid-execution would have invalidated D-06 + downstream sender-string locks (`"Portfolio Chat" <transcripts@mail.jackcutrara.com>` in CHAT_SENDER_EMAIL secret, in scripts/resend-warmup.mjs, in Phase 20 src/lib/email/resend.ts). Cleaner to leave send.* dust alone (no conflict) and stick with `mail.*`. The send.* records become low-priority cleanup post-phase. **Pattern: CONTEXT.md decision locks survive runtime discoveries that don't violate the lock's premise. Pre-existing artifacts on a different scope (send.* vs mail.*) are non-conflicting and don't justify re-litigation.**
- **5 warmup sends sufficient — second round of 5 NOT executed.** CONTEXT.md D-08 caps Phase 17 warming at 5-10 sends. Since 5/5 landed in Inbox on first try with NO manual Not-Spam feedback required, the 5-then-wait-then-5 pattern's wait+second-round step was unnecessary. Per RESEARCH Pitfall 7 — Gmail's reputation system penalizes sudden volume spikes from new From-domains; sending a second round when the first round was already 5/5 Inbox would have been over-warming with no clean signal-to-act. **Pattern: send-once-then-evaluate before send-many. At low expected volume (~1-30/day in v1.3), 5 sends is enough signal. The 5-10 range is a CAP not a target; reserve the upper end for cases where the first round buckets to Spam.**
- **Warmup-script-as-Phase-20-dry-run.** Plan 17-06's `scripts/resend-warmup.mjs` intentionally exercises the SAME `fetch()` shape Phase 20's `src/lib/email/resend.ts` will use: REST against `https://api.resend.com/emails` (NOT npm SDK); `Authorization: Bearer ${RESEND_API_KEY}`; `Idempotency-Key: warmup/{sessionId}` → Phase 20 `transcript/{sessionId}`; JSON body shape with from/to/reply_to/subject/text. **Pattern: when a future phase will introduce a new external HTTP call, author a throwaway script in an earlier phase that exercises the SAME endpoint + same auth shape + same idempotency convention. The script doubles as a wire-shape validator and a deliverability warming surface.** Phase 20 src/lib/email/resend.ts is now strictly REST-replay against a proven wire.
- **mail.jackcutrara.com sending-subdomain isolation decouples transactional reputation from apex.** Apex DMARC unchanged; subdomain DMARC starts at `p=none` (warming posture). A future deliverability incident on the transactional path cannot poison the apex's reputation that recruiter outreach relies on. **Pattern: prefer subdomain isolation for transactional email (`mail.*`, `transactional.*`, `no-reply.*`) over apex sending. Mirrors operational pattern at most production SaaS.**
- **Postmaster Tools enrollment is a one-time setup with a 24-48h data-lag debt.** At Phase 17 close, dashboard shows "enrolled; data pending volume." This is expected at low volume (5 sends total). Revisit at Phase 20 close-out (after first real transcript sends from cron path) AND v1.3 launch + 7 days. **Pattern: data-lag observability tools are valuable to enroll EARLY (the 24-48h clock starts at enrollment), even if their data isn't actionable at enrollment time. Don't defer enrollment to "when we need the data" — by then the lag is between you and the answer.**
- **Pre-existing Resend DNS dust on send.* + root left untouched in Phase 17.** The records (MX `send`, TXT `send` "v=spf1 include:amazon...", TXT `resend._domainkey` root, TXT `_dmarc` root with `v=DMARC1; p=none;`) don't conflict with the new `mail.*` records (different scope). Cleanup scheduled as low-priority `/gsd-quick` task post-phase per 17-RETROSPECTIVE.md. **Pattern: "operational hygiene" debts (pre-existing artifacts that are harmless but add audit noise) are valid `/gsd-quick` candidates and should NOT be folded into a scope-locked plan that would otherwise expand. The 4 records can be deleted in 10 minutes; the deliberation cost of folding them into Plan 17-06 mid-execution would exceed the cleanup cost.**
- **Phase 17 CLOSED-for-execution-purposes vs Pages-retirement-PENDING.** FOUND-03's Pages retirement sub-goal is a 24h-warm-window check (NOT a timer per CONTEXT.md A3) — gated on clean window + no open regressions. Window opened ~2026-05-10 22:00 UTC; user retires manually via Cloudflare dashboard. Phase 17 EXECUTION is closed (all plans complete, all requirements GREEN per design); Pages retirement is a separate scheduled action that lives in the meta layer. **Pattern: distinguish "phase EXECUTION closed" (all plans complete; orchestrator can move to next phase) from "phase ARTIFACTS closed" (every sub-goal physically retired). When a phase artifact is gated on a real-world clock that's longer than typical plan-execution time, it's acceptable to close execution and track the artifact-retirement as a separate scheduled action.**

Plan 17-09 execution decisions (2026-05-11) — UAT Gap #3 COPY button feedback window closed via CSS class wiring + M3 inline-color-write deletion:

- **CSS class as single source of truth for visual feedback state — extends DEBT-05 anti-imperative-write pattern from `display` to `color`.** Plan 17-03 closed DEBT-05 by removing `panel.style.display = 'flex'` writes in chat.ts and proving the `.is-open` CSS class was load-bearing. Plan 17-09 applies the SAME pattern shape to the COPY button color: deleted both `copyBtn.style.color = 'var(--accent)'` and `copyBtn.style.color = 'var(--ink-faint)'` inline writes, proved the new `.chat-copy-btn.copy-success { opacity: 1; color: var(--accent); }` CSS class rule is load-bearing. Inline writes (specificity 1,0,0,0) BEAT class rules (0,2,0); leaving the inline writes in place would have made the CSS rule's color portion silent dead code (latent fragility for a future contributor who deletes the inline writes thinking the CSS rule covers them). **Pattern: when a class-based CSS rule consumer is added to the codebase, audit the call site for inline style writes that would shadow the rule via specificity. If found, delete them — the CSS rule is the single source of truth, and the deletion proves it.**
- **TDD RED state spanned a commit boundary (Task 1 commit dcf597b → Task 2 commit b35ad94 transition).** I authored the M3-contract test in Task 1 (asserts `style.color === ''` and `classList.contains('copy-success') === true` after click) and committed it RED — Task 1's full-suite result was 408 PASS / 1 FAIL / 2 SKIP. Task 2 then deleted the inline color writes, turning the M3 test GREEN — Task 2's full-suite result was 409 PASS / 0 FAIL / 2 SKIP. **Pattern: TDD RED can span commit boundaries when the contract under test is delivered in a multi-task plan and the RED test is itself part of the contract documentation. The Task 1 commit message MUST document the RED state explicitly so future code-archaeologists understand the intermediate-state failure is intentional, not a regression.** This is structurally cleaner than the alternative (skipping the test in Task 1 and unskipping in Task 2) because the test-as-contract is visible in git history at every commit, just RED → GREEN across two commits.
- **Replaced existing chat-copy-button.test.ts entirely rather than appending — same pattern as Plan 17-03's chat-pulse-coordination.test.ts rewrite.** The pre-Plan-17-09 file had 5 tests, 2 of which asserted `expect(btn.style.color).toBe('var(--accent)')` and `expect(btn.style.color).toBe('var(--ink-faint)')` — the EXACT inline-write contract that Task 2's M3 fix DELETES. Leaving those assertions would have failed Task 2's GREEN gate. The 5 helper tests were retargeted to assert the post-M3 contract (style.color === '' AND classList.contains('copy-success') === true) instead of the pre-M3 contract. Net: 10 tests in the new file (4 M4-isolated CSS-cascade + 1 lifecycle + 5 retargeted helper). **Pattern lifted: when a plan removes an imperative behavior, audit existing tests that assert the imperative behavior and rewrite them as part of the same plan (same shape as Plan 17-03 SUMMARY's `tests/client/chat-pulse-coordination.test.ts` rewrite). The plan's `files_modified` list MUST capture the test file rewrite at plan-time — Plan 17-09's frontmatter listed `tests/client/chat-copy-button.test.ts` as a NEW file, but it was actually a REPLACEMENT. Future plan-authoring: the planner SHOULD verify file existence at plan-time and document REPLACEMENT vs CREATE explicitly.**
- **B-iter2 single-line anchor Edits prescription satisfied by a single multi-line Edit.** The plan specified 3 single-line anchor Edits for the M3 fix (defensive against multi-line whitespace drift); in practice the createCopyButton click handler block was so tightly scoped (9 lines, all unique substrings, byte-for-byte verified via Read tool prior to Edit) that a single multi-line Edit covering the full block was structurally robust AND simpler. The single-Edit shape avoided the intermediate-file-state risk inherent in 3 sequential Edits (each of which leaves the file in a partial-fix state). Result identical to what 3 anchor edits would have produced; B-iter2's safety property (byte-stable against drift) was preserved via the pre-Edit Read verification. **Pattern: B-iter2's "split into single-line anchors" prescription is a defensive shape against PRIOR ITERATION FAILURES. When the executor verifies the multi-line anchor matches byte-for-byte via the Read tool BEFORE Edit, the prescription's safety property is achieved without the splitting overhead. Defensive shapes that are no longer needed should be allowed to simplify; document the simplification in the SUMMARY.**
- **Manual UAT (10 steps requiring browser interaction) deferred to user — automated test suite locks the contract structurally.** The plan's manual UAT requires `pnpm preview` + cursor movement to verify the post-click visibility behavior. Agent-side I cannot drive a real browser to evaluate this. The chat-copy-button.test.ts lifecycle test uses `vi.useFakeTimers` to prove the 1500ms window structurally — the class is added at t=0, present at t=750 (mid-window), removed at t=1501. This proves the timing contract; the user runs the manual UAT post-Plan-17-08 deploy to close the operational loop. **Pattern: when a plan's manual UAT is structurally infeasible for an agent (real browser interaction, cursor movement, visual verification), the agent should document explicitly which contract is locked structurally (via fake-timer / fixture tests) vs which is deferred to user (operational verification). DO NOT silently mark manual UAT as PASS — this hides the gap.**
- **`pnpm build` failure on pre-existing typecheck errors blocks the plan-spec'd UAT command but does NOT block plan completion.** `pnpm build` runs `astro check && astro build`; the 2 pre-existing `tests/client/listener-dedup.test.ts` ts(7006) errors fail the check step. Per SCOPE BOUNDARY rule, those errors are not directly caused by Plan 17-09's task changes (carry-forward from Plan 17-03 commit 0ad77b3). The plan completes with `pnpm exec astro build` (skips check, builds cleanly) and the typecheck debt is logged for Plan 17-08 absorption (since 17-08 is the deploy gate and CANNOT push to main with a failing build). **Pattern: when a pre-existing build-step failure blocks a plan's verification commands but is OUT OF SCOPE per SCOPE BOUNDARY, the plan completes by running the next-step command directly (e.g., `pnpm exec astro build` instead of `pnpm build`). The pre-existing failure remains as deferred debt for the appropriate plan to absorb (here: Plan 17-08 absorbs because it's the deploy gate and cannot push past the failure).**

### Open Blockers (carried into v1.3)

The following items move into v1.3 scope as the chat tech debt sweep (all close in Phase 17):

- ~~`CHAT_RATE_LIMITER` Cloudflare binding never configured on Production or Preview.~~ **CLOSED 2026-05-10 in Plan 17-04** — DEBT-01 documentation closure. PROJECT.md Known issues entry reframed to "documented + Free-tier acceptable per v1.3 locked decision." Code path defensively skips when binding absent — byte-identical from Phase 7. Workers Paid plan upgrade is v1.4+ trigger; reconsider if Anthropic spend or chat volume crosses thresholds that justify $60/yr.
- ~~Chat cache-hit-rate observability not yet wired (Phase 14 deferred).~~ **CLOSED 2026-05-10 in Plan 17-05** — DEBT-02 closed. Server-side `chat.cache_metrics` structured log emitted at the `message_start` branch of the SSE stream loop in src/pages/api/chat.ts (commit `7c3827e`): flat-primitive fields `cache_read_input_tokens` / `cache_creation_input_tokens` / `input_tokens` / `output_tokens` from Anthropic usage block; defaults to 0 when omitted. Client-side `chat.response_metrics_client` DEV-only log in chat.ts streamChat() finally-block (commit `e54f09d`) gated on `import.meta.env.DEV` so Vite tree-shakes it out of production dist bundles; surfaces `elapsed_ms` as a cache-hit proxy since the client cannot read cache tokens through SSE (D-15 anchor). Planner reconciled `src/lib/chat-cache.ts` / `src/lib/content-snapshot.ts` references in the original closure-path bullet — those files do not exist in the repo; seams inlined into the actual cache-touching files per CLAUDE.md "Don't add abstractions beyond what the task requires." META-02 (transcript email surfacing of the same cache token counts) remains Phase 18 work.
- ~~`build:chat-context:check` not enforced in CI.~~ **CLOSED 2026-05-10 in Plan 17-04 (commit `e46aa2d`)** — DEBT-03 closed. `.github/workflows/sync-check.yml` now runs `pnpm build:chat-context:check` as a parallel step after the existing `pnpm sync:check` step (single-job two-step pattern per RESEARCH §"DEBT-03 CI Job Shape"). Path triggers expanded from 3 to 7 entries to cover `src/data/about.ts`, `src/data/portfolio-context.static.json`, `src/data/portfolio-context.json` (the generated artifact itself), and `scripts/build-chat-context.mjs` in addition to the existing `Projects/**`, `src/content/projects/**`, `scripts/sync-projects.mjs`. PRs fail-fast on local drift between sources and the generated portfolio-context.json.
- ~~`#chat-panel` JS-coupled display contract — `animatePanelOpen` flips `style.display='flex'` directly while `.is-open` only animates.~~ **CLOSED 2026-05-10 in Plan 17-03 (commit `1c148c9`)** — DEBT-05 closed. `.is-open` class in global.css now controls both display (display:none base → display:flex on .is-open) AND the existing keyframe scale-in animation. animatePanelOpen / animatePanelClose are no-op async stubs preserving await semantics.
- ~~WR-01 bootstrap listener registers without dedup (`analytics.ts:140-147`, `scroll-depth.ts:63-70`, `chat.ts:870-877`) — no observable double-count due to `*Initialized` guards, but long sessions could accumulate listeners.~~ **CLOSED 2026-05-10 in Plan 17-03 (commit `0ad77b3`)** — DEBT-04 closed. All three modules use remove-then-add at document level; browser API contract makes the registration idempotent regardless of module re-evaluation.

Carry-forwards NOT in v1.3 scope (out-of-scope for this milestone):

- Mobile menu breakpoint 380px → 768px UX revision (`.planning/todos/pending/2026-04-15-change-mobile-menu-breakpoint-from-380px-to-768px.md`).
- OG default image authoring (`.planning/todos/pending/2026-04-15-design-and-ship-og-default-image.md`).

## Deferred Items

Items deferred from v1.2 still applicable:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Chat | RAG / vector DB | Deferred (corpus too small) | v1.2 planning |
| Chat | Function-calling chat tools (`getProject`, `listProjects`) | Deferred (SSE complexity) | v1.2 planning |
| Chat | Keyword routing | Deferred (only if Anthropic spend > $5/mo) | v1.2 planning |
| Chat | 1-hour cache TTL | Current 5-minute TTL (D-14) sufficient | Phase 14 close-out 2026-04-23 |
| Chat | Live-API injection test suite automation | CI scheduling deferred | Phase 14 close-out 2026-04-23 |
| Chat | `Projects/7 — MULTI-DEX CRYPTO TRADER` inclusion | MDX conversion + relevance tagging deferred | Phase 14 close-out 2026-04-23 |
| Motion | Signature hero moment | User-excluded for v1.2 | v1.2 planning |
| Motion | Project → project view-transition-name morph | Deferred to v1.3+ | v1.2 planning |
| Analytics | ANAL-02 §7 preview-subdomain silence check | Deferred to next branch deploy | Phase 15 close-out 2026-04-26 |

Items deferred at v1.3 roadmap time (locked):

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Chat Visibility | `/api/resend-webhook` with Svix HMAC (bounce/complaint/delivered) | Deferred to v1.4+ | v1.3 roadmap 2026-05-09 |
| Chat Visibility | Per-IP session rate limit (transcript spam prevention) | Deferred to v1.4+ | v1.3 roadmap 2026-05-09 |
| Chat Visibility | HTML email body | Deferred — re-evaluate threat model only if plaintext friction reported | v1.3 roadmap 2026-05-09 |
| Chat Visibility | Workers Paid plan upgrade (binds `CHAT_RATE_LIMITER`) | Deferred — Free-tier acceptable; reconsider on traffic threshold | v1.3 roadmap 2026-05-09 |
| Chat Visibility | Cloudflare Workers Analytics Engine for transcript metrics | Deferred — anchor decision: Jack reads every email | v1.3 roadmap 2026-05-09 |
| Chat Visibility | Phase 21 Observability + Hardening (research SUMMARY.md optional phase) | Deferred to v1.4+ | v1.3 roadmap 2026-05-09 |

## Session Continuity

Last session: 2026-05-11T15:00:00.000Z
Stopped at: Plan 17-09 (Wave 8 -- gap-closure for UAT Gap #3 COPY button feedback window) COMPLETE. 2 atomic commits: dcf597b (Task 1 -- src/styles/global.css new rule .chat-copy-btn.copy-success { opacity: 1; color: var(--accent); } + tests/client/chat-copy-button.test.ts REPLACED with 10 tests across 3 describe blocks: 4 M4-isolated CSS-cascade + 1 vi.useFakeTimers lifecycle + 5 retargeted createCopyButton helper tests asserting post-M3 contract; 9/10 GREEN at end of Task 1, 1 RED was the M3-contract test waiting for Task 2's deletion of inline style.color writes), b35ad94 (Task 2 -- src/scripts/chat.ts new module-scope const COPY_FEEDBACK_MS = 1500 + copyToClipboard setTimeout literal 2000 -> COPY_FEEDBACK_MS + createCopyButton click handler M3 fix: deleted both `copyBtn.style.color = 'var(--accent)'` and `copyBtn.style.color = 'var(--ink-faint)'` inline writes, replaced 1000 -> COPY_FEEDBACK_MS, added M3 rationale comment block; CSS class is now SOLE source of truth for color during feedback window; M3-contract test turned GREEN). Phase-end pnpm test = 409 PASS / 0 FAIL / 2 SKIP (was 404/0/2 baseline at end of Plan 17-07; +5 net new tests; first time the suite has been all-green AND >= 405 tests). chat-copy-button.test.ts 10/10 GREEN; no-imperative-display-flip.test.ts 3/3 GREEN; sse-snapshot.test.ts 3/3 GREEN (D-15 anchor preserved). pnpm exec astro check = 2 pre-existing errors in tests/client/listener-dedup.test.ts (carry-forward from Plan 17-03 commit 0ad77b3; logged in deferred-items.md; Plan 17-09 introduced 0 new typecheck errors). pnpm exec astro build = clean (10 routes prerendered). Manual UAT (10 steps requiring `pnpm preview` + browser interaction) DEFERRED TO USER -- agent cannot drive a real browser to evaluate post-click visibility behavior outside the JS event loop; the automated suite locks the contract structurally with vi.useFakeTimers proving the 1500ms window. UAT-GAP-03 closed structurally; closes operationally when user runs manual UAT post-Plan-17-08 deploy. Phase 17 progress: 8/10 plans complete (was 7/10).
Resume file: (none -- next step is Plan 17-10, triggered by user invoking /gsd-execute-phase 17 --gaps-only or directly executing the plan)
Next command: `/gsd-execute-phase 17 --gaps-only` to continue with Plan 17-10 (Wave 9 -- view-transition AbortError handler in BaseLayout.astro head + MOTION.md §5 MOTN-01 rejection-handling spec + 4 build-time tests). After 17-10, Plan 17-08 (Wave 10 -- RELEASE BLOCKER deploy gate, removes inline display:none from #chat-panel ChatWidget.astro) closes the gap-closure chain. 17-08 must also address the pre-existing tests/client/listener-dedup.test.ts typecheck errors (Rule 3 — blocks `pnpm build` which 17-08 needs for deploy verification).
