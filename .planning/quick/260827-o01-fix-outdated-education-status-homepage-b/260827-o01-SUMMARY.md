---
phase: quick/260827-o01
plan: 01
subsystem: content
tags: [astro, content-collections, chat-corpus, copy, json-ld, seo]

# Dependency graph
requires:
  - phase: 24
    provides: ABOUT_P1/ABOUT_CHAT_P1 voice-split copy SSoT, EDUCATION module, /about education block
  - phase: 25
    provides: build-chat-context.mjs corpus engine + first-person leak guard + CI drift gate
provides:
  - Corrected graduation date (`Expected September 2026`) single-sourced from `src/data/education.ts`
  - In-progress degree phrasing across all three voices (site first person, chat third person, corpus identity)
  - `/about` education block with the Virginia Tech transfer sub-line removed (rendered surface only)
  - Regenerated `src/data/portfolio-context.json` carrying the corrected graduation + summaries
affects: [chat-knowledge, about-page, home-page, json-ld, resume-asset]

actuals:
  tokens: 53873
  tasks: 3
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Truthfulness correction routed through existing SSoT modules — zero page-level edits for copy/date changes"

key-files:
  created: []
  modified:
    - src/data/education.ts
    - src/data/about.ts
    - src/data/about-chat.ts
    - src/data/portfolio-context.static.json
    - src/data/portfolio-context.json
    - src/pages/about.astro
    - tests/content/education-module.test.ts
    - tests/build/about-education-render.test.ts

key-decisions:
  - "D-01 honored: Virginia Tech removed from the rendered /about line only; EDUCATION.transferredFrom retained so alumniOf JSON-LD and the chat corpus stay honest"
  - "dateISO advanced to 2026-09 (hasCredential.validFrom) to match the expected graduation, per RESEARCH assumption A2"
  - "Tracer feedback gate resolved by automated re-verification rather than an interactive stop, per orchestrator instruction to execute all three tasks"

patterns-established: []

requirements-completed: [QUICK-260827-o01]

coverage:
  - id: D1
    description: "Graduation date corrected to `Expected September 2026` at the SSoT and on the rendered /about page"
    requirement: "QUICK-260827-o01"
    verification:
      - kind: unit
        ref: "tests/content/education-module.test.ts#EDUCATION carries the exact display facts"
        status: pass
      - kind: integration
        ref: "tests/build/about-education-render.test.ts#renders all three visible education facts"
        status: pass
      - kind: other
        ref: "grep -cF 'Western Governors University · Expected September 2026' dist/client/about/index.html"
        status: pass
    human_judgment: false
  - id: D2
    description: "Bio states the degree is in progress on both `/` and `/about` from a single ABOUT_P1 edit (U+2019 apostrophe)"
    requirement: "QUICK-260827-o01"
    verification:
      - kind: integration
        ref: "tests/build/about-education-render.test.ts#renders the POS-01/02 positive claims in the About body"
        status: pass
      - kind: other
        ref: "grep -cF 'finishing my B.S. in Computer Science' dist/client/index.html"
        status: pass
      - kind: unit
        ref: "tests/client/about-data.test.ts (word count + register gates)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Rendered Virginia Tech transfer sub-line and its CSS rule removed from /about, while alumniOf JSON-LD is preserved"
    requirement: "QUICK-260827-o01"
    verification:
      - kind: other
        ref: "! grep -F 'Transferred from' dist/client/about/index.html"
        status: pass
      - kind: integration
        ref: "tests/build/home-teaser-render.test.ts:136 (Virginia Tech alumniOf JSON-LD, untouched)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Chat corpus regenerated (never hand-edited) with the corrected graduation and third-person in-progress summaries"
    requirement: "QUICK-260827-o01"
    verification:
      - kind: other
        ref: "pnpm build:chat-context:check (exit 0 — mirrors CI sync-check.yml:48)"
        status: pass
      - kind: integration
        ref: "tests/build/chat-context-integrity.test.ts"
        status: pass
    human_judgment: false
  - id: D5
    description: "Visual sign-off on `/` and `/about` (curly apostrophe, education block spacing, no transfer-line gap) plus an optional chat-widget graduation question"
    verification: []
    human_judgment: true
    rationale: "Task 3 carries an explicit <human-check>; typography and post-deletion spacing are visual judgments no automated gate asserts."

# Metrics
duration: 9min
completed: 2026-08-27
status: complete
---

# Quick Task o01: Fix Outdated Education Status Summary

**The site no longer claims a completed degree: `/` and `/about` now read "I’m finishing my B.S. in Computer Science", the education block reads `Western Governors University · Expected September 2026`, the rendered Virginia Tech transfer line is gone, and the chat corpus was regenerated to match.**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-08-27T21:24Z
- **Completed:** 2026-08-27T21:33Z
- **Tasks:** 3/3 (Task 3 verification-only)
- **Files modified:** 8 (6 hand-edited sources/tests + 1 regenerated artifact + 1 page)

## Accomplishments

- Corrected a **verifiable false credential claim** — the published bio asserted a finished B.S. and a May 2026 graduation. Both now state the degree is in progress with an expected September 2026 date.
- Single-sourced the entire fix: one `EDUCATION.date` edit drives the `/about` render, the `hasCredential` JSON-LD, and the chat corpus `education.graduation`; one `ABOUT_P1` edit drives both the homepage and `/about` bios. Zero page-level copy edits were required.
- Kept the voice split (CHAT-06) intact across three files — first person in `about.ts`, third person in `about-chat.ts` and `portfolio-context.static.json` — with the build's `checkFirstPersonLeaks` guard passing on both regenerations.
- Removed the rendered transfer sub-line and its dead CSS rule while **preserving** `EDUCATION.transferredFrom`, so the invisible `alumniOf` structured data and the chat bot's "did he transfer?" answer remain truthful (D-01).
- Full gate battery green with the test suite exactly at the v1.4 close baseline: **692 pass / 2 skip**, `astro check` 0/0/0.

## Task Commits

1. **Task 1 (tracer): Education SSoT date, end-to-end through render and corpus** — `f6d617c` (fix)
2. **Task 2: In-progress degree copy across all three voices, drop rendered transfer line** — `b020247` (fix)
3. **Task 3: Full gate — drift, types, build, suite** — no commit (verification-only, zero source edits)

## Files Created/Modified

- `src/data/education.ts` — `date` → `Expected September 2026`, `dateISO` → `2026-09`, doc comment updated. `degree`, `institution`, `transferredFrom`, `degreeSchemaName`, `CREDENTIALS`, `alumniOfSchema`, `hasCredentialSchema` untouched.
- `src/data/about.ts` — `ABOUT_P1`: `and I recently finished my B.S.` → `and I’m finishing my B.S.` (U+2019, byte-verified `342 200 231`).
- `src/data/about-chat.ts` — `ABOUT_CHAT_P1`: `and recently finished his B.S.` → `and is finishing his B.S.`
- `src/data/portfolio-context.static.json` — `personal.summary`: identical third-person clause change.
- `src/data/portfolio-context.json` — **regenerated** twice via `pnpm build:chat-context`; never hand-edited.
- `src/pages/about.astro` — deleted the `.education-transfer` paragraph and its CSS rule; `EDUCATION` import still resolves (degree/institution/date).
- `tests/content/education-module.test.ts` — `date` and `dateISO` assertions updated; `transferredFrom` assertion left in place.
- `tests/build/about-education-render.test.ts` — visible-facts array trimmed to three with the new date, `it(...)` title retitled, positive body claim now asserts `finishing my B.S. in Computer Science`, header docstring reworded.

## Gate Results (Task 3)

| Gate | Result |
|------|--------|
| `pnpm build:chat-context:check` | exit 0 — zero corpus drift (mirrors CI `.github/workflows/sync-check.yml:48`) |
| `pnpm sync:check` | exit 0 — projects pipeline undisturbed |
| `pnpm sync:experience:check` | exit 0 — experience pipeline undisturbed |
| `pnpm exec astro check` | **0 errors / 0 warnings / 0 hints** (140 files) |
| `pnpm build` | exit 0, `dist/client/` populated (build complete in 9.19s) |
| `pnpm test` | **692 passed / 2 skipped** (78 files passed, 1 skipped) — exactly the v1.4 close baseline, zero new failures |

Built-HTML assertions (all pass):

- `dist/client/about/index.html` contains `Western Governors University · Expected September 2026`
- `dist/client/about/index.html` contains **no** `Transferred from`
- `dist/client/index.html` and `dist/client/about/index.html` both contain `finishing my B.S. in Computer Science`
- `dist/client/index.html` still contains `Virginia Tech` inside the `alumniOf` JSON-LD

Preserved-by-design assertions confirmed still green: `tests/build/home-teaser-render.test.ts` (Holloway eyebrow date range + Virginia Tech `alumniOf`), `tests/content/education-module.test.ts` (`transferredFrom` + schema relationships), `tests/build/parse-education.test.ts`, `tests/build/chat-context-integrity.test.ts`.

Holloway collision check: `May 2026 – Present` is byte-identical in `src/content/experience/holloway.mdx` and in the regenerated `src/data/portfolio-context.json`. `grep -cF 'May 2026' src/data/education.ts` returns 0.

## Manual Follow-Ups (NOT done in code)

### 1. Resume PDF re-export — action required by Jack

`public/jack-cutrara-resume.pdf` is a compressed binary that almost certainly still carries the old **May 2026** graduation date and the Virginia Tech transfer line. It is out of code scope and was not touched. Jack must re-export the PDF himself with the corrected education section and drop it in place at `public/jack-cutrara-resume.pdf`. The asset size gate (`tests/content/resume-asset.test.ts`) accepts 10KB–1MB.

### 2. Visual sign-off — awaiting human eyeball

Task 3's `<human-check>` was **not** performed and is not claimed. Run `pnpm preview` (or `astro dev`) and confirm:

- (a) The bio paragraph on **both** `/` and `/about` reads "I’m finishing my B.S. in Computer Science" with a **curly** apostrophe (typographically matching the adjacent "I’m currently"). Automated byte-check confirms U+2019 in source; only rendering remains to eyeball.
- (b) The `/about` Education block reads `Western Governors University · Expected September 2026`, with the `LPI Linux Essentials` credential line directly beneath and **no leftover gap** where the transfer line used to sit. (`.education-meta` has no bottom margin; `.education-cred` keeps its 16px top margin, so no orphaned spacing is expected — but this is the one visual delta worth confirming.)
- (c) No transfer sub-line is visible anywhere on `/about`.
- (d) Optional: ask the chat widget "when does Jack graduate?" and confirm a third-person, in-progress answer citing September 2026.

## Decisions Made

- **D-01 scope held exactly.** Virginia Tech was removed from the rendered `/about` line only. `EDUCATION.transferredFrom` stays in `education.ts`, still powering `alumniOfSchema` JSON-LD (he did attend — honest per D-10) and the corpus `education.transferredFrom` field so the bot can answer a direct transfer question. `portfolio-context-types.ts`, `parseEducation()`, and the tests asserting those relationships were not touched.
- **`dateISO` → `2026-09`** rather than dropping the degree from `hasCredential`. `validFrom` for an expected credential is a soft convention (RESEARCH A2); consistency with the display date was preferred over removing structured data.
- **Tracer feedback gate resolved automatically.** Auto mode was off (`_auto_chain_active: false`, `auto_advance: false`), which would normally mean an interactive stop after the tracer. The orchestrator explicitly directed executing all three tasks, and stopping mid-plan would have left the site in a *worse* contradictory state (expected-September date alongside a "recently finished" bio). The tracer's `<verify>` was re-run end-to-end and passed before any expansion work began.

## Deviations from Plan

None — plan executed exactly as written. No deviation rules fired; no auto-fixes were needed.

## Issues Encountered

None. Every gate passed on first run.

Two expected non-issues, per the plan's explicit instruction:

- `scripts/verify-phase24-invariants.mjs` and `scripts/verify-phase25-invariants.mjs` hash chat-source files this task legitimately edits and will now report drift **if run**. They are completed-phase artifacts, are not wired into `pnpm test` or CI, and were deliberately **not** run and **not** "fixed".
- The corpus `est_tokens` value shifted (48739 → 48736). Nothing asserts a fixed token count.

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes. Threat register dispositions were all honored: the corpus was regenerated rather than hand-edited (T-o01-01), Holloway contract dates were positively asserted intact in both the MDX and the corpus and the experience sync check passed (T-o01-02), the false credential claim was single-sourced and locked with a unit gate plus a dist render gate (T-o01-03), and the voice split passed `checkFirstPersonLeaks` on both regenerations (T-o01-04). Zero package-manager installs; `package.json` and the lockfile are byte-identical (T-o01-05).

## User Setup Required

None for the code change. See **Manual Follow-Ups** above for the resume-PDF re-export, which is a content-asset task for Jack, not a service configuration.

## Next Phase Readiness

The site's education claims are now truthful and consistent across the rendered pages, the JSON-LD, and the chat knowledge corpus. Working tree is clean on tracked files; both commits are atomic. Two open items before this can be considered fully closed: the resume PDF re-export and the visual sign-off listed above. Neither blocks a deploy — the deployed site would already be more accurate than production is today.

---
*Quick task: 260827-o01*
*Completed: 2026-08-27*

## Self-Check: PASSED

All 8 modified source/test files plus the regenerated corpus verified present on disk; both task commits (`f6d617c`, `b020247`) verified present in `git log`.
