---
phase: quick/260827-o01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/education.ts
  - src/data/about.ts
  - src/data/about-chat.ts
  - src/data/portfolio-context.static.json
  - src/data/portfolio-context.json
  - src/pages/about.astro
  - tests/content/education-module.test.ts
  - tests/build/about-education-render.test.ts
autonomous: false
requirements:
  - QUICK-260827-o01

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/about` renders the education line `Western Governors University · Expected September 2026` (middot U+00B7 separator preserved, zero em dashes)"
    - "`/about` renders NO transfer sub-line: the `.education-transfer` paragraph and its CSS rule are gone from `src/pages/about.astro` (D-01 — visible line only)"
    - "`EDUCATION.transferredFrom` still exists in `src/data/education.ts` and still powers `alumniOfSchema` JSON-LD + the chat corpus `education.transferredFrom` field (D-01 — invisible structured data preserved, D-10 alumniOf-only honesty holds)"
    - "Homepage (`/`) AND `/about` both render `I’m finishing my B.S. in Computer Science` from the single `ABOUT_P1` SSoT edit, with typographic apostrophe U+2019 (not ASCII)"
    - "The generated chat corpus reports `\"graduation\": \"Expected September 2026\"` and third-person `is finishing his B.S. in Computer Science` in both `personal.summary` and `about.p1`"
    - "Holloway contract dates are byte-identical everywhere: `May 2026 – Present` still present in `src/content/experience/holloway.mdx` and in the regenerated `src/data/portfolio-context.json`"
    - "`pnpm build:chat-context:check` exits 0 (zero corpus drift, mirrors the CI gate at .github/workflows/sync-check.yml:48)"
    - "`pnpm exec astro check` reports 0 errors / 0 warnings / 0 hints and `pnpm test` is fully green with no new failures"
  artifacts:
    - path: "src/data/education.ts"
      provides: "Education SSoT with date `Expected September 2026` + dateISO `2026-09`; transferredFrom retained"
    - path: "src/data/about.ts"
      provides: "First-person ABOUT_P1 with the in-progress degree clause (U+2019 apostrophes)"
    - path: "src/data/about-chat.ts"
      provides: "Third-person ABOUT_CHAT_P1 with the in-progress degree clause"
    - path: "src/data/portfolio-context.static.json"
      provides: "Third-person personal.summary with the in-progress degree clause"
    - path: "src/data/portfolio-context.json"
      provides: "REGENERATED chat corpus (never hand-edited) carrying the corrected graduation + summaries"
    - path: "src/pages/about.astro"
      provides: "/about education block without the transfer sub-line or its dead CSS rule"
    - path: "tests/content/education-module.test.ts"
      provides: "Unit gate asserting the new date + dateISO (transferredFrom assertion unchanged)"
    - path: "tests/build/about-education-render.test.ts"
      provides: "Dist render gate asserting three visible facts + the in-progress degree claim"
    - path: ".planning/quick/260827-o01-fix-outdated-education-status-homepage-b/260827-o01-PLAN.md"
      provides: "This plan"
  key_links:
    - "`EDUCATION.date` (education.ts) → `about.astro:23` render AND `parseEducation()` in scripts/build-chat-context.mjs:397-446 → corpus `education.graduation` — one edit, three surfaces"
    - "`EDUCATION.dateISO` → `hasCredentialSchema[0].validFrom` JSON-LD on the homepage (education.ts:66-73)"
    - "`EDUCATION.transferredFrom` → `alumniOfSchema` JSON-LD, asserted at tests/build/home-teaser-render.test.ts:136 — MUST keep working after the /about line is deleted"
    - "`ABOUT_P1` (about.ts) → `src/pages/index.astro:140` AND `src/pages/about.astro:15` — single edit fixes both surfaces, no page edits"
    - "`about-chat.ts` + `portfolio-context.static.json` → scripts/build-chat-context.mjs merge (lines 632-635, 704-712) → corpus; both pass through `checkFirstPersonLeaks` which hard-exits 2 on a first-person leak"
---

<objective>
Correct Jack's degree status everywhere it is published: the bio now says he is *finishing* his B.S. (not that he recently finished it), the `/about` education block reads `Western Governors University · Expected September 2026`, and the rendered Virginia Tech transfer sub-line is removed. The chat knowledge corpus is regenerated from these sources so the bot answers consistently.

Purpose: the site currently makes a false credential claim (completed degree, May 2026 graduation). This is a truthfulness defect on the single artifact whose job is to make Jack credible to recruiters.

Output: 6 hand-edited source files, 1 regenerated corpus artifact, 5 updated test assertions, and a green full-gate run.

Scope boundary (D-01): Virginia Tech is removed from the **rendered `/about` line only**. `EDUCATION.transferredFrom` stays in `education.ts` so the invisible `alumniOf` JSON-LD stays honest (he did attend) and the chat corpus can still answer "did he transfer?" truthfully. Do NOT touch `alumniOfSchema`, `parseEducation()`'s transferredFrom handling, `portfolio-context-types.ts`, or the tests asserting those.

Definition of done:
1. `pnpm build:chat-context:check` exits 0.
2. `pnpm exec astro check` reports 0/0/0.
3. `pnpm build` succeeds, then `pnpm test` is green with no new failures.
4. Holloway's `May 2026 – Present` contract range survives untouched in the MDX and in the regenerated corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260827-o01-fix-outdated-education-status-homepage-b/260827-o01-RESEARCH.md
@.planning/STATE.md
@CLAUDE.md
</context>

<read_first>
`260827-o01-RESEARCH.md` is authoritative on file/line locations and verbatim old→new text (Change Set sections 1-6 and the "Test Updates Required" table). Read it before editing — do not re-derive the change set by grepping.

Two hard hazards from that research, both load-bearing:
1. The literal `May 2026` appears in the repo for two unrelated facts (graduation AND the Holloway contract start). Only `src/data/education.ts` may change. A repo-wide find/replace corrupts the experience data in four places.
2. `src/data/portfolio-context.json` is GENERATED. Never hand-edit it. Regenerate with `pnpm build:chat-context`; CI drift-checks it.

Also from research: `src/prompts/system-prompt.ts` contains zero education references — nothing to change there. `scripts/verify-phase25-invariants.mjs` and `scripts/verify-phase24-invariants.mjs` will now report drift on the chat-source files they hash; they are completed-phase artifacts, are NOT wired into `pnpm test` or CI, and must NOT be "fixed" by reverting these edits.
</read_first>

<!-- planner-discipline-allow: May 2026 -->
<!-- planner-discipline-allow: 2026-05 -->
<!-- planner-discipline-allow: recently finished -->
<!-- planner-discipline-allow: education-transfer -->
<!-- planner-discipline-allow: Transferred from -->

<tasks>

<task type="tracer">
  <name>Task 1: Education SSoT date, end-to-end through render and corpus</name>
  <files>src/data/education.ts, src/data/portfolio-context.json, tests/content/education-module.test.ts</files>
  <precondition>`pnpm` is installed and `node scripts/build-chat-context.mjs` runs from the repo root (it is exercised by `pnpm build`, so this holds on a working tree).</precondition>
  <action>
    Prove the single-source-of-truth wiring end-to-end on one fact (the graduation date) before touching any prose.

    In `src/data/education.ts` change exactly three lines inside the `EDUCATION` object, per RESEARCH.md Change Set 4: the `date` field value from `May 2026` to `Expected September 2026`; its doc comment on the line above so it describes the expected graduation date and quotes the new value; and `dateISO` from `2026-05` to `2026-09`. Change nothing else in the file — `degree`, `institution`, `transferredFrom`, `degreeSchemaName`, `CREDENTIALS`, `alumniOfSchema`, and `hasCredentialSchema` all stay exactly as they are (D-01).

    Do NOT edit `src/pages/about.astro` for the date: `about.astro:23` already renders `{EDUCATION.institution} · {EDUCATION.date}`, so the SSoT change produces the requested line with no page edit and the middot separator intact.

    Then regenerate the chat corpus with `pnpm build:chat-context`. This is the mandatory step, not a follow-up — `parseEducation()` reads `education.ts` at build time and CI runs the drift check. Do not hand-edit `src/data/portfolio-context.json`.

    Finally update the two stale assertions in `tests/content/education-module.test.ts` (lines 27 and 35) to the new date and ISO values. Leave line 28's `transferredFrom` assertion alone — it still holds.
  </action>
  <verify>
    <automated>pnpm build:chat-context &amp;&amp; grep -cF 'date: "Expected September 2026",' src/data/education.ts | grep -qx 1 &amp;&amp; grep -cF 'dateISO: "2026-09",' src/data/education.ts | grep -qx 1 &amp;&amp; grep -cF 'May 2026' src/data/education.ts | grep -qx 0 &amp;&amp; grep -cF '"graduation": "Expected September 2026"' src/data/portfolio-context.json | grep -qx 1 &amp;&amp; grep -cF '"dateRange": "May 2026 – Present"' src/data/portfolio-context.json | grep -qx 1 &amp;&amp; grep -cF 'May 2026 – Present' src/content/experience/holloway.mdx | grep -qx 1 &amp;&amp; grep -cF '"Virginia Tech"' src/data/education.ts | grep -qx 1 &amp;&amp; pnpm exec vitest run tests/content/education-module.test.ts</automated>
  </verify>
  <done>`education.ts` carries the expected-September date and `2026-09`, the regenerated corpus reports the new graduation value, Holloway's contract range is untouched in both the MDX and the corpus, `transferredFrom` survives (D-01), and the education unit gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: In-progress degree copy across all three voices, and drop the rendered transfer line</name>
  <files>src/data/about.ts, src/data/about-chat.ts, src/data/portfolio-context.static.json, src/pages/about.astro, src/data/portfolio-context.json, tests/build/about-education-render.test.ts</files>
  <action>
    Apply RESEARCH.md Change Sets 1, 2, 3 and 5 verbatim. The voice split (CHAT-06) means the same fact is worded three ways and each file must keep its own voice — a first-person leak into either chat file hard-exits the build with code 2.

    `src/data/about.ts` line 16 (`ABOUT_P1`, first person): change the clause `and I recently finished my B.S. in Computer Science.` to `and I’m finishing my B.S. in Computer Science.` CRITICAL: this file uses the typographic apostrophe U+2019 (`’`), not ASCII `'` — write `I’m`, matching the adjacent `I’m currently` in the same sentence. Leave the rest of the paragraph byte-identical. No page edit is needed: this single SSoT change fixes both the homepage (`index.astro:140`) and `/about` (`about.astro:15`).

    `src/data/about-chat.ts` line 24 (`ABOUT_CHAT_P1`, third person): change `and recently finished his B.S. in Computer Science.` to `and is finishing his B.S. in Computer Science.`

    `src/data/portfolio-context.static.json` line 6 (`personal.summary`, third person): apply the identical clause change.

    `src/pages/about.astro`: delete the entire `<p class="education-transfer meta-mono">Transferred from {EDUCATION.transferredFrom}</p>` element (line 24) and the now-dead `.education-transfer` CSS rule in the `<style>` block (line 47). Delete nothing else — the `EDUCATION` import stays (still used for degree/institution/date) and the surrounding spacing is already correct: `.education-meta` has no bottom margin and `.education-cred` keeps its 16px top margin, so no orphaned gap results (D-01).

    Regenerate the corpus again with `pnpm build:chat-context` so `personal.summary` and `about.p1` pick up the new prose.

    Update `tests/build/about-education-render.test.ts` per the research table: drop the transfer-line string from the visible-facts array (line 55) and change `May 2026` in that same array (line 54) to the new date; retitle that `it(...)` from four visible facts to three; and change the positive body claim on line 73 so it asserts the in-progress phrasing (`finishing my B.S. in Computer Science`). Also reword the file's header docstring where it describes four facts and a completed degree so the documentation matches the new gate.
  </action>
  <verify>
    <automated>pnpm build:chat-context &amp;&amp; grep -cF 'and I’m finishing my B.S. in Computer Science.' src/data/about.ts | grep -qx 1 &amp;&amp; grep -cF "I'm finishing" src/data/about.ts | grep -qx 0 &amp;&amp; grep -cF 'and is finishing his B.S. in Computer Science.' src/data/about-chat.ts | grep -qx 1 &amp;&amp; grep -cF 'and is finishing his B.S. in Computer Science.' src/data/portfolio-context.static.json | grep -qx 1 &amp;&amp; ! grep -rlF 'recently finished' src/ &amp;&amp; ! grep -nF 'education-transfer' src/pages/about.astro &amp;&amp; grep -c 'EDUCATION' src/pages/about.astro | grep -qv '^0$' &amp;&amp; pnpm exec vitest run tests/client/about-data.test.ts tests/content/site-copy-em-dash.test.ts tests/content/voice-em-dash.test.ts</automated>
  </verify>
  <done>All three voice variants state the degree is in progress, `about.ts` uses U+2019 apostrophes, no stale completed-degree phrasing survives anywhere under `src/`, the transfer paragraph and its CSS rule are gone from `about.astro` while the `EDUCATION` import still resolves, the corpus is regenerated, and the copy/word-count/em-dash/banlist gates pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate — drift, types, build, suite, and visual sign-off</name>
  <files>(no source edits; verification only)</files>
  <action>
    Run the complete verification battery from RESEARCH.md in order and record the numbers in the SUMMARY: `pnpm build:chat-context:check` (must exit 0 — this is the exact CI drift gate), `pnpm sync:check` and `pnpm sync:experience:check` (must exit 0 — proves the projects and experience pipelines were not disturbed), `pnpm exec astro check` (expect 0 errors / 0 warnings / 0 hints), `pnpm build` (populates `dist/client/`, which the render gates parse), then `pnpm test` (full suite).

    If any dist-parsing render test fails, confirm `pnpm build` ran first — `tests/build/about-education-render.test.ts` and `tests/build/home-teaser-render.test.ts` self-skip or fail on a missing `dist/`, they do not indicate a code regression.

    Expect `tests/build/home-teaser-render.test.ts` to stay green untouched: its line 106 asserts the Holloway teaser eyebrow date range and its line 136 asserts the Virginia Tech `alumniOf` JSON-LD, both of which this change deliberately preserves (D-01).

    Do NOT run or repair `scripts/verify-phase24-invariants.mjs` or `scripts/verify-phase25-invariants.mjs`. They hash chat-source files this task legitimately edits, are completed-phase artifacts, and are not wired into `pnpm test` or CI. Reporting drift there is the expected outcome, not a defect to revert.

    Record one follow-up in the SUMMARY under a clearly-labelled manual heading: `public/jack-cutrara-resume.pdf` is a compressed binary that almost certainly still carries the old graduation date and the Virginia Tech line. It is out of code scope. Jack must re-export the PDF himself and drop it in place; the asset size gate accepts 10KB-1MB. Do not attempt to edit the PDF.
  </action>
  <verify>
    <automated>pnpm build:chat-context:check &amp;&amp; pnpm sync:check &amp;&amp; pnpm sync:experience:check &amp;&amp; pnpm exec astro check &amp;&amp; pnpm build &amp;&amp; grep -cF 'Western Governors University · Expected September 2026' dist/client/about/index.html | grep -qv '^0$' &amp;&amp; ! grep -F 'Transferred from' dist/client/about/index.html &amp;&amp; grep -cF 'finishing my B.S. in Computer Science' dist/client/index.html | grep -qv '^0$' &amp;&amp; grep -cF 'Virginia Tech' dist/client/index.html | grep -qv '^0$' &amp;&amp; pnpm test</automated>
    <human-check>Load `/` and `/about` against `pnpm preview` (or `astro dev`). Confirm: (a) the bio paragraph reads "I’m finishing my B.S. in Computer Science" on both pages with a curly apostrophe; (b) the `/about` Education block reads `Western Governors University · Expected September 2026` with the credential line directly under it and no leftover gap where the transfer line used to sit; (c) no transfer sub-line is visible. Optionally ask the chat widget "when does Jack graduate?" and confirm a third-person, in-progress answer. Approve or report the visual delta.</human-check>
  </verify>
  <done>Drift check, both sync checks, and astro check all exit clean; `pnpm build` succeeds and the built `/about` HTML shows the expected-September line with no transfer sub-line while the homepage still carries the Virginia Tech `alumniOf` JSON-LD; `pnpm test` is green with no new failures; the resume-PDF follow-up is recorded in the SUMMARY; human visual sign-off approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → generated chat corpus | `scripts/build-chat-context.mjs` is the only sanctioned writer of `portfolio-context.json`; it enforces the voice split and the source allow-list |
| chat corpus → Claude Haiku prompt | corpus content is injected into the model context and surfaced to anonymous public visitors |
| repo source → public static site | rendered copy and JSON-LD are published claims about Jack's credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o01-01 | Tampering | `src/data/portfolio-context.json` | medium | mitigate | Never hand-edit the generated corpus; regenerate via `pnpm build:chat-context` in Tasks 1 and 2 and gate on `pnpm build:chat-context:check` exit 0 in Task 3. Hand-editing bypasses the first-person leak guard and the source allow-list, and fails the CI drift gate. |
| T-o01-02 | Tampering | `src/content/experience/holloway.mdx`, corpus experience block | high | mitigate | The literal graduation date collides with the Holloway contract start date. Scope every edit to `src/data/education.ts`; Task 1 asserts the Holloway range is still present in both the MDX and the regenerated corpus, and Task 3 runs `pnpm sync:experience:check`. |
| T-o01-03 | Repudiation | published credential claims (`/about` render + `hasCredential` JSON-LD) | high | mitigate | The current site asserts a completed degree that is not complete — a verifiable false claim to recruiters. Single-source the fix in `education.ts` and `about.ts`, and lock it with the unit gate plus the dist render gate. |
| T-o01-04 | Information Disclosure | `about-chat.ts`, `portfolio-context.static.json` | medium | mitigate | First-person copy leaking into chat sources breaks CHAT-06 and hard-exits the build with code 2. Task 2 keeps each file in its own voice; the build script's `checkFirstPersonLeaks` walk is the enforcing gate and runs on every regeneration. |
| T-o01-05 | Tampering | dependency tree | low | accept | Zero package-manager installs in this task — no `package.json` or lockfile changes, so no supply-chain surface is introduced. The QA-02 dependency-lock posture from v1.4 holds unchanged. |
</threat_model>

<verification>
Gate order matters — `pnpm build` must precede `pnpm test` or the two dist-parsing render suites cannot assert anything:

1. `pnpm build:chat-context:check` → exit 0 (mirrors `.github/workflows/sync-check.yml:48`)
2. `pnpm sync:check` → exit 0
3. `pnpm sync:experience:check` → exit 0
4. `pnpm exec astro check` → 0 errors / 0 warnings / 0 hints
5. `pnpm build` → exit 0, `dist/client/` populated
6. `pnpm test` → green, no new failures against the v1.4 close baseline of 692 pass / 2 skip

Preserved-by-design assertions that must still pass (do not "fix" them): `tests/build/home-teaser-render.test.ts:106` (Holloway eyebrow date range) and `:136` (Virginia Tech `alumniOf` JSON-LD); `tests/content/education-module.test.ts:28` and `:56-82` (`transferredFrom` + alumniOf/hasCredential relationships); `tests/build/parse-education.test.ts:44-49, 66-67`; `tests/build/chat-context-integrity.test.ts:143-146`.

The corpus `est_tokens` value will shift. Nothing asserts a fixed token count, so this is expected.
</verification>

<success_criteria>
- The homepage and `/about` bio both state the degree is in progress, sourced from one `ABOUT_P1` edit, with U+2019 apostrophes preserved.
- `/about` education block renders `Western Governors University · Expected September 2026` and no transfer sub-line; `.education-transfer` and its CSS rule no longer exist in `about.astro`.
- `EDUCATION.transferredFrom`, `alumniOfSchema`, and the corpus `education.transferredFrom` field are untouched and still functioning (D-01).
- `src/data/portfolio-context.json` was regenerated, never hand-edited, and `pnpm build:chat-context:check` exits 0.
- Holloway's contract dates are unchanged in `holloway.mdx` and the corpus.
- 5 test assertions updated; `pnpm test` green; `pnpm exec astro check` 0/0/0.
- Resume PDF re-export recorded as a manual follow-up for Jack, not attempted in code.
</success_criteria>

<source_audit>
| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Homepage bio: completed-degree wording → in-progress | Task 2 (`about.ts` SSoT, renders on `/` via `index.astro:140`) |
| GOAL | About education block: expected September 2026 | Task 1 (`education.ts` `date`, renders via `about.astro:23`) |
| GOAL | About education block: remove the Virginia Tech transfer line | Task 2 (delete element + CSS rule) |
| GOAL | Update chatbot knowledge if it references the old date/transfer info | Tasks 1 + 2 (`about-chat.ts`, `static.json`, corpus regeneration); `system-prompt.ts` verified to contain zero education references, so no edit exists to make |
| REQ | QUICK-260827-o01 | Tasks 1-3 |
| RESEARCH | Change Set 1 (`about.ts` first person, U+2019) | Task 2 |
| RESEARCH | Change Set 2 (`about-chat.ts` third person) | Task 2 |
| RESEARCH | Change Set 3 (`portfolio-context.static.json`) | Task 2 |
| RESEARCH | Change Set 4 (`education.ts` date, comment, dateISO) | Task 1 |
| RESEARCH | Change Set 5 (`about.astro` line + CSS rule) | Task 2 |
| RESEARCH | Change Set 6 (mandatory corpus regeneration) | Tasks 1 + 2, gated in Task 3 |
| RESEARCH | 5 test-assertion updates | Task 1 (education-module 27, 35), Task 2 (about-education-render 54, 55, 73) |
| RESEARCH | Files that must NOT change (Holloway MDX/corpus, 2 Holloway test assertions, `system-prompt.ts`) | Task 1 positive-assertion gate + Task 3 sync checks |
| RESEARCH | Open Question 2 — resume PDF re-export | Task 3, recorded as an explicit manual follow-up (out of code scope) |
| CONTEXT | D-01: remove the rendered `/about` transfer line only; keep `transferredFrom` in `education.ts` for `alumniOf` JSON-LD and chat truthfulness | Task 2 action + Task 1 preservation assertion + `must_haves.truths` |

No unplanned items. Research Open Question 1 is resolved by D-01; Open Question 2 is scoped out as a manual asset task and explicitly recorded rather than silently dropped.
</source_audit>

<output>
Create `.planning/quick/260827-o01-fix-outdated-education-status-homepage-b/260827-o01-SUMMARY.md` when done, including the recorded gate numbers and the resume-PDF manual follow-up.
</output>
